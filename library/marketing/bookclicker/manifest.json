{
  "manifest_version": "0.3",
  "name": "bookclicker-pp-mcp",
  "display_name": "Bookclicker",
  "version": "0.1.0",
  "description": "Every Bookclicker workflow, plus a local mirror of the newsletter marketplace that makes launch planning a single query.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/bookclicker-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/bookclicker-pp-mcp",
      "args": [],
      "env": {
        "BOOKCLICKER_SESSION": "${user_config.bookclicker_session}"
      }
    }
  },
  "user_config": {
    "bookclicker_session": {
      "type": "string",
      "title": "BOOKCLICKER_SESSION",
      "description": "Sets BOOKCLICKER_SESSION for the Bookclicker MCP server. This is the _bookclicker_session Rails cookie from a logged-in browser session.",
      "sensitive": true,
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
