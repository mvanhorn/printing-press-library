// Copyright 2026 syndicate-links. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAffiliateRecordClickCmd(flags *rootFlags) *cobra.Command {
	var bodyCountry string
	var bodyDeviceType string
	var bodyIpHash string
	var bodyReferrerContext string
	var bodyTrackingCode string
	var bodyUserAgent string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "record-click",
		Short: "Record a click",
		Example: "  syndicate-links-pp-cli affiliate record-click --tracking-code example-value",
		Annotations: map[string]string{"pp:endpoint": "affiliate.record-click", "pp:method": "POST", "pp:path": "/affiliate/events/click"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("tracking-code") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "tracking-code")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/affiliate/events/click"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCountry != "" {
					body["country"] = bodyCountry
				}
				if bodyDeviceType != "" {
					body["deviceType"] = bodyDeviceType
				}
				if bodyIpHash != "" {
					body["ipHash"] = bodyIpHash
				}
				if bodyReferrerContext != "" {
					var parsedReferrerContext any
					if err := json.Unmarshal([]byte(bodyReferrerContext), &parsedReferrerContext); err != nil {
						return fmt.Errorf("parsing --referrer-context JSON: %w", err)
					}
					body["referrerContext"] = parsedReferrerContext
				}
				if bodyTrackingCode != "" {
					body["trackingCode"] = bodyTrackingCode
				}
				if bodyUserAgent != "" {
					body["userAgent"] = bodyUserAgent
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "affiliate",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCountry, "country", "", "ISO 3166-1 alpha-2")
	cmd.Flags().StringVar(&bodyDeviceType, "device-type", "", "Device type")
	cmd.Flags().StringVar(&bodyIpHash, "ip-hash", "", "64-char SHA256 hex of the visitor IP.")
	cmd.Flags().StringVar(&bodyReferrerContext, "referrer-context", "", "AI surface metadata blob.")
	cmd.Flags().StringVar(&bodyTrackingCode, "tracking-code", "", "Tracking code")
	cmd.Flags().StringVar(&bodyUserAgent, "user-agent", "", "User agent")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
