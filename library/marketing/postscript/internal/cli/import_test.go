// Copyright 2026 Deb Mukherjee and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"strings"
	"testing"
)

func TestImportResourcePathUsesPostscriptEndpoints(t *testing.T) {
	path, err := importResourcePath("events")
	if err != nil {
		t.Fatalf("importResourcePath(events): %v", err)
	}
	if path != "/api/v2/events" {
		t.Fatalf("importResourcePath(events) = %q, want /api/v2/events", path)
	}
}

func TestImportResourcePathRejectsUnsupportedResources(t *testing.T) {
	_, err := importResourcePath("subscribers")
	if err == nil {
		t.Fatal("importResourcePath(subscribers) succeeded; want unsupported resource error")
	}
	if got, want := err.Error(), `unknown import resource "subscribers"`; !strings.Contains(got, want) {
		t.Fatalf("importResourcePath(subscribers) error = %q, want to contain %q", got, want)
	}
}

func TestImportCommandHidesFutureBatchSizeFlag(t *testing.T) {
	cmd := newImportCmd(&rootFlags{})
	flag := cmd.Flags().Lookup("batch-size")
	if flag == nil {
		t.Fatal("batch-size flag is missing")
	}
	if !flag.Hidden {
		t.Fatal("batch-size flag is visible even though batch import is not implemented")
	}
}
