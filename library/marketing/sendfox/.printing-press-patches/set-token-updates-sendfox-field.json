{
  "schema_version": 2,
  "id": "set-token-must-update-sendfox-token",
  "applied_at": "2026-07-11",
  "base_run_id": "20260609-205519",
  "base_printing_press_version": "4.3.0",
  "summary": "auth set-token must write SendfoxToken directly before calling SaveTokens so the new token takes effect; AuthHeader checks SendfoxToken before AccessToken.",
  "reason": "setup writes both SendfoxToken and AccessToken. set-token called SaveTokens which only updates AccessToken, leaving the old SendfoxToken from setup on disk. AuthHeader returns SendfoxToken first, so token rotation was silently ignored.",
  "files": ["internal/cli/auth.go"]
}
