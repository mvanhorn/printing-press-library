// Copyright 2026 pimmetjeoss. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "account get-subscription-details", Description: "Get subscription details", Group: "account"},
	{Command: "ai-search discover-brand-by-url", Description: "Discover brand by URL", Group: "ai-search"},
	{Command: "ai-search get-leaderboard", Description: "Get AI search leaderboard", Group: "ai-search"},
	{Command: "ai-search get-overview-time-series", Description: "Get AI search overview time series", Group: "ai-search"},
	{Command: "ai-search get-prompts-by-brand", Description: "Get prompts by brand", Group: "ai-search"},
	{Command: "ai-search get-prompts-by-target", Description: "Get prompts by target", Group: "ai-search"},
	{Command: "backlinks fetch-raw-batch", Description: "Fetch raw backlinks batch", Group: "backlinks"},
	{Command: "backlinks get-anchor-texts", Description: "Get backlink anchor texts", Group: "backlinks"},
	{Command: "backlinks get-count", Description: "Get backlink count", Group: "backlinks"},
	{Command: "backlinks get-cumulative-history", Description: "Get cumulative backlinks history", Group: "backlinks"},
	{Command: "backlinks get-domain-authority", Description: "Get domain authority", Group: "backlinks"},
	{Command: "backlinks get-domain-authority-distribution", Description: "Get domain authority distribution", Group: "backlinks"},
	{Command: "backlinks get-domain-authority-history", Description: "Get domain authority history", Group: "backlinks"},
	{Command: "backlinks get-history-count", Description: "Get backlink history count", Group: "backlinks"},
	{Command: "backlinks get-metrics", Description: "Get backlink metrics", Group: "backlinks"},
	{Command: "backlinks get-page-authority", Description: "Get page authority", Group: "backlinks"},
	{Command: "backlinks get-page-authority-history", Description: "Get page authority history", Group: "backlinks"},
	{Command: "backlinks get-page-or-domain-authority", Description: "Get page or domain authority", Group: "backlinks"},
	{Command: "backlinks get-referring-domain-count", Description: "Get referring domain count", Group: "backlinks"},
	{Command: "backlinks get-referring-domain-history-count", Description: "Get referring domain history count", Group: "backlinks"},
	{Command: "backlinks get-referring-ip-count", Description: "Get referring IP count", Group: "backlinks"},
	{Command: "backlinks get-referring-subnet-count", Description: "Get referring subnet count", Group: "backlinks"},
	{Command: "backlinks get-summary", Description: "Get backlink summary", Group: "backlinks"},
	{Command: "backlinks list", Description: "List backlinks", Group: "backlinks"},
	{Command: "backlinks list-new-or-lost", Description: "List new or lost backlinks", Group: "backlinks"},
	{Command: "backlinks list-new-or-lost-referring-domains", Description: "List new or lost referring domains", Group: "backlinks"},
	{Command: "backlinks list-pages-with", Description: "List pages with backlinks", Group: "backlinks"},
	{Command: "backlinks list-referring-domains", Description: "List referring domains", Group: "backlinks"},
	{Command: "backlinks list-referring-ips", Description: "List referring IPs", Group: "backlinks"},
	{Command: "domain analysis-analyze-keyword-overlap-and-gaps", Description: "Analyze keyword overlap and gaps", Group: "domain"},
	{Command: "domain analysis-get-competitors", Description: "Get domain competitors", Group: "domain"},
	{Command: "domain analysis-get-keyword-rankings", Description: "Get domain keyword rankings", Group: "domain"},
	{Command: "domain analysis-get-overview-by-region", Description: "Get domain overview by region", Group: "domain"},
	{Command: "domain analysis-get-pages", Description: "Get domain pages", Group: "domain"},
	{Command: "domain analysis-get-paid-ads-by-keyword-or", Description: "Get paid ads by keyword or domain", Group: "domain"},
	{Command: "domain analysis-get-subdomains", Description: "Get domain subdomains", Group: "domain"},
	{Command: "domain analysis-get-traffic-and-keyword-history", Description: "Get traffic and keyword history", Group: "domain"},
	{Command: "domain analysis-get-worldwide-overview", Description: "Get worldwide domain overview", Group: "domain"},
	{Command: "domain analysis-get-worldwide-url-overview", Description: "Get worldwide URL overview", Group: "domain"},
	{Command: "keywords research-export-metrics", Description: "Export keyword metrics", Group: "keywords"},
	{Command: "keywords research-get-longtail", Description: "Get longtail keywords", Group: "keywords"},
	{Command: "keywords research-get-question", Description: "Get question keywords", Group: "keywords"},
	{Command: "keywords research-get-related", Description: "Get related keywords", Group: "keywords"},
	{Command: "keywords research-get-similar", Description: "Get similar keywords", Group: "keywords"},
	{Command: "project-management ai-result-tracker-get-llm-statistics", Description: "Get LLM statistics", Group: "project-management"},
	{Command: "project-management ai-result-tracker-get-llm-status", Description: "Get LLM status", Group: "project-management"},
	{Command: "project-management ai-result-tracker-get-prompt-answer", Description: "Get prompt answer", Group: "project-management"},
	{Command: "project-management ai-result-tracker-get-prompt-rankings", Description: "Get prompt rankings", Group: "project-management"},
	{Command: "project-management ai-result-tracker-get-site-brand", Description: "Get site brand", Group: "project-management"},
	{Command: "project-management ai-result-tracker-list-or-get-llm-engines", Description: "List or get LLM engines", Group: "project-management"},
	{Command: "project-management ai-result-tracker-list-prompts", Description: "List prompts", Group: "project-management"},
	{Command: "project-management competitors-get-all-competitors-metrics", Description: "Get all competitors metrics", Group: "project-management"},
	{Command: "project-management competitors-get-competitor-keyword-positions", Description: "Get competitor keyword positions", Group: "project-management"},
	{Command: "project-management competitors-get-top-10-results", Description: "Get top 10 results", Group: "project-management"},
	{Command: "project-management competitors-get-top-100-results", Description: "Get top 100 results", Group: "project-management"},
	{Command: "project-management competitors-list-competitors", Description: "List competitors", Group: "project-management"},
	{Command: "project-management project-account-get-user-profile", Description: "Get user profile", Group: "project-management"},
	{Command: "project-management project-account-list-or-manage-sub-accounts", Description: "List or manage sub accounts", Group: "project-management"},
	{Command: "project-management project-account-list-owned-projects", Description: "List owned projects", Group: "project-management"},
	{Command: "project-management project-account-list-shared-projects", Description: "List shared projects", Group: "project-management"},
	{Command: "project-management projects-get-ads-chart-statistics", Description: "Get ads chart statistics", Group: "project-management"},
	{Command: "project-management projects-get-check-dates", Description: "Get check dates", Group: "project-management"},
	{Command: "project-management projects-get-historical-dates", Description: "Get historical dates", Group: "project-management"},
	{Command: "project-management projects-get-keyword-positions", Description: "Get keyword positions", Group: "project-management"},
	{Command: "project-management projects-get-project-position-history", Description: "Get project position history", Group: "project-management"},
	{Command: "project-management projects-get-project-summary-statistics", Description: "Get project summary statistics", Group: "project-management"},
	{Command: "project-management projects-list-or-manage-projects", Description: "List or manage projects", Group: "project-management"},
	{Command: "project-management projects-list-project-search-engines", Description: "List project search engines", Group: "project-management"},
	{Command: "project-management projects-list-website-keywords", Description: "List website keywords", Group: "project-management"},
	{Command: "project-management projects-run-position-check", Description: "Run position check", Group: "project-management"},
	{Command: "project-management reference-list-google-languages", Description: "List Google languages", Group: "project-management"},
	{Command: "project-management reference-list-google-regions", Description: "List Google regions", Group: "project-management"},
	{Command: "project-management reference-list-search-engines", Description: "List search engines", Group: "project-management"},
	{Command: "site-audit create-advanced-audit", Description: "Create advanced audit", Group: "site-audit"},
	{Command: "site-audit create-standard-audit", Description: "Create standard audit", Group: "site-audit"},
	{Command: "site-audit get-audit-history", Description: "Get audit history", Group: "site-audit"},
	{Command: "site-audit get-audit-links", Description: "Get audit links", Group: "site-audit"},
	{Command: "site-audit get-audit-pages-by-issue", Description: "Get audit pages by issue", Group: "site-audit"},
	{Command: "site-audit get-audit-report", Description: "Get audit report", Group: "site-audit"},
	{Command: "site-audit get-audit-status", Description: "Get audit status", Group: "site-audit"},
	{Command: "site-audit get-crawled-pages", Description: "Get crawled pages", Group: "site-audit"},
	{Command: "site-audit get-issues-for-url", Description: "Get issues for URL", Group: "site-audit"},
	{Command: "site-audit list-audits", Description: "List audits", Group: "site-audit"},
	{Command: "site-audit recheck-advanced-audit", Description: "Recheck advanced audit", Group: "site-audit"},
	{Command: "site-audit recheck-standard-audit", Description: "Recheck standard audit", Group: "site-audit"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  seranking-pp-cli which "stale tickets"
  seranking-pp-cli which "bottleneck"
  seranking-pp-cli which --limit 1 "send message"
  seranking-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
