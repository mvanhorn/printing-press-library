// Copyright 2026 Kent Martin and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newContactsSubscribersCmd(flags *rootFlags) *cobra.Command {
	var flagResfmt string
	var flagListkey string
	var flagSort string
	var flagFromindex int
	var flagRange int
	var flagStatus string

	cmd := &cobra.Command{
		Use:         "subscribers",
		Short:       "Subscribers of a list, filtered by status, paginated",
		Example:     "  zoho-campaigns-pp-cli contacts subscribers --listkey your-token-here",
		Annotations: map[string]string{"pp:endpoint": "contacts.subscribers", "pp:method": "GET", "pp:path": "/getlistsubscribers?resfmt=JSON", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !cmd.Flags().Changed("listkey") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "listkey")
			}
			if cmd.Flags().Changed("sort") {
				allowedSort := []string{"asc", "desc"}
				validSort := false
				for _, v := range allowedSort {
					if flagSort == v {
						validSort = true
						break
					}
				}
				if !validSort {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSort, "sort", allowedSort)
				}
			}
			if cmd.Flags().Changed("status") {
				allowedStatus := []string{"active", "recent", "mostrecent", "unsub", "bounce"}
				validStatus := false
				for _, v := range allowedStatus {
					if flagStatus == v {
						validStatus = true
						break
					}
				}
				if !validStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagStatus, "status", allowedStatus)
				}
			}
			path := "/getlistsubscribers?resfmt=JSON"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagResfmt != "" {
				params["resfmt"] = formatCLIParamValue(flagResfmt)
			}
			if flagListkey != "" {
				params["listkey"] = formatCLIParamValue(flagListkey)
			}
			if flagSort != "" {
				params["sort"] = formatCLIParamValue(flagSort)
			}
			if flagFromindex != 0 {
				params["fromindex"] = formatCLIParamValue(flagFromindex)
			}
			if flagRange != 0 {
				params["range"] = formatCLIParamValue(flagRange)
			}
			if flagStatus != "" {
				params["status"] = formatCLIParamValue(flagStatus)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "contacts", false, path, params, nil, "list_of_details", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagResfmt, "resfmt", "JSON", "Response format (JSON)")
	cmd.Flags().StringVar(&flagListkey, "listkey", "", "List key (from contacts lists)")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Sort direction (one of: asc, desc)")
	cmd.Flags().IntVar(&flagFromindex, "fromindex", 1, "Pagination start index (1-based)")
	cmd.Flags().IntVar(&flagRange, "range", 100, "Number of subscribers to return")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Subscriber status filter (one of: active, recent, mostrecent, unsub, bounce)")

	return cmd
}
