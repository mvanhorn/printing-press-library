version: 2
project_name: judgeme-pp-cli
changelog:
  disable: true
builds:
  - id: judgeme-pp-cli
    main: ./cmd/judgeme-pp-cli
    binary: judgeme-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/marketing/judgeme/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: judgeme-pp-mcp
    main: ./cmd/judgeme-pp-mcp
    binary: judgeme-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: judgeme-pp-cli
    repository:
      owner: cathrynlavery
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "A completeness-verified Judge.me review corpus with explicit populations and syndication-aware exports."
    install: |
      bin.install "judgeme-pp-cli"
      bin.install "judgeme-pp-mcp"
