// Copyright 2026 ubuntu and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/cliutil"
	"github.com/spf13/cobra"
)

func newAdInsightGetHistoricalKeywordPerformanceCmd(flags *rootFlags) *cobra.Command {
	var bodyDevices string
	var bodyKeywords string
	var bodyLanguage string
	var bodyMatchTypes string
	var bodyPublisherCountries string
	var bodyTargetAdPosition string
	var bodyTimeInterval string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "get-historical-keyword-performance",
		Short:       "get_historical_keyword_performance",
		Example:     "  bing-ads-pp-cli ad-insight get-historical-keyword-performance",
		Annotations: map[string]string{"pp:endpoint": "ad-insight.get-historical-keyword-performance", "pp:method": "POST", "pp:path": "/AdInsight/v13/HistoricalKeywordPerformance/query", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/AdInsight/v13/HistoricalKeywordPerformance/query"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("devices") {
					parsedDevices, parseErr := cliutil.ParseStringList(bodyDevices)
					if parseErr != nil {
						return fmt.Errorf("parsing --devices list: %w", parseErr)
					}
					bodyMap["Devices"] = parsedDevices
				}
				if cmd.Flags().Changed("keywords") {
					parsedKeywords, parseErr := cliutil.ParseStringList(bodyKeywords)
					if parseErr != nil {
						return fmt.Errorf("parsing --keywords list: %w", parseErr)
					}
					bodyMap["Keywords"] = parsedKeywords
				}
				if cmd.Flags().Changed("language") || bodyLanguage != "" {
					bodyMap["Language"] = bodyLanguage
				}
				if cmd.Flags().Changed("match-types") {
					parsedMatchTypes, parseErr := cliutil.ParseStringList(bodyMatchTypes)
					if parseErr != nil {
						return fmt.Errorf("parsing --match-types list: %w", parseErr)
					}
					bodyMap["MatchTypes"] = parsedMatchTypes
				}
				if cmd.Flags().Changed("publisher-countries") {
					parsedPublisherCountries, parseErr := cliutil.ParseStringList(bodyPublisherCountries)
					if parseErr != nil {
						return fmt.Errorf("parsing --publisher-countries list: %w", parseErr)
					}
					bodyMap["PublisherCountries"] = parsedPublisherCountries
				}
				if cmd.Flags().Changed("target-ad-position") || bodyTargetAdPosition != "" {
					bodyMap["TargetAdPosition"] = bodyTargetAdPosition
				}
				if cmd.Flags().Changed("time-interval") || bodyTimeInterval != "" {
					bodyMap["TimeInterval"] = bodyTimeInterval
				}
			}
			data, statusCode, err := c.PostQueryWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			if isDryRunResponse(c.IsDryRun(), data) {
				if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
					return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "dry-run"}, map[string]bool{"Keyword": true, "KeywordKPIs": true})
				}
				return nil
			}
			_ = statusCode
			if !flags.dryRun {
				data = applyResponsePath(data, "KeywordHistoricalPerformances")
			}
			outputData := data
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"Keyword": true, "KeywordKPIs": true})
		},
	}
	cmd.Flags().StringVar(&bodyDevices, "devices", "", "Devices")
	cmd.Flags().StringVar(&bodyKeywords, "keywords", "", "Keywords")
	cmd.Flags().StringVar(&bodyLanguage, "language", "", "Language")
	cmd.Flags().StringVar(&bodyMatchTypes, "match-types", "", "Match types")
	cmd.Flags().StringVar(&bodyPublisherCountries, "publisher-countries", "", "Publisher countries")
	cmd.Flags().StringVar(&bodyTargetAdPosition, "target-ad-position", "", "Target ad position")
	cmd.Flags().StringVar(&bodyTimeInterval, "time-interval", "", "Time interval")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
