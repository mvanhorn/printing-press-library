// Copyright 2026 Anatoliy Zbirovskiy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAdInsightGetBidLandscapeByAdGroupIdsCmd(flags *rootFlags) *cobra.Command {
	var bodyAdGroupBidLandscapeInputs string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "get-bid-landscape-by-ad-group-ids",
		Short:       "get_bid_landscape_by_ad_group_ids",
		Example:     "  bing-ads-pp-cli ad-insight get-bid-landscape-by-ad-group-ids",
		Annotations: map[string]string{"pp:endpoint": "ad-insight.get-bid-landscape-by-ad-group-ids", "pp:method": "POST", "pp:path": "/AdInsight/v13/BidLandscape/queryByAdGroupIds", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/AdInsight/v13/BidLandscape/queryByAdGroupIds"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("ad-group-bid-landscape-inputs") || bodyAdGroupBidLandscapeInputs != "" {
					var parsedAdGroupBidLandscapeInputs any
					if err := json.Unmarshal([]byte(bodyAdGroupBidLandscapeInputs), &parsedAdGroupBidLandscapeInputs); err != nil {
						return fmt.Errorf("parsing --ad-group-bid-landscape-inputs JSON: %w", err)
					}
					asArray, ok := parsedAdGroupBidLandscapeInputs.([]any)
					if !ok {
						return fmt.Errorf("--ad-group-bid-landscape-inputs must be a JSON array, got JSON %T", parsedAdGroupBidLandscapeInputs)
					}
					bodyMap["AdGroupBidLandscapeInputs"] = asArray
				}
			}
			data, statusCode, err := c.PostQueryWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			if isDryRunResponse(c.IsDryRun(), data) {
				if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
					return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "dry-run"}, map[string]bool{"AdGroupBidLandscapeType": true, "AdGroupId": true, "BidLandscapePoints": true, "EndDate": true, "StartDate": true})
				}
				return nil
			}
			_ = statusCode
			if !flags.dryRun {
				data = applyResponsePath(data, "BidLandscape")
			}
			outputData := data
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"AdGroupBidLandscapeType": true, "AdGroupId": true, "BidLandscapePoints": true, "EndDate": true, "StartDate": true})
		},
	}
	cmd.Flags().StringVar(&bodyAdGroupBidLandscapeInputs, "ad-group-bid-landscape-inputs", "", "Ad group bid landscape inputs")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
