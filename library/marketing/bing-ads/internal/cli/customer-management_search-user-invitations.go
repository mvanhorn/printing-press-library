// Copyright 2026 ubuntu and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCustomerManagementSearchUserInvitationsCmd(flags *rootFlags) *cobra.Command {
	var bodyPredicates string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "search-user-invitations",
		Short:       "search_user_invitations",
		Example:     "  bing-ads-pp-cli customer-management search-user-invitations",
		Annotations: map[string]string{"pp:endpoint": "customer-management.search-user-invitations", "pp:method": "POST", "pp:path": "/CustomerManagement/v13/UserInvitations/Search", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/CustomerManagement/v13/UserInvitations/Search"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("predicates") || bodyPredicates != "" {
					var parsedPredicates any
					if err := json.Unmarshal([]byte(bodyPredicates), &parsedPredicates); err != nil {
						return fmt.Errorf("parsing --predicates JSON: %w", err)
					}
					asArray, ok := parsedPredicates.([]any)
					if !ok {
						return fmt.Errorf("--predicates must be a JSON array, got JSON %T", parsedPredicates)
					}
					bodyMap["Predicates"] = asArray
				}
			}
			data, statusCode, err := c.PostQueryWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			if isDryRunResponse(c.IsDryRun(), data) {
				if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
					return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "dry-run"}, map[string]bool{"AccountIds": true, "CustomerId": true, "Email": true, "ExpirationDate": true, "FirstName": true, "Id": true, "LastName": true, "Lcid": true, "RoleId": true})
				}
				return nil
			}
			_ = statusCode
			if !flags.dryRun {
				data = applyResponsePath(data, "UserInvitations")
			}
			outputData := data
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"AccountIds": true, "CustomerId": true, "Email": true, "ExpirationDate": true, "FirstName": true, "Id": true, "LastName": true, "Lcid": true, "RoleId": true})
		},
	}
	cmd.Flags().StringVar(&bodyPredicates, "predicates", "", "Predicates")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
