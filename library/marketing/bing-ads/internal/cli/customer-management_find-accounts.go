// Copyright 2026 Anatoliy Zbirovskiy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCustomerManagementFindAccountsCmd(flags *rootFlags) *cobra.Command {
	var bodyAccountFilter string
	var bodyCustomerId string
	var bodyTopN int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "find-accounts",
		Short:       "find_accounts",
		Example:     "  bing-ads-pp-cli customer-management find-accounts",
		Annotations: map[string]string{"pp:endpoint": "customer-management.find-accounts", "pp:method": "POST", "pp:path": "/CustomerManagement/v13/Accounts/Find", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/CustomerManagement/v13/Accounts/Find"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("account-filter") || bodyAccountFilter != "" {
					bodyMap["AccountFilter"] = bodyAccountFilter
				}
				if cmd.Flags().Changed("customer-id") || bodyCustomerId != "" {
					bodyMap["CustomerId"] = bodyCustomerId
				}
				if cmd.Flags().Changed("top-n") || bodyTopN != 0 {
					bodyMap["TopN"] = bodyTopN
				}
			}
			data, statusCode, err := c.PostQueryWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			if isDryRunResponse(c.IsDryRun(), data) {
				if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
					return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "dry-run"}, map[string]bool{"AccountLifeCycleStatus": true, "Id": true, "Name": true, "Number": true, "PauseReason": true})
				}
				return nil
			}
			_ = statusCode
			if !flags.dryRun {
				data = applyResponsePath(data, "AccountsInfo")
			}
			outputData := data
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"AccountLifeCycleStatus": true, "Id": true, "Name": true, "Number": true, "PauseReason": true})
		},
	}
	cmd.Flags().StringVar(&bodyAccountFilter, "account-filter", "", "Account filter")
	cmd.Flags().StringVar(&bodyCustomerId, "customer-id", "", "Customer id")
	cmd.Flags().IntVar(&bodyTopN, "top-n", 0, "Top n")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
