// Copyright 2026 ubuntu and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCustomerManagementGetNotificationsCmd(flags *rootFlags) *cobra.Command {
	var bodyLocale string
	var bodyTypeIds string
	var bodyUserId int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "get-notifications",
		Short:       "get_notifications",
		Example:     "  bing-ads-pp-cli customer-management get-notifications",
		Annotations: map[string]string{"pp:endpoint": "customer-management.get-notifications", "pp:method": "POST", "pp:path": "/CustomerManagement/v13/Notifications/Query", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/CustomerManagement/v13/Notifications/Query"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("locale") || bodyLocale != "" {
					bodyMap["Locale"] = bodyLocale
				}
				if cmd.Flags().Changed("type-ids") || bodyTypeIds != "" {
					var parsedTypeIds any
					if err := json.Unmarshal([]byte(bodyTypeIds), &parsedTypeIds); err != nil {
						return fmt.Errorf("parsing --type-ids JSON: %w", err)
					}
					asArray, ok := parsedTypeIds.([]any)
					if !ok {
						return fmt.Errorf("--type-ids must be a JSON array, got JSON %T", parsedTypeIds)
					}
					bodyMap["TypeIds"] = asArray
				}
				if cmd.Flags().Changed("user-id") || bodyUserId != 0 {
					bodyMap["UserId"] = bodyUserId
				}
			}
			data, statusCode, err := c.PostQueryWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			if isDryRunResponse(c.IsDryRun(), data) {
				if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
					return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "dry-run"}, map[string]bool{"AdditionalInfo": true, "Id": true, "Message": true, "Severity": true, "Title": true, "TypeId": true})
				}
				return nil
			}
			_ = statusCode
			if !flags.dryRun {
				data = applyResponsePath(data, "Notifications")
			}
			outputData := data
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"AdditionalInfo": true, "Id": true, "Message": true, "Severity": true, "Title": true, "TypeId": true})
		},
	}
	cmd.Flags().StringVar(&bodyLocale, "locale", "", "Locale")
	cmd.Flags().StringVar(&bodyTypeIds, "type-ids", "", "Type ids")
	cmd.Flags().IntVar(&bodyUserId, "user-id", 0, "User id")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
