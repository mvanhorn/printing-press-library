// Copyright 2026 ubuntu and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/cliutil"
	"github.com/spf13/cobra"
)

func newCustomerBillingGetBillingDocumentsInfoCmd(flags *rootFlags) *cobra.Command {
	var bodyAccountIds string
	var bodyEndDate string
	var bodyReturnInvoiceNumber bool
	var bodyStartDate string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "get-billing-documents-info",
		Short:       "get_billing_documents_info",
		Example:     "  bing-ads-pp-cli customer-billing get-billing-documents-info",
		Annotations: map[string]string{"pp:endpoint": "customer-billing.get-billing-documents-info", "pp:method": "POST", "pp:path": "/CustomerBilling/v13/BillingDocumentsInfo/Query", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/CustomerBilling/v13/BillingDocumentsInfo/Query"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("account-ids") {
					parsedAccountIds, parseErr := cliutil.ParseStringList(bodyAccountIds)
					if parseErr != nil {
						return fmt.Errorf("parsing --account-ids list: %w", parseErr)
					}
					bodyMap["AccountIds"] = parsedAccountIds
				}
				if cmd.Flags().Changed("end-date") || bodyEndDate != "" {
					bodyMap["EndDate"] = bodyEndDate
				}
				if cmd.Flags().Changed("return-invoice-number") {
					bodyMap["ReturnInvoiceNumber"] = bodyReturnInvoiceNumber
				}
				if cmd.Flags().Changed("start-date") || bodyStartDate != "" {
					bodyMap["StartDate"] = bodyStartDate
				}
			}
			data, statusCode, err := c.PostQueryWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			if isDryRunResponse(c.IsDryRun(), data) {
				if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
					return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "dry-run"}, map[string]bool{"AccountId": true, "AccountName": true, "AccountNumber": true, "Amount": true, "CampaignId": true, "CurrencyCode": true, "CustomerId": true, "DocumentDate": true, "DocumentId": true, "DocumentNumber": true})
				}
				return nil
			}
			_ = statusCode
			if !flags.dryRun {
				data = applyResponsePath(data, "BillingDocumentsInfo")
			}
			outputData := data
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"AccountId": true, "AccountName": true, "AccountNumber": true, "Amount": true, "CampaignId": true, "CurrencyCode": true, "CustomerId": true, "DocumentDate": true, "DocumentId": true, "DocumentNumber": true})
		},
	}
	cmd.Flags().StringVar(&bodyAccountIds, "account-ids", "", "Account ids")
	cmd.Flags().StringVar(&bodyEndDate, "end-date", "", "End date")
	cmd.Flags().BoolVar(&bodyReturnInvoiceNumber, "return-invoice-number", false, "Return invoice number")
	cmd.Flags().StringVar(&bodyStartDate, "start-date", "", "Start date")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
