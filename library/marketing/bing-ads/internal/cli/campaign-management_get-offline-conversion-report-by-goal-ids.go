// Copyright 2026 ubuntu and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/cliutil"
	"github.com/spf13/cobra"
)

func newCampaignManagementGetOfflineConversionReportByGoalIdsCmd(flags *rootFlags) *cobra.Command {
	var bodyEndDateUtc string
	var bodyGoalIds string
	var bodyStartDateUtc string
	var bodyUploadChannelFilter string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "get-offline-conversion-report-by-goal-ids",
		Short:       "get_offline_conversion_report_by_goal_ids",
		Example:     "  bing-ads-pp-cli campaign-management get-offline-conversion-report-by-goal-ids",
		Annotations: map[string]string{"pp:endpoint": "campaign-management.get-offline-conversion-report-by-goal-ids", "pp:method": "POST", "pp:path": "/CampaignManagement/v13/OfflineConversionReport/QueryByGoalIds", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/CampaignManagement/v13/OfflineConversionReport/QueryByGoalIds"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("end-date-utc") || bodyEndDateUtc != "" {
					bodyMap["EndDateUtc"] = bodyEndDateUtc
				}
				if cmd.Flags().Changed("goal-ids") {
					parsedGoalIds, parseErr := cliutil.ParseStringList(bodyGoalIds)
					if parseErr != nil {
						return fmt.Errorf("parsing --goal-ids list: %w", parseErr)
					}
					bodyMap["GoalIds"] = parsedGoalIds
				}
				if cmd.Flags().Changed("start-date-utc") || bodyStartDateUtc != "" {
					bodyMap["StartDateUtc"] = bodyStartDateUtc
				}
				if cmd.Flags().Changed("upload-channel-filter") || bodyUploadChannelFilter != "" {
					var parsedUploadChannelFilter any
					if err := json.Unmarshal([]byte(bodyUploadChannelFilter), &parsedUploadChannelFilter); err != nil {
						return fmt.Errorf("parsing --upload-channel-filter JSON: %w", err)
					}
					asMap, ok := parsedUploadChannelFilter.(map[string]any)
					if !ok {
						return fmt.Errorf("--upload-channel-filter must be a JSON object, got JSON %T", parsedUploadChannelFilter)
					}
					bodyMap["UploadChannelFilter"] = asMap
				}
			}
			data, statusCode, err := c.PostQueryWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			if isDryRunResponse(c.IsDryRun(), data) {
				if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
					return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "dry-run"}, map[string]bool{"OfflineGoalConversionSummaries": true, "PartialErrors": true})
				}
				return nil
			}
			_ = statusCode
			outputData := data
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"OfflineGoalConversionSummaries": true, "PartialErrors": true})
		},
	}
	cmd.Flags().StringVar(&bodyEndDateUtc, "end-date-utc", "", "End date utc")
	cmd.Flags().StringVar(&bodyGoalIds, "goal-ids", "", "Goal ids")
	cmd.Flags().StringVar(&bodyStartDateUtc, "start-date-utc", "", "Start date utc")
	cmd.Flags().StringVar(&bodyUploadChannelFilter, "upload-channel-filter", "", "Upload channel filter")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
