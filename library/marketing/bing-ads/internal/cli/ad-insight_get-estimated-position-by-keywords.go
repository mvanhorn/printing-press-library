// Copyright 2026 ubuntu and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/cliutil"
	"github.com/spf13/cobra"
)

func newAdInsightGetEstimatedPositionByKeywordsCmd(flags *rootFlags) *cobra.Command {
	var bodyAdGroupId string
	var bodyCampaignId string
	var bodyCurrencyCode string
	var bodyKeywords string
	var bodyLanguage string
	var bodyLocationIds string
	var bodyMatchTypes string
	var bodyMaxBid float64
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "get-estimated-position-by-keywords",
		Short:       "get_estimated_position_by_keywords",
		Example:     "  bing-ads-pp-cli ad-insight get-estimated-position-by-keywords",
		Annotations: map[string]string{"pp:endpoint": "ad-insight.get-estimated-position-by-keywords", "pp:method": "POST", "pp:path": "/AdInsight/v13/EstimatedPosition/queryByKeywords", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/AdInsight/v13/EstimatedPosition/queryByKeywords"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("ad-group-id") || bodyAdGroupId != "" {
					bodyMap["AdGroupId"] = bodyAdGroupId
				}
				if cmd.Flags().Changed("campaign-id") || bodyCampaignId != "" {
					bodyMap["CampaignId"] = bodyCampaignId
				}
				if cmd.Flags().Changed("currency-code") || bodyCurrencyCode != "" {
					bodyMap["CurrencyCode"] = bodyCurrencyCode
				}
				if cmd.Flags().Changed("keywords") {
					parsedKeywords, parseErr := cliutil.ParseStringList(bodyKeywords)
					if parseErr != nil {
						return fmt.Errorf("parsing --keywords list: %w", parseErr)
					}
					bodyMap["Keywords"] = parsedKeywords
				}
				if cmd.Flags().Changed("language") || bodyLanguage != "" {
					bodyMap["Language"] = bodyLanguage
				}
				if cmd.Flags().Changed("location-ids") {
					parsedLocationIds, parseErr := cliutil.ParseStringList(bodyLocationIds)
					if parseErr != nil {
						return fmt.Errorf("parsing --location-ids list: %w", parseErr)
					}
					bodyMap["LocationIds"] = parsedLocationIds
				}
				if cmd.Flags().Changed("match-types") {
					parsedMatchTypes, parseErr := cliutil.ParseStringList(bodyMatchTypes)
					if parseErr != nil {
						return fmt.Errorf("parsing --match-types list: %w", parseErr)
					}
					bodyMap["MatchTypes"] = parsedMatchTypes
				}
				if cmd.Flags().Changed("max-bid") || bodyMaxBid != 0.0 {
					bodyMap["MaxBid"] = bodyMaxBid
				}
			}
			data, statusCode, err := c.PostQueryWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			if isDryRunResponse(c.IsDryRun(), data) {
				if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
					return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "dry-run"}, map[string]bool{"EstimatedPositions": true, "Keyword": true})
				}
				return nil
			}
			_ = statusCode
			if !flags.dryRun {
				data = applyResponsePath(data, "KeywordEstimatedPositions")
			}
			outputData := data
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"EstimatedPositions": true, "Keyword": true})
		},
	}
	cmd.Flags().StringVar(&bodyAdGroupId, "ad-group-id", "", "Ad group id")
	cmd.Flags().StringVar(&bodyCampaignId, "campaign-id", "", "Campaign id")
	cmd.Flags().StringVar(&bodyCurrencyCode, "currency-code", "", "Currency code")
	cmd.Flags().StringVar(&bodyKeywords, "keywords", "", "Keywords")
	cmd.Flags().StringVar(&bodyLanguage, "language", "", "Language")
	cmd.Flags().StringVar(&bodyLocationIds, "location-ids", "", "Location ids")
	cmd.Flags().StringVar(&bodyMatchTypes, "match-types", "", "Match types")
	cmd.Flags().Float64Var(&bodyMaxBid, "max-bid", 0.0, "Max bid")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
