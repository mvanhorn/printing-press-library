// Copyright 2026 ubuntu and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCampaignManagementGetSupportedFontsCmd(flags *rootFlags) *cobra.Command {
	var bodyAdSubType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "get-supported-fonts",
		Short:       "get_supported_fonts",
		Example:     "  bing-ads-pp-cli campaign-management get-supported-fonts",
		Annotations: map[string]string{"pp:endpoint": "campaign-management.get-supported-fonts", "pp:method": "POST", "pp:path": "/CampaignManagement/v13/SupportedFonts/Query", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/CampaignManagement/v13/SupportedFonts/Query"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("ad-sub-type") || bodyAdSubType != "" {
					var parsedAdSubType any
					if err := json.Unmarshal([]byte(bodyAdSubType), &parsedAdSubType); err != nil {
						return fmt.Errorf("parsing --ad-sub-type JSON: %w", err)
					}
					asMap, ok := parsedAdSubType.(map[string]any)
					if !ok {
						return fmt.Errorf("--ad-sub-type must be a JSON object, got JSON %T", parsedAdSubType)
					}
					bodyMap["AdSubType"] = asMap
				}
			}
			data, statusCode, err := c.PostQueryWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			if isDryRunResponse(c.IsDryRun(), data) {
				if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
					return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "dry-run"}, map[string]bool{"Data": true})
				}
				return nil
			}
			_ = statusCode
			outputData := data
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"Data": true})
		},
	}
	cmd.Flags().StringVar(&bodyAdSubType, "ad-sub-type", "", "Ad sub type")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
