// Copyright 2026 Anatoliy Zbirovskiy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/cliutil"
	"github.com/spf13/cobra"
)

func newAdInsightGetHistoricalSearchCountCmd(flags *rootFlags) *cobra.Command {
	var bodyDevices string
	var bodyEndDateDay int
	var bodyEndDateMonth int
	var bodyEndDateYear int
	var bodyKeywords string
	var bodyLanguage string
	var bodyPublisherCountries string
	var bodyStartDateDay int
	var bodyStartDateMonth int
	var bodyStartDateYear int
	var bodyTimePeriodRollup string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "get-historical-search-count",
		Short:       "get_historical_search_count",
		Example:     "  bing-ads-pp-cli ad-insight get-historical-search-count",
		Annotations: map[string]string{"pp:endpoint": "ad-insight.get-historical-search-count", "pp:method": "POST", "pp:path": "/AdInsight/v13/HistoricalSearchCount/query", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/AdInsight/v13/HistoricalSearchCount/query"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("devices") {
					parsedDevices, parseErr := cliutil.ParseStringList(bodyDevices)
					if parseErr != nil {
						return fmt.Errorf("parsing --devices list: %w", parseErr)
					}
					bodyMap["Devices"] = parsedDevices
				}
				{
					nestedEndDate := map[string]any{}
					if cmd.Flags().Changed("end-date-day") || bodyEndDateDay != 0 {
						nestedEndDate["Day"] = bodyEndDateDay
					}
					if cmd.Flags().Changed("end-date-month") || bodyEndDateMonth != 0 {
						nestedEndDate["Month"] = bodyEndDateMonth
					}
					if cmd.Flags().Changed("end-date-year") || bodyEndDateYear != 0 {
						nestedEndDate["Year"] = bodyEndDateYear
					}
					if len(nestedEndDate) > 0 {
						bodyMap["EndDate"] = nestedEndDate
					}
				}
				if cmd.Flags().Changed("keywords") {
					parsedKeywords, parseErr := cliutil.ParseStringList(bodyKeywords)
					if parseErr != nil {
						return fmt.Errorf("parsing --keywords list: %w", parseErr)
					}
					bodyMap["Keywords"] = parsedKeywords
				}
				if cmd.Flags().Changed("language") || bodyLanguage != "" {
					bodyMap["Language"] = bodyLanguage
				}
				if cmd.Flags().Changed("publisher-countries") {
					parsedPublisherCountries, parseErr := cliutil.ParseStringList(bodyPublisherCountries)
					if parseErr != nil {
						return fmt.Errorf("parsing --publisher-countries list: %w", parseErr)
					}
					bodyMap["PublisherCountries"] = parsedPublisherCountries
				}
				{
					nestedStartDate := map[string]any{}
					if cmd.Flags().Changed("start-date-day") || bodyStartDateDay != 0 {
						nestedStartDate["Day"] = bodyStartDateDay
					}
					if cmd.Flags().Changed("start-date-month") || bodyStartDateMonth != 0 {
						nestedStartDate["Month"] = bodyStartDateMonth
					}
					if cmd.Flags().Changed("start-date-year") || bodyStartDateYear != 0 {
						nestedStartDate["Year"] = bodyStartDateYear
					}
					if len(nestedStartDate) > 0 {
						bodyMap["StartDate"] = nestedStartDate
					}
				}
				if cmd.Flags().Changed("time-period-rollup") || bodyTimePeriodRollup != "" {
					bodyMap["TimePeriodRollup"] = bodyTimePeriodRollup
				}
			}
			data, statusCode, err := c.PostQueryWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			if isDryRunResponse(c.IsDryRun(), data) {
				if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
					return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "dry-run"}, map[string]bool{"Keyword": true, "SearchCountsByAttributes": true})
				}
				return nil
			}
			_ = statusCode
			if !flags.dryRun {
				data = applyResponsePath(data, "KeywordSearchCounts")
			}
			outputData := data
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"Keyword": true, "SearchCountsByAttributes": true})
		},
	}
	cmd.Flags().StringVar(&bodyDevices, "devices", "", "Devices")
	cmd.Flags().IntVar(&bodyEndDateDay, "end-date-day", 0, "Day")
	cmd.Flags().IntVar(&bodyEndDateMonth, "end-date-month", 0, "Month")
	cmd.Flags().IntVar(&bodyEndDateYear, "end-date-year", 0, "Year")
	cmd.Flags().StringVar(&bodyKeywords, "keywords", "", "Keywords")
	cmd.Flags().StringVar(&bodyLanguage, "language", "", "Language")
	cmd.Flags().StringVar(&bodyPublisherCountries, "publisher-countries", "", "Publisher countries")
	cmd.Flags().IntVar(&bodyStartDateDay, "start-date-day", 0, "Day")
	cmd.Flags().IntVar(&bodyStartDateMonth, "start-date-month", 0, "Month")
	cmd.Flags().IntVar(&bodyStartDateYear, "start-date-year", 0, "Year")
	cmd.Flags().StringVar(&bodyTimePeriodRollup, "time-period-rollup", "", "Time period rollup")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
