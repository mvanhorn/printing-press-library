// Copyright 2026 Anatoliy Zbirovskiy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"math"
	"path/filepath"
	"strconv"
	"strings"
	"time"

	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/client"
	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/config"
	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/learn"
	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/mcp/bound"
	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/marketing/bing-ads/internal/platform"
	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
)

const (
	// MCP hosts can fan out tool calls faster than a human CLI session.
	// Keep them on the same polite-client limiter path instead of disabling
	// pacing with rate=0; users can still tune human CLI calls with --rate-limit.
	defaultMCPRateLimit = 2
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	installFreshTenantGate(s)
	// Code-orchestration mode — the full surface is covered by registry tools
	// (<api>_search, <api>_get, and <api>_execute). Endpoint-mirror tools are suppressed.
	RegisterCodeOrchestrationTools(s)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
	BodyPath   []string
}

type mcpPageConfig struct {
	CursorParam    string
	NextCursorPath string
}

func formatMCPParamValue(v any) string {
	switch tv := v.(type) {
	case string:
		return tv
	case bool:
		return strconv.FormatBool(tv)
	case float64:
		if math.IsNaN(tv) || math.IsInf(tv, 0) {
			return strconv.FormatFloat(tv, 'f', -1, 64)
		}
		if math.Trunc(tv) == tv && math.Abs(tv) < 1e15 {
			return strconv.FormatInt(int64(tv), 10)
		}
		return strconv.FormatFloat(tv, 'f', -1, 64)
	case float32:
		f := float64(tv)
		if math.IsNaN(f) || math.IsInf(f, 0) {
			return strconv.FormatFloat(f, 'f', -1, 32)
		}
		if math.Trunc(f) == f && math.Abs(f) < 1e15 {
			return strconv.FormatInt(int64(f), 10)
		}
		return strconv.FormatFloat(f, 'f', -1, 32)
	default:
		// Composite values (a native []any / map[string]any from an array or
		// object param) reach this path when bound to a query or path slot;
		// JSON-encode them so the wire value is valid JSON rather than Go's
		// "[a b c]" / "map[...]" rendering. Body params never come through
		// here — they are stored natively in bodyArgs and marshalled there.
		if b, err := json.Marshal(v); err == nil {
			return string(b)
		}
		return fmt.Sprintf("%v", v)
	}
}

func mcpPathValue(v any) string {
	return cliutil.EscapePathParam(formatMCPParamValue(v))
}
func setNestedBodyArg(body map[string]any, path []string, value any) {
	if len(path) == 0 {
		return
	}
	if len(path) == 1 {
		body[path[0]] = value
		return
	}
	current := body
	for _, key := range path[:len(path)-1] {
		next, ok := current[key].(map[string]any)
		if !ok {
			next = map[string]any{}
			current[key] = next
		}
		current = next
	}
	current[path[len(path)-1]] = value
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, pageConfig mcpPageConfig, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, platformSession, err := newMCPClient(ctx)
		if err != nil {
			return mcpToolError(err.Error()), nil
		}
		if platformSession != nil {
			defer platformSession.ZeroCredentials()
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()
		if err := cli.AdoptMCPOutputSemantics(platformSession, args); err != nil {
			return mcpToolError(err.Error()), nil
		}

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		mcpCursor := ""
		if pageConfig.CursorParam != "" {
			knownArgs["cursor"] = true
			if v, ok := args["cursor"]; ok {
				s, ok := v.(string)
				if !ok {
					return mcpToolError("cursor must be an opaque string returned by a previous MCP response"), nil
				}
				mcpCursor = s
				upstreamCursor, err := bound.UpstreamCursor(s)
				if err != nil {
					return mcpToolError(err.Error()), nil
				}
				if upstreamCursor != "" {
					params[pageConfig.CursorParam] = upstreamCursor
				}
			}
		}
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, mcpPathValue(v), 1)
			case "header":
				if headers == nil {
					headers = map[string]string{}
				}
				headers[binding.WireName] = formatMCPParamValue(v)
			case "body":
				if len(binding.BodyPath) > 0 {
					setNestedBodyArg(bodyArgs, binding.BodyPath, v)
				} else {
					bodyArgs[binding.WireName] = v
				}
			default:
				params[binding.WireName] = formatMCPParamValue(v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, mcpPathValue(v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = formatMCPParamValue(v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				if readOnly {
					data, err = c.GetWithHeaders(ctx, path, params, headers)
				} else {
					data, err = c.GetMutatingWithHeaders(ctx, path, params, headers)
				}
				break
			}
			if readOnly {
				data, err = c.Get(ctx, path, params)
			} else {
				data, err = c.GetMutating(ctx, path, params)
			}
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PutQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PutQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
			}
		case "PATCH":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PatchQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PatchQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
			}
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcpToolError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcpToolTextWithPlatform("already exists (no-op)", platformSession), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcpToolError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key with: export BING_ADS_CUSTOMER_ACCOUNT_ID=\"your-token-here\"" +
					"\n      Run 'bing-ads-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcpToolError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set your API key with: export BING_ADS_CUSTOMER_ACCOUNT_ID=\"your-token-here\"" +
					"\n      Run 'bing-ads-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcpToolError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource. Check that they have the required permissions and match the API's expected auth scheme." +
					"\n      Set your API key with: export BING_ADS_CUSTOMER_ACCOUNT_ID=\"your-token-here\"" +
					"\n      Run 'bing-ads-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcpToolTextWithPlatform("already deleted (no-op)", platformSession), nil
				}
				return mcpToolError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcpToolError("rate limited: " + msg), nil
			default:
				return mcpToolError(msg), nil
			}
		}

		if binaryResponse {
			encoded := base64.StdEncoding.EncodeToString(data)
			out, err := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      encoded,
				"byte_count":       len(data),
			})
			if err != nil {
				return mcpToolError(fmt.Sprintf("encoding binary result: %v", err)), nil
			}
			if len(out) > bound.MaxBytes {
				return mcpToolError(fmt.Sprintf("binary response is too large for MCP text output: %d response bytes encode to %d base64 bytes and %d MCP result bytes, exceeding the %d byte budget. Use the companion CLI command with --output <file> to save the payload locally.", len(data), len(encoded), len(out), bound.MaxBytes)), nil
			}
			result := string(out)
			if platformSession != nil {
				result = bound.WithMetadata(result, platformSession.OutputMetadata())
			}
			return mcplib.NewToolResultText(result), nil
		}
		if pageConfig.CursorParam != "" {
			return mcpToolPageResultTextWithPlatform(method, data, pageConfig, mcpCursor, platformSession), nil
		}
		return mcpToolResultTextWithPlatform(method, data, platformSession), nil
	}
}

func mcpToolResultText(method string, data json.RawMessage) *mcplib.CallToolResult {
	return mcpToolResultTextWithPlatform(method, data, nil)
}

func mcpToolTextWithPlatform(result string, platformSession *platform.Session) *mcplib.CallToolResult {
	if platformSession != nil {
		result = bound.WithMetadata(result, platformSession.OutputMetadata())
	}
	return mcplib.NewToolResultText(result)
}

func mcpToolResultTextWithPlatform(method string, data json.RawMessage, platformSession *platform.Session) *mcplib.CallToolResult {
	result := bound.EndpointResponse(method, data)
	return mcpToolTextWithPlatform(result, platformSession)
}

// mcpToolError keeps provider-controlled typed endpoint errors within the MCP
// text-result budget just like successful endpoint results.
func mcpToolError(message string) *mcplib.CallToolResult {
	return mcplib.NewToolResultError(bound.Text(message))
}

func mcpToolPageResultText(method string, data json.RawMessage, pageConfig mcpPageConfig, cursor string) *mcplib.CallToolResult {
	return mcpToolPageResultTextWithPlatform(method, data, pageConfig, cursor, nil)
}

func mcpToolPageResultTextWithPlatform(method string, data json.RawMessage, pageConfig mcpPageConfig, cursor string, platformSession *platform.Session) *mcplib.CallToolResult {
	result := bound.EndpointPageResponse(method, data, bound.PageOptions{
		Cursor:         cursor,
		CursorParam:    pageConfig.CursorParam,
		NextCursorPath: pageConfig.NextCursorPath,
	})
	if platformSession != nil {
		result = bound.WithMetadata(result, platformSession.OutputMetadata())
	}
	return mcplib.NewToolResultText(result)
}

func newMCPClient(ctx context.Context) (*client.Client, *platform.Session, error) {
	cfg, err := newMCPConfig()
	if err != nil {
		return nil, nil, err
	}
	c := newMCPClientFromConfig(cfg)
	session, err := cli.BindMCPClient(ctx, c)
	if err != nil {
		return nil, nil, err
	}
	return c, session, nil
}

func newMCPConfig() (*config.Config, error) {
	cfg, err := config.Load("")
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	return cfg, nil
}

func newMCPClientFromConfig(cfg *config.Config) *client.Client {
	c := client.New(cfg, 60*time.Second, defaultMCPRateLimit)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c
}

func mcpDBPath() (string, error) {
	dir, err := cliutil.DataDir()
	if err != nil {
		return "", err
	}
	return filepath.Join(dir, "data.db"), nil
}

// toolResultJSON renders v as the indented JSON body of an MCP text result,
// surfacing a marshal failure as a tool error instead of empty content.
func toolResultJSON(v any) (*mcplib.CallToolResult, error) {
	text, err := bound.JSON(v)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("encoding result: %v", err)), nil
	}
	return mcplib.NewToolResultText(text), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	paths := map[string]string{}
	if dir, err := cliutil.ConfigDir(); err == nil {
		paths["config_dir"] = dir
	}
	if dir, err := cliutil.DataDir(); err == nil {
		paths["data_dir"] = dir
	}
	if dir, err := cliutil.StateDir(); err == nil {
		paths["state_dir"] = dir
	}
	if dir, err := cliutil.CacheDir(); err == nil {
		paths["cache_dir"] = dir
	}
	ctx := map[string]any{
		"api":         "bing-ads",
		"description": "Reconstructed from the official BingAds-Python-SDK openapi_client (openapi-generator output)",
		"archetype":   "generic",
		"tool_count":  287,
		"paths":       paths,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion bing-ads-pp-cli binary.",
		// learn_protocol is generated from the single shared source of
		// truth (the exported constant internal/learn.RecallFirstProtocol)
		// also consumed by the CLI agent-context command, so the MCP and
		// CLI agent surfaces cannot drift.
		"learn_protocol": learn.RecallFirstProtocol,
		"auth": map[string]any{
			"type": "api_key",
			"env_vars": []map[string]any{
				{
					"name":        "BING_ADS_CUSTOMER_ACCOUNT_ID",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
				{
					"name":        "MICROSOFT_ADVERTISING_BING_DEVELOPER_TOKEN",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
				{
					"name":        "MICROSOFT_ADVERTISING_BING_ENTRA_BEARER",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "ad-insight",
				"description": "Manage ad insight",
				"endpoints":   []string{"apply-recommendations", "dismiss-recommendations", "get-auction-insight-data", "get-audience-breakdown", "get-audience-full-estimation", "get-auto-apply-opt-in-status", "get-bid-landscape-by-ad-group-ids", "get-bid-landscape-by-campaign-ids", "get-bid-landscape-by-keyword-ids", "get-bid-opportunities", "get-budget-opportunities", "get-domain-categories", "get-estimated-bid-by-keyword-ids", "get-estimated-bid-by-keywords", "get-estimated-position-by-keyword-ids", "get-estimated-position-by-keywords", "get-historical-keyword-performance", "get-historical-search-count", "get-keyword-categories", "get-keyword-demographics", "get-keyword-idea-categories", "get-keyword-ideas", "get-keyword-locations", "get-keyword-opportunities", "get-keyword-traffic-estimates", "get-performance-insights-detail-data-by-account-id", "get-recommendations", "get-text-asset-suggestions-by-final-urls", "retrieve-recommendations", "set-auto-apply-opt-in-status", "suggest-keywords-for-url", "suggest-keywords-from-existing-keywords", "tag-recommendations"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "bulk",
				"description": "Manage bulk",
				"endpoints":   []string{"download-campaigns-by-account-ids", "download-campaigns-by-campaign-ids", "get-download-status", "get-upload-status", "get-upload-url", "upload-entity-records"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "campaign-management",
				"description": "Manage campaign management",
				"endpoints":   []string{"add-ad-extensions", "add-ad-group-criterions", "add-ad-groups", "add-ads", "add-asset-groups", "add-audience-groups", "add-audiences", "add-bid-strategies", "add-brand-kits", "add-budgets", "add-campaign-conversion-goals", "add-campaign-criterions", "add-campaigns", "add-conversion-goals", "add-conversion-value-rules", "add-data-exclusions", "add-experiments", "add-html5s", "add-import-jobs", "add-keywords", "add-labels", "add-linked-in-segments", "add-list-items-to-shared-list", "add-media", "add-negative-keywords-to-entities", "add-new-customer-acquisition-goals", "add-seasonality-adjustments", "add-shared-entity", "add-uet-tags", "add-videos", "appeal-editorial-rejections", "apply-asset-group-listing-group-actions", "apply-customer-list-items", "apply-customer-list-user-data", "apply-hotel-group-actions", "apply-offline-conversion-adjustments", "apply-offline-conversions", "apply-online-conversion-adjustments", "apply-product-partition-actions", "create-asset-group-recommendation", "create-brand-kit-recommendation", "create-responsive-ad-recommendation", "create-responsive-search-ad-recommendation", "delete-ad-extensions", "delete-ad-extensions-associations", "delete-ad-group-criterions", "delete-ad-groups", "delete-ads", "delete-asset-groups", "delete-audience-group-asset-group-associations", "delete-audience-groups", "delete-audiences", "delete-bid-strategies", "delete-brand-kits", "delete-budgets", "delete-campaign-conversion-goals", "delete-campaign-criterions", "delete-campaigns", "delete-data-exclusions", "delete-experiments", "delete-html5s", "delete-import-jobs", "delete-keywords", "delete-label-associations", "delete-labels", "delete-linked-in-segments", "delete-list-items-from-shared-list", "delete-media", "delete-negative-keywords-from-entities", "delete-seasonality-adjustments", "delete-shared-entities", "delete-shared-entity-associations", "delete-videos", "get-account-migration-statuses", "get-account-properties", "get-ad-extension-ids-by-account-id", "get-ad-extensions-associations", "get-ad-extensions-by-ids", "get-ad-extensions-editorial-reasons", "get-ad-group-criterions-by-ids", "get-ad-groups-by-campaign-id", "get-ad-groups-by-ids", "get-ads-by-ad-group-id", "get-ads-by-editorial-status", "get-ads-by-ids", "get-annotation-opt-out", "get-asset-group-listing-groups-by-ids", "get-asset-groups-by-campaign-id", "get-asset-groups-by-ids", "get-asset-groups-editorial-reasons", "get-audience-group-asset-group-associations-by-asset-group-ids", "get-audience-group-asset-group-associations-by-audience-group-ids", "get-audience-groups-by-ids", "get-audiences-by-ids", "get-bid-strategies-by-ids", "get-bmc-stores-by-customer-id", "get-brand-kits-by-account-id", "get-brand-kits-by-ids", "get-bsc-countries", "get-budgets-by-ids", "get-campaign-criterions-by-ids", "get-campaign-ids-by-bid-strategy-ids", "get-campaign-ids-by-budget-ids", "get-campaign-sizes-by-account-id", "get-campaigns-by-account-id", "get-campaigns-by-ids", "get-clipchamp-templates", "get-config-value", "get-conversion-goals-by-ids", "get-conversion-goals-by-tag-ids", "get-conversion-value-rules-by-account-id", "get-conversion-value-rules-by-ids", "get-data-exclusions-by-account-id", "get-data-exclusions-by-ids", "get-diagnostics", "get-editorial-reasons-by-ids", "get-experiments-by-ids", "get-file-import-upload-url", "get-geo-locations-file-url", "get-health-check", "get-html5s-by-ids", "get-import-entity-ids-mapping", "get-import-jobs-by-ids", "get-import-results", "get-keywords-by-ad-group-id", "get-keywords-by-editorial-status", "get-keywords-by-ids", "get-label-associations-by-entity-ids", "get-label-associations-by-label-ids", "get-labels-by-ids", "get-list-items-by-shared-list", "get-media-associations", "get-media-meta-data-by-account-id", "get-media-meta-data-by-ids", "get-negative-keywords-by-entity-ids", "get-negative-sites-by-ad-group-ids", "get-negative-sites-by-campaign-ids", "get-new-customer-acquisition-goals-by-account-id", "get-offline-conversion-report-by-goal-ids", "get-offline-conversion-reports", "get-profile-data-file-url", "get-responsive-ad-recommendation-job", "get-seasonality-adjustments-by-account-id", "get-seasonality-adjustments-by-ids", "get-shared-entities", "get-shared-entities-by-account-id", "get-shared-entity-associations-by-entity-ids", "get-shared-entity-associations-by-shared-entity-ids", "get-supported-clipchamp-audio", "get-supported-fonts", "get-uet-tag-auth-key", "get-uet-tags-by-ids", "get-videos-by-ids", "refine-asset-group-recommendation", "refine-responsive-ad-recommendation", "refine-responsive-search-ad-recommendation", "search-companies", "set-account-properties", "set-ad-extensions-associations", "set-audience-group-asset-group-associations", "set-label-associations", "set-negative-sites-to-ad-groups", "set-negative-sites-to-campaigns", "set-shared-entity-associations", "update-ad-extensions", "update-ad-group-criterions", "update-ad-groups", "update-ads", "update-annotation-opt-out", "update-asset-groups", "update-audience-groups", "update-audiences", "update-bid-strategies", "update-brand-kits", "update-budgets", "update-campaign-criterions", "update-campaigns", "update-conversion-goals", "update-conversion-value-rules", "update-conversion-value-rules-status", "update-data-exclusions", "update-experiments", "update-import-jobs", "update-keywords", "update-labels", "update-linked-in-segments", "update-new-customer-acquisition-goals", "update-seasonality-adjustments", "update-shared-entities", "update-uet-tags", "update-videos"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "customer-billing",
				"description": "Manage customer billing",
				"endpoints":   []string{"add-insertion-order", "check-feature-adoption-coupon-eligibility", "claim-feature-adoption-coupons", "dispatch-coupons", "distribute-coupons", "get-account-monthly-spend", "get-billing-documents", "get-billing-documents-info", "get-billing-groups", "get-coupon-info", "get-ungrouped-accounts", "redeem-coupon", "search-coupons", "search-insertion-orders", "update-billing-group-accounts", "update-insertion-order"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "customer-management",
				"description": "Manage customer management",
				"endpoints":   []string{"add-account", "add-client-links", "add-prepay-account", "delete-account", "delete-customer", "delete-user", "dismiss-notifications", "find-accounts", "find-accounts-or-customers-info", "get-accessible-customer", "get-account", "get-account-pilot-features", "get-accounts-info", "get-current-user", "get-customer", "get-customer-pilot-features", "get-customers-info", "get-linked-accounts-and-customers-info", "get-notifications", "get-pilot-features-countries", "get-user", "get-user-mfa-status", "get-users-info", "map-account-id-to-external-account-ids", "map-customer-id-to-external-customer-id", "search-accounts", "search-client-links", "search-customers", "search-user-invitations", "send-user-invitation", "signup-customer", "update-account", "update-client-links", "update-customer", "update-prepay-account", "update-user", "update-user-roles", "upgrade-customer-to-agency", "validate-address"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "reporting",
				"description": "Manage reporting",
				"endpoints":   []string{"poll-generate-report", "submit-generate-report"},
				"searchable":  true,
				"writable":    true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
		},
	}
	return toolResultJSON(ctx)
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
