// Copyright 2026 waveriderai and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newPublishersUnifiedCmd(flags *rootFlags) *cobra.Command {
	var flagPublisherIds string
	var flagPublisherIdType string

	cmd := &cobra.Command{
		Use:         "unified",
		Short:       "Resolve a publisher to its unified cross-platform record",
		Example:     "  sensortower-pp-cli publishers unified --publisher-ids 358612219 --publisher-id-type itunes --json",
		Annotations: map[string]string{"pp:endpoint": "publishers.unified", "pp:method": "GET", "pp:path": "/api/unified/publishers", "mcp:read-only": "true", "pp:happy-args": "--publisher-ids=358612219;--publisher-id-type=itunes"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !cmd.Flags().Changed("publisher-ids") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "publisher-ids")
			}
			if cmd.Flags().Changed("publisher-id-type") {
				allowedPublisherIdType := []string{"unified", "android", "itunes"}
				validPublisherIdType := false
				for _, v := range allowedPublisherIdType {
					if flagPublisherIdType == v {
						validPublisherIdType = true
						break
					}
				}
				if !validPublisherIdType {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagPublisherIdType, "publisher-id-type", allowedPublisherIdType)
				}
			}
			path := "/api/unified/publishers"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagPublisherIds != "" {
				params["publisher_ids"] = formatCLIParamValue(flagPublisherIds)
			}
			if flagPublisherIdType != "" {
				params["publisher_id_type"] = formatCLIParamValue(flagPublisherIdType)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "publishers", false, path, params, nil, "publishers", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagPublisherIds, "publisher-ids", "", "Comma-separated publisher IDs")
	cmd.Flags().StringVar(&flagPublisherIdType, "publisher-id-type", "itunes", "Which ID namespace publisher-ids are expressed in (one of: unified, android, itunes)")

	return cmd
}
