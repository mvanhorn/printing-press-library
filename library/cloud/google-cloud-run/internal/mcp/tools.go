// Copyright 2026 never-mind-3 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"google-cloud-run-pp-cli/internal/cli"
	"google-cloud-run-pp-cli/internal/client"
	"google-cloud-run-pp-cli/internal/cliutil"
	"google-cloud-run-pp-cli/internal/config"
	"google-cloud-run-pp-cli/internal/mcp/cobratree"
	"google-cloud-run-pp-cli/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("cloud-run-admin-jobs_create",
			mcplib.WithDescription("Create a Cloud Run Job in a GCP project. Required: parent (format: projects/{project}/locations/{region}). Key optional: jobId (name for the job), validateOnly (dry-run validation), annotations. Returns a long-running operation — use operations_wait to poll for completion. Use this before cloud-run-admin-jobs_run when the job doesn't exist yet."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The location and project in which this Job should be created.")),
			mcplib.WithString("jobId", mcplib.Description("Required. The unique identifier for the Job. The name of the job becomes {parent}/jobs/{job_id}.")),
			mcplib.WithBoolean("validateOnly", mcplib.Description("Indicates that the request should be validated and default values populated")),
			mcplib.WithString("annotations", mcplib.Description("KRM-style annotations for the resource.")),
			mcplib.WithString("binary-authorization-breakglass-justification", mcplib.Description("If present, indicates to use Breakglass using this justification. If use_default is False, then it must be empty.")),
			mcplib.WithBoolean("binary-authorization-use-default", mcplib.Description("If True, indicates to use the default project's binary authorization policy.")),
			mcplib.WithString("client", mcplib.Description("Arbitrary identifier for the API client.")),
			mcplib.WithString("clientVersion", mcplib.Description("Arbitrary version identifier for the API client.")),
			mcplib.WithString("conditions", mcplib.Description("Output only. The Conditions of all other associated sub-resources.")),
			mcplib.WithString("createTime", mcplib.Description("Output only. The creation time.")),
			mcplib.WithString("creator", mcplib.Description("Output only. Email address of the authenticated creator.")),
			mcplib.WithString("deleteTime", mcplib.Description("Output only. The deletion time.")),
			mcplib.WithString("etag", mcplib.Description("Output only. A system-generated fingerprint for this version of the resource. May be used to...")),
			mcplib.WithNumber("executionCount", mcplib.Description("Output only. Number of executions created for this job.")),
			mcplib.WithString("expireTime", mcplib.Description("Output only. For a deleted resource, the time after which it will be permamently deleted.")),
			mcplib.WithString("generation", mcplib.Description("Output only. A number that monotonically increases every time the user modifies the desired state.")),
			mcplib.WithString("labels", mcplib.Description("KRM-style labels for the resource.")),
			mcplib.WithString("lastModifier", mcplib.Description("Output only. Email address of the last authenticated modifier.")),
			mcplib.WithString("latest-created-execution-completion-time", mcplib.Description("Creation timestamp of the execution.")),
			mcplib.WithString("latest-created-execution-create-time", mcplib.Description("Creation timestamp of the execution.")),
			mcplib.WithString("latest-created-execution-name", mcplib.Description("Name of the execution.")),
			mcplib.WithString("launchStage", mcplib.Description("The launch stage as defined by [Google Cloud Platform Launch Stages](https://cloud.")),
			mcplib.WithString("name", mcplib.Description("The fully qualified name of this Job. Format: projects/{project}/locations/{location}/jobs/{job}")),
			mcplib.WithString("observedGeneration", mcplib.Description("Output only. The generation of this Job.")),
			mcplib.WithBoolean("reconciling", mcplib.Description("Output only. Returns true if the Job is currently being acted upon by the system to bring it into the desired state.")),
			mcplib.WithBoolean("satisfiesPzs", mcplib.Description("Output only. Reserved for future use.")),
			mcplib.WithString("template-annotations", mcplib.Description("KRM-style annotations for the resource. Cloud Run API v2 does not support annotations with `run.googleapis.com`, `cloud.")),
			mcplib.WithString("template-labels", mcplib.Description("KRM-style labels for the resource. Cloud Run API v2 does not support labels with `run.googleapis.com`, `cloud.")),
			mcplib.WithNumber("template-parallelism", mcplib.Description("Specifies the maximum desired number of tasks the execution should run at given time. Must be <= task_count.")),
			mcplib.WithNumber("template-task-count", mcplib.Description("Specifies the desired number of tasks the execution should run.")),
			mcplib.WithString("template-template-containers", mcplib.Description("Holds the single container that defines the unit of execution for this task.")),
			mcplib.WithString("template-template-encryption-key", mcplib.Description("A reference to a customer managed encryption key (CMEK) to use to encrypt this container image.")),
			mcplib.WithString("template-template-execution-environment", mcplib.Description("The execution environment being used to host this Task.")),
			mcplib.WithNumber("template-template-max-retries", mcplib.Description("Number of retries allowed per Task, before marking this Task failed. Defaults to 3.")),
			mcplib.WithString("template-template-service-account", mcplib.Description("Email address of the IAM service account associated with the Task of a Job.")),
			mcplib.WithString("template-template-timeout", mcplib.Description("Max allowed time duration the Task may be active before the system will actively try to mark it failed and kill")),
			mcplib.WithString("template-template-volumes", mcplib.Description("A list of Volumes to make available to containers.")),
			mcplib.WithString("terminal-condition-execution-reason", mcplib.Description("A reason for the execution condition.")),
			mcplib.WithString("terminal-condition-last-transition-time", mcplib.Description("Last time the condition transitioned from one status to another.")),
			mcplib.WithString("terminal-condition-message", mcplib.Description("Human readable message indicating details about the current status.")),
			mcplib.WithString("terminal-condition-reason", mcplib.Description("A common (service-level) reason for this condition.")),
			mcplib.WithString("terminal-condition-revision-reason", mcplib.Description("A reason for the revision condition.")),
			mcplib.WithString("terminal-condition-severity", mcplib.Description("How to interpret failures of this condition, one of Error, Warning, Info")),
			mcplib.WithString("terminal-condition-state", mcplib.Description("State of the condition.")),
			mcplib.WithString("terminal-condition-type", mcplib.Description("type is used to communicate the status of the reconciliation process. See also: https://github.")),
			mcplib.WithString("uid", mcplib.Description("Output only. Server assigned unique identifier for the Execution.")),
			mcplib.WithString("updateTime", mcplib.Description("Output only. The last-modified time.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/{parent}/jobs", false, false, nil, []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "jobId", WireName: "jobId", Location: "query"}, {PublicName: "validateOnly", WireName: "validateOnly", Location: "query"}, {PublicName: "annotations", WireName: "annotations", Location: "body"}, {PublicName: "binary-authorization-breakglass-justification", WireName: "breakglassJustification", Location: "body", BodyPath: []string{"binaryAuthorization", "breakglassJustification"}}, {PublicName: "binary-authorization-use-default", WireName: "useDefault", Location: "body", BodyPath: []string{"binaryAuthorization", "useDefault"}}, {PublicName: "client", WireName: "client", Location: "body"}, {PublicName: "clientVersion", WireName: "clientVersion", Location: "body"}, {PublicName: "conditions", WireName: "conditions", Location: "body"}, {PublicName: "createTime", WireName: "createTime", Location: "body"}, {PublicName: "creator", WireName: "creator", Location: "body"}, {PublicName: "deleteTime", WireName: "deleteTime", Location: "body"}, {PublicName: "etag", WireName: "etag", Location: "body"}, {PublicName: "executionCount", WireName: "executionCount", Location: "body"}, {PublicName: "expireTime", WireName: "expireTime", Location: "body"}, {PublicName: "generation", WireName: "generation", Location: "body"}, {PublicName: "labels", WireName: "labels", Location: "body"}, {PublicName: "lastModifier", WireName: "lastModifier", Location: "body"}, {PublicName: "latest-created-execution-completion-time", WireName: "completionTime", Location: "body", BodyPath: []string{"latestCreatedExecution", "completionTime"}}, {PublicName: "latest-created-execution-create-time", WireName: "createTime", Location: "body", BodyPath: []string{"latestCreatedExecution", "createTime"}}, {PublicName: "latest-created-execution-name", WireName: "name", Location: "body", BodyPath: []string{"latestCreatedExecution", "name"}}, {PublicName: "launchStage", WireName: "launchStage", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "observedGeneration", WireName: "observedGeneration", Location: "body"}, {PublicName: "reconciling", WireName: "reconciling", Location: "body"}, {PublicName: "satisfiesPzs", WireName: "satisfiesPzs", Location: "body"}, {PublicName: "template-annotations", WireName: "annotations", Location: "body", BodyPath: []string{"template", "annotations"}}, {PublicName: "template-labels", WireName: "labels", Location: "body", BodyPath: []string{"template", "labels"}}, {PublicName: "template-parallelism", WireName: "parallelism", Location: "body", BodyPath: []string{"template", "parallelism"}}, {PublicName: "template-task-count", WireName: "taskCount", Location: "body", BodyPath: []string{"template", "taskCount"}}, {PublicName: "template-template-containers", WireName: "containers", Location: "body", BodyPath: []string{"template", "template", "containers"}}, {PublicName: "template-template-encryption-key", WireName: "encryptionKey", Location: "body", BodyPath: []string{"template", "template", "encryptionKey"}}, {PublicName: "template-template-execution-environment", WireName: "executionEnvironment", Location: "body", BodyPath: []string{"template", "template", "executionEnvironment"}}, {PublicName: "template-template-max-retries", WireName: "maxRetries", Location: "body", BodyPath: []string{"template", "template", "maxRetries"}}, {PublicName: "template-template-service-account", WireName: "serviceAccount", Location: "body", BodyPath: []string{"template", "template", "serviceAccount"}}, {PublicName: "template-template-timeout", WireName: "timeout", Location: "body", BodyPath: []string{"template", "template", "timeout"}}, {PublicName: "template-template-volumes", WireName: "volumes", Location: "body", BodyPath: []string{"template", "template", "volumes"}}, {PublicName: "terminal-condition-execution-reason", WireName: "executionReason", Location: "body", BodyPath: []string{"terminalCondition", "executionReason"}}, {PublicName: "terminal-condition-last-transition-time", WireName: "lastTransitionTime", Location: "body", BodyPath: []string{"terminalCondition", "lastTransitionTime"}}, {PublicName: "terminal-condition-message", WireName: "message", Location: "body", BodyPath: []string{"terminalCondition", "message"}}, {PublicName: "terminal-condition-reason", WireName: "reason", Location: "body", BodyPath: []string{"terminalCondition", "reason"}}, {PublicName: "terminal-condition-revision-reason", WireName: "revisionReason", Location: "body", BodyPath: []string{"terminalCondition", "revisionReason"}}, {PublicName: "terminal-condition-severity", WireName: "severity", Location: "body", BodyPath: []string{"terminalCondition", "severity"}}, {PublicName: "terminal-condition-state", WireName: "state", Location: "body", BodyPath: []string{"terminalCondition", "state"}}, {PublicName: "terminal-condition-type", WireName: "type", Location: "body", BodyPath: []string{"terminalCondition", "type"}}, {PublicName: "uid", WireName: "uid", Location: "body"}, {PublicName: "updateTime", WireName: "updateTime", Location: "body"}}, []string{"parent"}),
	)
	s.AddTool(
		mcplib.NewTool("cloud-run-admin-jobs_list",
			mcplib.WithDescription("List Cloud Run Jobs in a project and region. Required: parent (format: projects/{project}/locations/{region}). Optional: pageSize (max results), pageToken (pagination cursor), showDeleted (include soft-deleted jobs). Returns job names, statuses, and metadata. Use cloud-run-admin-jobs_run to trigger an execution from a job returned here."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The location and project to list resources on.")),
			mcplib.WithNumber("pageSize", mcplib.Description("Maximum number of Jobs to return in this call.")),
			mcplib.WithString("pageToken", mcplib.Description("A page token received from a previous call to ListJobs. All other parameters must match.")),
			mcplib.WithBoolean("showDeleted", mcplib.Description("If true, returns deleted (but unexpired) resources along with active ones.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{parent}/jobs", true, false, nil, []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}, {PublicName: "showDeleted", WireName: "showDeleted", Location: "query"}}, []string{"parent"}),
	)
	s.AddTool(
		mcplib.NewTool("cloud-run-admin-jobs_run",
			mcplib.WithDescription("Trigger a new Execution of an existing Cloud Run Job. Required: name (full job resource name, e.g. projects/{project}/locations/{region}/jobs/{job}). Optional: etag (optimistic concurrency check), validateOnly (dry-run). Returns a long-running operation — use operations_wait to block until the execution completes. Prefer this over cloud-run-admin-jobs_create when the job already exists."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Required. The full name of the Job.")),
			mcplib.WithString("etag", mcplib.Description("A system-generated fingerprint for this version of the resource.")),
			mcplib.WithBoolean("validateOnly", mcplib.Description("Indicates that the request should be validated without actually deleting any resources.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/{name}:run", false, false, nil, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "etag", WireName: "etag", Location: "body"}, {PublicName: "validateOnly", WireName: "validateOnly", Location: "body"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("cloud-run-admin-jobs_executions_list",
			mcplib.WithDescription("List past Executions of a Cloud Run Job. Required: parent (full job resource name: projects/{project}/locations/{region}/jobs/{job}). Optional: pageSize, pageToken, showDeleted (include cancelled/deleted executions). Returns execution names, start times, completion status, and task counts. Use executions_tasks_list to drill into individual tasks from an execution returned here."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The Execution from which the Executions should be listed.")),
			mcplib.WithNumber("pageSize", mcplib.Description("Maximum number of Executions to return in this call.")),
			mcplib.WithString("pageToken", mcplib.Description("A page token received from a previous call to ListExecutions. All other parameters must match.")),
			mcplib.WithBoolean("showDeleted", mcplib.Description("If true, returns deleted (but unexpired) resources along with active ones.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{parent}/executions", true, false, nil, []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}, {PublicName: "showDeleted", WireName: "showDeleted", Location: "query"}}, []string{"parent"}),
	)
	s.AddTool(
		mcplib.NewTool("executions_tasks_list",
			mcplib.WithDescription("List individual Tasks within a specific Job Execution. Required: parent (full execution resource name: projects/{project}/locations/{region}/jobs/{job}/executions/{execution}). Optional: pageSize, pageToken, showDeleted. Returns task indices, start/completion times, and exit codes. Use this to diagnose which tasks in an execution failed."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The Execution from which the Tasks should be listed.")),
			mcplib.WithNumber("pageSize", mcplib.Description("Maximum number of Tasks to return in this call.")),
			mcplib.WithString("pageToken", mcplib.Description("A page token received from a previous call to ListTasks. All other parameters must match.")),
			mcplib.WithBoolean("showDeleted", mcplib.Description("If true, returns deleted (but unexpired) resources along with active ones.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{parent}/tasks", true, false, nil, []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}, {PublicName: "showDeleted", WireName: "showDeleted", Location: "query"}}, []string{"parent"}),
	)
	s.AddTool(
		mcplib.NewTool("operations_list",
			mcplib.WithDescription("List long-running operations for a given Cloud Run resource or location. Required: name (project or location path, e.g. projects/{project}/locations/{region}). Optional: filter (AIP-160 expression, e.g. 'done=false' for pending ops), pageSize, pageToken. Returns operation names, status, and metadata. Use operations_wait to block on a specific operation from this list."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Required. To query for all of the operations for a project.")),
			mcplib.WithString("filter", mcplib.Description("Optional. A filter for matching the completed or in-progress operations.")),
			mcplib.WithNumber("pageSize", mcplib.Description("The maximum number of records that should be returned. Requested page size cannot exceed 100.")),
			mcplib.WithString("pageToken", mcplib.Description("Token identifying which result to start with, which is returned by a previous list call.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{name}/operations", true, false, nil, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "filter", WireName: "filter", Location: "query"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("operations_wait",
			mcplib.WithDescription("Block until a Cloud Run long-running operation completes or a timeout elapses. Required: name (full operation resource name returned by create/update/delete commands). Optional: timeout (duration string, e.g. '30s'; defaults to HTTP timeout). Returns the operation's final state including any error. Call this after cloud-run-admin-jobs_create, services_create, services_patch, or services_revisions_delete to confirm the operation succeeded before proceeding."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("The name of the operation resource to wait on.")),
			mcplib.WithString("timeout", mcplib.Description("The maximum duration to wait before timing out.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/{name}:wait", false, false, nil, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "timeout", WireName: "timeout", Location: "body"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("services_create",
			mcplib.WithDescription("Create a new Cloud Run Service in a GCP project and region. Required: parent (format: projects/{project}/locations/{region}). Key optional: serviceId (service name), validateOnly (dry-run), ingress, scaling, container image settings. Returns a long-running operation — use operations_wait to confirm completion. Use services_patch to update an existing service instead."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The location and project in which this service should be created.")),
			mcplib.WithString("serviceId", mcplib.Description("Required. The unique identifier for the Service.")),
			mcplib.WithBoolean("validateOnly", mcplib.Description("Indicates that the request should be validated and default values populated")),
			mcplib.WithString("annotations", mcplib.Description("Unstructured key value map that may be set by external tools to store and arbitrary metadata.")),
			mcplib.WithString("binary-authorization-breakglass-justification", mcplib.Description("If present, indicates to use Breakglass using this justification. If use_default is False, then it must be empty.")),
			mcplib.WithBoolean("binary-authorization-use-default", mcplib.Description("If True, indicates to use the default project's binary authorization policy.")),
			mcplib.WithString("client", mcplib.Description("Arbitrary identifier for the API client.")),
			mcplib.WithString("clientVersion", mcplib.Description("Arbitrary version identifier for the API client.")),
			mcplib.WithString("conditions", mcplib.Description("Output only. The Conditions of all other associated sub-resources.")),
			mcplib.WithString("createTime", mcplib.Description("Output only. The creation time.")),
			mcplib.WithString("creator", mcplib.Description("Output only. Email address of the authenticated creator.")),
			mcplib.WithString("deleteTime", mcplib.Description("Output only. The deletion time.")),
			mcplib.WithString("description", mcplib.Description("User-provided description of the Service. This field currently has a 512-character limit.")),
			mcplib.WithString("etag", mcplib.Description("Output only. A system-generated fingerprint for this version of the resource. May be used to...")),
			mcplib.WithString("expireTime", mcplib.Description("Output only. For a deleted resource, the time after which it will be permamently deleted.")),
			mcplib.WithString("generation", mcplib.Description("Output only. A number that monotonically increases every time the user modifies the desired state.")),
			mcplib.WithString("ingress", mcplib.Description("Provides the ingress settings for this Service.")),
			mcplib.WithString("labels", mcplib.Description("Map of string keys and values that can be used to organize and categorize objects.")),
			mcplib.WithString("lastModifier", mcplib.Description("Output only. Email address of the last authenticated modifier.")),
			mcplib.WithString("latestCreatedRevision", mcplib.Description("Output only. Name of the last created revision.")),
			mcplib.WithString("latestReadyRevision", mcplib.Description("Output only. Name of the latest revision that is serving traffic.")),
			mcplib.WithString("launchStage", mcplib.Description("The launch stage as defined by [Google Cloud Platform Launch Stages](https://cloud.")),
			mcplib.WithString("name", mcplib.Description("The fully qualified name of this Service.")),
			mcplib.WithString("observedGeneration", mcplib.Description("Output only. The generation of this Service currently serving traffic.")),
			mcplib.WithBoolean("reconciling", mcplib.Description("Output only. Returns true if the Service is currently being acted upon by the system to bring it into the desired state.")),
			mcplib.WithBoolean("satisfiesPzs", mcplib.Description("Output only. Reserved for future use.")),
			mcplib.WithString("template-annotations", mcplib.Description("KRM-style annotations for the resource. Cloud Run API v2 does not support annotations with `run.googleapis.com`, `cloud.")),
			mcplib.WithString("template-containers", mcplib.Description("Holds the single container that defines the unit of execution for this Revision.")),
			mcplib.WithString("template-encryption-key", mcplib.Description("A reference to a customer managed encryption key (CMEK) to use to encrypt this container image.")),
			mcplib.WithString("template-execution-environment", mcplib.Description("The sandbox environment to host this Revision.")),
			mcplib.WithString("template-labels", mcplib.Description("KRM-style labels for the resource. Cloud Run API v2 does not support labels with `run.googleapis.com`, `cloud.")),
			mcplib.WithNumber("template-max-instance-request-concurrency", mcplib.Description("Sets the maximum number of requests that each serving instance can receive.")),
			mcplib.WithString("template-revision", mcplib.Description("The unique name for the revision.")),
			mcplib.WithNumber("template-scaling-max-instance-count", mcplib.Description("Maximum number of serving instances that this resource should have.")),
			mcplib.WithNumber("template-scaling-min-instance-count", mcplib.Description("Minimum number of serving instances that this resource should have.")),
			mcplib.WithString("template-service-account", mcplib.Description("Email address of the IAM service account associated with the revision of the service.")),
			mcplib.WithString("template-timeout", mcplib.Description("Max allowed time for an instance to respond to a request.")),
			mcplib.WithString("template-volumes", mcplib.Description("A list of Volumes to make available to containers.")),
			mcplib.WithString("template-vpc-access-connector", mcplib.Description("VPC Access connector name.")),
			mcplib.WithString("template-vpc-access-egress", mcplib.Description("Traffic VPC egress settings.")),
			mcplib.WithString("terminal-condition-execution-reason", mcplib.Description("A reason for the execution condition.")),
			mcplib.WithString("terminal-condition-last-transition-time", mcplib.Description("Last time the condition transitioned from one status to another.")),
			mcplib.WithString("terminal-condition-message", mcplib.Description("Human readable message indicating details about the current status.")),
			mcplib.WithString("terminal-condition-reason", mcplib.Description("A common (service-level) reason for this condition.")),
			mcplib.WithString("terminal-condition-revision-reason", mcplib.Description("A reason for the revision condition.")),
			mcplib.WithString("terminal-condition-severity", mcplib.Description("How to interpret failures of this condition, one of Error, Warning, Info")),
			mcplib.WithString("terminal-condition-state", mcplib.Description("State of the condition.")),
			mcplib.WithString("terminal-condition-type", mcplib.Description("type is used to communicate the status of the reconciliation process. See also: https://github.")),
			mcplib.WithString("traffic", mcplib.Description("Specifies how to distribute traffic over a collection of Revisions belonging to the Service.")),
			mcplib.WithString("trafficStatuses", mcplib.Description("Output only. Detailed status information for corresponding traffic targets.")),
			mcplib.WithString("uid", mcplib.Description("Output only. Server assigned unique identifier for the trigger.")),
			mcplib.WithString("updateTime", mcplib.Description("Output only. The last-modified time.")),
			mcplib.WithString("uri", mcplib.Description("Output only. The main URI in which this Service is serving traffic.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/{parent}/services", false, false, nil, []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "serviceId", WireName: "serviceId", Location: "query"}, {PublicName: "validateOnly", WireName: "validateOnly", Location: "query"}, {PublicName: "annotations", WireName: "annotations", Location: "body"}, {PublicName: "binary-authorization-breakglass-justification", WireName: "breakglassJustification", Location: "body", BodyPath: []string{"binaryAuthorization", "breakglassJustification"}}, {PublicName: "binary-authorization-use-default", WireName: "useDefault", Location: "body", BodyPath: []string{"binaryAuthorization", "useDefault"}}, {PublicName: "client", WireName: "client", Location: "body"}, {PublicName: "clientVersion", WireName: "clientVersion", Location: "body"}, {PublicName: "conditions", WireName: "conditions", Location: "body"}, {PublicName: "createTime", WireName: "createTime", Location: "body"}, {PublicName: "creator", WireName: "creator", Location: "body"}, {PublicName: "deleteTime", WireName: "deleteTime", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "etag", WireName: "etag", Location: "body"}, {PublicName: "expireTime", WireName: "expireTime", Location: "body"}, {PublicName: "generation", WireName: "generation", Location: "body"}, {PublicName: "ingress", WireName: "ingress", Location: "body"}, {PublicName: "labels", WireName: "labels", Location: "body"}, {PublicName: "lastModifier", WireName: "lastModifier", Location: "body"}, {PublicName: "latestCreatedRevision", WireName: "latestCreatedRevision", Location: "body"}, {PublicName: "latestReadyRevision", WireName: "latestReadyRevision", Location: "body"}, {PublicName: "launchStage", WireName: "launchStage", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "observedGeneration", WireName: "observedGeneration", Location: "body"}, {PublicName: "reconciling", WireName: "reconciling", Location: "body"}, {PublicName: "satisfiesPzs", WireName: "satisfiesPzs", Location: "body"}, {PublicName: "template-annotations", WireName: "annotations", Location: "body", BodyPath: []string{"template", "annotations"}}, {PublicName: "template-containers", WireName: "containers", Location: "body", BodyPath: []string{"template", "containers"}}, {PublicName: "template-encryption-key", WireName: "encryptionKey", Location: "body", BodyPath: []string{"template", "encryptionKey"}}, {PublicName: "template-execution-environment", WireName: "executionEnvironment", Location: "body", BodyPath: []string{"template", "executionEnvironment"}}, {PublicName: "template-labels", WireName: "labels", Location: "body", BodyPath: []string{"template", "labels"}}, {PublicName: "template-max-instance-request-concurrency", WireName: "maxInstanceRequestConcurrency", Location: "body", BodyPath: []string{"template", "maxInstanceRequestConcurrency"}}, {PublicName: "template-revision", WireName: "revision", Location: "body", BodyPath: []string{"template", "revision"}}, {PublicName: "template-scaling-max-instance-count", WireName: "maxInstanceCount", Location: "body", BodyPath: []string{"template", "scaling", "maxInstanceCount"}}, {PublicName: "template-scaling-min-instance-count", WireName: "minInstanceCount", Location: "body", BodyPath: []string{"template", "scaling", "minInstanceCount"}}, {PublicName: "template-service-account", WireName: "serviceAccount", Location: "body", BodyPath: []string{"template", "serviceAccount"}}, {PublicName: "template-timeout", WireName: "timeout", Location: "body", BodyPath: []string{"template", "timeout"}}, {PublicName: "template-volumes", WireName: "volumes", Location: "body", BodyPath: []string{"template", "volumes"}}, {PublicName: "template-vpc-access-connector", WireName: "connector", Location: "body", BodyPath: []string{"template", "vpcAccess", "connector"}}, {PublicName: "template-vpc-access-egress", WireName: "egress", Location: "body", BodyPath: []string{"template", "vpcAccess", "egress"}}, {PublicName: "terminal-condition-execution-reason", WireName: "executionReason", Location: "body", BodyPath: []string{"terminalCondition", "executionReason"}}, {PublicName: "terminal-condition-last-transition-time", WireName: "lastTransitionTime", Location: "body", BodyPath: []string{"terminalCondition", "lastTransitionTime"}}, {PublicName: "terminal-condition-message", WireName: "message", Location: "body", BodyPath: []string{"terminalCondition", "message"}}, {PublicName: "terminal-condition-reason", WireName: "reason", Location: "body", BodyPath: []string{"terminalCondition", "reason"}}, {PublicName: "terminal-condition-revision-reason", WireName: "revisionReason", Location: "body", BodyPath: []string{"terminalCondition", "revisionReason"}}, {PublicName: "terminal-condition-severity", WireName: "severity", Location: "body", BodyPath: []string{"terminalCondition", "severity"}}, {PublicName: "terminal-condition-state", WireName: "state", Location: "body", BodyPath: []string{"terminalCondition", "state"}}, {PublicName: "terminal-condition-type", WireName: "type", Location: "body", BodyPath: []string{"terminalCondition", "type"}}, {PublicName: "traffic", WireName: "traffic", Location: "body"}, {PublicName: "trafficStatuses", WireName: "trafficStatuses", Location: "body"}, {PublicName: "uid", WireName: "uid", Location: "body"}, {PublicName: "updateTime", WireName: "updateTime", Location: "body"}, {PublicName: "uri", WireName: "uri", Location: "body"}}, []string{"parent"}),
	)
	s.AddTool(
		mcplib.NewTool("services_get-iam-policy",
			mcplib.WithDescription("Retrieve the current IAM policy for a Cloud Run Service (who has which roles). Required: resource (full service name: projects/{project}/locations/{region}/services/{service}). Optional: options.requestedPolicyVersion (policy schema version). Returns role-to-member bindings. Use services_set-iam-policy to update the policy, or services_test-iam-permissions to check specific permissions."),
			mcplib.WithString("resource", mcplib.Required(), mcplib.Description("REQUIRED: The resource for which the policy is being requested. See [Resource names](https://cloud.google.")),
			mcplib.WithNumber("options.requestedPolicyVersion", mcplib.Description("Optional. The maximum policy version that will be used to format the policy. Valid values are 0, 1, and 3.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{resource}:getIamPolicy", true, false, nil, []mcpParamBinding{{PublicName: "resource", WireName: "resource", Location: "path"}, {PublicName: "options.requestedPolicyVersion", WireName: "options.requestedPolicyVersion", Location: "query"}}, []string{"resource"}),
	)
	s.AddTool(
		mcplib.NewTool("services_list",
			mcplib.WithDescription("List Cloud Run Services in a project and region. Required: parent (format: projects/{project}/locations/{region}). Optional: pageSize (max results), pageToken (pagination), showDeleted (include soft-deleted). Returns service names, URIs, latest revision, and traffic config. For multi-project fleet visibility, use the novel services list-all command instead."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The location and project to list resources on.")),
			mcplib.WithNumber("pageSize", mcplib.Description("Maximum number of Services to return in this call.")),
			mcplib.WithString("pageToken", mcplib.Description("A page token received from a previous call to ListServices. All other parameters must match.")),
			mcplib.WithBoolean("showDeleted", mcplib.Description("If true, returns deleted (but unexpired) resources along with active ones.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{parent}/services", true, false, nil, []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}, {PublicName: "showDeleted", WireName: "showDeleted", Location: "query"}}, []string{"parent"}),
	)
	s.AddTool(
		mcplib.NewTool("services_patch",
			mcplib.WithDescription("Update an existing Cloud Run Service (image, scaling, env vars, ingress, etc.). Required: name (full service resource name: projects/{project}/locations/{region}/services/{service}). Key optional: allowMissing (upsert behavior), validateOnly, container image, CPU/memory limits, env vars, scaling. Returns a long-running operation — use operations_wait to confirm. Prefer this over services_create for updates to existing services."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("The fully qualified name of this Service.")),
			mcplib.WithBoolean("allowMissing", mcplib.Description("This field is currently not used by Cloud Run; setting it does not have any effect.")),
			mcplib.WithBoolean("validateOnly", mcplib.Description("Indicates that the request should be validated and default values populated")),
			mcplib.WithString("annotations", mcplib.Description("Unstructured key value map that may be set by external tools to store and arbitrary metadata.")),
			mcplib.WithString("binary-authorization-breakglass-justification", mcplib.Description("If present, indicates to use Breakglass using this justification. If use_default is False, then it must be empty.")),
			mcplib.WithBoolean("binary-authorization-use-default", mcplib.Description("If True, indicates to use the default project's binary authorization policy.")),
			mcplib.WithString("client", mcplib.Description("Arbitrary identifier for the API client.")),
			mcplib.WithString("clientVersion", mcplib.Description("Arbitrary version identifier for the API client.")),
			mcplib.WithString("conditions", mcplib.Description("Output only. The Conditions of all other associated sub-resources.")),
			mcplib.WithString("createTime", mcplib.Description("Output only. The creation time.")),
			mcplib.WithString("creator", mcplib.Description("Output only. Email address of the authenticated creator.")),
			mcplib.WithString("deleteTime", mcplib.Description("Output only. The deletion time.")),
			mcplib.WithString("description", mcplib.Description("User-provided description of the Service. This field currently has a 512-character limit.")),
			mcplib.WithString("etag", mcplib.Description("Output only. A system-generated fingerprint for this version of the resource. May be used to...")),
			mcplib.WithString("expireTime", mcplib.Description("Output only. For a deleted resource, the time after which it will be permamently deleted.")),
			mcplib.WithString("generation", mcplib.Description("Output only. A number that monotonically increases every time the user modifies the desired state.")),
			mcplib.WithString("ingress", mcplib.Description("Provides the ingress settings for this Service.")),
			mcplib.WithString("labels", mcplib.Description("Map of string keys and values that can be used to organize and categorize objects.")),
			mcplib.WithString("lastModifier", mcplib.Description("Output only. Email address of the last authenticated modifier.")),
			mcplib.WithString("latestCreatedRevision", mcplib.Description("Output only. Name of the last created revision.")),
			mcplib.WithString("latestReadyRevision", mcplib.Description("Output only. Name of the latest revision that is serving traffic.")),
			mcplib.WithString("launchStage", mcplib.Description("The launch stage as defined by [Google Cloud Platform Launch Stages](https://cloud.")),
			mcplib.WithString("name-2", mcplib.Description("The fully qualified name of this Service.")),
			mcplib.WithString("observedGeneration", mcplib.Description("Output only. The generation of this Service currently serving traffic.")),
			mcplib.WithBoolean("reconciling", mcplib.Description("Output only. Returns true if the Service is currently being acted upon by the system to bring it into the desired state.")),
			mcplib.WithBoolean("satisfiesPzs", mcplib.Description("Output only. Reserved for future use.")),
			mcplib.WithString("template-annotations", mcplib.Description("KRM-style annotations for the resource. Cloud Run API v2 does not support annotations with `run.googleapis.com`, `cloud.")),
			mcplib.WithString("template-containers", mcplib.Description("Holds the single container that defines the unit of execution for this Revision.")),
			mcplib.WithString("template-encryption-key", mcplib.Description("A reference to a customer managed encryption key (CMEK) to use to encrypt this container image.")),
			mcplib.WithString("template-execution-environment", mcplib.Description("The sandbox environment to host this Revision.")),
			mcplib.WithString("template-labels", mcplib.Description("KRM-style labels for the resource. Cloud Run API v2 does not support labels with `run.googleapis.com`, `cloud.")),
			mcplib.WithNumber("template-max-instance-request-concurrency", mcplib.Description("Sets the maximum number of requests that each serving instance can receive.")),
			mcplib.WithString("template-revision", mcplib.Description("The unique name for the revision.")),
			mcplib.WithNumber("template-scaling-max-instance-count", mcplib.Description("Maximum number of serving instances that this resource should have.")),
			mcplib.WithNumber("template-scaling-min-instance-count", mcplib.Description("Minimum number of serving instances that this resource should have.")),
			mcplib.WithString("template-service-account", mcplib.Description("Email address of the IAM service account associated with the revision of the service.")),
			mcplib.WithString("template-timeout", mcplib.Description("Max allowed time for an instance to respond to a request.")),
			mcplib.WithString("template-volumes", mcplib.Description("A list of Volumes to make available to containers.")),
			mcplib.WithString("template-vpc-access-connector", mcplib.Description("VPC Access connector name.")),
			mcplib.WithString("template-vpc-access-egress", mcplib.Description("Traffic VPC egress settings.")),
			mcplib.WithString("terminal-condition-execution-reason", mcplib.Description("A reason for the execution condition.")),
			mcplib.WithString("terminal-condition-last-transition-time", mcplib.Description("Last time the condition transitioned from one status to another.")),
			mcplib.WithString("terminal-condition-message", mcplib.Description("Human readable message indicating details about the current status.")),
			mcplib.WithString("terminal-condition-reason", mcplib.Description("A common (service-level) reason for this condition.")),
			mcplib.WithString("terminal-condition-revision-reason", mcplib.Description("A reason for the revision condition.")),
			mcplib.WithString("terminal-condition-severity", mcplib.Description("How to interpret failures of this condition, one of Error, Warning, Info")),
			mcplib.WithString("terminal-condition-state", mcplib.Description("State of the condition.")),
			mcplib.WithString("terminal-condition-type", mcplib.Description("type is used to communicate the status of the reconciliation process. See also: https://github.")),
			mcplib.WithString("traffic", mcplib.Description("Specifies how to distribute traffic over a collection of Revisions belonging to the Service.")),
			mcplib.WithString("trafficStatuses", mcplib.Description("Output only. Detailed status information for corresponding traffic targets.")),
			mcplib.WithString("uid", mcplib.Description("Output only. Server assigned unique identifier for the trigger.")),
			mcplib.WithString("updateTime", mcplib.Description("Output only. The last-modified time.")),
			mcplib.WithString("uri", mcplib.Description("Output only. The main URI in which this Service is serving traffic.")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PATCH", "/v2/{name}", false, false, nil, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "allowMissing", WireName: "allowMissing", Location: "query"}, {PublicName: "validateOnly", WireName: "validateOnly", Location: "query"}, {PublicName: "annotations", WireName: "annotations", Location: "body"}, {PublicName: "binary-authorization-breakglass-justification", WireName: "breakglassJustification", Location: "body", BodyPath: []string{"binaryAuthorization", "breakglassJustification"}}, {PublicName: "binary-authorization-use-default", WireName: "useDefault", Location: "body", BodyPath: []string{"binaryAuthorization", "useDefault"}}, {PublicName: "client", WireName: "client", Location: "body"}, {PublicName: "clientVersion", WireName: "clientVersion", Location: "body"}, {PublicName: "conditions", WireName: "conditions", Location: "body"}, {PublicName: "createTime", WireName: "createTime", Location: "body"}, {PublicName: "creator", WireName: "creator", Location: "body"}, {PublicName: "deleteTime", WireName: "deleteTime", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "etag", WireName: "etag", Location: "body"}, {PublicName: "expireTime", WireName: "expireTime", Location: "body"}, {PublicName: "generation", WireName: "generation", Location: "body"}, {PublicName: "ingress", WireName: "ingress", Location: "body"}, {PublicName: "labels", WireName: "labels", Location: "body"}, {PublicName: "lastModifier", WireName: "lastModifier", Location: "body"}, {PublicName: "latestCreatedRevision", WireName: "latestCreatedRevision", Location: "body"}, {PublicName: "latestReadyRevision", WireName: "latestReadyRevision", Location: "body"}, {PublicName: "launchStage", WireName: "launchStage", Location: "body"}, {PublicName: "name-2", WireName: "name", Location: "body"}, {PublicName: "observedGeneration", WireName: "observedGeneration", Location: "body"}, {PublicName: "reconciling", WireName: "reconciling", Location: "body"}, {PublicName: "satisfiesPzs", WireName: "satisfiesPzs", Location: "body"}, {PublicName: "template-annotations", WireName: "annotations", Location: "body", BodyPath: []string{"template", "annotations"}}, {PublicName: "template-containers", WireName: "containers", Location: "body", BodyPath: []string{"template", "containers"}}, {PublicName: "template-encryption-key", WireName: "encryptionKey", Location: "body", BodyPath: []string{"template", "encryptionKey"}}, {PublicName: "template-execution-environment", WireName: "executionEnvironment", Location: "body", BodyPath: []string{"template", "executionEnvironment"}}, {PublicName: "template-labels", WireName: "labels", Location: "body", BodyPath: []string{"template", "labels"}}, {PublicName: "template-max-instance-request-concurrency", WireName: "maxInstanceRequestConcurrency", Location: "body", BodyPath: []string{"template", "maxInstanceRequestConcurrency"}}, {PublicName: "template-revision", WireName: "revision", Location: "body", BodyPath: []string{"template", "revision"}}, {PublicName: "template-scaling-max-instance-count", WireName: "maxInstanceCount", Location: "body", BodyPath: []string{"template", "scaling", "maxInstanceCount"}}, {PublicName: "template-scaling-min-instance-count", WireName: "minInstanceCount", Location: "body", BodyPath: []string{"template", "scaling", "minInstanceCount"}}, {PublicName: "template-service-account", WireName: "serviceAccount", Location: "body", BodyPath: []string{"template", "serviceAccount"}}, {PublicName: "template-timeout", WireName: "timeout", Location: "body", BodyPath: []string{"template", "timeout"}}, {PublicName: "template-volumes", WireName: "volumes", Location: "body", BodyPath: []string{"template", "volumes"}}, {PublicName: "template-vpc-access-connector", WireName: "connector", Location: "body", BodyPath: []string{"template", "vpcAccess", "connector"}}, {PublicName: "template-vpc-access-egress", WireName: "egress", Location: "body", BodyPath: []string{"template", "vpcAccess", "egress"}}, {PublicName: "terminal-condition-execution-reason", WireName: "executionReason", Location: "body", BodyPath: []string{"terminalCondition", "executionReason"}}, {PublicName: "terminal-condition-last-transition-time", WireName: "lastTransitionTime", Location: "body", BodyPath: []string{"terminalCondition", "lastTransitionTime"}}, {PublicName: "terminal-condition-message", WireName: "message", Location: "body", BodyPath: []string{"terminalCondition", "message"}}, {PublicName: "terminal-condition-reason", WireName: "reason", Location: "body", BodyPath: []string{"terminalCondition", "reason"}}, {PublicName: "terminal-condition-revision-reason", WireName: "revisionReason", Location: "body", BodyPath: []string{"terminalCondition", "revisionReason"}}, {PublicName: "terminal-condition-severity", WireName: "severity", Location: "body", BodyPath: []string{"terminalCondition", "severity"}}, {PublicName: "terminal-condition-state", WireName: "state", Location: "body", BodyPath: []string{"terminalCondition", "state"}}, {PublicName: "terminal-condition-type", WireName: "type", Location: "body", BodyPath: []string{"terminalCondition", "type"}}, {PublicName: "traffic", WireName: "traffic", Location: "body"}, {PublicName: "trafficStatuses", WireName: "trafficStatuses", Location: "body"}, {PublicName: "uid", WireName: "uid", Location: "body"}, {PublicName: "updateTime", WireName: "updateTime", Location: "body"}, {PublicName: "uri", WireName: "uri", Location: "body"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("services_set-iam-policy",
			mcplib.WithDescription("Replace the IAM policy on a Cloud Run Service. Required: resource (full service name: projects/{project}/locations/{region}/services/{service}). Key optional: policy-bindings (array of role/member pairs), policy-etag (optimistic lock), updateMask (limit which fields are overwritten). Returns the new policy. Warning: this overwrites all existing bindings — use services_get-iam-policy first to read the current policy before modifying."),
			mcplib.WithString("resource", mcplib.Required(), mcplib.Description("REQUIRED: The resource for which the policy is being specified. See [Resource names](https://cloud.google.")),
			mcplib.WithString("policy-audit-configs", mcplib.Description("Specifies cloud audit logging configuration for this policy.")),
			mcplib.WithString("policy-bindings", mcplib.Description("Associates a list of `members`, or principals, with a `role`.")),
			mcplib.WithString("policy-etag", mcplib.Description("`etag` is used for optimistic concurrency control as a way to help prevent simultaneous updates of a policy from")),
			mcplib.WithNumber("policy-version", mcplib.Description("Specifies the format of the policy. Valid values are `0`, `1`, and `3`.")),
			mcplib.WithString("updateMask", mcplib.Description("OPTIONAL: A FieldMask specifying which fields of the policy to modify. Only the fields in the mask will be modified.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/{resource}:setIamPolicy", false, false, nil, []mcpParamBinding{{PublicName: "resource", WireName: "resource", Location: "path"}, {PublicName: "policy-audit-configs", WireName: "auditConfigs", Location: "body", BodyPath: []string{"policy", "auditConfigs"}}, {PublicName: "policy-bindings", WireName: "bindings", Location: "body", BodyPath: []string{"policy", "bindings"}}, {PublicName: "policy-etag", WireName: "etag", Location: "body", BodyPath: []string{"policy", "etag"}}, {PublicName: "policy-version", WireName: "version", Location: "body", BodyPath: []string{"policy", "version"}}, {PublicName: "updateMask", WireName: "updateMask", Location: "body"}}, []string{"resource"}),
	)
	s.AddTool(
		mcplib.NewTool("services_test-iam-permissions",
			mcplib.WithDescription("Check which IAM permissions the caller holds on a Cloud Run Service, without modifying any state. Required: resource (full service name: projects/{project}/locations/{region}/services/{service}). Optional: permissions (list of permission strings to test, e.g. ['run.services.get', 'run.services.update']). Returns only the subset of requested permissions that the caller actually has."),
			mcplib.WithString("resource", mcplib.Required(), mcplib.Description("REQUIRED: The resource for which the policy detail is being requested. See [Resource names](https://cloud.google.")),
			mcplib.WithString("permissions", mcplib.Description("The set of permissions to check for the `resource`. Permissions with wildcards (such as `*` or `storage.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/{resource}:testIamPermissions", false, false, nil, []mcpParamBinding{{PublicName: "resource", WireName: "resource", Location: "path"}, {PublicName: "permissions", WireName: "permissions", Location: "body"}}, []string{"resource"}),
	)
	s.AddTool(
		mcplib.NewTool("services_revisions_delete",
			mcplib.WithDescription("Delete a specific Cloud Run Revision. Required: name (full revision name: projects/{project}/locations/{region}/services/{service}/revisions/{revision}). Optional: etag (optimistic concurrency), validateOnly (dry-run). Returns a long-running operation — use operations_wait to confirm. Revisions actively serving traffic cannot be deleted; use services_patch to shift traffic first. Destructive."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Required. The name of the Revision to delete.")),
			mcplib.WithString("etag", mcplib.Description("A system-generated fingerprint for this version of the resource.")),
			mcplib.WithBoolean("validateOnly", mcplib.Description("Indicates that the request should be validated without actually deleting any resources.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v2/{name}", false, false, nil, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}, {PublicName: "etag", WireName: "etag", Location: "query"}, {PublicName: "validateOnly", WireName: "validateOnly", Location: "query"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("services_revisions_get",
			mcplib.WithDescription("Retrieve detailed configuration and status for a specific Cloud Run Revision. Required: name (full revision name: projects/{project}/locations/{region}/services/{service}/revisions/{revision}). Returns container image, resource limits, env vars, creation time, and serving status. Use services_revisions_list to find valid revision names first."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Required. The full name of the Revision.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{name}", true, false, nil, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "path"}}, []string{"name"}),
	)
	s.AddTool(
		mcplib.NewTool("services_revisions_list",
			mcplib.WithDescription("List Revisions for a Cloud Run Service or location. Required: parent (service path: projects/{project}/locations/{region}/services/{service}, or location path for all revisions in a region). Optional: pageSize, pageToken, showDeleted. Returns revision names, images, creation times, and traffic allocation. Use services_revisions_get for full config on a specific revision."),
			mcplib.WithString("parent", mcplib.Required(), mcplib.Description("Required. The Service from which the Revisions should be listed.")),
			mcplib.WithNumber("pageSize", mcplib.Description("Maximum number of revisions to return in this call.")),
			mcplib.WithString("pageToken", mcplib.Description("A page token received from a previous call to ListRevisions. All other parameters must match.")),
			mcplib.WithBoolean("showDeleted", mcplib.Description("If true, returns deleted (but unexpired) resources along with active ones.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/{parent}/revisions", true, false, nil, []mcpParamBinding{{PublicName: "parent", WireName: "parent", Location: "path"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}, {PublicName: "showDeleted", WireName: "showDeleted", Location: "query"}}, []string{"parent"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
	BodyPath   []string
}

func setNestedBodyArg(body map[string]any, path []string, value any) {
	if len(path) == 0 {
		return
	}
	if len(path) == 1 {
		body[path[0]] = value
		return
	}
	current := body
	for _, key := range path[:len(path)-1] {
		next, ok := current[key].(map[string]any)
		if !ok {
			next = map[string]any{}
			current[key] = next
		}
		current = next
	}
	current[path[len(path)-1]] = value
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				if len(binding.BodyPath) > 0 {
					setNestedBodyArg(bodyArgs, binding.BodyPath, v)
				} else {
					bodyArgs[binding.WireName] = v
				}
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export GOOGLE_CLOUD_RUN_OAUTH2C=<your-key>" +
					"\n      See API docs: https://google.com" +
					"\n      Run 'google-cloud-run-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export GOOGLE_CLOUD_RUN_OAUTH2C=<your-key>" +
					"\n      See API docs: https://google.com" +
					"\n      Run 'google-cloud-run-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export GOOGLE_CLOUD_RUN_OAUTH2C=<your-key>" +
					"\n      See API docs: https://google.com" +
					"\n      Run 'google-cloud-run-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "google-cloud-run-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 60*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "google-cloud-run-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "google-cloud-run",
		"description": "Cloud Run Admin API for deploying and managing serverless container services, revisions, jobs, and executions",
		"archetype":   "communication",
		"tool_count":  16,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion google-cloud-run-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "GOOGLE_CLOUD_RUN_OAUTH2C",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
			"docs_url": "https://google.com",
		},
		"resources": []map[string]any{
			{
				"name":        "cloud-run-admin-jobs",
				"description": "Manage cloud run admin jobs",
				"endpoints":   []string{"create", "list", "run"},
				"searchable":  true,
			},
			{
				"name":        "executions",
				"description": "Manage executions",
				"endpoints":   []string{},
				"searchable":  true,
			},
			{
				"name":        "operations",
				"description": "Manage operations",
				"endpoints":   []string{"list", "wait"},
				"searchable":  true,
			},
			{
				"name":        "services",
				"description": "Manage services",
				"endpoints":   []string{"create", "get-iam-policy", "list", "patch", "set-iam-policy", "test-iam-permissions"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass pageToken parameter for subsequent pages.",
			"Control page size with the pageSize parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Cross-project service inventory", "command": "services list-all", "description": "List Cloud Run services across multiple GCP projects in a single unified table.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Zombie revision sweeper", "command": "revisions prune", "description": "Delete old revisions that are not serving any traffic, keeping only the N most recent.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Traffic split inspector", "command": "services traffic", "description": "Show the current traffic split across revisions with image tag and deploy timestamp in one table.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Execution outcome summary", "command": "executions summary", "description": "Show a per-execution summary of a Cloud Run Job with succeeded/failed task counts and duration.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Public exposure audit", "command": "iam audit", "description": "List all services in a project that are publicly accessible via allUsers or allAuthenticatedUsers IAM bindings.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Wait-for-traffic", "command": "revisions wait-traffic", "description": "Block until a specific revision reaches a target traffic percentage -- a CI/CD gate primitive.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Revision config diff", "command": "revisions diff", "description": "Show a field-by-field diff between two revisions: image, CPU/memory, scaling config, and env var key names.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "IAM policy diff", "command": "iam diff", "description": "Compare the current IAM policy of a service against a saved snapshot to detect added or removed bindings.", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Cross-project service inventory", "insight": ""},
			{"topic": "Zombie revision sweeper", "insight": ""},
			{"topic": "Traffic split inspector", "insight": ""},
			{"topic": "Execution outcome summary", "insight": ""},
			{"topic": "Public exposure audit", "insight": ""},
			{"topic": "Wait-for-traffic", "insight": ""},
			{"topic": "Revision config diff", "insight": ""},
			{"topic": "IAM policy diff", "insight": ""},
			{"topic": "Message search", "insight": "Use the search tool on synced data rather than paginating through message history. Message APIs often have aggressive rate limits."},
			{"topic": "Channel health", "insight": "When analyzing channel activity, use the channel-health command or sql aggregation on synced messages. Don't iterate individual messages via API."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
