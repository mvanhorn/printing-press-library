version: 2
project_name: browserbase-pp-cli
changelog:
  disable: true
builds:
  - id: browserbase-pp-cli
    main: ./cmd/browserbase-pp-cli
    binary: browserbase-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/cloud/browserbase/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: browserbase-pp-mcp
    main: ./cmd/browserbase-pp-mcp
    binary: browserbase-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: browserbase-pp-cli
    repository:
      owner: somsamantray
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Every Browserbase cloud feature, plus session lifecycle control, local history, and usage analytics no other tool has."
    install: |
      bin.install "browserbase-pp-cli"
      bin.install "browserbase-pp-mcp"
