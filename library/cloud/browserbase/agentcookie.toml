# agentcookie.toml: secrets-bus adoption manifest v2
# See docs/spec-agentcookie-secrets-bus-v2-adoption.md
schema_version = 2
name = "browserbase-pp-cli"
display_name = "Browserbase"
description = "Every Browserbase cloud feature, plus session lifecycle control, local history, and usage analytics no other tool has."
project_kind = "cli"

[secrets.file]
path = "~/.config/browserbase-pp-cli/config.toml"

[sync]
default = false

[sync.keys]
BROWSERBASE_API_KEY = true
