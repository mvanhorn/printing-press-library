// Copyright 2026 dstevens. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCompaniesRestoreQueuesCompaniesJobsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyExcludesJson string
	var bodyIncludesJson string
	var bodyName string
	var bodyResticrestorequeue string
	var bodyResticsnapshot int
	var bodyTarget string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "companies-jobs-create <company_pk> <resticrestorequeue_pk>",
		Short:       "Companies jobs create",
		Example:     "  servosity-pp-cli companies restore-queues companies-jobs-create example-value example-value",
		Annotations: map[string]string{"pp:endpoint": "restore-queues.companies-jobs-create", "pp:method": "POST", "pp:path": "/companies/{company_pk}/restore-queues/{resticrestorequeue_pk}/jobs/"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("resticsnapshot") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "resticsnapshot")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/companies/{company_pk}/restore-queues/{resticrestorequeue_pk}/jobs/"
			path = replacePathParam(path, "company_pk", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("resticrestorequeue_pk is required\nUsage: %s <%s>", cmd.CommandPath(), "resticrestorequeue_pk"))
			}
			path = replacePathParam(path, "resticrestorequeue_pk", args[1])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyExcludesJson != "" {
					body["excludes_json"] = bodyExcludesJson
				}
				if bodyIncludesJson != "" {
					body["includes_json"] = bodyIncludesJson
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyResticrestorequeue != "" {
					body["resticrestorequeue"] = bodyResticrestorequeue
				}
				if bodyResticsnapshot != 0 {
					body["resticsnapshot"] = bodyResticsnapshot
				}
				if bodyTarget != "" {
					body["target"] = bodyTarget
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "restore-queues",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyExcludesJson, "excludes-json", "", "Excludes json")
	cmd.Flags().StringVar(&bodyIncludesJson, "includes-json", "", "Includes json")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyResticrestorequeue, "resticrestorequeue", "", "Resticrestorequeue")
	cmd.Flags().IntVar(&bodyResticsnapshot, "resticsnapshot", 0, "Resticsnapshot")
	cmd.Flags().StringVar(&bodyTarget, "target", "", "Target")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
