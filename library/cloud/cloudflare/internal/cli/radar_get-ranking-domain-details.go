// Copyright 2026 alex-osti. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newRadarGetRankingDomainDetailsCmd(flags *rootFlags) *cobra.Command {
	var flagLimit int
	var flagRankingType string
	var flagName string
	var flagIncludeTopLocations bool
	var flagDate string
	var flagFormat string
	var flagAll bool

	cmd := &cobra.Command{
		Use:   "get-ranking-domain-details <domain>",
		Short: "Retrieves domain rank details. Cloudflare provides an ordered rank for the top 100 domains, but for the remainder it...",
		Example: "  cloudflare-pp-cli radar get-ranking-domain-details example-value",
		Annotations: map[string]string{"pp:endpoint": "radar.get-ranking-domain-details", "pp:method": "GET", "pp:path": "/radar/ranking/domain/{domain}", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("ranking-type") {
				allowedRankingType := []string{ "POPULAR", "TRENDING_RISE", "TRENDING_STEADY" }
				validRankingType := false
				for _, v := range allowedRankingType {
					if flagRankingType == v {
						validRankingType = true
						break
					}
				}
				if !validRankingType {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "ranking-type", flagRankingType, allowedRankingType)
				}
			}
			if cmd.Flags().Changed("format") {
				allowedFormat := []string{ "JSON", "CSV" }
				validFormat := false
				for _, v := range allowedFormat {
					if flagFormat == v {
						validFormat = true
						break
					}
				}
				if !validFormat {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "format", flagFormat, allowedFormat)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/radar/ranking/domain/{domain}"
			path = replacePathParam(path, "domain", args[0])
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "radar", path, map[string]string{
				"limit": fmt.Sprintf("%v", flagLimit),
				"rankingType": fmt.Sprintf("%v", flagRankingType),
				"name": fmt.Sprintf("%v", flagName),
				"includeTopLocations": fmt.Sprintf("%v", flagIncludeTopLocations),
				"date": fmt.Sprintf("%v", flagDate),
				"format": fmt.Sprintf("%v", flagFormat),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagLimit, "limit", 5, "Limits the number of objects returned in the response.")
	cmd.Flags().StringVar(&flagRankingType, "ranking-type", "POPULAR", "The ranking type. (one of: POPULAR, TRENDING_RISE, TRENDING_STEADY)")
	cmd.Flags().StringVar(&flagName, "name", "", "Array of names used to label the series in the response.")
	cmd.Flags().BoolVar(&flagIncludeTopLocations, "include-top-locations", false, "Includes top locations in the response.")
	cmd.Flags().StringVar(&flagDate, "date", "", "Filters results by the specified array of dates.")
	cmd.Flags().StringVar(&flagFormat, "format", "", "Format in which results will be returned. (one of: JSON, CSV)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
