// Copyright 2026 alex-osti. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newUserLoadBalancerHealthcheckEventsListHealthcheckEventsCmd(flags *rootFlags) *cobra.Command {
	var flagUntil string
	var flagPoolName string
	var flagOriginHealthy bool
	var flagPoolId string
	var flagSince string
	var flagOriginName string
	var flagPoolHealthy bool

	cmd := &cobra.Command{
		Use:   "load-balancer-healthcheck-events-list-healthcheck-events",
		Short: "List origin health changes.",
		Example: "  cloudflare-pp-cli user load-balancer-healthcheck-events-list-healthcheck-events",
		Annotations: map[string]string{"pp:endpoint": "user.load-balancer-healthcheck-events-list-healthcheck-events", "pp:method": "GET", "pp:path": "/user/load_balancing_analytics/events", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/user/load_balancing_analytics/events"
			params := map[string]string{}
			if flagUntil != "" {
				params["until"] = fmt.Sprintf("%v", flagUntil)
			}
			if flagPoolName != "" {
				params["pool_name"] = fmt.Sprintf("%v", flagPoolName)
			}
			if flagOriginHealthy != false {
				params["origin_healthy"] = fmt.Sprintf("%v", flagOriginHealthy)
			}
			if flagPoolId != "" {
				params["pool_id"] = fmt.Sprintf("%v", flagPoolId)
			}
			if flagSince != "" {
				params["since"] = fmt.Sprintf("%v", flagSince)
			}
			if flagOriginName != "" {
				params["origin_name"] = fmt.Sprintf("%v", flagOriginName)
			}
			if flagPoolHealthy != false {
				params["pool_healthy"] = fmt.Sprintf("%v", flagPoolHealthy)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "user", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagUntil, "until", "", "Until")
	cmd.Flags().StringVar(&flagPoolName, "pool-name", "", "Pool name")
	cmd.Flags().BoolVar(&flagOriginHealthy, "origin-healthy", true, "Origin healthy")
	cmd.Flags().StringVar(&flagPoolId, "pool-id", "", "Pool id")
	cmd.Flags().StringVar(&flagSince, "since", "", "Since")
	cmd.Flags().StringVar(&flagOriginName, "origin-name", "", "Origin name")
	cmd.Flags().BoolVar(&flagPoolHealthy, "pool-healthy", true, "Pool healthy")

	return cmd
}
