// Copyright 2026 alex-osti. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newOrganizationsListCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagName string
	var flagNameStartsWith string
	var flagNameEndsWith string
	var flagNameContains string
	var flagContainingAccount string
	var flagContainingUser string
	var flagContainingOrganization string
	var flagParentId string
	var flagPageToken string
	var flagPageSize int
	var flagAll bool

	cmd := &cobra.Command{
		Use:   "list",
		Short: "Retrieve a list of organizations a particular user has access to. (Currently in Closed Beta - see...",
		Example: "  cloudflare-pp-cli organizations list",
		Annotations: map[string]string{"pp:endpoint": "organizations.list", "pp:method": "GET", "pp:path": "/organizations", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/organizations"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "organizations", path, map[string]string{
				"id": fmt.Sprintf("%v", flagId),
				"name": fmt.Sprintf("%v", flagName),
				"name.startsWith": fmt.Sprintf("%v", flagNameStartsWith),
				"name.endsWith": fmt.Sprintf("%v", flagNameEndsWith),
				"name.contains": fmt.Sprintf("%v", flagNameContains),
				"containing.account": fmt.Sprintf("%v", flagContainingAccount),
				"containing.user": fmt.Sprintf("%v", flagContainingUser),
				"containing.organization": fmt.Sprintf("%v", flagContainingOrganization),
				"parent.id": fmt.Sprintf("%v", flagParentId),
				"page_token": fmt.Sprintf("%v", flagPageToken),
				"page_size": fmt.Sprintf("%v", flagPageSize),
			}, nil, flagAll, "page_token", "nextPageToken", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Only return organizations with the specified IDs (ex. id=foo&id=bar). Send multiple elements by repeating the query...")
	cmd.Flags().StringVar(&flagName, "name", "", "(case-sensitive) Filter the list of organizations to where the name is equal to a particular string.")
	cmd.Flags().StringVar(&flagNameStartsWith, "name-starts-with", "", "(case-insensitive) Filter the list of organizations to where the name starts with a particular string.")
	cmd.Flags().StringVar(&flagNameEndsWith, "name-ends-with", "", "(case-insensitive) Filter the list of organizations to where the name ends with a particular string.")
	cmd.Flags().StringVar(&flagNameContains, "name-contains", "", "(case-insensitive) Filter the list of organizations to where the name contains a particular string.")
	cmd.Flags().StringVar(&flagContainingAccount, "containing-account", "", "Filter the list of organizations to the ones that contain this particular account.")
	cmd.Flags().StringVar(&flagContainingUser, "containing-user", "", "Filter the list of organizations to the ones that contain this particular user. IMPORTANT: Just because an...")
	cmd.Flags().StringVar(&flagContainingOrganization, "containing-organization", "", "Filter the list of organizations to the ones that contain this particular organization.")
	cmd.Flags().StringVar(&flagParentId, "parent-id", "", "Filter the list of organizations to the ones that are a sub-organization of the specified organization. 'null' is a...")
	cmd.Flags().StringVar(&flagPageToken, "page-token", "", "An opaque token returned from the last list response that when provided will retrieve the next page. Parameters used...")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "The amount of items to return. Defaults to 10.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
