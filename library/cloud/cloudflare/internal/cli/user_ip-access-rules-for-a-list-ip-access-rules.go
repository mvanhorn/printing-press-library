// Copyright 2026 alex-osti. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newUserIpAccessRulesForAListIpAccessRulesCmd(flags *rootFlags) *cobra.Command {
	var flagMode string
	var flagConfigurationTarget string
	var flagConfigurationValue string
	var flagNotes string
	var flagMatch string
	var flagPage string
	var flagPerPage float64
	var flagOrder string
	var flagDirection string
	var flagAll bool

	cmd := &cobra.Command{
		Use:   "ip-access-rules-for-a-list-ip-access-rules",
		Short: "Fetches IP Access rules of the user. You can filter the results using several optional parameters.",
		Example: "  cloudflare-pp-cli user ip-access-rules-for-a-list-ip-access-rules",
		Annotations: map[string]string{"pp:endpoint": "user.ip-access-rules-for-a-list-ip-access-rules", "pp:method": "GET", "pp:path": "/user/firewall/access_rules/rules", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("mode") {
				allowedMode := []string{ "block", "challenge", "whitelist", "js_challenge", "managed_challenge" }
				validMode := false
				for _, v := range allowedMode {
					if flagMode == v {
						validMode = true
						break
					}
				}
				if !validMode {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "mode", flagMode, allowedMode)
				}
			}
			if cmd.Flags().Changed("configuration-target") {
				allowedConfigurationTarget := []string{ "ip", "ip_range", "asn", "country" }
				validConfigurationTarget := false
				for _, v := range allowedConfigurationTarget {
					if flagConfigurationTarget == v {
						validConfigurationTarget = true
						break
					}
				}
				if !validConfigurationTarget {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "configuration-target", flagConfigurationTarget, allowedConfigurationTarget)
				}
			}
			if cmd.Flags().Changed("match") {
				allowedMatch := []string{ "any", "all" }
				validMatch := false
				for _, v := range allowedMatch {
					if flagMatch == v {
						validMatch = true
						break
					}
				}
				if !validMatch {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "match", flagMatch, allowedMatch)
				}
			}
			if cmd.Flags().Changed("order") {
				allowedOrder := []string{ "configuration.target", "configuration.value", "mode" }
				validOrder := false
				for _, v := range allowedOrder {
					if flagOrder == v {
						validOrder = true
						break
					}
				}
				if !validOrder {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "order", flagOrder, allowedOrder)
				}
			}
			if cmd.Flags().Changed("direction") {
				allowedDirection := []string{ "asc", "desc" }
				validDirection := false
				for _, v := range allowedDirection {
					if flagDirection == v {
						validDirection = true
						break
					}
				}
				if !validDirection {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "direction", flagDirection, allowedDirection)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/user/firewall/access_rules/rules"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "user", path, map[string]string{
				"mode": fmt.Sprintf("%v", flagMode),
				"configuration.target": fmt.Sprintf("%v", flagConfigurationTarget),
				"configuration.value": fmt.Sprintf("%v", flagConfigurationValue),
				"notes": fmt.Sprintf("%v", flagNotes),
				"match": fmt.Sprintf("%v", flagMatch),
				"page": fmt.Sprintf("%v", flagPage),
				"per_page": fmt.Sprintf("%v", flagPerPage),
				"order": fmt.Sprintf("%v", flagOrder),
				"direction": fmt.Sprintf("%v", flagDirection),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagMode, "mode", "", "Mode (one of: block, challenge, whitelist, js_challenge, managed_challenge)")
	cmd.Flags().StringVar(&flagConfigurationTarget, "configuration-target", "", "Configuration target (one of: ip, ip_range, asn, country)")
	cmd.Flags().StringVar(&flagConfigurationValue, "configuration-value", "", "Configuration value")
	cmd.Flags().StringVar(&flagNotes, "notes", "", "Notes")
	cmd.Flags().StringVar(&flagMatch, "match", "all", "Match (one of: any, all)")
	cmd.Flags().StringVar(&flagPage, "page", "", "Page")
	cmd.Flags().Float64Var(&flagPerPage, "per-page", 0.0, "Per page")
	cmd.Flags().StringVar(&flagOrder, "order", "", "Order (one of: configuration.target, configuration.value, mode)")
	cmd.Flags().StringVar(&flagDirection, "direction", "", "Direction (one of: asc, desc)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
