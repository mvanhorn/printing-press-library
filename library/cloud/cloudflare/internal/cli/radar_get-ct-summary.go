// Copyright 2026 alex-osti. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newRadarGetCtSummaryCmd(flags *rootFlags) *cobra.Command {
	var flagDimension string
	var flagName string
	var flagDateRange string
	var flagDateStart string
	var flagDateEnd string
	var flagLimitPerGroup int
	var flagCa string
	var flagCaOwner string
	var flagDuration string
	var flagEntryType string
	var flagExpirationStatus string
	var flagHasIps string
	var flagHasWildcards string
	var flagLog string
	var flagLogApi string
	var flagLogOperator string
	var flagPublicKeyAlgorithm string
	var flagSignatureAlgorithm string
	var flagTld string
	var flagValidationLevel string
	var flagUniqueEntries string
	var flagNormalization string
	var flagFormat string

	cmd := &cobra.Command{
		Use:   "get-ct-summary",
		Short: "Retrieves an aggregated summary of certificates grouped by the specified dimension.",
		Example: "  cloudflare-pp-cli radar get-ct-summary",
		Annotations: map[string]string{"pp:endpoint": "radar.get-ct-summary", "pp:method": "GET", "pp:path": "/radar/ct/summary/{dimension}", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("dimension") {
				allowedDimension := []string{ "CA", "CA_OWNER", "DURATION", "ENTRY_TYPE", "EXPIRATION_STATUS", "HAS_IPS", "HAS_WILDCARDS", "LOG", "LOG_API", "LOG_OPERATOR", "PUBLIC_KEY_ALGORITHM", "SIGNATURE_ALGORITHM", "TLD", "VALIDATION_LEVEL" }
				validDimension := false
				for _, v := range allowedDimension {
					if flagDimension == v {
						validDimension = true
						break
					}
				}
				if !validDimension {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "dimension", flagDimension, allowedDimension)
				}
			}
			if cmd.Flags().Changed("normalization") {
				allowedNormalization := []string{ "RAW_VALUES", "PERCENTAGE" }
				validNormalization := false
				for _, v := range allowedNormalization {
					if flagNormalization == v {
						validNormalization = true
						break
					}
				}
				if !validNormalization {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "normalization", flagNormalization, allowedNormalization)
				}
			}
			if cmd.Flags().Changed("format") {
				allowedFormat := []string{ "JSON", "CSV" }
				validFormat := false
				for _, v := range allowedFormat {
					if flagFormat == v {
						validFormat = true
						break
					}
				}
				if !validFormat {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "format", flagFormat, allowedFormat)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/radar/ct/summary/{dimension}"
			path = replacePathParam(path, "dimension", fmt.Sprintf("%v", flagDimension))
			params := map[string]string{}
			if flagName != "" {
				params["name"] = fmt.Sprintf("%v", flagName)
			}
			if flagDateRange != "" {
				params["dateRange"] = fmt.Sprintf("%v", flagDateRange)
			}
			if flagDateStart != "" {
				params["dateStart"] = fmt.Sprintf("%v", flagDateStart)
			}
			if flagDateEnd != "" {
				params["dateEnd"] = fmt.Sprintf("%v", flagDateEnd)
			}
			if flagLimitPerGroup != 0 {
				params["limitPerGroup"] = fmt.Sprintf("%v", flagLimitPerGroup)
			}
			if flagCa != "" {
				params["ca"] = fmt.Sprintf("%v", flagCa)
			}
			if flagCaOwner != "" {
				params["caOwner"] = fmt.Sprintf("%v", flagCaOwner)
			}
			if flagDuration != "" {
				params["duration"] = fmt.Sprintf("%v", flagDuration)
			}
			if flagEntryType != "" {
				params["entryType"] = fmt.Sprintf("%v", flagEntryType)
			}
			if flagExpirationStatus != "" {
				params["expirationStatus"] = fmt.Sprintf("%v", flagExpirationStatus)
			}
			if flagHasIps != "" {
				params["hasIps"] = fmt.Sprintf("%v", flagHasIps)
			}
			if flagHasWildcards != "" {
				params["hasWildcards"] = fmt.Sprintf("%v", flagHasWildcards)
			}
			if flagLog != "" {
				params["log"] = fmt.Sprintf("%v", flagLog)
			}
			if flagLogApi != "" {
				params["logApi"] = fmt.Sprintf("%v", flagLogApi)
			}
			if flagLogOperator != "" {
				params["logOperator"] = fmt.Sprintf("%v", flagLogOperator)
			}
			if flagPublicKeyAlgorithm != "" {
				params["publicKeyAlgorithm"] = fmt.Sprintf("%v", flagPublicKeyAlgorithm)
			}
			if flagSignatureAlgorithm != "" {
				params["signatureAlgorithm"] = fmt.Sprintf("%v", flagSignatureAlgorithm)
			}
			if flagTld != "" {
				params["tld"] = fmt.Sprintf("%v", flagTld)
			}
			if flagValidationLevel != "" {
				params["validationLevel"] = fmt.Sprintf("%v", flagValidationLevel)
			}
			if flagUniqueEntries != "" {
				params["uniqueEntries"] = fmt.Sprintf("%v", flagUniqueEntries)
			}
			if flagNormalization != "" {
				params["normalization"] = fmt.Sprintf("%v", flagNormalization)
			}
			if flagFormat != "" {
				params["format"] = fmt.Sprintf("%v", flagFormat)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "radar", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDimension, "dimension", "CA", "Specifies the certificate attribute by which to group the results. (one of: CA, CA_OWNER, DURATION, ENTRY_TYPE, EXPIRATION_STATUS, HAS_IPS, HAS_WILDCARDS, LOG, LOG_API, LOG_OPERATOR, PUBLIC_KEY_ALGORITHM, SIGNATURE_ALGORITHM, TLD, VALIDATION_LEVEL)")
	cmd.Flags().StringVar(&flagName, "name", "", "Array of names used to label the series in the response.")
	cmd.Flags().StringVar(&flagDateRange, "date-range", "", "Filters results by date range. For example, use `7d` and `7dcontrol` to compare this week with the previous week....")
	cmd.Flags().StringVar(&flagDateStart, "date-start", "", "Start of the date range.")
	cmd.Flags().StringVar(&flagDateEnd, "date-end", "", "End of the date range (inclusive).")
	cmd.Flags().IntVar(&flagLimitPerGroup, "limit-per-group", 0, "Limits the number of objects per group to the top items within the specified time range. When item count exceeds the...")
	cmd.Flags().StringVar(&flagCa, "ca", "", "Filters results by certificate authority.")
	cmd.Flags().StringVar(&flagCaOwner, "ca-owner", "", "Filters results by certificate authority owner.")
	cmd.Flags().StringVar(&flagDuration, "duration", "", "Filters results by certificate duration.")
	cmd.Flags().StringVar(&flagEntryType, "entry-type", "", "Filters results by entry type (certificate vs. pre-certificate).")
	cmd.Flags().StringVar(&flagExpirationStatus, "expiration-status", "", "Filters results by expiration status (expired vs. valid).")
	cmd.Flags().StringVar(&flagHasIps, "has-ips", "", "Filters results based on whether the certificates are bound to specific IP addresses.")
	cmd.Flags().StringVar(&flagHasWildcards, "has-wildcards", "", "Filters results based on whether the certificates contain wildcard domains.")
	cmd.Flags().StringVar(&flagLog, "log", "", "Filters results by certificate log.")
	cmd.Flags().StringVar(&flagLogApi, "log-api", "", "Filters results by certificate log API (RFC6962 vs. static).")
	cmd.Flags().StringVar(&flagLogOperator, "log-operator", "", "Filters results by certificate log operator.")
	cmd.Flags().StringVar(&flagPublicKeyAlgorithm, "public-key-algorithm", "", "Filters results by public key algorithm.")
	cmd.Flags().StringVar(&flagSignatureAlgorithm, "signature-algorithm", "", "Filters results by signature algorithm.")
	cmd.Flags().StringVar(&flagTld, "tld", "", "Filters results by top-level domain.")
	cmd.Flags().StringVar(&flagValidationLevel, "validation-level", "", "Filters results by validation level.")
	cmd.Flags().StringVar(&flagUniqueEntries, "unique-entries", "", "Specifies whether to filter out duplicate certificates and pre-certificates. Set to true for unique entries only.")
	cmd.Flags().StringVar(&flagNormalization, "normalization", "RAW_VALUES", "Normalization method applied to the results. Refer to [Normalization... (one of: RAW_VALUES, PERCENTAGE)")
	cmd.Flags().StringVar(&flagFormat, "format", "", "Format in which results will be returned. (one of: JSON, CSV)")

	return cmd
}
