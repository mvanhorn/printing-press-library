// Copyright 2026 alex-osti. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newUserIpAccessRulesForACreateAnIpAccessRuleCmd(flags *rootFlags) *cobra.Command {
	var bodyConfiguration string
	var bodyMode string
	var bodyNotes string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "ip-access-rules-for-a-create-an-ip-access-rule",
		Short: "Creates a new IP Access rule for all zones owned by the current user. Note: To create an IP Access rule that applies...",
		Example: "  cloudflare-pp-cli user ip-access-rules-for-a-create-an-ip-access-rule --mode example-value",
		Annotations: map[string]string{"pp:endpoint": "user.ip-access-rules-for-a-create-an-ip-access-rule", "pp:method": "POST", "pp:path": "/user/firewall/access_rules/rules"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("configuration") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "configuration")
				}
				if !cmd.Flags().Changed("mode") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "mode")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/user/firewall/access_rules/rules"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyConfiguration != "" {
					var parsedConfiguration any
					if err := json.Unmarshal([]byte(bodyConfiguration), &parsedConfiguration); err != nil {
						return fmt.Errorf("parsing --configuration JSON: %w", err)
					}
					body["configuration"] = parsedConfiguration
				}
				if bodyMode != "" {
					body["mode"] = bodyMode
				}
				if bodyNotes != "" {
					body["notes"] = bodyNotes
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "user",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyConfiguration, "configuration", "", "The rule configuration.")
	cmd.Flags().StringVar(&bodyMode, "mode", "", "The action to apply to a matched request.")
	cmd.Flags().StringVar(&bodyNotes, "notes", "", "An informative summary of the rule, typically used as a reminder or explanation.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
