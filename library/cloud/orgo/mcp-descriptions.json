{
  "descriptions": {
    "projects_list": "List all workspaces (projects) for the authenticated user. No parameters. Returns an array of workspaces with id, name, status (active/inactive), icon_url, user_id, and timestamps. Use this to discover workspace IDs before scoping computer or file operations; pair with projects_get-by-name when you only have a workspace name."
  }
}
