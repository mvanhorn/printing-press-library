{
  "schema_version": 1,
  "applied_at": "2026-05-13",
  "base_run_id": "20260513-023951",
  "base_printing_press_version": "4.2.2",
  "patches": [
    {
      "id": "model-dedup-events",
      "summary": "Dedupe model events via MIN(aircraft_idx) subquery so multi-aircraft events surface once.",
      "reason": "The original join over event_aircraft produced one row per matching aircraft, double-counting events like a mid-air between two Cessna 172s in both ModelCounts and the returned Events slice.",
      "files": ["internal/cli/model.go"]
    },
    {
      "id": "model-true-totals",
      "summary": "Compute ModelCounts via a separate COUNT(DISTINCT) pass without LIMIT.",
      "reason": "Totals were accumulated by iterating the LIMIT window, so users saw 'NTSB events: 200 total' for popular models with thousands of matches.",
      "files": ["internal/cli/model.go"]
    },
    {
      "id": "ingest-http-timeout",
      "summary": "Use a 30-minute Timeout client for FAA/NTSB downloads instead of http.DefaultClient.",
      "reason": "http.DefaultClient has Timeout=0; a stalled FAA/NTSB origin could hang the sync indefinitely while pulling ~80–90 MB bodies.",
      "files": ["internal/ingest/http.go"]
    },
    {
      "id": "store-migrate-early-return",
      "summary": "Skip the migration write transaction when PRAGMA user_version already equals StoreSchemaVersion.",
      "reason": "Every Open was acquiring the SQLite write lock and re-executing all schemaV1 DDL plus a user_version stamp, blocking concurrent readers for no schema change.",
      "files": ["internal/store/store.go"]
    }
  ]
}
