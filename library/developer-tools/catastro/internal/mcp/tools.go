// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	"github.com/mvanhorn/printing-press-library/library/developer-tools/catastro/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/catastro/internal/client"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/catastro/internal/config"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/catastro/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/catastro/internal/store"
	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("municipalities_list",
			mcplib.WithDescription("Lista los municipios de una provincia (devuelve nombre + código INE). Required: provincia. Optional: filter. Returns array of Municipality."),
			mcplib.WithString("provincia", mcplib.Required(), mcplib.Description("Nombre de la provincia (ej: MADRID, BARCELONA)")),
			mcplib.WithString("filter", mcplib.Description("Filtro parcial por nombre de municipio (opcional)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/COVCCallejero.svc/json/ObtenerMunicipios", false, []mcpParamBinding{{PublicName: "provincia", WireName: "Provincia", Location: "query"}, {PublicName: "filter", WireName: "Municipio", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("numbers_list",
			mcplib.WithDescription("Lista los números de portal disponibles en una vía. Required: provincia, municipio, tipo, via. Optional: numero. Returns array of StreetNumber."),
			mcplib.WithString("provincia", mcplib.Required(), mcplib.Description("Nombre de la provincia")),
			mcplib.WithString("municipio", mcplib.Required(), mcplib.Description("Nombre del municipio")),
			mcplib.WithString("tipo", mcplib.Required(), mcplib.Description("Sigla de la vía (CL, AV, PZ, etc.)")),
			mcplib.WithString("via", mcplib.Required(), mcplib.Description("Nombre exacto de la vía")),
			mcplib.WithString("numero", mcplib.Description("Filtro por número específico (opcional)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/COVCCallejero.svc/json/ObtenerNumerero", false, []mcpParamBinding{{PublicName: "provincia", WireName: "Provincia", Location: "query"}, {PublicName: "municipio", WireName: "Municipio", Location: "query"}, {PublicName: "tipo", WireName: "TipoVia", Location: "query"}, {PublicName: "via", WireName: "NombreVia", Location: "query"}, {PublicName: "numero", WireName: "Numero", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("property_by-address",
			mcplib.WithDescription("Datos no protegidos de una finca urbana por dirección (DNPLOC). Devuelve referencia catastral + datos de propiedad. Required: provincia, municipio, calle, numero. Optional: sigla, bloque, escalera (plus 2 more). Returns the PropertyByAddress."),
			mcplib.WithString("provincia", mcplib.Required(), mcplib.Description("Nombre de la provincia (ej: MADRID)")),
			mcplib.WithString("municipio", mcplib.Required(), mcplib.Description("Nombre del municipio (ej: MADRID)")),
			mcplib.WithString("sigla", mcplib.Description("Sigla de la vía (CL=calle, AV=avenida, PZ=plaza, etc.)")),
			mcplib.WithString("calle", mcplib.Required(), mcplib.Description("Nombre de la vía (ej: ALCALA)")),
			mcplib.WithString("numero", mcplib.Required(), mcplib.Description("Número de portal (ej: 1)")),
			mcplib.WithString("bloque", mcplib.Description("Bloque dentro del portal (opcional)")),
			mcplib.WithString("escalera", mcplib.Description("Escalera dentro del bloque (opcional)")),
			mcplib.WithString("planta", mcplib.Description("Planta (opcional, ej: 03)")),
			mcplib.WithString("puerta", mcplib.Description("Puerta dentro de la planta (opcional)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/COVCCallejero.svc/json/Consulta_DNPLOC", false, []mcpParamBinding{{PublicName: "provincia", WireName: "Provincia", Location: "query"}, {PublicName: "municipio", WireName: "Municipio", Location: "query"}, {PublicName: "sigla", WireName: "Sigla", Location: "query"}, {PublicName: "calle", WireName: "Calle", Location: "query"}, {PublicName: "numero", WireName: "Numero", Location: "query"}, {PublicName: "bloque", WireName: "Bloque", Location: "query"}, {PublicName: "escalera", WireName: "Escalera", Location: "query"}, {PublicName: "planta", WireName: "Planta", Location: "query"}, {PublicName: "puerta", WireName: "Puerta", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("property_rural",
			mcplib.WithDescription("Datos no protegidos de finca rústica por polígono + parcela (DNPPP). Required: provincia, municipio, poligono, parcela. Returns the PropertyByPlot."),
			mcplib.WithString("provincia", mcplib.Required(), mcplib.Description("Nombre de la provincia")),
			mcplib.WithString("municipio", mcplib.Required(), mcplib.Description("Nombre del municipio")),
			mcplib.WithString("poligono", mcplib.Required(), mcplib.Description("Número de polígono catastral")),
			mcplib.WithString("parcela", mcplib.Required(), mcplib.Description("Número de parcela dentro del polígono")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/COVCCallejero.svc/json/Consulta_DNPPP", false, []mcpParamBinding{{PublicName: "provincia", WireName: "Provincia", Location: "query"}, {PublicName: "municipio", WireName: "Municipio", Location: "query"}, {PublicName: "poligono", WireName: "Poligono", Location: "query"}, {PublicName: "parcela", WireName: "Parcela", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("property_show",
			mcplib.WithDescription("Datos no protegidos por referencia catastral (DNPRC). Devuelve la finca completa con sus sub-parts. Required: rc. Optional: provincia, municipio. Returns the PropertyByRC."),
			mcplib.WithString("provincia", mcplib.Description("Nombre de provincia (opcional si la RC tiene 20 dígitos)")),
			mcplib.WithString("municipio", mcplib.Description("Nombre de municipio (opcional si la RC tiene 20 dígitos)")),
			mcplib.WithString("rc", mcplib.Required(), mcplib.Description("Referencia catastral (14, 18 o 20 caracteres)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/COVCCallejero.svc/json/Consulta_DNPRC", false, []mcpParamBinding{{PublicName: "provincia", WireName: "Provincia", Location: "query"}, {PublicName: "municipio", WireName: "Municipio", Location: "query"}, {PublicName: "rc", WireName: "RC", Location: "query"}}, []string{"RC"}),
	)
	s.AddTool(
		mcplib.NewTool("provinces_list",
			mcplib.WithDescription("Lista las 48 provincias donde el Catastro tiene competencia (excluye País Vasco y Navarra). Returns array of Province."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/COVCCallejero.svc/json/ObtenerProvincias", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("streets_list",
			mcplib.WithDescription("Lista las vías de un municipio (calle, plaza, avenida, etc.) con sus códigos. Required: provincia, municipio. Optional: tipo, nombre. Returns array of Street."),
			mcplib.WithString("provincia", mcplib.Required(), mcplib.Description("Nombre de la provincia")),
			mcplib.WithString("municipio", mcplib.Required(), mcplib.Description("Nombre del municipio")),
			mcplib.WithString("tipo", mcplib.Description("Filtro por sigla de vía (CL=calle, AV=avenida, PZ=plaza, etc.)")),
			mcplib.WithString("nombre", mcplib.Description("Filtro parcial por nombre de vía")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/COVCCallejero.svc/json/ObtenerCallejero", false, []mcpParamBinding{{PublicName: "provincia", WireName: "Provincia", Location: "query"}, {PublicName: "municipio", WireName: "Municipio", Location: "query"}, {PublicName: "tipo", WireName: "TipoVia", Location: "query"}, {PublicName: "nombre", WireName: "NombreVia", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, binaryResponse bool, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if binaryResponse {
			headers = map[string]string{client.BinaryResponseHeader: "true"}
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if binaryResponse {
				data, err = c.GetWithHeaders(path, params, headers)
				break
			}
			data, err = c.Get(path, params)
		case "POST":
			if binaryResponse {
				data, _, err = c.PostWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PostWithParams(path, params, bodyArgs)
		case "PUT":
			if binaryResponse {
				data, _, err = c.PutWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(path, params, bodyArgs)
		case "PATCH":
			if binaryResponse {
				data, _, err = c.PatchWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(path, params, bodyArgs)
		case "DELETE":
			if binaryResponse {
				data, _, err = c.DeleteWithParamsAndHeaders(path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + msg +
					"\nhint: check your API credentials." +
					"\n      Run 'catastro-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + msg +
					"\nhint: this API is configured without credentials; the service may be blocking the request by rate limit, geography, bot protection, or endpoint policy." +
					"\n      Run 'catastro-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "catastro-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "catastro-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "catastro",
		"description": "Every Catastro feature, plus offline SQLite, bulk RC enrichment, polygon export and change detection no other...",
		"archetype":   "generic",
		"tool_count":  7,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion catastro-pp-cli binary.",
		"resources": []map[string]any{
			{
				"name":        "municipalities",
				"description": "Listado de municipios por provincia",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "numbers",
				"description": "Numeración de portales en una vía catastral",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "property",
				"description": "Consulta de propiedades catastrales urbanas y rústicas",
				"endpoints":   []string{"by-address", "rural", "show"},
				"searchable":  true,
			},
			{
				"name":        "provinces",
				"description": "Listado oficial de provincias del Catastro español",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "streets",
				"description": "Callejero oficial del Catastro por municipio",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Reconcile local parcel table against Catastro", "command": "reconcile", "description": "Compara una tabla local (CSV/SQLite) de parcelas contra Catastro y reporta drift (superficie, uso, año, dirección)...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Pipe-friendly bulk enrichment", "command": "enrich", "description": "Lee RCs o direcciones por stdin y emite JSONL con la property card completa de cada uno, con cache en disco y...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Detect parcel changes between syncs", "command": "watch", "description": "Re-sincroniza un municipio y reporta deltas (superficie, uso, año, dirección) por RC respecto al último sync.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Polygon-clipped parcel export", "command": "export parcels", "description": "Exporta las parcelas dentro de un polígono arbitrario (GeoJSON) a GeoPackage/GeoJSON/SHP, reproyectando al EPSG pedido.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Expediente bundle for one RC", "command": "report", "description": "Dada una RC, compone un directorio (data.json, neighbors.geojson, map.png, parts.csv) con la parcela, sus vecinas,...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Nearest-neighbor parcels for an RC", "command": "neighbors", "description": "Para una RC, devuelve las parcelas vecinas dentro del radio dado (típico 50 m) con sus datos DNPRC ya unidos.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "FTS over local store", "command": "search", "description": "Búsqueda full-text sobre la tabla local de parcels (rc, address, use) y streets (nombre, municipio), con filtros...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Stale local data planning", "command": "stale", "description": "Lista RCs/municipios cuyo último sync supera la ventana --older-than, ordenados por antigüedad.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Area aggregate analysis", "command": "analyze-area", "description": "Para un polígono, devuelve número de parcelas, hectáreas totales, histograma de usos, año medio de construcción...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Local sync coverage overview", "command": "coverage", "description": "Resumen del estado local: por provincia/municipio, qué hay sincronizado y desde cuándo, con conteos por tipo...", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Reconcile local parcel table against Catastro", "insight": ""},
			{"topic": "Pipe-friendly bulk enrichment", "insight": ""},
			{"topic": "Detect parcel changes between syncs", "insight": ""},
			{"topic": "Polygon-clipped parcel export", "insight": ""},
			{"topic": "Expediente bundle for one RC", "insight": ""},
			{"topic": "Nearest-neighbor parcels for an RC", "insight": ""},
			{"topic": "FTS over local store", "insight": ""},
			{"topic": "Stale local data planning", "insight": ""},
			{"topic": "Area aggregate analysis", "insight": ""},
			{"topic": "Local sync coverage overview", "insight": ""},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
