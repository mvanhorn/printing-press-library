// Copyright 2026 mani. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCrawlPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyUrl string
	var bodyMaxDepth int
	var bodyMaxBreadth int
	var bodyLimit int
	var bodyInstructions string
	var bodyChunksPerSource int
	var bodyExtractDepth string
	var bodyFormat string
	var bodySelectPaths string
	var bodyExcludePaths string
	var bodySelectDomains string
	var bodyExcludeDomains string
	var bodyAllowExternal bool
	var bodyIncludeImages bool
	var bodyIncludeFavicon bool
	var bodyTimeout int
	var bodyIncludeUsage bool

	cmd := &cobra.Command{
		Use:         "crawl",
		Short:       "Systematically crawl a website using BFS, extracting content from each page",
		Long:        "Shortcut for 'crawl crawl'. Systematically crawl a website using BFS, extracting content from each page",
		Example:     "  tavily-pp-cli crawl --url https://example.com/resource",
		Annotations: map[string]string{"pp:endpoint": "crawl.crawl", "pp:method": "POST", "pp:path": "/crawl"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("url") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "url")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/crawl"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyUrl != "" {
				body["url"] = bodyUrl
			}
			if bodyMaxDepth != 0 {
				body["max_depth"] = bodyMaxDepth
			}
			if bodyMaxBreadth != 0 {
				body["max_breadth"] = bodyMaxBreadth
			}
			if bodyLimit != 0 {
				body["limit"] = bodyLimit
			}
			if bodyInstructions != "" {
				body["instructions"] = bodyInstructions
			}
			if bodyChunksPerSource != 0 {
				body["chunks_per_source"] = bodyChunksPerSource
			}
			if bodyExtractDepth != "" {
				body["extract_depth"] = bodyExtractDepth
			}
			if bodyFormat != "" {
				body["format"] = bodyFormat
			}
			if bodySelectPaths != "" {
				var parsedSelectPaths any
				if err := json.Unmarshal([]byte(bodySelectPaths), &parsedSelectPaths); err != nil {
					return fmt.Errorf("parsing --select-paths JSON: %w", err)
				}
				body["select_paths"] = parsedSelectPaths
			}
			if bodyExcludePaths != "" {
				var parsedExcludePaths any
				if err := json.Unmarshal([]byte(bodyExcludePaths), &parsedExcludePaths); err != nil {
					return fmt.Errorf("parsing --exclude-paths JSON: %w", err)
				}
				body["exclude_paths"] = parsedExcludePaths
			}
			if bodySelectDomains != "" {
				var parsedSelectDomains any
				if err := json.Unmarshal([]byte(bodySelectDomains), &parsedSelectDomains); err != nil {
					return fmt.Errorf("parsing --select-domains JSON: %w", err)
				}
				body["select_domains"] = parsedSelectDomains
			}
			if bodyExcludeDomains != "" {
				var parsedExcludeDomains any
				if err := json.Unmarshal([]byte(bodyExcludeDomains), &parsedExcludeDomains); err != nil {
					return fmt.Errorf("parsing --exclude-domains JSON: %w", err)
				}
				body["exclude_domains"] = parsedExcludeDomains
			}
			if bodyAllowExternal != false {
				body["allow_external"] = bodyAllowExternal
			}
			if bodyIncludeImages != false {
				body["include_images"] = bodyIncludeImages
			}
			if bodyIncludeFavicon != false {
				body["include_favicon"] = bodyIncludeFavicon
			}
			if bodyTimeout != 0 {
				body["timeout"] = bodyTimeout
			}
			if bodyIncludeUsage != false {
				body["include_usage"] = bodyIncludeUsage
			}
			data, _, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyUrl, "url", "", "Root URL to start crawling from")
	cmd.Flags().IntVar(&bodyMaxDepth, "max-depth", 0, "BFS max depth (1-5, default 1)")
	cmd.Flags().IntVar(&bodyMaxBreadth, "max-breadth", 0, "Max URLs per depth level (1-500, default 20)")
	cmd.Flags().IntVar(&bodyLimit, "limit", 0, "Total page limit (default 50)")
	cmd.Flags().StringVar(&bodyInstructions, "instructions", "", "Natural language guidance for which pages to include")
	cmd.Flags().IntVar(&bodyChunksPerSource, "chunks-per-source", 0, "Chunks per source (1-5, default 3)")
	cmd.Flags().StringVar(&bodyExtractDepth, "extract-depth", "", "Extraction depth: basic, advanced")
	cmd.Flags().StringVar(&bodyFormat, "format", "", "Output format: markdown, text")
	cmd.Flags().StringVar(&bodySelectPaths, "select-paths", "", "Regex patterns for URL path inclusion")
	cmd.Flags().StringVar(&bodyExcludePaths, "exclude-paths", "", "Regex patterns for URL path exclusion")
	cmd.Flags().StringVar(&bodySelectDomains, "select-domains", "", "Regex patterns for domain inclusion")
	cmd.Flags().StringVar(&bodyExcludeDomains, "exclude-domains", "", "Regex patterns for domain exclusion")
	cmd.Flags().BoolVar(&bodyAllowExternal, "allow-external", false, "Allow following links to external domains")
	cmd.Flags().BoolVar(&bodyIncludeImages, "include-images", false, "Include images in extracted pages")
	cmd.Flags().BoolVar(&bodyIncludeFavicon, "include-favicon", false, "Include favicon URLs")
	cmd.Flags().IntVar(&bodyTimeout, "timeout", 0, "Request timeout in seconds (10-150, default 150)")
	cmd.Flags().BoolVar(&bodyIncludeUsage, "include-usage", false, "Include credit usage stats in response")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
