// Copyright 2026 Kerry Morrison and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/developer-tools/webflow/internal/cliutil"
	"github.com/spf13/cobra"
)

func newSitesProductsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyProductFieldDataCategory string
	var bodyProductFieldDataDefaultSku string
	var bodyProductFieldDataDescription string
	var bodyProductFieldDataEcProductType string
	var bodyProductFieldDataName string
	var bodyProductFieldDataShippable bool
	var bodyProductFieldDataSkuProperties string
	var bodyProductFieldDataSlug string
	var bodyProductFieldDataTaxCategory string
	var bodyPublishStatus string
	var bodySkuFieldDataEcSkuBillingMethod string
	var bodySkuFieldDataMainImage string
	var bodySkuFieldDataName string
	var bodySkuFieldDataSku string
	var bodySkuFieldDataSkuProperties string
	var bodySkuFieldDataSkuValues string
	var bodySkuFieldDataSlug string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create <site_id>",
		Short:       "Create a new ecommerce product and defaultSKU. A product, at minimum, must have a single SKU.",
		Example:     "  webflow-pp-cli sites products create 580e63e98c9a982ac9b8b741",
		Annotations: map[string]string{"pp:endpoint": "products.create", "pp:method": "POST", "pp:path": "/sites/{site_id}/products"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <site_id>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <site_id>"))
			}
			if !stdinBody {
				if bodySkuFieldDataEcSkuBillingMethod != "" || bodySkuFieldDataMainImage != "" || bodySkuFieldDataName != "" || bodySkuFieldDataSku != "" || bodySkuFieldDataSkuProperties != "" || bodySkuFieldDataSkuValues != "" || bodySkuFieldDataSlug != "" {
					if !cmd.Flags().Changed("sku-field-data-name") && !flags.dryRun {
						return fmt.Errorf("required flag \"%s\" not set", "sku-field-data-name")
					}
					if !cmd.Flags().Changed("sku-field-data-slug") && !flags.dryRun {
						return fmt.Errorf("required flag \"%s\" not set", "sku-field-data-slug")
					}
				}
			}
			path := "/sites/{site_id}/products"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("site_id is required\nUsage: %s <%s>", cmd.CommandPath(), "site_id"))
			}
			path = replacePathParam(path, "site_id", args[0])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				{
					nestedProduct := map[string]any{}
					{
						nestedProductFieldData := map[string]any{}
						if cmd.Flags().Changed("product-field-data-category") {
							parsedProductFieldDataCategory, parseErr := cliutil.ParseStringList(bodyProductFieldDataCategory)
							if parseErr != nil {
								return fmt.Errorf("parsing --product-field-data-category list: %w", parseErr)
							}
							nestedProductFieldData["category"] = parsedProductFieldDataCategory
						}
						if bodyProductFieldDataDefaultSku != "" {
							nestedProductFieldData["default-sku"] = bodyProductFieldDataDefaultSku
						}
						if bodyProductFieldDataDescription != "" {
							nestedProductFieldData["description"] = bodyProductFieldDataDescription
						}
						if bodyProductFieldDataEcProductType != "" {
							nestedProductFieldData["ec-product-type"] = bodyProductFieldDataEcProductType
						}
						if bodyProductFieldDataName != "" {
							nestedProductFieldData["name"] = bodyProductFieldDataName
						}
						if cmd.Flags().Changed("product-field-data-shippable") {
							nestedProductFieldData["shippable"] = bodyProductFieldDataShippable
						}
						if bodyProductFieldDataSkuProperties != "" {
							var parsedProductFieldDataSkuProperties any
							if err := json.Unmarshal([]byte(bodyProductFieldDataSkuProperties), &parsedProductFieldDataSkuProperties); err != nil {
								return fmt.Errorf("parsing --product-field-data-sku-properties JSON: %w", err)
							}
							asArray, ok := parsedProductFieldDataSkuProperties.([]any)
							if !ok {
								return fmt.Errorf("--product-field-data-sku-properties must be a JSON array, got JSON %T", parsedProductFieldDataSkuProperties)
							}
							nestedProductFieldData["sku-properties"] = asArray
						}
						if bodyProductFieldDataSlug != "" {
							nestedProductFieldData["slug"] = bodyProductFieldDataSlug
						}
						if bodyProductFieldDataTaxCategory != "" {
							nestedProductFieldData["tax-category"] = bodyProductFieldDataTaxCategory
						}
						if len(nestedProductFieldData) > 0 {
							nestedProduct["fieldData"] = nestedProductFieldData
						}
					}
					if len(nestedProduct) > 0 {
						bodyMap["product"] = nestedProduct
					}
				}
				if bodyPublishStatus != "" {
					bodyMap["publishStatus"] = bodyPublishStatus
				}
				{
					nestedSku := map[string]any{}
					{
						nestedSkuFieldData := map[string]any{}
						if bodySkuFieldDataEcSkuBillingMethod != "" {
							var parsedSkuFieldDataEcSkuBillingMethod any
							if err := json.Unmarshal([]byte(bodySkuFieldDataEcSkuBillingMethod), &parsedSkuFieldDataEcSkuBillingMethod); err != nil {
								return fmt.Errorf("parsing --sku-field-data-ec-sku-billing-method JSON: %w", err)
							}
							nestedSkuFieldData["ec-sku-billing-method"] = bodySkuFieldDataEcSkuBillingMethod
						}
						if bodySkuFieldDataMainImage != "" {
							nestedSkuFieldData["main-image"] = bodySkuFieldDataMainImage
						}
						if bodySkuFieldDataName != "" {
							nestedSkuFieldData["name"] = bodySkuFieldDataName
						}
						if bodySkuFieldDataSku != "" {
							nestedSkuFieldData["sku"] = bodySkuFieldDataSku
						}
						if bodySkuFieldDataSkuProperties != "" {
							var parsedSkuFieldDataSkuProperties any
							if err := json.Unmarshal([]byte(bodySkuFieldDataSkuProperties), &parsedSkuFieldDataSkuProperties); err != nil {
								return fmt.Errorf("parsing --sku-field-data-sku-properties JSON: %w", err)
							}
							asArray, ok := parsedSkuFieldDataSkuProperties.([]any)
							if !ok {
								return fmt.Errorf("--sku-field-data-sku-properties must be a JSON array, got JSON %T", parsedSkuFieldDataSkuProperties)
							}
							nestedSkuFieldData["sku-properties"] = asArray
						}
						if bodySkuFieldDataSkuValues != "" {
							var parsedSkuFieldDataSkuValues any
							if err := json.Unmarshal([]byte(bodySkuFieldDataSkuValues), &parsedSkuFieldDataSkuValues); err != nil {
								return fmt.Errorf("parsing --sku-field-data-sku-values JSON: %w", err)
							}
							asMap, ok := parsedSkuFieldDataSkuValues.(map[string]any)
							if !ok {
								return fmt.Errorf("--sku-field-data-sku-values must be a JSON object, got JSON %T", parsedSkuFieldDataSkuValues)
							}
							nestedSkuFieldData["sku-values"] = asMap
						}
						if bodySkuFieldDataSlug != "" {
							nestedSkuFieldData["slug"] = bodySkuFieldDataSlug
						}
						if len(nestedSkuFieldData) > 0 {
							nestedSku["fieldData"] = nestedSkuFieldData
						}
					}
					if len(nestedSku) > 0 {
						bodyMap["sku"] = nestedSku
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "products", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "products", data, "skus")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "products", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "products", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "products", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "products",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "products", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "products", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyProductFieldDataCategory, "product-field-data-category", "", "The category your product belongs to.")
	cmd.Flags().StringVar(&bodyProductFieldDataDefaultSku, "product-field-data-default-sku", "", "The default SKU associated with this product.")
	cmd.Flags().StringVar(&bodyProductFieldDataDescription, "product-field-data-description", "", "A description of your product")
	cmd.Flags().StringVar(&bodyProductFieldDataEcProductType, "product-field-data-ec-product-type", "", "Product types. Enums reflect the following values in order: Physical, Digital, Service, Advanced'")
	cmd.Flags().StringVar(&bodyProductFieldDataName, "product-field-data-name", "", "Name of the Product")
	cmd.Flags().BoolVar(&bodyProductFieldDataShippable, "product-field-data-shippable", false, "Boolean determining if the Product is shippable")
	cmd.Flags().StringVar(&bodyProductFieldDataSkuProperties, "product-field-data-sku-properties", "", "Variant types to include in SKUs")
	cmd.Flags().StringVar(&bodyProductFieldDataSlug, "product-field-data-slug", "", "URL structure of the Product in your site.")
	cmd.Flags().StringVar(&bodyProductFieldDataTaxCategory, "product-field-data-tax-category", "", "Product tax class")
	cmd.Flags().StringVar(&bodyPublishStatus, "publish-status", "staging", "Indicate whether your Product should be set as 'staging' or 'live'")
	cmd.Flags().StringVar(&bodySkuFieldDataEcSkuBillingMethod, "sku-field-data-ec-sku-billing-method", "", "[Billing method](https://help.webflow.")
	cmd.Flags().StringVar(&bodySkuFieldDataMainImage, "sku-field-data-main-image", "", "The URL for the main image of the SKU")
	cmd.Flags().StringVar(&bodySkuFieldDataName, "sku-field-data-name", "", "Name of the Product")
	cmd.Flags().StringVar(&bodySkuFieldDataSku, "sku-field-data-sku", "", "A unique identifier for the SKU")
	cmd.Flags().StringVar(&bodySkuFieldDataSkuProperties, "sku-field-data-sku-properties", "", "The properties of the SKU")
	cmd.Flags().StringVar(&bodySkuFieldDataSkuValues, "sku-field-data-sku-values", "", "A mapping between SKU properties and their values, represented as key-value pairs.")
	cmd.Flags().StringVar(&bodySkuFieldDataSlug, "sku-field-data-slug", "", "URL structure of the Product in your site.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin (use this for deeply nested fields not exposed as flags)")

	return cmd
}
