# Phase 4.8 — Agentic SKILL Review

Run: 20260728-073020-15799c7c

## Findings and disposition

| # | Check | Severity | Finding | Disposition |
|---|---|---|---|---|
| 1 | 4 — stub/gated disclosure | error | All seven novel commands read the local SQLite mirror and return empty until `sync` runs. SKILL never said so at a decision point. | **Fixed** at source: `narrative.value_prop` and `narrative.when_to_use` in `research.json` now name all seven commands and state `sync --full` is a prerequisite. Regenerated. |
| 2 | 4 — misleading offline framing | error | "Offline-friendly — sync/search commands can use the local SQLite store when available" framed the mirror as optional. | **Fixed** at source with the same edit. The value prop now says "without it they return empty." |
| 3 | 3 — `overview` scope overstated | error | SKILL claimed "across your whole workspace"; the binary says "across every site in the local mirror." | **Fixed** at source: the `overview` novel-feature description and `why_it_matters` now say "one row per site you have synced locally." |
| 4 | 5 — doctor does not report scopes | error | `auth_narrative` said "Run `doctor` to see which scopes your token carries." Verified directly: `doctor` reports Config, Auth, Env Vars, Verify Mode, API reachability, Cache, and Paths — no scope probe. | **Fixed** at source: the narrative now says `doctor` reports whether auth is configured and the API is reachable but does not enumerate scopes, and points scope debugging at `token introspect` and the 403 body. |
| 5 | 5 — OAuth path unmentioned | warning | `doctor`'s own hint is `webflow-pp-cli login`, but the auth narrative covered only the bearer token. | **Fixed** at source: `login` and `auth status` are now named alongside `WEBFLOW_API_TOKEN` and `auth set-token`. |
| 6 | 6 — "pending redirects" claim | warning | `publish preview` emits `redirectRules`, a static count of rules in place, not pending redirects. | **Fixed** at source: the `publish preview` description now says "the site's redirect-rule count." |
| 7 | 6 — `--dry-run` / `--apply` conflation | warning | `items bulk-set` previews by default; `--dry-run` is the unrelated global "show request without sending." | **Fixed** at source: the description now says it "previews the change set by default; --apply writes it." |
| 8 | 5 — `auth set-token` does not exist | error (claimed) | Reviewer said the real subcommands are only `auth setup\|login\|logout\|status`. | **Rejected — reviewer was wrong.** `auth set-token --help` exits 0; the command exists and is simply hidden from the parent's `Available Commands` listing. No change made. Worth noting that a hidden command is hard to discover, but the SKILL reference is accurate. |
| 9 | 4 — `sync` undiscoverable | warning | `sync` does not appear in the SKILL's Command Reference, so an agent hitting empty results cannot find the remedy there. | **Template-shape, filed for retro.** The Command Reference is generated from spec resources only; framework commands (`sync`, `search`, `sql`, `doctor`) are structurally excluded. Mitigated in-run: the corrected narrative names `sync --full` in both the value prop and the when-to-use block, and every novel command's `--help` and empty-state message names it. |
| 10 | 1 — truncated Command Reference | warning | The `**workspaces**` heading renders with zero commands beneath it. `sites pages list` and `sites redirects` are absent though trigger phrases promise "list my Webflow pages" and "check my Webflow redirects." | **Template-shape, filed for retro.** The generator emits a resource heading even when no command rows survive for it, and the reference lists only a subset of the 164-leaf tree. Hand-editing the printed SKILL.md would be reverted by the next regen. The commands themselves exist and resolve — this is a documentation-completeness gap, not a capability gap. |

## Passed clean

- **Check 2 — verified-set alignment.** The SKILL's "Unique Capabilities" block exactly matches all seven entries in `novel_features_built`. No planned-only feature leaked through.
- **Check 7 — marketing-copy smell.** Concrete language throughout; no "comprehensive", "seamless", or "powerful" filler.

## Retro candidates (generator, not this CLI)

1. The SKILL Command Reference emits a resource heading with no command rows beneath it (`**workspaces** — Manage workspaces`).
2. Framework commands (`sync`, `search`, `sql`, `doctor`) are absent from the SKILL Command Reference even when the CLI's value proposition depends on them.
