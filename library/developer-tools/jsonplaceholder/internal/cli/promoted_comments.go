// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCommentsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagBody string
	var flagEmail string
	var flagId string
	var flagName string
	var flagPostId string

	cmd := &cobra.Command{
		Use:   "comments",
		Short: "GET /comments",
		Long:  "Shortcut for 'comments list_comments'. GET /comments",
		Example: "  jsonplaceholder-pp-cli comments",
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("body") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "body")
			}
			if !cmd.Flags().Changed("email") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "email")
			}
			if !cmd.Flags().Changed("id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "id")
			}
			if !cmd.Flags().Changed("name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "name")
			}
			if !cmd.Flags().Changed("post-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "post-id")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/comments"
			params := map[string]string{}
			if flagBody != "" {
				params["body"] = fmt.Sprintf("%v", flagBody)
			}
			if flagEmail != "" {
				params["email"] = fmt.Sprintf("%v", flagEmail)
			}
			if flagId != "" {
				params["id"] = fmt.Sprintf("%v", flagId)
			}
			if flagName != "" {
				params["name"] = fmt.Sprintf("%v", flagName)
			}
			if flagPostId != "" {
				params["postId"] = fmt.Sprintf("%v", flagPostId)
			}
			data, prov, err := resolveRead(c, flags, "comments", false, path, params)
			if err != nil {
				return classifyAPIError(err)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagBody, "body", "", "")
	cmd.Flags().StringVar(&flagEmail, "email", "", "")
	cmd.Flags().StringVar(&flagId, "id", "", "")
	cmd.Flags().StringVar(&flagName, "name", "", "")
	cmd.Flags().StringVar(&flagPostId, "post-id", "", "")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
