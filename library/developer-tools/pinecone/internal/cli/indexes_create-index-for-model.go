// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newIndexesCreateIndexForModelCmd(flags *rootFlags) *cobra.Command {
	var bodyCloud string
	var bodyDeletionProtection string
	var bodyEmbedDimension int
	var bodyEmbedFieldMap string
	var bodyEmbedMetric string
	var bodyEmbedModel string
	var bodyEmbedReadParameters string
	var bodyEmbedWriteParameters string
	var bodyName string
	var bodyReadCapacity string
	var bodyRegion string
	var bodySchemaFields string
	var bodyTags string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-index-for-model",
		Short:       "Create an index with integrated embedding. With this type of index, you provide source text, and Pinecone uses a...",
		Example:     "  pinecone-pp-cli indexes create-index-for-model --cloud example-value",
		Annotations: map[string]string{"pp:endpoint": "indexes.create-index-for-model", "pp:method": "POST", "pp:path": "/indexes/create-for-model"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("cloud") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "cloud")
				}
				if !cmd.Flags().Changed("embed-field-map") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "embed-field-map")
				}
				if !cmd.Flags().Changed("embed-model") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "embed-model")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("region") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "region")
				}
				if !cmd.Flags().Changed("schema-fields") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "schema-fields")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/indexes/create-for-model"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCloud != "" {
					body["cloud"] = bodyCloud
				}
				if bodyDeletionProtection != "" {
					body["deletion_protection"] = bodyDeletionProtection
				}
				{
					nestedEmbed := map[string]any{}
					if bodyEmbedDimension != 0 {
						nestedEmbed["dimension"] = bodyEmbedDimension
					}
					if bodyEmbedFieldMap != "" {
						var parsedEmbedFieldMap any
						if err := json.Unmarshal([]byte(bodyEmbedFieldMap), &parsedEmbedFieldMap); err != nil {
							return fmt.Errorf("parsing --embed-field-map JSON: %w", err)
						}
						nestedEmbed["field_map"] = parsedEmbedFieldMap
					}
					if bodyEmbedMetric != "" {
						nestedEmbed["metric"] = bodyEmbedMetric
					}
					if bodyEmbedModel != "" {
						nestedEmbed["model"] = bodyEmbedModel
					}
					if bodyEmbedReadParameters != "" {
						var parsedEmbedReadParameters any
						if err := json.Unmarshal([]byte(bodyEmbedReadParameters), &parsedEmbedReadParameters); err != nil {
							return fmt.Errorf("parsing --embed-read-parameters JSON: %w", err)
						}
						nestedEmbed["read_parameters"] = parsedEmbedReadParameters
					}
					if bodyEmbedWriteParameters != "" {
						var parsedEmbedWriteParameters any
						if err := json.Unmarshal([]byte(bodyEmbedWriteParameters), &parsedEmbedWriteParameters); err != nil {
							return fmt.Errorf("parsing --embed-write-parameters JSON: %w", err)
						}
						nestedEmbed["write_parameters"] = parsedEmbedWriteParameters
					}
					if len(nestedEmbed) > 0 {
						body["embed"] = nestedEmbed
					}
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyReadCapacity != "" {
					body["read_capacity"] = bodyReadCapacity
				}
				if bodyRegion != "" {
					body["region"] = bodyRegion
				}
				{
					nestedSchema := map[string]any{}
					if bodySchemaFields != "" {
						var parsedSchemaFields any
						if err := json.Unmarshal([]byte(bodySchemaFields), &parsedSchemaFields); err != nil {
							return fmt.Errorf("parsing --schema-fields JSON: %w", err)
						}
						nestedSchema["fields"] = parsedSchemaFields
					}
					if len(nestedSchema) > 0 {
						body["schema"] = nestedSchema
					}
				}
				if bodyTags != "" {
					var parsedTags any
					if err := json.Unmarshal([]byte(bodyTags), &parsedTags); err != nil {
						return fmt.Errorf("parsing --tags JSON: %w", err)
					}
					body["tags"] = parsedTags
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "indexes",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCloud, "cloud", "", "The public cloud where you would like your index hosted. Possible values: `gcp`, `aws`, or `azure`.")
	cmd.Flags().StringVar(&bodyDeletionProtection, "deletion-protection", "disabled", "Whether [deletion protection](http://docs.pinecone.io/guides/manage-data/manage-indexes#configure-deletion-protection...")
	cmd.Flags().IntVar(&bodyEmbedDimension, "embed-dimension", 0, "The dimension of embedding vectors produced for the index.")
	cmd.Flags().StringVar(&bodyEmbedFieldMap, "embed-field-map", "", "Identifies the name of the text field from your document model that will be embedded.")
	cmd.Flags().StringVar(&bodyEmbedMetric, "embed-metric", "", "The distance metric to be used for similarity search. You can use 'euclidean', 'cosine', or 'dotproduct'. If not...")
	cmd.Flags().StringVar(&bodyEmbedModel, "embed-model", "", "The name of the embedding model to use for the index.")
	cmd.Flags().StringVar(&bodyEmbedReadParameters, "embed-read-parameters", "", "The read parameters for the embedding model.")
	cmd.Flags().StringVar(&bodyEmbedWriteParameters, "embed-write-parameters", "", "The write parameters for the embedding model.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the index. Resource name must be 1-45 characters long, start and end with an alphanumeric character, and...")
	cmd.Flags().StringVar(&bodyReadCapacity, "read-capacity", "", "By default the index will be created with read capacity mode `OnDemand`. If you prefer to allocate dedicated read...")
	cmd.Flags().StringVar(&bodyRegion, "region", "", "The region where you would like your index to be created.")
	cmd.Flags().StringVar(&bodySchemaFields, "schema-fields", "", "A map of metadata field names to their configuration. The field name must be a valid metadata field name. The field...")
	cmd.Flags().StringVar(&bodyTags, "tags", "", "Custom user tags added to an index. Keys must be 80 characters or less. Values must be 120 characters or less. Keys...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
