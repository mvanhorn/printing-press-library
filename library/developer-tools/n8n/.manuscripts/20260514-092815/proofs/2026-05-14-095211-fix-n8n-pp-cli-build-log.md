# n8n-pp-cli Build Log

## What was built

### Priority 0 (generator — data layer)
- SQLite store: workflows, executions, credentials, tags, variables, projects, folders tables
- FTS5 index via resources_fts virtual table
- sync command wiring all resources

### Priority 1 (absorbed — all API endpoints)
- Generated by the Printing Press: all 47 API paths (~85 operations)
- workflows: create, delete, get, list, update, activate, deactivate, archive, unarchive, tags, transfer
- credentials: create, delete, get, get-id, get-schema, update, test, transfer
- executions: create, delete, get, list, retry, stop, tags
- tags: create, delete, get, list, update
- variables: create, delete, list, update
- projects: create, delete, list, update, folders, users
- data-tables: full CRUD + columns + rows
- community-packages: list, install, update, uninstall
- users: create, delete, get, list, role
- audit, insights, source-control promoted commands

### Priority 2 (transcendence — 11 novel features)
1. **workflows stale** — SQLite query, workflows not updated in N days (store-query shape)
2. **workflows node-inventory** — Aggregate node type usage across all workflows with workflow listing
3. **workflows deps** — executeWorkflow node dependency map; bidirectional caller/callee
4. **workflows bulk** — Tag/name-filtered bulk activate/deactivate/archive with --dry-run
5. **credentials audit** — Cross-reference credentials vs workflow nodes; unused credential detection
6. **executions export** — Structured export from local store with workflow/status/time filters
7. **executions wait** — Polling CI gate: blocks until terminal state, typed exit codes (0/1/2)
8. **executions rate-check** — Runaway trigger detection: rate/min per workflow in a time window
9. **diff** — Cross-instance workflow inventory diff (missing, active_mismatch, name_mismatch)
10. **health compare** — Side-by-side instance health metrics (workflow totals, reachability)
11. **variables diff** — Cross-instance variable drift (missing_on_target/source, value_mismatch)

## Auth fix applied
- All N8N_PUBLIC_BEARER_AUTH → N8N_API_KEY
- Auth sends X-N8N-API-KEY header (not Authorization: Bearer) via Config.Headers map
- Module path fixed: n8n-public-pp-cli → n8n-pp-cli (102 files)

## Intentionally deferred
- No items deferred from the approved manifest

## go build + go vet
- EXIT:0 on all packages
- No warnings

## Novel features: test coverage
- All 11 commands: --help shows realistic examples ✓
- Store-backed commands: --dry-run exits 0 ✓
- API-backed commands: --dry-run short-circuits with JSON output ✓
- executions wait: typed exit codes (0 success, 1 error/canceled, 2 timeout) ✓
