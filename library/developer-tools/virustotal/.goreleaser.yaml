version: 2
project_name: virustotal-pp-cli
changelog:
  disable: true
builds:
  - id: virustotal-pp-cli
    main: ./cmd/virustotal-pp-cli
    binary: virustotal-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X virustotal-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: virustotal-pp-mcp
    main: ./cmd/virustotal-pp-mcp
    binary: virustotal-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: virustotal-pp-cli
    repository:
      owner: user
      name: homebrew-tap
    homepage: "https://github.com/user/virustotal-pp-cli"
    description: "Malware analysis and threat intelligence platform"
    install: |
      bin.install "virustotal-pp-cli"
      bin.install "virustotal-pp-mcp"
