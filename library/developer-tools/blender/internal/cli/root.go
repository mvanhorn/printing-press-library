// Copyright 2026 markimus. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

var version = "1.0.0"

// Execute runs the CLI.
func Execute() error {
	rootCmd := &cobra.Command{
		Use:          "blender-pp-cli",
		Short:        "Agent-native Blender automation for CAD, rendering, animation, and MCP-assisted tandem sessions",
		SilenceUsage: true,
		Version:      version,
	}
	rootCmd.SetVersionTemplate("blender-pp-cli {{ .Version }}\n")
	rootCmd.AddCommand(newAssetCmd())
	rootCmd.AddCommand(newHandoffCmd())
	rootCmd.AddCommand(newMcpCmd())
	rootCmd.AddCommand(newRenderCmd())
	rootCmd.AddCommand(newResolveCmd())
	rootCmd.AddCommand(newSceneCmd())
	rootCmd.AddCommand(newScriptCmd())
	rootCmd.AddCommand(newDoctorCmd())
	rootCmd.AddCommand(newAgentContextCmd(rootCmd))
	rootCmd.AddCommand(newVersionCliCmd())

	return rootCmd.Execute()
}

// ExitCode extracts exit code from an error (always 1 for now).
func ExitCode(err error) int {
	return 1
}

func newVersionCliCmd() *cobra.Command {
	return &cobra.Command{
		Use:   "version",
		Short: "Print version",
		Run: func(cmd *cobra.Command, args []string) {
			fmt.Printf("blender-pp-cli %s\n", version)
		},
	}
}

