// Copyright 2026 james-frewin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newWebmastersSearchanalyticsQueryCmd(flags *rootFlags) *cobra.Command {
	var bodyAggregationType string
	var bodyDataState string
	var bodyDimensionFilterGroups string
	var bodyDimensions string
	var bodyEndDate string
	var bodyRowLimit int
	var bodySearchType string
	var bodyStartDate string
	var bodyStartRow int
	var bodyType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "searchanalytics-query <siteUrl>",
		Aliases:     []string{"create"},
		Short:       "Query your data with filters and parameters that you define. Returns zero or more rows grouped by the row keys that...",
		Example:     "  google-search-console-pp-cli webmasters searchanalytics-query https://example.com/resource",
		Annotations: map[string]string{"pp:endpoint": "webmasters.searchanalytics-query", "pp:method": "POST", "pp:path": "/webmasters/v3/sites/{siteUrl}/searchAnalytics/query"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/webmasters/v3/sites/{siteUrl}/searchAnalytics/query"
			path = replacePathParam(path, "siteUrl", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAggregationType != "" {
					var parsedAggregationType any
					if err := json.Unmarshal([]byte(bodyAggregationType), &parsedAggregationType); err != nil {
						return fmt.Errorf("parsing --aggregation-type JSON: %w", err)
					}
					body["aggregationType"] = bodyAggregationType
				}
				if bodyDataState != "" {
					body["dataState"] = bodyDataState
				}
				if bodyDimensionFilterGroups != "" {
					var parsedDimensionFilterGroups any
					if err := json.Unmarshal([]byte(bodyDimensionFilterGroups), &parsedDimensionFilterGroups); err != nil {
						return fmt.Errorf("parsing --dimension-filter-groups JSON: %w", err)
					}
					body["dimensionFilterGroups"] = parsedDimensionFilterGroups
				}
				if bodyDimensions != "" {
					var parsedDimensions any
					if err := json.Unmarshal([]byte(bodyDimensions), &parsedDimensions); err != nil {
						return fmt.Errorf("parsing --dimensions JSON: %w", err)
					}
					body["dimensions"] = parsedDimensions
				}
				if bodyEndDate != "" {
					var parsedEndDate any
					if err := json.Unmarshal([]byte(bodyEndDate), &parsedEndDate); err != nil {
						return fmt.Errorf("parsing --end-date JSON: %w", err)
					}
					body["endDate"] = bodyEndDate
				}
				if bodyRowLimit != 0 {
					body["rowLimit"] = bodyRowLimit
				}
				if bodySearchType != "" {
					var parsedSearchType any
					if err := json.Unmarshal([]byte(bodySearchType), &parsedSearchType); err != nil {
						return fmt.Errorf("parsing --search-type JSON: %w", err)
					}
					body["searchType"] = bodySearchType
				}
				if bodyStartDate != "" {
					var parsedStartDate any
					if err := json.Unmarshal([]byte(bodyStartDate), &parsedStartDate); err != nil {
						return fmt.Errorf("parsing --start-date JSON: %w", err)
					}
					body["startDate"] = bodyStartDate
				}
				if bodyStartRow != 0 {
					body["startRow"] = bodyStartRow
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "webmasters",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAggregationType, "aggregation-type", "", "[Optional; Default is 'auto'] How data is aggregated. If aggregated by property, all data for the same property is...")
	cmd.Flags().StringVar(&bodyDataState, "data-state", "", "The data state to be fetched, can be full or all, the latter including full and partial data.")
	cmd.Flags().StringVar(&bodyDimensionFilterGroups, "dimension-filter-groups", "", "[Optional] Zero or more filters to apply to the dimension grouping values; for example, 'query contains 'buy'' to...")
	cmd.Flags().StringVar(&bodyDimensions, "dimensions", "", "[Optional] Zero or more dimensions to group results by. Dimensions are the group-by values in the Search Analytics...")
	cmd.Flags().StringVar(&bodyEndDate, "end-date", "", "[Required] End date of the requested date range, in YYYY-MM-DD format, in PST (UTC - 8:00). Must be greater than or...")
	cmd.Flags().IntVar(&bodyRowLimit, "row-limit", 0, "[Optional; Default is 1000] The maximum number of rows to return. Must be a number from 1 to 25,000 (inclusive).")
	cmd.Flags().StringVar(&bodySearchType, "search-type", "", "[Optional; Default is 'web'] The search type to filter for.")
	cmd.Flags().StringVar(&bodyStartDate, "start-date", "", "[Required] Start date of the requested date range, in YYYY-MM-DD format, in PST time (UTC - 8:00). Must be less than...")
	cmd.Flags().IntVar(&bodyStartRow, "start-row", 0, "[Optional; Default is 0] Zero-based index of the first row in the response. Must be a non-negative number.")
	cmd.Flags().StringVar(&bodyType, "type", "", "Optional. [Optional; Default is 'web'] Type of report: search type, or either Discover or Gnews.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
