{
  "manifest_version": "0.3",
  "name": "bls-pp-mcp",
  "display_name": "BLS",
  "version": "4.5.1",
  "description": "Fetch U.S. labor and economic time-series from BLS with offline series search, a curated macro snapshot, the release calendar, and footnote decoding.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/bls-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/bls-pp-mcp",
      "args": [],
      "env": {
        "BLS_API_KEY": "${user_config.bls_api_key}"
      }
    }
  },
  "user_config": {
    "bls_api_key": {
      "type": "string",
      "title": "BLS_API_KEY",
      "description": "Optional. Sets BLS_API_KEY for the BLS MCP server. Get a credential from https://data.bls.gov/registrationEngine/.",
      "sensitive": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
