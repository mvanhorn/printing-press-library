// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newReposPullsCodespacesCreateWithPrForAuthenticatedUserCmd(flags *rootFlags) *cobra.Command {
	var bodyClientIp string
	var bodyDevcontainerPath string
	var bodyDisplayName string
	var bodyGeo string
	var bodyIdleTimeoutMinutes int
	var bodyLocation string
	var bodyMachine string
	var bodyMultiRepoPermissionsOptOut bool
	var bodyRetentionPeriodMinutes int
	var bodyWorkingDirectory string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "codespaces-create-with-pr-for-authenticated-user <owner> <repo> <pull_number>",
		Short: "Create a codespace from a pull request",
		Example: "  github-pp-cli repos pulls codespaces-create-with-pr-for-authenticated-user example-value example-value 42",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/repos/{owner}/{repo}/pulls/{pull_number}/codespaces"
			path = replacePathParam(path, "owner", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("repo is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "repo"))
			}
			path = replacePathParam(path, "repo", args[1])
			if len(args) < 3 {
				return usageErr(fmt.Errorf("pull_number is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "pull_number"))
			}
			path = replacePathParam(path, "pull_number", args[2])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyClientIp != "" {
					body["client_ip"] = bodyClientIp
				}
				if bodyDevcontainerPath != "" {
					body["devcontainer_path"] = bodyDevcontainerPath
				}
				if bodyDisplayName != "" {
					body["display_name"] = bodyDisplayName
				}
				if bodyGeo != "" {
					body["geo"] = bodyGeo
				}
				if bodyIdleTimeoutMinutes != 0 {
					body["idle_timeout_minutes"] = bodyIdleTimeoutMinutes
				}
				if bodyLocation != "" {
					body["location"] = bodyLocation
				}
				if bodyMachine != "" {
					body["machine"] = bodyMachine
				}
				if bodyMultiRepoPermissionsOptOut != false {
					body["multi_repo_permissions_opt_out"] = bodyMultiRepoPermissionsOptOut
				}
				if bodyRetentionPeriodMinutes != 0 {
					body["retention_period_minutes"] = bodyRetentionPeriodMinutes
				}
				if bodyWorkingDirectory != "" {
					body["working_directory"] = bodyWorkingDirectory
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "pulls",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyClientIp, "client-ip", "", "IP for location auto-detection when proxying a request")
	cmd.Flags().StringVar(&bodyDevcontainerPath, "devcontainer-path", "", "Path to devcontainer.json config to use for this codespace")
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "Display name for this codespace")
	cmd.Flags().StringVar(&bodyGeo, "geo", "", "The geographic area for this codespace. If not specified, the value is assigned by IP. This property replaces...")
	cmd.Flags().IntVar(&bodyIdleTimeoutMinutes, "idle-timeout-minutes", 0, "Time in minutes before codespace stops from inactivity")
	cmd.Flags().StringVar(&bodyLocation, "location", "", "The requested location for a new codespace. Best efforts are made to respect this upon creation. Assigned by IP if...")
	cmd.Flags().StringVar(&bodyMachine, "machine", "", "Machine type to use for this codespace")
	cmd.Flags().BoolVar(&bodyMultiRepoPermissionsOptOut, "multi-repo-permissions-opt-out", false, "Whether to authorize requested permissions from devcontainer.json")
	cmd.Flags().IntVar(&bodyRetentionPeriodMinutes, "retention-period-minutes", 0, "Duration in minutes after codespace has gone idle in which it will be deleted. Must be integer minutes between 0 and...")
	cmd.Flags().StringVar(&bodyWorkingDirectory, "working-directory", "", "Working directory for this codespace")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
