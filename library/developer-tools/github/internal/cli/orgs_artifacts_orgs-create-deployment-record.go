// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrgsArtifactsOrgsCreateDeploymentRecordCmd(flags *rootFlags) *cobra.Command {
	var bodyCluster string
	var bodyDeploymentName string
	var bodyDigest string
	var bodyGithubRepository string
	var bodyLogicalEnvironment string
	var bodyName string
	var bodyPhysicalEnvironment string
	var bodyReturnRecords bool
	var bodyRuntimeRisks string
	var bodyStatus string
	var bodyVersion string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "orgs-create-deployment-record <org>",
		Aliases: []string{"create"},
		Short: "Create an artifact deployment record",
		Example: "  github-pp-cli orgs artifacts orgs-create-deployment-record example-value --deployment-name example-resource",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("deployment-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "deployment-name")
				}
				if !cmd.Flags().Changed("digest") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "digest")
				}
				if !cmd.Flags().Changed("logical-environment") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "logical-environment")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("status") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "status")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/orgs/{org}/artifacts/metadata/deployment-record"
			path = replacePathParam(path, "org", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCluster != "" {
					body["cluster"] = bodyCluster
				}
				if bodyDeploymentName != "" {
					body["deployment_name"] = bodyDeploymentName
				}
				if bodyDigest != "" {
					body["digest"] = bodyDigest
				}
				if bodyGithubRepository != "" {
					body["github_repository"] = bodyGithubRepository
				}
				if bodyLogicalEnvironment != "" {
					body["logical_environment"] = bodyLogicalEnvironment
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPhysicalEnvironment != "" {
					body["physical_environment"] = bodyPhysicalEnvironment
				}
				if bodyReturnRecords != false {
					body["return_records"] = bodyReturnRecords
				}
				if bodyRuntimeRisks != "" {
					var parsedRuntimeRisks any
					if err := json.Unmarshal([]byte(bodyRuntimeRisks), &parsedRuntimeRisks); err != nil {
						return fmt.Errorf("parsing --runtime-risks JSON: %w", err)
					}
					body["runtime_risks"] = parsedRuntimeRisks
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodyVersion != "" {
					body["version"] = bodyVersion
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "artifacts",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCluster, "cluster", "", "The deployment cluster.")
	cmd.Flags().StringVar(&bodyDeploymentName, "deployment-name", "", "The unique identifier for the deployment represented by the new record. To accommodate differing containers and...")
	cmd.Flags().StringVar(&bodyDigest, "digest", "", "The hex encoded digest of the artifact.")
	cmd.Flags().StringVar(&bodyGithubRepository, "github-repository", "", "The name of the GitHub repository associated with the artifact. This should be used when there are no provenance...")
	cmd.Flags().StringVar(&bodyLogicalEnvironment, "logical-environment", "", "The stage of the deployment.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the artifact.")
	cmd.Flags().StringVar(&bodyPhysicalEnvironment, "physical-environment", "", "The physical region of the deployment.")
	cmd.Flags().BoolVar(&bodyReturnRecords, "return-records", true, "If true, the endpoint will return the created or updated record in the response body.")
	cmd.Flags().StringVar(&bodyRuntimeRisks, "runtime-risks", "", "A list of runtime risks associated with the deployment.")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "The status of the artifact. Can be either deployed or decommissioned.")
	cmd.Flags().StringVar(&bodyVersion, "version", "", "The artifact version.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
