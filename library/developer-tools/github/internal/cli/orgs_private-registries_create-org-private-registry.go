// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrgsPrivateRegistriesCreateOrgPrivateRegistryCmd(flags *rootFlags) *cobra.Command {
	var bodyAccountId string
	var bodyApiHost string
	var bodyAudience string
	var bodyAuthType string
	var bodyAwsRegion string
	var bodyClientId string
	var bodyDomain string
	var bodyDomainOwner string
	var bodyEncryptedValue string
	var bodyIdentityMappingName string
	var bodyJfrogOidcProviderName string
	var bodyKeyId string
	var bodyNamespace string
	var bodyRegistryType string
	var bodyReplacesBase bool
	var bodyRoleName string
	var bodyServiceSlug string
	var bodyTenantId string
	var bodyUrl string
	var bodyUsername string
	var bodyVisibility string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "create-org-private-registry <org>",
		Aliases: []string{"create"},
		Short: "Create a private registry for an organization",
		Example: "  github-pp-cli orgs private-registries create-org-private-registry example-value --registry-type example-value",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("registry-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "registry-type")
				}
				if !cmd.Flags().Changed("url") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "url")
				}
				if !cmd.Flags().Changed("visibility") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "visibility")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/orgs/{org}/private-registries"
			path = replacePathParam(path, "org", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAccountId != "" {
					body["account_id"] = bodyAccountId
				}
				if bodyApiHost != "" {
					body["api_host"] = bodyApiHost
				}
				if bodyAudience != "" {
					body["audience"] = bodyAudience
				}
				if bodyAuthType != "" {
					body["auth_type"] = bodyAuthType
				}
				if bodyAwsRegion != "" {
					body["aws_region"] = bodyAwsRegion
				}
				if bodyClientId != "" {
					body["client_id"] = bodyClientId
				}
				if bodyDomain != "" {
					body["domain"] = bodyDomain
				}
				if bodyDomainOwner != "" {
					body["domain_owner"] = bodyDomainOwner
				}
				if bodyEncryptedValue != "" {
					body["encrypted_value"] = bodyEncryptedValue
				}
				if bodyIdentityMappingName != "" {
					body["identity_mapping_name"] = bodyIdentityMappingName
				}
				if bodyJfrogOidcProviderName != "" {
					body["jfrog_oidc_provider_name"] = bodyJfrogOidcProviderName
				}
				if bodyKeyId != "" {
					body["key_id"] = bodyKeyId
				}
				if bodyNamespace != "" {
					body["namespace"] = bodyNamespace
				}
				if bodyRegistryType != "" {
					body["registry_type"] = bodyRegistryType
				}
				if bodyReplacesBase != false {
					body["replaces_base"] = bodyReplacesBase
				}
				if bodyRoleName != "" {
					body["role_name"] = bodyRoleName
				}
				if bodyServiceSlug != "" {
					body["service_slug"] = bodyServiceSlug
				}
				if bodyTenantId != "" {
					body["tenant_id"] = bodyTenantId
				}
				if bodyUrl != "" {
					body["url"] = bodyUrl
				}
				if bodyUsername != "" {
					body["username"] = bodyUsername
				}
				if bodyVisibility != "" {
					body["visibility"] = bodyVisibility
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "private-registries",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAccountId, "account-id", "", "The AWS account ID. Required when `auth_type` is `oidc_aws`.")
	cmd.Flags().StringVar(&bodyApiHost, "api-host", "", "The Cloudsmith API host. Optional for `oidc_cloudsmith` auth type. If omitted, `api.cloudsmith.io` is used by default.")
	cmd.Flags().StringVar(&bodyAudience, "audience", "", "The OIDC audience. Optional for `oidc_aws`, `oidc_jfrog`, and required for `oidc_cloudsmith` auth types.")
	cmd.Flags().StringVar(&bodyAuthType, "auth-type", "", "The authentication type for the private registry. Defaults to `token` if not specified. Use `oidc_azure`,...")
	cmd.Flags().StringVar(&bodyAwsRegion, "aws-region", "", "The AWS region. Required when `auth_type` is `oidc_aws`.")
	cmd.Flags().StringVar(&bodyClientId, "client-id", "", "The client ID of the Azure AD application. Required when `auth_type` is `oidc_azure`.")
	cmd.Flags().StringVar(&bodyDomain, "domain", "", "The CodeArtifact domain. Required when `auth_type` is `oidc_aws`.")
	cmd.Flags().StringVar(&bodyDomainOwner, "domain-owner", "", "The CodeArtifact domain owner (AWS account ID). Required when `auth_type` is `oidc_aws`.")
	cmd.Flags().StringVar(&bodyEncryptedValue, "encrypted-value", "", "The value for your secret, encrypted with [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages)...")
	cmd.Flags().StringVar(&bodyIdentityMappingName, "identity-mapping-name", "", "The JFrog identity mapping name. Optional for `oidc_jfrog` auth type.")
	cmd.Flags().StringVar(&bodyJfrogOidcProviderName, "jfrog-oidc-provider-name", "", "The JFrog OIDC provider name. Required when `auth_type` is `oidc_jfrog`.")
	cmd.Flags().StringVar(&bodyKeyId, "key-id", "", "The ID of the key you used to encrypt the secret. Required when `auth_type` is `token` or `username_password`....")
	cmd.Flags().StringVar(&bodyNamespace, "namespace", "", "The Cloudsmith organization namespace. Required when `auth_type` is `oidc_cloudsmith`.")
	cmd.Flags().StringVar(&bodyRegistryType, "registry-type", "", "The registry type.")
	cmd.Flags().BoolVar(&bodyReplacesBase, "replaces-base", false, "Whether this private registry should replace the base registry (e.g., npmjs.org for npm, rubygems.org for rubygems)....")
	cmd.Flags().StringVar(&bodyRoleName, "role-name", "", "The AWS IAM role name. Required when `auth_type` is `oidc_aws`.")
	cmd.Flags().StringVar(&bodyServiceSlug, "service-slug", "", "The Cloudsmith service account slug. Required when `auth_type` is `oidc_cloudsmith`.")
	cmd.Flags().StringVar(&bodyTenantId, "tenant-id", "", "The tenant ID of the Azure AD application. Required when `auth_type` is `oidc_azure`.")
	cmd.Flags().StringVar(&bodyUrl, "url", "", "The URL of the private registry.")
	cmd.Flags().StringVar(&bodyUsername, "username", "", "The username to use when authenticating with the private registry. This field should be omitted if the private...")
	cmd.Flags().StringVar(&bodyVisibility, "visibility", "", "Which type of organization repositories have access to the private registry. `selected` means only the repositories...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
