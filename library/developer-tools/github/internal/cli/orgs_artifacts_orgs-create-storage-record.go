// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrgsArtifactsOrgsCreateStorageRecordCmd(flags *rootFlags) *cobra.Command {
	var bodyArtifactUrl string
	var bodyDigest string
	var bodyGithubRepository string
	var bodyName string
	var bodyPath string
	var bodyRegistryUrl string
	var bodyRepository string
	var bodyReturnRecords bool
	var bodyStatus string
	var bodyVersion string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "orgs-create-storage-record <org>",
		Short: "Create artifact metadata storage record",
		Example: "  github-pp-cli orgs artifacts orgs-create-storage-record example-value --digest example-value",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("digest") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "digest")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("registry-url") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "registry-url")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/orgs/{org}/artifacts/metadata/storage-record"
			path = replacePathParam(path, "org", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyArtifactUrl != "" {
					body["artifact_url"] = bodyArtifactUrl
				}
				if bodyDigest != "" {
					body["digest"] = bodyDigest
				}
				if bodyGithubRepository != "" {
					body["github_repository"] = bodyGithubRepository
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPath != "" {
					body["path"] = bodyPath
				}
				if bodyRegistryUrl != "" {
					body["registry_url"] = bodyRegistryUrl
				}
				if bodyRepository != "" {
					body["repository"] = bodyRepository
				}
				if bodyReturnRecords != false {
					body["return_records"] = bodyReturnRecords
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodyVersion != "" {
					body["version"] = bodyVersion
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "artifacts",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyArtifactUrl, "artifact-url", "", "The URL where the artifact is stored.")
	cmd.Flags().StringVar(&bodyDigest, "digest", "", "The digest of the artifact (algorithm:hex-encoded-digest).")
	cmd.Flags().StringVar(&bodyGithubRepository, "github-repository", "", "The name of the GitHub repository associated with the artifact. This should be used when there are no provenance...")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the artifact.")
	cmd.Flags().StringVar(&bodyPath, "path", "", "The path of the artifact.")
	cmd.Flags().StringVar(&bodyRegistryUrl, "registry-url", "", "The base URL of the artifact registry.")
	cmd.Flags().StringVar(&bodyRepository, "repository", "", "The repository name within the registry.")
	cmd.Flags().BoolVar(&bodyReturnRecords, "return-records", true, "If true, the endpoint will return the created record in the response body.")
	cmd.Flags().StringVar(&bodyStatus, "status", "active", "The status of the artifact (e.g., active, inactive).")
	cmd.Flags().StringVar(&bodyVersion, "version", "", "The artifact version.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
