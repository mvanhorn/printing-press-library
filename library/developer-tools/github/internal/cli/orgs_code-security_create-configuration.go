// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrgsCodeSecurityCreateConfigurationCmd(flags *rootFlags) *cobra.Command {
	var bodyAdvancedSecurity string
	var bodyCodeScanningDefaultSetup string
	var bodyCodeScanningDelegatedAlertDismissal string
	var bodyCodeSecurity string
	var bodyDependabotAlerts string
	var bodyDependabotDelegatedAlertDismissal string
	var bodyDependabotSecurityUpdates string
	var bodyDependencyGraph string
	var bodyDependencyGraphAutosubmitAction string
	var bodyDescription string
	var bodyEnforcement string
	var bodyName string
	var bodyPrivateVulnerabilityReporting string
	var bodySecretProtection string
	var bodySecretScanning string
	var bodySecretScanningDelegatedAlertDismissal string
	var bodySecretScanningDelegatedBypass string
	var bodySecretScanningExtendedMetadata string
	var bodySecretScanningGenericSecrets string
	var bodySecretScanningNonProviderPatterns string
	var bodySecretScanningPushProtection string
	var bodySecretScanningValidityChecks string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "create-configuration <org>",
		Short: "Create a code security configuration",
		Example: "  github-pp-cli orgs code-security create-configuration example-value --description example-value",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("description") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "description")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/orgs/{org}/code-security/configurations"
			path = replacePathParam(path, "org", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAdvancedSecurity != "" {
					body["advanced_security"] = bodyAdvancedSecurity
				}
				if bodyCodeScanningDefaultSetup != "" {
					body["code_scanning_default_setup"] = bodyCodeScanningDefaultSetup
				}
				if bodyCodeScanningDelegatedAlertDismissal != "" {
					body["code_scanning_delegated_alert_dismissal"] = bodyCodeScanningDelegatedAlertDismissal
				}
				if bodyCodeSecurity != "" {
					body["code_security"] = bodyCodeSecurity
				}
				if bodyDependabotAlerts != "" {
					body["dependabot_alerts"] = bodyDependabotAlerts
				}
				if bodyDependabotDelegatedAlertDismissal != "" {
					body["dependabot_delegated_alert_dismissal"] = bodyDependabotDelegatedAlertDismissal
				}
				if bodyDependabotSecurityUpdates != "" {
					body["dependabot_security_updates"] = bodyDependabotSecurityUpdates
				}
				if bodyDependencyGraph != "" {
					body["dependency_graph"] = bodyDependencyGraph
				}
				if bodyDependencyGraphAutosubmitAction != "" {
					body["dependency_graph_autosubmit_action"] = bodyDependencyGraphAutosubmitAction
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyEnforcement != "" {
					body["enforcement"] = bodyEnforcement
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPrivateVulnerabilityReporting != "" {
					body["private_vulnerability_reporting"] = bodyPrivateVulnerabilityReporting
				}
				if bodySecretProtection != "" {
					body["secret_protection"] = bodySecretProtection
				}
				if bodySecretScanning != "" {
					body["secret_scanning"] = bodySecretScanning
				}
				if bodySecretScanningDelegatedAlertDismissal != "" {
					body["secret_scanning_delegated_alert_dismissal"] = bodySecretScanningDelegatedAlertDismissal
				}
				if bodySecretScanningDelegatedBypass != "" {
					body["secret_scanning_delegated_bypass"] = bodySecretScanningDelegatedBypass
				}
				if bodySecretScanningExtendedMetadata != "" {
					body["secret_scanning_extended_metadata"] = bodySecretScanningExtendedMetadata
				}
				if bodySecretScanningGenericSecrets != "" {
					body["secret_scanning_generic_secrets"] = bodySecretScanningGenericSecrets
				}
				if bodySecretScanningNonProviderPatterns != "" {
					body["secret_scanning_non_provider_patterns"] = bodySecretScanningNonProviderPatterns
				}
				if bodySecretScanningPushProtection != "" {
					body["secret_scanning_push_protection"] = bodySecretScanningPushProtection
				}
				if bodySecretScanningValidityChecks != "" {
					body["secret_scanning_validity_checks"] = bodySecretScanningValidityChecks
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "code-security",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAdvancedSecurity, "advanced-security", "disabled", "The enablement status of GitHub Advanced Security features. `enabled` will enable both Code Security and Secret...")
	cmd.Flags().StringVar(&bodyCodeScanningDefaultSetup, "code-scanning-default-setup", "disabled", "The enablement status of code scanning default setup")
	cmd.Flags().StringVar(&bodyCodeScanningDelegatedAlertDismissal, "code-scanning-delegated-alert-dismissal", "not_set", "The enablement status of code scanning delegated alert dismissal")
	cmd.Flags().StringVar(&bodyCodeSecurity, "code-security", "", "The enablement status of GitHub Code Security features.")
	cmd.Flags().StringVar(&bodyDependabotAlerts, "dependabot-alerts", "disabled", "The enablement status of Dependabot alerts")
	cmd.Flags().StringVar(&bodyDependabotDelegatedAlertDismissal, "dependabot-delegated-alert-dismissal", "disabled", "The enablement status of Dependabot delegated alert dismissal. Requires Dependabot alerts to be enabled.")
	cmd.Flags().StringVar(&bodyDependabotSecurityUpdates, "dependabot-security-updates", "disabled", "The enablement status of Dependabot security updates")
	cmd.Flags().StringVar(&bodyDependencyGraph, "dependency-graph", "enabled", "The enablement status of Dependency Graph")
	cmd.Flags().StringVar(&bodyDependencyGraphAutosubmitAction, "dependency-graph-autosubmit-action", "disabled", "The enablement status of Automatic dependency submission")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A description of the code security configuration")
	cmd.Flags().StringVar(&bodyEnforcement, "enforcement", "enforced", "The enforcement status for a security configuration")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the code security configuration. Must be unique within the organization.")
	cmd.Flags().StringVar(&bodyPrivateVulnerabilityReporting, "private-vulnerability-reporting", "disabled", "The enablement status of private vulnerability reporting")
	cmd.Flags().StringVar(&bodySecretProtection, "secret-protection", "", "The enablement status of GitHub Secret Protection features.")
	cmd.Flags().StringVar(&bodySecretScanning, "secret-scanning", "disabled", "The enablement status of secret scanning")
	cmd.Flags().StringVar(&bodySecretScanningDelegatedAlertDismissal, "secret-scanning-delegated-alert-dismissal", "", "The enablement status of secret scanning delegated alert dismissal")
	cmd.Flags().StringVar(&bodySecretScanningDelegatedBypass, "secret-scanning-delegated-bypass", "disabled", "The enablement status of secret scanning delegated bypass")
	cmd.Flags().StringVar(&bodySecretScanningExtendedMetadata, "secret-scanning-extended-metadata", "", "The enablement status of secret scanning extended metadata")
	cmd.Flags().StringVar(&bodySecretScanningGenericSecrets, "secret-scanning-generic-secrets", "disabled", "The enablement status of Copilot secret scanning")
	cmd.Flags().StringVar(&bodySecretScanningNonProviderPatterns, "secret-scanning-non-provider-patterns", "disabled", "The enablement status of secret scanning non provider patterns")
	cmd.Flags().StringVar(&bodySecretScanningPushProtection, "secret-scanning-push-protection", "disabled", "The enablement status of secret scanning push protection")
	cmd.Flags().StringVar(&bodySecretScanningValidityChecks, "secret-scanning-validity-checks", "disabled", "The enablement status of secret scanning validity checks")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
