// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAdvisoriesSecurityListGlobalCmd(flags *rootFlags) *cobra.Command {
	var flagGhsaId string
	var flagType string
	var flagCveId string
	var flagEcosystem string
	var flagSeverity string
	var flagCwes string
	var flagIsWithdrawn bool
	var flagAffects string
	var flagPublished string
	var flagUpdated string
	var flagModified string
	var flagEpssPercentage string
	var flagEpssPercentile string
	var flagBefore string
	var flagAfter string
	var flagDirection string
	var flagPerPage int
	var flagSort string
	var flagAll bool

	cmd := &cobra.Command{
		Use:   "security-list-global",
		Aliases: []string{"list"},
		Short: "List global security advisories",
		Example: "  github-pp-cli advisories security-list-global",
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("type") {
				allowedType := []string{ "reviewed", "malware", "unreviewed" }
				validType := false
				for _, v := range allowedType {
					if flagType == v {
						validType = true
						break
					}
				}
				if !validType {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "type", flagType, allowedType)
				}
			}
			if cmd.Flags().Changed("ecosystem") {
				allowedEcosystem := []string{ "rubygems", "npm", "pip", "maven", "nuget", "composer", "go", "rust", "erlang", "actions", "pub", "other", "swift" }
				validEcosystem := false
				for _, v := range allowedEcosystem {
					if flagEcosystem == v {
						validEcosystem = true
						break
					}
				}
				if !validEcosystem {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "ecosystem", flagEcosystem, allowedEcosystem)
				}
			}
			if cmd.Flags().Changed("severity") {
				allowedSeverity := []string{ "unknown", "low", "medium", "high", "critical" }
				validSeverity := false
				for _, v := range allowedSeverity {
					if flagSeverity == v {
						validSeverity = true
						break
					}
				}
				if !validSeverity {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "severity", flagSeverity, allowedSeverity)
				}
			}
			if cmd.Flags().Changed("direction") {
				allowedDirection := []string{ "asc", "desc" }
				validDirection := false
				for _, v := range allowedDirection {
					if flagDirection == v {
						validDirection = true
						break
					}
				}
				if !validDirection {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "direction", flagDirection, allowedDirection)
				}
			}
			if cmd.Flags().Changed("sort") {
				allowedSort := []string{ "updated", "published", "epss_percentage", "epss_percentile" }
				validSort := false
				for _, v := range allowedSort {
					if flagSort == v {
						validSort = true
						break
					}
				}
				if !validSort {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort", flagSort, allowedSort)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/advisories"
			data, prov, err := resolvePaginatedRead(c, flags, "advisories", path, map[string]string{
				"ghsa_id": fmt.Sprintf("%v", flagGhsaId),
				"type": fmt.Sprintf("%v", flagType),
				"cve_id": fmt.Sprintf("%v", flagCveId),
				"ecosystem": fmt.Sprintf("%v", flagEcosystem),
				"severity": fmt.Sprintf("%v", flagSeverity),
				"cwes": fmt.Sprintf("%v", flagCwes),
				"is_withdrawn": fmt.Sprintf("%v", flagIsWithdrawn),
				"affects": fmt.Sprintf("%v", flagAffects),
				"published": fmt.Sprintf("%v", flagPublished),
				"updated": fmt.Sprintf("%v", flagUpdated),
				"modified": fmt.Sprintf("%v", flagModified),
				"epss_percentage": fmt.Sprintf("%v", flagEpssPercentage),
				"epss_percentile": fmt.Sprintf("%v", flagEpssPercentile),
				"before": fmt.Sprintf("%v", flagBefore),
				"after": fmt.Sprintf("%v", flagAfter),
				"direction": fmt.Sprintf("%v", flagDirection),
				"per_page": fmt.Sprintf("%v", flagPerPage),
				"sort": fmt.Sprintf("%v", flagSort),
			}, nil, flagAll, "after", "", "")
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagGhsaId, "ghsa-id", "", "If specified, only advisories with this GHSA (GitHub Security Advisory) identifier will be returned.")
	cmd.Flags().StringVar(&flagType, "type", "reviewed", "If specified, only advisories of this type will be returned. By default, a request with no other parameters defined... (one of: reviewed, malware, unreviewed)")
	cmd.Flags().StringVar(&flagCveId, "cve-id", "", "If specified, only advisories with this CVE (Common Vulnerabilities and Exposures) identifier will be returned.")
	cmd.Flags().StringVar(&flagEcosystem, "ecosystem", "", "If specified, only advisories for these ecosystems will be returned. (one of: rubygems, npm, pip, maven, nuget, composer, go, rust, erlang, actions, pub, other, swift)")
	cmd.Flags().StringVar(&flagSeverity, "severity", "", "If specified, only advisories with these severities will be returned. (one of: unknown, low, medium, high, critical)")
	cmd.Flags().StringVar(&flagCwes, "cwes", "", "If specified, only advisories with these Common Weakness Enumerations (CWEs) will be returned. Example:...")
	cmd.Flags().BoolVar(&flagIsWithdrawn, "is-withdrawn", false, "Whether to only return advisories that have been withdrawn.")
	cmd.Flags().StringVar(&flagAffects, "affects", "", "If specified, only return advisories that affect any of `package` or `package@version`. A maximum of 1000 packages...")
	cmd.Flags().StringVar(&flagPublished, "published", "", "If specified, only return advisories that were published on a date or date range. For more information on the syntax...")
	cmd.Flags().StringVar(&flagUpdated, "updated", "", "If specified, only return advisories that were updated on a date or date range. For more information on the syntax...")
	cmd.Flags().StringVar(&flagModified, "modified", "", "If specified, only show advisories that were updated or published on a date or date range. For more information on...")
	cmd.Flags().StringVar(&flagEpssPercentage, "epss-percentage", "", "If specified, only return advisories that have an EPSS percentage score that matches the provided value. The EPSS...")
	cmd.Flags().StringVar(&flagEpssPercentile, "epss-percentile", "", "If specified, only return advisories that have an EPSS percentile score that matches the provided value. The EPSS...")
	cmd.Flags().StringVar(&flagBefore, "before", "", "A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-li...")
	cmd.Flags().StringVar(&flagAfter, "after", "", "A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-li...")
	cmd.Flags().StringVar(&flagDirection, "direction", "desc", "The direction to sort the results by. (one of: asc, desc)")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 30, "The number of results per page (max 100). For more information, see '[Using pagination in the REST...")
	cmd.Flags().StringVar(&flagSort, "sort", "published", "The property to sort the results by. (one of: updated, published, epss_percentage, epss_percentile)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
