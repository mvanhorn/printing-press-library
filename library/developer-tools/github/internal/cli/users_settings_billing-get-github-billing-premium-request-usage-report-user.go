// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newUsersSettingsBillingGetGithubBillingPremiumRequestUsageReportUserCmd(flags *rootFlags) *cobra.Command {
	var flagYear int
	var flagMonth int
	var flagDay int
	var flagModel string
	var flagProduct string

	cmd := &cobra.Command{
		Use:   "billing-get-github-billing-premium-request-usage-report-user <username>",
		Aliases: []string{"get"},
		Short: "Get billing premium request usage report for a user",
		Example: "  github-pp-cli users settings billing-get-github-billing-premium-request-usage-report-user example-resource",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/users/{username}/settings/billing/premium_request/usage"
			path = replacePathParam(path, "username", args[0])
			params := map[string]string{}
			if flagYear != 0 {
				params["year"] = fmt.Sprintf("%v", flagYear)
			}
			if flagMonth != 0 {
				params["month"] = fmt.Sprintf("%v", flagMonth)
			}
			if flagDay != 0 {
				params["day"] = fmt.Sprintf("%v", flagDay)
			}
			if flagModel != "" {
				params["model"] = fmt.Sprintf("%v", flagModel)
			}
			if flagProduct != "" {
				params["product"] = fmt.Sprintf("%v", flagProduct)
			}
			data, prov, err := resolveRead(c, flags, "settings", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagYear, "year", 0, "If specified, only return results for a single year. The value of `year` is an integer with four digits representing...")
	cmd.Flags().IntVar(&flagMonth, "month", 0, "If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`....")
	cmd.Flags().IntVar(&flagDay, "day", 0, "If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no...")
	cmd.Flags().StringVar(&flagModel, "model", "", "The model name to query usage for. The name is not case sensitive.")
	cmd.Flags().StringVar(&flagProduct, "product", "", "The product name to query usage for. The name is not case sensitive.")

	return cmd
}
