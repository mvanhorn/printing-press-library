// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newReposDependabotListAlertsForRepoCmd(flags *rootFlags) *cobra.Command {
	var flagClassification string
	var flagState string
	var flagSeverity string
	var flagEcosystem string
	var flagPackage string
	var flagManifest string
	var flagEpssPercentage string
	var flagHas string
	var flagAssignee string
	var flagScope string
	var flagSort string
	var flagDirection string
	var flagBefore string
	var flagAfter string
	var flagPerPage int
	var flagAll bool

	cmd := &cobra.Command{
		Use:   "list-alerts-for-repo <owner> <repo>",
		Short: "List Dependabot alerts for a repository",
		Example: "  github-pp-cli repos dependabot list-alerts-for-repo example-value example-value",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("scope") {
				allowedScope := []string{ "development", "runtime" }
				validScope := false
				for _, v := range allowedScope {
					if flagScope == v {
						validScope = true
						break
					}
				}
				if !validScope {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "scope", flagScope, allowedScope)
				}
			}
			if cmd.Flags().Changed("sort") {
				allowedSort := []string{ "created", "updated", "epss_percentage" }
				validSort := false
				for _, v := range allowedSort {
					if flagSort == v {
						validSort = true
						break
					}
				}
				if !validSort {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort", flagSort, allowedSort)
				}
			}
			if cmd.Flags().Changed("direction") {
				allowedDirection := []string{ "asc", "desc" }
				validDirection := false
				for _, v := range allowedDirection {
					if flagDirection == v {
						validDirection = true
						break
					}
				}
				if !validDirection {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "direction", flagDirection, allowedDirection)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/repos/{owner}/{repo}/dependabot/alerts"
			path = replacePathParam(path, "owner", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("repo is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "repo"))
			}
			path = replacePathParam(path, "repo", args[1])
			data, prov, err := resolvePaginatedRead(c, flags, "dependabot", path, map[string]string{
				"classification": fmt.Sprintf("%v", flagClassification),
				"state": fmt.Sprintf("%v", flagState),
				"severity": fmt.Sprintf("%v", flagSeverity),
				"ecosystem": fmt.Sprintf("%v", flagEcosystem),
				"package": fmt.Sprintf("%v", flagPackage),
				"manifest": fmt.Sprintf("%v", flagManifest),
				"epss_percentage": fmt.Sprintf("%v", flagEpssPercentage),
				"has": fmt.Sprintf("%v", flagHas),
				"assignee": fmt.Sprintf("%v", flagAssignee),
				"scope": fmt.Sprintf("%v", flagScope),
				"sort": fmt.Sprintf("%v", flagSort),
				"direction": fmt.Sprintf("%v", flagDirection),
				"before": fmt.Sprintf("%v", flagBefore),
				"after": fmt.Sprintf("%v", flagAfter),
				"per_page": fmt.Sprintf("%v", flagPerPage),
			}, nil, flagAll, "after", "", "")
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagClassification, "classification", "", "A comma-separated list of vulnerability classifications. If specified, only alerts for vulnerabilities with these...")
	cmd.Flags().StringVar(&flagState, "state", "", "A comma-separated list of states. If specified, only alerts with these states will be returned. Can be:...")
	cmd.Flags().StringVar(&flagSeverity, "severity", "", "A comma-separated list of severities. If specified, only alerts with these severities will be returned. Can be:...")
	cmd.Flags().StringVar(&flagEcosystem, "ecosystem", "", "A comma-separated list of ecosystems. If specified, only alerts for these ecosystems will be returned. Can be:...")
	cmd.Flags().StringVar(&flagPackage, "package", "", "A comma-separated list of package names. If specified, only alerts for these packages will be returned.")
	cmd.Flags().StringVar(&flagManifest, "manifest", "", "A comma-separated list of full manifest paths. If specified, only alerts for these manifests will be returned.")
	cmd.Flags().StringVar(&flagEpssPercentage, "epss-percentage", "", "CVE Exploit Prediction Scoring System (EPSS) percentage. Can be specified as: - An exact number (`n`) - Comparators...")
	cmd.Flags().StringVar(&flagHas, "has", "", "Filters the list of alerts based on whether the alert has the given value. If specified, only alerts meeting this...")
	cmd.Flags().StringVar(&flagAssignee, "assignee", "", "Filter alerts by assignees. Provide a comma-separated list of user handles (e.g., `octocat` or `octocat,hubot`) to...")
	cmd.Flags().StringVar(&flagScope, "scope", "", "The scope of the vulnerable dependency. If specified, only alerts with this scope will be returned. (one of: development, runtime)")
	cmd.Flags().StringVar(&flagSort, "sort", "created", "The property by which to sort the results. `created` means when the alert was created. `updated` means when the... (one of: created, updated, epss_percentage)")
	cmd.Flags().StringVar(&flagDirection, "direction", "desc", "The direction to sort the results by. (one of: asc, desc)")
	cmd.Flags().StringVar(&flagBefore, "before", "", "A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-li...")
	cmd.Flags().StringVar(&flagAfter, "after", "", "A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-li...")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 30, "The number of results per page (max 100). For more information, see '[Using pagination in the REST...")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
