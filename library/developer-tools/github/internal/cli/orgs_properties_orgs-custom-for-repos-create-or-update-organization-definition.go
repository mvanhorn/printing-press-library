// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrgsPropertiesOrgsCustomForReposCreateOrUpdateOrganizationDefinitionCmd(flags *rootFlags) *cobra.Command {
	var bodyAllowedValues string
	var bodyDefaultValue string
	var bodyDescription string
	var bodyRequireExplicitValues bool
	var bodyRequired bool
	var bodyValueType string
	var bodyValuesEditableBy string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "orgs-custom-for-repos-create-or-update-organization-definition <org> <custom_property_name>",
		Aliases: []string{"update"},
		Short: "Create or update a custom property for an organization",
		Example: "  github-pp-cli orgs properties orgs-custom-for-repos-create-or-update-organization-definition example-value example-resource --value-type example-value",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("value-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "value-type")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/orgs/{org}/properties/schema/{custom_property_name}"
			path = replacePathParam(path, "org", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("custom_property_name is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "custom_property_name"))
			}
			path = replacePathParam(path, "custom_property_name", args[1])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAllowedValues != "" {
					var parsedAllowedValues any
					if err := json.Unmarshal([]byte(bodyAllowedValues), &parsedAllowedValues); err != nil {
						return fmt.Errorf("parsing --allowed-values JSON: %w", err)
					}
					body["allowed_values"] = parsedAllowedValues
				}
				if bodyDefaultValue != "" {
					body["default_value"] = bodyDefaultValue
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyRequireExplicitValues != false {
					body["require_explicit_values"] = bodyRequireExplicitValues
				}
				if bodyRequired != false {
					body["required"] = bodyRequired
				}
				if bodyValueType != "" {
					body["value_type"] = bodyValueType
				}
				if bodyValuesEditableBy != "" {
					body["values_editable_by"] = bodyValuesEditableBy
				}
			}
			data, statusCode, err := c.Put(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "properties",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAllowedValues, "allowed-values", "", "An ordered list of the allowed values of the property. The property can have up to 200 allowed values.")
	cmd.Flags().StringVar(&bodyDefaultValue, "default-value", "", "Default value of the property")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Short description of the property")
	cmd.Flags().BoolVar(&bodyRequireExplicitValues, "require-explicit-values", false, "Whether setting properties values is mandatory")
	cmd.Flags().BoolVar(&bodyRequired, "required", false, "Whether the property is required.")
	cmd.Flags().StringVar(&bodyValueType, "value-type", "", "The type of the value for the property")
	cmd.Flags().StringVar(&bodyValuesEditableBy, "values-editable-by", "", "Who can edit the values of the property")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
