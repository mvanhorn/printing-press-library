// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newOrgsSecretScanningListAlertsForOrgCmd(flags *rootFlags) *cobra.Command {
	var flagState string
	var flagSecretType string
	var flagExcludeSecretTypes string
	var flagExcludeProviders string
	var flagProviders string
	var flagResolution string
	var flagAssignee string
	var flagSort string
	var flagDirection string
	var flagPage int
	var flagPerPage int
	var flagBefore string
	var flagAfter string
	var flagValidity string
	var flagIsPubliclyLeaked bool
	var flagIsMultiRepo bool
	var flagHideSecret bool
	var flagAll bool

	cmd := &cobra.Command{
		Use:   "list-alerts-for-org <org>",
		Aliases: []string{"get"},
		Short: "List secret scanning alerts for an organization",
		Example: "  github-pp-cli orgs secret-scanning list-alerts-for-org example-value",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("state") {
				allowedState := []string{ "open", "resolved" }
				validState := false
				for _, v := range allowedState {
					if flagState == v {
						validState = true
						break
					}
				}
				if !validState {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "state", flagState, allowedState)
				}
			}
			if cmd.Flags().Changed("sort") {
				allowedSort := []string{ "created", "updated" }
				validSort := false
				for _, v := range allowedSort {
					if flagSort == v {
						validSort = true
						break
					}
				}
				if !validSort {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort", flagSort, allowedSort)
				}
			}
			if cmd.Flags().Changed("direction") {
				allowedDirection := []string{ "asc", "desc" }
				validDirection := false
				for _, v := range allowedDirection {
					if flagDirection == v {
						validDirection = true
						break
					}
				}
				if !validDirection {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "direction", flagDirection, allowedDirection)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/orgs/{org}/secret-scanning/alerts"
			path = replacePathParam(path, "org", args[0])
			data, prov, err := resolvePaginatedRead(c, flags, "secret-scanning", path, map[string]string{
				"state": fmt.Sprintf("%v", flagState),
				"secret_type": fmt.Sprintf("%v", flagSecretType),
				"exclude_secret_types": fmt.Sprintf("%v", flagExcludeSecretTypes),
				"exclude_providers": fmt.Sprintf("%v", flagExcludeProviders),
				"providers": fmt.Sprintf("%v", flagProviders),
				"resolution": fmt.Sprintf("%v", flagResolution),
				"assignee": fmt.Sprintf("%v", flagAssignee),
				"sort": fmt.Sprintf("%v", flagSort),
				"direction": fmt.Sprintf("%v", flagDirection),
				"page": fmt.Sprintf("%v", flagPage),
				"per_page": fmt.Sprintf("%v", flagPerPage),
				"before": fmt.Sprintf("%v", flagBefore),
				"after": fmt.Sprintf("%v", flagAfter),
				"validity": fmt.Sprintf("%v", flagValidity),
				"is_publicly_leaked": fmt.Sprintf("%v", flagIsPubliclyLeaked),
				"is_multi_repo": fmt.Sprintf("%v", flagIsMultiRepo),
				"hide_secret": fmt.Sprintf("%v", flagHideSecret),
			}, nil, flagAll, "after", "", "")
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagState, "state", "", "Set to `open` or `resolved` to only list secret scanning alerts in a specific state. (one of: open, resolved)")
	cmd.Flags().StringVar(&flagSecretType, "secret-type", "", "A comma-separated list of secret types to return. All default secret patterns are returned. To return generic...")
	cmd.Flags().StringVar(&flagExcludeSecretTypes, "exclude-secret-types", "", "A comma-separated list of secret types to exclude from the results. All default secret patterns are returned except...")
	cmd.Flags().StringVar(&flagExcludeProviders, "exclude-providers", "", "A comma-separated list of provider slugs to exclude from the results. Provider slugs use lowercase with underscores...")
	cmd.Flags().StringVar(&flagProviders, "providers", "", "A comma-separated list of provider slugs to filter by. Provider slugs use lowercase with underscores (e.g.,...")
	cmd.Flags().StringVar(&flagResolution, "resolution", "", "A comma-separated list of resolutions. Only secret scanning alerts with one of these resolutions are listed. Valid...")
	cmd.Flags().StringVar(&flagAssignee, "assignee", "", "Filters alerts by assignee. Use `*` to get all assigned alerts, `none` to get all unassigned alerts, or a GitHub...")
	cmd.Flags().StringVar(&flagSort, "sort", "created", "The property to sort the results by. `created` means when the alert was created. `updated` means when the alert was... (one of: created, updated)")
	cmd.Flags().StringVar(&flagDirection, "direction", "desc", "The direction to sort the results by. (one of: asc, desc)")
	cmd.Flags().IntVar(&flagPage, "page", 1, "The page number of the results to fetch. For more information, see '[Using pagination in the REST...")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 30, "The number of results per page (max 100). For more information, see '[Using pagination in the REST...")
	cmd.Flags().StringVar(&flagBefore, "before", "", "A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-li...")
	cmd.Flags().StringVar(&flagAfter, "after", "", "A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-li...")
	cmd.Flags().StringVar(&flagValidity, "validity", "", "A comma-separated list of validities that, when present, will return alerts that match the validities in this list....")
	cmd.Flags().BoolVar(&flagIsPubliclyLeaked, "is-publicly-leaked", false, "A boolean value representing whether or not to filter alerts by the publicly-leaked tag being present.")
	cmd.Flags().BoolVar(&flagIsMultiRepo, "is-multi-repo", false, "A boolean value representing whether or not to filter alerts by the multi-repo tag being present.")
	cmd.Flags().BoolVar(&flagHideSecret, "hide-secret", false, "A boolean value representing whether or not to hide literal secrets in the results.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
