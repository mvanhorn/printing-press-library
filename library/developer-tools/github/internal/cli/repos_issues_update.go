// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newReposIssuesUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAssignee string
	var bodyAssignees string
	var bodyBody string
	var bodyMilestone string
	var bodyState string
	var bodyStateReason string
	var bodyTitle string
	var bodyType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "update <owner> <repo> <issue_number>",
		Short: "Update an issue",
		Example: "  github-pp-cli repos issues update example-value example-value 42",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/repos/{owner}/{repo}/issues/{issue_number}"
			path = replacePathParam(path, "owner", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("repo is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "repo"))
			}
			path = replacePathParam(path, "repo", args[1])
			if len(args) < 3 {
				return usageErr(fmt.Errorf("issue_number is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "issue_number"))
			}
			path = replacePathParam(path, "issue_number", args[2])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAssignee != "" {
					body["assignee"] = bodyAssignee
				}
				if bodyAssignees != "" {
					var parsedAssignees any
					if err := json.Unmarshal([]byte(bodyAssignees), &parsedAssignees); err != nil {
						return fmt.Errorf("parsing --assignees JSON: %w", err)
					}
					body["assignees"] = parsedAssignees
				}
				if bodyBody != "" {
					body["body"] = bodyBody
				}
				if bodyMilestone != "" {
					body["milestone"] = bodyMilestone
				}
				if bodyState != "" {
					body["state"] = bodyState
				}
				if bodyStateReason != "" {
					body["state_reason"] = bodyStateReason
				}
				if bodyTitle != "" {
					body["title"] = bodyTitle
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "issues",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAssignee, "assignee", "", "Username to assign to this issue. **This field is closing down.**")
	cmd.Flags().StringVar(&bodyAssignees, "assignees", "", "Usernames to assign to this issue. Pass one or more user logins to _replace_ the set of assignees on this issue....")
	cmd.Flags().StringVar(&bodyBody, "body", "", "The contents of the issue.")
	cmd.Flags().StringVar(&bodyMilestone, "milestone", "", "Milestone")
	cmd.Flags().StringVar(&bodyState, "state", "", "The open or closed state of the issue.")
	cmd.Flags().StringVar(&bodyStateReason, "state-reason", "", "The reason for the state change. Ignored unless `state` is changed.")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "The title of the issue.")
	cmd.Flags().StringVar(&bodyType, "type", "", "The name of the issue type to associate with this issue or use `null` to remove the current issue type. Only users...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
