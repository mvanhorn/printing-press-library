// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrgsSettingsHostedComputeCreateNetworkConfigurationForOrgCmd(flags *rootFlags) *cobra.Command {
	var bodyComputeService string
	var bodyFailoverNetworkEnabled bool
	var bodyFailoverNetworkSettingsIds string
	var bodyName string
	var bodyNetworkSettingsIds string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "hosted-compute-create-network-configuration-for-org <org>",
		Aliases: []string{"create"},
		Short: "Create a hosted compute network configuration for an organization",
		Example: "  github-pp-cli orgs settings hosted-compute-create-network-configuration-for-org example-value --name example-resource",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("network-settings-ids") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "network-settings-ids")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/orgs/{org}/settings/network-configurations"
			path = replacePathParam(path, "org", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyComputeService != "" {
					body["compute_service"] = bodyComputeService
				}
				if bodyFailoverNetworkEnabled != false {
					body["failover_network_enabled"] = bodyFailoverNetworkEnabled
				}
				if bodyFailoverNetworkSettingsIds != "" {
					var parsedFailoverNetworkSettingsIds any
					if err := json.Unmarshal([]byte(bodyFailoverNetworkSettingsIds), &parsedFailoverNetworkSettingsIds); err != nil {
						return fmt.Errorf("parsing --failover-network-settings-ids JSON: %w", err)
					}
					body["failover_network_settings_ids"] = parsedFailoverNetworkSettingsIds
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyNetworkSettingsIds != "" {
					var parsedNetworkSettingsIds any
					if err := json.Unmarshal([]byte(bodyNetworkSettingsIds), &parsedNetworkSettingsIds); err != nil {
						return fmt.Errorf("parsing --network-settings-ids JSON: %w", err)
					}
					body["network_settings_ids"] = parsedNetworkSettingsIds
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "settings",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyComputeService, "compute-service", "", "The hosted compute service to use for the network configuration.")
	cmd.Flags().BoolVar(&bodyFailoverNetworkEnabled, "failover-network-enabled", false, "Indicates whether the failover network resource is enabled.")
	cmd.Flags().StringVar(&bodyFailoverNetworkSettingsIds, "failover-network-settings-ids", "", "A list of identifiers of the failover network settings resources to use for the network configuration. Exactly one...")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name of the network configuration. Must be between 1 and 100 characters and may only contain upper and lowercase...")
	cmd.Flags().StringVar(&bodyNetworkSettingsIds, "network-settings-ids", "", "A list of identifiers of the network settings resources to use for the network configuration. Exactly one resource...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
