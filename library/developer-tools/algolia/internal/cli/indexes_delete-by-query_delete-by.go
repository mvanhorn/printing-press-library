// Copyright 2026 Som Samantray and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newIndexesDeleteByQueryDeleteByCmd(flags *rootFlags) *cobra.Command {
	var bodyAroundLatLng string
	var bodyAroundRadius string
	var bodyFacetFilters string
	var bodyFilters string
	var bodyInsideBoundingBox string
	var bodyInsidePolygon string
	var bodyNumericFilters string
	var bodyTagFilters string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "delete-by <indexName>",
		Aliases:     []string{"create"},
		Short:       "This operation doesn't accept empty filters. This operation is resource-intensive.",
		Example:     "  algolia-pp-cli indexes delete-by-query delete-by ALGOLIA_INDEX_NAME",
		Annotations: map[string]string{"pp:endpoint": "delete-by-query.delete-by", "pp:method": "POST", "pp:path": "/1/indexes/{indexName}/deleteByQuery"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <indexName>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <indexName>"))
			}
			if !stdinBody {
			}
			path := "/1/indexes/{indexName}/deleteByQuery"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("indexName is required\nUsage: %s <%s>", cmd.CommandPath(), "indexName"))
			}
			path = replacePathParam(path, "indexName", args[0])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("around-lat-lng") || bodyAroundLatLng != "" {
					bodyMap["aroundLatLng"] = bodyAroundLatLng
				}
				if cmd.Flags().Changed("around-radius") || bodyAroundRadius != "" {
					bodyMap["aroundRadius"] = bodyAroundRadius
				}
				if cmd.Flags().Changed("facet-filters") || bodyFacetFilters != "" {
					if looksLikeJSONComposite(bodyFacetFilters) {
						var parsedFacetFilters any
						if err := json.Unmarshal([]byte(bodyFacetFilters), &parsedFacetFilters); err != nil {
							return fmt.Errorf("parsing --facet-filters JSON: %w", err)
						}
						bodyMap["facetFilters"] = parsedFacetFilters
					} else {
						bodyMap["facetFilters"] = bodyFacetFilters
					}
				}
				if cmd.Flags().Changed("filters") || bodyFilters != "" {
					bodyMap["filters"] = bodyFilters
				}
				if cmd.Flags().Changed("inside-bounding-box") || bodyInsideBoundingBox != "" {
					if looksLikeJSONComposite(bodyInsideBoundingBox) {
						var parsedInsideBoundingBox any
						if err := json.Unmarshal([]byte(bodyInsideBoundingBox), &parsedInsideBoundingBox); err != nil {
							return fmt.Errorf("parsing --inside-bounding-box JSON: %w", err)
						}
						bodyMap["insideBoundingBox"] = parsedInsideBoundingBox
					} else {
						bodyMap["insideBoundingBox"] = bodyInsideBoundingBox
					}
				}
				if cmd.Flags().Changed("inside-polygon") || bodyInsidePolygon != "" {
					var parsedInsidePolygon any
					if err := json.Unmarshal([]byte(bodyInsidePolygon), &parsedInsidePolygon); err != nil {
						return fmt.Errorf("parsing --inside-polygon JSON: %w", err)
					}
					asArray, ok := parsedInsidePolygon.([]any)
					if !ok {
						return fmt.Errorf("--inside-polygon must be a JSON array, got JSON %T", parsedInsidePolygon)
					}
					bodyMap["insidePolygon"] = asArray
				}
				if cmd.Flags().Changed("numeric-filters") || bodyNumericFilters != "" {
					if looksLikeJSONComposite(bodyNumericFilters) {
						var parsedNumericFilters any
						if err := json.Unmarshal([]byte(bodyNumericFilters), &parsedNumericFilters); err != nil {
							return fmt.Errorf("parsing --numeric-filters JSON: %w", err)
						}
						bodyMap["numericFilters"] = parsedNumericFilters
					} else {
						bodyMap["numericFilters"] = bodyNumericFilters
					}
				}
				if cmd.Flags().Changed("tag-filters") || bodyTagFilters != "" {
					if looksLikeJSONComposite(bodyTagFilters) {
						var parsedTagFilters any
						if err := json.Unmarshal([]byte(bodyTagFilters), &parsedTagFilters); err != nil {
							return fmt.Errorf("parsing --tag-filters JSON: %w", err)
						}
						bodyMap["tagFilters"] = parsedTagFilters
					} else {
						bodyMap["tagFilters"] = bodyTagFilters
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "delete-by-query", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "delete-by-query", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "delete-by-query", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "delete-by-query", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "delete-by-query", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "delete-by-query",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "delete-by-query", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "delete-by-query", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAroundLatLng, "around-lat-lng", "", "Coordinates for the center of a circle, expressed as a comma-separated string of latitude and longitude.")
	cmd.Flags().StringVar(&bodyAroundRadius, "around-radius", "", "Maximum radius for a search around a central location.")
	cmd.Flags().StringVar(&bodyFacetFilters, "facet-filters", "", "Filter the search by facet values, so that only records with the same facet values are retrieved.")
	cmd.Flags().StringVar(&bodyFilters, "filters", "", "Filter expression to only include items that match the filter criteria in the response.")
	cmd.Flags().StringVar(&bodyInsideBoundingBox, "inside-bounding-box", "", "Inside bounding box")
	cmd.Flags().StringVar(&bodyInsidePolygon, "inside-polygon", "", "Coordinates of a polygon in which to search. Polygons are defined by 3 to 10,000 points.")
	cmd.Flags().StringVar(&bodyNumericFilters, "numeric-filters", "", "Filter by numeric facets.")
	cmd.Flags().StringVar(&bodyTagFilters, "tag-filters", "", "Filter the search by values of the special `_tags` attribute.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
