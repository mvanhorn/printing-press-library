version: 2
project_name: jules-pp-cli
changelog:
  disable: true
builds:
  - id: jules-pp-cli
    main: ./cmd/jules-pp-cli
    binary: jules-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/developer-tools/jules/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: jules-pp-mcp
    main: ./cmd/jules-pp-mcp
    binary: jules-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: jules-pp-cli
    repository:
      owner: wryenmeek
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Jules Planning & Progress API for async coding tasks"
    install: |
      bin.install "jules-pp-cli"
      bin.install "jules-pp-mcp"
