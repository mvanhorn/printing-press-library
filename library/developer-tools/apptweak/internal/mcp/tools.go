// Copyright 2026 Ryan Kelley and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"math"
	"os"
	"path/filepath"
	"strconv"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/apptweak/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/apptweak/internal/client"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/apptweak/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/apptweak/internal/config"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/apptweak/internal/mcp/bound"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/apptweak/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/developer-tools/apptweak/internal/store"
)

const (
	// MCP hosts can fan out tool calls faster than a human CLI session.
	// Keep them on the same polite-client limiter path instead of disabling
	// pacing with rate=0; users can still tune human CLI calls with --rate-limit.
	defaultMCPRateLimit = 2
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("apps_get-category-ranking-current",
			mcplib.WithDescription("Current free, paid, or grossing chart position for one or more apps. Required: apps, country, device. Returns the AppsGetCategoryRankingCurrentResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/category-rankings/current.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_get-category-ranking-history",
			mcplib.WithDescription("Track category chart position over time. Required: apps, country, device, start_date, end_date. Returns the AppsGetCategoryRankingHistoryResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("start_date", mcplib.Required(), mcplib.Description("Start date")),
			mcplib.WithString("end_date", mcplib.Required(), mcplib.Description("End date")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/category-rankings/history.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_get-featured-content",
			mcplib.WithDescription("When and in which editorial lists an app was featured (iOS only). Required: apps, country. Optional: start_date, end_date. Returns the AppsGetFeaturedContentResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("start_date", mcplib.Description("Start date")),
			mcplib.WithString("end_date", mcplib.Description("End date")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/featured.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_get-keyword-rankings-current",
			mcplib.WithDescription("Rank position for a list of keywords across a country and device. Required: apps, keywords, country, device. Optional: language. Returns the AppsGetKeywordRankingsCurrentResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("keywords", mcplib.Required(), mcplib.Description("Comma-separated keywords")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/keyword-rankings/current.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "keywords", WireName: "keywords", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_get-keyword-rankings-history",
			mcplib.WithDescription("Track keyword rank positions over time — core ASO monitoring endpoint. Required: apps, keywords, country, device, start_date, end_date. Optional: language. Returns the AppsGetKeywordRankingsHistoryResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("keywords", mcplib.Required(), mcplib.Description("Keywords")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("start_date", mcplib.Required(), mcplib.Description("Start date")),
			mcplib.WithString("end_date", mcplib.Required(), mcplib.Description("End date")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/keyword-rankings/history.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "keywords", WireName: "keywords", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_get-metadata",
			mcplib.WithDescription("Title, subtitle, description, screenshots, ratings, developer info, languages, in-app purchases, and more. Required: apps, country, device. Optional: language. Returns the AppsGetMetadataResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Comma-separated app IDs")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("ISO Alpha-2 country code")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Target device")),
			mcplib.WithString("language", mcplib.Description("ISO Alpha-2 language code")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/metadata.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_get-metadata-history",
			mcplib.WithDescription("Track title, subtitle, description changes over a date range. Required: apps, country, device, start_date, end_date. Optional: language. Returns the AppsGetMetadataHistoryResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("start_date", mcplib.Required(), mcplib.Description("Start date YYYY-MM-DD")),
			mcplib.WithString("end_date", mcplib.Required(), mcplib.Description("End date YYYY-MM-DD")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/metadata/history.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_get-metrics-current",
			mcplib.WithDescription("Estimated downloads, revenues, and App Power score. Required: apps, country, device. Returns the AppsGetMetricsCurrentResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/metrics/current.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_get-metrics-history",
			mcplib.WithDescription("Downloads, revenues, and App Power score over a date range. Required: apps, country, device, start_date, end_date. Returns the AppsGetMetricsHistoryResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("start_date", mcplib.Required(), mcplib.Description("Start date")),
			mcplib.WithString("end_date", mcplib.Required(), mcplib.Description("End date")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/metrics/history.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_get-paid-keywords",
			mcplib.WithDescription("Keywords an app bids on in Apple Search Ads — competitor paid strategy intelligence. Required: apps, country. Optional: language. Returns the AppsGetPaidKeywordsResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/paid-keywords.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_get-review-stats",
			mcplib.WithDescription("Rating distribution, average rating, review count over a date range. Required: apps, country, device. Optional: start_date, end_date, language. Returns the AppsGetReviewStatsResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("start_date", mcplib.Description("Start date")),
			mcplib.WithString("end_date", mcplib.Description("End date")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/reviews/stats.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_get-reviews-displayed",
			mcplib.WithDescription("Reviews currently displayed in the App Store or Google Play for an app. Required: apps, country, device. Optional: language, sort. Returns the AppsGetReviewsDisplayedResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithString("sort", mcplib.Description("Sort")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/reviews/displayed.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_get-twin",
			mcplib.WithDescription("Find the Android equivalent of an iOS app or vice versa. Required: apps, device. Returns the AppsGetTwinResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/twin.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_search-reviews",
			mcplib.WithDescription("Search through review text, filter by rating or date range. Requires tracked app. Required: apps, country, device. Optional: word, rating, date_from (plus 2 more). Returns the AppsSearchReviewsResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Up to 5 app IDs (requires tracked app)")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("word", mcplib.Description("Keyword to search in review text")),
			mcplib.WithNumber("rating", mcplib.Description("Filter by star rating")),
			mcplib.WithString("date_from", mcplib.Description("Date from")),
			mcplib.WithString("date_to", mcplib.Description("Date to")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/apps/reviews/search.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "word", WireName: "word", Location: "query"}, {PublicName: "rating", WireName: "rating", Location: "query"}, {PublicName: "date_from", WireName: "date_from", Location: "query"}, {PublicName: "date_to", WireName: "date_to", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("charts_get-category-metrics",
			mcplib.WithDescription("Aggregate downloads, revenue, and top performers for an App Store category. Required: country, device. Optional: category. Returns the ChartsGetCategoryMetricsResponse."),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("category", mcplib.Description("Category")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/charts/category-metrics.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "category", WireName: "category", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("charts_get-conversion-rate-benchmarks",
			mcplib.WithDescription("Industry average conversion rates by category — benchmark your app's store listing performance. Required: country, device. Optional: category. Returns the ChartsGetConversionRateBenchmarksResponse."),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("category", mcplib.Description("Category")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/charts/conversion-rate-benchmarks.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "category", WireName: "category", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("charts_get-dna-current",
			mcplib.WithDescription("AppTweak's proprietary DNA sub-genre taxonomy charts — more granular than official categories. Required: dna, country, device. Optional: chart_type. Returns the ChartsGetDnaCurrentResponse."),
			mcplib.WithString("dna", mcplib.Required(), mcplib.Description("DNA segment ID")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("chart_type", mcplib.Description("Chart type")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/charts/dna/current.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "dna", WireName: "dna", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "chart_type", WireName: "chart_type", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("charts_get-top-current",
			mcplib.WithDescription("Current free, paid, or grossing chart for a category and country. Required: country, device, chart_type. Optional: category. Returns the ChartsGetTopCurrentResponse."),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("chart_type", mcplib.Required(), mcplib.Description("Chart type")),
			mcplib.WithString("category", mcplib.Description("App Store category ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/charts/top/current.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "chart_type", WireName: "chart_type", Location: "query"}, {PublicName: "category", WireName: "category", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("charts_get-top-history",
			mcplib.WithDescription("How the free/paid/grossing chart changed over a date range. Required: country, device, chart_type, start_date, end_date. Optional: category. Returns the ChartsGetTopHistoryResponse."),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("chart_type", mcplib.Required(), mcplib.Description("Chart type")),
			mcplib.WithString("start_date", mcplib.Required(), mcplib.Description("Start date")),
			mcplib.WithString("end_date", mcplib.Required(), mcplib.Description("End date")),
			mcplib.WithString("category", mcplib.Description("Category")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/charts/top/history.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "chart_type", WireName: "chart_type", Location: "query"}, {PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "category", WireName: "category", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_get-live-search-ads-current",
			mcplib.WithDescription("Apps running Apple Search Ads for a keyword right now. Required: keywords, country. Optional: language. Returns the KeywordsGetLiveSearchAdsCurrentResponse."),
			mcplib.WithString("keywords", mcplib.Required(), mcplib.Description("Keywords")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords/search/ads/current.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "keywords", WireName: "keywords", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_get-live-search-current",
			mcplib.WithDescription("Apps appearing in search results right now for a given keyword. Required: keywords, country, device. Optional: language. Returns the KeywordsGetLiveSearchCurrentResponse."),
			mcplib.WithString("keywords", mcplib.Required(), mcplib.Description("Keywords")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords/search/current.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "keywords", WireName: "keywords", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_get-metrics-current",
			mcplib.WithDescription("Search volume, difficulty score, and brand score for keywords. Required: keywords, country, device. Optional: language. Returns the KeywordsGetMetricsCurrentResponse."),
			mcplib.WithString("keywords", mcplib.Required(), mcplib.Description("Keywords")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords/metrics/current.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "keywords", WireName: "keywords", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_get-metrics-history",
			mcplib.WithDescription("Search volume and difficulty score trends over time. Required: keywords, country, device, start_date, end_date. Optional: language. Returns the KeywordsGetMetricsHistoryResponse."),
			mcplib.WithString("keywords", mcplib.Required(), mcplib.Description("Keywords")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("start_date", mcplib.Required(), mcplib.Description("Start date")),
			mcplib.WithString("end_date", mcplib.Required(), mcplib.Description("End date")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords/metrics/history.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "keywords", WireName: "keywords", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_get-search-history",
			mcplib.WithDescription("How search results for a keyword changed over time. Required: keywords, country, device, start_date, end_date. Optional: language. Returns the KeywordsGetSearchHistoryResponse."),
			mcplib.WithString("keywords", mcplib.Required(), mcplib.Description("Keywords")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("start_date", mcplib.Required(), mcplib.Description("Start date")),
			mcplib.WithString("end_date", mcplib.Required(), mcplib.Description("End date")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords/search/history.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "keywords", WireName: "keywords", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_get-share-of-voice",
			mcplib.WithDescription("Ad impression share across all bidders for a keyword — identify dominant advertisers. Required: keywords, country. Optional: language. Returns the KeywordsGetShareOfVoiceResponse."),
			mcplib.WithString("keywords", mcplib.Required(), mcplib.Description("Keywords")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords/share-of-voice.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "keywords", WireName: "keywords", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_get-suggestions-by-app",
			mcplib.WithDescription("Keyword ideas based on an app's install patterns — top installs, trending, or volume change. Required: apps, country, device. Optional: type, language. Returns the KeywordsGetSuggestionsByAppResponse."),
			mcplib.WithString("apps", mcplib.Required(), mcplib.Description("Apps")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("type", mcplib.Description("Type")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords/suggestions/by-app.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "apps", WireName: "apps", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("keywords_get-suggestions-by-category",
			mcplib.WithDescription("Highest-volume and trending keywords for an App Store category. Required: category, country, device. Optional: language. Returns the KeywordsGetSuggestionsByCategoryResponse."),
			mcplib.WithString("category", mcplib.Required(), mcplib.Description("Category")),
			mcplib.WithString("country", mcplib.Required(), mcplib.Description("Country")),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/keywords/suggestions/by-category.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "category", WireName: "category", Location: "query"}, {PublicName: "country", WireName: "country", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("utils_get-dna-list",
			mcplib.WithDescription("AppTweak's proprietary DNA sub-genre taxonomy identifiers. Returns the UtilsGetDnaListResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/utils/dnas.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("utils_get-supported-countries",
			mcplib.WithDescription("List all countries supported for a given store. Returns the UtilsGetSupportedCountriesResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/utils/countries.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("utils_get-supported-languages",
			mcplib.WithDescription("List all supported language codes. Returns the UtilsGetSupportedLanguagesResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/utils/languages.json", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Synced records live in resources(resource_type, id, data); filter by resource_type and use json_extract on data, e.g. SELECT json_extract(data,'$.name') FROM resources WHERE resource_type='apps'.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

type mcpPageConfig struct {
	CursorParam    string
	NextCursorPath string
}

func formatMCPParamValue(v any) string {
	switch tv := v.(type) {
	case string:
		return tv
	case bool:
		return strconv.FormatBool(tv)
	case float64:
		if math.IsNaN(tv) || math.IsInf(tv, 0) {
			return strconv.FormatFloat(tv, 'f', -1, 64)
		}
		if math.Trunc(tv) == tv && math.Abs(tv) < 1e15 {
			return strconv.FormatInt(int64(tv), 10)
		}
		return strconv.FormatFloat(tv, 'f', -1, 64)
	case float32:
		f := float64(tv)
		if math.IsNaN(f) || math.IsInf(f, 0) {
			return strconv.FormatFloat(f, 'f', -1, 32)
		}
		if math.Trunc(f) == f && math.Abs(f) < 1e15 {
			return strconv.FormatInt(int64(f), 10)
		}
		return strconv.FormatFloat(f, 'f', -1, 32)
	default:
		// Composite values (a native []any / map[string]any from an array or
		// object param) reach this path when bound to a query or path slot;
		// JSON-encode them so the wire value is valid JSON rather than Go's
		// "[a b c]" / "map[...]" rendering. Body params never come through
		// here — they are stored natively in bodyArgs and marshalled there.
		if b, err := json.Marshal(v); err == nil {
			return string(b)
		}
		return fmt.Sprintf("%v", v)
	}
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, pageConfig mcpPageConfig, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		mcpCursor := ""
		if pageConfig.CursorParam != "" {
			knownArgs["cursor"] = true
			if v, ok := args["cursor"]; ok {
				s, ok := v.(string)
				if !ok {
					return mcplib.NewToolResultError("cursor must be an opaque string returned by a previous MCP response"), nil
				}
				mcpCursor = s
				upstreamCursor, err := bound.UpstreamCursor(s)
				if err != nil {
					return mcplib.NewToolResultError(err.Error()), nil
				}
				if upstreamCursor != "" {
					params[pageConfig.CursorParam] = upstreamCursor
				}
			}
		}
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, formatMCPParamValue(v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = formatMCPParamValue(v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, formatMCPParamValue(v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = formatMCPParamValue(v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key with: export APPTWEAK_API_KEY=\"your-token-here\"" +
					"\n      Run 'apptweak-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set your API key with: export APPTWEAK_API_KEY=\"your-token-here\"" +
					"\n      Run 'apptweak-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource. Check that they have the required permissions and match the API's expected auth scheme." +
					"\n      Set your API key with: export APPTWEAK_API_KEY=\"your-token-here\"" +
					"\n      Run 'apptweak-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		if binaryResponse {
			encoded := base64.StdEncoding.EncodeToString(data)
			out, err := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      encoded,
				"byte_count":       len(data),
			})
			if err != nil {
				return mcplib.NewToolResultError(fmt.Sprintf("encoding binary result: %v", err)), nil
			}
			if len(out) > bound.MaxBytes {
				return mcplib.NewToolResultError(fmt.Sprintf("binary response is too large for MCP text output: %d response bytes encode to %d base64 bytes and %d MCP result bytes, exceeding the %d byte budget. Use the companion CLI command with --output <file> to save the payload locally.", len(data), len(encoded), len(out), bound.MaxBytes)), nil
			}
			return mcplib.NewToolResultText(string(out)), nil
		}
		if pageConfig.CursorParam != "" {
			return mcpToolPageResultText(method, data, pageConfig, mcpCursor), nil
		}
		return mcpToolResultText(method, data), nil
	}
}

func mcpToolResultText(method string, data json.RawMessage) *mcplib.CallToolResult {
	return mcplib.NewToolResultText(bound.EndpointResponse(method, data))
}

func mcpToolPageResultText(method string, data json.RawMessage, pageConfig mcpPageConfig, cursor string) *mcplib.CallToolResult {
	return mcplib.NewToolResultText(bound.EndpointPageResponse(method, data, bound.PageOptions{
		Cursor:         cursor,
		CursorParam:    pageConfig.CursorParam,
		NextCursorPath: pageConfig.NextCursorPath,
	}))
}

func newMCPClient() (*client.Client, error) {
	cfg, err := newMCPConfig()
	if err != nil {
		return nil, err
	}
	return newMCPClientFromConfig(cfg), nil
}

func newMCPConfig() (*config.Config, error) {
	cfg, err := config.Load("")
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	return cfg, nil
}

func newMCPClientFromConfig(cfg *config.Config) *client.Client {
	c := client.New(cfg, 60*time.Second, defaultMCPRateLimit)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c
}

func mcpDBPath() (string, error) {
	dir, err := cliutil.DataDir()
	if err != nil {
		return "", err
	}
	return filepath.Join(dir, "data.db"), nil
}

type mcpStoreStatusKind string

const (
	mcpStoreStatusEmpty mcpStoreStatusKind = "empty"
	mcpStoreStatusReady mcpStoreStatusKind = "ready"
)

func openMCPReadOnlyStore(path string) (*store.Store, *mcplib.CallToolResult) {
	if _, err := os.Stat(path); err != nil {
		if os.IsNotExist(err) {
			return nil, mcplib.NewToolResultError(mcpMissingStoreMessage(path))
		}
		return nil, mcplib.NewToolResultError(fmt.Sprintf("checking local data store %s: %v", path, err))
	}
	db, err := store.OpenReadOnly(path)
	if err != nil {
		return nil, mcplib.NewToolResultError(fmt.Sprintf("opening local data store %s: %v. Run apptweak-pp-cli sync to refresh the store, or use live endpoint MCP tools for unsynced data.", path, err))
	}
	return db, nil
}

func mcpMissingStoreMessage(path string) string {
	return fmt.Sprintf("No local data store found at %s. Run apptweak-pp-cli sync before using MCP search/sql, or use live endpoint MCP tools for unsynced data.", path)
}

func mcpStoreStatus(db *store.Store) (mcpStoreStatusKind, error) {
	status, err := db.Status()
	if err != nil {
		return "", err
	}
	if len(status) == 0 {
		return mcpStoreStatusEmpty, nil
	}
	return mcpStoreStatusReady, nil
}

func mcpEmptyStoreNextStep() string {
	return "Run apptweak-pp-cli sync to populate the local SQLite store before using MCP search/sql."
}

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	path, err := mcpDBPath()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("resolving database: %v", err)), nil
	}
	db, toolErr := openMCPReadOnlyStore(path)
	if toolErr != nil {
		return toolErr, nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}
	storeStatus, err := mcpStoreStatus(db)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading store status: %v", err)), nil
	}

	return toolResultJSON(mcpSearchEnvelope(results, storeStatus))
}

func mcpSearchEnvelope(results []json.RawMessage, storeStatus mcpStoreStatusKind) map[string]any {
	if results == nil {
		results = []json.RawMessage{}
	}
	out := map[string]any{
		"count":        len(results),
		"results":      results,
		"store_status": storeStatus,
		"resumable":    false,
	}
	if len(results) == 0 {
		if storeStatus == mcpStoreStatusEmpty {
			out["next_step"] = mcpEmptyStoreNextStep()
		} else {
			out["next_step"] = "No local search matches. Try a broader query, a lower-specificity FTS expression, or sync again if data may be stale."
		}
	}
	return out
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate rejects multi-statement input, then applies an allowlist (SELECT or
// WITH only) AFTER stripping the leading whitespace, line comments, block
// comments, and semicolons that SQLite itself ignores before parsing. A naive
// HasPrefix check on a keyword blocklist is bypassable by prefixing the
// dangerous statement with "/* x */" or "-- x\n"; a naive leading-keyword
// allowlist is bypassable by appending "; ATTACH DATABASE ...". Combined with
// the empirical fact that modernc.org/sqlite's mode=ro does NOT block VACUUM
// INTO (writes a snapshot to a new file) or ATTACH DATABASE (opens a separate
// writable handle), either bypass produces silent exfiltration to an
// attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	stripped := stripLeadingSQLNoise(query)
	if hasTrailingSQLStatement(stripped) {
		return fmt.Errorf("only a single SELECT or WITH statement is allowed")
	}
	upper := strings.ToUpper(stripped)
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

// hasTrailingSQLStatement reports whether query contains a statement
// terminator followed by more executable SQL. A trailing semicolon is allowed;
// a second statement is not. Semicolons inside string literals, quoted
// identifiers, bracket identifiers, and comments are ignored to match SQLite's
// parser shape closely enough for this security gate.
func hasTrailingSQLStatement(query string) bool {
	inSingle := false
	inDouble := false
	inBacktick := false
	inBracket := false
	inLineComment := false
	inBlockComment := false

	for i := 0; i < len(query); i++ {
		ch := query[i]
		next := byte(0)
		if i+1 < len(query) {
			next = query[i+1]
		}

		switch {
		case inLineComment:
			if ch == '\n' {
				inLineComment = false
			}
			continue
		case inBlockComment:
			if ch == '*' && next == '/' {
				inBlockComment = false
				i++
			}
			continue
		case inSingle:
			if ch == '\'' {
				if next == '\'' {
					i++
					continue
				}
				inSingle = false
			}
			continue
		case inDouble:
			if ch == '"' {
				if next == '"' {
					i++
					continue
				}
				inDouble = false
			}
			continue
		case inBacktick:
			if ch == '`' {
				if next == '`' {
					i++
					continue
				}
				inBacktick = false
			}
			continue
		case inBracket:
			if ch == ']' {
				inBracket = false
			}
			continue
		}

		switch {
		case ch == '-' && next == '-':
			inLineComment = true
			i++
		case ch == '/' && next == '*':
			inBlockComment = true
			i++
		case ch == '\'':
			inSingle = true
		case ch == '"':
			inDouble = true
		case ch == '`':
			inBacktick = true
		case ch == '[':
			inBracket = true
		case ch == ';':
			if stripLeadingSQLNoise(query[i+1:]) != "" {
				return true
			}
			return false
		}
	}
	return false
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	path, err := mcpDBPath()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("resolving database: %v", err)), nil
	}
	db, toolErr := openMCPReadOnlyStore(path)
	if toolErr != nil {
		return toolErr, nil
	}
	defer db.Close()

	rows, err := db.DB().QueryContext(ctx, query)
	if err != nil {
		return mcplib.NewToolResultError(mcpSQLQueryError(err)), nil
	}
	defer rows.Close()

	cols, err := rows.Columns()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading columns: %v", err)), nil
	}
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		if err := rows.Scan(ptrs...); err != nil {
			return mcplib.NewToolResultError(fmt.Sprintf("scanning row: %v", err)), nil
		}
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}
	// rows.Next() stops on a mid-iteration error without failing the loop, so
	// skipping rows.Err() would return a truncated result set as success.
	if err := rows.Err(); err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading rows: %v", err)), nil
	}
	storeStatus, err := mcpStoreStatus(db)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading store status: %v", err)), nil
	}

	return toolResultJSON(mcpSQLEnvelope(results, cols, storeStatus))
}

func mcpSQLEnvelope(rows []map[string]any, columns []string, storeStatus mcpStoreStatusKind) map[string]any {
	if rows == nil {
		rows = []map[string]any{}
	}
	out := map[string]any{
		"count":        len(rows),
		"columns":      columns,
		"rows":         rows,
		"store_status": storeStatus,
		"resumable":    false,
	}
	if len(rows) == 0 {
		if storeStatus == mcpStoreStatusEmpty {
			out["next_step"] = mcpEmptyStoreNextStep()
		} else {
			out["next_step"] = "The read-only SQL query returned no rows. Check resource_type filters, json_extract paths, or run sync again if data may be stale."
		}
	}
	return out
}

func mcpSQLQueryError(err error) string {
	msg := err.Error()
	if strings.Contains(strings.ToLower(msg), "no such table") {
		return fmt.Sprintf("query failed: %v. Synced records live in resources(resource_type, id, data), not one SQL table per resource. Filter by resource_type, for example resource_type='apps', and read JSON fields with json_extract(data,'$.field').", err)
	}
	return fmt.Sprintf("query failed: %v", err)
}

// toolResultJSON renders v as the indented JSON body of an MCP text result,
// surfacing a marshal failure as a tool error instead of empty content.
func toolResultJSON(v any) (*mcplib.CallToolResult, error) {
	text, err := bound.JSON(v)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("encoding result: %v", err)), nil
	}
	return mcplib.NewToolResultText(text), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	paths := map[string]string{}
	if dir, err := cliutil.ConfigDir(); err == nil {
		paths["config_dir"] = dir
	}
	if dir, err := cliutil.DataDir(); err == nil {
		paths["data_dir"] = dir
	}
	if dir, err := cliutil.StateDir(); err == nil {
		paths["state_dir"] = dir
	}
	if dir, err := cliutil.CacheDir(); err == nil {
		paths["cache_dir"] = dir
	}
	ctx := map[string]any{
		"api":         "apptweak",
		"description": "Direct access to more than 5 million apps from Apple App Store and Google Play Store in 100+ countries.",
		"archetype":   "generic",
		"tool_count":  30,
		"paths":       paths,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion apptweak-pp-cli binary.",
		"auth": map[string]any{
			"type": "api_key",
			"env_vars": []map[string]any{
				{
					"name":        "APPTWEAK_API_KEY",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "apps",
				"description": "Manage apps",
				"endpoints":   []string{"get-category-ranking-current", "get-category-ranking-history", "get-featured-content", "get-keyword-rankings-current", "get-keyword-rankings-history", "get-metadata", "get-metadata-history", "get-metrics-current", "get-metrics-history", "get-paid-keywords", "get-review-stats", "get-reviews-displayed", "get-twin", "search-reviews"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "charts",
				"description": "Manage charts",
				"endpoints":   []string{"get-category-metrics", "get-conversion-rate-benchmarks", "get-dna-current", "get-top-current", "get-top-history"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "keywords",
				"description": "Manage keywords",
				"endpoints":   []string{"get-live-search-ads-current", "get-live-search-current", "get-metrics-current", "get-metrics-history", "get-search-history", "get-share-of-voice", "get-suggestions-by-app", "get-suggestions-by-category"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "utils",
				"description": "Manage utils",
				"endpoints":   []string{"get-dna-list", "get-supported-countries", "get-supported-languages"},
				"syncable":    true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use start_date for incremental fetches (filter by modification time).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Cross-Country Keyword Rank Fan-Out", "command": "keywords rank-fanout", "description": "Check keyword rankings across many countries in one command — no MCP tool or competitor offers multi-country fan-out.", "rationale": "Requires fanning out N API calls across a country list and aggregating results locally; only possible with a CLI that owns the loop.", "via": "mcp-command-mirror"},
			{"name": "Competitor Metadata Diff", "command": "apps metadata-diff", "description": "Compare metadata between two app versions or two competitor apps to spot ASO experiments.", "rationale": "Requires fetching two metadata snapshots and computing field-level diffs; only possible with local persistence.", "via": "mcp-command-mirror"},
			{"name": "Keyword Basket Tracker", "command": "keywords track", "description": "Sync and alert on ranking changes for a saved keyword basket across apps and countries.", "rationale": "Requires a local SQLite store to persist rank history and compute deltas between syncs.", "via": "mcp-command-mirror"},
			{"name": "Review Sentiment Pipeline", "command": "reviews sentiment", "description": "Aggregate review ratings by keyword term — find what topics drive 1-star vs 5-star reviews.", "rationale": "Requires searching reviews by keyword, grouping by rating, and computing sentiment ratios locally.", "via": "mcp-command-mirror"},
			{"name": "Paid vs Organic Keyword Gap", "command": "keywords paid-organic-gap", "description": "Find keywords a competitor bids on where your app doesn't rank organically — ASA opportunity map.", "rationale": "Requires fetching paid keywords for competitor + organic rankings for own app and computing the set difference locally.", "via": "mcp-command-mirror"},
			{"name": "Credits Monitor", "command": "credits status", "description": "Check remaining API credits and burn rate before running expensive bulk queries.", "rationale": "Requires the AppTweak account API endpoint plus local tracking of credit consumption per session.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Cross-Country Keyword Rank Fan-Out", "insight": ""},
			{"topic": "Competitor Metadata Diff", "insight": ""},
			{"topic": "Keyword Basket Tracker", "insight": ""},
			{"topic": "Review Sentiment Pipeline", "insight": ""},
			{"topic": "Paid vs Organic Keyword Gap", "insight": ""},
			{"topic": "Credits Monitor", "insight": ""},
		},
	}
	return toolResultJSON(ctx)
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
