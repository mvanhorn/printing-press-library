version: 2
project_name: npm-pp-cli
changelog:
  disable: true
builds:
  - id: npm-pp-cli
    main: ./cmd/npm-pp-cli
    binary: npm-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/developer-tools/npm/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: npm-pp-mcp
    main: ./cmd/npm-pp-mcp
    binary: npm-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: npm-pp-cli
    repository:
      owner: hiten-shah
      name: homebrew-tap
    homepage: "https://github.com/hiten-shah/npm-pp-cli"
    description: "NPM Registry Open APIs"
    install: |
      bin.install "npm-pp-cli"
      bin.install "npm-pp-mcp"
