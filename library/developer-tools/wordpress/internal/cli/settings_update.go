// Copyright 2026 bobe and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newSettingsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyPublicPostPreviewExpirationTime int
	var bodyWordpressApiKey string
	var bodyTitle string
	var bodyDescription string
	var bodyTimezone string
	var bodyDateFormat string
	var bodyTimeFormat string
	var bodyStartOfWeek int
	var bodyLanguage string
	var bodyUseSmilies bool
	var bodyDefaultCategory int
	var bodyDefaultPostFormat string
	var bodyPostsPerPage int
	var bodyShowOnFront string
	var bodyPageOnFront int
	var bodyPageForPosts int
	var bodyDefaultPingStatus string
	var bodyDefaultCommentStatus string
	var bodySiteLogo int
	var bodySiteIcon int
	var bodyJetpackSocialImageGeneratorSettings string
	var bodyJetpackSocialUtmSettings string
	var bodyJetpackSocialShowPricingPage bool
	var bodyJetpackSocialNote bool
	var bodyJetpackSocialNotesConfig string
	var bodyJetpackSocialMessageTemplate string
	var bodyCookieConsentTemplate string
	var bodyBlogrollRecommendations string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "",
		Example:     "  wordpress-pp-cli settings update",
		Annotations: map[string]string{"pp:endpoint": "settings.update", "pp:method": "POST", "pp:path": "/wp/v2/settings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/wp/v2/settings"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if bodyPublicPostPreviewExpirationTime != 0 {
					bodyMap["public_post_preview_expiration_time"] = bodyPublicPostPreviewExpirationTime
				}
				if bodyWordpressApiKey != "" {
					bodyMap["wordpress_api_key"] = bodyWordpressApiKey
				}
				if bodyTitle != "" {
					bodyMap["title"] = bodyTitle
				}
				if bodyDescription != "" {
					bodyMap["description"] = bodyDescription
				}
				if bodyTimezone != "" {
					bodyMap["timezone"] = bodyTimezone
				}
				if bodyDateFormat != "" {
					bodyMap["date_format"] = bodyDateFormat
				}
				if bodyTimeFormat != "" {
					bodyMap["time_format"] = bodyTimeFormat
				}
				if bodyStartOfWeek != 0 {
					bodyMap["start_of_week"] = bodyStartOfWeek
				}
				if bodyLanguage != "" {
					bodyMap["language"] = bodyLanguage
				}
				if cmd.Flags().Changed("use-smilies") {
					bodyMap["use_smilies"] = bodyUseSmilies
				}
				if bodyDefaultCategory != 0 {
					bodyMap["default_category"] = bodyDefaultCategory
				}
				if bodyDefaultPostFormat != "" {
					bodyMap["default_post_format"] = bodyDefaultPostFormat
				}
				if bodyPostsPerPage != 0 {
					bodyMap["posts_per_page"] = bodyPostsPerPage
				}
				if bodyShowOnFront != "" {
					bodyMap["show_on_front"] = bodyShowOnFront
				}
				if bodyPageOnFront != 0 {
					bodyMap["page_on_front"] = bodyPageOnFront
				}
				if bodyPageForPosts != 0 {
					bodyMap["page_for_posts"] = bodyPageForPosts
				}
				if bodyDefaultPingStatus != "" {
					bodyMap["default_ping_status"] = bodyDefaultPingStatus
				}
				if bodyDefaultCommentStatus != "" {
					bodyMap["default_comment_status"] = bodyDefaultCommentStatus
				}
				if bodySiteLogo != 0 {
					bodyMap["site_logo"] = bodySiteLogo
				}
				if bodySiteIcon != 0 {
					bodyMap["site_icon"] = bodySiteIcon
				}
				if bodyJetpackSocialImageGeneratorSettings != "" {
					bodyMap["jetpack_social_image_generator_settings"] = bodyJetpackSocialImageGeneratorSettings
				}
				if bodyJetpackSocialUtmSettings != "" {
					bodyMap["jetpack_social_utm_settings"] = bodyJetpackSocialUtmSettings
				}
				if cmd.Flags().Changed("jetpack-social-show-pricing-page") {
					bodyMap["jetpack-social_show_pricing_page"] = bodyJetpackSocialShowPricingPage
				}
				if cmd.Flags().Changed("jetpack-social-note") {
					bodyMap["jetpack-social-note"] = bodyJetpackSocialNote
				}
				if bodyJetpackSocialNotesConfig != "" {
					bodyMap["jetpack_social_notes_config"] = bodyJetpackSocialNotesConfig
				}
				if bodyJetpackSocialMessageTemplate != "" {
					bodyMap["jetpack_social_message_template"] = bodyJetpackSocialMessageTemplate
				}
				if bodyCookieConsentTemplate != "" {
					bodyMap["cookie_consent_template"] = bodyCookieConsentTemplate
				}
				if bodyBlogrollRecommendations != "" {
					bodyMap["Blogroll Recommendations"] = bodyBlogrollRecommendations
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "settings", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "settings", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "settings",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().IntVar(&bodyPublicPostPreviewExpirationTime, "public-post-preview-expiration-time", 0, "Default expiration time in seconds.")
	cmd.Flags().StringVar(&bodyWordpressApiKey, "wordpress-api-key", "", "API key for the Akismet Anti-spam connector.")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "Site title.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Site tagline.")
	cmd.Flags().StringVar(&bodyTimezone, "timezone", "", "A city in the same timezone as you.")
	cmd.Flags().StringVar(&bodyDateFormat, "date-format", "", "A date format for all date strings.")
	cmd.Flags().StringVar(&bodyTimeFormat, "time-format", "", "A time format for all time strings.")
	cmd.Flags().IntVar(&bodyStartOfWeek, "start-of-week", 0, "A day number of the week that the week should start on.")
	cmd.Flags().StringVar(&bodyLanguage, "language", "", "WordPress locale code.")
	cmd.Flags().BoolVar(&bodyUseSmilies, "use-smilies", false, "Convert emoticons like :-) and :-P to graphics on display.")
	cmd.Flags().IntVar(&bodyDefaultCategory, "default-category", 0, "Default post category.")
	cmd.Flags().StringVar(&bodyDefaultPostFormat, "default-post-format", "", "Default post format.")
	cmd.Flags().IntVar(&bodyPostsPerPage, "posts-per-page", 0, "Blog pages show at most.")
	cmd.Flags().StringVar(&bodyShowOnFront, "show-on-front", "", "What to show on the front page")
	cmd.Flags().IntVar(&bodyPageOnFront, "page-on-front", 0, "The ID of the page that should be displayed on the front page")
	cmd.Flags().IntVar(&bodyPageForPosts, "page-for-posts", 0, "The ID of the page that should display the latest posts")
	cmd.Flags().StringVar(&bodyDefaultPingStatus, "default-ping-status", "", "Allow link notifications from other blogs (pingbacks and trackbacks) on new articles.")
	cmd.Flags().StringVar(&bodyDefaultCommentStatus, "default-comment-status", "", "Allow people to submit comments on new posts.")
	cmd.Flags().IntVar(&bodySiteLogo, "site-logo", 0, "Site logo.")
	cmd.Flags().IntVar(&bodySiteIcon, "site-icon", 0, "Site icon.")
	cmd.Flags().StringVar(&bodyJetpackSocialImageGeneratorSettings, "jetpack-social-image-generator-settings", "", "")
	cmd.Flags().StringVar(&bodyJetpackSocialUtmSettings, "jetpack-social-utm-settings", "", "")
	cmd.Flags().BoolVar(&bodyJetpackSocialShowPricingPage, "jetpack-social-show-pricing-page", false, "")
	cmd.Flags().BoolVar(&bodyJetpackSocialNote, "jetpack-social-note", false, "")
	cmd.Flags().StringVar(&bodyJetpackSocialNotesConfig, "jetpack-social-notes-config", "", "")
	cmd.Flags().StringVar(&bodyJetpackSocialMessageTemplate, "jetpack-social-message-template", "", "")
	cmd.Flags().StringVar(&bodyCookieConsentTemplate, "cookie-consent-template", "", "")
	cmd.Flags().StringVar(&bodyBlogrollRecommendations, "blogroll-recommendations", "", "Site Recommendations (comma-separated)")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
