// Copyright 2026 bobe and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newMediaListCmd(flags *rootFlags) *cobra.Command {
	var flagContext string
	var flagPage string
	var flagPerPage int
	var flagSearch string
	var flagAfter string
	var flagModifiedAfter string
	var flagAuthor string
	var flagAuthorExclude string
	var flagBefore string
	var flagModifiedBefore string
	var flagExclude string
	var flagInclude string
	var flagSearchSemantics string
	var flagOffset string
	var flagOrder string
	var flagOrderby string
	var flagParent string
	var flagParentExclude string
	var flagSearchColumns string
	var flagSlug string
	var flagStatus string
	var flagMediaType string
	var flagMimeType string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "",
		Example:     "  wordpress-pp-cli media list",
		Annotations: map[string]string{"pp:endpoint": "media.list", "pp:method": "GET", "pp:path": "/wp/v2/media", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("context") {
				allowedContext := []string{"view", "embed", "edit"}
				validContext := false
				for _, v := range allowedContext {
					if flagContext == v {
						validContext = true
						break
					}
				}
				if !validContext {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagContext, "context", allowedContext)
				}
			}
			if cmd.Flags().Changed("search-semantics") {
				allowedSearchSemantics := []string{"exact"}
				validSearchSemantics := false
				for _, v := range allowedSearchSemantics {
					if flagSearchSemantics == v {
						validSearchSemantics = true
						break
					}
				}
				if !validSearchSemantics {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSearchSemantics, "search-semantics", allowedSearchSemantics)
				}
			}
			if cmd.Flags().Changed("order") {
				allowedOrder := []string{"asc", "desc"}
				validOrder := false
				for _, v := range allowedOrder {
					if flagOrder == v {
						validOrder = true
						break
					}
				}
				if !validOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrder, "order", allowedOrder)
				}
			}
			if cmd.Flags().Changed("orderby") {
				allowedOrderby := []string{"author", "date", "id", "include", "modified", "parent", "relevance", "slug", "include_slugs", "title"}
				validOrderby := false
				for _, v := range allowedOrderby {
					if flagOrderby == v {
						validOrderby = true
						break
					}
				}
				if !validOrderby {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrderby, "orderby", allowedOrderby)
				}
			}
			if cmd.Flags().Changed("search-columns") {
				allowedSearchColumns := []string{"post_title", "post_content", "post_excerpt"}
				validSearchColumns := false
				for _, v := range allowedSearchColumns {
					if flagSearchColumns == v {
						validSearchColumns = true
						break
					}
				}
				if !validSearchColumns {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSearchColumns, "search-columns", allowedSearchColumns)
				}
			}
			if cmd.Flags().Changed("status") {
				allowedStatus := []string{"inherit", "private", "trash"}
				validStatus := false
				for _, v := range allowedStatus {
					if flagStatus == v {
						validStatus = true
						break
					}
				}
				if !validStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagStatus, "status", allowedStatus)
				}
			}
			if cmd.Flags().Changed("media-type") {
				allowedMediaType := []string{"image", "audio", "video", "application", "text"}
				validMediaType := false
				for _, v := range allowedMediaType {
					if flagMediaType == v {
						validMediaType = true
						break
					}
				}
				if !validMediaType {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagMediaType, "media-type", allowedMediaType)
				}
			}
			path := "/wp/v2/media"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagContext != "" {
				params["context"] = formatCLIParamValue(flagContext)
			}
			if flagPage != "" {
				params["page"] = formatCLIParamValue(flagPage)
			}
			if flagPerPage != 0 {
				params["per_page"] = formatCLIParamValue(flagPerPage)
			}
			if flagSearch != "" {
				params["search"] = formatCLIParamValue(flagSearch)
			}
			if flagAfter != "" {
				params["after"] = formatCLIParamValue(flagAfter)
			}
			if flagModifiedAfter != "" {
				params["modified_after"] = formatCLIParamValue(flagModifiedAfter)
			}
			if flagAuthor != "" {
				params["author"] = formatCLIParamValue(flagAuthor)
			}
			if flagAuthorExclude != "" {
				params["author_exclude"] = formatCLIParamValue(flagAuthorExclude)
			}
			if flagBefore != "" {
				params["before"] = formatCLIParamValue(flagBefore)
			}
			if flagModifiedBefore != "" {
				params["modified_before"] = formatCLIParamValue(flagModifiedBefore)
			}
			if flagExclude != "" {
				params["exclude"] = formatCLIParamValue(flagExclude)
			}
			if flagInclude != "" {
				params["include"] = formatCLIParamValue(flagInclude)
			}
			if flagSearchSemantics != "" {
				params["search_semantics"] = formatCLIParamValue(flagSearchSemantics)
			}
			if flagOffset != "" {
				params["offset"] = formatCLIParamValue(flagOffset)
			}
			if flagOrder != "" {
				params["order"] = formatCLIParamValue(flagOrder)
			}
			if flagOrderby != "" {
				params["orderby"] = formatCLIParamValue(flagOrderby)
			}
			if flagParent != "" {
				params["parent"] = formatCLIParamValue(flagParent)
			}
			if flagParentExclude != "" {
				params["parent_exclude"] = formatCLIParamValue(flagParentExclude)
			}
			if flagSearchColumns != "" {
				params["search_columns"] = formatCLIParamValue(flagSearchColumns)
			}
			if flagSlug != "" {
				params["slug"] = formatCLIParamValue(flagSlug)
			}
			if flagStatus != "" {
				params["status"] = formatCLIParamValue(flagStatus)
			}
			if flagMediaType != "" {
				params["media_type"] = formatCLIParamValue(flagMediaType)
			}
			if flagMimeType != "" {
				params["mime_type"] = formatCLIParamValue(flagMimeType)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "media", true, path, params, nil, "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagContext, "context", "view", "Scope under which the request is made; determines fields present in response. (one of: view, embed, edit)")
	cmd.Flags().StringVar(&flagPage, "page", "1", "Current page of the collection.")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 10, "Maximum number of items to be returned in result set.")
	cmd.Flags().StringVar(&flagSearch, "search", "", "Limit results to those matching a string.")
	cmd.Flags().StringVar(&flagAfter, "after", "", "Limit response to posts published after a given ISO8601 compliant date.")
	cmd.Flags().StringVar(&flagModifiedAfter, "modified-after", "", "Limit response to posts modified after a given ISO8601 compliant date.")
	cmd.Flags().StringVar(&flagAuthor, "author", "", "Limit result set to posts assigned to specific authors. (comma-separated)")
	cmd.Flags().StringVar(&flagAuthorExclude, "author-exclude", "", "Ensure result set excludes posts assigned to specific authors. (comma-separated)")
	cmd.Flags().StringVar(&flagBefore, "before", "", "Limit response to posts published before a given ISO8601 compliant date.")
	cmd.Flags().StringVar(&flagModifiedBefore, "modified-before", "", "Limit response to posts modified before a given ISO8601 compliant date.")
	cmd.Flags().StringVar(&flagExclude, "exclude", "", "Ensure result set excludes specific IDs. (comma-separated)")
	cmd.Flags().StringVar(&flagInclude, "include", "", "Limit result set to specific IDs. (comma-separated)")
	cmd.Flags().StringVar(&flagSearchSemantics, "search-semantics", "", "How to interpret the search input. (one of: exact)")
	cmd.Flags().StringVar(&flagOffset, "offset", "", "Offset the result set by a specific number of items.")
	cmd.Flags().StringVar(&flagOrder, "order", "desc", "Order sort attribute ascending or descending. (one of: asc, desc)")
	cmd.Flags().StringVar(&flagOrderby, "orderby", "date", "Sort collection by post attribute. (one of: author, date, id, include, modified, parent, relevance, slug, include_slugs, title)")
	cmd.Flags().StringVar(&flagParent, "parent", "", "Limit result set to items with particular parent IDs. (comma-separated)")
	cmd.Flags().StringVar(&flagParentExclude, "parent-exclude", "", "Limit result set to all items except those of a particular parent ID. (comma-separated)")
	cmd.Flags().StringVar(&flagSearchColumns, "search-columns", "", "Array of column names to be searched. (comma-separated) (one of: post_title, post_content, post_excerpt)")
	cmd.Flags().StringVar(&flagSlug, "slug", "", "Limit result set to posts with one or more specific slugs. (comma-separated)")
	cmd.Flags().StringVar(&flagStatus, "status", "inherit", "Limit result set to posts assigned one or more statuses. (comma-separated) (one of: inherit, private, trash)")
	cmd.Flags().StringVar(&flagMediaType, "media-type", "", "Limit result set to attachments of a particular media type or media types. (comma-separated) (one of: image, audio, video, application, text)")
	cmd.Flags().StringVar(&flagMimeType, "mime-type", "", "Limit result set to attachments of a particular MIME type or MIME types. (comma-separated)")

	return cmd
}
