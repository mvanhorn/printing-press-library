// Copyright 2026 bobe and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCommentsListCmd(flags *rootFlags) *cobra.Command {
	var flagContext string
	var flagPage string
	var flagPerPage int
	var flagSearch string
	var flagAfter string
	var flagAuthor string
	var flagAuthorExclude string
	var flagAuthorEmail string
	var flagBefore string
	var flagExclude string
	var flagInclude string
	var flagOffset string
	var flagOrder string
	var flagOrderby string
	var flagParent string
	var flagParentExclude string
	var flagPost string
	var flagStatus string
	var flagType string
	var flagPassword string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "",
		Example:     "  wordpress-pp-cli comments list",
		Annotations: map[string]string{"pp:endpoint": "comments.list", "pp:method": "GET", "pp:path": "/wp/v2/comments", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("context") {
				allowedContext := []string{"view", "embed", "edit"}
				validContext := false
				for _, v := range allowedContext {
					if flagContext == v {
						validContext = true
						break
					}
				}
				if !validContext {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagContext, "context", allowedContext)
				}
			}
			if cmd.Flags().Changed("order") {
				allowedOrder := []string{"asc", "desc"}
				validOrder := false
				for _, v := range allowedOrder {
					if flagOrder == v {
						validOrder = true
						break
					}
				}
				if !validOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrder, "order", allowedOrder)
				}
			}
			if cmd.Flags().Changed("orderby") {
				allowedOrderby := []string{"date", "date_gmt", "id", "include", "post", "parent", "type"}
				validOrderby := false
				for _, v := range allowedOrderby {
					if flagOrderby == v {
						validOrderby = true
						break
					}
				}
				if !validOrderby {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrderby, "orderby", allowedOrderby)
				}
			}
			path := "/wp/v2/comments"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagContext != "" {
				params["context"] = formatCLIParamValue(flagContext)
			}
			if flagPage != "" {
				params["page"] = formatCLIParamValue(flagPage)
			}
			if flagPerPage != 0 {
				params["per_page"] = formatCLIParamValue(flagPerPage)
			}
			if flagSearch != "" {
				params["search"] = formatCLIParamValue(flagSearch)
			}
			if flagAfter != "" {
				params["after"] = formatCLIParamValue(flagAfter)
			}
			if flagAuthor != "" {
				params["author"] = formatCLIParamValue(flagAuthor)
			}
			if flagAuthorExclude != "" {
				params["author_exclude"] = formatCLIParamValue(flagAuthorExclude)
			}
			if flagAuthorEmail != "" {
				params["author_email"] = formatCLIParamValue(flagAuthorEmail)
			}
			if flagBefore != "" {
				params["before"] = formatCLIParamValue(flagBefore)
			}
			if flagExclude != "" {
				params["exclude"] = formatCLIParamValue(flagExclude)
			}
			if flagInclude != "" {
				params["include"] = formatCLIParamValue(flagInclude)
			}
			if flagOffset != "" {
				params["offset"] = formatCLIParamValue(flagOffset)
			}
			if flagOrder != "" {
				params["order"] = formatCLIParamValue(flagOrder)
			}
			if flagOrderby != "" {
				params["orderby"] = formatCLIParamValue(flagOrderby)
			}
			if flagParent != "" {
				params["parent"] = formatCLIParamValue(flagParent)
			}
			if flagParentExclude != "" {
				params["parent_exclude"] = formatCLIParamValue(flagParentExclude)
			}
			if flagPost != "" {
				params["post"] = formatCLIParamValue(flagPost)
			}
			if flagStatus != "" {
				params["status"] = formatCLIParamValue(flagStatus)
			}
			if flagType != "" {
				params["type"] = formatCLIParamValue(flagType)
			}
			if flagPassword != "" {
				params["password"] = formatCLIParamValue(flagPassword)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "comments", true, path, params, nil, "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagContext, "context", "view", "Scope under which the request is made; determines fields present in response. (one of: view, embed, edit)")
	cmd.Flags().StringVar(&flagPage, "page", "1", "Current page of the collection.")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 10, "Maximum number of items to be returned in result set.")
	cmd.Flags().StringVar(&flagSearch, "search", "", "Limit results to those matching a string.")
	cmd.Flags().StringVar(&flagAfter, "after", "", "Limit response to comments published after a given ISO8601 compliant date.")
	cmd.Flags().StringVar(&flagAuthor, "author", "", "Limit result set to comments assigned to specific user IDs. Requires authorization. (comma-separated)")
	cmd.Flags().StringVar(&flagAuthorExclude, "author-exclude", "", "Ensure result set excludes comments assigned to specific user IDs. Requires authorization. (comma-separated)")
	cmd.Flags().StringVar(&flagAuthorEmail, "author-email", "", "Limit result set to that from a specific author email. Requires authorization.")
	cmd.Flags().StringVar(&flagBefore, "before", "", "Limit response to comments published before a given ISO8601 compliant date.")
	cmd.Flags().StringVar(&flagExclude, "exclude", "", "Ensure result set excludes specific IDs. (comma-separated)")
	cmd.Flags().StringVar(&flagInclude, "include", "", "Limit result set to specific IDs. (comma-separated)")
	cmd.Flags().StringVar(&flagOffset, "offset", "", "Offset the result set by a specific number of items.")
	cmd.Flags().StringVar(&flagOrder, "order", "desc", "Order sort attribute ascending or descending. (one of: asc, desc)")
	cmd.Flags().StringVar(&flagOrderby, "orderby", "date_gmt", "Sort collection by comment attribute. (one of: date, date_gmt, id, include, post, parent, type)")
	cmd.Flags().StringVar(&flagParent, "parent", "", "Limit result set to comments of specific parent IDs. (comma-separated)")
	cmd.Flags().StringVar(&flagParentExclude, "parent-exclude", "", "Ensure result set excludes specific parent IDs. (comma-separated)")
	cmd.Flags().StringVar(&flagPost, "post", "", "Limit result set to comments assigned to specific post IDs. (comma-separated)")
	cmd.Flags().StringVar(&flagStatus, "status", "approve", "Limit result set to comments assigned a specific status. Requires authorization.")
	cmd.Flags().StringVar(&flagType, "type", "comment", "Limit result set to comments assigned a specific type. Requires authorization.")
	cmd.Flags().StringVar(&flagPassword, "password", "", "The password for the post if it is password protected.")

	return cmd
}
