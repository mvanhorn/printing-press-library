// Copyright 2026 bobe and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newUsersListCmd(flags *rootFlags) *cobra.Command {
	var flagContext string
	var flagPage string
	var flagPerPage int
	var flagSearch string
	var flagExclude string
	var flagInclude string
	var flagOffset string
	var flagOrder string
	var flagOrderby string
	var flagSlug string
	var flagRoles string
	var flagCapabilities string
	var flagWho string
	var flagHasPublishedPosts bool
	var flagSearchColumns string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "",
		Example:     "  wordpress-pp-cli users list",
		Annotations: map[string]string{"pp:endpoint": "users.list", "pp:method": "GET", "pp:path": "/wp/v2/users", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("context") {
				allowedContext := []string{"view", "embed", "edit"}
				validContext := false
				for _, v := range allowedContext {
					if flagContext == v {
						validContext = true
						break
					}
				}
				if !validContext {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagContext, "context", allowedContext)
				}
			}
			if cmd.Flags().Changed("order") {
				allowedOrder := []string{"asc", "desc"}
				validOrder := false
				for _, v := range allowedOrder {
					if flagOrder == v {
						validOrder = true
						break
					}
				}
				if !validOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrder, "order", allowedOrder)
				}
			}
			if cmd.Flags().Changed("orderby") {
				allowedOrderby := []string{"id", "include", "name", "registered_date", "slug", "include_slugs", "email", "url"}
				validOrderby := false
				for _, v := range allowedOrderby {
					if flagOrderby == v {
						validOrderby = true
						break
					}
				}
				if !validOrderby {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrderby, "orderby", allowedOrderby)
				}
			}
			if cmd.Flags().Changed("who") {
				allowedWho := []string{"authors"}
				validWho := false
				for _, v := range allowedWho {
					if flagWho == v {
						validWho = true
						break
					}
				}
				if !validWho {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagWho, "who", allowedWho)
				}
			}
			if cmd.Flags().Changed("search-columns") {
				allowedSearchColumns := []string{"email", "name", "id", "username", "slug"}
				validSearchColumns := false
				for _, v := range allowedSearchColumns {
					if flagSearchColumns == v {
						validSearchColumns = true
						break
					}
				}
				if !validSearchColumns {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSearchColumns, "search-columns", allowedSearchColumns)
				}
			}
			path := "/wp/v2/users"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagContext != "" {
				params["context"] = formatCLIParamValue(flagContext)
			}
			if flagPage != "" {
				params["page"] = formatCLIParamValue(flagPage)
			}
			if flagPerPage != 0 {
				params["per_page"] = formatCLIParamValue(flagPerPage)
			}
			if flagSearch != "" {
				params["search"] = formatCLIParamValue(flagSearch)
			}
			if flagExclude != "" {
				params["exclude"] = formatCLIParamValue(flagExclude)
			}
			if flagInclude != "" {
				params["include"] = formatCLIParamValue(flagInclude)
			}
			if flagOffset != "" {
				params["offset"] = formatCLIParamValue(flagOffset)
			}
			if flagOrder != "" {
				params["order"] = formatCLIParamValue(flagOrder)
			}
			if flagOrderby != "" {
				params["orderby"] = formatCLIParamValue(flagOrderby)
			}
			if flagSlug != "" {
				params["slug"] = formatCLIParamValue(flagSlug)
			}
			if flagRoles != "" {
				params["roles"] = formatCLIParamValue(flagRoles)
			}
			if flagCapabilities != "" {
				params["capabilities"] = formatCLIParamValue(flagCapabilities)
			}
			if flagWho != "" {
				params["who"] = formatCLIParamValue(flagWho)
			}
			if flagHasPublishedPosts != false {
				params["has_published_posts"] = formatCLIParamValue(flagHasPublishedPosts)
			}
			if flagSearchColumns != "" {
				params["search_columns"] = formatCLIParamValue(flagSearchColumns)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "users", true, path, params, nil, "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagContext, "context", "view", "Scope under which the request is made; determines fields present in response. (one of: view, embed, edit)")
	cmd.Flags().StringVar(&flagPage, "page", "1", "Current page of the collection.")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 10, "Maximum number of items to be returned in result set.")
	cmd.Flags().StringVar(&flagSearch, "search", "", "Limit results to those matching a string.")
	cmd.Flags().StringVar(&flagExclude, "exclude", "", "Ensure result set excludes specific IDs. (comma-separated)")
	cmd.Flags().StringVar(&flagInclude, "include", "", "Limit result set to specific IDs. (comma-separated)")
	cmd.Flags().StringVar(&flagOffset, "offset", "", "Offset the result set by a specific number of items.")
	cmd.Flags().StringVar(&flagOrder, "order", "asc", "Order sort attribute ascending or descending. (one of: asc, desc)")
	cmd.Flags().StringVar(&flagOrderby, "orderby", "name", "Sort collection by user attribute. (one of: id, include, name, registered_date, slug, include_slugs, email, url)")
	cmd.Flags().StringVar(&flagSlug, "slug", "", "Limit result set to users with one or more specific slugs. (comma-separated)")
	cmd.Flags().StringVar(&flagRoles, "roles", "", "Limit result set to users matching at least one specific role provided. Accepts csv list or single role.")
	cmd.Flags().StringVar(&flagCapabilities, "capabilities", "", "Limit result set to users matching at least one specific capability provided. Accepts csv list or single capability.")
	cmd.Flags().StringVar(&flagWho, "who", "", "Limit result set to users who are considered authors. (one of: authors)")
	cmd.Flags().BoolVar(&flagHasPublishedPosts, "has-published-posts", false, "Limit result set to users who have published posts. (one of: post, page, attachment, nav_menu_item, wp_block, wp_template, wp_template_part, wp_global_styles, wp_navigation, wp_font_family, wp_font_face, feedback, jetpack_form, jp_act_log_event, jp_pay_order, jp_pay_product, podcast)")
	cmd.Flags().StringVar(&flagSearchColumns, "search-columns", "", "Array of column names to be searched. (comma-separated) (one of: email, name, id, username, slug)")

	return cmd
}
