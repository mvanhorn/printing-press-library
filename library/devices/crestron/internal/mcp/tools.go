// Copyright 2026 drummerms and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"math"
	"os"
	"path/filepath"
	"strconv"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/devices/crestron/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/devices/crestron/internal/client"
	"github.com/mvanhorn/printing-press-library/library/devices/crestron/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/devices/crestron/internal/config"
	"github.com/mvanhorn/printing-press-library/library/devices/crestron/internal/learn"
	"github.com/mvanhorn/printing-press-library/library/devices/crestron/internal/mcp/bound"
	"github.com/mvanhorn/printing-press-library/library/devices/crestron/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/devices/crestron/internal/platform"
	"github.com/mvanhorn/printing-press-library/library/devices/crestron/internal/store"
)

const (
	// MCP hosts can fan out tool calls faster than a human CLI session.
	// Keep them on the same polite-client limiter path instead of disabling
	// pacing with rate=0; users can still tune human CLI calls with --rate-limit.
	defaultMCPRateLimit = 2
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	installFreshTenantGate(s)
	s.AddTool(
		mcplib.NewTool("account_status",
			mcplib.WithDescription("Check whether the stored Crestron.com session is still signed in. Optional: part (default: authlinks)."),
			mcplib.WithString("part", mcplib.Description("Header fragment to render")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/handlers/Header.ashx", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "part", WireName: "p", Location: "query", Default: "authlinks"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("asset_get",
			mcplib.WithDescription("Download a public documentation asset such as a spec sheet, manual, certificate, CAD drawing, or Revit family. Required: guid, filename."),
			mcplib.WithString("guid", mcplib.Required(), mcplib.Description("Asset guid from a resource search or product resource listing")),
			mcplib.WithString("filename", mcplib.Required(), mcplib.Description("Asset filename from the same listing")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/getmedia/{guid}/{filename}", true, true, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "guid", WireName: "guid", Location: "path"}, {PublicName: "filename", WireName: "filename", Location: "path"}}, []string{"guid", "filename"}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_category",
			mcplib.WithDescription("Open a catalog category page and read its subcategories and product counts. Required: path."),
			mcplib.WithString("path", mcplib.Required(), mcplib.Description("Category path below /Products/Catalog, e.g. AV-Over-IP/DM-NVX-AV-Over-IP")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Products/Catalog/{path}", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "path", WireName: "path", Location: "path"}}, []string{"path"}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_products",
			mcplib.WithDescription("List the products in a catalog category (needs the category's document and node ids). Required: document-id, node-id. Optional: per-page (default: 50), offset (default: 0), culture (default: en-us) (plus 3 more)."),
			mcplib.WithString("document-id", mcplib.Required(), mcplib.Description("Category document id, read from the category page's request block")),
			mcplib.WithString("node-id", mcplib.Required(), mcplib.Description("Category node id, read from the category page's request block")),
			mcplib.WithNumber("per-page", mcplib.Description("Products per request")),
			mcplib.WithNumber("offset", mcplib.Description("Result offset, advance by per-page until offset reaches the category's product count")),
			mcplib.WithString("culture", mcplib.Description("Content culture code")),
			mcplib.WithString("site-name", mcplib.Description("Kentico site name")),
			mcplib.WithString("sort", mcplib.Description("Product sort order")),
			mcplib.WithString("filter", mcplib.Description("Facet filter as semicolon-separated name:value pairs")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/CMSPages/ProductSubcategoryItemTemplate.aspx", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "document-id", WireName: "dId", Location: "query"}, {PublicName: "node-id", WireName: "nId", Location: "query"}, {PublicName: "per-page", WireName: "ps", Location: "query", Default: "50"}, {PublicName: "offset", WireName: "os", Location: "query", Default: "0"}, {PublicName: "culture", WireName: "cult", Location: "query", Default: "en-us"}, {PublicName: "site-name", WireName: "sn", Location: "query", Default: "Crestron"}, {PublicName: "sort", WireName: "sort", Location: "query", Default: "PageViews"}, {PublicName: "filter", WireName: "fltr", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("catalog_tree",
			mcplib.WithDescription("List every product category path in the catalog."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/sitemap", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("firmware_release",
			mcplib.WithDescription("Read a firmware release page including its version, date, release notes, and change log (requires sign-in). Required: path."),
			mcplib.WithString("path", mcplib.Required(), mcplib.Description("Release path below /Software-Firmware, taken from a firmware search result")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Software-Firmware/{path}", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "path", WireName: "path", Location: "path"}}, []string{"path"}),
	)
	s.AddTool(
		mcplib.NewTool("firmware_search",
			mcplib.WithDescription("Find firmware and software releases for a model or family. Optional: query, category (default: 4), release-type (plus 3 more)."),
			mcplib.WithString("query", mcplib.Description("Model number or product family, e.g. DM-NVX-384")),
			mcplib.WithString("category", mcplib.Description("Resource category; 4 is Software and Firmware")),
			mcplib.WithString("release-type", mcplib.Description("Release type: Firmware, Software, or Advanced-Support-Tools")),
			mcplib.WithString("sort", mcplib.Description("Sort order; Created:desc lists newest releases first")),
			mcplib.WithNumber("page", mcplib.Description("Result page number")),
			mcplib.WithNumber("per-page", mcplib.Description("Results per page (10, 25, or 50)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Support/Search-Results", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "category", WireName: "c", Location: "query", Default: "4"}, {PublicName: "release-type", WireName: "type", Location: "query"}, {PublicName: "sort", WireName: "o", Location: "query", Default: "Created:desc"}, {PublicName: "page", WireName: "p", Location: "query", Default: "1"}, {PublicName: "per-page", WireName: "m", Location: "query", Default: "25"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("product_accessories",
			mcplib.WithDescription("List optional accessories for a product. Required: ids."),
			mcplib.WithString("ids", mcplib.Required(), mcplib.Description("Semicolon-separated accessory ids from the product page's data-ids attribute")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Handlers/OptionalAccessoriesHandler.ashx", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "ids", WireName: "ids", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("product_page",
			mcplib.WithDescription("Fetch a product detail page including its JSON-LD, specification table, and document id. Required: path."),
			mcplib.WithString("path", mcplib.Required(), mcplib.Description("Product path below /Products/Catalog, ending in the model number")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Products/Catalog/{path}", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "path", WireName: "path", Location: "path"}}, []string{"path"}),
	)
	s.AddTool(
		mcplib.NewTool("product_replacements",
			mcplib.WithDescription("List replacement products for a discontinued item. Required: ids. Optional: label, culture (default: en-US)."),
			mcplib.WithString("ids", mcplib.Required(), mcplib.Description("Semicolon-separated replacement product ids from the discontinued product page")),
			mcplib.WithString("label", mcplib.Description("Optional display label")),
			mcplib.WithString("culture", mcplib.Description("Content culture code")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Handlers/ReplacementProductsHandler.ashx", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "ids", WireName: "ids", Location: "query"}, {PublicName: "label", WireName: "label", Location: "query"}, {PublicName: "culture", WireName: "culture", Location: "query", Default: "en-US"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("product_resources",
			mcplib.WithDescription("List every documentation asset for a product by its document id. Required: document-id."),
			mcplib.WithString("document-id", mcplib.Required(), mcplib.Description("Product document id, read from data-id on the product page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Handlers/ResourceHandler.ashx", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "document-id", WireName: "dID", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("product_variants",
			mcplib.WithDescription("List the member models of a product series. Required: series. Optional: load (default: list), class-name, culture (default: en-US) (plus 1 more)."),
			mcplib.WithString("load", mcplib.Description("Render mode: list or dropdown")),
			mcplib.WithString("series", mcplib.Required(), mcplib.Description("Series name as shown on the product page, e.g. DM-NVX-35 Series")),
			mcplib.WithString("class-name", mcplib.Description("Kentico product class name")),
			mcplib.WithString("culture", mcplib.Description("Content culture code")),
			mcplib.WithString("included-accessories", mcplib.Description("Included accessory ids")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Handlers/VariantProduct.ashx", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "load", WireName: "load", Location: "query", Default: "list"}, {PublicName: "series", WireName: "Product_Name", Location: "query"}, {PublicName: "class-name", WireName: "ClassName", Location: "query", Default: "Crestron.Crestron_Video_Product"}, {PublicName: "culture", WireName: "DocumentCulture", Location: "query", Default: "en-US"}, {PublicName: "included-accessories", WireName: "IncludedAcessories", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("resource_search",
			mcplib.WithDescription("Search spec sheets, manuals, firmware, certificates, and drawings. Optional: query, category (default: 0), resource-type (plus 3 more)."),
			mcplib.WithString("query", mcplib.Description("Search text, typically a model number or product family (e.g. DM-NVX-384)")),
			mcplib.WithString("category", mcplib.Description("Resource category: 0 all, 1 marketing, 3 technical, 4 software & firmware, 5 spec sheets, 6 guides & manuals")),
			mcplib.WithString("resource-type", mcplib.Description("Sub-type within a category, e.g. Firmware, Software, Manuals-Guides, Spec Sheets, Web CAD Drawings")),
			mcplib.WithString("sort", mcplib.Description("Sort order: empty for relevance, Created:desc for newest first, Name_Search.keyword:asc for A-Z")),
			mcplib.WithNumber("page", mcplib.Description("Result page number, starting at 1")),
			mcplib.WithNumber("per-page", mcplib.Description("Results per page (10, 25, or 50)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Support/Search-Results", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "category", WireName: "c", Location: "query", Default: "0"}, {PublicName: "resource-type", WireName: "type", Location: "query"}, {PublicName: "sort", WireName: "o", Location: "query"}, {PublicName: "page", WireName: "p", Location: "query", Default: "1"}, {PublicName: "per-page", WireName: "m", Location: "query", Default: "25"}}, []string{}),
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Synced records live in resources(resource_type, id, data); filter by resource_type and use json_extract on data, e.g. SELECT json_extract(data,'$.name') FROM resources WHERE resource_type='account'.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
	Default    string
}

type mcpPageConfig struct {
	CursorParam    string
	NextCursorPath string
}

func formatMCPParamValue(v any) string {
	switch tv := v.(type) {
	case string:
		return tv
	case bool:
		return strconv.FormatBool(tv)
	case float64:
		if math.IsNaN(tv) || math.IsInf(tv, 0) {
			return strconv.FormatFloat(tv, 'f', -1, 64)
		}
		if math.Trunc(tv) == tv && math.Abs(tv) < 1e15 {
			return strconv.FormatInt(int64(tv), 10)
		}
		return strconv.FormatFloat(tv, 'f', -1, 64)
	case float32:
		f := float64(tv)
		if math.IsNaN(f) || math.IsInf(f, 0) {
			return strconv.FormatFloat(f, 'f', -1, 32)
		}
		if math.Trunc(f) == f && math.Abs(f) < 1e15 {
			return strconv.FormatInt(int64(f), 10)
		}
		return strconv.FormatFloat(f, 'f', -1, 32)
	default:
		// Composite values (a native []any / map[string]any from an array or
		// object param) reach this path when bound to a query or path slot;
		// JSON-encode them so the wire value is valid JSON rather than Go's
		// "[a b c]" / "map[...]" rendering. Body params never come through
		// here — they are stored natively in bodyArgs and marshalled there.
		if b, err := json.Marshal(v); err == nil {
			return string(b)
		}
		return fmt.Sprintf("%v", v)
	}
}

func mcpPathValue(v any) string {
	return cliutil.EscapePathParam(formatMCPParamValue(v))
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, pageConfig mcpPageConfig, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, platformSession, err := newMCPClient(ctx)
		if err != nil {
			return mcpToolError(err.Error()), nil
		}
		if platformSession != nil {
			defer platformSession.ZeroCredentials()
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()
		if err := cli.AdoptMCPOutputSemantics(platformSession, args); err != nil {
			return mcpToolError(err.Error()), nil
		}

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		mcpCursor := ""
		if pageConfig.CursorParam != "" {
			knownArgs["cursor"] = true
			if v, ok := args["cursor"]; ok {
				s, ok := v.(string)
				if !ok {
					return mcpToolError("cursor must be an opaque string returned by a previous MCP response"), nil
				}
				mcpCursor = s
				upstreamCursor, err := bound.UpstreamCursor(s)
				if err != nil {
					return mcpToolError(err.Error()), nil
				}
				if upstreamCursor != "" {
					params[pageConfig.CursorParam] = upstreamCursor
				}
			}
		}
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				if binding.Default != "" {
					v = binding.Default
				} else {
					continue
				}
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, mcpPathValue(v), 1)
			case "header":
				if headers == nil {
					headers = map[string]string{}
				}
				headers[binding.WireName] = formatMCPParamValue(v)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = formatMCPParamValue(v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, mcpPathValue(v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = formatMCPParamValue(v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PutQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PutQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
			}
		case "PATCH":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PatchQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PatchQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
			}
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcpToolError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcpToolTextWithPlatform("already exists (no-op)", platformSession), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcpToolError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Run 'crestron-pp-cli auth login --chrome' to refresh browser-session credentials." +
					"\n      See API docs: https://www.crestron.com" +
					"\n      Run 'crestron-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcpToolError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API credentials." +
					"\n      Run 'crestron-pp-cli auth login --chrome' to refresh browser-session credentials." +
					"\n      See API docs: https://www.crestron.com" +
					"\n      Run 'crestron-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcpToolError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource. Check that they have the required permissions and match the API's expected auth scheme." +
					"\n      Run 'crestron-pp-cli auth login --chrome' to refresh browser-session credentials." +
					"\n      See API docs: https://www.crestron.com" +
					"\n      Run 'crestron-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcpToolTextWithPlatform("already deleted (no-op)", platformSession), nil
				}
				return mcpToolError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcpToolError("rate limited: " + msg), nil
			default:
				return mcpToolError(msg), nil
			}
		}

		if binaryResponse {
			encoded := base64.StdEncoding.EncodeToString(data)
			out, err := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      encoded,
				"byte_count":       len(data),
			})
			if err != nil {
				return mcpToolError(fmt.Sprintf("encoding binary result: %v", err)), nil
			}
			if len(out) > bound.MaxBytes {
				return mcpToolError(fmt.Sprintf("binary response is too large for MCP text output: %d response bytes encode to %d base64 bytes and %d MCP result bytes, exceeding the %d byte budget. Use the companion CLI command with --output <file> to save the payload locally.", len(data), len(encoded), len(out), bound.MaxBytes)), nil
			}
			result := string(out)
			if platformSession != nil {
				result = bound.WithMetadata(result, platformSession.OutputMetadata())
			}
			return mcplib.NewToolResultText(result), nil
		}
		if pageConfig.CursorParam != "" {
			return mcpToolPageResultTextWithPlatform(method, data, pageConfig, mcpCursor, platformSession), nil
		}
		return mcpToolResultTextWithPlatform(method, data, platformSession), nil
	}
}

func mcpToolResultText(method string, data json.RawMessage) *mcplib.CallToolResult {
	return mcpToolResultTextWithPlatform(method, data, nil)
}

func mcpToolTextWithPlatform(result string, platformSession *platform.Session) *mcplib.CallToolResult {
	if platformSession != nil {
		result = bound.WithMetadata(result, platformSession.OutputMetadata())
	}
	return mcplib.NewToolResultText(result)
}

func mcpToolResultTextWithPlatform(method string, data json.RawMessage, platformSession *platform.Session) *mcplib.CallToolResult {
	result := bound.EndpointResponse(method, data)
	return mcpToolTextWithPlatform(result, platformSession)
}

// mcpToolError keeps provider-controlled typed endpoint errors within the MCP
// text-result budget just like successful endpoint results.
func mcpToolError(message string) *mcplib.CallToolResult {
	return mcplib.NewToolResultError(bound.Text(message))
}

func mcpToolPageResultText(method string, data json.RawMessage, pageConfig mcpPageConfig, cursor string) *mcplib.CallToolResult {
	return mcpToolPageResultTextWithPlatform(method, data, pageConfig, cursor, nil)
}

func mcpToolPageResultTextWithPlatform(method string, data json.RawMessage, pageConfig mcpPageConfig, cursor string, platformSession *platform.Session) *mcplib.CallToolResult {
	result := bound.EndpointPageResponse(method, data, bound.PageOptions{
		Cursor:         cursor,
		CursorParam:    pageConfig.CursorParam,
		NextCursorPath: pageConfig.NextCursorPath,
	})
	if platformSession != nil {
		result = bound.WithMetadata(result, platformSession.OutputMetadata())
	}
	return mcplib.NewToolResultText(result)
}

func newMCPClient(ctx context.Context) (*client.Client, *platform.Session, error) {
	cfg, err := newMCPConfig()
	if err != nil {
		return nil, nil, err
	}
	c := newMCPClientFromConfig(cfg)
	session, err := cli.BindMCPClient(ctx, c)
	if err != nil {
		return nil, nil, err
	}
	return c, session, nil
}

func newMCPConfig() (*config.Config, error) {
	cfg, err := config.Load("")
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	return cfg, nil
}

func newMCPClientFromConfig(cfg *config.Config) *client.Client {
	c := client.New(cfg, 60*time.Second, defaultMCPRateLimit)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c
}

func mcpDBPath() (string, error) {
	dir, err := cliutil.DataDir()
	if err != nil {
		return "", err
	}
	return filepath.Join(dir, "data.db"), nil
}

type mcpStoreStatusKind string

const (
	mcpStoreStatusEmpty mcpStoreStatusKind = "empty"
	mcpStoreStatusReady mcpStoreStatusKind = "ready"
)

func openMCPReadOnlyStore(path string) (*store.Store, *mcplib.CallToolResult) {
	if _, err := os.Stat(path); err != nil {
		if os.IsNotExist(err) {
			return nil, mcplib.NewToolResultError(mcpMissingStoreMessage(path))
		}
		return nil, mcplib.NewToolResultError(fmt.Sprintf("checking local data store %s: %v", path, err))
	}
	db, err := store.OpenReadOnly(path)
	if err != nil {
		return nil, mcplib.NewToolResultError(fmt.Sprintf("opening local data store %s: %v. Run crestron-pp-cli sync to refresh the store, or use live endpoint MCP tools for unsynced data.", path, err))
	}
	return db, nil
}

func mcpMissingStoreMessage(path string) string {
	return fmt.Sprintf("No local data store found at %s. Run crestron-pp-cli sync before using MCP search/sql, or use live endpoint MCP tools for unsynced data.", path)
}

func mcpStoreStatus(db *store.Store) (mcpStoreStatusKind, error) {
	status, err := db.Status()
	if err != nil {
		return "", err
	}
	if len(status) == 0 {
		return mcpStoreStatusEmpty, nil
	}
	return mcpStoreStatusReady, nil
}

func mcpEmptyStoreNextStep() string {
	return "Run crestron-pp-cli sync to populate the local SQLite store before using MCP search/sql."
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate rejects multi-statement input, then applies an allowlist (SELECT or
// WITH only) AFTER stripping the leading whitespace, line comments, block
// comments, and semicolons that SQLite itself ignores before parsing. A naive
// HasPrefix check on a keyword blocklist is bypassable by prefixing the
// dangerous statement with "/* x */" or "-- x\n"; a naive leading-keyword
// allowlist is bypassable by appending "; ATTACH DATABASE ...". Combined with
// the empirical fact that modernc.org/sqlite's mode=ro does NOT block VACUUM
// INTO (writes a snapshot to a new file) or ATTACH DATABASE (opens a separate
// writable handle), either bypass produces silent exfiltration to an
// attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	stripped := stripLeadingSQLNoise(query)
	if hasTrailingSQLStatement(stripped) {
		return fmt.Errorf("only a single SELECT or WITH statement is allowed")
	}
	upper := strings.ToUpper(stripped)
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

// hasTrailingSQLStatement reports whether query contains a statement
// terminator followed by more executable SQL. A trailing semicolon is allowed;
// a second statement is not. Semicolons inside string literals, quoted
// identifiers, bracket identifiers, and comments are ignored to match SQLite's
// parser shape closely enough for this security gate.
func hasTrailingSQLStatement(query string) bool {
	inSingle := false
	inDouble := false
	inBacktick := false
	inBracket := false
	inLineComment := false
	inBlockComment := false

	for i := 0; i < len(query); i++ {
		ch := query[i]
		next := byte(0)
		if i+1 < len(query) {
			next = query[i+1]
		}

		switch {
		case inLineComment:
			if ch == '\n' {
				inLineComment = false
			}
			continue
		case inBlockComment:
			if ch == '*' && next == '/' {
				inBlockComment = false
				i++
			}
			continue
		case inSingle:
			if ch == '\'' {
				if next == '\'' {
					i++
					continue
				}
				inSingle = false
			}
			continue
		case inDouble:
			if ch == '"' {
				if next == '"' {
					i++
					continue
				}
				inDouble = false
			}
			continue
		case inBacktick:
			if ch == '`' {
				if next == '`' {
					i++
					continue
				}
				inBacktick = false
			}
			continue
		case inBracket:
			if ch == ']' {
				inBracket = false
			}
			continue
		}

		switch {
		case ch == '-' && next == '-':
			inLineComment = true
			i++
		case ch == '/' && next == '*':
			inBlockComment = true
			i++
		case ch == '\'':
			inSingle = true
		case ch == '"':
			inDouble = true
		case ch == '`':
			inBacktick = true
		case ch == '[':
			inBracket = true
		case ch == ';':
			if stripLeadingSQLNoise(query[i+1:]) != "" {
				return true
			}
			return false
		}
	}
	return false
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	path, err := mcpDBPath()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("resolving database: %v", err)), nil
	}
	db, toolErr := openMCPReadOnlyStore(path)
	if toolErr != nil {
		return toolErr, nil
	}
	defer db.Close()

	rows, err := db.DB().QueryContext(ctx, query)
	if err != nil {
		return mcplib.NewToolResultError(mcpSQLQueryError(err)), nil
	}
	defer rows.Close()

	cols, err := rows.Columns()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading columns: %v", err)), nil
	}
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		if err := rows.Scan(ptrs...); err != nil {
			return mcplib.NewToolResultError(fmt.Sprintf("scanning row: %v", err)), nil
		}
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}
	// rows.Next() stops on a mid-iteration error without failing the loop, so
	// skipping rows.Err() would return a truncated result set as success.
	if err := rows.Err(); err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading rows: %v", err)), nil
	}
	storeStatus, err := mcpStoreStatus(db)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading store status: %v", err)), nil
	}

	return toolResultJSON(mcpSQLEnvelope(results, cols, storeStatus))
}

func mcpSQLEnvelope(rows []map[string]any, columns []string, storeStatus mcpStoreStatusKind) map[string]any {
	if rows == nil {
		rows = []map[string]any{}
	}
	out := map[string]any{
		"count":        len(rows),
		"columns":      columns,
		"rows":         rows,
		"store_status": storeStatus,
		"resumable":    false,
	}
	if len(rows) == 0 {
		if storeStatus == mcpStoreStatusEmpty {
			out["next_step"] = mcpEmptyStoreNextStep()
		} else {
			out["next_step"] = "The read-only SQL query returned no rows. Check resource_type filters, json_extract paths, or run sync again if data may be stale."
		}
	}
	return out
}

func mcpSQLQueryError(err error) string {
	msg := err.Error()
	if strings.Contains(strings.ToLower(msg), "no such table") {
		return fmt.Sprintf("query failed: %v. Synced records live in resources(resource_type, id, data), not one SQL table per resource. Filter by resource_type, for example resource_type='account', and read JSON fields with json_extract(data,'$.field').", err)
	}
	return fmt.Sprintf("query failed: %v", err)
}

// toolResultJSON renders v as the indented JSON body of an MCP text result,
// surfacing a marshal failure as a tool error instead of empty content.
func toolResultJSON(v any) (*mcplib.CallToolResult, error) {
	text, err := bound.JSON(v)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("encoding result: %v", err)), nil
	}
	return mcplib.NewToolResultText(text), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	paths := map[string]string{}
	if dir, err := cliutil.ConfigDir(); err == nil {
		paths["config_dir"] = dir
	}
	if dir, err := cliutil.DataDir(); err == nil {
		paths["data_dir"] = dir
	}
	if dir, err := cliutil.StateDir(); err == nil {
		paths["state_dir"] = dir
	}
	if dir, err := cliutil.CacheDir(); err == nil {
		paths["cache_dir"] = dir
	}
	ctx := map[string]any{
		"api":         "crestron",
		"description": "Every Crestron product, spec sheet, and firmware release — searchable offline, with fleet-wide currency checks the website cannot do.",
		"archetype":   "content",
		"tool_count":  13,
		"paths":       paths,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion crestron-pp-cli binary.",
		// learn_protocol is generated from the single shared source of
		// truth (the exported constant internal/learn.RecallFirstProtocol)
		// also consumed by the CLI agent-context command, so the MCP and
		// CLI agent surfaces cannot drift.
		"learn_protocol": learn.RecallFirstProtocol,
		"auth": map[string]any{
			"type":     "cookie",
			"docs_url": "https://www.crestron.com",
		},
		"resources": []map[string]any{
			{
				"name":        "account",
				"description": "Crestron.com sign-in state",
				"endpoints":   []string{"status"},
				"searchable":  true,
			},
			{
				"name":        "asset",
				"description": "Download Crestron documentation and firmware files",
				"endpoints":   []string{"get"},
				"searchable":  true,
			},
			{
				"name":        "catalog",
				"description": "Browse the Crestron product catalog taxonomy",
				"endpoints":   []string{"category", "products", "tree"},
				"searchable":  true,
			},
			{
				"name":        "firmware",
				"description": "Crestron firmware and software releases",
				"endpoints":   []string{"release", "search"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "product",
				"description": "Look up Crestron products, specifications, and their documentation",
				"endpoints":   []string{"accessories", "page", "replacements", "resources", "variants"},
				"searchable":  true,
			},
			{
				"name":        "resource",
				"description": "Search Crestron's documentation and firmware resource library",
				"endpoints":   []string{"search"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Fleet firmware status", "command": "fleet status", "description": "Check every model in your installed fleet against current firmware in one command.", "rationale": "Firmware releases are family-scoped, so one release governs many models; resolving that many-to-many mapping requires a local series-to-model join that no single page provides.", "via": "mcp-command-mirror"},
			{"name": "Release-note full-text search", "command": "search", "description": "Search every firmware release note and change log at once for a term.", "rationale": "Release notes live on login-gated per-version pages and have never been searchable across versions anywhere.", "via": "mcp-command-mirror"},
			{"name": "Release-note diff", "command": "firmware diff", "description": "Show what changed between two firmware versions for a model.", "rationale": "Change logs exist only as separate per-version HTML pages with no diff view on the site.", "via": "mcp-command-mirror"},
			{"name": "Submittal package builder", "command": "submittal", "description": "Download every documentation asset for a list of models into per-model folders with a coverage report.", "rationale": "The per-product resource endpoint returns roughly twenty assets spanning CAD, Revit, CSI Guide Specs, Security Reference Guides, manuals and spec sheets; nothing on the site fans that out over a model list.", "via": "mcp-command-mirror"},
			{"name": "Lifecycle and replacement trace", "command": "lifecycle", "description": "Report whether a model is still sellable and trace its replacement chain.", "rationale": "An unadvertised replacement-products endpoint plus the dated End-of-Sale notice stream let the successor chain be walked transitively and in bulk.", "via": "mcp-command-mirror"},
			{"name": "Spec table comparison", "command": "specs compare", "description": "Compare two models field by field across the full specification table.", "rationale": "The parsed specification table is stored locally, so two products can be aligned by section and key; the website renders one product at a time.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Fleet firmware status", "insight": "Firmware releases are family-scoped, so one release governs many models; resolving that many-to-many mapping requires a local series-to-model join that no single page provides."},
			{"topic": "Release-note full-text search", "insight": "Release notes live on login-gated per-version pages and have never been searchable across versions anywhere."},
			{"topic": "Release-note diff", "insight": "Change logs exist only as separate per-version HTML pages with no diff view on the site."},
			{"topic": "Submittal package builder", "insight": "The per-product resource endpoint returns roughly twenty assets spanning CAD, Revit, CSI Guide Specs, Security Reference Guides, manuals and spec sheets; nothing on the site fans that out over a model list."},
			{"topic": "Lifecycle and replacement trace", "insight": "An unadvertised replacement-products endpoint plus the dated End-of-Sale notice stream let the successor chain be walked transitively and in bulk."},
			{"topic": "Spec table comparison", "insight": "The parsed specification table is stored locally, so two products can be aligned by section and key; the website renders one product at a time."},
		},
	}
	return toolResultJSON(ctx)
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
