{
  "manifest_version": "0.3",
  "name": "eero-pp-mcp",
  "display_name": "eero",
  "version": "0.1.0",
  "description": "Inspect an eero mesh network from the terminal with agent-friendly output for networks, nodes, connected devices, diagnostics, and speed tests.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/eero-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/eero-pp-mcp",
      "args": [],
      "env": {
        "PRINTING_PRESS_CLIENT_PROFILE": "${user_config.printing_press_client_profile}",
        "EERO_SESSION_TOKEN": "${user_config.eero_session_token}"
      }
    }
  },
  "user_config": {
    "printing_press_client_profile": {
      "type": "string",
      "title": "Client profile",
      "description": "Binds the MCP server to an existing tenant-gated Printing Press client profile.",
      "required": true
    },
    "eero_session_token": {
      "type": "string",
      "title": "eero session token",
      "description": "An eero session token used to send the s cookie to api-user.e2ro.com.",
      "sensitive": true,
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
