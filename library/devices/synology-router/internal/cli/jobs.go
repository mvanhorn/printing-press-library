// Copyright 2026 eric-jung. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"time"

	"github.com/spf13/cobra"
)

type jobEntry struct {
	ID        string    `json:"id"`
	Command   string    `json:"command"`
	Status    string    `json:"status"`
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}

func newJobsCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "jobs",
		Short: "Manage background sync and export jobs",
	}
	cmd.AddCommand(newJobsListCmd(flags))
	cmd.AddCommand(newJobsStatusCmd(flags))
	cmd.AddCommand(newJobsCancelCmd(flags))
	return cmd
}

func newJobsListCmd(flags *rootFlags) *cobra.Command {
	return &cobra.Command{
		Use:   "list",
		Short: "List recent background jobs and their statuses",
		RunE: func(cmd *cobra.Command, args []string) error {
			return fmt.Errorf("no jobs found — run 'sync' or 'export' to create one")
		},
	}
}

func newJobsStatusCmd(flags *rootFlags) *cobra.Command {
	return &cobra.Command{
		Use:   "status <job-id>",
		Short: "Show detailed status of a specific background job",
		Args:  cobra.ExactArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			return fmt.Errorf("job %s not found", args[0])
		},
	}
}

func newJobsCancelCmd(flags *rootFlags) *cobra.Command {
	return &cobra.Command{
		Use:   "cancel <job-id>",
		Short: "Cancel a running background job by its identifier",
		Args:  cobra.ExactArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			return fmt.Errorf("job %s not found", args[0])
		},
	}
}
