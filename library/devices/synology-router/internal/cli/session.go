// Copyright 2026 eric-jung. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"
)

func newSessionCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "session",
		Short: "Authenticate with the Synology Router (login/logout)",
		Long: `Manage SRM sessions. Use 'session auth-login' to authenticate and obtain
a session ID, and 'session auth-logout' to end the current session.

For a quicker login shortcut, use the top-level 'login' command instead.`,
		RunE: parentNoSubcommandRunE(flags),
	}

	cmd.AddCommand(newSessionAuthLoginCmd(flags))
	cmd.AddCommand(newSessionAuthLogoutCmd(flags))
	return cmd
}
