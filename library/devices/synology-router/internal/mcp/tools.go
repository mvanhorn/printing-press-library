// Copyright 2026 eric-jung. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"net/url"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/devices/synology-router/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/devices/synology-router/internal/client"
	"github.com/mvanhorn/printing-press-library/library/devices/synology-router/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/devices/synology-router/internal/config"
	"github.com/mvanhorn/printing-press-library/library/devices/synology-router/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/devices/synology-router/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("access-control_groups-list",
			mcplib.WithDescription("Returns array of access control groups with member devices and usage stats."),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/access-control/groups", false, []mcpParamBinding{{PublicName: "additional", WireName: "additional", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("devices_list",
			mcplib.WithDescription("Returns array of devices with IP, MAC, hostname, and online status."),
			mcplib.WithString("conntype", mcplib.Description("all|wireless|wired")),
			mcplib.WithString("info", mcplib.Description("basic|online")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/devices", false, []mcpParamBinding{{PublicName: "conntype", WireName: "conntype", Location: "query"}, {PublicName: "info", WireName: "info", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dns_ddns-records-list",
			mcplib.WithDescription("Returns array of DDNS records with update status."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/dns/ddns", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dns_external-ip-get",
			mcplib.WithDescription("Returns object with current external (WAN) IP address."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/dns/external-ip", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("firewall_rules-list",
			mcplib.WithDescription("Returns array of firewall policy route rules."),
			mcplib.WithString("type", mcplib.Description("ipv4|ipv6")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/firewall/rules", false, []mcpParamBinding{{PublicName: "type", WireName: "type", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("firewall_rules-set",
			mcplib.WithDescription("Replace all firewall rules. Full replace, not patch."),
			mcplib.WithString("rules", mcplib.Required(), mcplib.Description("JSON rules array")),
			mcplib.WithString("type", mcplib.Description("ipv4|ipv6")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/firewall/rules", false, []mcpParamBinding{{PublicName: "rules", WireName: "rules", Location: "body"}, {PublicName: "type", WireName: "type", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mesh_nodes-list",
			mcplib.WithDescription("Returns array of mesh nodes with status, rate, and device count."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mesh/nodes", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mesh_system-info-get",
			mcplib.WithDescription("Returns object with router model, firmware, serial, uptime."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mesh/info", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("qos_rules-list",
			mcplib.WithDescription("Returns array of QoS bandwidth rules per device."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/qos/rules", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("session_auth-login",
			mcplib.WithDescription("Authenticate and obtain a session ID. Returns object with sid."),
			mcplib.WithString("account", mcplib.Required(), mcplib.Description("Admin username")),
			mcplib.WithString("passwd", mcplib.Required(), mcplib.Description("Admin password")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/session/login", false, []mcpParamBinding{{PublicName: "account", WireName: "account", Location: "body", RequestContentType: "application/x-www-form-urlencoded"}, {PublicName: "passwd", WireName: "passwd", Location: "body", RequestContentType: "application/x-www-form-urlencoded"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("session_auth-logout",
			mcplib.WithDescription("Destroy the current SRM session."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/session/logout", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("smartwan_smart-wan-config-get",
			mcplib.WithDescription("Returns object with Smart WAN config: failover mode, load-balancing, interfaces."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/smartwan/config", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("smartwan_smart-wan-config-set",
			mcplib.WithDescription("Update Smart WAN config."),
			mcplib.WithNumber("dw_weight_ratio", mcplib.Description("Download weight 0-100")),
			mcplib.WithBoolean("smartwan_failback", mcplib.Description("Auto-failback to primary")),
			mcplib.WithString("smartwan_ifname_1", mcplib.Description("Primary WAN interface")),
			mcplib.WithString("smartwan_ifname_2", mcplib.Description("Secondary WAN interface")),
			mcplib.WithString("smartwan_mode", mcplib.Description("failover|load_balance|spillover")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/smartwan/config", false, []mcpParamBinding{{PublicName: "dw_weight_ratio", WireName: "dw_weight_ratio", Location: "body"}, {PublicName: "smartwan_failback", WireName: "smartwan_failback", Location: "body"}, {PublicName: "smartwan_ifname_1", WireName: "smartwan_ifname_1", Location: "body"}, {PublicName: "smartwan_ifname_2", WireName: "smartwan_ifname_2", Location: "body"}, {PublicName: "smartwan_mode", WireName: "smartwan_mode", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("smartwan_smart-wan-gateway-list",
			mcplib.WithDescription("Returns array of Smart WAN gateways with status and health."),
			mcplib.WithString("gatewaytype", mcplib.Description("ipv4|ipv6")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/smartwan/gateways", false, []mcpParamBinding{{PublicName: "gatewaytype", WireName: "gatewaytype", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("system_info-query",
			mcplib.WithDescription("Returns object with available SRM API namespaces. No auth required."),
			mcplib.WithString("query", mcplib.Description("ALL or comma-separated namespaces")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/system/api-info", false, []mcpParamBinding{{PublicName: "query", WireName: "query", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("traffic_get",
			mcplib.WithDescription("Returns array of traffic per device sorted by download with human-readable bandwidth."),
			mcplib.WithString("interval", mcplib.Required(), mcplib.Description("live|day|week|month")),
			mcplib.WithString("mode", mcplib.Description("net|net_l7")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/traffic", false, []mcpParamBinding{{PublicName: "interval", WireName: "interval", Location: "query"}, {PublicName: "mode", WireName: "mode", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("utilization_get",
			mcplib.WithDescription("Returns object with router CPU, memory, and network utilization."),
			mcplib.WithString("resource", mcplib.Description("JSON array: cpu, memory, network")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/utilization", false, []mcpParamBinding{{PublicName: "resource", WireName: "resource", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("wan_status-get",
			mcplib.WithDescription("Returns object with WAN status: IP, gateway, DNS, connection uptime."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/wan/status", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("wifi_settings-get",
			mcplib.WithDescription("Returns array of Wi-Fi profiles: SSIDs, security, band, radio config."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/wifi/settings", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("wifi_settings-set",
			mcplib.WithDescription("Update Wi-Fi profiles. Full replace — read current first."),
			mcplib.WithString("profiles", mcplib.Required(), mcplib.Description("Complete Wi-Fi profiles array")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/wifi/settings", false, []mcpParamBinding{{PublicName: "profiles", WireName: "profiles", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("wol_device-add",
			mcplib.WithDescription("Register a MAC for Wake-on-LAN."),
			mcplib.WithString("host", mcplib.Description("Optional hostname")),
			mcplib.WithString("mac", mcplib.Required(), mcplib.Description("MAC address (AA:BB:CC:DD:EE:FF)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/wol/devices", false, []mcpParamBinding{{PublicName: "host", WireName: "host", Location: "body"}, {PublicName: "mac", WireName: "mac", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("wol_devices-list",
			mcplib.WithDescription("Returns array of WOL-registered devices with MAC and hostname."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/wol/devices", false, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("wol_wake",
			mcplib.WithDescription("Send WOL magic packet to wake a device."),
			mcplib.WithString("mac", mcplib.Required(), mcplib.Description("MAC of device to wake")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/wol/wake", false, []mcpParamBinding{{PublicName: "mac", WireName: "mac", Location: "body"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("FTS across synced data. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("FTS5 query: AND, OR, NOT, phrases")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Read-only SQL on local DB. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SELECT or WITH...SELECT query")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("API context: resource taxonomy, auth, tips. Call first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Intent tools — high-level agent abstractions that combine endpoints.
	RegisterIntentTools(s)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName         string
	WireName           string
	Location           string
	Format             string
	RequestContentType string
}

func mcpFormFieldValue(v any) string {
	if s, ok := v.(string); ok {
		return s
	}
	if data, err := json.Marshal(v); err == nil {
		return string(data)
	}
	return fmt.Sprintf("%v", v)
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, binaryResponse bool, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if binaryResponse {
			headers = map[string]string{client.BinaryResponseHeader: "true"}
		}
		formFields := url.Values{}
		formEncoded := false
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			if strings.EqualFold(binding.RequestContentType, "application/x-www-form-urlencoded") {
				formEncoded = true
			}
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
				if formEncoded {
					formFields.Set(binding.WireName, mcpFormFieldValue(v))
				}
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
				if formEncoded {
					formFields.Set(k, mcpFormFieldValue(v))
				}
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if binaryResponse {
				data, err = c.GetWithHeaders(path, params, headers)
				break
			}
			data, err = c.Get(path, params)
		case "POST":
			if formEncoded {
				if binaryResponse {
					data, _, err = c.PostFormWithParamsAndHeaders(path, params, formFields, headers)
					break
				}
				data, _, err = c.PostFormWithParams(path, params, formFields)
				break
			}
			if binaryResponse {
				data, _, err = c.PostWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PostWithParams(path, params, bodyArgs)
		case "PUT":
			if formEncoded {
				if binaryResponse {
					data, _, err = c.PutFormWithParamsAndHeaders(path, params, formFields, headers)
					break
				}
				data, _, err = c.PutFormWithParams(path, params, formFields)
				break
			}
			if binaryResponse {
				data, _, err = c.PutWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(path, params, bodyArgs)
		case "PATCH":
			if formEncoded {
				if binaryResponse {
					data, _, err = c.PatchFormWithParamsAndHeaders(path, params, formFields, headers)
					break
				}
				data, _, err = c.PatchFormWithParams(path, params, formFields)
				break
			}
			if binaryResponse {
				data, _, err = c.PatchWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(path, params, bodyArgs)
		case "DELETE":
			if binaryResponse {
				data, _, err = c.DeleteWithParamsAndHeaders(path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export SYNOLOGY_ROUTER_COOKIE_AUTH=<your-key>" +
					"\n      See API docs: https://www.synology.com" +
					"\n      Run 'synology-router-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set it with: export SYNOLOGY_ROUTER_COOKIE_AUTH=<your-key>" +
					"\n      See API docs: https://www.synology.com" +
					"\n      Run 'synology-router-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export SYNOLOGY_ROUTER_COOKIE_AUTH=<your-key>" +
					"\n      See API docs: https://www.synology.com" +
					"\n      Run 'synology-router-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "synology-router-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "synology-router-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "synology-router",
		"description": "REST-style API for managing Synology routers via SRM (Synology Router Manager). All operations use form-encoded POST...",
		"archetype":   "generic",
		"tool_count":  23,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion synology-router-pp-cli binary.",
		"auth": map[string]any{
			"type":     "api_key",
			"env_vars": []map[string]any{},
			"docs_url": "https://www.synology.com",
		},
		"resources": []map[string]any{
			{
				"name":        "access-control",
				"description": "Access control groups and safe access (SYNO.SafeAccess)",
				"endpoints":   []string{"groups-list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "devices",
				"description": "Connected device management — IP, MAC, hostname, online status (SYNO.Core.Network.NSM.Device)",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "dns",
				"description": "DDNS records and external IP (SYNO.Core.DDNS)",
				"endpoints":   []string{"ddns-records-list", "external-ip-get"},
				"syncable":    true,
			},
			{
				"name":        "firewall",
				"description": "Firewall policy route rules (SYNO.Core.Network.Router.PolicyRoute)",
				"endpoints":   []string{"rules-list", "rules-set"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "mesh",
				"description": "Mesh network nodes and system info (SYNO.Mesh)",
				"endpoints":   []string{"nodes-list", "system-info-get"},
				"syncable":    true,
			},
			{
				"name":        "qos",
				"description": "Quality of Service rules (SYNO.Core.NGFW.QoS.Rules)",
				"endpoints":   []string{"rules-list"},
				"syncable":    true,
			},
			{
				"name":        "session",
				"description": "SRM authentication (login/logout). Use auth-login to get a session ID; set SYNOLOGY_ROUTER_COOKIE_AUTH to persist it.",
				"endpoints":   []string{"auth-login", "auth-logout"},
				"searchable":  true,
			},
			{
				"name":        "smartwan",
				"description": "Smart WAN / multi-WAN configuration (SYNO.Core.Network.SmartWAN)",
				"endpoints":   []string{"smart-wan-config-get", "smart-wan-config-set", "smart-wan-gateway-list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "system",
				"description": "System information and utilization (SYNO.Core.System)",
				"endpoints":   []string{"info-query"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "traffic",
				"description": "Network traffic and bandwidth statistics (SYNO.Core.NGFW.Traffic)",
				"endpoints":   []string{"get"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "utilization",
				"description": "Router CPU, memory, and network interface utilization (SYNO.Core.System.Utilization)",
				"endpoints":   []string{"get"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "wan",
				"description": "WAN connection status (SYNO.Core.Network.Router.ConnectionStatus)",
				"endpoints":   []string{"status-get"},
				"syncable":    true,
			},
			{
				"name":        "wifi",
				"description": "Wi-Fi network and client management (SYNO.Wifi.Network.Setting)",
				"endpoints":   []string{"settings-get", "settings-set"},
				"syncable":    true,
			},
			{
				"name":        "wol",
				"description": "Wake-on-LAN management (SYNO.Core.Network.WOL)",
				"endpoints":   []string{"device-add", "devices-list", "wake"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
