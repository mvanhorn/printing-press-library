// Copyright 2026 pimmetjeoss. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newChargehistoryInstallationreportGetCmd(flags *rootFlags) *cobra.Command {
	var flagFromDate string
	var flagEndDate string
	var flagInstallationId string
	var flagUserIds string
	var flagChargerIds string
	var flagGroupBy int
	var flagReportFormat int

	cmd := &cobra.Command{
		Use:         "installationreport-get",
		Short:       "**Deprecated**: Use the POST version instead. Retrieves a usage report based on the provided filters (requires...",
		Example:     "  zaptec-pp-cli chargehistory installationreport-get",
		Annotations: map[string]string{"pp:endpoint": "chargehistory.installationreport-get", "pp:method": "GET", "pp:path": "/api/chargehistory/installationreport", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/chargehistory/installationreport"
			params := map[string]string{}
			if flagFromDate != "" {
				params["FromDate"] = fmt.Sprintf("%v", flagFromDate)
			}
			if flagEndDate != "" {
				params["EndDate"] = fmt.Sprintf("%v", flagEndDate)
			}
			if flagInstallationId != "" {
				params["InstallationId"] = fmt.Sprintf("%v", flagInstallationId)
			}
			if flagUserIds != "" {
				params["UserIds"] = fmt.Sprintf("%v", flagUserIds)
			}
			if flagChargerIds != "" {
				params["ChargerIds"] = fmt.Sprintf("%v", flagChargerIds)
			}
			if flagGroupBy != 0 {
				params["GroupBy"] = fmt.Sprintf("%v", flagGroupBy)
			}
			if flagReportFormat != 0 {
				params["ReportFormat"] = fmt.Sprintf("%v", flagReportFormat)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "chargehistory", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagFromDate, "from-date", "", "From date")
	cmd.Flags().StringVar(&flagEndDate, "end-date", "", "End date")
	cmd.Flags().StringVar(&flagInstallationId, "installation-id", "", "Installation id")
	cmd.Flags().StringVar(&flagUserIds, "user-ids", "", "User ids")
	cmd.Flags().StringVar(&flagChargerIds, "charger-ids", "", "Charger ids")
	cmd.Flags().IntVar(&flagGroupBy, "group-by", 0, "0 = User 1 = Charger 2 = ChargeCardName (one of: 0, 1, 2)")
	cmd.Flags().IntVar(&flagReportFormat, "report-format", 0, "0 = Summary 1 = Detailed (one of: 0, 1)")

	return cmd
}
