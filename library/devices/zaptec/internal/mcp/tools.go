// Copyright 2026 pimmetjeoss. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/devices/zaptec/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/devices/zaptec/internal/client"
	"github.com/mvanhorn/printing-press-library/library/devices/zaptec/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/devices/zaptec/internal/config"
	"github.com/mvanhorn/printing-press-library/library/devices/zaptec/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/devices/zaptec/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("chargehistory_get",
			mcplib.WithDescription("Retrieves all completed charge sessions accessible by the current user, matching the provided filters. Default page size: 50. Max: 100. Optional: InstallationId, UserId, ChargerId (plus 11 more). Returns array of SessionListModel."),
			mcplib.WithString("InstallationId", mcplib.Description("Installation id")),
			mcplib.WithString("UserId", mcplib.Description("User id")),
			mcplib.WithString("ChargerId", mcplib.Description("Charger id")),
			mcplib.WithString("From", mcplib.Description("From")),
			mcplib.WithString("To", mcplib.Description("To")),
			mcplib.WithString("GroupBy", mcplib.Description("0 = group by charger (default), 1 = group by calendar day, 2 = group by user. 0 = Charger 1 = Day 2 = User")),
			mcplib.WithString("DetailLevel", mcplib.Description("Bitflag: 0 = session summary (default), 1 = include detailed session energy data. 0 = Summary 1 = EnergyDetails")),
			mcplib.WithString("SortProperty", mcplib.Description("Name of the property used to sort the result.")),
			mcplib.WithString("SortDescending", mcplib.Description("Set to true to sort items in descending order.")),
			mcplib.WithString("PageSize", mcplib.Description("Number of items to return per page.")),
			mcplib.WithString("PageIndex", mcplib.Description("The page index to return, first page is 0.")),
			mcplib.WithString("IncludeDisabled", mcplib.Description("If true, and your user has access, include disabled objects.")),
			mcplib.WithString("Exclude", mcplib.Description("Get or set a list of item Id's that will be excluded from the result.")),
			mcplib.WithString("PageInfoOnly", mcplib.Description("Returns only information about pages and no data info.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/chargehistory", []mcpParamBinding{{PublicName: "InstallationId", WireName: "InstallationId", Location: "query"}, {PublicName: "UserId", WireName: "UserId", Location: "query"}, {PublicName: "ChargerId", WireName: "ChargerId", Location: "query"}, {PublicName: "From", WireName: "From", Location: "query"}, {PublicName: "To", WireName: "To", Location: "query"}, {PublicName: "GroupBy", WireName: "GroupBy", Location: "query"}, {PublicName: "DetailLevel", WireName: "DetailLevel", Location: "query"}, {PublicName: "SortProperty", WireName: "SortProperty", Location: "query"}, {PublicName: "SortDescending", WireName: "SortDescending", Location: "query"}, {PublicName: "PageSize", WireName: "PageSize", Location: "query"}, {PublicName: "PageIndex", WireName: "PageIndex", Location: "query"}, {PublicName: "IncludeDisabled", WireName: "IncludeDisabled", Location: "query"}, {PublicName: "Exclude", WireName: "Exclude", Location: "query"}, {PublicName: "PageInfoOnly", WireName: "PageInfoOnly", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("chargehistory_installationreport-get",
			mcplib.WithDescription("**Deprecated**: Use the POST version instead. Retrieves a usage report based on the provided filters (requires installation owner permissions). GET requests are limited to a 2048-character URL length and may fail if exceeded. Optional: FromDate, EndDate, InstallationId (plus 4 more)."),
			mcplib.WithString("FromDate", mcplib.Description("From date")),
			mcplib.WithString("EndDate", mcplib.Description("End date")),
			mcplib.WithString("InstallationId", mcplib.Description("Installation id")),
			mcplib.WithString("UserIds", mcplib.Description("User ids")),
			mcplib.WithString("ChargerIds", mcplib.Description("Charger ids")),
			mcplib.WithString("GroupBy", mcplib.Description("0 = User 1 = Charger 2 = ChargeCardName")),
			mcplib.WithString("ReportFormat", mcplib.Description("0 = Summary 1 = Detailed")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/chargehistory/installationreport", []mcpParamBinding{{PublicName: "FromDate", WireName: "FromDate", Location: "query"}, {PublicName: "EndDate", WireName: "EndDate", Location: "query"}, {PublicName: "InstallationId", WireName: "InstallationId", Location: "query"}, {PublicName: "UserIds", WireName: "UserIds", Location: "query"}, {PublicName: "ChargerIds", WireName: "ChargerIds", Location: "query"}, {PublicName: "GroupBy", WireName: "GroupBy", Location: "query"}, {PublicName: "ReportFormat", WireName: "ReportFormat", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("chargehistory_installationreport-post",
			mcplib.WithDescription("Retrieves a usage report based on the provided filters (requires installation owner permissions). Optional: chargerIds, endDate, fromDate (plus 4 more). Returns the new CustomTotalChargerReportModel."),
			mcplib.WithString("chargerIds", mcplib.Description("Charger ids")),
			mcplib.WithString("endDate", mcplib.Description("End date")),
			mcplib.WithString("fromDate", mcplib.Description("From date")),
			mcplib.WithString("groupBy", mcplib.Description("0 = User 1 = Charger 2 = ChargeCardName")),
			mcplib.WithString("installationId", mcplib.Description("Installation id")),
			mcplib.WithString("reportFormat", mcplib.Description("0 = Summary 1 = Detailed")),
			mcplib.WithString("userIds", mcplib.Description("User ids")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/chargehistory/installationreport", []mcpParamBinding{{PublicName: "chargerIds", WireName: "chargerIds", Location: "body"}, {PublicName: "endDate", WireName: "endDate", Location: "body"}, {PublicName: "fromDate", WireName: "fromDate", Location: "body"}, {PublicName: "groupBy", WireName: "groupBy", Location: "body"}, {PublicName: "installationId", WireName: "installationId", Location: "body"}, {PublicName: "reportFormat", WireName: "reportFormat", Location: "body"}, {PublicName: "userIds", WireName: "userIds", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("charger-firmware_installation-installationid-get",
			mcplib.WithDescription("Retrieves firmware details for all chargers in the specified installation (requires owner or service permissions). Required: installationId. Returns array of InstallationChargerFirmwareModel."),
			mcplib.WithString("installationId", mcplib.Required(), mcplib.Description("Installation ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/chargerFirmware/installation/{installationId}", []mcpParamBinding{{PublicName: "installationId", WireName: "installationId", Location: "path"}}, []string{"installationId"}),
	)
	s.AddTool(
		mcplib.NewTool("chargers_get",
			mcplib.WithDescription("Retrieves all chargers accessible by the current user, matching the provided filters. By default, returns the first 50 items. Use `pageIndex` for pagination or adjust `pageSize` (max: 100). Optional: Roles, DeviceType, InstallationType (plus 10 more)."),
			mcplib.WithString("Roles", mcplib.Description("Return only charge points where current user has any of the provided user roles. Note that this is a bit-flag and...")),
			mcplib.WithString("DeviceType", mcplib.Description("Return only charge points of the provided type. 0 = Unknown 1 = Smart 2 = Portable 3 = HomeApm 4 = Apollo 5 =...")),
			mcplib.WithString("InstallationType", mcplib.Description("Return only chargers installed in installations of this time. 0 = Pro 1 = Smart")),
			mcplib.WithString("InstallationId", mcplib.Description("Return only chargers in the specified installation.")),
			mcplib.WithString("NameFilter", mcplib.Description("A filter used to filter items. The filter may be applied to multiple properties depending on the object type.")),
			mcplib.WithString("ReturnIdNameOnly", mcplib.Description("If true objects are only returned with their basic properties like Id and name.")),
			mcplib.WithString("SortProperty", mcplib.Description("Name of the property used to sort the result.")),
			mcplib.WithString("SortDescending", mcplib.Description("Set to true to sort items in descending order.")),
			mcplib.WithString("PageSize", mcplib.Description("Number of items to return per page.")),
			mcplib.WithString("PageIndex", mcplib.Description("The page index to return, first page is 0.")),
			mcplib.WithString("IncludeDisabled", mcplib.Description("If true, and your user has access, include disabled objects.")),
			mcplib.WithString("Exclude", mcplib.Description("Get or set a list of item Id's that will be excluded from the result.")),
			mcplib.WithString("PageInfoOnly", mcplib.Description("Returns only information about pages and no data info.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/chargers", []mcpParamBinding{{PublicName: "Roles", WireName: "Roles", Location: "query"}, {PublicName: "DeviceType", WireName: "DeviceType", Location: "query"}, {PublicName: "InstallationType", WireName: "InstallationType", Location: "query"}, {PublicName: "InstallationId", WireName: "InstallationId", Location: "query"}, {PublicName: "NameFilter", WireName: "NameFilter", Location: "query"}, {PublicName: "ReturnIdNameOnly", WireName: "ReturnIdNameOnly", Location: "query"}, {PublicName: "SortProperty", WireName: "SortProperty", Location: "query"}, {PublicName: "SortDescending", WireName: "SortDescending", Location: "query"}, {PublicName: "PageSize", WireName: "PageSize", Location: "query"}, {PublicName: "PageIndex", WireName: "PageIndex", Location: "query"}, {PublicName: "IncludeDisabled", WireName: "IncludeDisabled", Location: "query"}, {PublicName: "Exclude", WireName: "Exclude", Location: "query"}, {PublicName: "PageInfoOnly", WireName: "PageInfoOnly", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("chargers_id-get",
			mcplib.WithDescription("Retrieves the specified charger (requires owner or service permissions). Required: id. Returns the ChargerListModel."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Charger ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/chargers/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("chargers_send-command_chargers-id-commandid-post",
			mcplib.WithDescription("<b>Permissions:</b> Requires owner or service-level access. <b>Supported Command IDs:</b> - <b>102</b> – Restart the charger - <b>200</b> – Upgrade charger firmware - <b>506</b> – Stop or pause charging - <b>507</b> – Resume charging - <b>10001</b> – Deauthorize and stop charging <b>Command 10001 – Deauthorize and Stop:</b> The caller must ensure that no new charging sessions are authorized until the command is completed. <b>Command 506 – Pause/Stop Charging:</b> - Requires firmware version > 3.2 on PRO chargers - Typical usage: For chargers in active charging mode (state <b>ChargeOperationMode, 710</b> = 3). Other edge cases are supported, but this is the most common scenario. - The command will be rejected if the charger is already paused (state <b>ChargeOperationMode, 710</b> = 5 and <b>FinalStopActive, 718</b> = 1) or disconnected (state <b>ChargeOperationMode, 710</b> = 1) <b>Commands 507 – Resume Charging:</b> - Requires firmware version > 3.2 on PRO chargers - Typical usage: For chargers in paused state (state <b>ChargeOperationMode, 710</b> = 5 and <b>FinalStopActive, 718</b> = 1). Other edge cases are supported, but this is the most common scenario. - The command will be rejected if the charger is not in paused mode (state <b>ChargeOperationMode, 710</b> = 5 and <b>FinalStopActive, 718</b> = 1) neither Scheduler is active (state <b>NextScheduleEvent, 763</b> is set). Required: id, commandId."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Unique identifier of the charger.")),
			mcplib.WithString("commandId", mcplib.Required(), mcplib.Description("The ID of the command to execute.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/chargers/{id}/sendCommand/{commandId}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "commandId", WireName: "commandId", Location: "path"}}, []string{"id", "commandId"}),
	)
	s.AddTool(
		mcplib.NewTool("chargers_state_chargers-id-get",
			mcplib.WithDescription("Retrieves the current state properties (observations) for the specified charger (requires owner or service permissions). State IDs may vary by charger model. Current-generation ZapCharger Smart supports <b>Observations</b> from <a href='/api/constants' target='_blank'>api/constants</a>. Required: id. Returns array of ChargerStateModel."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Charger ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/chargers/{id}/state", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("chargers_update_chargers-id-post",
			mcplib.WithDescription("Updates charger properties (requires owner or service permissions). <b>Charge Current Settings:</b> The `maxChargeCurrent` and `minChargeCurrent` properties should generally not be used. These are intended for special cases where an external party must override the Zaptec load balancer, which may cause unexpected behavior. It is recommended to control the charging system using the installation’s `AvailableCurrent` setting instead. <b>Offline Current/Phase Override:</b> Use only when charging stations must not automatically optimize offline current or phases. - Set `OfflineChargeCurrent` to `-1` to enable automatic allocation. - Any value between `0` and `32` overrides the automatic setting. `null` is ignored. - Set `OfflineChargePhase` to `0 (Phases.None)` to enable automatic allocation. - Any other valid value will override the automatic setting. Ensure that the total of all overridden values in an installation does not exceed the fuse limit. <b>Meter Value Interval:</b> Controls how often periodic energy data is reported. The recommended default is `30 minutes`. Setting this to `0` disables periodic reporting, affecting both session data and signed meter values. Required: id. Optional: maxChargeCurrent, maxChargePhases, meterValueInterval (plus 3 more)."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Charger ID")),
			mcplib.WithString("maxChargeCurrent", mcplib.Description("Adjustable between 0 and 32A. If charge current is below the charger minimum charge current (usually 6A), no charge...")),
			mcplib.WithString("maxChargePhases", mcplib.Description("1 = One 3 = Three")),
			mcplib.WithString("meterValueInterval", mcplib.Description("The interval in seconds for a charger to report meter values. Defaults to 900 seconds for Pro and 3600 seconds for Go")),
			mcplib.WithString("minChargeCurrent", mcplib.Description("The minimum allocated charge current. If there is not enough current available to provide the chargers minimum...")),
			mcplib.WithString("offlineChargeCurrent", mcplib.Description("Adjustable between 0 and 32A. If offline charge current is below the charger minimum charge current (usually 6A), no...")),
			mcplib.WithString("offlineChargePhase", mcplib.Description("0 = None 1 = Phase_1 2 = Phase_2 4 = Phase_3 7 = All")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/chargers/{id}/update", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "maxChargeCurrent", WireName: "maxChargeCurrent", Location: "body"}, {PublicName: "maxChargePhases", WireName: "maxChargePhases", Location: "body"}, {PublicName: "meterValueInterval", WireName: "meterValueInterval", Location: "body"}, {PublicName: "minChargeCurrent", WireName: "minChargeCurrent", Location: "body"}, {PublicName: "offlineChargeCurrent", WireName: "offlineChargeCurrent", Location: "body"}, {PublicName: "offlineChargePhase", WireName: "offlineChargePhase", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("constants_get",
			mcplib.WithDescription("Retrieves a set of predefined constants that rarely change but may be updated occasionally. These constants include schemas, enumerations, supported languages, country data, charger operation modes, network types, user roles, error codes, settings, and more. This data provides system-wide reference values, reducing the need for frequent queries. Returns the ConstantsModel."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/constants", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("installation_get",
			mcplib.WithDescription("Retrieves all installations accessible by the current user, matching the provided filters. By default, returns the first 50 items. Use <b>pageIndex</b> for pagination or adjust <b>pageSize</b> (max: 100). Optional: Roles, InstallationType, ExcludeIfVisibleForUserGroupLookupKey (plus 9 more)."),
			mcplib.WithString("Roles", mcplib.Description("Return only installations where current user has any of the provided user roles. Note that this is a bit-flag and...")),
			mcplib.WithString("InstallationType", mcplib.Description("Return only installations of the provided type. 0 = Pro 1 = Smart")),
			mcplib.WithString("ExcludeIfVisibleForUserGroupLookupKey", mcplib.Description("Excludes installations that is visible for the user group identified by lookup key.")),
			mcplib.WithString("NameFilter", mcplib.Description("A filter used to filter items. The filter may be applied to multiple properties depending on the object type.")),
			mcplib.WithString("ReturnIdNameOnly", mcplib.Description("If true objects are only returned with their basic properties like Id and name.")),
			mcplib.WithString("SortProperty", mcplib.Description("Name of the property used to sort the result.")),
			mcplib.WithString("SortDescending", mcplib.Description("Set to true to sort items in descending order.")),
			mcplib.WithString("PageSize", mcplib.Description("Number of items to return per page.")),
			mcplib.WithString("PageIndex", mcplib.Description("The page index to return, first page is 0.")),
			mcplib.WithString("IncludeDisabled", mcplib.Description("If true, and your user has access, include disabled objects.")),
			mcplib.WithString("Exclude", mcplib.Description("Get or set a list of item Id's that will be excluded from the result.")),
			mcplib.WithString("PageInfoOnly", mcplib.Description("Returns only information about pages and no data info.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/installation", []mcpParamBinding{{PublicName: "Roles", WireName: "Roles", Location: "query"}, {PublicName: "InstallationType", WireName: "InstallationType", Location: "query"}, {PublicName: "ExcludeIfVisibleForUserGroupLookupKey", WireName: "ExcludeIfVisibleForUserGroupLookupKey", Location: "query"}, {PublicName: "NameFilter", WireName: "NameFilter", Location: "query"}, {PublicName: "ReturnIdNameOnly", WireName: "ReturnIdNameOnly", Location: "query"}, {PublicName: "SortProperty", WireName: "SortProperty", Location: "query"}, {PublicName: "SortDescending", WireName: "SortDescending", Location: "query"}, {PublicName: "PageSize", WireName: "PageSize", Location: "query"}, {PublicName: "PageIndex", WireName: "PageIndex", Location: "query"}, {PublicName: "IncludeDisabled", WireName: "IncludeDisabled", Location: "query"}, {PublicName: "Exclude", WireName: "Exclude", Location: "query"}, {PublicName: "PageInfoOnly", WireName: "PageInfoOnly", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("installation_id-get",
			mcplib.WithDescription("Retrieves details for the specified installation. The level of detail available depends on the current user's permissions. Required: id. Optional: DetailLevel. Returns the InstallationModel."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Installation ID")),
			mcplib.WithString("DetailLevel", mcplib.Description("0 = no extra details (default), 1 = include list of users that are present in the installation's charge history, 2 =...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/installation/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "DetailLevel", WireName: "DetailLevel", Location: "query"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("installation_hierarchy_installation-id-get",
			mcplib.WithDescription("Retrieves an installation along with its circuit and charger hierarchy (requires owner or service permissions). <b>Note:</b> Only basic properties of the returned model will be populated. Required: id. Returns the InstallationTreeModel."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Installation ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/installation/{id}/hierarchy", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("installation_messaging-connection-details_installation-id-get",
			mcplib.WithDescription("Retrieves connection details for subscribing to live AMQP 1.0 charger events for the specified installation (requires owner or service permissions). <b>WARNING:</b> This feature is <b>deprecated</b>. Use a single <b>User Group</b> subscription to receive updates for all installations instead. Required: id. Returns the ConnectionDescriptor."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Installation ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/installation/{id}/messagingConnectionDetails", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("installation_update_installation-id-post",
			mcplib.WithDescription("Updates installation properties (requires owner or service permissions). <b>Available Current:</b> Provide <b>either</b><i>AvailableCurrent</i> or (<i>AvailableCurrentPhase1, AvailableCurrentPhase2, AvailableCurrentPhase3</i>). Updating available current will instantly trigger phase and load balancing for the installation. Vehicles will adjust their charging settings within <b>5 seconds</b>. <b>Usage Recommendations:</b> - Do not update this setting more than once every <b>15 minutes</b> to prevent charging interruptions. - Available current is limited to the installation’s configured maximum current (circuit breaker current). - If set to <b>null</b> (not provided), the installation will revert to its maximum current setting. Required: id. Optional: availableCurrent, availableCurrentPhase1, availableCurrentPhase2 (plus 4 more)."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Installation ID")),
			mcplib.WithString("availableCurrent", mcplib.Description("Available current to set on all phases.")),
			mcplib.WithString("availableCurrentPhase1", mcplib.Description("Available current to set on phase 1. When setting current on individual phases, any phase without specified current...")),
			mcplib.WithString("availableCurrentPhase2", mcplib.Description("Available current to set on phase 2. When setting current on individual phases, any phase without specified current...")),
			mcplib.WithString("availableCurrentPhase3", mcplib.Description("Available current to set on phase 3. When setting current on individual phases, any phase without specified current...")),
			mcplib.WithString("maxCurrent", mcplib.Description("The maximum allowed current for the installation. This setting requires caller to have service permission...")),
			mcplib.WithString("minPowerOfflineMode", mcplib.Description("When set to true, offline power will be limited to the chargers miniumum charge current.")),
			mcplib.WithString("threeToOnePhaseSwitchCurrent", mcplib.Description("Set 3to1 switch current for installation")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/installation/{id}/update", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "availableCurrent", WireName: "availableCurrent", Location: "body"}, {PublicName: "availableCurrentPhase1", WireName: "availableCurrentPhase1", Location: "body"}, {PublicName: "availableCurrentPhase2", WireName: "availableCurrentPhase2", Location: "body"}, {PublicName: "availableCurrentPhase3", WireName: "availableCurrentPhase3", Location: "body"}, {PublicName: "maxCurrent", WireName: "maxCurrent", Location: "body"}, {PublicName: "minPowerOfflineMode", WireName: "minPowerOfflineMode", Location: "body"}, {PublicName: "threeToOnePhaseSwitchCurrent", WireName: "threeToOnePhaseSwitchCurrent", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("session_id-get",
			mcplib.WithDescription("Retrieves details for the specified charging session. Required: id. Returns the SessionEndData."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Session ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/session/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("session_priority_session-id-post",
			mcplib.WithDescription("Assigns priority to a charging session (requires owner or service permissions). Prioritized sessions are allocated before unprioritized ones and will receive at least the prioritized current. <b>Important considerations:</b> - Multiple sessions can be prioritized; the caller must ensure enough available current. - If there is insufficient current, prioritized sessions are allocated on a first-come, first-served basis. - After allocating prioritized sessions, any remaining current is distributed among unprioritized sessions. Required: id. Optional: prioritizedCurrent, prioritizedPhases."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Session ID")),
			mcplib.WithString("prioritizedCurrent", mcplib.Description("Prioritized current")),
			mcplib.WithString("prioritizedPhases", mcplib.Description("1 = One 3 = Three")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/session/{id}/priority", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "prioritizedCurrent", WireName: "prioritizedCurrent", Location: "body"}, {PublicName: "prioritizedPhases", WireName: "prioritizedPhases", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("user-groups_messaging-connection-details_usergroups-id-get",
			mcplib.WithDescription("Retrieves connection details for subscribing to live AMQP 1.0 charger events for installations within the specified user group (requires owner or service permissions). For more details on working with AMQP and Service Bus, refer to the <a href='https://learn.microsoft.com/en-us/azure/service-bus-messaging/' target='_blank'> Microsoft Azure Service Bus documentation</a>. Required: id. Returns the ConnectionDescriptor."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("User Group ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/userGroups/{id}/messagingConnectionDetails", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export ZAPTEC_LEGACY_AUTH=<your-key>" +
					"\n      Run 'zaptec-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export ZAPTEC_LEGACY_AUTH=<your-key>" +
					"\n      Run 'zaptec-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export ZAPTEC_LEGACY_AUTH=<your-key>" +
					"\n      Run 'zaptec-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "zaptec-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "zaptec-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "zaptec",
		"description": "The only command-line tool for Zaptec Go chargers — pause and resume charging, read live state in plain English,...",
		"archetype":   "payments",
		"tool_count":  18,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion zaptec-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "ZAPTEC_LEGACY_AUTH",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "chargehistory",
				"description": "Manage chargehistory",
				"endpoints":   []string{"get", "installationreport-get", "installationreport-post"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "charger-firmware",
				"description": "Manage charger firmware",
				"endpoints":   []string{"installation-installationid-get"},
				"searchable":  true,
			},
			{
				"name":        "chargers",
				"description": "Manage chargers",
				"endpoints":   []string{"get", "id-get"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "constants",
				"description": "Manage constants",
				"endpoints":   []string{"get"},
			},
			{
				"name":        "installation",
				"description": "Manage installation",
				"endpoints":   []string{"get", "id-get"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "session",
				"description": "Manage session",
				"endpoints":   []string{"id-get"},
				"searchable":  true,
			},
			{
				"name":        "user-groups",
				"description": "Manage user groups",
				"endpoints":   []string{},
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the pagesize parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Energy & cost rollup", "command": "cost", "description": "See total kWh and cost for your charging over any period, rolled up by month or by charger.", "rationale": "The Zaptec portal only shows session-by-session; a monthly total requires aggregating the local session store, which...", "via": "mcp-command-mirror"},
			{"name": "What's-charging-now snapshot", "command": "live", "description": "One table showing every charger's mode (charging/connected/finished/offline) with decoded power, current, and phase.", "rationale": "Joins synced charger rows with a live state fetch and decodes numeric observation IDs via the baked constants table...", "via": "mcp-command-mirror"},
			{"name": "Load-balancing headroom", "command": "current headroom", "description": "Show how many amps of the installation's breaker limit are uncommitted versus actively drawn.", "rationale": "Subtracts decoded active per-charger draw from the installation's max available-current; operators otherwise tune...", "via": "mcp-command-mirror"},
			{"name": "Stale / offline charger watch", "command": "chargers stale", "description": "List chargers that are offline, disconnected, or haven't reported within a threshold.", "rationale": "A pure local query over synced charger/state rows; the Home Assistant integration polls but offers no offline alerting.", "via": "mcp-command-mirror"},
			{"name": "Firmware drift", "command": "firmware drift", "description": "Group an installation's chargers by firmware version and flag the ones behind the fleet majority.", "rationale": "Turns the raw firmware list into an upgrade decision by computing the modal version across the fleet locally.", "via": "mcp-command-mirror"},
			{"name": "Session anomaly scan", "command": "sessions anomalies", "description": "Flag charging sessions with near-zero energy, abnormally long duration, or zero cost.", "rationale": "Threshold SQL over the local session table surfaces quality issues no incumbent tool reports.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Energy & cost rollup", "insight": "The Zaptec portal only shows session-by-session; a monthly total requires aggregating the local session store, which no single API call returns."},
			{"topic": "What's-charging-now snapshot", "insight": "Joins synced charger rows with a live state fetch and decodes numeric observation IDs via the baked constants table — there is no portal or Home Assistant single-view equivalent."},
			{"topic": "Load-balancing headroom", "insight": "Subtracts decoded active per-charger draw from the installation's max available-current; operators otherwise tune the 15-minute-guarded limit blind."},
			{"topic": "Stale / offline charger watch", "insight": "A pure local query over synced charger/state rows; the Home Assistant integration polls but offers no offline alerting."},
			{"topic": "Firmware drift", "insight": "Turns the raw firmware list into an upgrade decision by computing the modal version across the fleet locally."},
			{"topic": "Session anomaly scan", "insight": "Threshold SQL over the local session table surfaces quality issues no incumbent tool reports."},
			{"topic": "Financial data", "insight": "Always use read-only operations for financial queries. Never use create/update tools for payment data without explicit user confirmation."},
			{"topic": "Reconciliation", "insight": "For reconciliation tasks, sync first then use sql for cross-referencing. API pagination over financial records is slow and rate-limited."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
