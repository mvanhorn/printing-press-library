{
  "manifest_version": "0.3",
  "name": "whoop-pp-mcp",
  "display_name": "Whoop",
  "version": "4.2.2",
  "description": "Whoop API surface as MCP tools.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/whoop-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/whoop-pp-mcp",
      "args": [],
      "env": {
        "WHOOP_OAUTH2": "${user_config.whoop_oauth2}"
      }
    }
  },
  "user_config": {
    "whoop_oauth2": {
      "type": "string",
      "title": "WHOOP_OAUTH2",
      "description": "Sets WHOOP_OAUTH2 for the Whoop MCP server.",
      "sensitive": true,
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
