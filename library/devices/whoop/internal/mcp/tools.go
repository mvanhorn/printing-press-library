// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/devices/whoop/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/devices/whoop/internal/client"
	"github.com/mvanhorn/printing-press-library/library/devices/whoop/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/devices/whoop/internal/config"
	"github.com/mvanhorn/printing-press-library/library/devices/whoop/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/devices/whoop/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("activity_get-sleep",
			mcplib.WithDescription("Get a single sleep by UUID. Required: sleepId. Returns the Sleep."),
			mcplib.WithString("sleepId", mcplib.Required(), mcplib.Description("Sleep id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/activity/sleep/{sleepId}", []mcpParamBinding{{PublicName: "sleepId", WireName: "sleepId", Location: "path"}}, []string{"sleepId"}),
	)
	s.AddTool(
		mcplib.NewTool("activity_get-workout",
			mcplib.WithDescription("Get a single workout by UUID. Required: workoutId. Returns the Workout."),
			mcplib.WithString("workoutId", mcplib.Required(), mcplib.Description("Workout id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/activity/workout/{workoutId}", []mcpParamBinding{{PublicName: "workoutId", WireName: "workoutId", Location: "path"}}, []string{"workoutId"}),
	)
	s.AddTool(
		mcplib.NewTool("activity_list-sleeps",
			mcplib.WithDescription("List sleep activities. Optional: limit (default: 10), start, end (plus 1 more). Returns the ActivityListSleepsResponse."),
			mcplib.WithString("limit", mcplib.Description("Page size (server max 25; default 10).")),
			mcplib.WithString("start", mcplib.Description("ISO-8601 UTC start of time window (defaults to 24h ago).")),
			mcplib.WithString("end", mcplib.Description("ISO-8601 UTC end of time window (defaults to now).")),
			mcplib.WithString("nextToken", mcplib.Description("Cursor from prior response's `next_token`.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/activity/sleep", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "start", WireName: "start", Location: "query"}, {PublicName: "end", WireName: "end", Location: "query"}, {PublicName: "nextToken", WireName: "nextToken", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("activity_list-workouts",
			mcplib.WithDescription("List workouts. Optional: limit (default: 10), start, end (plus 1 more). Returns the ActivityListWorkoutsResponse."),
			mcplib.WithString("limit", mcplib.Description("Page size (server max 25; default 10).")),
			mcplib.WithString("start", mcplib.Description("ISO-8601 UTC start of time window (defaults to 24h ago).")),
			mcplib.WithString("end", mcplib.Description("ISO-8601 UTC end of time window (defaults to now).")),
			mcplib.WithString("nextToken", mcplib.Description("Cursor from prior response's `next_token`.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/activity/workout", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "start", WireName: "start", Location: "query"}, {PublicName: "end", WireName: "end", Location: "query"}, {PublicName: "nextToken", WireName: "nextToken", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("activity-mapping_get",
			mcplib.WithDescription("Translate a v1 sleep/workout id to a v2 UUID. Required: v1Id, type. Returns the ActivityMappingGetResponse."),
			mcplib.WithString("v1Id", mcplib.Required(), mcplib.Description("V1 id")),
			mcplib.WithString("type", mcplib.Required(), mcplib.Description("Resource type being translated.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/activity-mapping/{v1Id}", []mcpParamBinding{{PublicName: "v1Id", WireName: "v1Id", Location: "path"}, {PublicName: "type", WireName: "type", Location: "query"}}, []string{"v1Id"}),
	)
	s.AddTool(
		mcplib.NewTool("cycle_get",
			mcplib.WithDescription("Get a single cycle by id. Required: cycleId. Returns the Cycle."),
			mcplib.WithString("cycleId", mcplib.Required(), mcplib.Description("Cycle id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/cycle/{cycleId}", []mcpParamBinding{{PublicName: "cycleId", WireName: "cycleId", Location: "path"}}, []string{"cycleId"}),
	)
	s.AddTool(
		mcplib.NewTool("cycle_list",
			mcplib.WithDescription("Returns the user's cycles (a WHOOP day) ordered by start time descending. Optional: limit (default: 10), start, end (plus 1 more)."),
			mcplib.WithString("limit", mcplib.Description("Page size (server max 25; default 10).")),
			mcplib.WithString("start", mcplib.Description("ISO-8601 UTC start of time window (defaults to 24h ago).")),
			mcplib.WithString("end", mcplib.Description("ISO-8601 UTC end of time window (defaults to now).")),
			mcplib.WithString("nextToken", mcplib.Description("Cursor from prior response's `next_token`.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/cycle", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "start", WireName: "start", Location: "query"}, {PublicName: "end", WireName: "end", Location: "query"}, {PublicName: "nextToken", WireName: "nextToken", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("cycle_recovery_get-for-cycle",
			mcplib.WithDescription("Get the recovery associated with a cycle. Required: cycleId. Returns the Recovery."),
			mcplib.WithString("cycleId", mcplib.Required(), mcplib.Description("Cycle id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/cycle/{cycleId}/recovery", []mcpParamBinding{{PublicName: "cycleId", WireName: "cycleId", Location: "path"}}, []string{"cycleId"}),
	)
	s.AddTool(
		mcplib.NewTool("cycle_sleep_get-for-cycle",
			mcplib.WithDescription("Get the sleep associated with a cycle. Required: cycleId. Returns the Sleep."),
			mcplib.WithString("cycleId", mcplib.Required(), mcplib.Description("Cycle id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/cycle/{cycleId}/sleep", []mcpParamBinding{{PublicName: "cycleId", WireName: "cycleId", Location: "path"}}, []string{"cycleId"}),
	)
	s.AddTool(
		mcplib.NewTool("recovery_list-recoveries",
			mcplib.WithDescription("List recovery records. Optional: limit (default: 10), start, end (plus 1 more). Returns the RecoveryListRecoveriesResponse."),
			mcplib.WithString("limit", mcplib.Description("Page size (server max 25; default 10).")),
			mcplib.WithString("start", mcplib.Description("ISO-8601 UTC start of time window (defaults to 24h ago).")),
			mcplib.WithString("end", mcplib.Description("ISO-8601 UTC end of time window (defaults to now).")),
			mcplib.WithString("nextToken", mcplib.Description("Cursor from prior response's `next_token`.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/recovery", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "start", WireName: "start", Location: "query"}, {PublicName: "end", WireName: "end", Location: "query"}, {PublicName: "nextToken", WireName: "nextToken", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("user_get-body-measurement",
			mcplib.WithDescription("Get the authenticated user's body measurements from WHOOP. No parameters. Returns the BodyMeasurement object with height_meter (float), weight_kilogram (float), and max_heart_rate (integer). Used by analytics commands that need physiological context; prefer 'whoami' if you also need profile info in one call."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/user/measurement/body", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("user_get-profile",
			mcplib.WithDescription("Get the authenticated user's basic WHOOP profile. No parameters. Returns the UserProfile object with user_id (int64), email, first_name, and last_name. Use this to confirm the active account or look up the user_id needed for cross-referencing other records; prefer 'whoami' if you also need body measurements in one call."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/user/profile/basic", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("user_revoke-oauth-access",
			mcplib.WithDescription("Revoke the user's OAuth access (delete token grants). Destructive."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v1/user/access", []mcpParamBinding{}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export WHOOP_OAUTH2=<your-key>" +
					"\n      Run 'whoop-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export WHOOP_OAUTH2=<your-key>" +
					"\n      Run 'whoop-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export WHOOP_OAUTH2=<your-key>" +
					"\n      Run 'whoop-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "whoop-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "whoop-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "whoop",
		"description": "Official WHOOP Developer Platform v2 API. Provides programmatic access to a user's cycles, sleep, recovery,...",
		"archetype":   "generic",
		"tool_count":  13,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion whoop-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "WHOOP_OAUTH2",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "activity",
				"description": "Manage activity",
				"endpoints":   []string{"get-sleep", "get-workout", "list-sleeps", "list-workouts"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "activity-mapping",
				"description": "V1 -> V2 identifier mapping helper.",
				"endpoints":   []string{"get"},
				"searchable":  true,
			},
			{
				"name":        "cycle",
				"description": "Physiological cycles (a WHOOP 'day').",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "recovery",
				"description": "Recovery scores for each cycle.",
				"endpoints":   []string{"list-recoveries"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "user",
				"description": "User profile and body measurements.",
				"endpoints":   []string{"get-body-measurement", "get-profile", "revoke-oauth-access"},
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Recovery-vs-strain efficiency curve", "command": "analyze efficiency", "description": "Buckets cycles by strain (0-5, 5-10, 10-15, 15-21) and shows mean recovery per bucket vs. the prior equivalent window.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Sleep debt cumulative tracker", "command": "analyze sleep-debt", "description": "Cumulative sum of need_from_sleep_debt_milli weekly, with trend slope and human-friendly interpretation.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Overtraining detector", "command": "analyze overtraining", "description": "Flags days where strain exceeds N sigma above the 90-day mean and shows the recovery delta vs. window mean.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Pairwise metric correlation", "command": "analyze correlate", "description": "Pearson correlation between any two whitelisted WHOOP metrics over a chosen window.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Why-was-today-bad attribution", "command": "analyze why-today", "description": "Ranks today's recovery, HRV, RHR, sleep consistency, and prior-day strain by abs(z-score) vs. personal 14-day baseline.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Raw SQL escape hatch", "command": "sql", "description": "Execute arbitrary read-only SELECT queries against the local SQLite store.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "FTS5 cross-resource search", "command": "search", "description": "FTS5 full-text search across all synced resources (cycle, sleep, recovery, workouts).", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Recovery-vs-strain efficiency curve", "insight": ""},
			{"topic": "Sleep debt cumulative tracker", "insight": ""},
			{"topic": "Overtraining detector", "insight": ""},
			{"topic": "Pairwise metric correlation", "insight": ""},
			{"topic": "Why-was-today-bad attribution", "insight": ""},
			{"topic": "Raw SQL escape hatch", "insight": ""},
			{"topic": "FTS5 cross-resource search", "insight": ""},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
