// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newRecoveryPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagLimit int
	var flagStart string
	var flagEnd string
	var flagNextToken string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "recovery",
		Short:       "List recovery records",
		Long:        "Shortcut for 'recovery list-recoveries'. List recovery records",
		Example:     "  whoop-pp-cli recovery",
		Annotations: map[string]string{"pp:endpoint": "recovery.list-recoveries", "pp:method": "GET", "pp:path": "/v2/recovery", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/recovery"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "recovery", path, map[string]string{
				"limit":     fmt.Sprintf("%v", flagLimit),
				"start":     fmt.Sprintf("%v", flagStart),
				"end":       fmt.Sprintf("%v", flagEnd),
				"nextToken": fmt.Sprintf("%v", flagNextToken),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagLimit, "limit", 10, "Page size (server max 25; default 10).")
	cmd.Flags().StringVar(&flagStart, "start", "", "ISO-8601 UTC start of time window (defaults to 24h ago).")
	cmd.Flags().StringVar(&flagEnd, "end", "", "ISO-8601 UTC end of time window (defaults to now).")
	cmd.Flags().StringVar(&flagNextToken, "next-token", "", "Cursor from prior response's `next_token`.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
