{
  "manifest_version": "0.3",
  "name": "qsys-pp-mcp",
  "display_name": "Q-SYS",
  "version": "0.1.0",
  "description": "Every Q-SYS product spec, configuration procedure, and connection guide in one local index - with equipment-list compatibility checks neither QSC website can do.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/qsys-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/qsys-pp-mcp",
      "args": [],
      "env": {
        "PRINTING_PRESS_CLIENT_PROFILE": "${user_config.printing_press_client_profile}"
      }
    }
  },
  "user_config": {
    "printing_press_client_profile": {
      "type": "string",
      "title": "Client profile",
      "description": "Binds the MCP server to an existing tenant-gated Printing Press client profile.",
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
