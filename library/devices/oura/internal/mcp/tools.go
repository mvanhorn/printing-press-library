// Copyright 2026 slinsmaier and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"math"
	"os"
	"path/filepath"
	"strconv"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/devices/oura/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/devices/oura/internal/client"
	"github.com/mvanhorn/printing-press-library/library/devices/oura/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/devices/oura/internal/config"
	"github.com/mvanhorn/printing-press-library/library/devices/oura/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/devices/oura/internal/store"
)

const (
	mcpToolResultMaxBytes = 60000
	mcpToolResultMaxItems = 50
	// MCP hosts can fan out tool calls faster than a human CLI session.
	// Keep them on the same polite-client limiter path instead of disabling
	// pacing with rate=0; users can still tune human CLI calls with --rate-limit.
	defaultMCPRateLimit = 2
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("daily-activity_list",
			mcplib.WithDescription("List daily activity summaries. Optional: start_date, end_date, next_token. Returns array of DailyActivity."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/daily_activity", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("daily-cardiovascular-age_list",
			mcplib.WithDescription("List daily cardiovascular age estimates. Optional: start_date, end_date, next_token. Returns array of DailyCardiovascularAge."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/daily_cardiovascular_age", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("daily-readiness_list",
			mcplib.WithDescription("List daily readiness scores with contributors. Optional: start_date, end_date, next_token. Returns array of DailyReadiness."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/daily_readiness", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("daily-resilience_list",
			mcplib.WithDescription("List daily resilience scores. Optional: start_date, end_date, next_token. Returns array of DailyResilience."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/daily_resilience", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("daily-sleep_list",
			mcplib.WithDescription("List daily sleep summaries with scores and contributors. Optional: start_date, end_date, next_token. Returns array of DailySleep."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/daily_sleep", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("daily-spo2_list",
			mcplib.WithDescription("List daily SpO2 blood oxygen averages. Optional: start_date, end_date, next_token. Returns array of DailySpo2."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/daily_spo2", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("daily-stress_list",
			mcplib.WithDescription("List daily stress and recovery data. Optional: start_date, end_date, next_token. Returns array of DailyStress."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/daily_stress", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("enhanced-tag_create",
			mcplib.WithDescription("Create an enhanced tag. Required: tag_type_code, start_time. Optional: end_time, comment. Returns the new EnhancedTag."),
			mcplib.WithString("tag_type_code", mcplib.Required(), mcplib.Description("Tag type identifier")),
			mcplib.WithString("start_time", mcplib.Required(), mcplib.Description("Tag start timestamp (ISO 8601)")),
			mcplib.WithString("end_time", mcplib.Description("Tag end timestamp (ISO 8601)")),
			mcplib.WithString("comment", mcplib.Description("Optional text note")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/usercollection/enhanced_tag", false, false, nil, []mcpParamBinding{{PublicName: "tag_type_code", WireName: "tag_type_code", Location: "body"}, {PublicName: "start_time", WireName: "start_time", Location: "body"}, {PublicName: "end_time", WireName: "end_time", Location: "body"}, {PublicName: "comment", WireName: "comment", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("enhanced-tag_delete",
			mcplib.WithDescription("Delete an enhanced tag by document ID. Required: document_id. Returns the DeleteResponse. Destructive."),
			mcplib.WithString("document_id", mcplib.Required(), mcplib.Description("Tag document ID")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v2/usercollection/enhanced_tag/{document_id}", false, false, nil, []mcpParamBinding{{PublicName: "document_id", WireName: "document_id", Location: "path"}}, []string{"document_id"}),
	)
	s.AddTool(
		mcplib.NewTool("enhanced-tag_list",
			mcplib.WithDescription("List enhanced tags (user annotations with optional text). Optional: start_date, end_date, next_token. Returns array of EnhancedTag."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/enhanced_tag", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("heartrate_list",
			mcplib.WithDescription("List heart rate samples for a datetime range (up to 5-min resolution). Optional: start_datetime, end_datetime, next_token. Returns array of HeartRate."),
			mcplib.WithString("start_datetime", mcplib.Description("Start datetime (ISO 8601, e.g. 2026-06-15T00:00:00+00:00)")),
			mcplib.WithString("end_datetime", mcplib.Description("End datetime (ISO 8601)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/heartrate", true, false, nil, []mcpParamBinding{{PublicName: "start_datetime", WireName: "start_datetime", Location: "query"}, {PublicName: "end_datetime", WireName: "end_datetime", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("personal-info_get",
			mcplib.WithDescription("Fetch the authenticated user's personal profile from Oura. No parameters. Returns id, age (years), weight (kg), height (m), and biological_sex. Use this for the static demographics behind derived metrics (e.g. VO2 max, calorie estimates) rather than for daily readiness or activity data, which come from the daily-* commands."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/personal_info", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rest-mode-period_list",
			mcplib.WithDescription("List rest mode periods. Optional: start_date, end_date, next_token. Returns array of RestModePeriod."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/rest_mode_period", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("ring-configuration_list",
			mcplib.WithDescription("List ring configuration records. Optional: start_date, end_date, next_token. Returns array of RingConfiguration."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/ring_configuration", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("session_list",
			mcplib.WithDescription("List mindfulness and breathing sessions. Optional: start_date, end_date, next_token. Returns array of Session."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/session", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sleep_list",
			mcplib.WithDescription("List detailed sleep sessions with stage timelines and HRV data. Optional: start_date, end_date, next_token. Returns array of SleepSession."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/sleep", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sleep-time_list",
			mcplib.WithDescription("List optimal sleep time recommendations. Optional: start_date, end_date, next_token. Returns array of SleepTime."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/sleep_time", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("webhook-subscription_create",
			mcplib.WithDescription("Create a webhook subscription. Required: callback_url, event_type, data_type. Returns the new WebhookSubscription."),
			mcplib.WithString("callback_url", mcplib.Required(), mcplib.Description("HTTPS URL to receive webhook events")),
			mcplib.WithString("event_type", mcplib.Required(), mcplib.Description("Event type to subscribe to (e.g. create, update, delete)")),
			mcplib.WithString("data_type", mcplib.Required(), mcplib.Description("Data type to receive events for")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v2/webhook/subscription", false, false, nil, []mcpParamBinding{{PublicName: "callback_url", WireName: "callback_url", Location: "body"}, {PublicName: "event_type", WireName: "event_type", Location: "body"}, {PublicName: "data_type", WireName: "data_type", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("webhook-subscription_delete",
			mcplib.WithDescription("Delete a webhook subscription. Required: id. Returns the DeleteResponse. Destructive."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Subscription ID")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v2/webhook/subscription/{id}", false, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("webhook-subscription_list",
			mcplib.WithDescription("List webhook subscriptions. Returns array of WebhookSubscription."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/webhook/subscription", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("workout_list",
			mcplib.WithDescription("List workout sessions. Optional: start_date, end_date, next_token. Returns array of Workout."),
			mcplib.WithString("start_date", mcplib.Description("Start date (YYYY-MM-DD)")),
			mcplib.WithString("end_date", mcplib.Description("End date (YYYY-MM-DD)")),
			mcplib.WithString("next_token", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v2/usercollection/workout", true, false, nil, []mcpParamBinding{{PublicName: "start_date", WireName: "start_date", Location: "query"}, {PublicName: "end_date", WireName: "end_date", Location: "query"}, {PublicName: "next_token", WireName: "next_token", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Synced records live in resources(resource_type, id, data); filter by resource_type and use json_extract on data, e.g. SELECT json_extract(data,'$.name') FROM resources WHERE resource_type='items'.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

func formatMCPParamValue(v any) string {
	switch tv := v.(type) {
	case string:
		return tv
	case bool:
		return strconv.FormatBool(tv)
	case float64:
		if math.IsNaN(tv) || math.IsInf(tv, 0) {
			return strconv.FormatFloat(tv, 'f', -1, 64)
		}
		if math.Trunc(tv) == tv && math.Abs(tv) < 1e15 {
			return strconv.FormatInt(int64(tv), 10)
		}
		return strconv.FormatFloat(tv, 'f', -1, 64)
	case float32:
		f := float64(tv)
		if math.IsNaN(f) || math.IsInf(f, 0) {
			return strconv.FormatFloat(f, 'f', -1, 32)
		}
		if math.Trunc(f) == f && math.Abs(f) < 1e15 {
			return strconv.FormatInt(int64(f), 10)
		}
		return strconv.FormatFloat(f, 'f', -1, 32)
	default:
		return fmt.Sprintf("%v", v)
	}
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, formatMCPParamValue(v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = formatMCPParamValue(v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, formatMCPParamValue(v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = formatMCPParamValue(v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export OURA_ACCESS_TOKEN=<your-key>" +
					"\n      Run 'oura-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export OURA_ACCESS_TOKEN=<your-key>" +
					"\n      Run 'oura-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export OURA_ACCESS_TOKEN=<your-key>" +
					"\n      Run 'oura-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcpToolResultText(method, data), nil
	}
}

func mcpToolResultText(method string, data json.RawMessage) *mcplib.CallToolResult {
	trimmed := strings.TrimSpace(string(data))
	if strings.EqualFold(method, "GET") && len(trimmed) > 0 && trimmed[0] == '[' {
		var items []json.RawMessage
		if json.Unmarshal(data, &items) == nil {
			return mcplib.NewToolResultText(string(mcpBoundedListEnvelope("items", items, len(data))))
		}
	}
	if len(data) <= mcpToolResultMaxBytes {
		return mcplib.NewToolResultText(string(data))
	}
	if strings.EqualFold(method, "GET") {
		if out, ok := mcpBoundedSingleArrayObject(data); ok {
			return mcplib.NewToolResultText(string(out))
		}
	}
	return mcplib.NewToolResultText(string(mcpOversizedPreviewEnvelope(data)))
}

func mcpBoundedSingleArrayObject(data json.RawMessage) ([]byte, bool) {
	var obj map[string]json.RawMessage
	if json.Unmarshal(data, &obj) != nil {
		return nil, false
	}
	arrayField := ""
	var items []json.RawMessage
	for key, raw := range obj {
		trimmed := strings.TrimSpace(string(raw))
		if len(trimmed) == 0 || trimmed[0] != '[' {
			continue
		}
		var candidate []json.RawMessage
		if json.Unmarshal(raw, &candidate) != nil {
			continue
		}
		if arrayField != "" {
			return nil, false
		}
		arrayField = key
		items = candidate
	}
	if arrayField == "" {
		return nil, false
	}
	build := func(subset []json.RawMessage) any {
		out := make(map[string]any, len(obj)+6)
		for key, raw := range obj {
			if key == arrayField {
				out[key] = subset
				continue
			}
			out[key] = raw
		}
		if len(subset) < len(items) {
			out["_pp_truncated"] = true
			out["_pp_total_count"] = len(items)
			out["_pp_returned_count"] = len(subset)
			out["_pp_original_bytes"] = len(data)
			out["_pp_max_bytes"] = mcpToolResultMaxBytes
			out["_pp_note"] = "Typed MCP endpoint response exceeded the tool result budget. Narrow the request with limit, offset, filters, search/sql, or a command-mirror tool with --agent/--compact/--select."
		}
		return out
	}
	out := mcpFitJSONItems(items, build)
	if len(out) > mcpToolResultMaxBytes {
		return nil, false
	}
	return out, true
}

func mcpBoundedListEnvelope(field string, items []json.RawMessage, originalBytes int) []byte {
	build := func(subset []json.RawMessage) any {
		out := map[string]any{
			"count": len(items),
			field:   subset,
		}
		if len(subset) < len(items) {
			out["truncated"] = true
			out["returned_count"] = len(subset)
			out["original_bytes"] = originalBytes
			out["max_bytes"] = mcpToolResultMaxBytes
			out["note"] = "Typed MCP endpoint response exceeded the tool result budget. Narrow the request with limit, offset, filters, search/sql, or a command-mirror tool with --agent/--compact/--select."
		}
		return out
	}
	return mcpFitJSONItems(items, build)
}

func mcpFitJSONItems(items []json.RawMessage, build func([]json.RawMessage) any) []byte {
	limit := len(items)
	if limit > mcpToolResultMaxItems {
		limit = mcpToolResultMaxItems
	}
	for n := limit; n >= 0; n-- {
		out, err := json.Marshal(build(items[:n]))
		if err != nil {
			continue
		}
		if len(out) <= mcpToolResultMaxBytes || n == 0 {
			return out
		}
	}
	out, _ := json.Marshal(build(items[:0]))
	return out
}

func mcpOversizedPreviewEnvelope(data json.RawMessage) []byte {
	previewBytes := data
	if len(previewBytes) > 4000 {
		previewBytes = previewBytes[:4000]
	}
	out, _ := json.Marshal(map[string]any{
		"truncated":      true,
		"original_bytes": len(data),
		"max_bytes":      mcpToolResultMaxBytes,
		"preview":        string(previewBytes),
		"note":           "Typed MCP endpoint response exceeded the tool result budget and was not a recognized list envelope. Narrow the request with filters, search/sql, or a command-mirror tool with --agent/--compact/--select.",
	})
	return out
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "oura-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 60*time.Second, defaultMCPRateLimit)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "oura-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	return toolResultJSON(results)
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate rejects multi-statement input, then applies an allowlist (SELECT or
// WITH only) AFTER stripping the leading whitespace, line comments, block
// comments, and semicolons that SQLite itself ignores before parsing. A naive
// HasPrefix check on a keyword blocklist is bypassable by prefixing the
// dangerous statement with "/* x */" or "-- x\n"; a naive leading-keyword
// allowlist is bypassable by appending "; ATTACH DATABASE ...". Combined with
// the empirical fact that modernc.org/sqlite's mode=ro does NOT block VACUUM
// INTO (writes a snapshot to a new file) or ATTACH DATABASE (opens a separate
// writable handle), either bypass produces silent exfiltration to an
// attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	stripped := stripLeadingSQLNoise(query)
	if hasTrailingSQLStatement(stripped) {
		return fmt.Errorf("only a single SELECT or WITH statement is allowed")
	}
	upper := strings.ToUpper(stripped)
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

// hasTrailingSQLStatement reports whether query contains a statement
// terminator followed by more executable SQL. A trailing semicolon is allowed;
// a second statement is not. Semicolons inside string literals, quoted
// identifiers, bracket identifiers, and comments are ignored to match SQLite's
// parser shape closely enough for this security gate.
func hasTrailingSQLStatement(query string) bool {
	inSingle := false
	inDouble := false
	inBacktick := false
	inBracket := false
	inLineComment := false
	inBlockComment := false

	for i := 0; i < len(query); i++ {
		ch := query[i]
		next := byte(0)
		if i+1 < len(query) {
			next = query[i+1]
		}

		switch {
		case inLineComment:
			if ch == '\n' {
				inLineComment = false
			}
			continue
		case inBlockComment:
			if ch == '*' && next == '/' {
				inBlockComment = false
				i++
			}
			continue
		case inSingle:
			if ch == '\'' {
				if next == '\'' {
					i++
					continue
				}
				inSingle = false
			}
			continue
		case inDouble:
			if ch == '"' {
				if next == '"' {
					i++
					continue
				}
				inDouble = false
			}
			continue
		case inBacktick:
			if ch == '`' {
				if next == '`' {
					i++
					continue
				}
				inBacktick = false
			}
			continue
		case inBracket:
			if ch == ']' {
				inBracket = false
			}
			continue
		}

		switch {
		case ch == '-' && next == '-':
			inLineComment = true
			i++
		case ch == '/' && next == '*':
			inBlockComment = true
			i++
		case ch == '\'':
			inSingle = true
		case ch == '"':
			inDouble = true
		case ch == '`':
			inBacktick = true
		case ch == '[':
			inBracket = true
		case ch == ';':
			if stripLeadingSQLNoise(query[i+1:]) != "" {
				return true
			}
			return false
		}
	}
	return false
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, err := rows.Columns()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading columns: %v", err)), nil
	}
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		if err := rows.Scan(ptrs...); err != nil {
			return mcplib.NewToolResultError(fmt.Sprintf("scanning row: %v", err)), nil
		}
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}
	// rows.Next() stops on a mid-iteration error without failing the loop, so
	// skipping rows.Err() would return a truncated result set as success.
	if err := rows.Err(); err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading rows: %v", err)), nil
	}

	return toolResultJSON(results)
}

// toolResultJSON renders v as the indented JSON body of an MCP text result,
// surfacing a marshal failure as a tool error instead of empty content.
func toolResultJSON(v any) (*mcplib.CallToolResult, error) {
	data, err := json.MarshalIndent(v, "", "  ")
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("encoding result: %v", err)), nil
	}
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "oura",
		"description": "Oura Ring health metrics CLI — sync, query, and analyze your ring data offline",
		"archetype":   "payments",
		"tool_count":  21,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion oura-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "OURA_ACCESS_TOKEN",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "daily-activity",
				"description": "Daily activity scores, steps, calories, and MET data",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "daily-cardiovascular-age",
				"description": "Estimated daily cardiovascular age",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "daily-readiness",
				"description": "Daily readiness scores and contributor breakdown",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "daily-resilience",
				"description": "Daily resilience scores",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "daily-sleep",
				"description": "Daily sleep scores and contributor breakdown",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "daily-spo2",
				"description": "Daily blood oxygen (SpO2) averages",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "daily-stress",
				"description": "Daily stress levels and recovery status",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "enhanced-tag",
				"description": "User-created enhanced tags with timestamps and text notes",
				"endpoints":   []string{"create", "delete", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "heartrate",
				"description": "Continuous heart rate time-series samples",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "personal-info",
				"description": "Personal profile information (age, weight, height, biological sex)",
				"endpoints":   []string{"get"},
			},
			{
				"name":        "rest-mode-period",
				"description": "Rest mode periods (user-activated recovery mode)",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "ring-configuration",
				"description": "Ring hardware configuration and firmware version",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "session",
				"description": "Mindfulness and breathing sessions (meditation, napping, breathing exercises)",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "sleep",
				"description": "Detailed sleep sessions with stage breakdown, HRV, and respiratory rate",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "sleep-time",
				"description": "Optimal sleep time recommendations",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "webhook-subscription",
				"description": "Webhook subscriptions for real-time event delivery",
				"endpoints":   []string{"create", "delete", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "workout",
				"description": "Workout sessions with type, duration, calories, and heart rate data",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use start_date for incremental fetches (filter by modification time).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Tag-Outcome Correlation", "command": "correlate", "description": "Find which habits actually move your scores: correlate any enhanced tag string with the next-day change in sleep", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Personal Baseline Engine", "command": "baseline", "description": "See today's metric against your personal rolling mean and standard deviation bands — not Oura's population norms", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "HRV Trend Analysis", "command": "hrv-trend", "description": "Track whether overnight HRV is improving or declining: 7-day and 30-day rolling means, coefficient of variation", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Training Load vs. Readiness Overlay", "command": "training-load", "description": "See your accumulated training stress (7-day rolling)", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Threshold Alert with Exit Codes", "command": "alert", "description": "Exit 1 when a metric has been above or below a threshold for N consecutive days — the scripting primitive that Oura has", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Anomaly Detection", "command": "anomalies", "description": "Flag days where any metric fell more than N standard deviations from your personal rolling mean", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Weekly Digest", "command": "digest", "description": "One structured call returns the week's metric averages, week-over-week deltas, best and worst recovery days", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Webhook Listener", "command": "webhook serve", "description": "Start a local HTTP server that registers with Oura's webhook API and writes incoming events to the local SQLite store", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Sleep Stage Personal Comparison", "command": "sleep-stages", "description": "Compare any night's sleep stage durations against your personal 30-day averages", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Pre/Post Event Analysis", "command": "event", "description": "Show all key metrics for N days before and after a named date to measure the health impact of travel, illness, a race", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Data Gap Audit", "command": "gaps", "description": "List every day in a date range where expected sync data is missing from the local store — identifies ring non-wear and", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Tag-Outcome Correlation", "insight": ""},
			{"topic": "Personal Baseline Engine", "insight": ""},
			{"topic": "HRV Trend Analysis", "insight": ""},
			{"topic": "Training Load vs. Readiness Overlay", "insight": ""},
			{"topic": "Threshold Alert with Exit Codes", "insight": ""},
			{"topic": "Anomaly Detection", "insight": ""},
			{"topic": "Weekly Digest", "insight": ""},
			{"topic": "Webhook Listener", "insight": ""},
			{"topic": "Sleep Stage Personal Comparison", "insight": ""},
			{"topic": "Pre/Post Event Analysis", "insight": ""},
			{"topic": "Data Gap Audit", "insight": ""},
			{"topic": "Financial data", "insight": "Always use read-only operations for financial queries. Never use create/update tools for payment data without explicit user confirmation."},
			{"topic": "Reconciliation", "insight": "For reconciliation tasks, sync first then use sql for cross-referencing. API pagination over financial records is slow and rate-limited."},
		},
	}
	return toolResultJSON(ctx)
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
