version: 2
project_name: synology-dsm-pp-cli
changelog:
  disable: true
builds:
  - id: synology-dsm-pp-cli
    main: ./cmd/synology-dsm-pp-cli
    binary: synology-dsm-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/devices/synology-dsm/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: synology-dsm-pp-mcp
    main: ./cmd/synology-dsm-pp-mcp
    binary: synology-dsm-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: synology-dsm-pp-cli
    repository:
      owner: eric-jung
      name: homebrew-tap
    homepage: "https://github.com/eric-jung/synology-dsm-pp-cli"
    description: "REST API for Synology DiskStation Manager (DSM 7.x). All API calls are dispatched through /webapi/entry.cgi with..."
    install: |
      bin.install "synology-dsm-pp-cli"
      bin.install "synology-dsm-pp-mcp"
