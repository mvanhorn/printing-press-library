# AVer USA CLI Absorb Manifest

Run: 20260811-213616-3ef4d308 · Stamp: 2026-08-11-213616

## Absorbed (match or beat everything that exists)

| # | Feature | Best Source | Our Implementation | Added Value |
|---|---------|-----------|-------------------|-------------|
| 1 | Search manuals by brand + model | manualslib.com / manualzz.com search | averusa-pp-cli docs search <query> | Type-filtered, offline FTS5, `--json`/`--agent`, no ads |
| 2 | Browse manual content pages | manualslib.com / manualzz.com browse | (behavior in averusa-pp-cli docs get <doc-id>) ... local cached PDF + metadata, offline read | Official vendor PDF, offline, no ad-walled page flips |
| 3 | Download manual PDF | AVer support portal fileField (`/support/servlet/fileField?entityId={kaId}&field=File__Body__s`) | averusa-pp-cli docs download <doc-id> --out <dir> | Direct vendor PDF, `--dry-run`, resumable, stable names |
| 4 | Per-product document list | averusa.com product pages (`/products/{category}/{model}/`) | averusa-pp-cli products docs <model> | Type-tagged per-model doc set, offline |
| 5 | Product catalog browse | averusa.com `/products/` | averusa-pp-cli products list --category <cat> | Offline SQLite, `--json`, no portal maze |
| 6 | Category / doc-type filtered library | AVer support portal category article lists | (behavior in averusa-pp-cli docs list --type user-manual) ... first-class doc-type filters | Headline filters: **user manual, spec sheet, white paper** — the user's first-class doc types |
| 7 | Spec sheet / datasheet PDF | averusa.com `/business/downloads/datasheet-brochure/{model}-datasheet.pdf` | (behavior in averusa-pp-cli docs download) ... datasheet entries indexed by model slug | Spec sheets tied to model slugs, plain HTTP |
| 8 | Doc metadata (title, date, views) | AVer portal article SSR (crawler UA) | (behavior in averusa-pp-cli docs list) ... richer schema for sort/filter | Structured sort/filter no site page offers |

## Transcendence (only possible with our approach)

| # | Feature | Command | Score | Buildability | How It Works | Evidence | Long Description |
|---|---------|---------|-------|--------------|--------------|----------|------------------|
| 1 | Model spec comparison | `compare CAM570 CAM550` | 8/10 | hand-code | Joins local `products` ⋈ `spec_fields` (fields extracted from each model's datasheet PDF at sync time) and renders a transposed spec matrix, no external dependencies | Brief workflow #3 "Compare models"; live probe: `cam570-datasheet.pdf` returns 200 application/pdf; product page has zero `<table>` elements, so specs exist only in PDFs | Use this command to compare spec fields side-by-side across AVer models. Do NOT use it for a single-model spec dump; use 'specs <model>' instead. Do NOT use it to check document availability; use 'coverage <category>' instead. |
| 2 | Single-model spec dump | `specs CAM570 --json --agent` | 8/10 | hand-code | Reads local `spec_fields` (populated at sync from the datasheet) and emits structured text/JSON for RFP tables, no external dependencies | Brief workflow #2 explicitly asks for "extractable spec fields"; live probe confirms Specifications links resolve only to PDFs | Use this command to dump one model's structured spec fields for RFP compliance or agents. Do NOT use it to compare models; use 'compare' instead. |
| 3 | Doc link audit | `docs audit` | 8/10 | hand-code | Reads PDF URLs from the local `documents` table and issues HTTP HEAD/GET checks to flag 404s and 61301-byte soft-404 shells, caching results to SQLite, no external dependencies | Brief Reachability Risk documents the soft-404 shell; live probe found the CAM570 page linking `cam520pro3-datasheet.pdf` alongside `cam570-datasheet.pdf` (mislink) — dead/mislinked links are real | none |
| 4 | What's-new diff | `whats-new --since 7d` | 7/10 | hand-code | Compares the local `sync_state` cursor against `documents`/`products` `updated_at` to list rows added or updated since last sync, no external dependencies | No changelog or diff surface exists on averusa.com (homepage/support pages show none); offline-library workflow implies churn tracking | none |
| 5 | Doc coverage matrix | `coverage conference-camera` | 7/10 | hand-code | LEFT JOINs local `products` ⋈ `documents` and pivots on the 8-value doc-type enum to render a model×type matrix with MISSING flags, no external dependencies | Brief data layer defines categories→models→documents with the doc-type enum; live /support/ page shows per-model doc coverage is non-uniform | Use this command to see which doc types exist per model in a category and what is missing. Do NOT use it to compare spec values; use 'compare' instead. |
| 6 | Offline job bag | `docs pack CAM570 --out ./site/ --dry-run` | 8/10 | hand-code | Groups the local `documents` table by model and runs the existing `docs download` flow per member with deterministic `<model>-<type>` naming, no external dependencies | Brief workflow #1 "Get the manual" (field ritual) + offline-library workflow; portal filenames are unstructured ("AVer PTZ Link User Manual EN v1.02_2021.07.12.pdf") so stable naming is required | Use this command to batch-download every document for a model into one offline folder. Do NOT use it to fetch a single named document; use 'docs download <doc-id>' instead. |
| 7 | Current vs discontinued | `products status --category conference-camera` | 6/10 | hand-code | Reads a local `products.discontinued` flag (set at sync from the /support/ page's "Discontinued Devices" sections) and filters/group by category, no external dependencies | Live probe: /support/ page contains 6 "Discontinued Devices" sections (e.g., CAM130, VC520, PTZ330N) — the only current/discontinued signal, not structured in the brief's data layer | none |

## Source Notes
- **No community tooling exists for averusa.com documents.** Step 1.5a searches (github/npm/PyPI/lobehub/fastmcp/mcpmarket) surfaced only `companion-module-aver-ptz` (Bitfocus Companion module for AVer PTZ camera *device control* over VISCA/IP) — an adjacent domain, deliberately **not absorbed**; it belongs in anti-triggers, not features. Table stakes manualslib/manualzz/manualsonline have no CLI, no API, and no spec-sheet/white-paper coverage — every document feature here is a beat.
- Absorbed features 1-8 match or beat the manual-mirror sites and the vendor's own portal; all work offline, with `--json`, and SQLite persistence.

## Scope Notes
- The CLI targets the **document catalog** (user manual, spec sheet/datasheet, white paper + product catalog and PDF download) per the user's ask. AVer **device control** (PTZ/IP camera control — VISCA/HTTP) is out of scope and belongs in anti-triggers.
- **EntityId gap: FIXED.** The article→`entityId` mapping is now **complete: 737/737** urlNames resolved via Salesforce Aura replay (`RecordServiceComponentController.getArticleVersionId`) over **plain HTTP** (fresh fwuid from the shell HTML, `aura.token: null` guest session). Census of all 737 SSRs found **53 file-bearing articles**; **49 verified to download real PDFs** (200 `application/pdf`) via `fileField` with freshly-resolved ids (4 no-body 204s link to external downloaders/App stores). Full mapping: `discovery/averusa-article-entityids.json`; census: `discovery/averusa-attachment-census.json`. The CLI's sync resolves entityIds at runtime the same way — no hardcoded seeds, no browser.
- **Data-source strategy:** all 7 novel commands are `hand-code` (`local` for compare/specs/whats-new/coverage/docs pack/products status; `auto` for docs audit). No `spec-emits` rows. Hand-code commitment: 7 features, each ~50-150 LoC plus root.go wiring.
