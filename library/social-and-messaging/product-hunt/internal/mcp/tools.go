// Copyright 2026 actionsslave. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/product-hunt/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/product-hunt/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/product-hunt/internal/client"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/product-hunt/internal/config"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/product-hunt/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/product-hunt/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("collections_get",
			mcplib.WithDescription("Get a collection by slug or numeric ID. Required: slug. Returns the Collection."),
			mcplib.WithString("slug", mcplib.Required(), mcplib.Description("Collection slug or numeric ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql/collections/{slug}", []mcpParamBinding{{PublicName: "slug", WireName: "slug", Location: "path"}, }, []string{"slug", }),
	)
	s.AddTool(
		mcplib.NewTool("collections_list",
			mcplib.WithDescription("List collections. Optional: featured, user, limit (plus 1 more). Returns array of Collection."),
			mcplib.WithString("featured", mcplib.Description("Only show featured collections")),
			mcplib.WithString("user", mcplib.Description("Filter by user ID")),
			mcplib.WithString("limit", mcplib.Description("Maximum collections to return (default: 20)")),
			mcplib.WithString("sort", mcplib.Description("Sort order: NEWEST, FOLLOWERS_COUNT, FEATURED_AT (default: FOLLOWERS_COUNT)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql/collections", []mcpParamBinding{{PublicName: "featured", WireName: "featured", Location: "query"},{PublicName: "user", WireName: "user", Location: "query"},{PublicName: "limit", WireName: "limit", Location: "query"},{PublicName: "sort", WireName: "sort", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("comments_get",
			mcplib.WithDescription("Get a comment by ID. Required: id. Returns the Comment."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Comment numeric ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql/comments/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, }, []string{"id", }),
	)
	s.AddTool(
		mcplib.NewTool("posts_comments",
			mcplib.WithDescription("List comments for a post. Required: slug. Optional: sort, limit. Returns array of Comment."),
			mcplib.WithString("slug", mcplib.Required(), mcplib.Description("Post slug or numeric ID")),
			mcplib.WithString("sort", mcplib.Description("Sort order: NEWEST, VOTES (default: VOTES)")),
			mcplib.WithString("limit", mcplib.Description("Maximum comments to return (default: 20)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql/{slug}/comments", []mcpParamBinding{{PublicName: "slug", WireName: "slug", Location: "path"},{PublicName: "sort", WireName: "sort", Location: "query"},{PublicName: "limit", WireName: "limit", Location: "query"}, }, []string{"slug", }),
	)
	s.AddTool(
		mcplib.NewTool("posts_get",
			mcplib.WithDescription("Get a specific post by slug or numeric ID. Required: slug. Returns the Post."),
			mcplib.WithString("slug", mcplib.Required(), mcplib.Description("Post slug or numeric ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql/{slug}", []mcpParamBinding{{PublicName: "slug", WireName: "slug", Location: "path"}, }, []string{"slug", }),
	)
	s.AddTool(
		mcplib.NewTool("posts_list",
			mcplib.WithDescription("List posts with optional topic, sort, and date filters. Optional: topic, sort, featured (plus 3 more). Returns array of Post."),
			mcplib.WithString("topic", mcplib.Description("Filter by topic slug (e.g. developer-tools)")),
			mcplib.WithString("sort", mcplib.Description("Sort order: RANKING, NEWEST, VOTES, FEATURED_AT (default: RANKING)")),
			mcplib.WithString("featured", mcplib.Description("Only show featured posts")),
			mcplib.WithString("after", mcplib.Description("Show posts after this date (ISO 8601)")),
			mcplib.WithString("before", mcplib.Description("Show posts before this date (ISO 8601)")),
			mcplib.WithString("limit", mcplib.Description("Maximum number of posts to return (default: 20)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", []mcpParamBinding{{PublicName: "topic", WireName: "topic", Location: "query"},{PublicName: "sort", WireName: "sort", Location: "query"},{PublicName: "featured", WireName: "featured", Location: "query"},{PublicName: "after", WireName: "after", Location: "query"},{PublicName: "before", WireName: "before", Location: "query"},{PublicName: "limit", WireName: "limit", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("topics_get",
			mcplib.WithDescription("Get a topic by slug or numeric ID. Required: slug. Returns the Topic."),
			mcplib.WithString("slug", mcplib.Required(), mcplib.Description("Topic slug or numeric ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql/topics/{slug}", []mcpParamBinding{{PublicName: "slug", WireName: "slug", Location: "path"}, }, []string{"slug", }),
	)
	s.AddTool(
		mcplib.NewTool("topics_list",
			mcplib.WithDescription("List topics with optional search query. Optional: query, limit, sort. Returns array of Topic."),
			mcplib.WithString("query", mcplib.Description("Filter topics by name")),
			mcplib.WithString("limit", mcplib.Description("Maximum topics to return (default: 20)")),
			mcplib.WithString("sort", mcplib.Description("Sort order: NEWEST, FOLLOWERS_COUNT (default: FOLLOWERS_COUNT)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql/topics", []mcpParamBinding{{PublicName: "query", WireName: "query", Location: "query"},{PublicName: "limit", WireName: "limit", Location: "query"},{PublicName: "sort", WireName: "sort", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("users_follow",
			mcplib.WithDescription("Follow a user. Required: username. Returns the new User."),
			mcplib.WithString("username", mcplib.Required(), mcplib.Description("Username to follow")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql/users/{username}/follow", []mcpParamBinding{{PublicName: "username", WireName: "username", Location: "path"}, }, []string{"username", }),
	)
	s.AddTool(
		mcplib.NewTool("users_get",
			mcplib.WithDescription("Get a user profile by username or numeric ID. Required: username. Optional: posts_limit. Returns the User."),
			mcplib.WithString("username", mcplib.Required(), mcplib.Description("Username or numeric ID")),
			mcplib.WithString("posts_limit", mcplib.Description("Number of recent posts to include (default: 10)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql/users/{username}", []mcpParamBinding{{PublicName: "username", WireName: "username", Location: "path"},{PublicName: "posts_limit", WireName: "posts_limit", Location: "query"}, }, []string{"username", }),
	)
	s.AddTool(
		mcplib.NewTool("users_me",
			mcplib.WithDescription("Get the authenticated user's profile. Returns the User."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql/viewer", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("users_unfollow",
			mcplib.WithDescription("Unfollow a user. Required: username. Returns the User. Destructive."),
			mcplib.WithString("username", mcplib.Required(), mcplib.Description("Username to unfollow")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/graphql/users/{username}/follow", []mcpParamBinding{{PublicName: "username", WireName: "username", Location: "path"}, }, []string{"username", }),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export PRODUCT_HUNT_TOKEN=<your-key>" +
					"\n      Run 'product-hunt-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export PRODUCT_HUNT_TOKEN=<your-key>" +
					"\n      Run 'product-hunt-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export PRODUCT_HUNT_TOKEN=<your-key>" +
					"\n      Run 'product-hunt-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "product-hunt-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "product-hunt-pp-cli", "data.db")
}
// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "product-hunt",
		"description": "Every Product Hunt feature, plus launch-day momentum tracking, offline search, and a local database no other Product...",
		"archetype":   "content",
		"tool_count":  12,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion product-hunt-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name": "PRODUCT_HUNT_TOKEN",
					"kind": "per_call",
					"required": true,
					"sensitive": true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name": "collections",
				"description": "Browse Product Hunt curated collections",
				"endpoints": []string{"get", "list",  },
				"syncable": true,
				"searchable": true,
			},
			{
				"name": "comments",
				"description": "Look up individual Product Hunt comments",
				"endpoints": []string{"get",  },
				"searchable": true,
			},
			{
				"name": "posts",
				"description": "Browse and track Product Hunt launches",
				"endpoints": []string{"comments", "get", "list",  },
				"syncable": true,
				"searchable": true,
			},
			{
				"name": "topics",
				"description": "Browse and monitor Product Hunt topic categories",
				"endpoints": []string{"get", "list",  },
				"syncable": true,
				"searchable": true,
			},
			{
				"name": "users",
				"description": "Look up Product Hunt maker and hunter profiles",
				"endpoints": []string{"follow", "get", "me", "unfollow",  },
				"searchable": true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Launch Momentum Tracker", "command": "posts momentum", "description": "See live vote and comment deltas for any post since your last check — no browser required on launch day.", "rationale": "Requires diffing a live GraphQL query against a SQLite snapshot; no existing PH tool has a named momentum command.", "via": "mcp-command-mirror"},
			{"name": "Competitor Audit", "command": "topics audit", "description": "Get every recent launch in a topic ranked by votes, with maker overlap detection across top posts.", "rationale": "Requires joining live API results with the synced maker table; no existing CLI exposes this as a named command.", "via": "mcp-command-mirror"},
			{"name": "Daily Digest", "command": "digest", "description": "Structured summary of yesterday's or today's top launches per topic — works offline after sync.", "rationale": "Requires time-windowed aggregation from the local store; the output shape is designed for LLM pipelines.", "via": "mcp-command-mirror"},
			{"name": "Watchlist", "command": "watchlist", "description": "Pin any posts (yours, competitors), then batch-refresh all of them at once to see current stats and deltas.", "rationale": "Requires local JSON state for the pinned list plus GraphQL queries for each; no existing PH CLI has a named watchlist.", "via": "mcp-command-mirror"},
			{"name": "Topic Subscription Inbox", "command": "topics inbox", "description": "See only posts that are new since you last checked for each subscribed topic — cursor-based, never re-shows seen...", "rationale": "Requires storing a per-topic last-seen cursor locally and querying the API for posts newer than that cursor.", "via": "mcp-command-mirror"},
			{"name": "Topic Velocity", "command": "topics velocity", "description": "Week-over-week post count and average vote delta for a topic — is your category heating up or cooling down?", "rationale": "Requires time-windowed aggregation grouped by week from the synced store; not computable from a single API call.", "via": "mcp-command-mirror"},
			{"name": "Post Vote-Rate", "command": "posts vote-rate", "description": "Rank posts by votes-per-day-since-launch instead of raw vote totals — surfaces underrated recent launches.", "rationale": "Requires computing votes ÷ days-since-launch for each synced post; raw-votes ranking buries recent products under...", "via": "mcp-command-mirror"},
			{"name": "Trending Topics", "command": "topics trending", "description": "Which topics have the fastest-growing post count and vote volume this week versus last week?", "rationale": "Requires comparing this week vs prior week grouped by topic from the synced store; a single API call cannot return this.", "via": "mcp-command-mirror"},
			{"name": "Cross-Topic Post Finder", "command": "posts cross-topic", "description": "Find posts that appear in multiple specified topics simultaneously — 'AI tools that are also productivity tools'.", "rationale": "Requires a SQLite JOIN on the post_topics table across multiple topic slugs; not a trivial query for non-SQL users.", "via": "mcp-command-mirror"},
			{"name": "Maker Portfolio Summary", "command": "makers portfolio", "description": "Aggregate stats across a maker's full product history: total votes, avg per launch, best launch, days since last launch.", "rationale": "Requires fetching all user posts from the API and aggregating across them; no existing CLI surfaces these combined...", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Launch Momentum Tracker", "insight": "Requires diffing a live GraphQL query against a SQLite snapshot; no existing PH tool has a named momentum command."},
			{"topic": "Competitor Audit", "insight": "Requires joining live API results with the synced maker table; no existing CLI exposes this as a named command."},
			{"topic": "Daily Digest", "insight": "Requires time-windowed aggregation from the local store; the output shape is designed for LLM pipelines."},
			{"topic": "Watchlist", "insight": "Requires local JSON state for the pinned list plus GraphQL queries for each; no existing PH CLI has a named watchlist."},
			{"topic": "Topic Subscription Inbox", "insight": "Requires storing a per-topic last-seen cursor locally and querying the API for posts newer than that cursor."},
			{"topic": "Topic Velocity", "insight": "Requires time-windowed aggregation grouped by week from the synced store; not computable from a single API call."},
			{"topic": "Post Vote-Rate", "insight": "Requires computing votes ÷ days-since-launch for each synced post; raw-votes ranking buries recent products under old ones."},
			{"topic": "Trending Topics", "insight": "Requires comparing this week vs prior week grouped by topic from the synced store; a single API call cannot return this."},
			{"topic": "Cross-Topic Post Finder", "insight": "Requires a SQLite JOIN on the post_topics table across multiple topic slugs; not a trivial query for non-SQL users."},
			{"topic": "Maker Portfolio Summary", "insight": "Requires fetching all user posts from the API and aggregating across them; no existing CLI surfaces these combined stats."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
