// Copyright 2026 actionsslave. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"

	"github.com/spf13/cobra"
)

func newUsersFollowCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "follow <username>",
		Short: "Follow a user",
		Example: "  product-hunt-pp-cli users follow example-resource",
		Annotations: map[string]string{"pp:endpoint": "users.follow", "pp:method": "POST", "pp:path": "/graphql/users/{username}/follow"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				fmt.Fprintf(cmd.OutOrStdout(), "would follow user: %s\n", args[0])
				return nil
			}
			phc, err := flags.newPHClient()
			if err != nil {
				return err
			}

			user, err := phc.FollowUser(cmd.Context(), args[0])
			if err != nil {
				return classifyAPIError(err, flags)
			}

			data, err := json.Marshal(user)
			if err != nil {
				return err
			}

			envelope := map[string]any{
				"action":  "follow",
				"success": true,
				"data":    user,
			}
			envelopeJSON, _ := json.Marshal(envelope)
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	return cmd
}
