// Copyright 2026 actionsslave. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCollectionsGetCmd(flags *rootFlags) *cobra.Command {

	cmd := &cobra.Command{
		Use:   "get <slug>",
		Short: "Get a collection by slug or numeric ID",
		Example: "  product-hunt-pp-cli collections get example-value",
		Annotations: map[string]string{"pp:endpoint": "collections.get", "pp:method": "GET", "pp:path": "/graphql/collections/{slug}", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				return nil
			}
			phc, err := flags.newPHClient()
			if err != nil {
				return err
			}

			collection, err := phc.GetCollection(cmd.Context(), args[0])
			if err != nil {
				return classifyAPIError(err, flags)
			}

			data, err := json.Marshal(collection)
			if err != nil {
				return err
			}

			prov := DataProvenance{Source: "live"}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}

	return cmd
}
