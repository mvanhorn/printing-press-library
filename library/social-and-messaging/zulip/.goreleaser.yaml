version: 2
project_name: zulip-pp-cli
changelog:
  disable: true
builds:
  - id: zulip-pp-cli
    main: ./cmd/zulip-pp-cli
    binary: zulip-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/social-and-messaging/zulip/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: zulip-pp-mcp
    main: ./cmd/zulip-pp-mcp
    binary: zulip-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: zulip-pp-cli
    repository:
      owner: haqiramadhani
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Powerful open source group chat"
    install: |
      bin.install "zulip-pp-cli"
      bin.install "zulip-pp-mcp"
