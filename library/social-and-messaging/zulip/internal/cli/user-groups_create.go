// Copyright 2026 haqiramadhani. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newUserGroupsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyCanAddMembersGroup string
	var bodyCanJoinGroup string
	var bodyCanLeaveGroup string
	var bodyCanManageGroup string
	var bodyCanMentionGroup string
	var bodyCanRemoveMembersGroup string
	var bodyDescription string
	var bodyMembers string
	var bodyName string
	var bodySubgroups string

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create a new [user group](/help/user-groups). **Changes**: Prior to Zulip 12.0 (feature level 496), bot users were...",
		Example:     "  zulip-pp-cli user-groups create --description example-value",
		Annotations: map[string]string{"pp:endpoint": "user-groups.create", "pp:method": "POST", "pp:path": "/user_groups/create"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("description") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "description")
			}
			if !cmd.Flags().Changed("members") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "members")
			}
			if !cmd.Flags().Changed("name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "name")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/user_groups/create"
			params := map[string]string{}
			fields := url.Values{}
			if bodyCanAddMembersGroup != "" {
				fields.Set("can_add_members_group", bodyCanAddMembersGroup)
			}
			if bodyCanJoinGroup != "" {
				fields.Set("can_join_group", bodyCanJoinGroup)
			}
			if bodyCanLeaveGroup != "" {
				fields.Set("can_leave_group", bodyCanLeaveGroup)
			}
			if bodyCanManageGroup != "" {
				fields.Set("can_manage_group", bodyCanManageGroup)
			}
			if bodyCanMentionGroup != "" {
				fields.Set("can_mention_group", bodyCanMentionGroup)
			}
			if bodyCanRemoveMembersGroup != "" {
				fields.Set("can_remove_members_group", bodyCanRemoveMembersGroup)
			}
			if bodyDescription != "" {
				fields.Set("description", bodyDescription)
			}
			if bodyMembers != "" {
				if !json.Valid([]byte(bodyMembers)) {
					return fmt.Errorf("parsing --members JSON: invalid JSON")
				}
				fields.Set("members", bodyMembers)
			}
			if bodyName != "" {
				fields.Set("name", bodyName)
			}
			if bodySubgroups != "" {
				if !json.Valid([]byte(bodySubgroups)) {
					return fmt.Errorf("parsing --subgroups JSON: invalid JSON")
				}
				fields.Set("subgroups", bodySubgroups)
			}

			data, statusCode, err := c.PostFormWithParams(path, params, fields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "user-groups", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user-groups", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user-groups", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user-groups", partialFailure.Message))
					}
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "user-groups",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user-groups", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "user-groups", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyCanAddMembersGroup, "can-add-members-group", "", "Can add members group")
	cmd.Flags().StringVar(&bodyCanJoinGroup, "can-join-group", "", "Can join group")
	cmd.Flags().StringVar(&bodyCanLeaveGroup, "can-leave-group", "", "Can leave group")
	cmd.Flags().StringVar(&bodyCanManageGroup, "can-manage-group", "", "Can manage group")
	cmd.Flags().StringVar(&bodyCanMentionGroup, "can-mention-group", "", "Can mention group")
	cmd.Flags().StringVar(&bodyCanRemoveMembersGroup, "can-remove-members-group", "", "Can remove members group")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "The description of the user group.")
	cmd.Flags().StringVar(&bodyMembers, "members", "", "An array containing the user IDs of the initial members for the new user group.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the user group.")
	cmd.Flags().StringVar(&bodySubgroups, "subgroups", "", "An array containing the IDs of the initial subgroups for the new user group. User can add subgroups to the new group...")

	return cmd
}
