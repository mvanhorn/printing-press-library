// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/instagram/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/instagram/internal/client"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/instagram/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/instagram/internal/config"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/instagram/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/instagram/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("accounts_info",
			mcplib.WithDescription("Get a profile's web info by username (id, full_name, biography, follower_count, profile_pic_url, is_private). Required: username. Returns the ProfileInfo."),
			mcplib.WithString("username", mcplib.Required(), mcplib.Description("Instagram username, with or without leading @.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/users/web_profile_info/", []mcpParamBinding{{PublicName: "username", WireName: "username", Location: "query"}}, []string{"username"}),
	)
	s.AddTool(
		mcplib.NewTool("accounts_user",
			mcplib.WithDescription("Get a user's full mobile-API profile by numeric user ID (richer fields than web info). Required: user_id. Returns the UserInfo."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("Numeric Instagram user ID (e.g., 25025320 for @instagram).")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/users/{user_id}/info/", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("feed_liked",
			mcplib.WithDescription("Fetch the authenticated user's liked posts. Optional: cursor. Returns the FeedPage."),
			mcplib.WithString("cursor", mcplib.Description("Pagination cursor returned from a prior page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/feed/liked/", []mcpParamBinding{{PublicName: "cursor", WireName: "max_id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("feed_reels",
			mcplib.WithDescription("Fetch active stories (reels media) for a list of user IDs. Stories expire 24 hours after posting. Required: user-ids. Returns the ReelsMedia."),
			mcplib.WithString("user-ids", mcplib.Required(), mcplib.Description("Comma-separated numeric user IDs to fetch stories for.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/feed/reels_media/", []mcpParamBinding{{PublicName: "user-ids", WireName: "reel_ids", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("feed_saved",
			mcplib.WithDescription("Fetch the authenticated user's saved (bookmarked) posts. Optional: cursor. Returns the FeedPage."),
			mcplib.WithString("cursor", mcplib.Description("Pagination cursor returned from a prior page (max_id from the previous response).")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/feed/saved/", []mcpParamBinding{{PublicName: "cursor", WireName: "max_id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("feed_timeline",
			mcplib.WithDescription("Fetch the authenticated user's home feed. Optional: cursor. Returns the FeedPage."),
			mcplib.WithString("cursor", mcplib.Description("Pagination cursor returned from a prior page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/feed/timeline/", []mcpParamBinding{{PublicName: "cursor", WireName: "max_id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("feed_user",
			mcplib.WithDescription("Fetch a profile's media feed by numeric user ID. Pagination cursor is max_id. Required: user_id. Optional: cursor. Returns the FeedPage."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("Numeric Instagram user ID.")),
			mcplib.WithString("cursor", mcplib.Description("Pagination cursor returned from a prior page.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/feed/user/{user_id}/", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}, {PublicName: "cursor", WireName: "max_id", Location: "query"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("hashtag_info",
			mcplib.WithDescription("Get a hashtag's web info (post count, top posts, recent posts cursor). Required: tag. Returns the HashtagInfo."),
			mcplib.WithString("tag", mcplib.Required(), mcplib.Description("Hashtag name without the leading # (e.g., 'sunset').")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/tags/web_info/", []mcpParamBinding{{PublicName: "tag", WireName: "tag_name", Location: "query"}}, []string{"tag_name"}),
	)
	s.AddTool(
		mcplib.NewTool("highlights_reel",
			mcplib.WithDescription("Fetch a single highlight reel's items by highlight reel ID (prefix with 'highlight:', e.g., highlight:17855928765404872). Required: highlight-ids. Returns the ReelsMedia."),
			mcplib.WithString("highlight-ids", mcplib.Required(), mcplib.Description("Comma-separated highlight reel IDs (each prefixed with 'highlight:').")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/feed/reels_media/", []mcpParamBinding{{PublicName: "highlight-ids", WireName: "reel_ids", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("highlights_tray",
			mcplib.WithDescription("Fetch the highlight tray for a profile (cover images, titles, ordered list of highlight reels). Required: user_id. Returns the HighlightTray."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("Numeric Instagram user ID.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/highlights/{user_id}/highlights_tray/", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("location_sections",
			mcplib.WithDescription("Fetch posts at a location ID (top, recent, ranked sections). Required: location_id. Optional: tab (default: ranked), max_id. Returns the new LocationPage."),
			mcplib.WithString("location_id", mcplib.Required(), mcplib.Description("Numeric Instagram location ID.")),
			mcplib.WithString("tab", mcplib.Description("Which section tab to return.")),
			mcplib.WithString("max_id", mcplib.Description("Pagination cursor.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/v1/locations/{location_id}/sections/", []mcpParamBinding{{PublicName: "location_id", WireName: "location_id", Location: "path"}, {PublicName: "tab", WireName: "tab", Location: "body"}, {PublicName: "max_id", WireName: "max_id", Location: "body"}}, []string{"location_id"}),
	)
	s.AddTool(
		mcplib.NewTool("media_info",
			mcplib.WithDescription("Get full media JSON for a single post, reel, or IGTV by media ID, including carousel children, captions, alt-text, comments, and CDN URLs. Required: media_id. Returns the MediaInfo."),
			mcplib.WithString("media_id", mcplib.Required(), mcplib.Description("Instagram media ID (numeric, e.g., 3147251234567890123_25025320). Use 'instagram-pp-cli get <url-or-shortcode>' to...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/media/{media_id}/info/", []mcpParamBinding{{PublicName: "media_id", WireName: "media_id", Location: "path"}}, []string{"media_id"}),
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Run 'instagram-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API credentials." +
					"\n      Run 'instagram-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Run 'instagram-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "instagram-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "instagram-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "instagram",
		"description": "Single-binary Instagram archiver — local SQLite catalog, FTS over captions and Instagram-generated alt-text, and...",
		"archetype":   "crm",
		"tool_count":  12,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion instagram-pp-cli binary.",
		"auth": map[string]any{
			"type": "cookie",
		},
		"resources": []map[string]any{
			{
				"name":        "accounts",
				"description": "Look up Instagram profiles by username or user ID.",
				"endpoints":   []string{"info", "user"},
				"searchable":  true,
			},
			{
				"name":        "feed",
				"description": "Fetch authenticated user feeds: saved, liked, timeline, and per-user media.",
				"endpoints":   []string{"liked", "reels", "saved", "timeline", "user"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "hashtag",
				"description": "Look up hashtag pages and download tagged media.",
				"endpoints":   []string{"info"},
				"searchable":  true,
			},
			{
				"name":        "highlights",
				"description": "Fetch profile highlight trays and per-highlight items.",
				"endpoints":   []string{"reel", "tray"},
				"searchable":  true,
			},
			{
				"name":        "location",
				"description": "Fetch geotagged location pages and the posts at them.",
				"endpoints":   []string{"sections"},
				"searchable":  true,
			},
			{
				"name":        "media",
				"description": "Fetch single posts, reels, and IGTV by media ID.",
				"endpoints":   []string{"info"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Local-corpus FTS search", "command": "search", "description": "Full-text search across every caption, hashtag, mention, location, owner handle, and IG-generated alt-text in your...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Alt-text capture and search", "command": "search", "description": "Captures Instagram's auto-generated accessibility_caption ('may contain: two people, sunglasses, outdoors') into the...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Watchlist with shared rate budget", "command": "sync", "description": "Register profiles and hashtags as a watchlist, then run one sync that walks the whole list under one shared rate...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Highlight-tray diff", "command": "highlights diff", "description": "Compares the current highlight tray and items for a profile against the prior snapshot in the local store, reporting...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "What's new since last sync", "command": "whatsnew", "description": "Lists every shortcode added to the local catalog since the prior sync run, grouped by owner, with caption snippets.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Dry-run with size and rate-budget estimate", "command": "user", "description": "Walks the cursor without downloading, sums estimated bytes from each media_info, and reports request count plus...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Mirror-diff (remote vs local)", "command": "diff", "description": "Cursor-walks shortcodes from Instagram, set-diffs against the local download_history, and reports both new posts and...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Carousel slide-kind filter", "command": "get", "description": "Filter carousel children by media kind so a sync keeps only stills (skipping reels and video children) or only videos.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Sync-health doctor", "command": "doctor", "description": "Joins sessions, rate_limit_events, and download_history to report last-sync per watchlist entry, current 429 backoff...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "LLM-rerank theme search", "command": "search", "description": "Opt-in --rerank flag pipes the top FTS candidates' captions, alt-text, and first comments to whichever LLM env is...", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Local-corpus FTS search", "insight": ""},
			{"topic": "Alt-text capture and search", "insight": ""},
			{"topic": "Watchlist with shared rate budget", "insight": ""},
			{"topic": "Highlight-tray diff", "insight": ""},
			{"topic": "What's new since last sync", "insight": ""},
			{"topic": "Dry-run with size and rate-budget estimate", "insight": ""},
			{"topic": "Mirror-diff (remote vs local)", "insight": ""},
			{"topic": "Carousel slide-kind filter", "insight": ""},
			{"topic": "Sync-health doctor", "insight": ""},
			{"topic": "LLM-rerank theme search", "insight": ""},
			{"topic": "Contact lookup", "insight": "Use search for finding contacts by name/email. List endpoints return unsorted results and require pagination for large datasets."},
			{"topic": "Activity tracking", "insight": "When checking deal activity, sync first and query locally. CRM APIs often throttle activity-log endpoints heavily."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
