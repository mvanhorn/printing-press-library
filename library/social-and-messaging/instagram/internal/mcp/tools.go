// Copyright 2026 bryanaaron. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"instagram-pp-cli/internal/cli"
	"instagram-pp-cli/internal/cliutil"
	"instagram-pp-cli/internal/client"
	"instagram-pp-cli/internal/config"
	"instagram-pp-cli/internal/mcp/cobratree"
	"instagram-pp-cli/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("explore_grid",
			mcplib.WithDescription("Get explore grid content (trending posts)."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/discover/web/explore_grid/", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("feed_liked",
			mcplib.WithDescription("Get media liked by the authenticated user. Optional: count."),
			mcplib.WithString("count", mcplib.Description("Number of liked posts to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/feed/liked/", []mcpParamBinding{{PublicName: "count", WireName: "count", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("feed_saved",
			mcplib.WithDescription("Get the authenticated user's saved posts. Optional: count."),
			mcplib.WithString("count", mcplib.Description("Number of saved posts to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/feed/saved/posts/", []mcpParamBinding{{PublicName: "count", WireName: "count", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("feed_timeline",
			mcplib.WithDescription("Get the home feed (posts from accounts you follow). Optional: is_prefetch."),
			mcplib.WithString("is_prefetch", mcplib.Description("Whether this is a prefetch request")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/feed/timeline/", []mcpParamBinding{{PublicName: "is_prefetch", WireName: "is_prefetch", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("feed_user_posts",
			mcplib.WithDescription("Get posts from a specific user. Required: user_id. Optional: count."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("Instagram user ID")),
			mcplib.WithString("count", mcplib.Description("Number of posts to return (default 12)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/feed/user/{user_id}/", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"},{PublicName: "count", WireName: "count", Location: "query"}, }, []string{"user_id", }),
	)
	s.AddTool(
		mcplib.NewTool("friendships_followers",
			mcplib.WithDescription("Get followers of a user. Required: user_id. Optional: count."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("Instagram user ID")),
			mcplib.WithString("count", mcplib.Description("Number of followers to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/friendships/{user_id}/followers/", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"},{PublicName: "count", WireName: "count", Location: "query"}, }, []string{"user_id", }),
	)
	s.AddTool(
		mcplib.NewTool("friendships_following",
			mcplib.WithDescription("Get accounts a user is following. Required: user_id. Optional: count."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("Instagram user ID")),
			mcplib.WithString("count", mcplib.Description("Number of following to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/friendships/{user_id}/following/", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"},{PublicName: "count", WireName: "count", Location: "query"}, }, []string{"user_id", }),
	)
	s.AddTool(
		mcplib.NewTool("friendships_pending",
			mcplib.WithDescription("Get pending follow requests (for private accounts)."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/friendships/pending/", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("graphql_query",
			mcplib.WithDescription("Execute a GraphQL query against the Instagram API."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("hashtags_info",
			mcplib.WithDescription("Get info about a hashtag including post count. Required: hashtag."),
			mcplib.WithString("hashtag", mcplib.Required(), mcplib.Description("Hashtag name (without")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/tags/web_info/", []mcpParamBinding{{PublicName: "hashtag", WireName: "tag_name", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("locations_search",
			mcplib.WithDescription("Search for locations and places. Required: query."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Location name to search for")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/location_search/", []mcpParamBinding{{PublicName: "query", WireName: "search_query", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("media_comments",
			mcplib.WithDescription("Get comments on a post. Required: media_id. Optional: count."),
			mcplib.WithString("media_id", mcplib.Required(), mcplib.Description("Instagram media ID")),
			mcplib.WithString("count", mcplib.Description("Number of comments to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/media/{media_id}/comments/", []mcpParamBinding{{PublicName: "media_id", WireName: "media_id", Location: "path"},{PublicName: "count", WireName: "count", Location: "query"}, }, []string{"media_id", }),
	)
	s.AddTool(
		mcplib.NewTool("media_info",
			mcplib.WithDescription("Get detailed info for a specific post/reel. Required: media_id."),
			mcplib.WithString("media_id", mcplib.Required(), mcplib.Description("Instagram media ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/media/{media_id}/info/", []mcpParamBinding{{PublicName: "media_id", WireName: "media_id", Location: "path"}, }, []string{"media_id", }),
	)
	s.AddTool(
		mcplib.NewTool("media_likers",
			mcplib.WithDescription("Get users who liked a post. Required: media_id."),
			mcplib.WithString("media_id", mcplib.Required(), mcplib.Description("Instagram media ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/media/{media_id}/likers/", []mcpParamBinding{{PublicName: "media_id", WireName: "media_id", Location: "path"}, }, []string{"media_id", }),
	)
	s.AddTool(
		mcplib.NewTool("notifications_inbox",
			mcplib.WithDescription("Get the notifications inbox (likes, comments, follows, mentions)."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/news/inbox/", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("notifications_mark_seen",
			mcplib.WithDescription("Mark notifications as seen."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/v1/news/inbox_seen/", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("reels_user_clips",
			mcplib.WithDescription("Get reels posted by a specific user. Required: user-id. Optional: page_size."),
			mcplib.WithString("user-id", mcplib.Required(), mcplib.Description("Target user's Instagram ID")),
			mcplib.WithString("page_size", mcplib.Description("Number of reels to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/clips/user/", []mcpParamBinding{{PublicName: "user-id", WireName: "target_user_id", Location: "query"},{PublicName: "page_size", WireName: "page_size", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("stories_highlights_tray",
			mcplib.WithDescription("Get highlights for a user. Required: user_id. Optional: supported_capabilities_new."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("Instagram user ID")),
			mcplib.WithString("supported_capabilities_new", mcplib.Description("JSON array of supported capabilities")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/highlights/{user_id}/highlights_tray/", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"},{PublicName: "supported_capabilities_new", WireName: "supported_capabilities_new", Location: "query"}, }, []string{"user_id", }),
	)
	s.AddTool(
		mcplib.NewTool("stories_reels_media",
			mcplib.WithDescription("Get story reels for specific users. Required: reel_ids."),
			mcplib.WithString("reel_ids", mcplib.Required(), mcplib.Description("Comma-separated list of user IDs")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/feed/reels_media/", []mcpParamBinding{{PublicName: "reel_ids", WireName: "reel_ids", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("stories_tray",
			mcplib.WithDescription("Get the stories tray (all unviewed stories from followed accounts)."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/feed/reels_tray/", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("users_current_user",
			mcplib.WithDescription("Get the authenticated user's own account info. Optional: edit."),
			mcplib.WithString("edit", mcplib.Description("Include editable fields when true")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/accounts/current_user/", []mcpParamBinding{{PublicName: "edit", WireName: "edit", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("users_profile",
			mcplib.WithDescription("Get full user profile by username. Required: username."),
			mcplib.WithString("username", mcplib.Required(), mcplib.Description("Instagram username")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/users/web_profile_info/", []mcpParamBinding{{PublicName: "username", WireName: "username", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("users_search",
			mcplib.WithDescription("Search users, hashtags, and places. Required: query. Optional: context, count."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query string")),
			mcplib.WithString("context", mcplib.Description("Search context (blended, user, hashtag, place)")),
			mcplib.WithString("count", mcplib.Description("Number of results to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v1/web/search/topsearch/", []mcpParamBinding{{PublicName: "query", WireName: "query", Location: "query"},{PublicName: "context", WireName: "context", Location: "query"},{PublicName: "count", WireName: "count", Location: "query"}, }, []string{ }),
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export INSTAGRAM_SESSION_ID=<your-key>" +
					"\n      Run 'instagram-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API credentials." +
					"\n      Set it with: export INSTAGRAM_SESSION_ID=<your-key>" +
					"\n      Run 'instagram-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export INSTAGRAM_SESSION_ID=<your-key>" +
					"\n      Run 'instagram-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "instagram-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 2)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "instagram-pp-cli", "data.db")
}
// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "instagram",
		"description": "Instagram Web API (reverse-engineered from authenticated browser session)",
		"archetype":   "content",
		"tool_count":  23,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion instagram-pp-cli binary.",
		"auth": map[string]any{
			"type": "cookie",
			"env_vars": []map[string]any{
				{
					"name": "INSTAGRAM_SESSION_ID",
					"kind": "per_call",
					"required": true,
					"sensitive": true,
					"description": "Set to your API credential.",
				},
				{
					"name": "INSTAGRAM_CSRF_TOKEN",
					"kind": "per_call",
					"required": true,
					"sensitive": true,
					"description": "Set to your API credential.",
				},
				{
					"name": "INSTAGRAM_USER_ID",
					"kind": "per_call",
					"required": true,
					"sensitive": true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name": "explore",
				"description": "Explore/discover content",
				"endpoints": []string{"grid",  },
			},
			{
				"name": "feed",
				"description": "Home feed and user posts",
				"endpoints": []string{"liked", "saved", "timeline", "user_posts",  },
			},
			{
				"name": "friendships",
				"description": "Follow relationships and pending requests",
				"endpoints": []string{"followers", "following", "pending",  },
			},
			{
				"name": "graphql",
				"description": "Instagram GraphQL API (for complex queries)",
				"endpoints": []string{"query",  },
			},
			{
				"name": "hashtags",
				"description": "Hashtag search and info",
				"endpoints": []string{"info",  },
			},
			{
				"name": "locations",
				"description": "Location search",
				"endpoints": []string{"search",  },
			},
			{
				"name": "media",
				"description": "Posts, reels, and media",
				"endpoints": []string{"comments", "info", "likers",  },
			},
			{
				"name": "notifications",
				"description": "Activity notifications",
				"endpoints": []string{"inbox", "mark_seen",  },
			},
			{
				"name": "reels",
				"description": "Reels/Clips",
				"endpoints": []string{"user_clips",  },
			},
			{
				"name": "stories",
				"description": "Stories and highlights",
				"endpoints": []string{"highlights_tray", "reels_media", "tray",  },
			},
			{
				"name": "users",
				"description": "User profiles and account info",
				"endpoints": []string{"current_user", "profile", "search",  },
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
