// Copyright 2026 yung-bing. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newEditMessageCaptionPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyCaption string
	var bodyCaptionEntities string
	var bodyChatId string
	var bodyInlineMessageId string
	var bodyMessageId int
	var bodyParseMode string
	var bodyReplyMarkup string

	cmd := &cobra.Command{
		Use:         "edit-message-caption",
		Short:       "Use this method to edit captions of messages. On success, if the edited message is not an inline message, the edited...",
		Long:        "Shortcut for 'edit-message-caption create'. Use this method to edit captions of messages. On success, if the edited message is not an inline message, the edited...",
		Example:     "  telegram-pp-cli edit-message-caption",
		Annotations: map[string]string{"pp:endpoint": "edit-message-caption.create", "pp:method": "POST", "pp:path": "/editMessageCaption"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/editMessageCaption"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyCaption != "" {
				body["caption"] = bodyCaption
			}
			if bodyCaptionEntities != "" {
				var parsedCaptionEntities any
				if err := json.Unmarshal([]byte(bodyCaptionEntities), &parsedCaptionEntities); err != nil {
					return fmt.Errorf("parsing --caption-entities JSON: %w", err)
				}
				body["caption_entities"] = parsedCaptionEntities
			}
			if bodyChatId != "" {
				body["chat_id"] = bodyChatId
			}
			if bodyInlineMessageId != "" {
				body["inline_message_id"] = bodyInlineMessageId
			}
			if bodyMessageId != 0 {
				body["message_id"] = bodyMessageId
			}
			if bodyParseMode != "" {
				body["parse_mode"] = bodyParseMode
			}
			if bodyReplyMarkup != "" {
				var parsedReplyMarkup any
				if err := json.Unmarshal([]byte(bodyReplyMarkup), &parsedReplyMarkup); err != nil {
					return fmt.Errorf("parsing --reply-markup JSON: %w", err)
				}
				body["reply_markup"] = parsedReplyMarkup
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCaption, "caption", "", "New caption of the message, 0-1024 characters after entities parsing")
	cmd.Flags().StringVar(&bodyCaptionEntities, "caption-entities", "", "List of special entities that appear in the caption, which can be specified instead of *parse_mode*")
	cmd.Flags().StringVar(&bodyChatId, "chat-id", "", "Required if *inline_message_id* is not specified. Unique identifier for the target chat or username of the target...")
	cmd.Flags().StringVar(&bodyInlineMessageId, "inline-message-id", "", "Required if *chat_id* and *message_id* are not specified. Identifier of the inline message")
	cmd.Flags().IntVar(&bodyMessageId, "message-id", 0, "Required if *inline_message_id* is not specified. Identifier of the message to edit")
	cmd.Flags().StringVar(&bodyParseMode, "parse-mode", "", "Mode for parsing entities in the message caption. See [formatting...")
	cmd.Flags().StringVar(&bodyReplyMarkup, "reply-markup", "", "This object represents an [inline keyboard](https://core.telegram.org/bots#inline-keyboards-and-on-the-fly-updating)...")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
