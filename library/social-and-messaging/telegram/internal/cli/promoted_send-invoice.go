// Copyright 2026 yung-bing. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSendInvoicePromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyAllowSendingWithoutReply bool
	var bodyChatId int
	var bodyCurrency string
	var bodyDescription string
	var bodyDisableNotification bool
	var bodyIsFlexible bool
	var bodyNeedEmail bool
	var bodyNeedName bool
	var bodyNeedPhoneNumber bool
	var bodyNeedShippingAddress bool
	var bodyPayload string
	var bodyPhotoHeight int
	var bodyPhotoSize int
	var bodyPhotoUrl string
	var bodyPhotoWidth int
	var bodyPrices string
	var bodyProviderData string
	var bodyProviderToken string
	var bodyReplyMarkup string
	var bodyReplyToMessageId int
	var bodySendEmailToProvider bool
	var bodySendPhoneNumberToProvider bool
	var bodyStartParameter string
	var bodyTitle string

	cmd := &cobra.Command{
		Use:         "send-invoice",
		Short:       "Use this method to send invoices. On success, the sent [Message](https://core.telegram.org/bots/api/#message) is...",
		Long:        "Shortcut for 'send-invoice create'. Use this method to send invoices. On success, the sent [Message](https://core.telegram.org/bots/api/#message) is...",
		Example:     "  telegram-pp-cli send-invoice --currency example-value",
		Annotations: map[string]string{"pp:endpoint": "send-invoice.create", "pp:method": "POST", "pp:path": "/sendInvoice"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("chat-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "chat-id")
			}
			if !cmd.Flags().Changed("currency") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "currency")
			}
			if !cmd.Flags().Changed("description") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "description")
			}
			if !cmd.Flags().Changed("payload") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "payload")
			}
			if !cmd.Flags().Changed("prices") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "prices")
			}
			if !cmd.Flags().Changed("provider-token") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "provider-token")
			}
			if !cmd.Flags().Changed("start-parameter") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "start-parameter")
			}
			if !cmd.Flags().Changed("title") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "title")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/sendInvoice"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyAllowSendingWithoutReply != false {
				body["allow_sending_without_reply"] = bodyAllowSendingWithoutReply
			}
			if bodyChatId != 0 {
				body["chat_id"] = bodyChatId
			}
			if bodyCurrency != "" {
				body["currency"] = bodyCurrency
			}
			if bodyDescription != "" {
				body["description"] = bodyDescription
			}
			if bodyDisableNotification != false {
				body["disable_notification"] = bodyDisableNotification
			}
			if bodyIsFlexible != false {
				body["is_flexible"] = bodyIsFlexible
			}
			if bodyNeedEmail != false {
				body["need_email"] = bodyNeedEmail
			}
			if bodyNeedName != false {
				body["need_name"] = bodyNeedName
			}
			if bodyNeedPhoneNumber != false {
				body["need_phone_number"] = bodyNeedPhoneNumber
			}
			if bodyNeedShippingAddress != false {
				body["need_shipping_address"] = bodyNeedShippingAddress
			}
			if bodyPayload != "" {
				body["payload"] = bodyPayload
			}
			if bodyPhotoHeight != 0 {
				body["photo_height"] = bodyPhotoHeight
			}
			if bodyPhotoSize != 0 {
				body["photo_size"] = bodyPhotoSize
			}
			if bodyPhotoUrl != "" {
				body["photo_url"] = bodyPhotoUrl
			}
			if bodyPhotoWidth != 0 {
				body["photo_width"] = bodyPhotoWidth
			}
			if bodyPrices != "" {
				var parsedPrices any
				if err := json.Unmarshal([]byte(bodyPrices), &parsedPrices); err != nil {
					return fmt.Errorf("parsing --prices JSON: %w", err)
				}
				body["prices"] = parsedPrices
			}
			if bodyProviderData != "" {
				body["provider_data"] = bodyProviderData
			}
			if bodyProviderToken != "" {
				body["provider_token"] = bodyProviderToken
			}
			if bodyReplyMarkup != "" {
				var parsedReplyMarkup any
				if err := json.Unmarshal([]byte(bodyReplyMarkup), &parsedReplyMarkup); err != nil {
					return fmt.Errorf("parsing --reply-markup JSON: %w", err)
				}
				body["reply_markup"] = parsedReplyMarkup
			}
			if bodyReplyToMessageId != 0 {
				body["reply_to_message_id"] = bodyReplyToMessageId
			}
			if bodySendEmailToProvider != false {
				body["send_email_to_provider"] = bodySendEmailToProvider
			}
			if bodySendPhoneNumberToProvider != false {
				body["send_phone_number_to_provider"] = bodySendPhoneNumberToProvider
			}
			if bodyStartParameter != "" {
				body["start_parameter"] = bodyStartParameter
			}
			if bodyTitle != "" {
				body["title"] = bodyTitle
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAllowSendingWithoutReply, "allow-sending-without-reply", false, "Pass *True*, if the message should be sent even if the specified replied-to message is not found")
	cmd.Flags().IntVar(&bodyChatId, "chat-id", 0, "Unique identifier for the target private chat")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "Three-letter ISO 4217 currency code, see [more on currencies](/bots/payments#supported-currencies)")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Product description, 1-255 characters")
	cmd.Flags().BoolVar(&bodyDisableNotification, "disable-notification", false, "Sends the message [silently](https://telegram.org/blog/channels-2-0#silent-messages). Users will receive a...")
	cmd.Flags().BoolVar(&bodyIsFlexible, "is-flexible", false, "Pass *True*, if the final price depends on the shipping method")
	cmd.Flags().BoolVar(&bodyNeedEmail, "need-email", false, "Pass *True*, if you require the user's email address to complete the order")
	cmd.Flags().BoolVar(&bodyNeedName, "need-name", false, "Pass *True*, if you require the user's full name to complete the order")
	cmd.Flags().BoolVar(&bodyNeedPhoneNumber, "need-phone-number", false, "Pass *True*, if you require the user's phone number to complete the order")
	cmd.Flags().BoolVar(&bodyNeedShippingAddress, "need-shipping-address", false, "Pass *True*, if you require the user's shipping address to complete the order")
	cmd.Flags().StringVar(&bodyPayload, "payload", "", "Bot-defined invoice payload, 1-128 bytes. This will not be displayed to the user, use for your internal processes.")
	cmd.Flags().IntVar(&bodyPhotoHeight, "photo-height", 0, "Photo height")
	cmd.Flags().IntVar(&bodyPhotoSize, "photo-size", 0, "Photo size")
	cmd.Flags().StringVar(&bodyPhotoUrl, "photo-url", "", "URL of the product photo for the invoice. Can be a photo of the goods or a marketing image for a service. People...")
	cmd.Flags().IntVar(&bodyPhotoWidth, "photo-width", 0, "Photo width")
	cmd.Flags().StringVar(&bodyPrices, "prices", "", "Price breakdown, a JSON-serialized list of components (e.g. product price, tax, discount, delivery cost, delivery...")
	cmd.Flags().StringVar(&bodyProviderData, "provider-data", "", "A JSON-serialized data about the invoice, which will be shared with the payment provider. A detailed description of...")
	cmd.Flags().StringVar(&bodyProviderToken, "provider-token", "", "Payments provider token, obtained via [Botfather](https://t.me/botfather)")
	cmd.Flags().StringVar(&bodyReplyMarkup, "reply-markup", "", "This object represents an [inline keyboard](https://core.telegram.org/bots#inline-keyboards-and-on-the-fly-updating)...")
	cmd.Flags().IntVar(&bodyReplyToMessageId, "reply-to-message-id", 0, "If the message is a reply, ID of the original message")
	cmd.Flags().BoolVar(&bodySendEmailToProvider, "send-email-to-provider", false, "Pass *True*, if user's email address should be sent to provider")
	cmd.Flags().BoolVar(&bodySendPhoneNumberToProvider, "send-phone-number-to-provider", false, "Pass *True*, if user's phone number should be sent to provider")
	cmd.Flags().StringVar(&bodyStartParameter, "start-parameter", "", "Unique deep-linking parameter that can be used to generate this invoice when used as a start parameter")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "Product name, 1-32 characters")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
