// Copyright 2026 yung-bing. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newGetGameHighScoresPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyChatId int
	var bodyInlineMessageId string
	var bodyMessageId int
	var bodyUserId int

	cmd := &cobra.Command{
		Use:         "get-game-high-scores",
		Short:       "Use this method to get data for high score tables. Will return the score of the specified user and several of their...",
		Long:        "Shortcut for 'get-game-high-scores create'. Use this method to get data for high score tables. Will return the score of the specified user and several of their...",
		Example:     "  telegram-pp-cli get-game-high-scores",
		Annotations: map[string]string{"pp:endpoint": "get-game-high-scores.create", "pp:method": "POST", "pp:path": "/getGameHighScores"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("user-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "user-id")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/getGameHighScores"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyChatId != 0 {
				body["chat_id"] = bodyChatId
			}
			if bodyInlineMessageId != "" {
				body["inline_message_id"] = bodyInlineMessageId
			}
			if bodyMessageId != 0 {
				body["message_id"] = bodyMessageId
			}
			if bodyUserId != 0 {
				body["user_id"] = bodyUserId
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyChatId, "chat-id", 0, "Required if *inline_message_id* is not specified. Unique identifier for the target chat")
	cmd.Flags().StringVar(&bodyInlineMessageId, "inline-message-id", "", "Required if *chat_id* and *message_id* are not specified. Identifier of the inline message")
	cmd.Flags().IntVar(&bodyMessageId, "message-id", 0, "Required if *inline_message_id* is not specified. Identifier of the sent message")
	cmd.Flags().IntVar(&bodyUserId, "user-id", 0, "Target user id")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
