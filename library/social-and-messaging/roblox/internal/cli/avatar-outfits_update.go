// Copyright 2026 Kieran Maynard and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAvatarOutfitsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAssets string
	var bodyBodyColorsHeadColorId string
	var bodyBodyColorsLeftArmColorId string
	var bodyBodyColorsLeftLegColorId string
	var bodyBodyColorsRightArmColorId string
	var bodyBodyColorsRightLegColorId string
	var bodyBodyColorsTorsoColorId string
	var bodyName string
	var bodyOutfitType int
	var bodyPlayerAvatarType string
	var bodyScaleBodyType float64
	var bodyScaleDepth float64
	var bodyScaleHead float64
	var bodyScaleHeight float64
	var bodyScaleProportion float64
	var bodyScaleWidth float64
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update <userOutfitId>",
		Short:       "Fails if the user does not own any of the assetIds or if they are not wearable asset types. Accepts partial updates.",
		Example:     "  roblox-pp-cli avatar-outfits update 42",
		Annotations: map[string]string{"pp:endpoint": "avatar-outfits.update", "pp:method": "PATCH", "pp:path": "https://avatar.roblox.com/v2/outfits/{userOutfitId}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <userOutfitId>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <userOutfitId>"))
			}
			if !stdinBody {
			}
			path := "https://avatar.roblox.com/v2/outfits/{userOutfitId}"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("userOutfitId is required\nUsage: %s <%s>", cmd.CommandPath(), "userOutfitId"))
			}
			path = replacePathParam(path, "userOutfitId", args[0])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if bodyAssets != "" {
					var parsedAssets any
					if err := json.Unmarshal([]byte(bodyAssets), &parsedAssets); err != nil {
						return fmt.Errorf("parsing --assets JSON: %w", err)
					}
					asArray, ok := parsedAssets.([]any)
					if !ok {
						return fmt.Errorf("--assets must be a JSON array, got JSON %T", parsedAssets)
					}
					bodyMap["assets"] = asArray
				}
				{
					nestedBodyColors := map[string]any{}
					if bodyBodyColorsHeadColorId != "" {
						nestedBodyColors["headColorId"] = bodyBodyColorsHeadColorId
					}
					if bodyBodyColorsLeftArmColorId != "" {
						nestedBodyColors["leftArmColorId"] = bodyBodyColorsLeftArmColorId
					}
					if bodyBodyColorsLeftLegColorId != "" {
						nestedBodyColors["leftLegColorId"] = bodyBodyColorsLeftLegColorId
					}
					if bodyBodyColorsRightArmColorId != "" {
						nestedBodyColors["rightArmColorId"] = bodyBodyColorsRightArmColorId
					}
					if bodyBodyColorsRightLegColorId != "" {
						nestedBodyColors["rightLegColorId"] = bodyBodyColorsRightLegColorId
					}
					if bodyBodyColorsTorsoColorId != "" {
						nestedBodyColors["torsoColorId"] = bodyBodyColorsTorsoColorId
					}
					if len(nestedBodyColors) > 0 {
						bodyMap["bodyColors"] = nestedBodyColors
					}
				}
				if bodyName != "" {
					bodyMap["name"] = bodyName
				}
				if bodyOutfitType != 0 {
					bodyMap["outfitType"] = bodyOutfitType
				}
				if bodyPlayerAvatarType != "" {
					bodyMap["playerAvatarType"] = bodyPlayerAvatarType
				}
				{
					nestedScale := map[string]any{}
					if bodyScaleBodyType != 0.0 {
						nestedScale["bodyType"] = bodyScaleBodyType
					}
					if bodyScaleDepth != 0.0 {
						nestedScale["depth"] = bodyScaleDepth
					}
					if bodyScaleHead != 0.0 {
						nestedScale["head"] = bodyScaleHead
					}
					if bodyScaleHeight != 0.0 {
						nestedScale["height"] = bodyScaleHeight
					}
					if bodyScaleProportion != 0.0 {
						nestedScale["proportion"] = bodyScaleProportion
					}
					if bodyScaleWidth != 0.0 {
						nestedScale["width"] = bodyScaleWidth
					}
					if len(nestedScale) > 0 {
						bodyMap["scale"] = nestedScale
					}
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "avatar-outfits", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "avatar-outfits", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "avatar-outfits", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "avatar-outfits", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "avatar-outfits", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "avatar-outfits",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "avatar-outfits", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "avatar-outfits", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAssets, "assets", "", "Array of assets")
	cmd.Flags().StringVar(&bodyBodyColorsHeadColorId, "body-colors-head-color-id", "", "The BrickColor id for head color")
	cmd.Flags().StringVar(&bodyBodyColorsLeftArmColorId, "body-colors-left-arm-color-id", "", "The BrickColor id for left arm color")
	cmd.Flags().StringVar(&bodyBodyColorsLeftLegColorId, "body-colors-left-leg-color-id", "", "The BrickColor id for left leg color")
	cmd.Flags().StringVar(&bodyBodyColorsRightArmColorId, "body-colors-right-arm-color-id", "", "The BrickColor id for right arm color")
	cmd.Flags().StringVar(&bodyBodyColorsRightLegColorId, "body-colors-right-leg-color-id", "", "The BrickColor id for right leg color")
	cmd.Flags().StringVar(&bodyBodyColorsTorsoColorId, "body-colors-torso-color-id", "", "The BrickColor id for torso color")
	cmd.Flags().StringVar(&bodyName, "name", "", "The outfit name")
	cmd.Flags().IntVar(&bodyOutfitType, "outfit-type", 0, "The type of outfit")
	cmd.Flags().StringVar(&bodyPlayerAvatarType, "player-avatar-type", "", "The avatar scale")
	cmd.Flags().Float64Var(&bodyScaleBodyType, "scale-body-type", 0.0, "Body type")
	cmd.Flags().Float64Var(&bodyScaleDepth, "scale-depth", 0.0, "Depth")
	cmd.Flags().Float64Var(&bodyScaleHead, "scale-head", 0.0, "Head")
	cmd.Flags().Float64Var(&bodyScaleHeight, "scale-height", 0.0, "Height")
	cmd.Flags().Float64Var(&bodyScaleProportion, "scale-proportion", 0.0, "Proportion")
	cmd.Flags().Float64Var(&bodyScaleWidth, "scale-width", 0.0, "Width")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
