// Copyright 2026 Kieran Maynard and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newGroups2SettingsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAccountTenureRequirement int
	var bodyAreEnemiesAllowed bool
	var bodyAreGroupFundsVisible bool
	var bodyAreGroupGamesVisible bool
	var bodyIsApprovalRequired bool
	var bodyIsMemberListVisibleToPublic bool
	var bodySlowmode int
	var bodyVerificationLevel int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update <groupId>",
		Short:       "Updates the group's settings",
		Example:     "  roblox-pp-cli groups-2 settings update 42",
		Annotations: map[string]string{"pp:endpoint": "settings.update", "pp:method": "PATCH", "pp:path": "https://groups.roblox.com/v1/groups/{groupId}/settings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <groupId>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <groupId>"))
			}
			if !stdinBody {
			}
			path := "https://groups.roblox.com/v1/groups/{groupId}/settings"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("groupId is required\nUsage: %s <%s>", cmd.CommandPath(), "groupId"))
			}
			path = replacePathParam(path, "groupId", args[0])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if bodyAccountTenureRequirement != 0 {
					bodyMap["accountTenureRequirement"] = bodyAccountTenureRequirement
				}
				if cmd.Flags().Changed("are-enemies-allowed") {
					bodyMap["areEnemiesAllowed"] = bodyAreEnemiesAllowed
				}
				if cmd.Flags().Changed("are-group-funds-visible") {
					bodyMap["areGroupFundsVisible"] = bodyAreGroupFundsVisible
				}
				if cmd.Flags().Changed("are-group-games-visible") {
					bodyMap["areGroupGamesVisible"] = bodyAreGroupGamesVisible
				}
				if cmd.Flags().Changed("is-approval-required") {
					bodyMap["isApprovalRequired"] = bodyIsApprovalRequired
				}
				if cmd.Flags().Changed("is-member-list-visible-to-public") {
					bodyMap["isMemberListVisibleToPublic"] = bodyIsMemberListVisibleToPublic
				}
				if bodySlowmode != 0 {
					bodyMap["slowmode"] = bodySlowmode
				}
				if bodyVerificationLevel != 0 {
					bodyMap["verificationLevel"] = bodyVerificationLevel
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "settings", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "settings", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "settings",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "settings", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().IntVar(&bodyAccountTenureRequirement, "account-tenure-requirement", 0, "The account tenure requirement for the group.")
	cmd.Flags().BoolVar(&bodyAreEnemiesAllowed, "are-enemies-allowed", false, "Whether enemy club declarations are allowed.")
	cmd.Flags().BoolVar(&bodyAreGroupFundsVisible, "are-group-funds-visible", false, "Whether funds are publicly visible.")
	cmd.Flags().BoolVar(&bodyAreGroupGamesVisible, "are-group-games-visible", false, "Whether games are publicly visible.")
	cmd.Flags().BoolVar(&bodyIsApprovalRequired, "is-approval-required", false, "Whether public entry is allowed.")
	cmd.Flags().BoolVar(&bodyIsMemberListVisibleToPublic, "is-member-list-visible-to-public", false, "Whether the group member list is visible to public.")
	cmd.Flags().IntVar(&bodySlowmode, "slowmode", 0, "The slowmode level for the group (0-4). 0 = No slowmode, 1-4 = Slowmode levels with increasing restrictions.")
	cmd.Flags().IntVar(&bodyVerificationLevel, "verification-level", 0, "The verification level for the group.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
