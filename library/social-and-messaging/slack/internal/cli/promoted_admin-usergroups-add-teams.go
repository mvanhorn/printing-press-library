// Copyright 2026 erick-holm. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newAdminUsergroupsAddTeamsPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyAutoProvision bool
	var bodyTeamIds string
	var bodyUsergroupId string

	cmd := &cobra.Command{
		Use:         "admin-usergroups-add-teams",
		Short:       "Associate one or more default workspaces with an organization-wide IDP group.",
		Long:        "Shortcut for 'admin-usergroups-add-teams admin_usergroups_add_teams'. Associate one or more default workspaces with an organization-wide IDP group.",
		Example:     "  slack-pp-cli admin-usergroups-add-teams --team-ids example-value",
		Annotations: map[string]string{"pp:endpoint": "admin-usergroups-add-teams.admin_usergroups_add_teams", "pp:method": "POST", "pp:path": "/admin.usergroups.addTeams"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("team-ids") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "team-ids")
			}
			if !cmd.Flags().Changed("usergroup-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "usergroup-id")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/admin.usergroups.addTeams"
			fields := url.Values{}
			if bodyAutoProvision != false {
				fields.Set("auto_provision", fmt.Sprintf("%v", bodyAutoProvision))
			}
			if bodyTeamIds != "" {
				fields.Set("team_ids", bodyTeamIds)
			}
			if bodyUsergroupId != "" {
				fields.Set("usergroup_id", bodyUsergroupId)
			}

			data, _, err := c.PostForm(path, fields)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAutoProvision, "auto-provision", false, "When `true`, this method automatically creates new workspace accounts for the IDP group members.")
	cmd.Flags().StringVar(&bodyTeamIds, "team-ids", "", "A comma separated list of encoded team (workspace) IDs. Each workspace *MUST* belong to the organization associated...")
	cmd.Flags().StringVar(&bodyUsergroupId, "usergroup-id", "", "An encoded usergroup (IDP Group) ID.")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
