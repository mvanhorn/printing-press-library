// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newSbcsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAudioPort int
	var bodyConnectionCalls int
	var bodyConnectionCpu string
	var bodyConnectionDisk string
	var bodyConnectionElapsedTime string
	var bodyConnectionLatency int
	var bodyConnectionMemory string
	var bodyConnectionRegisteredPhones int
	var bodyConnectionUdpActive bool
	var bodyConnectionUp bool
	var bodyDisplayName string
	var bodyGroup string
	var bodyHasConnection bool
	var bodyLocalIPv4 string
	var bodyLogLevel string
	var bodyLogSize int
	var bodyName string
	var bodyPassiveServer string
	var bodyPassiveServerIsEnabled bool
	var bodyPassword string
	var bodyPhoneMAC string
	var bodyPhoneUserId string
	var bodyProvisionLink string
	var bodyPublicIP string
	var bodySecurity string
	var bodyVersion string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Add new entity to Sbcs",
		Example:     "  3cx-xapi-pp-cli sbcs create",
		Annotations: map[string]string{"pp:endpoint": "sbcs.create", "pp:method": "POST", "pp:path": "/Sbcs"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/Sbcs"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAudioPort != 0 {
					body["AudioPort"] = bodyAudioPort
				}
				{
					nestedConnection := map[string]any{}
					if bodyConnectionCalls != 0 {
						nestedConnection["Calls"] = bodyConnectionCalls
					}
					if bodyConnectionCpu != "" {
						nestedConnection["Cpu"] = bodyConnectionCpu
					}
					if bodyConnectionDisk != "" {
						nestedConnection["Disk"] = bodyConnectionDisk
					}
					if bodyConnectionElapsedTime != "" {
						nestedConnection["ElapsedTime"] = bodyConnectionElapsedTime
					}
					if bodyConnectionLatency != 0 {
						nestedConnection["Latency"] = bodyConnectionLatency
					}
					if bodyConnectionMemory != "" {
						nestedConnection["Memory"] = bodyConnectionMemory
					}
					if bodyConnectionRegisteredPhones != 0 {
						nestedConnection["RegisteredPhones"] = bodyConnectionRegisteredPhones
					}
					if cmd.Flags().Changed("connection-udp-active") {
						nestedConnection["UdpActive"] = bodyConnectionUdpActive
					}
					if cmd.Flags().Changed("connection-up") {
						nestedConnection["Up"] = bodyConnectionUp
					}
					if len(nestedConnection) > 0 {
						body["Connection"] = nestedConnection
					}
				}
				if bodyDisplayName != "" {
					body["DisplayName"] = bodyDisplayName
				}
				if bodyGroup != "" {
					body["Group"] = bodyGroup
				}
				if cmd.Flags().Changed("has-connection") {
					body["HasConnection"] = bodyHasConnection
				}
				if bodyLocalIPv4 != "" {
					body["LocalIPv4"] = bodyLocalIPv4
				}
				if bodyLogLevel != "" {
					body["LogLevel"] = bodyLogLevel
				}
				if bodyLogSize != 0 {
					body["LogSize"] = bodyLogSize
				}
				if bodyName != "" {
					body["Name"] = bodyName
				}
				if bodyPassiveServer != "" {
					body["PassiveServer"] = bodyPassiveServer
				}
				if cmd.Flags().Changed("passive-server-is-enabled") {
					body["PassiveServerIsEnabled"] = bodyPassiveServerIsEnabled
				}
				if bodyPassword != "" {
					body["Password"] = bodyPassword
				}
				if bodyPhoneMAC != "" {
					body["PhoneMAC"] = bodyPhoneMAC
				}
				if bodyPhoneUserId != "" {
					body["PhoneUserId"] = bodyPhoneUserId
				}
				if bodyProvisionLink != "" {
					body["ProvisionLink"] = bodyProvisionLink
				}
				if bodyPublicIP != "" {
					body["PublicIP"] = bodyPublicIP
				}
				if bodySecurity != "" {
					body["Security"] = bodySecurity
				}
				if bodyVersion != "" {
					body["Version"] = bodyVersion
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "sbcs", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "sbcs", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sbcs", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sbcs", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sbcs", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "sbcs",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sbcs", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "sbcs", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().IntVar(&bodyAudioPort, "audio-port", 0, "Audio port")
	cmd.Flags().IntVar(&bodyConnectionCalls, "connection-calls", 0, "Calls")
	cmd.Flags().StringVar(&bodyConnectionCpu, "connection-cpu", "", "Cpu")
	cmd.Flags().StringVar(&bodyConnectionDisk, "connection-disk", "", "Disk")
	cmd.Flags().StringVar(&bodyConnectionElapsedTime, "connection-elapsed-time", "", "Elapsed time")
	cmd.Flags().IntVar(&bodyConnectionLatency, "connection-latency", 0, "Latency")
	cmd.Flags().StringVar(&bodyConnectionMemory, "connection-memory", "", "Memory")
	cmd.Flags().IntVar(&bodyConnectionRegisteredPhones, "connection-registered-phones", 0, "Registered phones")
	cmd.Flags().BoolVar(&bodyConnectionUdpActive, "connection-udp-active", false, "Udp active")
	cmd.Flags().BoolVar(&bodyConnectionUp, "connection-up", false, "Up")
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "Display name")
	cmd.Flags().StringVar(&bodyGroup, "group", "", "Group")
	cmd.Flags().BoolVar(&bodyHasConnection, "has-connection", false, "Has connection")
	cmd.Flags().StringVar(&bodyLocalIPv4, "local-i-pv4", "", "Local ipv4")
	cmd.Flags().StringVar(&bodyLogLevel, "log-level", "", "Log level")
	cmd.Flags().IntVar(&bodyLogSize, "log-size", 0, "Log size")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyPassiveServer, "passive-server", "", "Passive server")
	cmd.Flags().BoolVar(&bodyPassiveServerIsEnabled, "passive-server-is-enabled", false, "Passive server is enabled")
	cmd.Flags().StringVar(&bodyPassword, "password", "", "Password")
	cmd.Flags().StringVar(&bodyPhoneMAC, "phone-mac", "", "Phone mac")
	cmd.Flags().StringVar(&bodyPhoneUserId, "phone-user-id", "", "Phone user id")
	cmd.Flags().StringVar(&bodyProvisionLink, "provision-link", "", "Provision link")
	cmd.Flags().StringVar(&bodyPublicIP, "public-ip", "", "Public ip")
	cmd.Flags().StringVar(&bodySecurity, "security", "", "Security")
	cmd.Flags().StringVar(&bodyVersion, "version", "", "Version")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
