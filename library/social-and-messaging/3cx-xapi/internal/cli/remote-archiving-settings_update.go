// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newRemoteArchivingSettingsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyBackupsArchiving bool
	var bodyBackupsEnabled bool
	var bodyBackupsFolder string
	var bodyBackupsScheduleDays int
	var bodyChatsArchiving bool
	var bodyChatsEnabled bool
	var bodyChatsFolder string
	var bodyChatsScheduleDays int
	var bodyFaxesArchiving bool
	var bodyFaxesEnabled bool
	var bodyFaxesFolder string
	var bodyFaxesScheduleDays int
	var bodyId string
	var bodyLocationFileSystemType string
	var bodyLocationFtpPasswordConcealed bool
	var bodyLocationFtpPasswordValue string
	var bodyLocationFtpPath string
	var bodyLocationFtpUser string
	var bodyLocationFtpValidateCertificate bool
	var bodyLocationGbJsonConcealed bool
	var bodyLocationGbJsonContents string
	var bodyLocationGbJsonName string
	var bodyLocationGbPath string
	var bodyLocationLocalPath string
	var bodyLocationNsDomain string
	var bodyLocationNsPasswordConcealed bool
	var bodyLocationNsPasswordValue string
	var bodyLocationNsPath string
	var bodyLocationNsUser string
	var bodyLocationS3Path string
	var bodyLocationSftpPasswordConcealed bool
	var bodyLocationSftpPasswordValue string
	var bodyLocationSftpPath string
	var bodyLocationSftpPrivateKeyConcealed bool
	var bodyLocationSftpPrivateKeyContents string
	var bodyLocationSftpPrivateKeyName string
	var bodyLocationSftpUser string
	var bodyLocationSharePointPath string
	var bodyRecordingsArchiving bool
	var bodyRecordingsEnabled bool
	var bodyRecordingsFolder string
	var bodyRecordingsScheduleDays int
	var bodyVoicemailsArchiving bool
	var bodyVoicemailsEnabled bool
	var bodyVoicemailsFolder string
	var bodyVoicemailsScheduleDays int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Update RemoteArchivingSettings",
		Example:     "  3cx-xapi-pp-cli remote-archiving-settings update",
		Annotations: map[string]string{"pp:endpoint": "remote-archiving-settings.update", "pp:method": "PATCH", "pp:path": "/RemoteArchivingSettings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/RemoteArchivingSettings"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedBackups := map[string]any{}
					if cmd.Flags().Changed("backups-archiving") {
						nestedBackups["Archiving"] = bodyBackupsArchiving
					}
					if cmd.Flags().Changed("backups-enabled") {
						nestedBackups["Enabled"] = bodyBackupsEnabled
					}
					if bodyBackupsFolder != "" {
						nestedBackups["Folder"] = bodyBackupsFolder
					}
					if bodyBackupsScheduleDays != 0 {
						nestedBackups["ScheduleDays"] = bodyBackupsScheduleDays
					}
					if len(nestedBackups) > 0 {
						body["Backups"] = nestedBackups
					}
				}
				{
					nestedChats := map[string]any{}
					if cmd.Flags().Changed("chats-archiving") {
						nestedChats["Archiving"] = bodyChatsArchiving
					}
					if cmd.Flags().Changed("chats-enabled") {
						nestedChats["Enabled"] = bodyChatsEnabled
					}
					if bodyChatsFolder != "" {
						nestedChats["Folder"] = bodyChatsFolder
					}
					if bodyChatsScheduleDays != 0 {
						nestedChats["ScheduleDays"] = bodyChatsScheduleDays
					}
					if len(nestedChats) > 0 {
						body["Chats"] = nestedChats
					}
				}
				{
					nestedFaxes := map[string]any{}
					if cmd.Flags().Changed("faxes-archiving") {
						nestedFaxes["Archiving"] = bodyFaxesArchiving
					}
					if cmd.Flags().Changed("faxes-enabled") {
						nestedFaxes["Enabled"] = bodyFaxesEnabled
					}
					if bodyFaxesFolder != "" {
						nestedFaxes["Folder"] = bodyFaxesFolder
					}
					if bodyFaxesScheduleDays != 0 {
						nestedFaxes["ScheduleDays"] = bodyFaxesScheduleDays
					}
					if len(nestedFaxes) > 0 {
						body["Faxes"] = nestedFaxes
					}
				}
				if bodyId != "" {
					body["Id"] = bodyId
				}
				{
					nestedLocation := map[string]any{}
					if bodyLocationFileSystemType != "" {
						nestedLocation["FileSystemType"] = bodyLocationFileSystemType
					}
					{
						nestedLocationFtpPassword := map[string]any{}
						if cmd.Flags().Changed("location-ftp-password-concealed") {
							nestedLocationFtpPassword["Concealed"] = bodyLocationFtpPasswordConcealed
						}
						if bodyLocationFtpPasswordValue != "" {
							nestedLocationFtpPassword["Value"] = bodyLocationFtpPasswordValue
						}
						if len(nestedLocationFtpPassword) > 0 {
							nestedLocation["FtpPassword"] = nestedLocationFtpPassword
						}
					}
					if bodyLocationFtpPath != "" {
						nestedLocation["FtpPath"] = bodyLocationFtpPath
					}
					if bodyLocationFtpUser != "" {
						nestedLocation["FtpUser"] = bodyLocationFtpUser
					}
					if cmd.Flags().Changed("location-ftp-validate-certificate") {
						nestedLocation["FtpValidateCertificate"] = bodyLocationFtpValidateCertificate
					}
					{
						nestedLocationGbJson := map[string]any{}
						if cmd.Flags().Changed("location-gb-json-concealed") {
							nestedLocationGbJson["Concealed"] = bodyLocationGbJsonConcealed
						}
						if bodyLocationGbJsonContents != "" {
							nestedLocationGbJson["Contents"] = bodyLocationGbJsonContents
						}
						if bodyLocationGbJsonName != "" {
							nestedLocationGbJson["Name"] = bodyLocationGbJsonName
						}
						if len(nestedLocationGbJson) > 0 {
							nestedLocation["GbJson"] = nestedLocationGbJson
						}
					}
					if bodyLocationGbPath != "" {
						nestedLocation["GbPath"] = bodyLocationGbPath
					}
					if bodyLocationLocalPath != "" {
						nestedLocation["LocalPath"] = bodyLocationLocalPath
					}
					if bodyLocationNsDomain != "" {
						nestedLocation["NsDomain"] = bodyLocationNsDomain
					}
					{
						nestedLocationNsPassword := map[string]any{}
						if cmd.Flags().Changed("location-ns-password-concealed") {
							nestedLocationNsPassword["Concealed"] = bodyLocationNsPasswordConcealed
						}
						if bodyLocationNsPasswordValue != "" {
							nestedLocationNsPassword["Value"] = bodyLocationNsPasswordValue
						}
						if len(nestedLocationNsPassword) > 0 {
							nestedLocation["NsPassword"] = nestedLocationNsPassword
						}
					}
					if bodyLocationNsPath != "" {
						nestedLocation["NsPath"] = bodyLocationNsPath
					}
					if bodyLocationNsUser != "" {
						nestedLocation["NsUser"] = bodyLocationNsUser
					}
					if bodyLocationS3Path != "" {
						nestedLocation["S3Path"] = bodyLocationS3Path
					}
					{
						nestedLocationSftpPassword := map[string]any{}
						if cmd.Flags().Changed("location-sftp-password-concealed") {
							nestedLocationSftpPassword["Concealed"] = bodyLocationSftpPasswordConcealed
						}
						if bodyLocationSftpPasswordValue != "" {
							nestedLocationSftpPassword["Value"] = bodyLocationSftpPasswordValue
						}
						if len(nestedLocationSftpPassword) > 0 {
							nestedLocation["SftpPassword"] = nestedLocationSftpPassword
						}
					}
					if bodyLocationSftpPath != "" {
						nestedLocation["SftpPath"] = bodyLocationSftpPath
					}
					{
						nestedLocationSftpPrivateKey := map[string]any{}
						if cmd.Flags().Changed("location-sftp-private-key-concealed") {
							nestedLocationSftpPrivateKey["Concealed"] = bodyLocationSftpPrivateKeyConcealed
						}
						if bodyLocationSftpPrivateKeyContents != "" {
							nestedLocationSftpPrivateKey["Contents"] = bodyLocationSftpPrivateKeyContents
						}
						if bodyLocationSftpPrivateKeyName != "" {
							nestedLocationSftpPrivateKey["Name"] = bodyLocationSftpPrivateKeyName
						}
						if len(nestedLocationSftpPrivateKey) > 0 {
							nestedLocation["SftpPrivateKey"] = nestedLocationSftpPrivateKey
						}
					}
					if bodyLocationSftpUser != "" {
						nestedLocation["SftpUser"] = bodyLocationSftpUser
					}
					if bodyLocationSharePointPath != "" {
						nestedLocation["SharePointPath"] = bodyLocationSharePointPath
					}
					if len(nestedLocation) > 0 {
						body["Location"] = nestedLocation
					}
				}
				{
					nestedRecordings := map[string]any{}
					if cmd.Flags().Changed("recordings-archiving") {
						nestedRecordings["Archiving"] = bodyRecordingsArchiving
					}
					if cmd.Flags().Changed("recordings-enabled") {
						nestedRecordings["Enabled"] = bodyRecordingsEnabled
					}
					if bodyRecordingsFolder != "" {
						nestedRecordings["Folder"] = bodyRecordingsFolder
					}
					if bodyRecordingsScheduleDays != 0 {
						nestedRecordings["ScheduleDays"] = bodyRecordingsScheduleDays
					}
					if len(nestedRecordings) > 0 {
						body["Recordings"] = nestedRecordings
					}
				}
				{
					nestedVoicemails := map[string]any{}
					if cmd.Flags().Changed("voicemails-archiving") {
						nestedVoicemails["Archiving"] = bodyVoicemailsArchiving
					}
					if cmd.Flags().Changed("voicemails-enabled") {
						nestedVoicemails["Enabled"] = bodyVoicemailsEnabled
					}
					if bodyVoicemailsFolder != "" {
						nestedVoicemails["Folder"] = bodyVoicemailsFolder
					}
					if bodyVoicemailsScheduleDays != 0 {
						nestedVoicemails["ScheduleDays"] = bodyVoicemailsScheduleDays
					}
					if len(nestedVoicemails) > 0 {
						body["Voicemails"] = nestedVoicemails
					}
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "remote-archiving-settings", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "remote-archiving-settings", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "remote-archiving-settings", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "remote-archiving-settings", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "remote-archiving-settings", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "remote-archiving-settings",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "remote-archiving-settings", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "remote-archiving-settings", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyBackupsArchiving, "backups-archiving", false, "Archiving")
	cmd.Flags().BoolVar(&bodyBackupsEnabled, "backups-enabled", false, "Enabled")
	cmd.Flags().StringVar(&bodyBackupsFolder, "backups-folder", "", "Folder")
	cmd.Flags().IntVar(&bodyBackupsScheduleDays, "backups-schedule-days", 0, "Schedule days")
	cmd.Flags().BoolVar(&bodyChatsArchiving, "chats-archiving", false, "Archiving")
	cmd.Flags().BoolVar(&bodyChatsEnabled, "chats-enabled", false, "Enabled")
	cmd.Flags().StringVar(&bodyChatsFolder, "chats-folder", "", "Folder")
	cmd.Flags().IntVar(&bodyChatsScheduleDays, "chats-schedule-days", 0, "Schedule days")
	cmd.Flags().BoolVar(&bodyFaxesArchiving, "faxes-archiving", false, "Archiving")
	cmd.Flags().BoolVar(&bodyFaxesEnabled, "faxes-enabled", false, "Enabled")
	cmd.Flags().StringVar(&bodyFaxesFolder, "faxes-folder", "", "Folder")
	cmd.Flags().IntVar(&bodyFaxesScheduleDays, "faxes-schedule-days", 0, "Schedule days")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().StringVar(&bodyLocationFileSystemType, "location-file-system-type", "", "File system type")
	cmd.Flags().BoolVar(&bodyLocationFtpPasswordConcealed, "location-ftp-password-concealed", false, "Concealed")
	cmd.Flags().StringVar(&bodyLocationFtpPasswordValue, "location-ftp-password-value", "", "Value")
	cmd.Flags().StringVar(&bodyLocationFtpPath, "location-ftp-path", "", "Ftp path")
	cmd.Flags().StringVar(&bodyLocationFtpUser, "location-ftp-user", "", "Ftp user")
	cmd.Flags().BoolVar(&bodyLocationFtpValidateCertificate, "location-ftp-validate-certificate", false, "Ftp validate certificate")
	cmd.Flags().BoolVar(&bodyLocationGbJsonConcealed, "location-gb-json-concealed", false, "Concealed")
	cmd.Flags().StringVar(&bodyLocationGbJsonContents, "location-gb-json-contents", "", "Contents")
	cmd.Flags().StringVar(&bodyLocationGbJsonName, "location-gb-json-name", "", "Name")
	cmd.Flags().StringVar(&bodyLocationGbPath, "location-gb-path", "", "Gb path")
	cmd.Flags().StringVar(&bodyLocationLocalPath, "location-local-path", "", "Local path")
	cmd.Flags().StringVar(&bodyLocationNsDomain, "location-ns-domain", "", "Ns domain")
	cmd.Flags().BoolVar(&bodyLocationNsPasswordConcealed, "location-ns-password-concealed", false, "Concealed")
	cmd.Flags().StringVar(&bodyLocationNsPasswordValue, "location-ns-password-value", "", "Value")
	cmd.Flags().StringVar(&bodyLocationNsPath, "location-ns-path", "", "Ns path")
	cmd.Flags().StringVar(&bodyLocationNsUser, "location-ns-user", "", "Ns user")
	cmd.Flags().StringVar(&bodyLocationS3Path, "location-s3-path", "", "S3 path")
	cmd.Flags().BoolVar(&bodyLocationSftpPasswordConcealed, "location-sftp-password-concealed", false, "Concealed")
	cmd.Flags().StringVar(&bodyLocationSftpPasswordValue, "location-sftp-password-value", "", "Value")
	cmd.Flags().StringVar(&bodyLocationSftpPath, "location-sftp-path", "", "Sftp path")
	cmd.Flags().BoolVar(&bodyLocationSftpPrivateKeyConcealed, "location-sftp-private-key-concealed", false, "Concealed")
	cmd.Flags().StringVar(&bodyLocationSftpPrivateKeyContents, "location-sftp-private-key-contents", "", "Contents")
	cmd.Flags().StringVar(&bodyLocationSftpPrivateKeyName, "location-sftp-private-key-name", "", "Name")
	cmd.Flags().StringVar(&bodyLocationSftpUser, "location-sftp-user", "", "Sftp user")
	cmd.Flags().StringVar(&bodyLocationSharePointPath, "location-share-point-path", "", "Share point path")
	cmd.Flags().BoolVar(&bodyRecordingsArchiving, "recordings-archiving", false, "Archiving")
	cmd.Flags().BoolVar(&bodyRecordingsEnabled, "recordings-enabled", false, "Enabled")
	cmd.Flags().StringVar(&bodyRecordingsFolder, "recordings-folder", "", "Folder")
	cmd.Flags().IntVar(&bodyRecordingsScheduleDays, "recordings-schedule-days", 0, "Schedule days")
	cmd.Flags().BoolVar(&bodyVoicemailsArchiving, "voicemails-archiving", false, "Archiving")
	cmd.Flags().BoolVar(&bodyVoicemailsEnabled, "voicemails-enabled", false, "Enabled")
	cmd.Flags().StringVar(&bodyVoicemailsFolder, "voicemails-folder", "", "Folder")
	cmd.Flags().IntVar(&bodyVoicemailsScheduleDays, "voicemails-schedule-days", 0, "Schedule days")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
