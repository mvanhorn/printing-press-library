// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTrunksFinalizeProvisioningCmd(flags *rootFlags) *cobra.Command {
	var bodyModelAccountSid string
	var bodyModelApiKey string
	var bodyModelDefaultRuleAlterDestinationDuringHolidays bool
	var bodyModelDefaultRuleAlterDestinationDuringOutOfOfficeHours bool
	var bodyModelDefaultRuleCallType string
	var bodyModelDefaultRuleCondition string
	var bodyModelDefaultRuleCustomData string
	var bodyModelDefaultRuleData string
	var bodyModelDefaultRuleId string
	var bodyModelDefaultRuleRuleName string
	var bodyModelGroups string
	var bodyModelName string
	var bodyModelPhoneNumbers string
	var bodyModelType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "finalize-provisioning",
		Short:       "Invoke action FinalizeTrunkProvisioning",
		Example:     "  3cx-xapi-pp-cli trunks finalize-provisioning",
		Annotations: map[string]string{"pp:endpoint": "trunks.finalize-provisioning", "pp:method": "POST", "pp:path": "/Trunks/Pbx.FinalizeTrunkProvisioning"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/Trunks/Pbx.FinalizeTrunkProvisioning"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedModel := map[string]any{}
					if bodyModelAccountSid != "" {
						nestedModel["AccountSid"] = bodyModelAccountSid
					}
					if bodyModelApiKey != "" {
						nestedModel["ApiKey"] = bodyModelApiKey
					}
					{
						nestedModelDefaultRule := map[string]any{}
						if cmd.Flags().Changed("model-default-rule-alter-destination-during-holidays") {
							nestedModelDefaultRule["AlterDestinationDuringHolidays"] = bodyModelDefaultRuleAlterDestinationDuringHolidays
						}
						if cmd.Flags().Changed("model-default-rule-alter-destination-during-out-of-office-hours") {
							nestedModelDefaultRule["AlterDestinationDuringOutOfOfficeHours"] = bodyModelDefaultRuleAlterDestinationDuringOutOfOfficeHours
						}
						if bodyModelDefaultRuleCallType != "" {
							nestedModelDefaultRule["CallType"] = bodyModelDefaultRuleCallType
						}
						if bodyModelDefaultRuleCondition != "" {
							nestedModelDefaultRule["Condition"] = bodyModelDefaultRuleCondition
						}
						if bodyModelDefaultRuleCustomData != "" {
							nestedModelDefaultRule["CustomData"] = bodyModelDefaultRuleCustomData
						}
						if bodyModelDefaultRuleData != "" {
							nestedModelDefaultRule["Data"] = bodyModelDefaultRuleData
						}
						if bodyModelDefaultRuleId != "" {
							nestedModelDefaultRule["Id"] = bodyModelDefaultRuleId
						}
						if bodyModelDefaultRuleRuleName != "" {
							nestedModelDefaultRule["RuleName"] = bodyModelDefaultRuleRuleName
						}
						if len(nestedModelDefaultRule) > 0 {
							nestedModel["DefaultRule"] = nestedModelDefaultRule
						}
					}
					if bodyModelGroups != "" {
						var parsedModelGroups any
						if err := json.Unmarshal([]byte(bodyModelGroups), &parsedModelGroups); err != nil {
							return fmt.Errorf("parsing --model-groups JSON: %w", err)
						}
						nestedModel["Groups"] = parsedModelGroups
					}
					if bodyModelName != "" {
						nestedModel["Name"] = bodyModelName
					}
					if bodyModelPhoneNumbers != "" {
						var parsedModelPhoneNumbers any
						if err := json.Unmarshal([]byte(bodyModelPhoneNumbers), &parsedModelPhoneNumbers); err != nil {
							return fmt.Errorf("parsing --model-phone-numbers JSON: %w", err)
						}
						nestedModel["PhoneNumbers"] = parsedModelPhoneNumbers
					}
					if bodyModelType != "" {
						nestedModel["Type"] = bodyModelType
					}
					if len(nestedModel) > 0 {
						body["model"] = nestedModel
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "trunks", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "trunks", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "trunks", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "trunks", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "trunks", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "trunks",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "trunks", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "trunks", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyModelAccountSid, "model-account-sid", "", "Account sid")
	cmd.Flags().StringVar(&bodyModelApiKey, "model-api-key", "", "Api key")
	cmd.Flags().BoolVar(&bodyModelDefaultRuleAlterDestinationDuringHolidays, "model-default-rule-alter-destination-during-holidays", false, "Alter destination during holidays")
	cmd.Flags().BoolVar(&bodyModelDefaultRuleAlterDestinationDuringOutOfOfficeHours, "model-default-rule-alter-destination-during-out-of-office-hours", false, "Alter destination during out of office hours")
	cmd.Flags().StringVar(&bodyModelDefaultRuleCallType, "model-default-rule-call-type", "", "Call type")
	cmd.Flags().StringVar(&bodyModelDefaultRuleCondition, "model-default-rule-condition", "", "Condition")
	cmd.Flags().StringVar(&bodyModelDefaultRuleCustomData, "model-default-rule-custom-data", "", "Custom data")
	cmd.Flags().StringVar(&bodyModelDefaultRuleData, "model-default-rule-data", "", "Data")
	cmd.Flags().StringVar(&bodyModelDefaultRuleId, "model-default-rule-id", "", "Id")
	cmd.Flags().StringVar(&bodyModelDefaultRuleRuleName, "model-default-rule-rule-name", "", "Rule name")
	cmd.Flags().StringVar(&bodyModelGroups, "model-groups", "", "Groups")
	cmd.Flags().StringVar(&bodyModelName, "model-name", "", "Name")
	cmd.Flags().StringVar(&bodyModelPhoneNumbers, "model-phone-numbers", "", "Phone numbers")
	cmd.Flags().StringVar(&bodyModelType, "model-type", "", "Type")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin (use this for deeply nested fields not exposed as flags)")

	return cmd
}
