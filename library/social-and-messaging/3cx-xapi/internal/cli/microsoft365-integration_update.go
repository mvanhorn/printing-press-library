// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/3cx-xapi/internal/cliutil"
	"github.com/spf13/cobra"
)

func newMicrosoft365IntegrationUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAdUsersEnableSSO bool
	var bodyAdUsersIsEnabled bool
	var bodyAdUsersIsSyncDepartments bool
	var bodyAdUsersIsSyncDetails bool
	var bodyAdUsersIsSyncOfficePhone bool
	var bodyAdUsersIsSyncPhoto bool
	var bodyAdUsersSelectedGroups string
	var bodyAdUsersSelectedUsers string
	var bodyAdUsersSetTeamsPresence bool
	var bodyAdUsersStartingExtensionNumber string
	var bodyAdUsersStatusSyncConfigurationM365ToPbxBindings string
	var bodyAdUsersStatusSyncConfigurationPBXToM365Bindings string
	var bodyAdUsersStatusSyncConfigurationPBXToM365Busy bool
	var bodyAdUsersSyncEvents bool
	var bodyAdUsersSyncGuestUsers bool
	var bodyAdUsersSyncPersonalContacts bool
	var bodyAdUsersSyncType string
	var bodyApplicationId string
	var bodyId string
	var bodySharedMailboxesSyncIsEnabled bool
	var bodySharedMailboxesSyncSelectedUsers string
	var bodySharedMailboxesSyncSyncType string
	var bodyTenantId string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Update Microsoft365Integration",
		Example:     "  3cx-xapi-pp-cli microsoft365-integration update",
		Annotations: map[string]string{"pp:endpoint": "microsoft365-integration.update", "pp:method": "PATCH", "pp:path": "/Microsoft365Integration"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/Microsoft365Integration"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedAdUsers := map[string]any{}
					if cmd.Flags().Changed("ad-users-enable-sso") {
						nestedAdUsers["EnableSSO"] = bodyAdUsersEnableSSO
					}
					if cmd.Flags().Changed("ad-users-is-enabled") {
						nestedAdUsers["IsEnabled"] = bodyAdUsersIsEnabled
					}
					if cmd.Flags().Changed("ad-users-is-sync-departments") {
						nestedAdUsers["IsSyncDepartments"] = bodyAdUsersIsSyncDepartments
					}
					if cmd.Flags().Changed("ad-users-is-sync-details") {
						nestedAdUsers["IsSyncDetails"] = bodyAdUsersIsSyncDetails
					}
					if cmd.Flags().Changed("ad-users-is-sync-office-phone") {
						nestedAdUsers["IsSyncOfficePhone"] = bodyAdUsersIsSyncOfficePhone
					}
					if cmd.Flags().Changed("ad-users-is-sync-photo") {
						nestedAdUsers["IsSyncPhoto"] = bodyAdUsersIsSyncPhoto
					}
					if bodyAdUsersSelectedGroups != "" {
						parsedAdUsersSelectedGroups, parseErr := cliutil.ParseStringList(bodyAdUsersSelectedGroups)
						if parseErr != nil {
							return fmt.Errorf("parsing --ad-users-selected-groups list: %w", parseErr)
						}
						nestedAdUsers["SelectedGroups"] = parsedAdUsersSelectedGroups
					}
					if bodyAdUsersSelectedUsers != "" {
						parsedAdUsersSelectedUsers, parseErr := cliutil.ParseStringList(bodyAdUsersSelectedUsers)
						if parseErr != nil {
							return fmt.Errorf("parsing --ad-users-selected-users list: %w", parseErr)
						}
						nestedAdUsers["SelectedUsers"] = parsedAdUsersSelectedUsers
					}
					if cmd.Flags().Changed("ad-users-set-teams-presence") {
						nestedAdUsers["SetTeamsPresence"] = bodyAdUsersSetTeamsPresence
					}
					if bodyAdUsersStartingExtensionNumber != "" {
						nestedAdUsers["StartingExtensionNumber"] = bodyAdUsersStartingExtensionNumber
					}
					{
						nestedAdUsersStatusSyncConfiguration := map[string]any{}
						if bodyAdUsersStatusSyncConfigurationM365ToPbxBindings != "" {
							var parsedAdUsersStatusSyncConfigurationM365ToPbxBindings any
							if err := json.Unmarshal([]byte(bodyAdUsersStatusSyncConfigurationM365ToPbxBindings), &parsedAdUsersStatusSyncConfigurationM365ToPbxBindings); err != nil {
								return fmt.Errorf("parsing --ad-users-status-sync-configuration-m365-to-pbx-bindings JSON: %w", err)
							}
							nestedAdUsersStatusSyncConfiguration["M365ToPbxBindings"] = parsedAdUsersStatusSyncConfigurationM365ToPbxBindings
						}
						if bodyAdUsersStatusSyncConfigurationPBXToM365Bindings != "" {
							var parsedAdUsersStatusSyncConfigurationPBXToM365Bindings any
							if err := json.Unmarshal([]byte(bodyAdUsersStatusSyncConfigurationPBXToM365Bindings), &parsedAdUsersStatusSyncConfigurationPBXToM365Bindings); err != nil {
								return fmt.Errorf("parsing --ad-users-status-sync-configuration-pbx-to-m365-bindings JSON: %w", err)
							}
							nestedAdUsersStatusSyncConfiguration["PBXToM365Bindings"] = parsedAdUsersStatusSyncConfigurationPBXToM365Bindings
						}
						if cmd.Flags().Changed("ad-users-status-sync-configuration-pbx-to-m365-busy") {
							nestedAdUsersStatusSyncConfiguration["PBXToM365Busy"] = bodyAdUsersStatusSyncConfigurationPBXToM365Busy
						}
						if len(nestedAdUsersStatusSyncConfiguration) > 0 {
							nestedAdUsers["StatusSyncConfiguration"] = nestedAdUsersStatusSyncConfiguration
						}
					}
					if cmd.Flags().Changed("ad-users-sync-events") {
						nestedAdUsers["SyncEvents"] = bodyAdUsersSyncEvents
					}
					if cmd.Flags().Changed("ad-users-sync-guest-users") {
						nestedAdUsers["SyncGuestUsers"] = bodyAdUsersSyncGuestUsers
					}
					if cmd.Flags().Changed("ad-users-sync-personal-contacts") {
						nestedAdUsers["SyncPersonalContacts"] = bodyAdUsersSyncPersonalContacts
					}
					if bodyAdUsersSyncType != "" {
						nestedAdUsers["SyncType"] = bodyAdUsersSyncType
					}
					if len(nestedAdUsers) > 0 {
						body["AdUsers"] = nestedAdUsers
					}
				}
				if bodyApplicationId != "" {
					body["ApplicationId"] = bodyApplicationId
				}
				if bodyId != "" {
					body["Id"] = bodyId
				}
				{
					nestedSharedMailboxesSync := map[string]any{}
					if cmd.Flags().Changed("shared-mailboxes-sync-is-enabled") {
						nestedSharedMailboxesSync["IsEnabled"] = bodySharedMailboxesSyncIsEnabled
					}
					if bodySharedMailboxesSyncSelectedUsers != "" {
						parsedSharedMailboxesSyncSelectedUsers, parseErr := cliutil.ParseStringList(bodySharedMailboxesSyncSelectedUsers)
						if parseErr != nil {
							return fmt.Errorf("parsing --shared-mailboxes-sync-selected-users list: %w", parseErr)
						}
						nestedSharedMailboxesSync["SelectedUsers"] = parsedSharedMailboxesSyncSelectedUsers
					}
					if bodySharedMailboxesSyncSyncType != "" {
						nestedSharedMailboxesSync["SyncType"] = bodySharedMailboxesSyncSyncType
					}
					if len(nestedSharedMailboxesSync) > 0 {
						body["SharedMailboxesSync"] = nestedSharedMailboxesSync
					}
				}
				if bodyTenantId != "" {
					body["TenantId"] = bodyTenantId
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "microsoft365-integration", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "microsoft365-integration", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "microsoft365-integration", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "microsoft365-integration", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "microsoft365-integration", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "microsoft365-integration",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "microsoft365-integration", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "microsoft365-integration", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAdUsersEnableSSO, "ad-users-enable-sso", false, "Enable sso")
	cmd.Flags().BoolVar(&bodyAdUsersIsEnabled, "ad-users-is-enabled", false, "Is enabled")
	cmd.Flags().BoolVar(&bodyAdUsersIsSyncDepartments, "ad-users-is-sync-departments", false, "Is sync departments")
	cmd.Flags().BoolVar(&bodyAdUsersIsSyncDetails, "ad-users-is-sync-details", false, "Is sync details")
	cmd.Flags().BoolVar(&bodyAdUsersIsSyncOfficePhone, "ad-users-is-sync-office-phone", false, "Is sync office phone")
	cmd.Flags().BoolVar(&bodyAdUsersIsSyncPhoto, "ad-users-is-sync-photo", false, "Is sync photo")
	cmd.Flags().StringVar(&bodyAdUsersSelectedGroups, "ad-users-selected-groups", "", "Selected groups")
	cmd.Flags().StringVar(&bodyAdUsersSelectedUsers, "ad-users-selected-users", "", "Selected users")
	cmd.Flags().BoolVar(&bodyAdUsersSetTeamsPresence, "ad-users-set-teams-presence", false, "Set teams presence")
	cmd.Flags().StringVar(&bodyAdUsersStartingExtensionNumber, "ad-users-starting-extension-number", "", "Starting extension number")
	cmd.Flags().StringVar(&bodyAdUsersStatusSyncConfigurationM365ToPbxBindings, "ad-users-status-sync-configuration-m365-to-pbx-bindings", "", "M365 to pbx bindings")
	cmd.Flags().StringVar(&bodyAdUsersStatusSyncConfigurationPBXToM365Bindings, "ad-users-status-sync-configuration-pbx-to-m365-bindings", "", "Pbxto m365 bindings")
	cmd.Flags().BoolVar(&bodyAdUsersStatusSyncConfigurationPBXToM365Busy, "ad-users-status-sync-configuration-pbx-to-m365-busy", false, "Pbxto m365 busy")
	cmd.Flags().BoolVar(&bodyAdUsersSyncEvents, "ad-users-sync-events", false, "Sync events")
	cmd.Flags().BoolVar(&bodyAdUsersSyncGuestUsers, "ad-users-sync-guest-users", false, "Sync guest users")
	cmd.Flags().BoolVar(&bodyAdUsersSyncPersonalContacts, "ad-users-sync-personal-contacts", false, "Sync personal contacts")
	cmd.Flags().StringVar(&bodyAdUsersSyncType, "ad-users-sync-type", "", "Sync type")
	cmd.Flags().StringVar(&bodyApplicationId, "application-id", "", "Application id")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().BoolVar(&bodySharedMailboxesSyncIsEnabled, "shared-mailboxes-sync-is-enabled", false, "Is enabled")
	cmd.Flags().StringVar(&bodySharedMailboxesSyncSelectedUsers, "shared-mailboxes-sync-selected-users", "", "Selected users")
	cmd.Flags().StringVar(&bodySharedMailboxesSyncSyncType, "shared-mailboxes-sync-sync-type", "", "Sync type")
	cmd.Flags().StringVar(&bodyTenantId, "tenant-id", "", "Tenant id")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
