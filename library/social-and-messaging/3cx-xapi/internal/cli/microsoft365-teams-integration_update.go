// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newMicrosoft365TeamsIntegrationUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAreaCode string
	var bodyDialPlanCode string
	var bodyEnabled bool
	var bodyId string
	var bodyIsDynamicIP bool
	var bodyIsNativeFQDN bool
	var bodySbcCertificateConcealed bool
	var bodySbcCertificateContents string
	var bodySbcCertificateName string
	var bodySbcCertificateExpirationDate string
	var bodySbcFQDN string
	var bodySbcPrivateKeyConcealed bool
	var bodySbcPrivateKeyContents string
	var bodySbcPrivateKeyName string
	var bodySecureSipEnabled bool
	var bodySipDomain string
	var bodyTlsPortForNativeFQDN int
	var bodyTlsPortForNonNativeFQDN int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Update Microsoft365TeamsIntegration",
		Example:     "  3cx-xapi-pp-cli microsoft365-teams-integration update",
		Annotations: map[string]string{"pp:endpoint": "microsoft365-teams-integration.update", "pp:method": "PATCH", "pp:path": "/Microsoft365TeamsIntegration"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/Microsoft365TeamsIntegration"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAreaCode != "" {
					body["AreaCode"] = bodyAreaCode
				}
				if bodyDialPlanCode != "" {
					body["DialPlanCode"] = bodyDialPlanCode
				}
				if cmd.Flags().Changed("enabled") {
					body["Enabled"] = bodyEnabled
				}
				if bodyId != "" {
					body["Id"] = bodyId
				}
				if cmd.Flags().Changed("is-dynamic-ip") {
					body["IsDynamicIP"] = bodyIsDynamicIP
				}
				if cmd.Flags().Changed("is-native-fqdn") {
					body["IsNativeFQDN"] = bodyIsNativeFQDN
				}
				{
					nestedSbcCertificate := map[string]any{}
					if cmd.Flags().Changed("sbc-certificate-concealed") {
						nestedSbcCertificate["Concealed"] = bodySbcCertificateConcealed
					}
					if bodySbcCertificateContents != "" {
						nestedSbcCertificate["Contents"] = bodySbcCertificateContents
					}
					if bodySbcCertificateName != "" {
						nestedSbcCertificate["Name"] = bodySbcCertificateName
					}
					if len(nestedSbcCertificate) > 0 {
						body["SbcCertificate"] = nestedSbcCertificate
					}
				}
				if bodySbcCertificateExpirationDate != "" {
					body["SbcCertificateExpirationDate"] = bodySbcCertificateExpirationDate
				}
				if bodySbcFQDN != "" {
					body["SbcFQDN"] = bodySbcFQDN
				}
				{
					nestedSbcPrivateKey := map[string]any{}
					if cmd.Flags().Changed("sbc-private-key-concealed") {
						nestedSbcPrivateKey["Concealed"] = bodySbcPrivateKeyConcealed
					}
					if bodySbcPrivateKeyContents != "" {
						nestedSbcPrivateKey["Contents"] = bodySbcPrivateKeyContents
					}
					if bodySbcPrivateKeyName != "" {
						nestedSbcPrivateKey["Name"] = bodySbcPrivateKeyName
					}
					if len(nestedSbcPrivateKey) > 0 {
						body["SbcPrivateKey"] = nestedSbcPrivateKey
					}
				}
				if cmd.Flags().Changed("secure-sip-enabled") {
					body["SecureSipEnabled"] = bodySecureSipEnabled
				}
				if bodySipDomain != "" {
					body["SipDomain"] = bodySipDomain
				}
				if bodyTlsPortForNativeFQDN != 0 {
					body["TlsPortForNativeFQDN"] = bodyTlsPortForNativeFQDN
				}
				if bodyTlsPortForNonNativeFQDN != 0 {
					body["TlsPortForNonNativeFQDN"] = bodyTlsPortForNonNativeFQDN
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "microsoft365-teams-integration", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "microsoft365-teams-integration", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "microsoft365-teams-integration", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "microsoft365-teams-integration", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "microsoft365-teams-integration", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "microsoft365-teams-integration",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "microsoft365-teams-integration", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "microsoft365-teams-integration", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAreaCode, "area-code", "", "Area code")
	cmd.Flags().StringVar(&bodyDialPlanCode, "dial-plan-code", "", "Dial plan code")
	cmd.Flags().BoolVar(&bodyEnabled, "enabled", false, "Enabled")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().BoolVar(&bodyIsDynamicIP, "is-dynamic-ip", false, "Is dynamic ip")
	cmd.Flags().BoolVar(&bodyIsNativeFQDN, "is-native-fqdn", false, "Is native fqdn")
	cmd.Flags().BoolVar(&bodySbcCertificateConcealed, "sbc-certificate-concealed", false, "Concealed")
	cmd.Flags().StringVar(&bodySbcCertificateContents, "sbc-certificate-contents", "", "Contents")
	cmd.Flags().StringVar(&bodySbcCertificateName, "sbc-certificate-name", "", "Name")
	cmd.Flags().StringVar(&bodySbcCertificateExpirationDate, "sbc-certificate-expiration-date", "", "Sbc certificate expiration date")
	cmd.Flags().StringVar(&bodySbcFQDN, "sbc-fqdn", "", "Sbc fqdn")
	cmd.Flags().BoolVar(&bodySbcPrivateKeyConcealed, "sbc-private-key-concealed", false, "Concealed")
	cmd.Flags().StringVar(&bodySbcPrivateKeyContents, "sbc-private-key-contents", "", "Contents")
	cmd.Flags().StringVar(&bodySbcPrivateKeyName, "sbc-private-key-name", "", "Name")
	cmd.Flags().BoolVar(&bodySecureSipEnabled, "secure-sip-enabled", false, "Secure sip enabled")
	cmd.Flags().StringVar(&bodySipDomain, "sip-domain", "", "Sip domain")
	cmd.Flags().IntVar(&bodyTlsPortForNativeFQDN, "tls-port-for-native-fqdn", 0, "Tls port for native fqdn")
	cmd.Flags().IntVar(&bodyTlsPortForNonNativeFQDN, "tls-port-for-non-native-fqdn", 0, "Tls port for non native fqdn")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
