// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newReportAverageQueueWaitingTimeDownloadAverageQueueWaitingTimeReportCmd(flags *rootFlags) *cobra.Command {
	var flagChartDate string

	cmd := &cobra.Command{
		Use:     "download-average-queue-waiting-time-report <chartBy> <queueDnStr> <waitInterval> <clientTimeZone>",
		Aliases: []string{"get"},
		Short:   "Invoke function DownloadAverageQueueWaitingTimeReport",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  3cx-xapi-pp-cli report-average-queue-waiting-time download-average-queue-waiting-time-report example-value example-value example-value 2026-01-15T09:00:00Z --chart-date 2026-01-15",
		Annotations: map[string]string{"pp:endpoint": "report-average-queue-waiting-time.download-average-queue-waiting-time-report", "pp:method": "GET", "pp:path": "/ReportAverageQueueWaitingTime/Pbx.DownloadAverageQueueWaitingTimeReport(chartDate={chartDate},chartBy={chartBy},queueDnStr={queueDnStr},waitInterval={waitInterval},clientTimeZone={clientTimeZone})", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("chart-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "chart-date")
			}
			path := "/ReportAverageQueueWaitingTime/Pbx.DownloadAverageQueueWaitingTimeReport(chartDate={chartDate},chartBy={chartBy},queueDnStr={queueDnStr},waitInterval={waitInterval},clientTimeZone={clientTimeZone})"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("chartBy is required\nUsage: %s <%s>", cmd.CommandPath(), "chartBy"))
			}
			path = replacePathParam(path, "chartBy", args[0])
			if len(args) < 2 || args[1] == "" {
				return usageErr(fmt.Errorf("queueDnStr is required\nUsage: %s <%s>", cmd.CommandPath(), "queueDnStr"))
			}
			path = replacePathParam(path, "queueDnStr", args[1])
			if len(args) < 3 || args[2] == "" {
				return usageErr(fmt.Errorf("waitInterval is required\nUsage: %s <%s>", cmd.CommandPath(), "waitInterval"))
			}
			path = replacePathParam(path, "waitInterval", args[2])
			if len(args) < 4 || args[3] == "" {
				return usageErr(fmt.Errorf("clientTimeZone is required\nUsage: %s <%s>", cmd.CommandPath(), "clientTimeZone"))
			}
			path = replacePathParam(path, "clientTimeZone", args[3])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			path = replacePathParam(path, "chartDate", formatCLIParamValue(flagChartDate))
			params := map[string]string{}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "report-average-queue-waiting-time", false, path, params, nil, "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagChartDate, "chart-date", "", "Usage: chartDate={chartDate}")

	return cmd
}
