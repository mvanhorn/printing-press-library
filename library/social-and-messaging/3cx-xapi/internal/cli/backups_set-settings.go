// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newBackupsSetSettingsCmd(flags *rootFlags) *cobra.Command {
	var bodySettingsContentsCallHistory bool
	var bodySettingsContentsDisableBackupCompression bool
	var bodySettingsContentsEncryptBackup bool
	var bodySettingsContentsFQDN bool
	var bodySettingsContentsLicense bool
	var bodySettingsContentsPhoneProvisioning bool
	var bodySettingsContentsPrompts bool
	var bodySettingsContentsRecordings bool
	var bodySettingsContentsVoiceMails bool
	var bodySettingsRotation int
	var bodySettingsScheduleDay string
	var bodySettingsScheduleRepeatHours int
	var bodySettingsScheduleRepeatMinutes int
	var bodySettingsScheduleScheduleType string
	var bodySettingsScheduleTime string
	var bodySettingsScheduleEnabled bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "set-settings",
		Short:       "Invoke action SetBackupSettings",
		Example:     "  3cx-xapi-pp-cli backups set-settings",
		Annotations: map[string]string{"pp:endpoint": "backups.set-settings", "pp:method": "POST", "pp:path": "/Backups/Pbx.SetBackupSettings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/Backups/Pbx.SetBackupSettings"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedSettings := map[string]any{}
					{
						nestedSettingsContents := map[string]any{}
						if cmd.Flags().Changed("settings-contents-call-history") {
							nestedSettingsContents["CallHistory"] = bodySettingsContentsCallHistory
						}
						if cmd.Flags().Changed("settings-contents-disable-backup-compression") {
							nestedSettingsContents["DisableBackupCompression"] = bodySettingsContentsDisableBackupCompression
						}
						if cmd.Flags().Changed("settings-contents-encrypt-backup") {
							nestedSettingsContents["EncryptBackup"] = bodySettingsContentsEncryptBackup
						}
						if cmd.Flags().Changed("settings-contents-fqdn") {
							nestedSettingsContents["FQDN"] = bodySettingsContentsFQDN
						}
						if cmd.Flags().Changed("settings-contents-license") {
							nestedSettingsContents["License"] = bodySettingsContentsLicense
						}
						if cmd.Flags().Changed("settings-contents-phone-provisioning") {
							nestedSettingsContents["PhoneProvisioning"] = bodySettingsContentsPhoneProvisioning
						}
						if cmd.Flags().Changed("settings-contents-prompts") {
							nestedSettingsContents["Prompts"] = bodySettingsContentsPrompts
						}
						if cmd.Flags().Changed("settings-contents-recordings") {
							nestedSettingsContents["Recordings"] = bodySettingsContentsRecordings
						}
						if cmd.Flags().Changed("settings-contents-voice-mails") {
							nestedSettingsContents["VoiceMails"] = bodySettingsContentsVoiceMails
						}
						if len(nestedSettingsContents) > 0 {
							nestedSettings["Contents"] = nestedSettingsContents
						}
					}
					if bodySettingsRotation != 0 {
						nestedSettings["Rotation"] = bodySettingsRotation
					}
					{
						nestedSettingsSchedule := map[string]any{}
						if bodySettingsScheduleDay != "" {
							nestedSettingsSchedule["Day"] = bodySettingsScheduleDay
						}
						if bodySettingsScheduleRepeatHours != 0 {
							nestedSettingsSchedule["RepeatHours"] = bodySettingsScheduleRepeatHours
						}
						if bodySettingsScheduleRepeatMinutes != 0 {
							nestedSettingsSchedule["RepeatMinutes"] = bodySettingsScheduleRepeatMinutes
						}
						if bodySettingsScheduleScheduleType != "" {
							nestedSettingsSchedule["ScheduleType"] = bodySettingsScheduleScheduleType
						}
						if bodySettingsScheduleTime != "" {
							nestedSettingsSchedule["Time"] = bodySettingsScheduleTime
						}
						if len(nestedSettingsSchedule) > 0 {
							nestedSettings["Schedule"] = nestedSettingsSchedule
						}
					}
					if cmd.Flags().Changed("settings-schedule-enabled") {
						nestedSettings["ScheduleEnabled"] = bodySettingsScheduleEnabled
					}
					if len(nestedSettings) > 0 {
						body["settings"] = nestedSettings
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "backups", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "backups", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "backups", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "backups", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "backups", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "backups",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "backups", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "backups", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodySettingsContentsCallHistory, "settings-contents-call-history", false, "Call history")
	cmd.Flags().BoolVar(&bodySettingsContentsDisableBackupCompression, "settings-contents-disable-backup-compression", false, "Disable backup compression")
	cmd.Flags().BoolVar(&bodySettingsContentsEncryptBackup, "settings-contents-encrypt-backup", false, "Encrypt backup")
	cmd.Flags().BoolVar(&bodySettingsContentsFQDN, "settings-contents-fqdn", false, "Fqdn")
	cmd.Flags().BoolVar(&bodySettingsContentsLicense, "settings-contents-license", false, "License")
	cmd.Flags().BoolVar(&bodySettingsContentsPhoneProvisioning, "settings-contents-phone-provisioning", false, "Phone provisioning")
	cmd.Flags().BoolVar(&bodySettingsContentsPrompts, "settings-contents-prompts", false, "Prompts")
	cmd.Flags().BoolVar(&bodySettingsContentsRecordings, "settings-contents-recordings", false, "Recordings")
	cmd.Flags().BoolVar(&bodySettingsContentsVoiceMails, "settings-contents-voice-mails", false, "Voice mails")
	cmd.Flags().IntVar(&bodySettingsRotation, "settings-rotation", 0, "Rotation")
	cmd.Flags().StringVar(&bodySettingsScheduleDay, "settings-schedule-day", "", "Day")
	cmd.Flags().IntVar(&bodySettingsScheduleRepeatHours, "settings-schedule-repeat-hours", 0, "Repeat hours")
	cmd.Flags().IntVar(&bodySettingsScheduleRepeatMinutes, "settings-schedule-repeat-minutes", 0, "Repeat minutes")
	cmd.Flags().StringVar(&bodySettingsScheduleScheduleType, "settings-schedule-schedule-type", "", "Schedule type")
	cmd.Flags().StringVar(&bodySettingsScheduleTime, "settings-schedule-time", "", "Time")
	cmd.Flags().BoolVar(&bodySettingsScheduleEnabled, "settings-schedule-enabled", false, "Schedule enabled")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin (use this for deeply nested fields not exposed as flags)")

	return cmd
}
