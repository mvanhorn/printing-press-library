// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newNetworkSettingsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAllowSourceAsOutbound bool
	var bodyDirectSIPAllowExternal bool
	var bodyDirectSIPLocalDomain string
	var bodyFirewallKeepAlive bool
	var bodyFirewallKeepAliveInterval int
	var bodyId string
	var bodyIpV6BindingEnabled bool
	var bodyPbxPublicFQDN string
	var bodyPublicInterface string
	var bodyPublicStaticIP string
	var bodySipPort int
	var bodyStunDisabled bool
	var bodyStunPrimaryHost string
	var bodyStunPrimaryPort int
	var bodyStunQuery int
	var bodyStunSecondaryHost string
	var bodyStunSecondaryPort int
	var bodyStunThirdHost string
	var bodyStunThirdPort int
	var bodyTunnelPort int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Update NetworkSettings",
		Example:     "  3cx-xapi-pp-cli network-settings update",
		Annotations: map[string]string{"pp:endpoint": "network-settings.update", "pp:method": "PATCH", "pp:path": "/NetworkSettings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/NetworkSettings"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("allow-source-as-outbound") {
					body["AllowSourceAsOutbound"] = bodyAllowSourceAsOutbound
				}
				if cmd.Flags().Changed("direct-sip-allow-external") {
					body["DirectSIPAllowExternal"] = bodyDirectSIPAllowExternal
				}
				if bodyDirectSIPLocalDomain != "" {
					body["DirectSIPLocalDomain"] = bodyDirectSIPLocalDomain
				}
				if cmd.Flags().Changed("firewall-keep-alive") {
					body["FirewallKeepAlive"] = bodyFirewallKeepAlive
				}
				if bodyFirewallKeepAliveInterval != 0 {
					body["FirewallKeepAliveInterval"] = bodyFirewallKeepAliveInterval
				}
				if bodyId != "" {
					body["Id"] = bodyId
				}
				if cmd.Flags().Changed("ip-v6-binding-enabled") {
					body["IpV6BindingEnabled"] = bodyIpV6BindingEnabled
				}
				if bodyPbxPublicFQDN != "" {
					body["PbxPublicFQDN"] = bodyPbxPublicFQDN
				}
				if bodyPublicInterface != "" {
					body["PublicInterface"] = bodyPublicInterface
				}
				if bodyPublicStaticIP != "" {
					body["PublicStaticIP"] = bodyPublicStaticIP
				}
				if bodySipPort != 0 {
					body["SipPort"] = bodySipPort
				}
				if cmd.Flags().Changed("stun-disabled") {
					body["StunDisabled"] = bodyStunDisabled
				}
				if bodyStunPrimaryHost != "" {
					body["StunPrimaryHost"] = bodyStunPrimaryHost
				}
				if bodyStunPrimaryPort != 0 {
					body["StunPrimaryPort"] = bodyStunPrimaryPort
				}
				if bodyStunQuery != 0 {
					body["StunQuery"] = bodyStunQuery
				}
				if bodyStunSecondaryHost != "" {
					body["StunSecondaryHost"] = bodyStunSecondaryHost
				}
				if bodyStunSecondaryPort != 0 {
					body["StunSecondaryPort"] = bodyStunSecondaryPort
				}
				if bodyStunThirdHost != "" {
					body["StunThirdHost"] = bodyStunThirdHost
				}
				if bodyStunThirdPort != 0 {
					body["StunThirdPort"] = bodyStunThirdPort
				}
				if bodyTunnelPort != 0 {
					body["TunnelPort"] = bodyTunnelPort
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "network-settings", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "network-settings", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "network-settings", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "network-settings", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "network-settings", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "network-settings",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "network-settings", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "network-settings", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAllowSourceAsOutbound, "allow-source-as-outbound", false, "Allow source as outbound")
	cmd.Flags().BoolVar(&bodyDirectSIPAllowExternal, "direct-sip-allow-external", false, "Direct sipallow external")
	cmd.Flags().StringVar(&bodyDirectSIPLocalDomain, "direct-sip-local-domain", "", "Direct siplocal domain")
	cmd.Flags().BoolVar(&bodyFirewallKeepAlive, "firewall-keep-alive", false, "Firewall keep alive")
	cmd.Flags().IntVar(&bodyFirewallKeepAliveInterval, "firewall-keep-alive-interval", 0, "Firewall keep alive interval")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().BoolVar(&bodyIpV6BindingEnabled, "ip-v6-binding-enabled", false, "Ip v6 binding enabled")
	cmd.Flags().StringVar(&bodyPbxPublicFQDN, "pbx-public-fqdn", "", "Pbx public fqdn")
	cmd.Flags().StringVar(&bodyPublicInterface, "public-interface", "", "Public interface")
	cmd.Flags().StringVar(&bodyPublicStaticIP, "public-static-ip", "", "Public static ip")
	cmd.Flags().IntVar(&bodySipPort, "sip-port", 0, "Sip port")
	cmd.Flags().BoolVar(&bodyStunDisabled, "stun-disabled", false, "Stun disabled")
	cmd.Flags().StringVar(&bodyStunPrimaryHost, "stun-primary-host", "", "Stun primary host")
	cmd.Flags().IntVar(&bodyStunPrimaryPort, "stun-primary-port", 0, "Stun primary port")
	cmd.Flags().IntVar(&bodyStunQuery, "stun-query", 0, "Stun query")
	cmd.Flags().StringVar(&bodyStunSecondaryHost, "stun-secondary-host", "", "Stun secondary host")
	cmd.Flags().IntVar(&bodyStunSecondaryPort, "stun-secondary-port", 0, "Stun secondary port")
	cmd.Flags().StringVar(&bodyStunThirdHost, "stun-third-host", "", "Stun third host")
	cmd.Flags().IntVar(&bodyStunThirdPort, "stun-third-port", 0, "Stun third port")
	cmd.Flags().IntVar(&bodyTunnelPort, "tunnel-port", 0, "Tunnel port")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
