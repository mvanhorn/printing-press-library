// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newReportCallLogDataDownloadCallLogCmd(flags *rootFlags) *cobra.Command {
	var flagPeriodFrom string
	var flagPeriodTo string
	var flagHidePcalls bool

	cmd := &cobra.Command{
		Use:     "download-call-log <sourceType> <sourceFilter> <destinationType> <destinationFilter> <callsType> <callTimeFilterType> <callTimeFilterFrom> <callTimeFilterTo> <clientTimeZone>",
		Aliases: []string{"get"},
		Short:   "Invoke function DownloadCallLog",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  3cx-xapi-pp-cli report-call-log-data download-call-log 42 example-value 42 example-value 42 42 2026-01-15T09:00:00Z 2026-01-15T09:00:00Z 2026-01-15T09:00:00Z --period-from 2026-01-15T09:00:00Z --period-to 2026-01-15T09:00:00Z --hide-pcalls true",
		Annotations: map[string]string{"pp:endpoint": "report-call-log-data.download-call-log", "pp:method": "GET", "pp:path": "/ReportCallLogData/Pbx.DownloadCallLog(periodFrom={periodFrom},periodTo={periodTo},sourceType={sourceType},sourceFilter={sourceFilter},destinationType={destinationType},destinationFilter={destinationFilter},callsType={callsType},callTimeFilterType={callTimeFilterType},callTimeFilterFrom={callTimeFilterFrom},callTimeFilterTo={callTimeFilterTo},hidePcalls={hidePcalls},clientTimeZone={clientTimeZone})", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("period-from") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "period-from")
			}
			if !cmd.Flags().Changed("period-to") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "period-to")
			}
			if !cmd.Flags().Changed("hide-pcalls") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "hide-pcalls")
			}
			path := "/ReportCallLogData/Pbx.DownloadCallLog(periodFrom={periodFrom},periodTo={periodTo},sourceType={sourceType},sourceFilter={sourceFilter},destinationType={destinationType},destinationFilter={destinationFilter},callsType={callsType},callTimeFilterType={callTimeFilterType},callTimeFilterFrom={callTimeFilterFrom},callTimeFilterTo={callTimeFilterTo},hidePcalls={hidePcalls},clientTimeZone={clientTimeZone})"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("sourceType is required\nUsage: %s <%s>", cmd.CommandPath(), "sourceType"))
			}
			path = replacePathParam(path, "sourceType", args[0])
			if len(args) < 2 || args[1] == "" {
				return usageErr(fmt.Errorf("sourceFilter is required\nUsage: %s <%s>", cmd.CommandPath(), "sourceFilter"))
			}
			path = replacePathParam(path, "sourceFilter", args[1])
			if len(args) < 3 || args[2] == "" {
				return usageErr(fmt.Errorf("destinationType is required\nUsage: %s <%s>", cmd.CommandPath(), "destinationType"))
			}
			path = replacePathParam(path, "destinationType", args[2])
			if len(args) < 4 || args[3] == "" {
				return usageErr(fmt.Errorf("destinationFilter is required\nUsage: %s <%s>", cmd.CommandPath(), "destinationFilter"))
			}
			path = replacePathParam(path, "destinationFilter", args[3])
			if len(args) < 5 || args[4] == "" {
				return usageErr(fmt.Errorf("callsType is required\nUsage: %s <%s>", cmd.CommandPath(), "callsType"))
			}
			path = replacePathParam(path, "callsType", args[4])
			if len(args) < 6 || args[5] == "" {
				return usageErr(fmt.Errorf("callTimeFilterType is required\nUsage: %s <%s>", cmd.CommandPath(), "callTimeFilterType"))
			}
			path = replacePathParam(path, "callTimeFilterType", args[5])
			if len(args) < 7 || args[6] == "" {
				return usageErr(fmt.Errorf("callTimeFilterFrom is required\nUsage: %s <%s>", cmd.CommandPath(), "callTimeFilterFrom"))
			}
			path = replacePathParam(path, "callTimeFilterFrom", args[6])
			if len(args) < 8 || args[7] == "" {
				return usageErr(fmt.Errorf("callTimeFilterTo is required\nUsage: %s <%s>", cmd.CommandPath(), "callTimeFilterTo"))
			}
			path = replacePathParam(path, "callTimeFilterTo", args[7])
			if len(args) < 9 || args[8] == "" {
				return usageErr(fmt.Errorf("clientTimeZone is required\nUsage: %s <%s>", cmd.CommandPath(), "clientTimeZone"))
			}
			path = replacePathParam(path, "clientTimeZone", args[8])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			path = replacePathParam(path, "periodFrom", formatCLIParamValue(flagPeriodFrom))
			path = replacePathParam(path, "periodTo", formatCLIParamValue(flagPeriodTo))
			path = replacePathParam(path, "hidePcalls", formatCLIParamValue(flagHidePcalls))
			params := map[string]string{}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "report-call-log-data", false, path, params, nil, "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagPeriodFrom, "period-from", "", "Usage: periodFrom={periodFrom}")
	cmd.Flags().StringVar(&flagPeriodTo, "period-to", "", "Usage: periodTo={periodTo}")
	cmd.Flags().BoolVar(&flagHidePcalls, "hide-pcalls", false, "Usage: hidePcalls={hidePcalls}")

	return cmd
}
