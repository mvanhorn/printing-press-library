// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newGeneralSettingsForAppsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAllowBlockCalls bool
	var bodyAllowChangePassword bool
	var bodyAutoSchedulerSettingsEnabled bool
	var bodyAutoSchedulerSettingsProfileAvailable string
	var bodyAutoSchedulerSettingsProfileAway string
	var bodyAutoSchedulerSettingsProfileDND string
	var bodyAvatarStyle string
	var bodyBrandLogoImage string
	var bodyBrandMainImage string
	var bodyBrandUrl string
	var bodyEnableChat bool
	var bodyHideAbandonedQueueCalls bool
	var bodyHideCRMContacts bool
	var bodyHideInteractionHistory bool
	var bodyHideSystemExtensions bool
	var bodyNameOfCustomAvailableStatus string
	var bodyNameOfCustomOutOfOfficeStatus string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Update GeneralSettingsForApps",
		Example:     "  3cx-xapi-pp-cli general-settings-for-apps update",
		Annotations: map[string]string{"pp:endpoint": "general-settings-for-apps.update", "pp:method": "PATCH", "pp:path": "/GeneralSettingsForApps"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/GeneralSettingsForApps"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("allow-block-calls") {
					body["AllowBlockCalls"] = bodyAllowBlockCalls
				}
				if cmd.Flags().Changed("allow-change-password") {
					body["AllowChangePassword"] = bodyAllowChangePassword
				}
				{
					nestedAutoSchedulerSettings := map[string]any{}
					if cmd.Flags().Changed("auto-scheduler-settings-enabled") {
						nestedAutoSchedulerSettings["Enabled"] = bodyAutoSchedulerSettingsEnabled
					}
					if bodyAutoSchedulerSettingsProfileAvailable != "" {
						nestedAutoSchedulerSettings["ProfileAvailable"] = bodyAutoSchedulerSettingsProfileAvailable
					}
					if bodyAutoSchedulerSettingsProfileAway != "" {
						nestedAutoSchedulerSettings["ProfileAway"] = bodyAutoSchedulerSettingsProfileAway
					}
					if bodyAutoSchedulerSettingsProfileDND != "" {
						nestedAutoSchedulerSettings["ProfileDND"] = bodyAutoSchedulerSettingsProfileDND
					}
					if len(nestedAutoSchedulerSettings) > 0 {
						body["AutoSchedulerSettings"] = nestedAutoSchedulerSettings
					}
				}
				if bodyAvatarStyle != "" {
					body["AvatarStyle"] = bodyAvatarStyle
				}
				if bodyBrandLogoImage != "" {
					body["BrandLogoImage"] = bodyBrandLogoImage
				}
				if bodyBrandMainImage != "" {
					body["BrandMainImage"] = bodyBrandMainImage
				}
				if bodyBrandUrl != "" {
					body["BrandUrl"] = bodyBrandUrl
				}
				if cmd.Flags().Changed("enable-chat") {
					body["EnableChat"] = bodyEnableChat
				}
				if cmd.Flags().Changed("hide-abandoned-queue-calls") {
					body["HideAbandonedQueueCalls"] = bodyHideAbandonedQueueCalls
				}
				if cmd.Flags().Changed("hide-crm-contacts") {
					body["HideCRMContacts"] = bodyHideCRMContacts
				}
				if cmd.Flags().Changed("hide-interaction-history") {
					body["HideInteractionHistory"] = bodyHideInteractionHistory
				}
				if cmd.Flags().Changed("hide-system-extensions") {
					body["HideSystemExtensions"] = bodyHideSystemExtensions
				}
				if bodyNameOfCustomAvailableStatus != "" {
					body["NameOfCustomAvailableStatus"] = bodyNameOfCustomAvailableStatus
				}
				if bodyNameOfCustomOutOfOfficeStatus != "" {
					body["NameOfCustomOutOfOfficeStatus"] = bodyNameOfCustomOutOfOfficeStatus
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "general-settings-for-apps", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "general-settings-for-apps", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "general-settings-for-apps", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "general-settings-for-apps", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "general-settings-for-apps", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "general-settings-for-apps",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "general-settings-for-apps", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "general-settings-for-apps", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAllowBlockCalls, "allow-block-calls", false, "Allow block calls")
	cmd.Flags().BoolVar(&bodyAllowChangePassword, "allow-change-password", false, "Allow change password")
	cmd.Flags().BoolVar(&bodyAutoSchedulerSettingsEnabled, "auto-scheduler-settings-enabled", false, "Enabled")
	cmd.Flags().StringVar(&bodyAutoSchedulerSettingsProfileAvailable, "auto-scheduler-settings-profile-available", "", "Profile available")
	cmd.Flags().StringVar(&bodyAutoSchedulerSettingsProfileAway, "auto-scheduler-settings-profile-away", "", "Profile away")
	cmd.Flags().StringVar(&bodyAutoSchedulerSettingsProfileDND, "auto-scheduler-settings-profile-dnd", "", "Profile dnd")
	cmd.Flags().StringVar(&bodyAvatarStyle, "avatar-style", "", "Avatar style")
	cmd.Flags().StringVar(&bodyBrandLogoImage, "brand-logo-image", "", "Brand logo image")
	cmd.Flags().StringVar(&bodyBrandMainImage, "brand-main-image", "", "Brand main image")
	cmd.Flags().StringVar(&bodyBrandUrl, "brand-url", "", "Brand url")
	cmd.Flags().BoolVar(&bodyEnableChat, "enable-chat", false, "Enable chat")
	cmd.Flags().BoolVar(&bodyHideAbandonedQueueCalls, "hide-abandoned-queue-calls", false, "Hide abandoned queue calls")
	cmd.Flags().BoolVar(&bodyHideCRMContacts, "hide-crm-contacts", false, "Hide crmcontacts")
	cmd.Flags().BoolVar(&bodyHideInteractionHistory, "hide-interaction-history", false, "Hide interaction history")
	cmd.Flags().BoolVar(&bodyHideSystemExtensions, "hide-system-extensions", false, "Hide system extensions")
	cmd.Flags().StringVar(&bodyNameOfCustomAvailableStatus, "name-of-custom-available-status", "", "Name of custom available status")
	cmd.Flags().StringVar(&bodyNameOfCustomOutOfOfficeStatus, "name-of-custom-out-of-office-status", "", "Name of custom out of office status")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
