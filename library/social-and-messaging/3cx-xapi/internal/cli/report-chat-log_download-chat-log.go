// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newReportChatLogDownloadChatLogCmd(flags *rootFlags) *cobra.Command {
	var flagPeriodFrom string
	var flagPeriodTo string

	cmd := &cobra.Command{
		Use:     "download-chat-log <clientTimeZone> <fromType> <fromExtension> <fromText> <toType> <toExtension> <toText> <chatType> <participantType>",
		Aliases: []string{"get"},
		Short:   "Invoke function DownloadChatLog",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  3cx-xapi-pp-cli report-chat-log download-chat-log 2026-01-15T09:00:00Z 42 example-value example-value 42 example-value example-value 42 42 --period-from 2026-01-15T09:00:00Z --period-to 2026-01-15T09:00:00Z",
		Annotations: map[string]string{"pp:endpoint": "report-chat-log.download-chat-log", "pp:method": "GET", "pp:path": "/ReportChatLog/Pbx.DownloadChatLog(clientTimeZone={clientTimeZone},periodFrom={periodFrom},periodTo={periodTo},fromType={fromType},fromExtension={fromExtension},fromText={fromText},toType={toType},toExtension={toExtension},toText={toText},chatType={chatType},participantType={participantType})", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("period-from") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "period-from")
			}
			if !cmd.Flags().Changed("period-to") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "period-to")
			}
			path := "/ReportChatLog/Pbx.DownloadChatLog(clientTimeZone={clientTimeZone},periodFrom={periodFrom},periodTo={periodTo},fromType={fromType},fromExtension={fromExtension},fromText={fromText},toType={toType},toExtension={toExtension},toText={toText},chatType={chatType},participantType={participantType})"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("clientTimeZone is required\nUsage: %s <%s>", cmd.CommandPath(), "clientTimeZone"))
			}
			path = replacePathParam(path, "clientTimeZone", args[0])
			if len(args) < 2 || args[1] == "" {
				return usageErr(fmt.Errorf("fromType is required\nUsage: %s <%s>", cmd.CommandPath(), "fromType"))
			}
			path = replacePathParam(path, "fromType", args[1])
			if len(args) < 3 || args[2] == "" {
				return usageErr(fmt.Errorf("fromExtension is required\nUsage: %s <%s>", cmd.CommandPath(), "fromExtension"))
			}
			path = replacePathParam(path, "fromExtension", args[2])
			if len(args) < 4 || args[3] == "" {
				return usageErr(fmt.Errorf("fromText is required\nUsage: %s <%s>", cmd.CommandPath(), "fromText"))
			}
			path = replacePathParam(path, "fromText", args[3])
			if len(args) < 5 || args[4] == "" {
				return usageErr(fmt.Errorf("toType is required\nUsage: %s <%s>", cmd.CommandPath(), "toType"))
			}
			path = replacePathParam(path, "toType", args[4])
			if len(args) < 6 || args[5] == "" {
				return usageErr(fmt.Errorf("toExtension is required\nUsage: %s <%s>", cmd.CommandPath(), "toExtension"))
			}
			path = replacePathParam(path, "toExtension", args[5])
			if len(args) < 7 || args[6] == "" {
				return usageErr(fmt.Errorf("toText is required\nUsage: %s <%s>", cmd.CommandPath(), "toText"))
			}
			path = replacePathParam(path, "toText", args[6])
			if len(args) < 8 || args[7] == "" {
				return usageErr(fmt.Errorf("chatType is required\nUsage: %s <%s>", cmd.CommandPath(), "chatType"))
			}
			path = replacePathParam(path, "chatType", args[7])
			if len(args) < 9 || args[8] == "" {
				return usageErr(fmt.Errorf("participantType is required\nUsage: %s <%s>", cmd.CommandPath(), "participantType"))
			}
			path = replacePathParam(path, "participantType", args[8])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			path = replacePathParam(path, "periodFrom", formatCLIParamValue(flagPeriodFrom))
			path = replacePathParam(path, "periodTo", formatCLIParamValue(flagPeriodTo))
			params := map[string]string{}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "report-chat-log", false, path, params, nil, "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagPeriodFrom, "period-from", "", "Usage: periodFrom={periodFrom}")
	cmd.Flags().StringVar(&flagPeriodTo, "period-to", "", "Usage: periodTo={periodTo}")

	return cmd
}
