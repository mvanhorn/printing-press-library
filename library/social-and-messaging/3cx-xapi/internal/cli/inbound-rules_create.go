// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/3cx-xapi/internal/cliutil"
	"github.com/spf13/cobra"
)

func newInboundRulesCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAlterDestinationDuringHolidays bool
	var bodyAlterDestinationDuringOutOfOfficeHours bool
	var bodyCallType string
	var bodyCondition string
	var bodyCustomData string
	var bodyData string
	var bodyHolidaysDestinationExternal string
	var bodyHolidaysDestinationName string
	var bodyHolidaysDestinationNumber string
	var bodyHolidaysDestinationTags string
	var bodyHolidaysDestinationTo string
	var bodyHolidaysDestinationType string
	var bodyHoursIgnoreHolidays bool
	var bodyHoursPeriods string
	var bodyHoursType string
	var bodyId string
	var bodyOfficeHoursDestinationExternal string
	var bodyOfficeHoursDestinationName string
	var bodyOfficeHoursDestinationNumber string
	var bodyOfficeHoursDestinationTags string
	var bodyOfficeHoursDestinationTo string
	var bodyOfficeHoursDestinationType string
	var bodyOutOfOfficeHoursDestinationExternal string
	var bodyOutOfOfficeHoursDestinationName string
	var bodyOutOfOfficeHoursDestinationNumber string
	var bodyOutOfOfficeHoursDestinationTags string
	var bodyOutOfOfficeHoursDestinationTo string
	var bodyOutOfOfficeHoursDestinationType string
	var bodyRuleName string
	var bodyTrunkDNHidden bool
	var bodyTrunkDNId string
	var bodyTrunkDNMemberOf string
	var bodyTrunkDNName string
	var bodyTrunkDNNumber string
	var bodyTrunkDNTags string
	var bodyTrunkDNType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Add new entity to InboundRules",
		Example:     "  3cx-xapi-pp-cli inbound-rules create",
		Annotations: map[string]string{"pp:endpoint": "inbound-rules.create", "pp:method": "POST", "pp:path": "/InboundRules"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/InboundRules"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("alter-destination-during-holidays") {
					body["AlterDestinationDuringHolidays"] = bodyAlterDestinationDuringHolidays
				}
				if cmd.Flags().Changed("alter-destination-during-out-of-office-hours") {
					body["AlterDestinationDuringOutOfOfficeHours"] = bodyAlterDestinationDuringOutOfOfficeHours
				}
				if bodyCallType != "" {
					body["CallType"] = bodyCallType
				}
				if bodyCondition != "" {
					body["Condition"] = bodyCondition
				}
				if bodyCustomData != "" {
					body["CustomData"] = bodyCustomData
				}
				if bodyData != "" {
					body["Data"] = bodyData
				}
				{
					nestedHolidaysDestination := map[string]any{}
					if bodyHolidaysDestinationExternal != "" {
						nestedHolidaysDestination["External"] = bodyHolidaysDestinationExternal
					}
					if bodyHolidaysDestinationName != "" {
						nestedHolidaysDestination["Name"] = bodyHolidaysDestinationName
					}
					if bodyHolidaysDestinationNumber != "" {
						nestedHolidaysDestination["Number"] = bodyHolidaysDestinationNumber
					}
					if bodyHolidaysDestinationTags != "" {
						parsedHolidaysDestinationTags, parseErr := cliutil.ParseStringList(bodyHolidaysDestinationTags)
						if parseErr != nil {
							return fmt.Errorf("parsing --holidays-destination-tags list: %w", parseErr)
						}
						nestedHolidaysDestination["Tags"] = parsedHolidaysDestinationTags
					}
					if bodyHolidaysDestinationTo != "" {
						nestedHolidaysDestination["To"] = bodyHolidaysDestinationTo
					}
					if bodyHolidaysDestinationType != "" {
						nestedHolidaysDestination["Type"] = bodyHolidaysDestinationType
					}
					if len(nestedHolidaysDestination) > 0 {
						body["HolidaysDestination"] = nestedHolidaysDestination
					}
				}
				{
					nestedHours := map[string]any{}
					if cmd.Flags().Changed("hours-ignore-holidays") {
						nestedHours["IgnoreHolidays"] = bodyHoursIgnoreHolidays
					}
					if bodyHoursPeriods != "" {
						var parsedHoursPeriods any
						if err := json.Unmarshal([]byte(bodyHoursPeriods), &parsedHoursPeriods); err != nil {
							return fmt.Errorf("parsing --hours-periods JSON: %w", err)
						}
						nestedHours["Periods"] = parsedHoursPeriods
					}
					if bodyHoursType != "" {
						nestedHours["Type"] = bodyHoursType
					}
					if len(nestedHours) > 0 {
						body["Hours"] = nestedHours
					}
				}
				if bodyId != "" {
					body["Id"] = bodyId
				}
				{
					nestedOfficeHoursDestination := map[string]any{}
					if bodyOfficeHoursDestinationExternal != "" {
						nestedOfficeHoursDestination["External"] = bodyOfficeHoursDestinationExternal
					}
					if bodyOfficeHoursDestinationName != "" {
						nestedOfficeHoursDestination["Name"] = bodyOfficeHoursDestinationName
					}
					if bodyOfficeHoursDestinationNumber != "" {
						nestedOfficeHoursDestination["Number"] = bodyOfficeHoursDestinationNumber
					}
					if bodyOfficeHoursDestinationTags != "" {
						parsedOfficeHoursDestinationTags, parseErr := cliutil.ParseStringList(bodyOfficeHoursDestinationTags)
						if parseErr != nil {
							return fmt.Errorf("parsing --office-hours-destination-tags list: %w", parseErr)
						}
						nestedOfficeHoursDestination["Tags"] = parsedOfficeHoursDestinationTags
					}
					if bodyOfficeHoursDestinationTo != "" {
						nestedOfficeHoursDestination["To"] = bodyOfficeHoursDestinationTo
					}
					if bodyOfficeHoursDestinationType != "" {
						nestedOfficeHoursDestination["Type"] = bodyOfficeHoursDestinationType
					}
					if len(nestedOfficeHoursDestination) > 0 {
						body["OfficeHoursDestination"] = nestedOfficeHoursDestination
					}
				}
				{
					nestedOutOfOfficeHoursDestination := map[string]any{}
					if bodyOutOfOfficeHoursDestinationExternal != "" {
						nestedOutOfOfficeHoursDestination["External"] = bodyOutOfOfficeHoursDestinationExternal
					}
					if bodyOutOfOfficeHoursDestinationName != "" {
						nestedOutOfOfficeHoursDestination["Name"] = bodyOutOfOfficeHoursDestinationName
					}
					if bodyOutOfOfficeHoursDestinationNumber != "" {
						nestedOutOfOfficeHoursDestination["Number"] = bodyOutOfOfficeHoursDestinationNumber
					}
					if bodyOutOfOfficeHoursDestinationTags != "" {
						parsedOutOfOfficeHoursDestinationTags, parseErr := cliutil.ParseStringList(bodyOutOfOfficeHoursDestinationTags)
						if parseErr != nil {
							return fmt.Errorf("parsing --out-of-office-hours-destination-tags list: %w", parseErr)
						}
						nestedOutOfOfficeHoursDestination["Tags"] = parsedOutOfOfficeHoursDestinationTags
					}
					if bodyOutOfOfficeHoursDestinationTo != "" {
						nestedOutOfOfficeHoursDestination["To"] = bodyOutOfOfficeHoursDestinationTo
					}
					if bodyOutOfOfficeHoursDestinationType != "" {
						nestedOutOfOfficeHoursDestination["Type"] = bodyOutOfOfficeHoursDestinationType
					}
					if len(nestedOutOfOfficeHoursDestination) > 0 {
						body["OutOfOfficeHoursDestination"] = nestedOutOfOfficeHoursDestination
					}
				}
				if bodyRuleName != "" {
					body["RuleName"] = bodyRuleName
				}
				{
					nestedTrunkDN := map[string]any{}
					if cmd.Flags().Changed("trunk-dn-hidden") {
						nestedTrunkDN["Hidden"] = bodyTrunkDNHidden
					}
					if bodyTrunkDNId != "" {
						nestedTrunkDN["Id"] = bodyTrunkDNId
					}
					if bodyTrunkDNMemberOf != "" {
						var parsedTrunkDNMemberOf any
						if err := json.Unmarshal([]byte(bodyTrunkDNMemberOf), &parsedTrunkDNMemberOf); err != nil {
							return fmt.Errorf("parsing --trunk-dn-member-of JSON: %w", err)
						}
						nestedTrunkDN["MemberOf"] = parsedTrunkDNMemberOf
					}
					if bodyTrunkDNName != "" {
						nestedTrunkDN["Name"] = bodyTrunkDNName
					}
					if bodyTrunkDNNumber != "" {
						nestedTrunkDN["Number"] = bodyTrunkDNNumber
					}
					if bodyTrunkDNTags != "" {
						parsedTrunkDNTags, parseErr := cliutil.ParseStringList(bodyTrunkDNTags)
						if parseErr != nil {
							return fmt.Errorf("parsing --trunk-dn-tags list: %w", parseErr)
						}
						nestedTrunkDN["Tags"] = parsedTrunkDNTags
					}
					if bodyTrunkDNType != "" {
						nestedTrunkDN["Type"] = bodyTrunkDNType
					}
					if len(nestedTrunkDN) > 0 {
						body["TrunkDN"] = nestedTrunkDN
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "inbound-rules", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "inbound-rules", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "inbound-rules", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "inbound-rules", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "inbound-rules", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "inbound-rules",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "inbound-rules", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "inbound-rules", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAlterDestinationDuringHolidays, "alter-destination-during-holidays", false, "Alter destination during holidays")
	cmd.Flags().BoolVar(&bodyAlterDestinationDuringOutOfOfficeHours, "alter-destination-during-out-of-office-hours", false, "Alter destination during out of office hours")
	cmd.Flags().StringVar(&bodyCallType, "call-type", "", "Call type")
	cmd.Flags().StringVar(&bodyCondition, "condition", "", "Condition")
	cmd.Flags().StringVar(&bodyCustomData, "custom-data", "", "Custom data")
	cmd.Flags().StringVar(&bodyData, "data", "", "Data")
	cmd.Flags().StringVar(&bodyHolidaysDestinationExternal, "holidays-destination-external", "", "External")
	cmd.Flags().StringVar(&bodyHolidaysDestinationName, "holidays-destination-name", "", "Name")
	cmd.Flags().StringVar(&bodyHolidaysDestinationNumber, "holidays-destination-number", "", "Number")
	cmd.Flags().StringVar(&bodyHolidaysDestinationTags, "holidays-destination-tags", "", "Tags")
	cmd.Flags().StringVar(&bodyHolidaysDestinationTo, "holidays-destination-to", "", "To")
	cmd.Flags().StringVar(&bodyHolidaysDestinationType, "holidays-destination-type", "", "Type")
	cmd.Flags().BoolVar(&bodyHoursIgnoreHolidays, "hours-ignore-holidays", false, "Ignore holidays")
	cmd.Flags().StringVar(&bodyHoursPeriods, "hours-periods", "", "Periods")
	cmd.Flags().StringVar(&bodyHoursType, "hours-type", "", "Type")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().StringVar(&bodyOfficeHoursDestinationExternal, "office-hours-destination-external", "", "External")
	cmd.Flags().StringVar(&bodyOfficeHoursDestinationName, "office-hours-destination-name", "", "Name")
	cmd.Flags().StringVar(&bodyOfficeHoursDestinationNumber, "office-hours-destination-number", "", "Number")
	cmd.Flags().StringVar(&bodyOfficeHoursDestinationTags, "office-hours-destination-tags", "", "Tags")
	cmd.Flags().StringVar(&bodyOfficeHoursDestinationTo, "office-hours-destination-to", "", "To")
	cmd.Flags().StringVar(&bodyOfficeHoursDestinationType, "office-hours-destination-type", "", "Type")
	cmd.Flags().StringVar(&bodyOutOfOfficeHoursDestinationExternal, "out-of-office-hours-destination-external", "", "External")
	cmd.Flags().StringVar(&bodyOutOfOfficeHoursDestinationName, "out-of-office-hours-destination-name", "", "Name")
	cmd.Flags().StringVar(&bodyOutOfOfficeHoursDestinationNumber, "out-of-office-hours-destination-number", "", "Number")
	cmd.Flags().StringVar(&bodyOutOfOfficeHoursDestinationTags, "out-of-office-hours-destination-tags", "", "Tags")
	cmd.Flags().StringVar(&bodyOutOfOfficeHoursDestinationTo, "out-of-office-hours-destination-to", "", "To")
	cmd.Flags().StringVar(&bodyOutOfOfficeHoursDestinationType, "out-of-office-hours-destination-type", "", "Type")
	cmd.Flags().StringVar(&bodyRuleName, "rule-name", "", "Rule name")
	cmd.Flags().BoolVar(&bodyTrunkDNHidden, "trunk-dn-hidden", false, "Hidden")
	cmd.Flags().StringVar(&bodyTrunkDNId, "trunk-dn-id", "", "Id")
	cmd.Flags().StringVar(&bodyTrunkDNMemberOf, "trunk-dn-member-of", "", "Member of")
	cmd.Flags().StringVar(&bodyTrunkDNName, "trunk-dn-name", "", "Name")
	cmd.Flags().StringVar(&bodyTrunkDNNumber, "trunk-dn-number", "", "Number")
	cmd.Flags().StringVar(&bodyTrunkDNTags, "trunk-dn-tags", "", "Tags")
	cmd.Flags().StringVar(&bodyTrunkDNType, "trunk-dn-type", "", "Type")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
