// Copyright 2026 Richard Gill and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newReportCallDistributionDownloadGetGetCallDistributionCmd(flags *rootFlags) *cobra.Command {
	var flagPeriodFrom string
	var flagPeriodTo string
	var flagIncludeQueueCalls bool

	cmd := &cobra.Command{
		Use:     "download-get-get-call-distribution <clientTimeZone> <groupNumber> <extensionDns> <waitInterval> <callArea> <groupingType>",
		Aliases: []string{"get"},
		Short:   "Invoke function DownloadGetGetCallDistribution",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  3cx-xapi-pp-cli report-call-distribution download-get-get-call-distribution 2026-01-15T09:00:00Z example-value example-value example-value 42 42 --period-from 2026-01-15T09:00:00Z --period-to 2026-01-15T09:00:00Z --include-queue-calls true",
		Annotations: map[string]string{"pp:endpoint": "report-call-distribution.download-get-get-call-distribution", "pp:method": "GET", "pp:path": "/ReportCallDistribution/Pbx.DownloadGetGetCallDistribution(clientTimeZone={clientTimeZone},periodFrom={periodFrom},periodTo={periodTo},groupNumber={groupNumber},extensionDns={extensionDns},waitInterval={waitInterval},includeQueueCalls={includeQueueCalls},callArea={callArea},groupingType={groupingType})", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("period-from") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "period-from")
			}
			if !cmd.Flags().Changed("period-to") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "period-to")
			}
			if !cmd.Flags().Changed("include-queue-calls") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "include-queue-calls")
			}
			path := "/ReportCallDistribution/Pbx.DownloadGetGetCallDistribution(clientTimeZone={clientTimeZone},periodFrom={periodFrom},periodTo={periodTo},groupNumber={groupNumber},extensionDns={extensionDns},waitInterval={waitInterval},includeQueueCalls={includeQueueCalls},callArea={callArea},groupingType={groupingType})"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("clientTimeZone is required\nUsage: %s <%s>", cmd.CommandPath(), "clientTimeZone"))
			}
			path = replacePathParam(path, "clientTimeZone", args[0])
			if len(args) < 2 || args[1] == "" {
				return usageErr(fmt.Errorf("groupNumber is required\nUsage: %s <%s>", cmd.CommandPath(), "groupNumber"))
			}
			path = replacePathParam(path, "groupNumber", args[1])
			if len(args) < 3 || args[2] == "" {
				return usageErr(fmt.Errorf("extensionDns is required\nUsage: %s <%s>", cmd.CommandPath(), "extensionDns"))
			}
			path = replacePathParam(path, "extensionDns", args[2])
			if len(args) < 4 || args[3] == "" {
				return usageErr(fmt.Errorf("waitInterval is required\nUsage: %s <%s>", cmd.CommandPath(), "waitInterval"))
			}
			path = replacePathParam(path, "waitInterval", args[3])
			if len(args) < 5 || args[4] == "" {
				return usageErr(fmt.Errorf("callArea is required\nUsage: %s <%s>", cmd.CommandPath(), "callArea"))
			}
			path = replacePathParam(path, "callArea", args[4])
			if len(args) < 6 || args[5] == "" {
				return usageErr(fmt.Errorf("groupingType is required\nUsage: %s <%s>", cmd.CommandPath(), "groupingType"))
			}
			path = replacePathParam(path, "groupingType", args[5])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			path = replacePathParam(path, "periodFrom", formatCLIParamValue(flagPeriodFrom))
			path = replacePathParam(path, "periodTo", formatCLIParamValue(flagPeriodTo))
			path = replacePathParam(path, "includeQueueCalls", formatCLIParamValue(flagIncludeQueueCalls))
			params := map[string]string{}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "report-call-distribution", false, path, params, nil, "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagPeriodFrom, "period-from", "", "Usage: periodFrom={periodFrom}")
	cmd.Flags().StringVar(&flagPeriodTo, "period-to", "", "Usage: periodTo={periodTo}")
	cmd.Flags().BoolVar(&flagIncludeQueueCalls, "include-queue-calls", false, "Usage: includeQueueCalls={includeQueueCalls}")

	return cmd
}
