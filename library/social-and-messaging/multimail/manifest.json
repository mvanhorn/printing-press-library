{
  "manifest_version": "0.3",
  "name": "multimail-pp-mcp",
  "display_name": "Multimail",
  "version": "4.0.3",
  "description": "Multimail API surface as MCP tools.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/multimail-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/multimail-pp-mcp",
      "args": [],
      "env": {
        "MULTIMAIL_API_KEY": "${user_config.multimail_api_key}"
      }
    }
  },
  "user_config": {
    "multimail_api_key": {
      "type": "string",
      "title": "MULTIMAIL_API_KEY",
      "description": "Sets MULTIMAIL_API_KEY for the Multimail MCP server.",
      "sensitive": true,
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
