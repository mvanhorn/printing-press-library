{
  "schema_version": 1,
  "applied_at": "2026-05-24",
  "base_run_id": "20260523-201101",
  "base_printing_press_version": "4.14.0",
  "patches": [
    {
      "id": "defer-in-loop-connection-exhaustion",
      "summary": "Replace defer rows.Close() with explicit Close() after iteration in novel commands.",
      "reason": "SQLite MaxOpenConns=2; defer inside a for-mailboxes loop defers to function return, not loop end. Inner queries need a connection from the same pool, causing deadlock when both connections are held by deferred closes.",
      "files": [
        "internal/cli/threads_stale.go",
        "internal/cli/trust_status.go",
        "internal/cli/oversight_velocity.go",
        "internal/cli/inbox_health.go",
        "internal/cli/allowlist_coverage.go"
      ],
      "validated_outcome": "Greptile review 5/5 on PR #806 after fix."
    },
    {
      "id": "trust-status-upgraded-at",
      "summary": "Use upgraded_at timestamp from API response instead of synced_at for last upgrade time.",
      "reason": "synced_at reflects when the record was cached locally, not when the trust upgrade actually occurred. The API returns upgraded_at in the response data.",
      "files": [
        "internal/cli/trust_status.go"
      ]
    },
    {
      "id": "upsert-batch-test-mailboxes-id",
      "summary": "Add mailboxes_id to test items in TestUpsertBatch_SetsMailboxesEmailsParentID.",
      "reason": "The mailboxes_emails typed table has a NOT NULL constraint on mailboxes_id. Test items without it cause the typed upsert to fail, falling through to generic resources only.",
      "files": [
        "internal/store/upsert_batch_test.go"
      ],
      "validated_outcome": "go test ./internal/store/ passes."
    },
    {
      "id": "oversight-velocity-median-bias",
      "summary": "Average two middle elements for even-length latency slices in median calculation.",
      "reason": "Integer division picks the upper-middle element for even-length sorted slices (e.g. [1,2,3,4] returns 3 instead of 2.5), consistently skewing median_latency upward.",
      "files": [
        "internal/cli/oversight_velocity.go"
      ]
    },
    {
      "id": "allowlist-coverage-json-array-recipients",
      "summary": "Parse $.recipients JSON array so multi-recipient sends are matched against allowlist patterns.",
      "reason": "json_extract returns the raw JSON array string for $.recipients. The string never matches any pattern, causing coverage_percent to be systematically under-reported for mailboxes using multi-recipient sends.",
      "files": [
        "internal/cli/allowlist_coverage.go"
      ]
    },
    {
      "id": "oversight-velocity-latency-from-audit-log",
      "summary": "Derive median_latency from audit-log decision events joined with oversight items instead of querying only the oversight table.",
      "reason": "Sync maps oversight to /v1/oversight/pending, so decided items leave that endpoint and the local DB never has status != 'pending'. The original query filtered status != 'pending' → always zero rows → always '—'. The fix joins audit-log approve/reject events (decision timestamp) with oversight items (received_at) via resource_id.",
      "files": [
        "internal/cli/oversight_velocity.go"
      ]
    },
    {
      "id": "oversight-velocity-pending-count-window",
      "summary": "Apply --days cutoff filter to pending_count for consistency with windowed decision counts.",
      "reason": "total_decisions, approved, and rejected are filtered by --days window but pending_count was unwindowed, making the row metrics inconsistent when comparing volumes across the same time window.",
      "files": [
        "internal/cli/oversight_velocity.go"
      ]
    },
    {
      "id": "sync-add-allowlist-dependent",
      "summary": "Add allowlist as a dependent sync resource so allowlist coverage has data after sync --full.",
      "reason": "The allowlist table was defined in the schema but never populated by sync. GET /v1/mailboxes/{mailboxId}/allowlist exists in the API.",
      "files": [
        "internal/cli/sync.go"
      ]
    },
    {
      "id": "threads-stale-derive-from-emails",
      "summary": "Derive thread data from mailboxes_emails parent_id groups when the threads table is empty.",
      "reason": "The API has no list-threads endpoint (only GET single thread by ID), so sync --full cannot populate the threads table. The command now falls back to grouping emails by parent_id to detect conversation threads.",
      "files": [
        "internal/cli/threads_stale.go"
      ]
    },
    {
      "id": "inbox-health-derive-reply-rate-from-emails",
      "summary": "Derive reply_rate from mailboxes_emails outbound emails with parent_id instead of the reply table.",
      "reason": "The reply endpoint is POST-only (no list GET), so sync cannot populate the reply table. Outbound emails with a parent_id are replies to existing conversations.",
      "files": [
        "internal/cli/inbox_health.go"
      ]
    },
    {
      "id": "inbox-health-derive-threads-from-emails",
      "summary": "Derive thread_count and avg_thread_depth from mailboxes_emails parent_id groups instead of the threads table.",
      "reason": "The API has no list-threads endpoint (only GET single thread by ID), so sync --full cannot populate the threads table. Thread metrics are now derived from parent_id grouping with HAVING COUNT(*) >= 2.",
      "files": [
        "internal/cli/inbox_health.go"
      ]
    },
    {
      "id": "trust-status-derive-upgrades-from-audit-log",
      "summary": "Derive upgrade_count and last_upgrade_at from audit-log events instead of the upgrade table.",
      "reason": "The upgrade endpoint is POST-only (no list GET), so sync cannot populate the upgrade table. Audit-log entries with upgrade-related actions provide the same data.",
      "files": [
        "internal/cli/trust_status.go"
      ]
    },
    {
      "id": "allowlist-coverage-derive-sends-from-emails",
      "summary": "Derive recent_sends from mailboxes_emails outbound emails instead of the send table.",
      "reason": "The send endpoint is POST-only (no list GET), so sync cannot populate the send table. Outbound emails from mailboxes_emails provide a richer and syncable data source for coverage analysis.",
      "files": [
        "internal/cli/allowlist_coverage.go"
      ]
    },
    {
      "id": "time-window-use-event-timestamp",
      "summary": "Use event timestamps ($.created_at, $.received_at) instead of synced_at for --days window filtering.",
      "reason": "synced_at is when the record was cached locally. On first-time sync, all historical records land with synced_at ≈ now, making --days 7 include all history. Using the actual event timestamp produces correct windowed results regardless of sync timing.",
      "files": [
        "internal/cli/oversight_velocity.go",
        "internal/cli/allowlist_coverage.go"
      ]
    }
  ]
}
