// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"
)

func newFindCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:    "find",
		Short:  "Online discovery against Naver integrated search (m.search.naver.com). Use 'find posts' for keyword queries, 'find...",
		Hidden: true,
		// Annotated read-only because every subcommand under find is a
		// read-only SERP query; the MCP surface mirrors this annotation.
		Annotations: map[string]string{
			"mcp:read-only": "true",
		},
		RunE: parentNoSubcommandRunE(flags),
	}

	cmd.AddCommand(newFindHashtagCmd(flags))
	cmd.AddCommand(newFindPostsCmd(flags))
	return cmd
}
