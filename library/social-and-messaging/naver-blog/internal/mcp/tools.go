// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/naver-blog/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/naver-blog/internal/client"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/naver-blog/internal/config"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/naver-blog/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/social-and-messaging/naver-blog/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("blogs_feed",
			mcplib.WithDescription("List recent posts from a Naver blog with engagement metrics already populated. Pagination via --page; items array is empty when no more pages. Required: blog_id. Optional: category (default: 0), limit (default: 30), page (default: 1). Returns array of FeedPost."),
			mcplib.WithString("blog_id", mcplib.Required(), mcplib.Description("Blog ID to list (e.g., 'naver_diary')")),
			mcplib.WithNumber("category", mcplib.Description("Category ID filter (0 = all categories; positive int = specific category)")),
			mcplib.WithNumber("limit", mcplib.Description("Max posts per page (Naver caps at ~30)")),
			mcplib.WithNumber("page", mcplib.Description("Page number (1-indexed)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/blogs/{blog_id}/post-list", false, []mcpParamBinding{{PublicName: "blog_id", WireName: "blog_id", Location: "path"}, {PublicName: "category", WireName: "categoryNo", Location: "query"}, {PublicName: "limit", WireName: "itemCount", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{"blog_id"}),
	)
	s.AddTool(
		mcplib.NewTool("find_hashtag",
			mcplib.WithDescription("Search Naver Blog posts by hashtag(s). Use 'tag1,tag2' for multiple; --require-all returns only posts containing all tags. Required: tags. Optional: where (default: m_view), require-all (default: false), month (plus 1 more)."),
			mcplib.WithString("where", mcplib.Description("Naver search vertical")),
			mcplib.WithString("tags", mcplib.Required(), mcplib.Description("Comma-separated hashtag(s) without '#'; the CLI adds '#' to each before query")),
			mcplib.WithBoolean("require-all", mcplib.Description("When set, return only posts that match every tag (intersect results)")),
			mcplib.WithString("month", mcplib.Description("Optional YYYY-MM filter")),
			mcplib.WithNumber("limit", mcplib.Description("Max results per tag")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "https://m.search.naver.com/search.naver", false, []mcpParamBinding{{PublicName: "where", WireName: "where", Location: "query"}, {PublicName: "tags", WireName: "tags", Location: "query"}, {PublicName: "require-all", WireName: "require-all", Location: "query"}, {PublicName: "month", WireName: "month", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("find_posts",
			mcplib.WithDescription("Find Naver Blog posts by keyword (live Naver search). Returns mobile blog URLs, title, and snippet. Pair with --agent --select to narrow the output for AI consumers. Required: query. Optional: where (default: m_view), month, limit (default: 22)."),
			mcplib.WithString("where", mcplib.Description("Naver search vertical (default 'm_view' = mobile integrated view; do not change unless you know the Naver internal...")),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (Korean preferred; UTF-8). URL-encoded automatically.")),
			mcplib.WithString("month", mcplib.Description("Optional YYYY-MM filter to bound results by upload month (applied client-side)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results to return (1-22; one SERP page)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "https://m.search.naver.com/search.naver", false, []mcpParamBinding{{PublicName: "where", WireName: "where", Location: "query"}, {PublicName: "query", WireName: "query", Location: "query"}, {PublicName: "month", WireName: "month", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("posts_get",
			mcplib.WithDescription("Get a single Naver Blog post by blog ID and log number. Extracts title, body, tags from the mobile post page, then queries the reaction API for the like count and PostView.naver for comment count. Output matches what you see in a browser. Required: blog_id, log_no. Returns the Post."),
			mcplib.WithString("blog_id", mcplib.Required(), mcplib.Description("Blog ID (the part after the slash in blog.naver.com/<this>/123456789)")),
			mcplib.WithString("log_no", mcplib.Required(), mcplib.Description("Log/post number (12+ digit integer at the end of the post URL)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/{blog_id}/{log_no}", false, []mcpParamBinding{{PublicName: "blog_id", WireName: "blog_id", Location: "path"}, {PublicName: "log_no", WireName: "log_no", Location: "path"}}, []string{"blog_id", "log_no"}),
	)
	s.AddTool(
		mcplib.NewTool("reactions_get",
			mcplib.WithDescription("Get like/공감 counts for one or more posts. The 'q' parameter is composed as BLOG[blog_id_log_no,blog_id_log_no,...] — the post command builds this automatically; use directly only for batch queries. Required: q. Optional: pool (default: blogid), dedupe (default: false). Returns the ReactionBatch."),
			mcplib.WithString("pool", mcplib.Description("Reaction pool (always 'blogid' for Naver Blog)")),
			mcplib.WithString("q", mcplib.Required(), mcplib.Description("Query composed as BLOG[id_logno,id_logno,...]")),
			mcplib.WithBoolean("dedupe", mcplib.Description("Naver internal: deduplicate contents (false in production)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "https://apis.naver.com/blogserver/like/v1/search/contents", false, []mcpParamBinding{{PublicName: "pool", WireName: "pool", Location: "query"}, {PublicName: "q", WireName: "q", Location: "query"}, {PublicName: "dedupe", WireName: "isDuplication", Location: "query"}}, []string{}),
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, binaryResponse bool, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if binaryResponse {
			headers = map[string]string{client.BinaryResponseHeader: "true"}
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if binaryResponse {
				data, err = c.GetWithHeaders(path, params, headers)
				break
			}
			data, err = c.Get(path, params)
		case "POST":
			if binaryResponse {
				data, _, err = c.PostWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PostWithParams(path, params, bodyArgs)
		case "PUT":
			if binaryResponse {
				data, _, err = c.PutWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(path, params, bodyArgs)
		case "PATCH":
			if binaryResponse {
				data, _, err = c.PatchWithParamsAndHeaders(path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(path, params, bodyArgs)
		case "DELETE":
			if binaryResponse {
				data, _, err = c.DeleteWithParamsAndHeaders(path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + msg +
					"\nhint: check your API credentials." +
					"\n      Run 'naver-blog-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + msg +
					"\nhint: this API is configured without credentials; the service may be blocking the request by rate limit, geography, bot protection, or endpoint policy." +
					"\n      Run 'naver-blog-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "naver-blog-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "naver-blog-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "naver-blog",
		"description": "Search, fetch, and aggregate public Naver Blog posts — title, body, tags, likes (공감), comments — with a...",
		"archetype":   "content",
		"tool_count":  5,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion naver-blog-pp-cli binary.",
		"resources": []map[string]any{
			{
				"name":        "blogs",
				"description": "Per-blog feeds. The feed JSON endpoint returns 30 recent posts per page, each with sympathyCnt (likes), commentCnt,...",
				"endpoints":   []string{"feed"},
				"searchable":  true,
			},
			{
				"name":        "find",
				"description": "Online discovery against Naver integrated search (m.search.naver.com). Use 'find posts' for keyword queries, 'find...",
				"endpoints":   []string{"hashtag", "posts"},
				"searchable":  true,
			},
			{
				"name":        "posts",
				"description": "Public post lookups (single post or batch). Returns title, body, tags, likes (공감), comments. Engagement matches...",
				"endpoints":   []string{"get"},
				"searchable":  true,
			},
			{
				"name":        "reactions",
				"description": "Public reaction (likes/공감) API. Single REST call returns counts for one or many posts. Same integer Naver...",
				"endpoints":   []string{"get"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass page parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Engagement diff over time", "command": "posts-diff", "description": "See exactly which posts gained likes or comments since you last checked, without re-scraping everything.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Sponsored-post detector", "command": "posts --flag-sponsored", "description": "Mechanically detects KFTC-required sponsored-post disclosure phrases in the body — 협찬, 체험단, 광고...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Influencer health snapshot", "command": "blogs-health", "description": "Given a file of blog IDs, returns posts-this-week, median likes, median comments, sponsored ratio, and...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Offline Korean FTS", "command": "query", "description": "Korean-aware FTS5 over every post ever cached — title, body, tags, nickname — ranked by FTS relevance and likes.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Hashtag co-occurrence", "command": "neighbors", "description": "For a target hashtag, returns the top N hashtags that travel with it across the cached corpus — the campaign...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "File-driven URL backfill (order-preserving, mixed-format)", "command": "batch", "description": "Accepts CSV / JSON column / newline-delimited URLs in any mix of m.blog.naver.com / blog.naver.com / PostView.naver...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Category browse", "command": "categories", "description": "Lists a blog's numbered Naver categories with post counts; --category flag filters feed/local-search results by...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Comments extraction", "command": "posts-comments", "description": "Fetch the actual content of comments on a Naver Blog post — author, body, timestamps, like counts — via the...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Blog profile metadata", "command": "blogs-info", "description": "Get rich blog profile data: subscriber count, daily/total visitor counts, Naver Power Blog status, directory...", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Engagement diff over time", "insight": ""},
			{"topic": "Sponsored-post detector", "insight": ""},
			{"topic": "Influencer health snapshot", "insight": ""},
			{"topic": "Offline Korean FTS", "insight": ""},
			{"topic": "Hashtag co-occurrence", "insight": ""},
			{"topic": "File-driven URL backfill (order-preserving, mixed-format)", "insight": ""},
			{"topic": "Category browse", "insight": ""},
			{"topic": "Comments extraction", "insight": ""},
			{"topic": "Blog profile metadata", "insight": ""},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
