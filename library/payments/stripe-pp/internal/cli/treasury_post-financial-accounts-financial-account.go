// Copyright 2026 primiasolutions. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newTreasuryPostFinancialAccountsFinancialAccountCmd(flags *rootFlags) *cobra.Command {
	var bodyExpand string
	var bodyFeatures string
	var bodyForwardingSettings string
	var bodyMetadata string
	var bodyNickname string
	var bodyPlatformRestrictions string

	cmd := &cobra.Command{
		Use:         "post-financial-accounts-financial-account",
		Short:       "Updates the details of a FinancialAccount.",
		Example:     "  stripe-pp-pp-cli treasury post-financial-accounts-financial-account example-value",
		Annotations: map[string]string{"pp:endpoint": "treasury.post-financial-accounts-financial-account", "pp:method": "POST", "pp:path": "/v1/treasury/financial_accounts/{financial_account}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/treasury/financial_accounts/{financial_account}"
			path = replacePathParam(path, "financial_account", args[0])
			fields := url.Values{}
			if bodyExpand != "" {
				if !json.Valid([]byte(bodyExpand)) {
					return fmt.Errorf("parsing --expand JSON: invalid JSON")
				}
				fields.Set("expand", bodyExpand)
			}
			if bodyFeatures != "" {
				if !json.Valid([]byte(bodyFeatures)) {
					return fmt.Errorf("parsing --features JSON: invalid JSON")
				}
				fields.Set("features", bodyFeatures)
			}
			if bodyForwardingSettings != "" {
				if !json.Valid([]byte(bodyForwardingSettings)) {
					return fmt.Errorf("parsing --forwarding-settings JSON: invalid JSON")
				}
				fields.Set("forwarding_settings", bodyForwardingSettings)
			}
			if bodyMetadata != "" {
				if !json.Valid([]byte(bodyMetadata)) {
					return fmt.Errorf("parsing --metadata JSON: invalid JSON")
				}
				fields.Set("metadata", bodyMetadata)
			}
			if bodyNickname != "" {
				fields.Set("nickname", bodyNickname)
			}
			if bodyPlatformRestrictions != "" {
				if !json.Valid([]byte(bodyPlatformRestrictions)) {
					return fmt.Errorf("parsing --platform-restrictions JSON: invalid JSON")
				}
				fields.Set("platform_restrictions", bodyPlatformRestrictions)
			}

			data, statusCode, err := c.PostForm(path, fields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "treasury",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyFeatures, "features", "", "Encodes whether a FinancialAccount has access to a particular feature, with a status enum and associated...")
	cmd.Flags().StringVar(&bodyForwardingSettings, "forwarding-settings", "", "A different bank account where funds can be deposited/debited in order to get the closing FA's balance to $0")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyNickname, "nickname", "", "The nickname for the FinancialAccount.")
	cmd.Flags().StringVar(&bodyPlatformRestrictions, "platform-restrictions", "", "The set of functionalities that the platform can restrict on the FinancialAccount.")

	return cmd
}
