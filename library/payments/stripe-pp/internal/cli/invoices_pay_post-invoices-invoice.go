// Copyright 2026 primiasolutions. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newInvoicesPayPostInvoicesInvoiceCmd(flags *rootFlags) *cobra.Command {
	var bodyExpand string
	var bodyForgive bool
	var bodyMandate string
	var bodyOffSession bool
	var bodyPaidOutOfBand bool
	var bodyPaymentMethod string
	var bodySource string

	cmd := &cobra.Command{
		Use:         "post-invoices-invoice",
		Aliases:     []string{"create"},
		Short:       "Stripe automatically creates and then attempts to collect payment on invoices for customers on subscriptions...",
		Example:     "  stripe-pp-pp-cli invoices pay post-invoices-invoice example-value",
		Annotations: map[string]string{"pp:endpoint": "pay.post-invoices-invoice", "pp:method": "POST", "pp:path": "/v1/invoices/{invoice}/pay"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/invoices/{invoice}/pay"
			path = replacePathParam(path, "invoice", args[0])
			fields := url.Values{}
			if bodyExpand != "" {
				if !json.Valid([]byte(bodyExpand)) {
					return fmt.Errorf("parsing --expand JSON: invalid JSON")
				}
				fields.Set("expand", bodyExpand)
			}
			if bodyForgive != false {
				fields.Set("forgive", fmt.Sprintf("%v", bodyForgive))
			}
			if bodyMandate != "" {
				fields.Set("mandate", bodyMandate)
			}
			if bodyOffSession != false {
				fields.Set("off_session", fmt.Sprintf("%v", bodyOffSession))
			}
			if bodyPaidOutOfBand != false {
				fields.Set("paid_out_of_band", fmt.Sprintf("%v", bodyPaidOutOfBand))
			}
			if bodyPaymentMethod != "" {
				fields.Set("payment_method", bodyPaymentMethod)
			}
			if bodySource != "" {
				fields.Set("source", bodySource)
			}

			data, statusCode, err := c.PostForm(path, fields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "pay",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().BoolVar(&bodyForgive, "forgive", false, "In cases where the source used to pay the invoice has insufficient funds, passing `forgive=true` controls whether a...")
	cmd.Flags().StringVar(&bodyMandate, "mandate", "", "ID of the mandate to be used for this invoice. It must correspond to the payment method used to pay the invoice,...")
	cmd.Flags().BoolVar(&bodyOffSession, "off-session", false, "Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `true` (off-session).")
	cmd.Flags().BoolVar(&bodyPaidOutOfBand, "paid-out-of-band", false, "Boolean representing whether an invoice is paid outside of Stripe. This will result in no charge being made....")
	cmd.Flags().StringVar(&bodyPaymentMethod, "payment-method", "", "A PaymentMethod to be charged. The PaymentMethod must be the ID of a PaymentMethod belonging to the customer...")
	cmd.Flags().StringVar(&bodySource, "source", "", "A payment source to be charged. The source must be the ID of a source belonging to the customer associated with the...")

	return cmd
}
