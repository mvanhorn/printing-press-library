// Copyright 2026 primiasolutions. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newIssuingPostCardholdersCardholderCmd(flags *rootFlags) *cobra.Command {
	var bodyBilling string
	var bodyCompany string
	var bodyEmail string
	var bodyExpand string
	var bodyIndividual string
	var bodyMetadata string
	var bodyPhoneNumber string
	var bodyPreferredLocales string
	var bodySpendingControls string
	var bodyStatus string

	cmd := &cobra.Command{
		Use:         "post-cardholders-cardholder",
		Short:       "Updates the specified Issuing Cardholder object by setting the values of the parameters passed. Any...",
		Example:     "  stripe-pp-pp-cli issuing post-cardholders-cardholder example-value",
		Annotations: map[string]string{"pp:endpoint": "issuing.post-cardholders-cardholder", "pp:method": "POST", "pp:path": "/v1/issuing/cardholders/{cardholder}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/issuing/cardholders/{cardholder}"
			path = replacePathParam(path, "cardholder", args[0])
			fields := url.Values{}
			if bodyBilling != "" {
				if !json.Valid([]byte(bodyBilling)) {
					return fmt.Errorf("parsing --billing JSON: invalid JSON")
				}
				fields.Set("billing", bodyBilling)
			}
			if bodyCompany != "" {
				if !json.Valid([]byte(bodyCompany)) {
					return fmt.Errorf("parsing --company JSON: invalid JSON")
				}
				fields.Set("company", bodyCompany)
			}
			if bodyEmail != "" {
				fields.Set("email", bodyEmail)
			}
			if bodyExpand != "" {
				if !json.Valid([]byte(bodyExpand)) {
					return fmt.Errorf("parsing --expand JSON: invalid JSON")
				}
				fields.Set("expand", bodyExpand)
			}
			if bodyIndividual != "" {
				if !json.Valid([]byte(bodyIndividual)) {
					return fmt.Errorf("parsing --individual JSON: invalid JSON")
				}
				fields.Set("individual", bodyIndividual)
			}
			if bodyMetadata != "" {
				if !json.Valid([]byte(bodyMetadata)) {
					return fmt.Errorf("parsing --metadata JSON: invalid JSON")
				}
				fields.Set("metadata", bodyMetadata)
			}
			if bodyPhoneNumber != "" {
				fields.Set("phone_number", bodyPhoneNumber)
			}
			if bodyPreferredLocales != "" {
				if !json.Valid([]byte(bodyPreferredLocales)) {
					return fmt.Errorf("parsing --preferred-locales JSON: invalid JSON")
				}
				fields.Set("preferred_locales", bodyPreferredLocales)
			}
			if bodySpendingControls != "" {
				if !json.Valid([]byte(bodySpendingControls)) {
					return fmt.Errorf("parsing --spending-controls JSON: invalid JSON")
				}
				fields.Set("spending_controls", bodySpendingControls)
			}
			if bodyStatus != "" {
				fields.Set("status", bodyStatus)
			}

			data, statusCode, err := c.PostForm(path, fields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "issuing",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyBilling, "billing", "", "The cardholder's billing address.")
	cmd.Flags().StringVar(&bodyCompany, "company", "", "Additional information about a `company` cardholder.")
	cmd.Flags().StringVar(&bodyEmail, "email", "", "The cardholder's email address.")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyIndividual, "individual", "", "Additional information about an `individual` cardholder.")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyPhoneNumber, "phone-number", "", "The cardholder's phone number. This is required for all cardholders who will be creating EU cards. See the [3D...")
	cmd.Flags().StringVar(&bodyPreferredLocales, "preferred-locales", "", "The cardholder’s preferred locales (languages), ordered by preference. Locales can be `da`, `de`, `en`, `es`,...")
	cmd.Flags().StringVar(&bodySpendingControls, "spending-controls", "", "Rules that control spending across this cardholder's cards. Refer to our...")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Specifies whether to permit authorizations on this cardholder's cards.")

	return cmd
}
