// Copyright 2026 primiasolutions. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newTestHelpersPostIssuingSettlementsCmd(flags *rootFlags) *cobra.Command {
	var bodyBin string
	var bodyClearingDate int
	var bodyCurrency string
	var bodyExpand string
	var bodyInterchangeFeesAmount int
	var bodyNetTotalAmount int
	var bodyNetwork string
	var bodyNetworkSettlementIdentifier string
	var bodyTransactionAmount int
	var bodyTransactionCount int

	cmd := &cobra.Command{
		Use:         "post-issuing-settlements",
		Short:       "Allows the user to create an Issuing settlement.",
		Example:     "  stripe-pp-pp-cli test-helpers post-issuing-settlements --bin example-value",
		Annotations: map[string]string{"pp:endpoint": "test-helpers.post-issuing-settlements", "pp:method": "POST", "pp:path": "/v1/test_helpers/issuing/settlements"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("bin") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "bin")
			}
			if !cmd.Flags().Changed("clearing-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "clearing-date")
			}
			if !cmd.Flags().Changed("currency") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "currency")
			}
			if !cmd.Flags().Changed("net-total-amount") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "net-total-amount")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/test_helpers/issuing/settlements"
			fields := url.Values{}
			if bodyBin != "" {
				fields.Set("bin", bodyBin)
			}
			if bodyClearingDate != 0 {
				fields.Set("clearing_date", fmt.Sprintf("%v", bodyClearingDate))
			}
			if bodyCurrency != "" {
				fields.Set("currency", bodyCurrency)
			}
			if bodyExpand != "" {
				if !json.Valid([]byte(bodyExpand)) {
					return fmt.Errorf("parsing --expand JSON: invalid JSON")
				}
				fields.Set("expand", bodyExpand)
			}
			if bodyInterchangeFeesAmount != 0 {
				fields.Set("interchange_fees_amount", fmt.Sprintf("%v", bodyInterchangeFeesAmount))
			}
			if bodyNetTotalAmount != 0 {
				fields.Set("net_total_amount", fmt.Sprintf("%v", bodyNetTotalAmount))
			}
			if bodyNetwork != "" {
				fields.Set("network", bodyNetwork)
			}
			if bodyNetworkSettlementIdentifier != "" {
				fields.Set("network_settlement_identifier", bodyNetworkSettlementIdentifier)
			}
			if bodyTransactionAmount != 0 {
				fields.Set("transaction_amount", fmt.Sprintf("%v", bodyTransactionAmount))
			}
			if bodyTransactionCount != 0 {
				fields.Set("transaction_count", fmt.Sprintf("%v", bodyTransactionCount))
			}

			data, statusCode, err := c.PostForm(path, fields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "test-helpers",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyBin, "bin", "", "The Bank Identification Number reflecting this settlement record.")
	cmd.Flags().IntVar(&bodyClearingDate, "clearing-date", 0, "The date that the transactions are cleared and posted to user's accounts.")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a...")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().IntVar(&bodyInterchangeFeesAmount, "interchange-fees-amount", 0, "The total interchange received as reimbursement for the transactions.")
	cmd.Flags().IntVar(&bodyNetTotalAmount, "net-total-amount", 0, "The total net amount required to settle with the network.")
	cmd.Flags().StringVar(&bodyNetwork, "network", "", "The card network for this settlement. One of ['visa', 'maestro']")
	cmd.Flags().StringVar(&bodyNetworkSettlementIdentifier, "network-settlement-identifier", "", "The Settlement Identification Number assigned by the network.")
	cmd.Flags().IntVar(&bodyTransactionAmount, "transaction-amount", 0, "The total transaction amount reflected in this settlement.")
	cmd.Flags().IntVar(&bodyTransactionCount, "transaction-count", 0, "The total number of transactions reflected in this settlement.")

	return cmd
}
