// Copyright 2026 primiasolutions. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newTokensPostCmd(flags *rootFlags) *cobra.Command {
	var bodyAccount string
	var bodyBankAccount string
	var bodyCard string
	var bodyCustomer string
	var bodyCvcUpdate string
	var bodyExpand string
	var bodyPerson string
	var bodyPii string

	cmd := &cobra.Command{
		Use:         "post",
		Aliases:     []string{"create"},
		Short:       "Creates a single-use token that represents a bank account’s details. You can use this token with any v1 API...",
		Example:     "  stripe-pp-pp-cli tokens post",
		Annotations: map[string]string{"pp:endpoint": "tokens.post", "pp:method": "POST", "pp:path": "/v1/tokens"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/tokens"
			fields := url.Values{}
			if bodyAccount != "" {
				if !json.Valid([]byte(bodyAccount)) {
					return fmt.Errorf("parsing --account JSON: invalid JSON")
				}
				fields.Set("account", bodyAccount)
			}
			if bodyBankAccount != "" {
				if !json.Valid([]byte(bodyBankAccount)) {
					return fmt.Errorf("parsing --bank-account JSON: invalid JSON")
				}
				fields.Set("bank_account", bodyBankAccount)
			}
			if bodyCard != "" {
				fields.Set("card", bodyCard)
			}
			if bodyCustomer != "" {
				fields.Set("customer", bodyCustomer)
			}
			if bodyCvcUpdate != "" {
				if !json.Valid([]byte(bodyCvcUpdate)) {
					return fmt.Errorf("parsing --cvc-update JSON: invalid JSON")
				}
				fields.Set("cvc_update", bodyCvcUpdate)
			}
			if bodyExpand != "" {
				if !json.Valid([]byte(bodyExpand)) {
					return fmt.Errorf("parsing --expand JSON: invalid JSON")
				}
				fields.Set("expand", bodyExpand)
			}
			if bodyPerson != "" {
				if !json.Valid([]byte(bodyPerson)) {
					return fmt.Errorf("parsing --person JSON: invalid JSON")
				}
				fields.Set("person", bodyPerson)
			}
			if bodyPii != "" {
				if !json.Valid([]byte(bodyPii)) {
					return fmt.Errorf("parsing --pii JSON: invalid JSON")
				}
				fields.Set("pii", bodyPii)
			}

			data, statusCode, err := c.PostForm(path, fields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "tokens",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAccount, "account", "", "Information for the account this token represents.")
	cmd.Flags().StringVar(&bodyBankAccount, "bank-account", "", "The bank account this token will represent.")
	cmd.Flags().StringVar(&bodyCard, "card", "", "The card this token will represent. If you also pass in a customer, the card must be the ID of a card belonging to...")
	cmd.Flags().StringVar(&bodyCustomer, "customer", "", "Create a token for the customer, which is owned by the application's account. You can only use this with an [OAuth...")
	cmd.Flags().StringVar(&bodyCvcUpdate, "cvc-update", "", "The updated CVC value this token represents.")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyPerson, "person", "", "Information for the person this token represents.")
	cmd.Flags().StringVar(&bodyPii, "pii", "", "The PII this token represents.")

	return cmd
}
