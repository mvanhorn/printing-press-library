// Copyright 2026 primiasolutions. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newSetupIntentsPostCmd(flags *rootFlags) *cobra.Command {
	var bodyAttachToSelf bool
	var bodyAutomaticPaymentMethods string
	var bodyConfirm bool
	var bodyConfirmationToken string
	var bodyCustomer string
	var bodyCustomerAccount string
	var bodyDescription string
	var bodyExcludedPaymentMethodTypes string
	var bodyExpand string
	var bodyFlowDirections string
	var bodyMandateData string
	var bodyMetadata string
	var bodyOnBehalfOf string
	var bodyPaymentMethod string
	var bodyPaymentMethodConfiguration string
	var bodyPaymentMethodData string
	var bodyPaymentMethodOptions string
	var bodyPaymentMethodTypes string
	var bodyReturnUrl string
	var bodySingleUse string
	var bodyUsage string
	var bodyUseStripeSdk bool

	cmd := &cobra.Command{
		Use:         "post",
		Aliases:     []string{"create"},
		Short:       "Creates a SetupIntent object. After you create the SetupIntent, attach a payment method and...",
		Example:     "  stripe-pp-pp-cli setup-intents post",
		Annotations: map[string]string{"pp:endpoint": "setup-intents.post", "pp:method": "POST", "pp:path": "/v1/setup_intents"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/setup_intents"
			fields := url.Values{}
			if bodyAttachToSelf != false {
				fields.Set("attach_to_self", fmt.Sprintf("%v", bodyAttachToSelf))
			}
			if bodyAutomaticPaymentMethods != "" {
				if !json.Valid([]byte(bodyAutomaticPaymentMethods)) {
					return fmt.Errorf("parsing --automatic-payment-methods JSON: invalid JSON")
				}
				fields.Set("automatic_payment_methods", bodyAutomaticPaymentMethods)
			}
			if bodyConfirm != false {
				fields.Set("confirm", fmt.Sprintf("%v", bodyConfirm))
			}
			if bodyConfirmationToken != "" {
				fields.Set("confirmation_token", bodyConfirmationToken)
			}
			if bodyCustomer != "" {
				fields.Set("customer", bodyCustomer)
			}
			if bodyCustomerAccount != "" {
				fields.Set("customer_account", bodyCustomerAccount)
			}
			if bodyDescription != "" {
				fields.Set("description", bodyDescription)
			}
			if bodyExcludedPaymentMethodTypes != "" {
				if !json.Valid([]byte(bodyExcludedPaymentMethodTypes)) {
					return fmt.Errorf("parsing --excluded-payment-method-types JSON: invalid JSON")
				}
				fields.Set("excluded_payment_method_types", bodyExcludedPaymentMethodTypes)
			}
			if bodyExpand != "" {
				if !json.Valid([]byte(bodyExpand)) {
					return fmt.Errorf("parsing --expand JSON: invalid JSON")
				}
				fields.Set("expand", bodyExpand)
			}
			if bodyFlowDirections != "" {
				if !json.Valid([]byte(bodyFlowDirections)) {
					return fmt.Errorf("parsing --flow-directions JSON: invalid JSON")
				}
				fields.Set("flow_directions", bodyFlowDirections)
			}
			if bodyMandateData != "" {
				fields.Set("mandate_data", bodyMandateData)
			}
			if bodyMetadata != "" {
				if !json.Valid([]byte(bodyMetadata)) {
					return fmt.Errorf("parsing --metadata JSON: invalid JSON")
				}
				fields.Set("metadata", bodyMetadata)
			}
			if bodyOnBehalfOf != "" {
				fields.Set("on_behalf_of", bodyOnBehalfOf)
			}
			if bodyPaymentMethod != "" {
				fields.Set("payment_method", bodyPaymentMethod)
			}
			if bodyPaymentMethodConfiguration != "" {
				fields.Set("payment_method_configuration", bodyPaymentMethodConfiguration)
			}
			if bodyPaymentMethodData != "" {
				if !json.Valid([]byte(bodyPaymentMethodData)) {
					return fmt.Errorf("parsing --payment-method-data JSON: invalid JSON")
				}
				fields.Set("payment_method_data", bodyPaymentMethodData)
			}
			if bodyPaymentMethodOptions != "" {
				if !json.Valid([]byte(bodyPaymentMethodOptions)) {
					return fmt.Errorf("parsing --payment-method-options JSON: invalid JSON")
				}
				fields.Set("payment_method_options", bodyPaymentMethodOptions)
			}
			if bodyPaymentMethodTypes != "" {
				if !json.Valid([]byte(bodyPaymentMethodTypes)) {
					return fmt.Errorf("parsing --payment-method-types JSON: invalid JSON")
				}
				fields.Set("payment_method_types", bodyPaymentMethodTypes)
			}
			if bodyReturnUrl != "" {
				fields.Set("return_url", bodyReturnUrl)
			}
			if bodySingleUse != "" {
				if !json.Valid([]byte(bodySingleUse)) {
					return fmt.Errorf("parsing --single-use JSON: invalid JSON")
				}
				fields.Set("single_use", bodySingleUse)
			}
			if bodyUsage != "" {
				fields.Set("usage", bodyUsage)
			}
			if bodyUseStripeSdk != false {
				fields.Set("use_stripe_sdk", fmt.Sprintf("%v", bodyUseStripeSdk))
			}

			data, statusCode, err := c.PostForm(path, fields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "setup-intents",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAttachToSelf, "attach-to-self", false, "If present, the SetupIntent's payment method will be attached to the in-context Stripe Account. It can only be used...")
	cmd.Flags().StringVar(&bodyAutomaticPaymentMethods, "automatic-payment-methods", "", "When you enable this parameter, this SetupIntent accepts payment methods that you enable in the Dashboard and that...")
	cmd.Flags().BoolVar(&bodyConfirm, "confirm", false, "Set to `true` to attempt to confirm this SetupIntent immediately. This parameter defaults to `false`. If a card is...")
	cmd.Flags().StringVar(&bodyConfirmationToken, "confirmation-token", "", "ID of the ConfirmationToken used to confirm this SetupIntent. If the provided ConfirmationToken contains properties...")
	cmd.Flags().StringVar(&bodyCustomer, "customer", "", "ID of the Customer this SetupIntent belongs to, if one exists. If present, the SetupIntent's payment method will be...")
	cmd.Flags().StringVar(&bodyCustomerAccount, "customer-account", "", "ID of the Account this SetupIntent belongs to, if one exists. If present, the SetupIntent's payment method will be...")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "An arbitrary string attached to the object. Often useful for displaying to users.")
	cmd.Flags().StringVar(&bodyExcludedPaymentMethodTypes, "excluded-payment-method-types", "", "The list of payment method types to exclude from use with this SetupIntent.")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyFlowDirections, "flow-directions", "", "Indicates the directions of money movement for which this payment method is intended to be used. Include `inbound`...")
	cmd.Flags().StringVar(&bodyMandateData, "mandate-data", "", "This hash contains details about the mandate to create. This parameter can only be used with...")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyOnBehalfOf, "on-behalf-of", "", "The Stripe account ID created for this SetupIntent.")
	cmd.Flags().StringVar(&bodyPaymentMethod, "payment-method", "", "ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent.")
	cmd.Flags().StringVar(&bodyPaymentMethodConfiguration, "payment-method-configuration", "", "The ID of the [payment method configuration](https://docs.stripe.com/api/payment_method_configurations) to use with...")
	cmd.Flags().StringVar(&bodyPaymentMethodData, "payment-method-data", "", "When included, this hash creates a PaymentMethod that is set as the...")
	cmd.Flags().StringVar(&bodyPaymentMethodOptions, "payment-method-options", "", "Payment method-specific configuration for this SetupIntent.")
	cmd.Flags().StringVar(&bodyPaymentMethodTypes, "payment-method-types", "", "The list of payment method types (for example, card) that this SetupIntent can use. If you don't provide this,...")
	cmd.Flags().StringVar(&bodyReturnUrl, "return-url", "", "The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's...")
	cmd.Flags().StringVar(&bodySingleUse, "single-use", "", "If you populate this hash, this SetupIntent generates a `single_use` mandate after successful completion. Single-use...")
	cmd.Flags().StringVar(&bodyUsage, "usage", "", "Indicates how the payment method is intended to be used in the future. If not provided, this value defaults to...")
	cmd.Flags().BoolVar(&bodyUseStripeSdk, "use-stripe-sdk", false, "Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next...")

	return cmd
}
