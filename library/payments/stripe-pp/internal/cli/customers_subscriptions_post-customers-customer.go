// Copyright 2026 primiasolutions. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newCustomersSubscriptionsPostCustomersCustomerCmd(flags *rootFlags) *cobra.Command {
	var bodyAddInvoiceItems string
	var bodyApplicationFeePercent string
	var bodyAutomaticTax string
	var bodyBackdateStartDate int
	var bodyBillingCycleAnchor int
	var bodyBillingThresholds string
	var bodyCancelAt string
	var bodyCancelAtPeriodEnd bool
	var bodyCollectionMethod string
	var bodyCurrency string
	var bodyDaysUntilDue int
	var bodyDefaultPaymentMethod string
	var bodyDefaultSource string
	var bodyDefaultTaxRates string
	var bodyDiscounts string
	var bodyExpand string
	var bodyInvoiceSettings string
	var bodyItems string
	var bodyMetadata string
	var bodyOffSession bool
	var bodyPaymentBehavior string
	var bodyPaymentSettings string
	var bodyPendingInvoiceItemInterval string
	var bodyProrationBehavior string
	var bodyTransferData string
	var bodyTrialEnd string
	var bodyTrialFromPlan bool
	var bodyTrialPeriodDays int
	var bodyTrialSettings string

	cmd := &cobra.Command{
		Use:         "post-customers-customer",
		Aliases:     []string{"create"},
		Short:       "Creates a new subscription on an existing customer.",
		Example:     "  stripe-pp-pp-cli customers subscriptions post-customers-customer example-value",
		Annotations: map[string]string{"pp:endpoint": "subscriptions.post-customers-customer", "pp:method": "POST", "pp:path": "/v1/customers/{customer}/subscriptions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/customers/{customer}/subscriptions"
			path = replacePathParam(path, "customer", args[0])
			fields := url.Values{}
			if bodyAddInvoiceItems != "" {
				if !json.Valid([]byte(bodyAddInvoiceItems)) {
					return fmt.Errorf("parsing --add-invoice-items JSON: invalid JSON")
				}
				fields.Set("add_invoice_items", bodyAddInvoiceItems)
			}
			if bodyApplicationFeePercent != "" {
				fields.Set("application_fee_percent", bodyApplicationFeePercent)
			}
			if bodyAutomaticTax != "" {
				if !json.Valid([]byte(bodyAutomaticTax)) {
					return fmt.Errorf("parsing --automatic-tax JSON: invalid JSON")
				}
				fields.Set("automatic_tax", bodyAutomaticTax)
			}
			if bodyBackdateStartDate != 0 {
				fields.Set("backdate_start_date", fmt.Sprintf("%v", bodyBackdateStartDate))
			}
			if bodyBillingCycleAnchor != 0 {
				fields.Set("billing_cycle_anchor", fmt.Sprintf("%v", bodyBillingCycleAnchor))
			}
			if bodyBillingThresholds != "" {
				fields.Set("billing_thresholds", bodyBillingThresholds)
			}
			if bodyCancelAt != "" {
				fields.Set("cancel_at", bodyCancelAt)
			}
			if bodyCancelAtPeriodEnd != false {
				fields.Set("cancel_at_period_end", fmt.Sprintf("%v", bodyCancelAtPeriodEnd))
			}
			if bodyCollectionMethod != "" {
				fields.Set("collection_method", bodyCollectionMethod)
			}
			if bodyCurrency != "" {
				fields.Set("currency", bodyCurrency)
			}
			if bodyDaysUntilDue != 0 {
				fields.Set("days_until_due", fmt.Sprintf("%v", bodyDaysUntilDue))
			}
			if bodyDefaultPaymentMethod != "" {
				fields.Set("default_payment_method", bodyDefaultPaymentMethod)
			}
			if bodyDefaultSource != "" {
				fields.Set("default_source", bodyDefaultSource)
			}
			if bodyDefaultTaxRates != "" {
				fields.Set("default_tax_rates", bodyDefaultTaxRates)
			}
			if bodyDiscounts != "" {
				fields.Set("discounts", bodyDiscounts)
			}
			if bodyExpand != "" {
				if !json.Valid([]byte(bodyExpand)) {
					return fmt.Errorf("parsing --expand JSON: invalid JSON")
				}
				fields.Set("expand", bodyExpand)
			}
			if bodyInvoiceSettings != "" {
				if !json.Valid([]byte(bodyInvoiceSettings)) {
					return fmt.Errorf("parsing --invoice-settings JSON: invalid JSON")
				}
				fields.Set("invoice_settings", bodyInvoiceSettings)
			}
			if bodyItems != "" {
				if !json.Valid([]byte(bodyItems)) {
					return fmt.Errorf("parsing --items JSON: invalid JSON")
				}
				fields.Set("items", bodyItems)
			}
			if bodyMetadata != "" {
				fields.Set("metadata", bodyMetadata)
			}
			if bodyOffSession != false {
				fields.Set("off_session", fmt.Sprintf("%v", bodyOffSession))
			}
			if bodyPaymentBehavior != "" {
				fields.Set("payment_behavior", bodyPaymentBehavior)
			}
			if bodyPaymentSettings != "" {
				if !json.Valid([]byte(bodyPaymentSettings)) {
					return fmt.Errorf("parsing --payment-settings JSON: invalid JSON")
				}
				fields.Set("payment_settings", bodyPaymentSettings)
			}
			if bodyPendingInvoiceItemInterval != "" {
				fields.Set("pending_invoice_item_interval", bodyPendingInvoiceItemInterval)
			}
			if bodyProrationBehavior != "" {
				fields.Set("proration_behavior", bodyProrationBehavior)
			}
			if bodyTransferData != "" {
				if !json.Valid([]byte(bodyTransferData)) {
					return fmt.Errorf("parsing --transfer-data JSON: invalid JSON")
				}
				fields.Set("transfer_data", bodyTransferData)
			}
			if bodyTrialEnd != "" {
				fields.Set("trial_end", bodyTrialEnd)
			}
			if bodyTrialFromPlan != false {
				fields.Set("trial_from_plan", fmt.Sprintf("%v", bodyTrialFromPlan))
			}
			if bodyTrialPeriodDays != 0 {
				fields.Set("trial_period_days", fmt.Sprintf("%v", bodyTrialPeriodDays))
			}
			if bodyTrialSettings != "" {
				if !json.Valid([]byte(bodyTrialSettings)) {
					return fmt.Errorf("parsing --trial-settings JSON: invalid JSON")
				}
				fields.Set("trial_settings", bodyTrialSettings)
			}

			data, statusCode, err := c.PostForm(path, fields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "subscriptions",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAddInvoiceItems, "add-invoice-items", "", "A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription....")
	cmd.Flags().StringVar(&bodyApplicationFeePercent, "application-fee-percent", "", "A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the...")
	cmd.Flags().StringVar(&bodyAutomaticTax, "automatic-tax", "", "Automatic tax settings for this subscription.")
	cmd.Flags().IntVar(&bodyBackdateStartDate, "backdate-start-date", 0, "A past timestamp to backdate the subscription's start date to. If set, the first invoice will contain line items for...")
	cmd.Flags().IntVar(&bodyBillingCycleAnchor, "billing-cycle-anchor", 0, "A future timestamp in UTC format to anchor the subscription's [billing...")
	cmd.Flags().StringVar(&bodyBillingThresholds, "billing-thresholds", "", "Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period. When...")
	cmd.Flags().StringVar(&bodyCancelAt, "cancel-at", "", "A timestamp at which the subscription should cancel. If set to a date before the current period ends, this will...")
	cmd.Flags().BoolVar(&bodyCancelAtPeriodEnd, "cancel-at-period-end", false, "Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults...")
	cmd.Flags().StringVar(&bodyCollectionMethod, "collection-method", "", "Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this...")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a...")
	cmd.Flags().IntVar(&bodyDaysUntilDue, "days-until-due", 0, "Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where...")
	cmd.Flags().StringVar(&bodyDefaultPaymentMethod, "default-payment-method", "", "ID of the default payment method for the subscription. It must belong to the customer associated with the...")
	cmd.Flags().StringVar(&bodyDefaultSource, "default-source", "", "ID of the default payment source for the subscription. It must belong to the customer associated with the...")
	cmd.Flags().StringVar(&bodyDefaultTaxRates, "default-tax-rates", "", "The tax rates that will apply to any subscription item that does not have `tax_rates` set. Invoices created will...")
	cmd.Flags().StringVar(&bodyDiscounts, "discounts", "", "The coupons to redeem into discounts for the subscription. If not specified or empty, inherits the discount from the...")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyInvoiceSettings, "invoice-settings", "", "All invoices will be billed using the specified settings.")
	cmd.Flags().StringVar(&bodyItems, "items", "", "A list of up to 20 subscription items, each with an attached price.")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().BoolVar(&bodyOffSession, "off-session", false, "Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).")
	cmd.Flags().StringVar(&bodyPaymentBehavior, "payment-behavior", "", "Only applies to subscriptions with `collection_method=charge_automatically`. Use `allow_incomplete` to create...")
	cmd.Flags().StringVar(&bodyPaymentSettings, "payment-settings", "", "Payment settings to pass to invoices created by the subscription.")
	cmd.Flags().StringVar(&bodyPendingInvoiceItemInterval, "pending-invoice-item-interval", "", "Specifies an interval for how often to bill for any pending invoice items. It is analogous to calling [Create an...")
	cmd.Flags().StringVar(&bodyProrationBehavior, "proration-behavior", "", "Determines how to handle [prorations](https://docs.stripe.com/billing/subscriptions/prorations) resulting from the...")
	cmd.Flags().StringVar(&bodyTransferData, "transfer-data", "", "If specified, the funds from the subscription's invoices will be transferred to the destination and the ID of the...")
	cmd.Flags().StringVar(&bodyTrialEnd, "trial-end", "", "Unix timestamp representing the end of the trial period the customer will get before being charged for the first...")
	cmd.Flags().BoolVar(&bodyTrialFromPlan, "trial-from-plan", false, "Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per...")
	cmd.Flags().IntVar(&bodyTrialPeriodDays, "trial-period-days", 0, "Integer representing the number of trial period days before the customer is charged for the first time. This will...")
	cmd.Flags().StringVar(&bodyTrialSettings, "trial-settings", "", "Settings related to subscription trials.")

	return cmd
}
