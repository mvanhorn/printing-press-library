// Copyright 2026 primiasolutions. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newIdentityPostVerificationSessionsCmd(flags *rootFlags) *cobra.Command {
	var bodyClientReferenceId string
	var bodyExpand string
	var bodyMetadata string
	var bodyOptions string
	var bodyProvidedDetails string
	var bodyRelatedCustomer string
	var bodyRelatedCustomerAccount string
	var bodyRelatedPerson string
	var bodyReturnUrl string
	var bodyType string
	var bodyVerificationFlow string

	cmd := &cobra.Command{
		Use:         "post-verification-sessions",
		Aliases:     []string{"create"},
		Short:       "Creates a VerificationSession object. After the VerificationSession is created, display a verification...",
		Example:     "  stripe-pp-pp-cli identity post-verification-sessions",
		Annotations: map[string]string{"pp:endpoint": "identity.post-verification-sessions", "pp:method": "POST", "pp:path": "/v1/identity/verification_sessions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/identity/verification_sessions"
			fields := url.Values{}
			if bodyClientReferenceId != "" {
				fields.Set("client_reference_id", bodyClientReferenceId)
			}
			if bodyExpand != "" {
				if !json.Valid([]byte(bodyExpand)) {
					return fmt.Errorf("parsing --expand JSON: invalid JSON")
				}
				fields.Set("expand", bodyExpand)
			}
			if bodyMetadata != "" {
				if !json.Valid([]byte(bodyMetadata)) {
					return fmt.Errorf("parsing --metadata JSON: invalid JSON")
				}
				fields.Set("metadata", bodyMetadata)
			}
			if bodyOptions != "" {
				if !json.Valid([]byte(bodyOptions)) {
					return fmt.Errorf("parsing --options JSON: invalid JSON")
				}
				fields.Set("options", bodyOptions)
			}
			if bodyProvidedDetails != "" {
				if !json.Valid([]byte(bodyProvidedDetails)) {
					return fmt.Errorf("parsing --provided-details JSON: invalid JSON")
				}
				fields.Set("provided_details", bodyProvidedDetails)
			}
			if bodyRelatedCustomer != "" {
				fields.Set("related_customer", bodyRelatedCustomer)
			}
			if bodyRelatedCustomerAccount != "" {
				fields.Set("related_customer_account", bodyRelatedCustomerAccount)
			}
			if bodyRelatedPerson != "" {
				if !json.Valid([]byte(bodyRelatedPerson)) {
					return fmt.Errorf("parsing --related-person JSON: invalid JSON")
				}
				fields.Set("related_person", bodyRelatedPerson)
			}
			if bodyReturnUrl != "" {
				fields.Set("return_url", bodyReturnUrl)
			}
			if bodyType != "" {
				fields.Set("type", bodyType)
			}
			if bodyVerificationFlow != "" {
				fields.Set("verification_flow", bodyVerificationFlow)
			}

			data, statusCode, err := c.PostForm(path, fields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "identity",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyClientReferenceId, "client-reference-id", "", "A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile...")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyOptions, "options", "", "A set of options for the session’s verification checks.")
	cmd.Flags().StringVar(&bodyProvidedDetails, "provided-details", "", "Details provided about the user being verified. These details might be shown to the user.")
	cmd.Flags().StringVar(&bodyRelatedCustomer, "related-customer", "", "Customer ID")
	cmd.Flags().StringVar(&bodyRelatedCustomerAccount, "related-customer-account", "", "The ID of the Account representing a customer.")
	cmd.Flags().StringVar(&bodyRelatedPerson, "related-person", "", "Tokens referencing a Person resource and its associated account.")
	cmd.Flags().StringVar(&bodyReturnUrl, "return-url", "", "The URL that the user will be redirected to upon completing the verification flow.")
	cmd.Flags().StringVar(&bodyType, "type", "", "The type of [verification check](https://docs.stripe.com/identity/verification-checks) to be performed. You must...")
	cmd.Flags().StringVar(&bodyVerificationFlow, "verification-flow", "", "The ID of a verification flow from the Dashboard. See https://docs.stripe.com/identity/verification-flows.")

	return cmd
}
