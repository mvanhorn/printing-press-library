// Copyright 2026 primiasolutions. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newInvoiceitemsPostInvoiceitemCmd(flags *rootFlags) *cobra.Command {
	var bodyAmount int
	var bodyDescription string
	var bodyDiscountable bool
	var bodyDiscounts string
	var bodyExpand string
	var bodyMetadata string
	var bodyPeriod string
	var bodyPriceData string
	var bodyPricing string
	var bodyQuantity int
	var bodyQuantityDecimal string
	var bodyTaxBehavior string
	var bodyTaxCode string
	var bodyTaxRates string
	var bodyUnitAmountDecimal string

	cmd := &cobra.Command{
		Use:         "post-invoiceitem",
		Short:       "Updates the amount or description of an invoice item on an upcoming invoice. Updating an invoice item is only...",
		Example:     "  stripe-pp-pp-cli invoiceitems post-invoiceitem example-value",
		Annotations: map[string]string{"pp:endpoint": "invoiceitems.post-invoiceitem", "pp:method": "POST", "pp:path": "/v1/invoiceitems/{invoiceitem}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/invoiceitems/{invoiceitem}"
			path = replacePathParam(path, "invoiceitem", args[0])
			fields := url.Values{}
			if bodyAmount != 0 {
				fields.Set("amount", fmt.Sprintf("%v", bodyAmount))
			}
			if bodyDescription != "" {
				fields.Set("description", bodyDescription)
			}
			if bodyDiscountable != false {
				fields.Set("discountable", fmt.Sprintf("%v", bodyDiscountable))
			}
			if bodyDiscounts != "" {
				fields.Set("discounts", bodyDiscounts)
			}
			if bodyExpand != "" {
				if !json.Valid([]byte(bodyExpand)) {
					return fmt.Errorf("parsing --expand JSON: invalid JSON")
				}
				fields.Set("expand", bodyExpand)
			}
			if bodyMetadata != "" {
				fields.Set("metadata", bodyMetadata)
			}
			if bodyPeriod != "" {
				if !json.Valid([]byte(bodyPeriod)) {
					return fmt.Errorf("parsing --period JSON: invalid JSON")
				}
				fields.Set("period", bodyPeriod)
			}
			if bodyPriceData != "" {
				if !json.Valid([]byte(bodyPriceData)) {
					return fmt.Errorf("parsing --price-data JSON: invalid JSON")
				}
				fields.Set("price_data", bodyPriceData)
			}
			if bodyPricing != "" {
				if !json.Valid([]byte(bodyPricing)) {
					return fmt.Errorf("parsing --pricing JSON: invalid JSON")
				}
				fields.Set("pricing", bodyPricing)
			}
			if bodyQuantity != 0 {
				fields.Set("quantity", fmt.Sprintf("%v", bodyQuantity))
			}
			if bodyQuantityDecimal != "" {
				fields.Set("quantity_decimal", bodyQuantityDecimal)
			}
			if bodyTaxBehavior != "" {
				fields.Set("tax_behavior", bodyTaxBehavior)
			}
			if bodyTaxCode != "" {
				fields.Set("tax_code", bodyTaxCode)
			}
			if bodyTaxRates != "" {
				fields.Set("tax_rates", bodyTaxRates)
			}
			if bodyUnitAmountDecimal != "" {
				fields.Set("unit_amount_decimal", bodyUnitAmountDecimal)
			}

			data, statusCode, err := c.PostForm(path, fields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "invoiceitems",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyAmount, "amount", 0, "The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. If you want...")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy...")
	cmd.Flags().BoolVar(&bodyDiscountable, "discountable", false, "Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items,...")
	cmd.Flags().StringVar(&bodyDiscounts, "discounts", "", "The coupons, promotion codes & existing discounts which apply to the invoice item or invoice line item. Item...")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyPeriod, "period", "", "The period associated with this invoice item. When set to different values, the period will be rendered on the...")
	cmd.Flags().StringVar(&bodyPriceData, "price-data", "", "Data used to generate a new [Price](https://docs.stripe.com/api/prices) object inline.")
	cmd.Flags().StringVar(&bodyPricing, "pricing", "", "The pricing information for the invoice item.")
	cmd.Flags().IntVar(&bodyQuantity, "quantity", 0, "Non-negative integer. The quantity of units for the invoice item. Use `quantity_decimal` instead to provide decimal...")
	cmd.Flags().StringVar(&bodyQuantityDecimal, "quantity-decimal", "", "Non-negative decimal with at most 12 decimal places. The quantity of units for the line item.")
	cmd.Flags().StringVar(&bodyTaxBehavior, "tax-behavior", "", "Only required if a [default tax behavior](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#set...")
	cmd.Flags().StringVar(&bodyTaxCode, "tax-code", "", "A [tax code](https://docs.stripe.com/tax/tax-categories) ID.")
	cmd.Flags().StringVar(&bodyTaxRates, "tax-rates", "", "The tax rates which apply to the invoice item. When set, the `default_tax_rates` on the invoice do not apply to this...")
	cmd.Flags().StringVar(&bodyUnitAmountDecimal, "unit-amount-decimal", "", "The decimal unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This...")

	return cmd
}
