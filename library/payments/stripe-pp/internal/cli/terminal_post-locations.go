// Copyright 2026 primiasolutions. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newTerminalPostLocationsCmd(flags *rootFlags) *cobra.Command {
	var bodyAddress string
	var bodyAddressKana string
	var bodyAddressKanji string
	var bodyConfigurationOverrides string
	var bodyDisplayName string
	var bodyDisplayNameKana string
	var bodyDisplayNameKanji string
	var bodyExpand string
	var bodyMetadata string
	var bodyPhone string

	cmd := &cobra.Command{
		Use:         "post-locations",
		Short:       "Creates a new Location object. For further details, including which address fields are required in...",
		Example:     "  stripe-pp-pp-cli terminal post-locations",
		Annotations: map[string]string{"pp:endpoint": "terminal.post-locations", "pp:method": "POST", "pp:path": "/v1/terminal/locations"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/terminal/locations"
			fields := url.Values{}
			if bodyAddress != "" {
				if !json.Valid([]byte(bodyAddress)) {
					return fmt.Errorf("parsing --address JSON: invalid JSON")
				}
				fields.Set("address", bodyAddress)
			}
			if bodyAddressKana != "" {
				if !json.Valid([]byte(bodyAddressKana)) {
					return fmt.Errorf("parsing --address-kana JSON: invalid JSON")
				}
				fields.Set("address_kana", bodyAddressKana)
			}
			if bodyAddressKanji != "" {
				if !json.Valid([]byte(bodyAddressKanji)) {
					return fmt.Errorf("parsing --address-kanji JSON: invalid JSON")
				}
				fields.Set("address_kanji", bodyAddressKanji)
			}
			if bodyConfigurationOverrides != "" {
				fields.Set("configuration_overrides", bodyConfigurationOverrides)
			}
			if bodyDisplayName != "" {
				fields.Set("display_name", bodyDisplayName)
			}
			if bodyDisplayNameKana != "" {
				fields.Set("display_name_kana", bodyDisplayNameKana)
			}
			if bodyDisplayNameKanji != "" {
				fields.Set("display_name_kanji", bodyDisplayNameKanji)
			}
			if bodyExpand != "" {
				if !json.Valid([]byte(bodyExpand)) {
					return fmt.Errorf("parsing --expand JSON: invalid JSON")
				}
				fields.Set("expand", bodyExpand)
			}
			if bodyMetadata != "" {
				fields.Set("metadata", bodyMetadata)
			}
			if bodyPhone != "" {
				fields.Set("phone", bodyPhone)
			}

			data, statusCode, err := c.PostForm(path, fields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "terminal",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAddress, "address", "", "The full address of the location.")
	cmd.Flags().StringVar(&bodyAddressKana, "address-kana", "", "The Kana variation of the full address of the location (Japan only).")
	cmd.Flags().StringVar(&bodyAddressKanji, "address-kanji", "", "The Kanji variation of the full address of the location (Japan only).")
	cmd.Flags().StringVar(&bodyConfigurationOverrides, "configuration-overrides", "", "The ID of a configuration that will be used to customize all readers in this location.")
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "A name for the location. Maximum length is 1000 characters.")
	cmd.Flags().StringVar(&bodyDisplayNameKana, "display-name-kana", "", "The Kana variation of the name for the location (Japan only). Maximum length is 1000 characters.")
	cmd.Flags().StringVar(&bodyDisplayNameKanji, "display-name-kanji", "", "The Kanji variation of the name for the location (Japan only). Maximum length is 1000 characters.")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyPhone, "phone", "", "The phone number for the location.")

	return cmd
}
