// Copyright 2026 primiasolutions. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"net/url"
	"os"

	"github.com/spf13/cobra"
)

func newCustomersPostCustomerCmd(flags *rootFlags) *cobra.Command {
	var bodyAddress string
	var bodyBalance int
	var bodyBankAccount string
	var bodyBusinessName string
	var bodyCard string
	var bodyCashBalance string
	var bodyDefaultAlipayAccount string
	var bodyDefaultBankAccount string
	var bodyDefaultCard string
	var bodyDefaultSource string
	var bodyDescription string
	var bodyEmail string
	var bodyExpand string
	var bodyIndividualName string
	var bodyInvoicePrefix string
	var bodyInvoiceSettings string
	var bodyMetadata string
	var bodyName string
	var bodyNextInvoiceSequence int
	var bodyPhone string
	var bodyPreferredLocales string
	var bodyShipping string
	var bodySource string
	var bodyTax string
	var bodyTaxExempt string

	cmd := &cobra.Command{
		Use:         "post-customer",
		Short:       "Updates the specified customer by setting the values of the parameters passed. Any parameters not provided are...",
		Example:     "  stripe-pp-pp-cli customers post-customer example-value",
		Annotations: map[string]string{"pp:endpoint": "customers.post-customer", "pp:method": "POST", "pp:path": "/v1/customers/{customer}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/customers/{customer}"
			path = replacePathParam(path, "customer", args[0])
			fields := url.Values{}
			if bodyAddress != "" {
				fields.Set("address", bodyAddress)
			}
			if bodyBalance != 0 {
				fields.Set("balance", fmt.Sprintf("%v", bodyBalance))
			}
			if bodyBankAccount != "" {
				fields.Set("bank_account", bodyBankAccount)
			}
			if bodyBusinessName != "" {
				fields.Set("business_name", bodyBusinessName)
			}
			if bodyCard != "" {
				fields.Set("card", bodyCard)
			}
			if bodyCashBalance != "" {
				if !json.Valid([]byte(bodyCashBalance)) {
					return fmt.Errorf("parsing --cash-balance JSON: invalid JSON")
				}
				fields.Set("cash_balance", bodyCashBalance)
			}
			if bodyDefaultAlipayAccount != "" {
				fields.Set("default_alipay_account", bodyDefaultAlipayAccount)
			}
			if bodyDefaultBankAccount != "" {
				fields.Set("default_bank_account", bodyDefaultBankAccount)
			}
			if bodyDefaultCard != "" {
				fields.Set("default_card", bodyDefaultCard)
			}
			if bodyDefaultSource != "" {
				fields.Set("default_source", bodyDefaultSource)
			}
			if bodyDescription != "" {
				fields.Set("description", bodyDescription)
			}
			if bodyEmail != "" {
				fields.Set("email", bodyEmail)
			}
			if bodyExpand != "" {
				if !json.Valid([]byte(bodyExpand)) {
					return fmt.Errorf("parsing --expand JSON: invalid JSON")
				}
				fields.Set("expand", bodyExpand)
			}
			if bodyIndividualName != "" {
				fields.Set("individual_name", bodyIndividualName)
			}
			if bodyInvoicePrefix != "" {
				fields.Set("invoice_prefix", bodyInvoicePrefix)
			}
			if bodyInvoiceSettings != "" {
				if !json.Valid([]byte(bodyInvoiceSettings)) {
					return fmt.Errorf("parsing --invoice-settings JSON: invalid JSON")
				}
				fields.Set("invoice_settings", bodyInvoiceSettings)
			}
			if bodyMetadata != "" {
				fields.Set("metadata", bodyMetadata)
			}
			if bodyName != "" {
				fields.Set("name", bodyName)
			}
			if bodyNextInvoiceSequence != 0 {
				fields.Set("next_invoice_sequence", fmt.Sprintf("%v", bodyNextInvoiceSequence))
			}
			if bodyPhone != "" {
				fields.Set("phone", bodyPhone)
			}
			if bodyPreferredLocales != "" {
				if !json.Valid([]byte(bodyPreferredLocales)) {
					return fmt.Errorf("parsing --preferred-locales JSON: invalid JSON")
				}
				fields.Set("preferred_locales", bodyPreferredLocales)
			}
			if bodyShipping != "" {
				fields.Set("shipping", bodyShipping)
			}
			if bodySource != "" {
				fields.Set("source", bodySource)
			}
			if bodyTax != "" {
				if !json.Valid([]byte(bodyTax)) {
					return fmt.Errorf("parsing --tax JSON: invalid JSON")
				}
				fields.Set("tax", bodyTax)
			}
			if bodyTaxExempt != "" {
				fields.Set("tax_exempt", bodyTaxExempt)
			}

			data, statusCode, err := c.PostForm(path, fields)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "customers",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAddress, "address", "", "The customer's address. Learn about [country-specific requirements for calculating...")
	cmd.Flags().IntVar(&bodyBalance, "balance", 0, "An integer amount in cents (or local equivalent) that represents the customer's current balance, which affect the...")
	cmd.Flags().StringVar(&bodyBankAccount, "bank-account", "", "Either a token, like the ones returned by [Stripe.js](https://stripe.com/docs/js), or a dictionary containing a...")
	cmd.Flags().StringVar(&bodyBusinessName, "business-name", "", "The customer's business name. This may be up to *150 characters*.")
	cmd.Flags().StringVar(&bodyCard, "card", "", "A token, like the ones returned by [Stripe.js](https://stripe.com/docs/js).")
	cmd.Flags().StringVar(&bodyCashBalance, "cash-balance", "", "Balance information and default balance settings for this customer.")
	cmd.Flags().StringVar(&bodyDefaultAlipayAccount, "default-alipay-account", "", "ID of Alipay account to make the customer's new default for invoice payments.")
	cmd.Flags().StringVar(&bodyDefaultBankAccount, "default-bank-account", "", "ID of bank account to make the customer's new default for invoice payments.")
	cmd.Flags().StringVar(&bodyDefaultCard, "default-card", "", "ID of card to make the customer's new default for invoice payments.")
	cmd.Flags().StringVar(&bodyDefaultSource, "default-source", "", "If you are using payment methods created via the PaymentMethods API, see the...")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "An arbitrary string that you can attach to a customer object. It is displayed alongside the customer in the dashboard.")
	cmd.Flags().StringVar(&bodyEmail, "email", "", "Customer's email address. It's displayed alongside the customer in your dashboard and can be useful for searching...")
	cmd.Flags().StringVar(&bodyExpand, "expand", "", "Specifies which fields in the response should be expanded.")
	cmd.Flags().StringVar(&bodyIndividualName, "individual-name", "", "The customer's full name. This may be up to *150 characters*.")
	cmd.Flags().StringVar(&bodyInvoicePrefix, "invoice-prefix", "", "The prefix for the customer used to generate unique invoice numbers. Must be 3–12 uppercase letters or numbers.")
	cmd.Flags().StringVar(&bodyInvoiceSettings, "invoice-settings", "", "Default invoice settings for this customer.")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful...")
	cmd.Flags().StringVar(&bodyName, "name", "", "The customer's full name or business name.")
	cmd.Flags().IntVar(&bodyNextInvoiceSequence, "next-invoice-sequence", 0, "The sequence to be used on the customer's next invoice. Defaults to 1.")
	cmd.Flags().StringVar(&bodyPhone, "phone", "", "The customer's phone number.")
	cmd.Flags().StringVar(&bodyPreferredLocales, "preferred-locales", "", "Customer's preferred languages, ordered by preference.")
	cmd.Flags().StringVar(&bodyShipping, "shipping", "", "The customer's shipping information. Appears on invoices emailed to this customer.")
	cmd.Flags().StringVar(&bodySource, "source", "", "Source")
	cmd.Flags().StringVar(&bodyTax, "tax", "", "Tax details about the customer.")
	cmd.Flags().StringVar(&bodyTaxExempt, "tax-exempt", "", "The customer's tax exemption. One of `none`, `exempt`, or `reverse`.")

	return cmd
}
