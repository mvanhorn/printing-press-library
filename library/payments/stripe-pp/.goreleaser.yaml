version: 2
project_name: stripe-pp-pp-cli
changelog:
  disable: true
builds:
  - id: stripe-pp-pp-cli
    main: ./cmd/stripe-pp-pp-cli
    binary: stripe-pp-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/payments/stripe/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: stripe-pp-pp-mcp
    main: ./cmd/stripe-pp-pp-mcp
    binary: stripe-pp-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: stripe-pp-pp-cli
    repository:
      owner: primiasolutions
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Every Stripe feature, plus a local SQLite mirror, FTS search, and compound-analytics commands no other Stripe tool has."
    install: |
      bin.install "stripe-pp-pp-cli"
      bin.install "stripe-pp-pp-mcp"
