// Copyright 2026 giuseppe-bisemi. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCustomersCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyType string
	var bodyCompanyname string
	var bodyName string
	var bodySurname string
	var bodyPIva string
	var bodyCodfis string
	var bodyAddress string
	var bodyCap string
	var bodyCity string
	var bodyProv string
	var bodyCountry string
	var bodyCountryType string
	var bodyTel string
	var bodyEmail string
	var bodyCodDestPec string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create a customer",
		Example:     "  partitaiva24-pp-cli customers create",
		Annotations: map[string]string{"pp:endpoint": "customers.create", "pp:method": "POST", "pp:path": "/user/customers"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/user/customers"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyType != "" {
					body["type"] = bodyType
				}
				if bodyCompanyname != "" {
					body["companyname"] = bodyCompanyname
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodySurname != "" {
					body["surname"] = bodySurname
				}
				if bodyPIva != "" {
					body["p_iva"] = bodyPIva
				}
				if bodyCodfis != "" {
					body["codfis"] = bodyCodfis
				}
				if bodyAddress != "" {
					body["address"] = bodyAddress
				}
				if bodyCap != "" {
					body["cap"] = bodyCap
				}
				if bodyCity != "" {
					body["city"] = bodyCity
				}
				if bodyProv != "" {
					body["prov"] = bodyProv
				}
				if bodyCountry != "" {
					body["country"] = bodyCountry
				}
				if bodyCountryType != "" {
					body["country_type"] = bodyCountryType
				}
				if bodyTel != "" {
					body["tel"] = bodyTel
				}
				if bodyEmail != "" {
					body["email"] = bodyEmail
				}
				if bodyCodDestPec != "" {
					body["cod_dest_pec"] = bodyCodDestPec
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "customers",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyType, "type", "", "pf | pa | other")
	cmd.Flags().StringVar(&bodyCompanyname, "companyname", "", "")
	cmd.Flags().StringVar(&bodyName, "name", "", "")
	cmd.Flags().StringVar(&bodySurname, "surname", "", "")
	cmd.Flags().StringVar(&bodyPIva, "p-iva", "", "Italian VAT (P.IVA)")
	cmd.Flags().StringVar(&bodyCodfis, "codfis", "", "Codice fiscale")
	cmd.Flags().StringVar(&bodyAddress, "address", "", "")
	cmd.Flags().StringVar(&bodyCap, "cap", "", "")
	cmd.Flags().StringVar(&bodyCity, "city", "", "")
	cmd.Flags().StringVar(&bodyProv, "prov", "", "")
	cmd.Flags().StringVar(&bodyCountry, "country", "", "")
	cmd.Flags().StringVar(&bodyCountryType, "country-type", "", "it | eu | extra")
	cmd.Flags().StringVar(&bodyTel, "tel", "", "")
	cmd.Flags().StringVar(&bodyEmail, "email", "", "")
	cmd.Flags().StringVar(&bodyCodDestPec, "cod-dest-pec", "", "Destinatario SDI o PEC")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
