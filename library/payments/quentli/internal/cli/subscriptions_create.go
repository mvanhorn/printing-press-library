// Copyright 2026 bobe and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newSubscriptionsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyInputBeneficiaryId string
	var bodyInputCollectionMethod string
	var bodyInputCustomerId string
	var bodyInputDescription string
	var bodyInputDiscounts string
	var bodyInputInvoices string
	var bodyInputItems string
	var bodyInputModifiers string
	var bodyInputNextCollectionDate string
	var bodyInputNumberOfPayments float64
	var bodyInputOnlyAutomaticCollection bool
	var bodyInputPaymentMethodId string
	var bodyInputRecurrentDetailChargeEach float64
	var bodyInputRecurrentDetailCollectionInterval string
	var bodyInputRecurrentDetailToleranceDays int
	var bodyInputTaxProfileId string
	var bodyInputTemplateId string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Creates a new recurring subscription for a customer.",
		Example:     "  quentli-pp-cli subscriptions create",
		Annotations: map[string]string{"pp:endpoint": "subscriptions.create", "pp:method": "POST", "pp:path": "/v1/subscriptions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("input-collection-method") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "input-collection-method")
				}
				if !cmd.Flags().Changed("input-customer-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "input-customer-id")
				}
				if (cmd.Flags().Changed("input-recurrent-detail-charge-each") || bodyInputRecurrentDetailChargeEach != 0.0) || (cmd.Flags().Changed("input-recurrent-detail-collection-interval") || bodyInputRecurrentDetailCollectionInterval != "") || (cmd.Flags().Changed("input-recurrent-detail-tolerance-days") || bodyInputRecurrentDetailToleranceDays != 0) {
					if !cmd.Flags().Changed("input-recurrent-detail-charge-each") && !flags.dryRun {
						return fmt.Errorf("required flag \"%s\" not set", "input-recurrent-detail-charge-each")
					}
					if !cmd.Flags().Changed("input-recurrent-detail-collection-interval") && !flags.dryRun {
						return fmt.Errorf("required flag \"%s\" not set", "input-recurrent-detail-collection-interval")
					}
				}
			}
			path := "/v1/subscriptions"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				{
					nestedInput := map[string]any{}
					if cmd.Flags().Changed("input-beneficiary-id") || bodyInputBeneficiaryId != "" {
						nestedInput["beneficiaryId"] = bodyInputBeneficiaryId
					}
					if cmd.Flags().Changed("input-collection-method") || bodyInputCollectionMethod != "" {
						nestedInput["collectionMethod"] = bodyInputCollectionMethod
					}
					if cmd.Flags().Changed("input-customer-id") || bodyInputCustomerId != "" {
						nestedInput["customerId"] = bodyInputCustomerId
					}
					if cmd.Flags().Changed("input-description") || bodyInputDescription != "" {
						nestedInput["description"] = bodyInputDescription
					}
					if cmd.Flags().Changed("input-discounts") || bodyInputDiscounts != "" {
						var parsedInputDiscounts any
						if err := json.Unmarshal([]byte(bodyInputDiscounts), &parsedInputDiscounts); err != nil {
							return fmt.Errorf("parsing --input-discounts JSON: %w", err)
						}
						asArray, ok := parsedInputDiscounts.([]any)
						if !ok {
							return fmt.Errorf("--input-discounts must be a JSON array, got JSON %T", parsedInputDiscounts)
						}
						nestedInput["discounts"] = asArray
					}
					if cmd.Flags().Changed("input-invoices") || bodyInputInvoices != "" {
						var parsedInputInvoices any
						if err := json.Unmarshal([]byte(bodyInputInvoices), &parsedInputInvoices); err != nil {
							return fmt.Errorf("parsing --input-invoices JSON: %w", err)
						}
						asArray, ok := parsedInputInvoices.([]any)
						if !ok {
							return fmt.Errorf("--input-invoices must be a JSON array, got JSON %T", parsedInputInvoices)
						}
						nestedInput["invoices"] = asArray
					}
					if cmd.Flags().Changed("input-items") || bodyInputItems != "" {
						var parsedInputItems any
						if err := json.Unmarshal([]byte(bodyInputItems), &parsedInputItems); err != nil {
							return fmt.Errorf("parsing --input-items JSON: %w", err)
						}
						asArray, ok := parsedInputItems.([]any)
						if !ok {
							return fmt.Errorf("--input-items must be a JSON array, got JSON %T", parsedInputItems)
						}
						nestedInput["items"] = asArray
					}
					if cmd.Flags().Changed("input-modifiers") || bodyInputModifiers != "" {
						var parsedInputModifiers any
						if err := json.Unmarshal([]byte(bodyInputModifiers), &parsedInputModifiers); err != nil {
							return fmt.Errorf("parsing --input-modifiers JSON: %w", err)
						}
						asArray, ok := parsedInputModifiers.([]any)
						if !ok {
							return fmt.Errorf("--input-modifiers must be a JSON array, got JSON %T", parsedInputModifiers)
						}
						nestedInput["modifiers"] = asArray
					}
					if cmd.Flags().Changed("input-next-collection-date") || bodyInputNextCollectionDate != "" {
						nestedInput["nextCollectionDate"] = bodyInputNextCollectionDate
					}
					if cmd.Flags().Changed("input-number-of-payments") || bodyInputNumberOfPayments != 0.0 {
						nestedInput["numberOfPayments"] = bodyInputNumberOfPayments
					}
					if cmd.Flags().Changed("input-only-automatic-collection") {
						nestedInput["onlyAutomaticCollection"] = bodyInputOnlyAutomaticCollection
					}
					if cmd.Flags().Changed("input-payment-method-id") || bodyInputPaymentMethodId != "" {
						nestedInput["paymentMethodId"] = bodyInputPaymentMethodId
					}
					{
						nestedInputRecurrentDetail := map[string]any{}
						if cmd.Flags().Changed("input-recurrent-detail-charge-each") || bodyInputRecurrentDetailChargeEach != 0.0 {
							nestedInputRecurrentDetail["chargeEach"] = bodyInputRecurrentDetailChargeEach
						}
						if cmd.Flags().Changed("input-recurrent-detail-collection-interval") || bodyInputRecurrentDetailCollectionInterval != "" {
							nestedInputRecurrentDetail["collectionInterval"] = bodyInputRecurrentDetailCollectionInterval
						}
						if cmd.Flags().Changed("input-recurrent-detail-tolerance-days") || bodyInputRecurrentDetailToleranceDays != 0 {
							nestedInputRecurrentDetail["toleranceDays"] = bodyInputRecurrentDetailToleranceDays
						}
						if len(nestedInputRecurrentDetail) > 0 {
							nestedInput["recurrentDetail"] = nestedInputRecurrentDetail
						}
					}
					if cmd.Flags().Changed("input-tax-profile-id") || bodyInputTaxProfileId != "" {
						nestedInput["taxProfileId"] = bodyInputTaxProfileId
					}
					if cmd.Flags().Changed("input-template-id") || bodyInputTemplateId != "" {
						nestedInput["templateId"] = bodyInputTemplateId
					}
					if len(nestedInput) > 0 {
						bodyMap["input"] = nestedInput
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "subscriptions", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "subscriptions", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "subscriptions", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "subscriptions", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "subscriptions", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "subscriptions",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "subscriptions", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "subscriptions", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyInputBeneficiaryId, "input-beneficiary-id", "", "Optional identifier of a different customer who is the actual beneficiary.")
	cmd.Flags().StringVar(&bodyInputCollectionMethod, "input-collection-method", "", "How payment is collected: SEND_REMINDER sends a notification, AUTOMATIC charges a saved payment method")
	cmd.Flags().StringVar(&bodyInputCustomerId, "input-customer-id", "", "Identifier of the customer who will be charged.")
	cmd.Flags().StringVar(&bodyInputDescription, "input-description", "", "Human-readable description of the subscription. Auto-generated from item names when omitted.")
	cmd.Flags().StringVar(&bodyInputDiscounts, "input-discounts", "", "Discounts to apply to generated invoices.")
	cmd.Flags().StringVar(&bodyInputInvoices, "input-invoices", "", "Pre-defined invoices with explicit due dates and items. Mutually exclusive with `items`.")
	cmd.Flags().StringVar(&bodyInputItems, "input-items", "", "Recurring items whose invoices are auto-generated on each collection date. Mutually exclusive with `invoices`.")
	cmd.Flags().StringVar(&bodyInputModifiers, "input-modifiers", "", "Price modifiers (early payment discounts or late payment interest) to apply to every generated invoice.")
	cmd.Flags().StringVar(&bodyInputNextCollectionDate, "input-next-collection-date", "", "Date of the first (or next) charge. Required when using recurring `items`.")
	cmd.Flags().Float64Var(&bodyInputNumberOfPayments, "input-number-of-payments", 0.0, "Total number of invoices to generate before the subscription ends. Null means indefinite.")
	cmd.Flags().BoolVar(&bodyInputOnlyAutomaticCollection, "input-only-automatic-collection", false, "Whether this subscription only allows automatic payment collection.")
	cmd.Flags().StringVar(&bodyInputPaymentMethodId, "input-payment-method-id", "", "Identifier of the payment method to use for automatic collection. Falls back to the customer's default method.")
	cmd.Flags().Float64Var(&bodyInputRecurrentDetailChargeEach, "input-recurrent-detail-charge-each", 0.0, "Number of intervals between charges.")
	cmd.Flags().StringVar(&bodyInputRecurrentDetailCollectionInterval, "input-recurrent-detail-collection-interval", "", "Time interval unit (e.g. MONTH, WEEK).")
	cmd.Flags().IntVar(&bodyInputRecurrentDetailToleranceDays, "input-recurrent-detail-tolerance-days", 0, "Grace days after the due date before the invoice expires.")
	cmd.Flags().StringVar(&bodyInputTaxProfileId, "input-tax-profile-id", "", "Optional tax profile to use for invoice generation.")
	cmd.Flags().StringVar(&bodyInputTemplateId, "input-template-id", "", "Optional template to use for subscription messaging.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
