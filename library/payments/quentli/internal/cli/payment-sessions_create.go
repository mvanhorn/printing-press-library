// Copyright 2026 bobe and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newPaymentSessionsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyInputAmount float64
	var bodyInputCancelUrl string
	var bodyInputCurrency string
	var bodyInputCustomerEmail string
	var bodyInputCustomerExternalId string
	var bodyInputCustomerForceUpdate bool
	var bodyInputCustomerMetadata string
	var bodyInputCustomerName string
	var bodyInputCustomerPhoneNumber string
	var bodyInputDescription string
	var bodyInputDisplayMode string
	var bodyInputExpiresAt string
	var bodyInputItems string
	var bodyInputMetadata string
	var bodyInputReturnUrl string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Creates a hosted payment session. Resolves or creates the customer and returns an authenticated payment URL.",
		Example:     "  quentli-pp-cli payment-sessions create",
		Annotations: map[string]string{"pp:endpoint": "payment-sessions.create", "pp:method": "POST", "pp:path": "/v1/payment-sessions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("input-customer-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "input-customer-name")
				}
			}
			path := "/v1/payment-sessions"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				{
					nestedInput := map[string]any{}
					if cmd.Flags().Changed("input-amount") || bodyInputAmount != 0.0 {
						nestedInput["amount"] = bodyInputAmount
					}
					if cmd.Flags().Changed("input-cancel-url") || bodyInputCancelUrl != "" {
						nestedInput["cancelUrl"] = bodyInputCancelUrl
					}
					if cmd.Flags().Changed("input-currency") || bodyInputCurrency != "" {
						nestedInput["currency"] = bodyInputCurrency
					}
					{
						nestedInputCustomer := map[string]any{}
						if cmd.Flags().Changed("input-customer-email") || bodyInputCustomerEmail != "" {
							nestedInputCustomer["email"] = bodyInputCustomerEmail
						}
						if cmd.Flags().Changed("input-customer-external-id") || bodyInputCustomerExternalId != "" {
							nestedInputCustomer["externalId"] = bodyInputCustomerExternalId
						}
						if cmd.Flags().Changed("input-customer-force-update") {
							nestedInputCustomer["forceUpdate"] = bodyInputCustomerForceUpdate
						}
						if cmd.Flags().Changed("input-customer-metadata") || bodyInputCustomerMetadata != "" {
							var parsedInputCustomerMetadata any
							if err := json.Unmarshal([]byte(bodyInputCustomerMetadata), &parsedInputCustomerMetadata); err != nil {
								return fmt.Errorf("parsing --input-customer-metadata JSON: %w", err)
							}
							asArray, ok := parsedInputCustomerMetadata.([]any)
							if !ok {
								return fmt.Errorf("--input-customer-metadata must be a JSON array, got JSON %T", parsedInputCustomerMetadata)
							}
							nestedInputCustomer["metadata"] = asArray
						}
						if cmd.Flags().Changed("input-customer-name") || bodyInputCustomerName != "" {
							nestedInputCustomer["name"] = bodyInputCustomerName
						}
						if cmd.Flags().Changed("input-customer-phone-number") || bodyInputCustomerPhoneNumber != "" {
							nestedInputCustomer["phoneNumber"] = bodyInputCustomerPhoneNumber
						}
						if len(nestedInputCustomer) > 0 {
							nestedInput["customer"] = nestedInputCustomer
						}
					}
					if cmd.Flags().Changed("input-description") || bodyInputDescription != "" {
						nestedInput["description"] = bodyInputDescription
					}
					if cmd.Flags().Changed("input-display-mode") || bodyInputDisplayMode != "" {
						nestedInput["displayMode"] = bodyInputDisplayMode
					}
					if cmd.Flags().Changed("input-expires-at") || bodyInputExpiresAt != "" {
						nestedInput["expiresAt"] = bodyInputExpiresAt
					}
					if cmd.Flags().Changed("input-items") || bodyInputItems != "" {
						var parsedInputItems any
						if err := json.Unmarshal([]byte(bodyInputItems), &parsedInputItems); err != nil {
							return fmt.Errorf("parsing --input-items JSON: %w", err)
						}
						asArray, ok := parsedInputItems.([]any)
						if !ok {
							return fmt.Errorf("--input-items must be a JSON array, got JSON %T", parsedInputItems)
						}
						nestedInput["items"] = asArray
					}
					if cmd.Flags().Changed("input-metadata") || bodyInputMetadata != "" {
						var parsedInputMetadata any
						if err := json.Unmarshal([]byte(bodyInputMetadata), &parsedInputMetadata); err != nil {
							return fmt.Errorf("parsing --input-metadata JSON: %w", err)
						}
						asArray, ok := parsedInputMetadata.([]any)
						if !ok {
							return fmt.Errorf("--input-metadata must be a JSON array, got JSON %T", parsedInputMetadata)
						}
						nestedInput["metadata"] = asArray
					}
					if cmd.Flags().Changed("input-return-url") || bodyInputReturnUrl != "" {
						nestedInput["returnUrl"] = bodyInputReturnUrl
					}
					if len(nestedInput) > 0 {
						bodyMap["input"] = nestedInput
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "payment-sessions", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "payment-sessions", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "payment-sessions", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "payment-sessions", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "payment-sessions", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "payment-sessions",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "payment-sessions", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "payment-sessions", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().Float64Var(&bodyInputAmount, "input-amount", 0.0, "Total amount in minor currency units (used when items are not provided).")
	cmd.Flags().StringVar(&bodyInputCancelUrl, "input-cancel-url", "", "URL to redirect to if the payer cancels.")
	cmd.Flags().StringVar(&bodyInputCurrency, "input-currency", "", "ISO 4217 currency code (used when items are not provided).")
	cmd.Flags().StringVar(&bodyInputCustomerEmail, "input-customer-email", "", "Customer email address.")
	cmd.Flags().StringVar(&bodyInputCustomerExternalId, "input-customer-external-id", "", "External ID used to match or create the customer.")
	cmd.Flags().BoolVar(&bodyInputCustomerForceUpdate, "input-customer-force-update", false, "Whether to treat the provided customer contact fields as authoritative.")
	cmd.Flags().StringVar(&bodyInputCustomerMetadata, "input-customer-metadata", "", "Metadata entries to attach to the customer.")
	cmd.Flags().StringVar(&bodyInputCustomerName, "input-customer-name", "", "Customer full name.")
	cmd.Flags().StringVar(&bodyInputCustomerPhoneNumber, "input-customer-phone-number", "", "Customer phone number in E.164 format.")
	cmd.Flags().StringVar(&bodyInputDescription, "input-description", "", "Payment description (used when items are not provided).")
	cmd.Flags().StringVar(&bodyInputDisplayMode, "input-display-mode", "", "How the payment UI is presented.")
	cmd.Flags().StringVar(&bodyInputExpiresAt, "input-expires-at", "", "Optional expiration time for the session.")
	cmd.Flags().StringVar(&bodyInputItems, "input-items", "", "Line items to include in the session. Overrides amount/currency/description if provided.")
	cmd.Flags().StringVar(&bodyInputMetadata, "input-metadata", "", "Metadata entries to attach to the session.")
	cmd.Flags().StringVar(&bodyInputReturnUrl, "input-return-url", "", "URL to redirect to after successful payment.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
