// Copyright 2026 ardihanan and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrdersCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAdminFeeMethod string
	var bodyAmount string
	var bodyCurrency string
	var bodyCustomerAddressAddressLine string
	var bodyCustomerAddressCity string
	var bodyCustomerAddressLabel string
	var bodyCustomerAddressLandmark string
	var bodyCustomerAddressPostalCode string
	var bodyCustomerAddressReceiverName string
	var bodyCustomerAddressRegion string
	var bodyCustomerCustomerRefId string
	var bodyCustomerEmail string
	var bodyCustomerGivenName string
	var bodyCustomerId string
	var bodyCustomerMiddleName string
	var bodyCustomerMobile string
	var bodyCustomerSurName string
	var bodyDescription string
	var bodyExpiryDate string
	var bodyIsPaymentLink bool
	var bodyItems string
	var bodyMetadata string
	var bodyOrderRefId string
	var bodyPaymentOption string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "The following endpoint creates an Order",
		Example:     "  durianpay-pp-cli orders create --amount example-value",
		Annotations: map[string]string{"pp:endpoint": "orders.create", "pp:method": "POST", "pp:path": "/orders"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/orders"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAdminFeeMethod != "" {
					body["admin_fee_method"] = bodyAdminFeeMethod
				}
				if bodyAmount != "" {
					body["amount"] = bodyAmount
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				{
					nestedCustomer := map[string]any{}
					{
						nestedCustomerAddress := map[string]any{}
						if bodyCustomerAddressAddressLine != "" {
							nestedCustomerAddress["address_line"] = bodyCustomerAddressAddressLine
						}
						if bodyCustomerAddressCity != "" {
							nestedCustomerAddress["city"] = bodyCustomerAddressCity
						}
						if bodyCustomerAddressLabel != "" {
							nestedCustomerAddress["label"] = bodyCustomerAddressLabel
						}
						if bodyCustomerAddressLandmark != "" {
							nestedCustomerAddress["landmark"] = bodyCustomerAddressLandmark
						}
						if bodyCustomerAddressPostalCode != "" {
							nestedCustomerAddress["postal_code"] = bodyCustomerAddressPostalCode
						}
						if bodyCustomerAddressReceiverName != "" {
							nestedCustomerAddress["receiver_name"] = bodyCustomerAddressReceiverName
						}
						if bodyCustomerAddressRegion != "" {
							nestedCustomerAddress["region"] = bodyCustomerAddressRegion
						}
						if len(nestedCustomerAddress) > 0 {
							nestedCustomer["address"] = nestedCustomerAddress
						}
					}
					if bodyCustomerCustomerRefId != "" {
						nestedCustomer["customer_ref_id"] = bodyCustomerCustomerRefId
					}
					if bodyCustomerEmail != "" {
						nestedCustomer["email"] = bodyCustomerEmail
					}
					if bodyCustomerGivenName != "" {
						nestedCustomer["given_name"] = bodyCustomerGivenName
					}
					if bodyCustomerId != "" {
						nestedCustomer["id"] = bodyCustomerId
					}
					if bodyCustomerMiddleName != "" {
						nestedCustomer["middle_name"] = bodyCustomerMiddleName
					}
					if bodyCustomerMobile != "" {
						nestedCustomer["mobile"] = bodyCustomerMobile
					}
					if bodyCustomerSurName != "" {
						nestedCustomer["sur_name"] = bodyCustomerSurName
					}
					if len(nestedCustomer) > 0 {
						body["customer"] = nestedCustomer
					}
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyExpiryDate != "" {
					body["expiry_date"] = bodyExpiryDate
				}
				if cmd.Flags().Changed("is-payment-link") {
					body["is_payment_link"] = bodyIsPaymentLink
				}
				if bodyItems != "" {
					var parsedItems any
					if err := json.Unmarshal([]byte(bodyItems), &parsedItems); err != nil {
						return fmt.Errorf("parsing --items JSON: %w", err)
					}
					body["items"] = parsedItems
				}
				if bodyMetadata != "" {
					body["metadata"] = bodyMetadata
				}
				if bodyOrderRefId != "" {
					body["order_ref_id"] = bodyOrderRefId
				}
				if bodyPaymentOption != "" {
					body["payment_option"] = bodyPaymentOption
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "orders", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "orders", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "orders", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "orders", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "orders", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "orders",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "orders", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "orders", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAdminFeeMethod, "admin-fee-method", "excluded", "Used to specify if the transactional charges are to be included and paid by the merchant or customer.")
	cmd.Flags().StringVar(&bodyAmount, "amount", "1000", "The amount for which the Order was created. Example: '10000' or '10000.23' for amount with decimal value")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "IDR", "The currency associated with the Order's amount. Example: 'IDR'")
	cmd.Flags().StringVar(&bodyCustomerAddressAddressLine, "customer-address-address-line", "", "Address line")
	cmd.Flags().StringVar(&bodyCustomerAddressCity, "customer-address-city", "", "City")
	cmd.Flags().StringVar(&bodyCustomerAddressLabel, "customer-address-label", "", "Label")
	cmd.Flags().StringVar(&bodyCustomerAddressLandmark, "customer-address-landmark", "", "Landmark")
	cmd.Flags().StringVar(&bodyCustomerAddressPostalCode, "customer-address-postal-code", "", "Postal code")
	cmd.Flags().StringVar(&bodyCustomerAddressReceiverName, "customer-address-receiver-name", "", "Receiver name")
	cmd.Flags().StringVar(&bodyCustomerAddressRegion, "customer-address-region", "", "Region")
	cmd.Flags().StringVar(&bodyCustomerCustomerRefId, "customer-customer-ref-id", "", "A unique reference ID from the merchant to identify the customer")
	cmd.Flags().StringVar(&bodyCustomerEmail, "customer-email", "", "The customer's email address. Should of the format jane.doe@gmail.")
	cmd.Flags().StringVar(&bodyCustomerGivenName, "customer-given-name", "", "The customer's given name")
	cmd.Flags().StringVar(&bodyCustomerId, "customer-id", "", "The customer ID from Durianpay")
	cmd.Flags().StringVar(&bodyCustomerMiddleName, "customer-middle-name", "", "The customer's middle name")
	cmd.Flags().StringVar(&bodyCustomerMobile, "customer-mobile", "", "The customer's mobile number.")
	cmd.Flags().StringVar(&bodyCustomerSurName, "customer-sur-name", "", "The customer's surname")
	cmd.Flags().StringVar(&bodyDescription, "description", "This order is delicate handle with care", "String to store description of the order. Example: 'This order is delicate. Handle with Care!'")
	cmd.Flags().StringVar(&bodyExpiryDate, "expiry-date", "2026-09-29T10:00:00.000Z", "The time stamp at which the order will expire.")
	cmd.Flags().BoolVar(&bodyIsPaymentLink, "is-payment-link", false, "Whether Payment Link is to be generated. Example: true or false")
	cmd.Flags().StringVar(&bodyItems, "items", "", "Array of objects with each object including name, brand, category, sku, price, qty, total_price.")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Key-Value pair that can be used to store additional information about the entity.")
	cmd.Flags().StringVar(&bodyOrderRefId, "order-ref-id", "INV/1/2021", "The unique identifier of Order from the merchant. Example: 'INV/1/2021'")
	cmd.Flags().StringVar(&bodyPaymentOption, "payment-option", "full_payment", "The type of payment option with which order was created. Possible values: 'full_payment' or 'installment'")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
