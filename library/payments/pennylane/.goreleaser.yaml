version: 2
project_name: accounting-pp-cli
changelog:
  disable: true
builds:
  - id: accounting-pp-cli
    main: ./cmd/accounting-pp-cli
    binary: accounting-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X accounting-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: accounting-pp-mcp
    main: ./cmd/accounting-pp-mcp
    binary: accounting-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: accounting-pp-cli
    repository:
      owner: user
      name: homebrew-tap
    homepage: "https://github.com/user/accounting-pp-cli"
    description: "Printing Press CLI for Accounting."
    install: |
      bin.install "accounting-pp-cli"
      bin.install "accounting-pp-mcp"
