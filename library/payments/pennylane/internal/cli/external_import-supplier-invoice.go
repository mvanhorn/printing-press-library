// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newExternalImportSupplierInvoiceCmd(flags *rootFlags) *cobra.Command {
	var flagUse2026ApiChanges bool
	var bodyAmount string
	var bodyCurrency string
	var bodyCurrencyAmount string
	var bodyCurrencyAmountBeforeTax string
	var bodyCurrencyTax string
	var bodyDate string
	var bodyDeadline string
	var bodyExternalReference string
	var bodyFileAttachmentId int
	var bodyImportAsIncomplete bool
	var bodyInvoiceLines string
	var bodyInvoiceNumber string
	var bodyLabel string
	var bodySupplierId int
	var bodyTax string
	var bodyTransactionReference string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "import-supplier-invoice",
		Short:       "This endpoint allows you to import a supplier invoice with a file attached. > ℹ️ > This endpoint requires the...",
		Example:     "  accounting-pp-cli external import-supplier-invoice --currency-amount example-value",
		Annotations: map[string]string{"pp:endpoint": "external.import-supplier-invoice", "pp:method": "POST", "pp:path": "/api/external/v2/supplier_invoices/import"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("currency-amount") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "currency-amount")
				}
				if !cmd.Flags().Changed("currency-amount-before-tax") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "currency-amount-before-tax")
				}
				if !cmd.Flags().Changed("currency-tax") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "currency-tax")
				}
				if !cmd.Flags().Changed("date") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "date")
				}
				if !cmd.Flags().Changed("deadline") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "deadline")
				}
				if !cmd.Flags().Changed("file-attachment-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "file-attachment-id")
				}
				if !cmd.Flags().Changed("invoice-lines") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "invoice-lines")
				}
				if !cmd.Flags().Changed("supplier-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "supplier-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/external/v2/supplier_invoices/import"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAmount != "" {
					body["amount"] = bodyAmount
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				if bodyCurrencyAmount != "" {
					body["currency_amount"] = bodyCurrencyAmount
				}
				if bodyCurrencyAmountBeforeTax != "" {
					body["currency_amount_before_tax"] = bodyCurrencyAmountBeforeTax
				}
				if bodyCurrencyTax != "" {
					body["currency_tax"] = bodyCurrencyTax
				}
				if bodyDate != "" {
					body["date"] = bodyDate
				}
				if bodyDeadline != "" {
					body["deadline"] = bodyDeadline
				}
				if bodyExternalReference != "" {
					body["external_reference"] = bodyExternalReference
				}
				if bodyFileAttachmentId != 0 {
					body["file_attachment_id"] = bodyFileAttachmentId
				}
				if bodyImportAsIncomplete != false {
					body["import_as_incomplete"] = bodyImportAsIncomplete
				}
				if bodyInvoiceLines != "" {
					var parsedInvoiceLines any
					if err := json.Unmarshal([]byte(bodyInvoiceLines), &parsedInvoiceLines); err != nil {
						return fmt.Errorf("parsing --invoice-lines JSON: %w", err)
					}
					body["invoice_lines"] = parsedInvoiceLines
				}
				if bodyInvoiceNumber != "" {
					body["invoice_number"] = bodyInvoiceNumber
				}
				if bodyLabel != "" {
					body["label"] = bodyLabel
				}
				if bodySupplierId != 0 {
					body["supplier_id"] = bodySupplierId
				}
				if bodyTax != "" {
					body["tax"] = bodyTax
				}
				if bodyTransactionReference != "" {
					var parsedTransactionReference any
					if err := json.Unmarshal([]byte(bodyTransactionReference), &parsedTransactionReference); err != nil {
						return fmt.Errorf("parsing --transaction-reference JSON: %w", err)
					}
					body["transaction_reference"] = parsedTransactionReference
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "external",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&flagUse2026ApiChanges, "use-2026-api-changes", true, "If you are already using the `X-Use-2026-API-Changes` header, you can ignore this parameter. The Pennylane API is...")
	cmd.Flags().StringVar(&bodyAmount, "amount", "", "Invoice amount in euros (total value of the invoice in euros). If the currency is euro, `currency_amount` and...")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "EUR", "Currency")
	cmd.Flags().StringVar(&bodyCurrencyAmount, "currency-amount", "", "Invoice currency amount (total value of the invoice in the currency of the invoice)")
	cmd.Flags().StringVar(&bodyCurrencyAmountBeforeTax, "currency-amount-before-tax", "", "Invoice currency amount before tax (total value before tax of the invoice in the currency of the invoice)")
	cmd.Flags().StringVar(&bodyCurrencyTax, "currency-tax", "", "Invoice taxable amount (in invoice currency)")
	cmd.Flags().StringVar(&bodyDate, "date", "", "The date of the invoice (ISO 8601)")
	cmd.Flags().StringVar(&bodyDeadline, "deadline", "", "Invoice payment deadline (ISO 8601)")
	cmd.Flags().StringVar(&bodyExternalReference, "external-reference", "", "A unique external reference you can provide to track this supplier. If not provided, Pennylane will generate an...")
	cmd.Flags().IntVar(&bodyFileAttachmentId, "file-attachment-id", 0, "The ID of the supplier invoice file attachment to import")
	cmd.Flags().BoolVar(&bodyImportAsIncomplete, "import-as-incomplete", false, "This will mark the invoice as incomplete.")
	cmd.Flags().StringVar(&bodyInvoiceLines, "invoice-lines", "", "Invoice lines")
	cmd.Flags().StringVar(&bodyInvoiceNumber, "invoice-number", "", "The invoice number")
	cmd.Flags().StringVar(&bodyLabel, "label", "", "Custom label for the invoice used on accounting (ledger) entries. If not provided, Pennylane generates a label...")
	cmd.Flags().IntVar(&bodySupplierId, "supplier-id", 0, "The ID of the supplier to import the invoice for")
	cmd.Flags().StringVar(&bodyTax, "tax", "", "Invoice taxable amount (in euros). If the currency is euro, `currency_tax` and `tax` are identical.")
	cmd.Flags().StringVar(&bodyTransactionReference, "transaction-reference", "", "By adding this field you can automatically reconcile the newly imported invoice with a transaction. See...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
