# Compiled binaries (root only — leading slash anchors to .gitignore dir)
/stripe-pp-cli
/stripe-pp-mcp
/build/

# Source spec (regenerable from upstream URL via printing-press)
/spec.json
/spec.yaml

# Local SQLite store + WAL
*.db
*.db-shm
*.db-wal

# Go test/coverage
coverage.out
*.test

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*~

# Local config / cursors (transcendence command state)
.printing-press.local
