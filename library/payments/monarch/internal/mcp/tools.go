// Copyright 2026 kyle-kirkland. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/payments/monarch/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/payments/monarch/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/payments/monarch/internal/client"
	"github.com/mvanhorn/printing-press-library/library/payments/monarch/internal/config"
	"github.com/mvanhorn/printing-press-library/library/payments/monarch/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/payments/monarch/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("accounts_balance_history",
			mcplib.WithDescription("Recent daily balances per account for the dashboard chart. Optional: start-date. Returns array of AccountBalanceSnapshot."),
			mcplib.WithString("start-date", mcplib.Description("Earliest date to include (YYYY-MM-DD); defaults to 30 days ago")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "start-date", WireName: "start_date", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("accounts_list",
			mcplib.WithDescription("List every account with current balance, type, and institution. Optional: include-hidden. Returns array of Account."),
			mcplib.WithString("include-hidden", mcplib.Description("Include archived/hidden accounts in the result")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "include-hidden", WireName: "include_hidden", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("accounts_networth_snapshots",
			mcplib.WithDescription("Net-worth aggregate snapshots over time (asset/liability breakdown). Optional: start-date, end-date. Returns array of NetWorthSnapshot."),
			mcplib.WithString("start-date", mcplib.Description("Earliest date to include (YYYY-MM-DD)")),
			mcplib.WithString("end-date", mcplib.Description("Latest date to include (YYYY-MM-DD)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "start-date", WireName: "start_date", Location: "query"},{PublicName: "end-date", WireName: "end_date", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("accounts_refresh",
			mcplib.WithDescription("Trigger a force-refresh of synced accounts. Returns the new RefreshStatus."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("accounts_refresh_status",
			mcplib.WithDescription("Check the status of the latest force-refresh request. Returns the new RefreshStatus."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("accounts_types",
			mcplib.WithDescription("List account type and subtype catalog used by Monarch. Returns array of AccountType."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("budgets_list",
			mcplib.WithDescription("List active budgets with budgeted amounts. Returns array of Budget."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("budgets_set_amount",
			mcplib.WithDescription("Set the budgeted amount for a category. Required: category, amount. Optional: start-date."),
			mcplib.WithString("category", mcplib.Required(), mcplib.Description("Category ID to budget")),
			mcplib.WithString("amount", mcplib.Required(), mcplib.Description("Monthly budget amount")),
			mcplib.WithString("start-date", mcplib.Description("Effective start date (defaults to current period)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "category", WireName: "category_id", Location: "query"},{PublicName: "amount", WireName: "amount", Location: "body"},{PublicName: "start-date", WireName: "start_date", Location: "body"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("budgets_status",
			mcplib.WithDescription("Get budget-versus-actual for the active period. Optional: start-date, end-date. Returns the new BudgetStatus."),
			mcplib.WithString("start-date", mcplib.Description("Earliest date (YYYY-MM-DD); defaults to current period")),
			mcplib.WithString("end-date", mcplib.Description("Latest date (YYYY-MM-DD); defaults to current period")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "start-date", WireName: "start_date", Location: "query"},{PublicName: "end-date", WireName: "end_date", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("cashflow_by_category",
			mcplib.WithDescription("Cashflow grouped by category. Optional: start-date, end-date. Returns array of CategoryCashflow."),
			mcplib.WithString("start-date", mcplib.Description("Earliest date (YYYY-MM-DD)")),
			mcplib.WithString("end-date", mcplib.Description("Latest date (YYYY-MM-DD)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "start-date", WireName: "start_date", Location: "query"},{PublicName: "end-date", WireName: "end_date", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("cashflow_summary",
			mcplib.WithDescription("Income vs expenses for a date window with savings rate. Optional: start-date, end-date. Returns the new CashflowSummary."),
			mcplib.WithString("start-date", mcplib.Description("Earliest date (YYYY-MM-DD)")),
			mcplib.WithString("end-date", mcplib.Description("Latest date (YYYY-MM-DD)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "start-date", WireName: "start_date", Location: "query"},{PublicName: "end-date", WireName: "end_date", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("categories_create",
			mcplib.WithDescription("Create a custom category. Required: name, group. Optional: icon. Returns the new Category."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Category display name")),
			mcplib.WithString("group", mcplib.Required(), mcplib.Description("Parent group ID")),
			mcplib.WithString("icon", mcplib.Description("Emoji or icon identifier")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body"},{PublicName: "group", WireName: "group_id", Location: "body"},{PublicName: "icon", WireName: "icon", Location: "body"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("categories_delete",
			mcplib.WithDescription("Delete a category. Required: category-id."),
			mcplib.WithString("category-id", mcplib.Required(), mcplib.Description("Category ID")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "category-id", WireName: "category_id", Location: "query"}, }, []string{"category_id", }),
	)
	s.AddTool(
		mcplib.NewTool("categories_groups",
			mcplib.WithDescription("List category groups (top-level groupings). Returns array of CategoryGroup."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("categories_list",
			mcplib.WithDescription("List all categories (system + custom). Returns array of Category."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("credit_report",
			mcplib.WithDescription("Get the current credit-score and report summary. Returns the new CreditReport."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("goals_create",
			mcplib.WithDescription("Create a savings/debt goal. Required: name, target. Optional: target-date. Returns the new Goal."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Goal name")),
			mcplib.WithString("target", mcplib.Required(), mcplib.Description("Target amount")),
			mcplib.WithString("target-date", mcplib.Description("Target completion date (YYYY-MM-DD)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body"},{PublicName: "target", WireName: "target_amount", Location: "body"},{PublicName: "target-date", WireName: "target_date", Location: "body"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("goals_delete",
			mcplib.WithDescription("Delete a goal. Required: goal-id."),
			mcplib.WithString("goal-id", mcplib.Required(), mcplib.Description("Goal ID")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "goal-id", WireName: "goal_id", Location: "query"}, }, []string{"goal_id", }),
	)
	s.AddTool(
		mcplib.NewTool("goals_list",
			mcplib.WithDescription("List goals with progress and target amounts. Returns array of Goal."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("holdings_list",
			mcplib.WithDescription("List investment holdings with quantity, price, and market value. Optional: account. Returns array of Holding."),
			mcplib.WithString("account", mcplib.Description("Filter by account ID")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "account", WireName: "account_id", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("institutions_list",
			mcplib.WithDescription("List connected institutions and their connection health. Returns array of Institution."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("me_get",
			mcplib.WithDescription("Get the authenticated user and household preferences. Returns the new Me."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("recurring_list",
			mcplib.WithDescription("List active recurring streams with cadence and next-occurrence. Optional: start-date, end-date. Returns array of RecurringStream."),
			mcplib.WithString("start-date", mcplib.Description("Earliest date (YYYY-MM-DD); defaults to today")),
			mcplib.WithString("end-date", mcplib.Description("Latest date (YYYY-MM-DD); defaults to +90 days")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "start-date", WireName: "start_date", Location: "query"},{PublicName: "end-date", WireName: "end_date", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("recurring_search",
			mcplib.WithDescription("Search merchants for adding new recurring streams. Required: query. Returns array of Merchant."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Merchant search text")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "query", WireName: "query", Location: "query"}, }, []string{"query", }),
	)
	s.AddTool(
		mcplib.NewTool("reports_configurations",
			mcplib.WithDescription("List saved report configurations. Returns array of ReportConfiguration."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("reports_data",
			mcplib.WithDescription("Get aggregated reports data with grouping (category, merchant, date). Optional: start-date, end-date, group-by. Returns array of ReportRow."),
			mcplib.WithString("start-date", mcplib.Description("Earliest date (YYYY-MM-DD)")),
			mcplib.WithString("end-date", mcplib.Description("Latest date (YYYY-MM-DD)")),
			mcplib.WithString("group-by", mcplib.Description("Grouping: category, category_group, merchant, month, day (default category)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "start-date", WireName: "start_date", Location: "query"},{PublicName: "end-date", WireName: "end_date", Location: "query"},{PublicName: "group-by", WireName: "group_by", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("subscription_get",
			mcplib.WithDescription("Get current Monarch plan, billing cycle, and trial status. Returns the new Subscription."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("tags_create",
			mcplib.WithDescription("Create a transaction tag. Required: name. Optional: color. Returns the new Tag."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Tag name")),
			mcplib.WithString("color", mcplib.Description("Tag color hex")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body"},{PublicName: "color", WireName: "color", Location: "body"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("tags_list",
			mcplib.WithDescription("List tags with their transaction counts. Returns array of Tag."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("transactions_create",
			mcplib.WithDescription("Create a manual transaction. Required: account_id, amount, date. Optional: merchant, category, notes. Returns the new Transaction."),
			mcplib.WithString("account_id", mcplib.Required(), mcplib.Description("Account ID")),
			mcplib.WithString("amount", mcplib.Required(), mcplib.Description("Signed amount (negative = expense, positive = income)")),
			mcplib.WithString("date", mcplib.Required(), mcplib.Description("Transaction date (YYYY-MM-DD)")),
			mcplib.WithString("merchant", mcplib.Description("Merchant or payee")),
			mcplib.WithString("category", mcplib.Description("Category ID")),
			mcplib.WithString("notes", mcplib.Description("Free-form notes")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "account_id", WireName: "account_id", Location: "body"},{PublicName: "amount", WireName: "amount", Location: "body"},{PublicName: "date", WireName: "date", Location: "body"},{PublicName: "merchant", WireName: "merchant_name", Location: "body"},{PublicName: "category", WireName: "category_id", Location: "body"},{PublicName: "notes", WireName: "notes", Location: "body"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("transactions_delete",
			mcplib.WithDescription("Delete a transaction. Required: transaction-id."),
			mcplib.WithString("transaction-id", mcplib.Required(), mcplib.Description("Transaction ID")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "transaction-id", WireName: "transaction_id", Location: "query"}, }, []string{"transaction_id", }),
	)
	s.AddTool(
		mcplib.NewTool("transactions_get",
			mcplib.WithDescription("Get a transaction with its splits, tags, and rule provenance. Required: transaction-id. Returns the new TransactionDetail."),
			mcplib.WithString("transaction-id", mcplib.Required(), mcplib.Description("Transaction ID")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "transaction-id", WireName: "transaction_id", Location: "query"}, }, []string{"transaction_id", }),
	)
	s.AddTool(
		mcplib.NewTool("transactions_list",
			mcplib.WithDescription("List transactions with filters (date, account, category, merchant, amount, tag). Optional: limit, offset, start-date (plus 9 more). Returns array of Transaction."),
			mcplib.WithString("limit", mcplib.Description("Max transactions to return (default 100)")),
			mcplib.WithString("offset", mcplib.Description("Pagination offset")),
			mcplib.WithString("start-date", mcplib.Description("Earliest date (YYYY-MM-DD)")),
			mcplib.WithString("end-date", mcplib.Description("Latest date (YYYY-MM-DD)")),
			mcplib.WithString("account", mcplib.Description("Filter by account ID (repeatable as comma-separated)")),
			mcplib.WithString("category", mcplib.Description("Filter by category ID (comma-separated for multiple)")),
			mcplib.WithString("merchant", mcplib.Description("Filter by merchant ID")),
			mcplib.WithString("tag", mcplib.Description("Filter by tag ID (comma-separated)")),
			mcplib.WithString("search", mcplib.Description("Plain-text search in description/notes/merchant")),
			mcplib.WithString("min-amount", mcplib.Description("Minimum absolute amount")),
			mcplib.WithString("max-amount", mcplib.Description("Maximum absolute amount")),
			mcplib.WithString("order-by", mcplib.Description("Sort order: date_desc (default), date_asc, amount_desc, amount_asc")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"},{PublicName: "offset", WireName: "offset", Location: "query"},{PublicName: "start-date", WireName: "start_date", Location: "query"},{PublicName: "end-date", WireName: "end_date", Location: "query"},{PublicName: "account", WireName: "account_id", Location: "query"},{PublicName: "category", WireName: "category_id", Location: "query"},{PublicName: "merchant", WireName: "merchant_id", Location: "query"},{PublicName: "tag", WireName: "tag_id", Location: "query"},{PublicName: "search", WireName: "search", Location: "query"},{PublicName: "min-amount", WireName: "min_amount", Location: "query"},{PublicName: "max-amount", WireName: "max_amount", Location: "query"},{PublicName: "order-by", WireName: "order_by", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("transactions_splits_get",
			mcplib.WithDescription("Get the splits for a transaction. Required: transaction-id. Returns array of TransactionSplit."),
			mcplib.WithString("transaction-id", mcplib.Required(), mcplib.Description("Transaction ID")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "transaction-id", WireName: "transaction_id", Location: "query"}, }, []string{"transaction_id", }),
	)
	s.AddTool(
		mcplib.NewTool("transactions_splits_set",
			mcplib.WithDescription("Replace the splits for a transaction. Required: transaction-id, splits-json."),
			mcplib.WithString("transaction-id", mcplib.Required(), mcplib.Description("Transaction ID")),
			mcplib.WithString("splits-json", mcplib.Required(), mcplib.Description("JSON array of {amount, category_id, notes?} objects")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "transaction-id", WireName: "transaction_id", Location: "query"},{PublicName: "splits-json", WireName: "splits_json", Location: "body"}, }, []string{"transaction_id", }),
	)
	s.AddTool(
		mcplib.NewTool("transactions_summary",
			mcplib.WithDescription("Aggregated transaction summary card (counts, totals) for a date window. Optional: start-date, end-date. Returns the new TransactionsSummary."),
			mcplib.WithString("start-date", mcplib.Description("Earliest date (YYYY-MM-DD)")),
			mcplib.WithString("end-date", mcplib.Description("Latest date (YYYY-MM-DD)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "start-date", WireName: "start_date", Location: "query"},{PublicName: "end-date", WireName: "end_date", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("transactions_tags_set",
			mcplib.WithDescription("Set the tags on a transaction (replaces existing). Required: transaction-id, tag-ids."),
			mcplib.WithString("transaction-id", mcplib.Required(), mcplib.Description("Transaction ID")),
			mcplib.WithString("tag-ids", mcplib.Required(), mcplib.Description("Comma-separated tag IDs")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "transaction-id", WireName: "transaction_id", Location: "query"},{PublicName: "tag-ids", WireName: "tag_ids", Location: "body"}, }, []string{"transaction_id", }),
	)
	s.AddTool(
		mcplib.NewTool("transactions_update",
			mcplib.WithDescription("Update a transaction's category, notes, merchant, or tags. Required: transaction-id. Optional: category, merchant, notes (plus 1 more). Returns the new Transaction."),
			mcplib.WithString("transaction-id", mcplib.Required(), mcplib.Description("Transaction ID")),
			mcplib.WithString("category", mcplib.Description("New category ID")),
			mcplib.WithString("merchant", mcplib.Description("New merchant name")),
			mcplib.WithString("notes", mcplib.Description("New notes")),
			mcplib.WithString("hide-from-reports", mcplib.Description("Hide this transaction from reports")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{{PublicName: "transaction-id", WireName: "transaction_id", Location: "query"},{PublicName: "category", WireName: "category_id", Location: "body"},{PublicName: "merchant", WireName: "merchant_name", Location: "body"},{PublicName: "notes", WireName: "notes", Location: "body"},{PublicName: "hide-from-reports", WireName: "hide_from_reports", Location: "body"}, }, []string{"transaction_id", }),
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export MONARCH_TOKEN=<your-key>" +
					"\n      Run 'monarch-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API credentials." +
					"\n      Set it with: export MONARCH_TOKEN=<your-key>" +
					"\n      Run 'monarch-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export MONARCH_TOKEN=<your-key>" +
					"\n      Run 'monarch-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "monarch-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "monarch-pp-cli", "data.db")
}
// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "monarch",
		"description": "Monarch Money CLI with local SQLite store, offline search, and agent-native output across accounts, transactions,...",
		"archetype":   "payments",
		"tool_count":  38,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion monarch-pp-cli binary.",
		"auth": map[string]any{
			"type": "composed",
			"env_vars": []map[string]any{
				{
					"name": "MONARCH_TOKEN",
					"kind": "per_call",
					"required": true,
					"sensitive": true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name": "accounts",
				"description": "All linked and manual accounts (cash, credit, investment, loans, real estate)",
				"endpoints": []string{"balance_history", "list", "networth_snapshots", "refresh", "refresh_status", "types",  },
			},
			{
				"name": "budgets",
				"description": "Per-category and flex budgets, with actuals and variance",
				"endpoints": []string{"list", "set_amount", "status",  },
				"searchable": true,
			},
			{
				"name": "cashflow",
				"description": "Income vs expenses summaries and category-level cashflow",
				"endpoints": []string{"by_category", "summary",  },
			},
			{
				"name": "categories",
				"description": "Transaction categories and category groups",
				"endpoints": []string{"create", "delete", "groups", "list",  },
				"searchable": true,
			},
			{
				"name": "credit",
				"description": "Credit score and report (powered by Spinwheel)",
				"endpoints": []string{"report",  },
			},
			{
				"name": "goals",
				"description": "Savings, debt-paydown, and investment goals",
				"endpoints": []string{"create", "delete", "list",  },
				"searchable": true,
			},
			{
				"name": "holdings",
				"description": "Investment holdings within brokerage accounts",
				"endpoints": []string{"list",  },
			},
			{
				"name": "institutions",
				"description": "Connected financial institutions",
				"endpoints": []string{"list",  },
			},
			{
				"name": "me",
				"description": "Current user and household profile",
				"endpoints": []string{"get",  },
			},
			{
				"name": "recurring",
				"description": "Recurring merchants and bills (subscriptions, rent, paychecks)",
				"endpoints": []string{"list", "search",  },
			},
			{
				"name": "reports",
				"description": "Aggregated reports (spending, income, net worth) over time",
				"endpoints": []string{"configurations", "data",  },
			},
			{
				"name": "subscription",
				"description": "Monarch subscription details for the household",
				"endpoints": []string{"get",  },
			},
			{
				"name": "tags",
				"description": "Transaction tags (with usage counts)",
				"endpoints": []string{"create", "list",  },
				"searchable": true,
			},
			{
				"name": "transactions",
				"description": "Bank, credit, and investment transactions with splits, tags, and categorization",
				"endpoints": []string{"create", "delete", "get", "list", "splits_get", "splits_set", "summary", "tags_set", "update",  },
				"searchable": true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		"playbook": []map[string]string{
			{"topic": "Financial data", "insight": "Always use read-only operations for financial queries. Never use create/update tools for payment data without explicit user confirmation."},
			{"topic": "Reconciliation", "insight": "For reconciliation tasks, sync first then use sql for cross-referencing. API pagination over financial records is slow and rate-limited."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
