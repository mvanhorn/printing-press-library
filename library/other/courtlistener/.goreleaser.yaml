version: 2
project_name: courtlistener-pp-cli
changelog:
  disable: true
builds:
  - id: courtlistener-pp-cli
    main: ./cmd/courtlistener-pp-cli
    binary: courtlistener-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/other/courtlistener/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: courtlistener-pp-mcp
    main: ./cmd/courtlistener-pp-mcp
    binary: courtlistener-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: courtlistener-pp-cli
    repository:
      owner: avanderheyde
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Search case law and query authenticated RECAP litigation records."
    install: |
      bin.install "courtlistener-pp-cli"
      bin.install "courtlistener-pp-mcp"
