// Copyright 2026 avanderheyde and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newEchoRestServicesGetFacilitiesPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagOutput string
	var flagPFn string
	var flagPFntype string
	var flagPFrs string
	var flagPSt string
	var flagPCt string
	var flagPZip string
	var flagPLat float64
	var flagPLong float64
	var flagPRadius float64
	var flagResponseset int

	cmd := &cobra.Command{
		Use:         "echo-rest-services-get-facilities",
		Short:       "Start an all-media facility search",
		Long:        "Start an all-media facility search",
		Example:     "  epa-echo-pp-cli echo-rest-services-get-facilities",
		Annotations: map[string]string{"pp:endpoint": "echo-rest-services-get-facilities.search-facilities", "pp:method": "GET", "pp:path": "/echo_rest_services.get_facilities", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/echo_rest_services.get_facilities"
			params := map[string]string{}
			if flagOutput != "" {
				params["output"] = formatCLIParamValue(flagOutput)
			}
			if flagPFn != "" {
				params["p_fn"] = formatCLIParamValue(flagPFn)
			}
			if flagPFntype != "" {
				params["p_fntype"] = formatCLIParamValue(flagPFntype)
			}
			if flagPFrs != "" {
				params["p_frs"] = formatCLIParamValue(flagPFrs)
			}
			if flagPSt != "" {
				params["p_st"] = formatCLIParamValue(flagPSt)
			}
			if flagPCt != "" {
				params["p_ct"] = formatCLIParamValue(flagPCt)
			}
			if flagPZip != "" {
				params["p_zip"] = formatCLIParamValue(flagPZip)
			}
			if flagPLat != 0.0 {
				params["p_lat"] = formatCLIParamValue(flagPLat)
			}
			if flagPLong != 0.0 {
				params["p_long"] = formatCLIParamValue(flagPLong)
			}
			if flagPRadius != 0.0 {
				params["p_radius"] = formatCLIParamValue(flagPRadius)
			}
			if flagResponseset != 0 {
				params["responseset"] = formatCLIParamValue(flagResponseset)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "echo-rest-services-get-facilities", false, path, params, nil, "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagOutput, "output", "JSON", "Output")
	cmd.Flags().StringVar(&flagPFn, "p-fn", "", "P fn")
	cmd.Flags().StringVar(&flagPFntype, "p-fntype", "CONTAINS", "P fntype")
	cmd.Flags().StringVar(&flagPFrs, "p-frs", "", "P frs")
	cmd.Flags().StringVar(&flagPSt, "p-st", "", "P st")
	cmd.Flags().StringVar(&flagPCt, "p-ct", "", "P ct")
	cmd.Flags().StringVar(&flagPZip, "p-zip", "", "P zip")
	cmd.Flags().Float64Var(&flagPLat, "p-lat", 0.0, "P lat")
	cmd.Flags().Float64Var(&flagPLong, "p-long", 0.0, "P long")
	cmd.Flags().Float64Var(&flagPRadius, "p-radius", 0.0, "P radius")
	cmd.Flags().IntVar(&flagResponseset, "responseset", 25, "Responseset")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
