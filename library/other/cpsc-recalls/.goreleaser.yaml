version: 2
project_name: cpsc-recalls-pp-cli
changelog:
  disable: true
builds:
  - id: cpsc-recalls-pp-cli
    main: ./cmd/cpsc-recalls-pp-cli
    binary: cpsc-recalls-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/other/cpsc-recalls/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: cpsc-recalls-pp-mcp
    main: ./cmd/cpsc-recalls-pp-mcp
    binary: cpsc-recalls-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: cpsc-recalls-pp-cli
    repository:
      owner: avanderheyde
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Query US Consumer Product Safety Commission recall records."
    install: |
      bin.install "cpsc-recalls-pp-cli"
      bin.install "cpsc-recalls-pp-mcp"
