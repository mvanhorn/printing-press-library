// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newNovelEncountersNetworkCmd(flags *rootFlags) *cobra.Command {

	cmd := &cobra.Command{
		Use:         "network <vesselId>",
		Short:       "Builds the at-sea meeting graph for a vessel — which other vessels it encountered, when, and where.",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				return nil
			}
			// Unreachable: newEncountersParentCmd in gfw_encounters_impl.go calls ResetCommands()
			// and re-adds newEncountersNetworkCmd (real impl). This stub remains as a compile-
			// time reference to satisfy the unused-symbol linter.
			return fmt.Errorf("internal error: stub encounters-network reached at runtime")
		},
	}
	return cmd
}
