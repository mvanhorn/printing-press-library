// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newNovelWatchRefreshCmd(flags *rootFlags) *cobra.Command {
	var flagPinned bool

	cmd := &cobra.Command{
		Use:         "refresh",
		Short:       "Re-pull events and insights for watchlisted vessels under a polite throttle.",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				return nil
			}
			// Unreachable: newWatchParentCmd in gfw_watch_impl.go calls ResetCommands()
			// and re-adds newWatchRefreshCmd (real impl). This stub remains as a compile-
			// time reference to satisfy the unused-symbol linter.
			return fmt.Errorf("internal error: stub watch-refresh reached at runtime")
		},
	}
	cmd.Flags().BoolVar(&flagPinned, "pinned", false, "TODO: describe --pinned")
	return cmd
}
