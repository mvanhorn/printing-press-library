// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newGwGetApiCmd(flags *rootFlags) *cobra.Command {
	var flagType string
	var flagCurrency string
	var flagIbEntity string
	var flagMdStatusNonPro string
	var flagFormNumber string
	var flagLanguage string
	var flagAccountId string
	var flagUserName string

	cmd := &cobra.Command{
		Use:   "get-api",
		Short: "Used to query list of enumerations for attributes included within extPositionsTransfers, occupation,...",
		Example: "  ibkr-pp-cli gw get-api",
		Annotations: map[string]string{"pp:endpoint": "gw.get-api", "pp:method": "GET", "pp:path": "/gw/api/v1/enumerations/{enumerationType}", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("type") {
				allowedType := []string{ "exchange-bundles", "business-and-occupation", "employee-track", "fin-info-ranges", "acats", "aton", "market-data", "edd-avt", "prohibited-country", "employee-plans", "questionnaires", "security-questions", "quiz-questions", "wire-instructions", "product-country-bundles", "countries", "states" }
				validType := false
				for _, v := range allowedType {
					if flagType == v {
						validType = true
						break
					}
				}
				if !validType {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "type", flagType, allowedType)
				}
			}
			if cmd.Flags().Changed("language") {
				allowedLanguage := []string{ "en", "ar", "de", "es", "fr", "he", "hu", "it", "ja", "nl", "pt", "ru", "zh_CN", "zh_TW" }
				validLanguage := false
				for _, v := range allowedLanguage {
					if flagLanguage == v {
						validLanguage = true
						break
					}
				}
				if !validLanguage {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "language", flagLanguage, allowedLanguage)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/gw/api/v1/enumerations/{enumerationType}"
			path = replacePathParam(path, "type", fmt.Sprintf("%v", flagType))
			params := map[string]string{}
			if flagCurrency != "" {
				params["currency"] = fmt.Sprintf("%v", flagCurrency)
			}
			if flagIbEntity != "" {
				params["ibEntity"] = fmt.Sprintf("%v", flagIbEntity)
			}
			if flagMdStatusNonPro != "" {
				params["mdStatusNonPro"] = fmt.Sprintf("%v", flagMdStatusNonPro)
			}
			if flagFormNumber != "" {
				params["form-number"] = fmt.Sprintf("%v", flagFormNumber)
			}
			if flagLanguage != "" {
				params["language"] = fmt.Sprintf("%v", flagLanguage)
			}
			if flagAccountId != "" {
				params["accountId"] = fmt.Sprintf("%v", flagAccountId)
			}
			if flagUserName != "" {
				params["userName"] = fmt.Sprintf("%v", flagUserName)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "gw", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagType, "type", "exchange-bundles", "Type (one of: exchange-bundles, business-and-occupation, employee-track, fin-info-ranges, acats, aton, market-data, edd-avt, prohibited-country, employee-plans, questionnaires, security-questions, quiz-questions, wire-instructions, product-country-bundles, countries, states)")
	cmd.Flags().StringVar(&flagCurrency, "currency", "", "Currency")
	cmd.Flags().StringVar(&flagIbEntity, "ib-entity", "", "Ib entity")
	cmd.Flags().StringVar(&flagMdStatusNonPro, "md-status-non-pro", "", "Md status non pro")
	cmd.Flags().StringVar(&flagFormNumber, "form-number", "", "Form number")
	cmd.Flags().StringVar(&flagLanguage, "language", "", "Language (one of: en, ar, de, es, fr, he, hu, it, ja, nl, pt, ru, zh_CN, zh_TW)")
	cmd.Flags().StringVar(&flagAccountId, "account-id", "", "Account id")
	cmd.Flags().StringVar(&flagUserName, "user-name", "", "User name")

	return cmd
}
