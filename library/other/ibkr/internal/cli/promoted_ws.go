// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newWsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagBearerToken string
	var flagOauthToken string

	cmd := &cobra.Command{
		Use:   "ws",
		Short: "Open websocket.",
		Long:  "Shortcut for 'ws open-websocket'. Open websocket.",
		Example: "  ibkr-pp-cli ws",
		Annotations: map[string]string{"pp:endpoint": "ws.open-websocket", "pp:method": "GET", "pp:path": "https://api.ibkr.com/v1/api/ws", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "https://api.ibkr.com/v1/api/ws"
			params := map[string]string{}
			if flagBearerToken != "" {
				params["bearer_token"] = fmt.Sprintf("%v", flagBearerToken)
			}
			if flagOauthToken != "" {
				params["oauth_token"] = fmt.Sprintf("%v", flagOauthToken)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "ws", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagBearerToken, "bearer-token", "", "Required for OAuth 2.0 users. Query parameter defining Bearer Token to authorize connection.")
	cmd.Flags().StringVar(&flagOauthToken, "oauth-token", "", "Required when authroizing with OAuth 1.0a. 8-character OAuth access token.")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
