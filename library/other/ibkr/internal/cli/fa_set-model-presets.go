// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newFaSetModelPresetsCmd(flags *rootFlags) *cobra.Command {
	var bodyAvoidNegativeCashInIndependent bool
	var bodyCloseDivestIndependentPosition bool
	var bodyFullyInvestExistingLongPositions bool
	var bodyKeepModelOpen bool
	var bodyPreferCrossWithIndependent bool
	var bodyPreferTransferFromIndependent bool
	var bodyReqID int
	var bodyRoundAllocationQuantityToExchangeBoardLot bool
	var bodySubscriptionStatus int
	var bodyUseNonBaseCcy bool
	var bodyUseToleranceRange bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "set-model-presets",
		Short: "Set the preset behavior for models.",
		Example: "  ibkr-pp-cli fa set-model-presets",
		Annotations: map[string]string{"pp:endpoint": "fa.set-model-presets", "pp:method": "POST", "pp:path": "/fa/fa-preset/save"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/fa/fa-preset/save"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAvoidNegativeCashInIndependent != false {
					body["avoidNegativeCashInIndependent"] = bodyAvoidNegativeCashInIndependent
				}
				if bodyCloseDivestIndependentPosition != false {
					body["closeDivestIndependentPosition"] = bodyCloseDivestIndependentPosition
				}
				if bodyFullyInvestExistingLongPositions != false {
					body["fullyInvestExistingLongPositions"] = bodyFullyInvestExistingLongPositions
				}
				if bodyKeepModelOpen != false {
					body["keepModelOpen"] = bodyKeepModelOpen
				}
				if bodyPreferCrossWithIndependent != false {
					body["preferCrossWithIndependent"] = bodyPreferCrossWithIndependent
				}
				if bodyPreferTransferFromIndependent != false {
					body["preferTransferFromIndependent"] = bodyPreferTransferFromIndependent
				}
				if bodyReqID != 0 {
					body["reqID"] = bodyReqID
				}
				if bodyRoundAllocationQuantityToExchangeBoardLot != false {
					body["roundAllocationQuantityToExchangeBoardLot"] = bodyRoundAllocationQuantityToExchangeBoardLot
				}
				if bodySubscriptionStatus != 0 {
					body["subscriptionStatus"] = bodySubscriptionStatus
				}
				if bodyUseNonBaseCcy != false {
					body["useNonBaseCcy"] = bodyUseNonBaseCcy
				}
				if bodyUseToleranceRange != false {
					body["useToleranceRange"] = bodyUseToleranceRange
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "fa",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAvoidNegativeCashInIndependent, "avoid-negative-cash-in-independent", false, "Avoid negative offsetting cash in Independent")
	cmd.Flags().BoolVar(&bodyCloseDivestIndependentPosition, "close-divest-independent-position", false, "Close out the full position while divesting.")
	cmd.Flags().BoolVar(&bodyFullyInvestExistingLongPositions, "fully-invest-existing-long-positions", false, "Use the maximum avialble funds to increase long positions.")
	cmd.Flags().BoolVar(&bodyKeepModelOpen, "keep-model-open", false, "Keep model open for fully divested accounts.")
	cmd.Flags().BoolVar(&bodyPreferCrossWithIndependent, "prefer-cross-with-independent", false, "Transfer positions to Independent instead of liquidating.")
	cmd.Flags().BoolVar(&bodyPreferTransferFromIndependent, "prefer-transfer-from-independent", false, "Transfer positions from Independent structure when possible.")
	cmd.Flags().IntVar(&bodyReqID, "req-id", 0, "Request identifier to uniquely track a request.")
	cmd.Flags().BoolVar(&bodyRoundAllocationQuantityToExchangeBoardLot, "round-allocation-quantity-to-exchange-board-lot", false, "Determine if allocation quantities should be handled by lot size.")
	cmd.Flags().IntVar(&bodySubscriptionStatus, "subscription-status", 0, "Describes if the model is in polling mode.")
	cmd.Flags().BoolVar(&bodyUseNonBaseCcy, "use-non-base-ccy", false, "Use non-base balances when available.")
	cmd.Flags().BoolVar(&bodyUseToleranceRange, "use-tolerance-range", false, "Designate of tolerance ranges should be used for rebalancing.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
