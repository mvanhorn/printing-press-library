// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newOauth2PromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyClientAssertion string
	var bodyClientAssertionType string
	var bodyClientAuthenticationMethod string
	var bodyClientId string
	var bodyClientSecret string

	cmd := &cobra.Command{
		Use:   "oauth2",
		Short: "Generate OAuth 2.0 access tokens based on request parameters.",
		Long:  "Shortcut for 'oauth2 generate-token'. Generate OAuth 2.0 access tokens based on request parameters.",
		Example: "  ibkr-pp-cli oauth2",
		Annotations: map[string]string{"pp:endpoint": "oauth2.generate-token", "pp:method": "POST", "pp:path": "/oauth2/api/v1/token"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/oauth2/api/v1/token"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyClientAssertion != "" {
				body["clientAssertion"] = bodyClientAssertion
			}
			if bodyClientAssertionType != "" {
				body["clientAssertionType"] = bodyClientAssertionType
			}
			if bodyClientAuthenticationMethod != "" {
				body["clientAuthenticationMethod"] = bodyClientAuthenticationMethod
			}
			if bodyClientId != "" {
				body["clientId"] = bodyClientId
			}
			if bodyClientSecret != "" {
				body["clientSecret"] = bodyClientSecret
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyClientAssertion, "client-assertion", "", "Client assertion")
	cmd.Flags().StringVar(&bodyClientAssertionType, "client-assertion-type", "", "Client assertion type")
	cmd.Flags().StringVar(&bodyClientAuthenticationMethod, "client-authentication-method", "", "Client authentication method")
	cmd.Flags().StringVar(&bodyClientId, "client-id", "", "Client id")
	cmd.Flags().StringVar(&bodyClientSecret, "client-secret", "", "Client secret")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
