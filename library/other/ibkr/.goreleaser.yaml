version: 2
project_name: ibkr-pp-cli
changelog:
  disable: true
builds:
  - id: ibkr-pp-cli
    main: ./cmd/ibkr-pp-cli
    binary: ibkr-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X ibkr-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: ibkr-pp-mcp
    main: ./cmd/ibkr-pp-mcp
    binary: ibkr-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: ibkr-pp-cli
    repository:
      owner: user
      name: homebrew-tap
    homepage: "https://github.com/user/ibkr-pp-cli"
    description: "The IB REST API reference documentation"
    install: |
      bin.install "ibkr-pp-cli"
      bin.install "ibkr-pp-mcp"
