{
  "manifest_version": "0.3",
  "name": "screener-pp-mcp",
  "display_name": "Screener",
  "version": "0.1.0",
  "description": "Every Screener.in feature, plus offline search, screen intersections, and a local fundamentals mirror no other tool has.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/screener-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/screener-pp-mcp",
      "args": [],
      "env": {
        "PRINTING_PRESS_CLIENT_PROFILE": "${user_config.printing_press_client_profile}",
        "SCREENER_SESSION_COOKIE": "${user_config.screener_session_cookie}"
      }
    }
  },
  "user_config": {
    "printing_press_client_profile": {
      "type": "string",
      "title": "Client profile",
      "description": "Binds the MCP server to an existing tenant-gated Printing Press client profile.",
      "required": true
    },
    "screener_session_cookie": {
      "type": "string",
      "title": "SCREENER_SESSION_COOKIE",
      "description": "Screener.in sessionid/csrftoken cookie for authenticated access.",
      "sensitive": true,
      "required": false
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
