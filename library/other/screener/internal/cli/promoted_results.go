// Copyright 2026 Som Samantray and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newResultsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagP int
	var flagSectors string
	var flagMcap string
	var flagSort string
	var flagSme string
	var flagWatchlist string

	cmd := &cobra.Command{
		Use:         "results",
		Short:       "Latest quarterly results with YOY growth (Sales, EBIDT, Net profit, EPS) and filters",
		Long:        "Latest quarterly results with YOY growth (Sales, EBIDT, Net profit, EPS) and filters",
		Example:     "  screener-pp-cli results latest --sort growth --json",
		Annotations: map[string]string{"pp:endpoint": "results.latest", "pp:method": "GET", "pp:path": "/results/latest/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/results/latest/"
			htmlRequestParams := map[string]string{}
			if flagP != 0 {
				htmlRequestParams["p"] = formatCLIParamValue(flagP)
			}
			if flagSectors != "" {
				htmlRequestParams["sectors"] = formatCLIParamValue(flagSectors)
			}
			if flagMcap != "" {
				htmlRequestParams["mcap"] = formatCLIParamValue(flagMcap)
			}
			if flagSort != "" {
				htmlRequestParams["sort"] = formatCLIParamValue(flagSort)
			}
			if flagSme != "" {
				htmlRequestParams["sme"] = formatCLIParamValue(flagSme)
			}
			if flagWatchlist != "" {
				htmlRequestParams["watchlist"] = formatCLIParamValue(flagWatchlist)
			}
			params := map[string]string{}
			if flagP != 0 {
				params["p"] = formatCLIParamValue(flagP)
			}
			if flagSectors != "" {
				params["sectors"] = formatCLIParamValue(flagSectors)
			}
			if flagMcap != "" {
				params["mcap"] = formatCLIParamValue(flagMcap)
			}
			if flagSort != "" {
				params["sort"] = formatCLIParamValue(flagSort)
			}
			if flagSme != "" {
				params["sme"] = formatCLIParamValue(flagSme)
			}
			if flagWatchlist != "" {
				params["watchlist"] = formatCLIParamValue(flagWatchlist)
			}
			data, prov, err := resolveReadWithStrategyResponsePathAndJSONGuard(cmd.Context(), c, flags, "auto", "results", false, path, params, nil, "", false, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if !flags.dryRun {
				data, err = extractHTMLResponse(data, htmlExtractionOptions{
					Mode:        "links",
					BaseURL:     htmlExtractionRequestURL(c.BaseURL, path, htmlRequestParams),
					ContentType: c.LastContentType(),
					LinkPrefixes: []string{
						"/company/",
					},
					Limit:          0,
					ScriptSelector: "script#__NEXT_DATA__",
					JSONPath:       "",
				})
				if err != nil {
					return err
				}
			}
			outputData := data
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(outputData, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{outputData}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().IntVar(&flagP, "p", 1, "Page number")
	cmd.Flags().StringVar(&flagSectors, "sectors", "", "Sector code filter (e.g. IN080101)")
	cmd.Flags().StringVar(&flagMcap, "mcap", "", "Market cap bucket: 0_to_50, 50_to_500, 500_to_5000, above_5000")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Sort: growth, turnaround, pe, qtr-pe")
	cmd.Flags().StringVar(&flagSme, "sme", "", "Exchange: bse, nse, sme, main-board")
	cmd.Flags().StringVar(&flagWatchlist, "watchlist", "", "Watchlist ID to filter")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
