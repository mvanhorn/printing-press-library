// Copyright 2026 Hunter Veltri and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newRadAnalystPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagPage string
	var flagPerPage int
	var flagCommitteeId string
	var flagAnalystId string
	var flagAnalystShortId string
	var flagTelephoneExt string
	var flagName string
	var flagEmail string
	var flagTitle string
	var flagMinAssignmentUpdateDate string
	var flagMaxAssignmentUpdateDate string
	var flagSortHideNull bool
	var flagSortNullOnly bool
	var flagSortNullsLast bool
	var flagApiKey string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "rad-analyst",
		Short:       "Use this endpoint to look up the RAD Analyst for a committee.",
		Long:        "Use this endpoint to look up the RAD Analyst for a committee.",
		Example:     "  fec-pp-cli rad-analyst",
		Annotations: map[string]string{"pp:endpoint": "rad-analyst.list", "pp:method": "GET", "pp:path": "/v1/rad-analyst/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/rad-analyst/"
			if flagCommitteeId != "" {
				path = appendArrayQueryParam(path, "committee_id", flagCommitteeId, "form", true)
			}
			if flagAnalystId != "" {
				path = appendArrayQueryParam(path, "analyst_id", flagAnalystId, "form", true)
			}
			if flagAnalystShortId != "" {
				path = appendArrayQueryParam(path, "analyst_short_id", flagAnalystShortId, "form", true)
			}
			if flagTelephoneExt != "" {
				path = appendArrayQueryParam(path, "telephone_ext", flagTelephoneExt, "form", true)
			}
			if flagName != "" {
				path = appendArrayQueryParam(path, "name", flagName, "form", true)
			}
			if flagEmail != "" {
				path = appendArrayQueryParam(path, "email", flagEmail, "form", true)
			}
			if flagTitle != "" {
				path = appendArrayQueryParam(path, "title", flagTitle, "form", true)
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "rad-analyst", path, map[string]string{
				"page":                       formatCLIParamValue(flagPage),
				"per_page":                   formatCLIParamValue(flagPerPage),
				"min_assignment_update_date": formatCLIParamValue(flagMinAssignmentUpdateDate),
				"max_assignment_update_date": formatCLIParamValue(flagMaxAssignmentUpdateDate),
				"sort_hide_null":             formatCLIParamValue(flagSortHideNull),
				"sort_null_only":             formatCLIParamValue(flagSortNullOnly),
				"sort_nulls_last":            formatCLIParamValue(flagSortNullsLast),
				"api_key":                    formatCLIParamValue(flagApiKey),
			}, nil, flagAll, "page", "page", "per_page", 20, "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagPage, "page", "1", "For paginating through results, starting at page 1")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 20, "The number of results returned per page. Defaults to 20.")
	cmd.Flags().StringVar(&flagCommitteeId, "committee-id", "", "A unique identifier assigned to each committee or filer registered with the FEC.")
	cmd.Flags().StringVar(&flagAnalystId, "analyst-id", "", "ID of RAD analyst")
	cmd.Flags().StringVar(&flagAnalystShortId, "analyst-short-id", "", "Short ID of RAD analyst")
	cmd.Flags().StringVar(&flagTelephoneExt, "telephone-ext", "", "Telephone extension of RAD analyst")
	cmd.Flags().StringVar(&flagName, "name", "", "Name of RAD analyst")
	cmd.Flags().StringVar(&flagEmail, "email", "", "Email of RAD analyst")
	cmd.Flags().StringVar(&flagTitle, "title", "", "Title of RAD analyst")
	cmd.Flags().StringVar(&flagMinAssignmentUpdateDate, "min-assignment-update-date", "", "Filter results for assignment updates made after this date")
	cmd.Flags().StringVar(&flagMaxAssignmentUpdateDate, "max-assignment-update-date", "", "Filter results for assignment updates made before this date")
	cmd.Flags().BoolVar(&flagSortHideNull, "sort-hide-null", false, "Hide null values on sorted column(s).")
	cmd.Flags().BoolVar(&flagSortNullOnly, "sort-null-only", false, "Toggle that filters out all rows having sort column that is non-null")
	cmd.Flags().BoolVar(&flagSortNullsLast, "sort-nulls-last", false, "Toggle that sorts null values last")
	cmd.Flags().StringVar(&flagApiKey, "api-key", "DEMO_KEY", "API key for https://api.data.gov. Get one at https://api.data.gov/signup.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
