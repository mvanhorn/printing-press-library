// Copyright 2026 Hunter Veltri and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSchedulesListScheduleeCmd(flags *rootFlags) *cobra.Command {
	var flagImageNumber string
	var flagMinImageNumber string
	var flagMaxImageNumber string
	var flagMinAmount float64
	var flagMaxAmount float64
	var flagMinDate string
	var flagMaxDate string
	var flagCandidateOffice string
	var flagCandidateParty string
	var flagCandidateOfficeState string
	var flagCandidateOfficeDistrict string
	var flagCycle string
	var flagCommitteeId string
	var flagCandidateId string
	var flagFilingForm string
	var flagLastExpenditureDate string
	var flagLastExpenditureAmount float64
	var flagLastOfficeTotalYtd float64
	var flagPayeeName string
	var flagSupportOpposeIndicator string
	var flagLastSupportOpposeIndicator string
	var flagIsNotice string
	var flagMinDisseminationDate string
	var flagMaxDisseminationDate string
	var flagMinFilingDate string
	var flagMaxFilingDate string
	var flagMostRecent bool
	var flagQSpender string
	var flagFormLineNumber string
	var flagPerPage int
	var flagLastIndex int
	var flagSort string
	var flagSortHideNull bool
	var flagSortNullOnly bool
	var flagSortNullsLast bool
	var flagApiKey string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list-schedulee",
		Short:       "Schedule E covers the line item expenditures for independent expenditures.",
		Example:     "  fec-pp-cli schedules list-schedulee",
		Annotations: map[string]string{"pp:endpoint": "schedules.list-schedulee", "pp:method": "GET", "pp:path": "/v1/schedules/schedule_e/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			path := "/v1/schedules/schedule_e/"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			if flagImageNumber != "" {
				path = appendArrayQueryParam(path, "image_number", flagImageNumber, "form", true)
			}
			if flagCandidateOffice != "" {
				path = appendArrayQueryParam(path, "candidate_office", flagCandidateOffice, "form", true)
			}
			if flagCandidateParty != "" {
				path = appendArrayQueryParam(path, "candidate_party", flagCandidateParty, "form", true)
			}
			if flagCandidateOfficeState != "" {
				path = appendArrayQueryParam(path, "candidate_office_state", flagCandidateOfficeState, "form", true)
			}
			if flagCandidateOfficeDistrict != "" {
				path = appendArrayQueryParam(path, "candidate_office_district", flagCandidateOfficeDistrict, "form", true)
			}
			if flagCycle != "" {
				path = appendArrayQueryParam(path, "cycle", flagCycle, "form", true)
			}
			if flagCommitteeId != "" {
				path = appendArrayQueryParam(path, "committee_id", flagCommitteeId, "form", true)
			}
			if flagCandidateId != "" {
				path = appendArrayQueryParam(path, "candidate_id", flagCandidateId, "form", true)
			}
			if flagFilingForm != "" {
				path = appendArrayQueryParam(path, "filing_form", flagFilingForm, "form", true)
			}
			if flagPayeeName != "" {
				path = appendArrayQueryParam(path, "payee_name", flagPayeeName, "form", true)
			}
			if flagSupportOpposeIndicator != "" {
				path = appendArrayQueryParam(path, "support_oppose_indicator", flagSupportOpposeIndicator, "form", true)
			}
			if flagIsNotice != "" {
				path = appendArrayQueryParam(path, "is_notice", flagIsNotice, "form", true)
			}
			if flagQSpender != "" {
				path = appendArrayQueryParam(path, "q_spender", flagQSpender, "form", true)
			}
			if flagFormLineNumber != "" {
				path = appendArrayQueryParam(path, "form_line_number", flagFormLineNumber, "form", true)
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "schedules", path, map[string]string{
				"min_image_number":              formatCLIParamValue(flagMinImageNumber),
				"max_image_number":              formatCLIParamValue(flagMaxImageNumber),
				"min_amount":                    formatCLIParamValue(flagMinAmount),
				"max_amount":                    formatCLIParamValue(flagMaxAmount),
				"min_date":                      formatCLIParamValue(flagMinDate),
				"max_date":                      formatCLIParamValue(flagMaxDate),
				"last_expenditure_date":         formatCLIParamValue(flagLastExpenditureDate),
				"last_expenditure_amount":       formatCLIParamValue(flagLastExpenditureAmount),
				"last_office_total_ytd":         formatCLIParamValue(flagLastOfficeTotalYtd),
				"last_support_oppose_indicator": formatCLIParamValue(flagLastSupportOpposeIndicator),
				"min_dissemination_date":        formatCLIParamValue(flagMinDisseminationDate),
				"max_dissemination_date":        formatCLIParamValue(flagMaxDisseminationDate),
				"min_filing_date":               formatCLIParamValue(flagMinFilingDate),
				"max_filing_date":               formatCLIParamValue(flagMaxFilingDate),
				"most_recent":                   formatCLIParamValue(flagMostRecent),
				"per_page":                      formatCLIParamValue(flagPerPage),
				"last_index":                    formatCLIParamValue(flagLastIndex),
				"sort":                          formatCLIParamValue(flagSort),
				"sort_hide_null":                formatCLIParamValue(flagSortHideNull),
				"sort_null_only":                formatCLIParamValue(flagSortNullOnly),
				"sort_nulls_last":               formatCLIParamValue(flagSortNullsLast),
				"api_key":                       formatCLIParamValue(flagApiKey),
			}, nil, flagAll, "", "offset", "per_page", 20, "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagImageNumber, "image-number", "", "An unique identifier for each page where the electronic or paper filing is reported.")
	cmd.Flags().StringVar(&flagMinImageNumber, "min-image-number", "", "Minium image number of the page where the schedule item is reported")
	cmd.Flags().StringVar(&flagMaxImageNumber, "max-image-number", "", "Maxium image number of the page where the schedule item is reported")
	cmd.Flags().Float64Var(&flagMinAmount, "min-amount", 0.0, "Filter for all amounts greater than a value.")
	cmd.Flags().Float64Var(&flagMaxAmount, "max-amount", 0.0, "Filter for all amounts less than a value.")
	cmd.Flags().StringVar(&flagMinDate, "min-date", "", "The earliest date used to filter reported activity.")
	cmd.Flags().StringVar(&flagMaxDate, "max-date", "", "The latest date used to filter reported activity.")
	cmd.Flags().StringVar(&flagCandidateOffice, "candidate-office", "", "Federal office candidate runs for: H, S or P")
	cmd.Flags().StringVar(&flagCandidateParty, "candidate-party", "", "Three-letter code for the party affiliated with a candidate or committee.")
	cmd.Flags().StringVar(&flagCandidateOfficeState, "candidate-office-state", "", "A valid two-letter U.S. state or territory code.")
	cmd.Flags().StringVar(&flagCandidateOfficeDistrict, "candidate-office-district", "", "Two-digit US House distirict of the office the candidate is running for.")
	cmd.Flags().StringVar(&flagCycle, "cycle", "", "Filter records to only those that were applicable to a given two-year period.")
	cmd.Flags().StringVar(&flagCommitteeId, "committee-id", "", "A unique identifier assigned to each committee or filer registered with the FEC.")
	cmd.Flags().StringVar(&flagCandidateId, "candidate-id", "", "A unique identifier assigned to each candidate registered with the FEC.")
	cmd.Flags().StringVar(&flagFilingForm, "filing-form", "", "The form where the underlying data comes from, for example, Form 1 would appear as F1")
	cmd.Flags().StringVar(&flagLastExpenditureDate, "last-expenditure-date", "", "When sorting by `expenditure_date`, this is populated with the `expenditure_date` of the last result.")
	cmd.Flags().Float64Var(&flagLastExpenditureAmount, "last-expenditure-amount", 0.0, "When sorting by `expenditure_amount`, this is populated with the `expenditure_amount` of the last result.")
	cmd.Flags().Float64Var(&flagLastOfficeTotalYtd, "last-office-total-ytd", 0.0, "When sorting by `office_total_ytd`, this is populated with the `office_total_ytd` of the last result.")
	cmd.Flags().StringVar(&flagPayeeName, "payee-name", "", "Name of the entity that received the payment.")
	cmd.Flags().StringVar(&flagSupportOpposeIndicator, "support-oppose-indicator", "", "Explains if the money was spent in order to support or oppose a candidate or candidates.")
	cmd.Flags().StringVar(&flagLastSupportOpposeIndicator, "last-support-oppose-indicator", "", "When sorting by `support_oppose_indicator`, this is populated with the `support_oppose_indicator` of the last result.")
	cmd.Flags().StringVar(&flagIsNotice, "is-notice", "", "Record filed as 24- or 48-hour notice.")
	cmd.Flags().StringVar(&flagMinDisseminationDate, "min-dissemination-date", "", "Selects all items distributed by this committee after this date")
	cmd.Flags().StringVar(&flagMaxDisseminationDate, "max-dissemination-date", "", "Selects all items distributed by this committee before this date")
	cmd.Flags().StringVar(&flagMinFilingDate, "min-filing-date", "", "Selects all filings received after this date")
	cmd.Flags().StringVar(&flagMaxFilingDate, "max-filing-date", "", "Selects all filings received before this date")
	cmd.Flags().BoolVar(&flagMostRecent, "most-recent", false, "The report associated with the transaction is either new or is the most-recently filed amendment.")
	cmd.Flags().StringVar(&flagQSpender, "q-spender", "", "Keyword search for spender name or ID")
	cmd.Flags().StringVar(&flagFormLineNumber, "form-line-number", "", "Filter for form and line number using the following format: `FORM-LINENUMBER`.")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 20, "The number of results returned per page. Defaults to 20.")
	cmd.Flags().IntVar(&flagLastIndex, "last-index", 0, "Index of last result from previous page")
	cmd.Flags().StringVar(&flagSort, "sort", "-expenditure_date", "Provide a field to sort by. Use `-` for descending order.")
	cmd.Flags().BoolVar(&flagSortHideNull, "sort-hide-null", false, "Hide null values on sorted column(s).")
	cmd.Flags().BoolVar(&flagSortNullOnly, "sort-null-only", false, "Toggle that filters out all rows having sort column that is non-null")
	cmd.Flags().BoolVar(&flagSortNullsLast, "sort-nulls-last", false, "Toggle that sorts null values last")
	cmd.Flags().StringVar(&flagApiKey, "api-key", "DEMO_KEY", "API key for https://api.data.gov. Get one at https://api.data.gov/signup.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
