// Copyright 2026 Hunter Veltri and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newEfileListForm1Cmd(flags *rootFlags) *cobra.Command {
	var flagPage string
	var flagPerPage int
	var flagFileNumber string
	var flagCommitteeId string
	var flagCandidateId string
	var flagElectionState string
	var flagCandidateOffice string
	var flagCandidateDistrict string
	var flagCandidateParty string
	var flagImageNumber string
	var flagMinLoadTimestamp string
	var flagMaxLoadTimestamp string
	var flagCommitteeType string
	var flagOrganizationType string
	var flagSort string
	var flagSortHideNull bool
	var flagSortNullOnly bool
	var flagSortNullsLast bool
	var flagApiKey string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list-form1",
		Short:       "Basic information about electronic files coming into the FEC, posted as they are received.",
		Example:     "  fec-pp-cli efile list-form1",
		Annotations: map[string]string{"pp:endpoint": "efile.list-form1", "pp:method": "GET", "pp:path": "/v1/efile/form1/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			path := "/v1/efile/form1/"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			if flagFileNumber != "" {
				path = appendArrayQueryParam(path, "file_number", flagFileNumber, "form", true)
			}
			if flagCommitteeId != "" {
				path = appendArrayQueryParam(path, "committee_id", flagCommitteeId, "form", true)
			}
			if flagCandidateId != "" {
				path = appendArrayQueryParam(path, "candidate_id", flagCandidateId, "form", true)
			}
			if flagElectionState != "" {
				path = appendArrayQueryParam(path, "election_state", flagElectionState, "form", true)
			}
			if flagCandidateOffice != "" {
				path = appendArrayQueryParam(path, "candidate_office", flagCandidateOffice, "form", true)
			}
			if flagCandidateDistrict != "" {
				path = appendArrayQueryParam(path, "candidate_district", flagCandidateDistrict, "form", true)
			}
			if flagCandidateParty != "" {
				path = appendArrayQueryParam(path, "candidate_party", flagCandidateParty, "form", true)
			}
			if flagImageNumber != "" {
				path = appendArrayQueryParam(path, "image_number", flagImageNumber, "form", true)
			}
			if flagCommitteeType != "" {
				path = appendArrayQueryParam(path, "committee_type", flagCommitteeType, "form", true)
			}
			if flagOrganizationType != "" {
				path = appendArrayQueryParam(path, "organization_type", flagOrganizationType, "form", true)
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "efile", path, map[string]string{
				"page":               formatCLIParamValue(flagPage),
				"per_page":           formatCLIParamValue(flagPerPage),
				"min_load_timestamp": formatCLIParamValue(flagMinLoadTimestamp),
				"max_load_timestamp": formatCLIParamValue(flagMaxLoadTimestamp),
				"sort":               formatCLIParamValue(flagSort),
				"sort_hide_null":     formatCLIParamValue(flagSortHideNull),
				"sort_null_only":     formatCLIParamValue(flagSortNullOnly),
				"sort_nulls_last":    formatCLIParamValue(flagSortNullsLast),
				"api_key":            formatCLIParamValue(flagApiKey),
			}, nil, flagAll, "page", "page", "per_page", 20, "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagPage, "page", "1", "For paginating through results, starting at page 1")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 20, "The number of results returned per page. Defaults to 20.")
	cmd.Flags().StringVar(&flagFileNumber, "file-number", "", "Filing ID number")
	cmd.Flags().StringVar(&flagCommitteeId, "committee-id", "", "A unique identifier assigned to each committee or filer registered with the FEC.")
	cmd.Flags().StringVar(&flagCandidateId, "candidate-id", "", "A unique identifier assigned to each candidate registered with the FEC.")
	cmd.Flags().StringVar(&flagElectionState, "election-state", "", "State or territory of the office sought.")
	cmd.Flags().StringVar(&flagCandidateOffice, "candidate-office", "", "Federal office candidate runs for: H, S or P")
	cmd.Flags().StringVar(&flagCandidateDistrict, "candidate-district", "", "House district of the office sought, if applicable.")
	cmd.Flags().StringVar(&flagCandidateParty, "candidate-party", "", "Three-letter code for the party affiliated with a candidate or committee.")
	cmd.Flags().StringVar(&flagImageNumber, "image-number", "", "An unique identifier for each page where the electronic or paper filing is reported.")
	cmd.Flags().StringVar(&flagMinLoadTimestamp, "min-load-timestamp", "", "Date the information was loaded into the FEC systems.")
	cmd.Flags().StringVar(&flagMaxLoadTimestamp, "max-load-timestamp", "", "Date the information was loaded into the FEC systems.")
	cmd.Flags().StringVar(&flagCommitteeType, "committee-type", "", "The one-letter type code of the organization")
	cmd.Flags().StringVar(&flagOrganizationType, "organization-type", "", "The one-letter code for the kind for organization")
	cmd.Flags().StringVar(&flagSort, "sort", "-load_timestamp", "Provide a field to sort by. Use `-` for descending order.")
	cmd.Flags().BoolVar(&flagSortHideNull, "sort-hide-null", false, "Hide null values on sorted column(s).")
	cmd.Flags().BoolVar(&flagSortNullOnly, "sort-null-only", false, "Toggle that filters out all rows having sort column that is non-null")
	cmd.Flags().BoolVar(&flagSortNullsLast, "sort-nulls-last", false, "Toggle that sorts null values last")
	cmd.Flags().StringVar(&flagApiKey, "api-key", "DEMO_KEY", "API key for https://api.data.gov. Get one at https://api.data.gov/signup.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
