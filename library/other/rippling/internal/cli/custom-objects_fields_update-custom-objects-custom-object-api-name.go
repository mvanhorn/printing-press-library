// Copyright 2026 carter. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCustomObjectsFieldsUpdateCustomObjectsCustomObjectApiNameCmd(flags *rootFlags) *cobra.Command {
	var bodyDataType string
	var bodyDerivedFieldFormula string
	var bodyDescription string
	var bodyEnableHistory bool
	var bodyFormulaAttrMetas string
	var bodyIsUnique bool
	var bodyName string
	var bodyNameFieldDetails string
	var bodyRequired bool
	var bodyRqlDefinition string
	var bodySection string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "update-custom-objects-custom-object-api-name <custom_object_api_name> <field_api_name>",
		Aliases: []string{"update"},
		Short: "Update a specific custom object field.",
		Example: "  rippling-pp-cli custom-objects fields update-custom-objects-custom-object-api-name example-resource example-resource",
		Annotations: map[string]string{"pp:endpoint": "fields.update-custom-objects-custom-object-api-name", "pp:method": "PATCH", "pp:path": "/custom-objects/{custom_object_api_name}/fields/{field_api_name}/"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/custom-objects/{custom_object_api_name}/fields/{field_api_name}/"
			path = replacePathParam(path, "custom_object_api_name", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("field_api_name is required\nUsage: %s <%s>", cmd.CommandPath(), "field_api_name"))
			}
			path = replacePathParam(path, "field_api_name", args[1])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyDataType != "" {
					var parsedDataType any
					if err := json.Unmarshal([]byte(bodyDataType), &parsedDataType); err != nil {
						return fmt.Errorf("parsing --data-type JSON: %w", err)
					}
					body["data_type"] = parsedDataType
				}
				if bodyDerivedFieldFormula != "" {
					body["derived_field_formula"] = bodyDerivedFieldFormula
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyEnableHistory != false {
					body["enable_history"] = bodyEnableHistory
				}
				if bodyFormulaAttrMetas != "" {
					var parsedFormulaAttrMetas any
					if err := json.Unmarshal([]byte(bodyFormulaAttrMetas), &parsedFormulaAttrMetas); err != nil {
						return fmt.Errorf("parsing --formula-attr-metas JSON: %w", err)
					}
					body["formula_attr_metas"] = parsedFormulaAttrMetas
				}
				if bodyIsUnique != false {
					body["is_unique"] = bodyIsUnique
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyNameFieldDetails != "" {
					var parsedNameFieldDetails any
					if err := json.Unmarshal([]byte(bodyNameFieldDetails), &parsedNameFieldDetails); err != nil {
						return fmt.Errorf("parsing --name-field-details JSON: %w", err)
					}
					body["name_field_details"] = parsedNameFieldDetails
				}
				if bodyRequired != false {
					body["required"] = bodyRequired
				}
				if bodyRqlDefinition != "" {
					var parsedRqlDefinition any
					if err := json.Unmarshal([]byte(bodyRqlDefinition), &parsedRqlDefinition); err != nil {
						return fmt.Errorf("parsing --rql-definition JSON: %w", err)
					}
					body["rql_definition"] = parsedRqlDefinition
				}
				if bodySection != "" {
					var parsedSection any
					if err := json.Unmarshal([]byte(bodySection), &parsedSection); err != nil {
						return fmt.Errorf("parsing --section JSON: %w", err)
					}
					body["section"] = parsedSection
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "fields",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyDataType, "data-type", "", "The data type configuration for the field.")
	cmd.Flags().StringVar(&bodyDerivedFieldFormula, "derived-field-formula", "", "The formula for derived fields.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A description of the field.")
	cmd.Flags().BoolVar(&bodyEnableHistory, "enable-history", false, "Whether to track history for this field.")
	cmd.Flags().StringVar(&bodyFormulaAttrMetas, "formula-attr-metas", "", "Metadata for formula attributes.")
	cmd.Flags().BoolVar(&bodyIsUnique, "is-unique", false, "Whether field values must be unique.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the field.")
	cmd.Flags().StringVar(&bodyNameFieldDetails, "name-field-details", "", "Configuration for name field display.")
	cmd.Flags().BoolVar(&bodyRequired, "required", false, "Whether this field is required.")
	cmd.Flags().StringVar(&bodyRqlDefinition, "rql-definition", "", "The RQL definition for computed fields.")
	cmd.Flags().StringVar(&bodySection, "section", "", "The section configuration for the field.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
