// Copyright 2026 carter. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newLeaveRequestsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyComments string
	var bodyEndDate string
	var bodyEndDateCustomHours float64
	var bodyEndTime string
	var bodyLeaveEventId string
	var bodyLeavePolicyId string
	var bodyLeaveTypeId string
	var bodyReasonForLeave string
	var bodyRequesterId string
	var bodyReviewedAt string
	var bodyReviewerId string
	var bodyStartDate string
	var bodyStartDateCustomHours float64
	var bodyStartTime string
	var bodyStatus string
	var bodyWorkerId string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "create",
		Short: "Create a new leave request",
		Example: "  rippling-pp-cli leave-requests create --end-date 2026-01-15",
		Annotations: map[string]string{"pp:endpoint": "leave-requests.create", "pp:method": "POST", "pp:path": "/leave-requests/"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("end-date") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "end-date")
				}
				if !cmd.Flags().Changed("start-date") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "start-date")
				}
				if !cmd.Flags().Changed("status") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "status")
				}
				if !cmd.Flags().Changed("worker-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "worker-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/leave-requests/"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyComments != "" {
					body["comments"] = bodyComments
				}
				if bodyEndDate != "" {
					body["end_date"] = bodyEndDate
				}
				if bodyEndDateCustomHours != 0.0 {
					body["end_date_custom_hours"] = bodyEndDateCustomHours
				}
				if bodyEndTime != "" {
					body["end_time"] = bodyEndTime
				}
				if bodyLeaveEventId != "" {
					body["leave_event_id"] = bodyLeaveEventId
				}
				if bodyLeavePolicyId != "" {
					body["leave_policy_id"] = bodyLeavePolicyId
				}
				if bodyLeaveTypeId != "" {
					body["leave_type_id"] = bodyLeaveTypeId
				}
				if bodyReasonForLeave != "" {
					body["reason_for_leave"] = bodyReasonForLeave
				}
				if bodyRequesterId != "" {
					body["requester_id"] = bodyRequesterId
				}
				if bodyReviewedAt != "" {
					body["reviewed_at"] = bodyReviewedAt
				}
				if bodyReviewerId != "" {
					body["reviewer_id"] = bodyReviewerId
				}
				if bodyStartDate != "" {
					body["start_date"] = bodyStartDate
				}
				if bodyStartDateCustomHours != 0.0 {
					body["start_date_custom_hours"] = bodyStartDateCustomHours
				}
				if bodyStartTime != "" {
					body["start_time"] = bodyStartTime
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodyWorkerId != "" {
					body["worker_id"] = bodyWorkerId
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "leave-requests",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyComments, "comments", "", "The comments associated with the leave request.")
	cmd.Flags().StringVar(&bodyEndDate, "end-date", "", "The end date of the leave request.")
	cmd.Flags().Float64Var(&bodyEndDateCustomHours, "end-date-custom-hours", 0.0, "The number of hours to take off on the end date.")
	cmd.Flags().StringVar(&bodyEndTime, "end-time", "", "The end time of the leave request.")
	cmd.Flags().StringVar(&bodyLeaveEventId, "leave-event-id", "", "The ID of the leave event associated with the leave request, if any.")
	cmd.Flags().StringVar(&bodyLeavePolicyId, "leave-policy-id", "", "The ID of the leave policy associated with the leave request, either leave_policy_id or leave_type_id must be provided.")
	cmd.Flags().StringVar(&bodyLeaveTypeId, "leave-type-id", "", "The ID of the leave type associated with the leave request, either leave_policy_id or leave_type_id must be provided.")
	cmd.Flags().StringVar(&bodyReasonForLeave, "reason-for-leave", "", "The reason for the leave request.")
	cmd.Flags().StringVar(&bodyRequesterId, "requester-id", "", "The ID of the worker who requested the leave request.")
	cmd.Flags().StringVar(&bodyReviewedAt, "reviewed-at", "", "The timestamp of when the leave request was reviewed.")
	cmd.Flags().StringVar(&bodyReviewerId, "reviewer-id", "", "The ID of the worker who reviewed the leave request.")
	cmd.Flags().StringVar(&bodyStartDate, "start-date", "", "The start date of the leave request.")
	cmd.Flags().Float64Var(&bodyStartDateCustomHours, "start-date-custom-hours", 0.0, "The number of hours to take off on the start date.")
	cmd.Flags().StringVar(&bodyStartTime, "start-time", "", "The start time of the leave request.")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "The status of the leave request.")
	cmd.Flags().StringVar(&bodyWorkerId, "worker-id", "", "The ID of the worker associated with the leave request.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
