// Copyright 2026 carter. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newEarningsInputsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAmount string
	var bodyAssignmentType string
	var bodyCompanyEntityId string
	var bodyEarningCode string
	var bodyExternalId string
	var bodyHours string
	var bodyIsOverridden bool
	var bodyPayPeriodBasisDate string
	var bodyRunId string
	var bodyStatus string
	var bodyTimeLabels string
	var bodyWorkerId string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "create",
		Short: "Create a new earnings-input",
		Example: "  rippling-pp-cli earnings-inputs create",
		Annotations: map[string]string{"pp:endpoint": "earnings-inputs.create", "pp:method": "POST", "pp:path": "/earnings-inputs/"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/earnings-inputs/"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAmount != "" {
					body["amount"] = bodyAmount
				}
				if bodyAssignmentType != "" {
					body["assignment_type"] = bodyAssignmentType
				}
				if bodyCompanyEntityId != "" {
					body["company_entity_id"] = bodyCompanyEntityId
				}
				if bodyEarningCode != "" {
					body["earning_code"] = bodyEarningCode
				}
				if bodyExternalId != "" {
					body["external_id"] = bodyExternalId
				}
				if bodyHours != "" {
					body["hours"] = bodyHours
				}
				if bodyIsOverridden != false {
					body["is_overridden"] = bodyIsOverridden
				}
				if bodyPayPeriodBasisDate != "" {
					body["pay_period_basis_date"] = bodyPayPeriodBasisDate
				}
				if bodyRunId != "" {
					body["run_id"] = bodyRunId
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodyTimeLabels != "" {
					var parsedTimeLabels any
					if err := json.Unmarshal([]byte(bodyTimeLabels), &parsedTimeLabels); err != nil {
						return fmt.Errorf("parsing --time-labels JSON: %w", err)
					}
					body["time_labels"] = parsedTimeLabels
				}
				if bodyWorkerId != "" {
					body["worker_id"] = bodyWorkerId
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "earnings-inputs",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAmount, "amount", "", "Earning amount as a string-encoded decimal.")
	cmd.Flags().StringVar(&bodyAssignmentType, "assignment-type", "", "How the earning is assigned (e.g. BY_PAY_PERIOD).")
	cmd.Flags().StringVar(&bodyCompanyEntityId, "company-entity-id", "", "Identifier for the legal entity (company entity relationship) this earning is associated with. Use this to correlate...")
	cmd.Flags().StringVar(&bodyEarningCode, "earning-code", "", "Code identifying the earning type (e.g. regular_salary).")
	cmd.Flags().StringVar(&bodyExternalId, "external-id", "", "External reference identifier for this earning.")
	cmd.Flags().StringVar(&bodyHours, "hours", "", "Hours associated with this earning as a string-encoded decimal.")
	cmd.Flags().BoolVar(&bodyIsOverridden, "is-overridden", false, "Whether this earning's amount was manually overridden instead of using the calculated value. Always false for newly...")
	cmd.Flags().StringVar(&bodyPayPeriodBasisDate, "pay-period-basis-date", "", "The pay period basis date for this earning.")
	cmd.Flags().StringVar(&bodyRunId, "run-id", "", "Identifier of the payroll run this earning is assigned to, if any.")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Current status of the earnings input (e.g. UNASSIGNED, ASSIGNED).")
	cmd.Flags().StringVar(&bodyTimeLabels, "time-labels", "", "Time tracking labels associated with this earning.")
	cmd.Flags().StringVar(&bodyWorkerId, "worker-id", "", "Identifier of the worker (role ID).")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
