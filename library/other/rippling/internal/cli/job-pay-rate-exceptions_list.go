// Copyright 2026 carter. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newJobPayRateExceptionsListCmd(flags *rootFlags) *cobra.Command {
	var flagFilter string
	var flagExpand string
	var flagOrderBy string
	var flagCursor string
	var flagAll bool

	cmd := &cobra.Command{
		Use:   "list",
		Short: "A list of job pay rate exceptions. - Requires: `API Tier 2` - Filterable fields: `job_code_id` - Expandable fields:...",
		Example: "  rippling-pp-cli job-pay-rate-exceptions list",
		Annotations: map[string]string{"pp:endpoint": "job-pay-rate-exceptions.list", "pp:method": "GET", "pp:path": "/job-pay-rate-exceptions/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/job-pay-rate-exceptions/"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "job-pay-rate-exceptions", path, map[string]string{
				"filter": fmt.Sprintf("%v", flagFilter),
				"expand": fmt.Sprintf("%v", flagExpand),
				"order_by": fmt.Sprintf("%v", flagOrderBy),
				"cursor": fmt.Sprintf("%v", flagCursor),
			}, nil, flagAll, "cursor", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagFilter, "filter", "", "Filter")
	cmd.Flags().StringVar(&flagExpand, "expand", "", "Expand")
	cmd.Flags().StringVar(&flagOrderBy, "order-by", "", "Order by")
	cmd.Flags().StringVar(&flagCursor, "cursor", "", "Cursor")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
