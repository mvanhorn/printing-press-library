// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newListingsListCmd(flags *rootFlags) *cobra.Command {
	var flagAl int
	var flagMarket string
	var flagRegionId string
	var flagRegionType int
	var flagStatus int
	var flagMinPrice int
	var flagMaxPrice int
	var flagNumBeds int
	var flagMaxNumBeds int
	var flagNumBaths float64
	var flagMinListingApproxSize int
	var flagMaxListingApproxSize int
	var flagMinYearBuilt int
	var flagMaxYearBuilt int
	var flagUipt string
	var flagSf string
	var flagHoa int
	var flagNumHomes int
	var flagPageNumber int
	var flagOrd string
	var flagV int

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Search properties by geographic filters",
		Example:     "  redfin-pp-cli listings list",
		Annotations: map[string]string{"pp:endpoint": "listings.list", "pp:method": "GET", "pp:path": "/api/gis", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/gis"
			params := map[string]string{}
			if flagAl != 0 {
				params["al"] = fmt.Sprintf("%v", flagAl)
			}
			if flagMarket != "" {
				params["market"] = fmt.Sprintf("%v", flagMarket)
			}
			if flagRegionId != "" {
				params["region_id"] = fmt.Sprintf("%v", flagRegionId)
			}
			if flagRegionType != 0 {
				params["region_type"] = fmt.Sprintf("%v", flagRegionType)
			}
			if flagStatus != 0 {
				params["status"] = fmt.Sprintf("%v", flagStatus)
			}
			if flagMinPrice != 0 {
				params["min_price"] = fmt.Sprintf("%v", flagMinPrice)
			}
			if flagMaxPrice != 0 {
				params["max_price"] = fmt.Sprintf("%v", flagMaxPrice)
			}
			if flagNumBeds != 0 {
				params["num_beds"] = fmt.Sprintf("%v", flagNumBeds)
			}
			if flagMaxNumBeds != 0 {
				params["max_num_beds"] = fmt.Sprintf("%v", flagMaxNumBeds)
			}
			if flagNumBaths != 0.0 {
				params["num_baths"] = fmt.Sprintf("%v", flagNumBaths)
			}
			if flagMinListingApproxSize != 0 {
				params["min_listing_approx_size"] = fmt.Sprintf("%v", flagMinListingApproxSize)
			}
			if flagMaxListingApproxSize != 0 {
				params["max_listing_approx_size"] = fmt.Sprintf("%v", flagMaxListingApproxSize)
			}
			if flagMinYearBuilt != 0 {
				params["min_year_built"] = fmt.Sprintf("%v", flagMinYearBuilt)
			}
			if flagMaxYearBuilt != 0 {
				params["max_year_built"] = fmt.Sprintf("%v", flagMaxYearBuilt)
			}
			if flagUipt != "" {
				params["uipt"] = fmt.Sprintf("%v", flagUipt)
			}
			if flagSf != "" {
				params["sf"] = fmt.Sprintf("%v", flagSf)
			}
			if flagHoa != 0 {
				params["hoa"] = fmt.Sprintf("%v", flagHoa)
			}
			if flagNumHomes != 0 {
				params["num_homes"] = fmt.Sprintf("%v", flagNumHomes)
			}
			if flagPageNumber != 0 {
				params["page_number"] = fmt.Sprintf("%v", flagPageNumber)
			}
			if flagOrd != "" {
				params["ord"] = fmt.Sprintf("%v", flagOrd)
			}
			if flagV != 0 {
				params["v"] = fmt.Sprintf("%v", flagV)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "listings", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagAl, "al", 0, "Include assessment and loan data (1=yes)")
	cmd.Flags().StringVar(&flagMarket, "market", "", "Market/city name for context")
	cmd.Flags().StringVar(&flagRegionId, "region-id", "", "Numeric region identifier from location-autocomplete")
	cmd.Flags().IntVar(&flagRegionType, "region-type", 0, "Region type code (2=zip, 6=city, 5=county, 11=neighborhood, 1=country)")
	cmd.Flags().IntVar(&flagStatus, "status", 0, "Listing status (1=active, 8=sold, 9=all)")
	cmd.Flags().IntVar(&flagMinPrice, "min-price", 0, "Minimum list price in dollars")
	cmd.Flags().IntVar(&flagMaxPrice, "max-price", 0, "Maximum list price in dollars")
	cmd.Flags().IntVar(&flagNumBeds, "num-beds", 0, "Minimum number of bedrooms")
	cmd.Flags().IntVar(&flagMaxNumBeds, "max-num-beds", 0, "Maximum number of bedrooms")
	cmd.Flags().Float64Var(&flagNumBaths, "num-baths", 0.0, "Minimum number of bathrooms")
	cmd.Flags().IntVar(&flagMinListingApproxSize, "min-listing-approx-size", 0, "Minimum square footage")
	cmd.Flags().IntVar(&flagMaxListingApproxSize, "max-listing-approx-size", 0, "Maximum square footage")
	cmd.Flags().IntVar(&flagMinYearBuilt, "min-year-built", 0, "Minimum year built")
	cmd.Flags().IntVar(&flagMaxYearBuilt, "max-year-built", 0, "Maximum year built")
	cmd.Flags().StringVar(&flagUipt, "uipt", "", "Property type (1=house, 2=condo, 3=townhouse, 4=multi-family, 5=land, 6=other)")
	cmd.Flags().StringVar(&flagSf, "sf", "", "Feature flags comma-separated (1=has_garage, 4=has_pool, 5=has_basement)")
	cmd.Flags().IntVar(&flagHoa, "hoa", 0, "Max HOA fee in dollars (0 = no HOA)")
	cmd.Flags().IntVar(&flagNumHomes, "num-homes", 0, "Number of results per page")
	cmd.Flags().IntVar(&flagPageNumber, "page-number", 0, "Page number for pagination")
	cmd.Flags().StringVar(&flagOrd, "ord", "", "Sort order (days-on-redfin-asc, price-asc, price-desc, newest)")
	cmd.Flags().IntVar(&flagV, "v", 0, "API version")

	return cmd
}
