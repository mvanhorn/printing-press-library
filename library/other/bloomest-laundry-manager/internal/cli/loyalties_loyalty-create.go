// Copyright 2026 andrea-tagliazucchi. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newLoyaltiesLoyaltyCreateCmd(flags *rootFlags) *cobra.Command {
	var flagStart bool
	var bodyAmount float64
	var bodyAward string
	var bodyDaysOfAward int
	var bodyEndDate string
	var bodyErogationAtTheEnd bool
	var bodyIdLoyalty int
	var bodyIsEnabled bool
	var bodyLaundries string
	var bodyName string
	var bodyNotifications string
	var bodyService string
	var bodyStartDate string
	var bodyTarget string
	var bodyTargetUserGroups string
	var bodyThreshold float64
	var bodyType int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "loyalty-create",
		Aliases:     []string{"create"},
		Short:       "Crea / aggiorna promozione",
		Example:     "  bloomest-laundry-manager-pp-cli loyalties loyalty-create",
		Annotations: map[string]string{"pp:endpoint": "loyalties.loyalty-create", "pp:method": "POST", "pp:path": "/loyalties"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/loyalties"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAmount != 0.0 {
					body["amount"] = bodyAmount
				}
				if bodyAward != "" {
					var parsedAward any
					if err := json.Unmarshal([]byte(bodyAward), &parsedAward); err != nil {
						return fmt.Errorf("parsing --award JSON: %w", err)
					}
					body["award"] = parsedAward
				}
				if bodyDaysOfAward != 0 {
					body["daysOfAward"] = bodyDaysOfAward
				}
				if bodyEndDate != "" {
					body["endDate"] = bodyEndDate
				}
				if bodyErogationAtTheEnd != false {
					body["erogationAtTheEnd"] = bodyErogationAtTheEnd
				}
				if bodyIdLoyalty != 0 {
					body["idLoyalty"] = bodyIdLoyalty
				}
				if bodyIsEnabled != false {
					body["isEnabled"] = bodyIsEnabled
				}
				if bodyLaundries != "" {
					var parsedLaundries any
					if err := json.Unmarshal([]byte(bodyLaundries), &parsedLaundries); err != nil {
						return fmt.Errorf("parsing --laundries JSON: %w", err)
					}
					body["laundries"] = parsedLaundries
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyNotifications != "" {
					var parsedNotifications any
					if err := json.Unmarshal([]byte(bodyNotifications), &parsedNotifications); err != nil {
						return fmt.Errorf("parsing --notifications JSON: %w", err)
					}
					body["notifications"] = parsedNotifications
				}
				if bodyService != "" {
					var parsedService any
					if err := json.Unmarshal([]byte(bodyService), &parsedService); err != nil {
						return fmt.Errorf("parsing --service JSON: %w", err)
					}
					body["service"] = parsedService
				}
				if bodyStartDate != "" {
					body["startDate"] = bodyStartDate
				}
				if bodyTarget != "" {
					var parsedTarget any
					if err := json.Unmarshal([]byte(bodyTarget), &parsedTarget); err != nil {
						return fmt.Errorf("parsing --target JSON: %w", err)
					}
					body["target"] = parsedTarget
				}
				if bodyTargetUserGroups != "" {
					var parsedTargetUserGroups any
					if err := json.Unmarshal([]byte(bodyTargetUserGroups), &parsedTargetUserGroups); err != nil {
						return fmt.Errorf("parsing --target-user-groups JSON: %w", err)
					}
					body["targetUserGroups"] = parsedTargetUserGroups
				}
				if bodyThreshold != 0.0 {
					body["threshold"] = bodyThreshold
				}
				if bodyType != 0 {
					body["type"] = bodyType
				}
			}
			params := map[string]string{}
			if flagStart != false {
				params["start"] = fmt.Sprintf("%v", flagStart)
			}
			data, statusCode, err := c.PostWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "loyalties",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&flagStart, "start", false, "Start")
	cmd.Flags().Float64Var(&bodyAmount, "amount", 0.0, "Amount")
	cmd.Flags().StringVar(&bodyAward, "award", "", "Award")
	cmd.Flags().IntVar(&bodyDaysOfAward, "days-of-award", 0, "Days of award")
	cmd.Flags().StringVar(&bodyEndDate, "end-date", "", "End date")
	cmd.Flags().BoolVar(&bodyErogationAtTheEnd, "erogation-at-the-end", false, "Erogation at the end")
	cmd.Flags().IntVar(&bodyIdLoyalty, "id-loyalty", 0, "Id loyalty")
	cmd.Flags().BoolVar(&bodyIsEnabled, "is-enabled", false, "Is enabled")
	cmd.Flags().StringVar(&bodyLaundries, "laundries", "", "Laundries")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyNotifications, "notifications", "", "Notifications")
	cmd.Flags().StringVar(&bodyService, "service", "", "Service")
	cmd.Flags().StringVar(&bodyStartDate, "start-date", "", "Start date")
	cmd.Flags().StringVar(&bodyTarget, "target", "", "Target")
	cmd.Flags().StringVar(&bodyTargetUserGroups, "target-user-groups", "", "Target user groups")
	cmd.Flags().Float64Var(&bodyThreshold, "threshold", 0.0, "Threshold")
	cmd.Flags().IntVar(&bodyType, "type", 0, "Tipo loyalty (1-4 secondo ANALISI § 4)")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
