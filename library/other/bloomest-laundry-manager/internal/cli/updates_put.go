// Copyright 2026 andrea-tagliazucchi. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newUpdatesPutCmd(flags *rootFlags) *cobra.Command {
	var flagIdLCS string
	var flagKey string
	var bodyFirmware int
	var bodyOperationDetails string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "put",
		Aliases:     []string{"update"},
		Short:       "Aggiorna firmware o configurazione device",
		Example:     "  bloomest-laundry-manager-pp-cli updates put --id-lcs example-value --key your-token-here",
		Annotations: map[string]string{"pp:endpoint": "updates.put", "pp:method": "PUT", "pp:path": "/updates"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("id-lcs") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "id-lcs")
			}
			if !cmd.Flags().Changed("key") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "key")
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/updates"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyFirmware != 0 {
					body["firmware"] = bodyFirmware
				}
				if bodyOperationDetails != "" {
					var parsedOperationDetails any
					if err := json.Unmarshal([]byte(bodyOperationDetails), &parsedOperationDetails); err != nil {
						return fmt.Errorf("parsing --operation-details JSON: %w", err)
					}
					body["operationDetails"] = parsedOperationDetails
				}
			}
			params := map[string]string{}
			if flagIdLCS != "" {
				params["idLCS"] = fmt.Sprintf("%v", flagIdLCS)
			}
			if flagKey != "" {
				params["key"] = fmt.Sprintf("%v", flagKey)
			}
			data, statusCode, err := c.PutWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "updates",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagIdLCS, "id-lcs", "", "Serial number LCS (Laundry Control System)")
	cmd.Flags().StringVar(&flagKey, "key", "", "Key")
	cmd.Flags().IntVar(&bodyFirmware, "firmware", 0, "Firmware")
	cmd.Flags().StringVar(&bodyOperationDetails, "operation-details", "", "Operation details")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
