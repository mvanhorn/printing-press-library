// Copyright 2026 andrea-tagliazucchi. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	"bloomest-laundry-manager-pp-cli/internal/cli"
	"bloomest-laundry-manager-pp-cli/internal/client"
	"bloomest-laundry-manager-pp-cli/internal/cliutil"
	"bloomest-laundry-manager-pp-cli/internal/config"
	"bloomest-laundry-manager-pp-cli/internal/mcp/cobratree"
	"bloomest-laundry-manager-pp-cli/internal/store"
	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("accesses_access-delete",
			mcplib.WithDescription("Cancella utente gestionale. Required: user. Returns the Empty. Destructive."),
			mcplib.WithString("user", mcplib.Required(), mcplib.Description("User")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/accesses", []mcpParamBinding{{PublicName: "user", WireName: "user", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("accesses_access-save",
			mcplib.WithDescription("Crea / aggiorna utente gestionale. Optional: disabled, idUser, laundries (plus 3 more). Returns the new Empty."),
			mcplib.WithString("disabled", mcplib.Description("Disabled")),
			mcplib.WithString("idUser", mcplib.Description("Id user")),
			mcplib.WithString("laundries", mcplib.Description("Laundries")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("password", mcplib.Description("SHA1 hex")),
			mcplib.WithString("userLevel", mcplib.Description("1=ADM, 2=CAT, 3=GES, 6=GES_EXE, 7=LCS_MOBILE, 9=OEM, 10=GES_OPERATOR, 11=PERFORMANCE")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/accesses", []mcpParamBinding{{PublicName: "disabled", WireName: "disabled", Location: "body"}, {PublicName: "idUser", WireName: "idUser", Location: "body"}, {PublicName: "laundries", WireName: "laundries", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "password", WireName: "password", Location: "body"}, {PublicName: "userLevel", WireName: "userLevel", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("accesses_list",
			mcplib.WithDescription("Lista utenti gestionale. Returns array of Access."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/accesses", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("actions_search",
			mcplib.WithDescription("Query coda azioni cassa per medias + intervallo date. Optional: endDate, medias, startDate. Returns array of Action."),
			mcplib.WithString("endDate", mcplib.Description("YYYY-MM-DD HH:mm:ss")),
			mcplib.WithString("medias", mcplib.Description("Medias")),
			mcplib.WithString("startDate", mcplib.Description("YYYY-MM-DD HH:mm:ss")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/actions", []mcpParamBinding{{PublicName: "endDate", WireName: "endDate", Location: "body"}, {PublicName: "medias", WireName: "medias", Location: "body"}, {PublicName: "startDate", WireName: "startDate", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("alarms_get",
			mcplib.WithDescription("Configurazione allarmi per tutte le lavanderie. Returns array of Alarm."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/alarms", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("alarms_save",
			mcplib.WithDescription("Salva configurazione allarmi. Returns the new Empty."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/alarms", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("automation_get",
			mcplib.WithDescription("Configurazione domotica per LCS. Required: idLCS. Returns the AutomationGetResponse."),
			mcplib.WithString("idLCS", mcplib.Required(), mcplib.Description("Serial number LCS (Laundry Control System)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/automation", []mcpParamBinding{{PublicName: "idLCS", WireName: "idLCS", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("automation_update",
			mcplib.WithDescription("Cambia stato automazione (isOn / isManual / reboot). Required: idLCS, idAutomation. Optional: isOn, isManual, key. Returns the updated Empty."),
			mcplib.WithString("idLCS", mcplib.Required(), mcplib.Description("Serial number LCS (Laundry Control System)")),
			mcplib.WithString("idAutomation", mcplib.Required(), mcplib.Description("Id automation")),
			mcplib.WithString("isOn", mcplib.Description("Is on")),
			mcplib.WithString("isManual", mcplib.Description("Is manual")),
			mcplib.WithString("key", mcplib.Description("Key")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/automation", []mcpParamBinding{{PublicName: "idLCS", WireName: "idLCS", Location: "query"}, {PublicName: "idAutomation", WireName: "idAutomation", Location: "query"}, {PublicName: "isOn", WireName: "isOn", Location: "query"}, {PublicName: "isManual", WireName: "isManual", Location: "query"}, {PublicName: "key", WireName: "key", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("bloomest-laundry-manager-profile_change-password",
			mcplib.WithDescription("Cambia password utente gestionale corrente. Required: username, change. Returns the updated Empty."),
			mcplib.WithString("username", mcplib.Required(), mcplib.Description("Username")),
			mcplib.WithString("change", mcplib.Required(), mcplib.Description("Change")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/profile", []mcpParamBinding{{PublicName: "username", WireName: "username", Location: "query"}, {PublicName: "change", WireName: "change", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("bloomest-laundry-manager-version_get",
			mcplib.WithDescription("Versione applicazione. Returns the BloomestLaundryManagerVersionGetResponse. (public)"),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/version", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("cashbox-updates_cancel",
			mcplib.WithDescription("Cancella aggiornamento firmware pianificato. Required: configuration, firmware. Returns the Empty. Destructive."),
			mcplib.WithString("configuration", mcplib.Required(), mcplib.Description("Configuration")),
			mcplib.WithString("firmware", mcplib.Required(), mcplib.Description("Firmware")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/cashboxUpdates", []mcpParamBinding{{PublicName: "configuration", WireName: "configuration", Location: "query"}, {PublicName: "firmware", WireName: "firmware", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("cashbox-updates_get",
			mcplib.WithDescription("Lista firmware cassa + configurazioni pianificate. Returns the CashboxUpdatesGetResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/cashboxUpdates", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("cashbox-updates_schedule",
			mcplib.WithDescription("Pianifica aggiornamento firmware cassa. Required: laundry, startDate. Optional: firmware. Returns the new Empty."),
			mcplib.WithString("laundry", mcplib.Required(), mcplib.Description("idLaundry")),
			mcplib.WithString("startDate", mcplib.Required(), mcplib.Description("Start date")),
			mcplib.WithString("firmware", mcplib.Description("Firmware")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/cashboxUpdates", []mcpParamBinding{{PublicName: "laundry", WireName: "laundry", Location: "query"}, {PublicName: "startDate", WireName: "startDate", Location: "query"}, {PublicName: "firmware", WireName: "firmware", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("comments_delete",
			mcplib.WithDescription("Cancella commento. Required: comment. Returns the Empty. Destructive."),
			mcplib.WithString("comment", mcplib.Required(), mcplib.Description("Comment")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/comments", []mcpParamBinding{{PublicName: "comment", WireName: "comment", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("comments_list",
			mcplib.WithDescription("Lista commenti clienti. Returns array of Comment."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/comments", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_list",
			mcplib.WithDescription("Lista contatti aziendali. Returns array of Contact."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/contacts", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("contacts_save",
			mcplib.WithDescription("Salva intera lista contatti (CRUD bulk, filtra isRemoved client-side). Returns array of Contact."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/contacts", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dispenser_get",
			mcplib.WithDescription("Configurazione dispenser per lavanderia. Required: laundry. Returns array of DispenserGetItem."),
			mcplib.WithString("laundry", mcplib.Required(), mcplib.Description("idLaundry")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/dispenser", []mcpParamBinding{{PublicName: "laundry", WireName: "laundry", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dispenser_save",
			mcplib.WithDescription("Salva configurazione dispenser. Required: laundry. Returns the new Empty."),
			mcplib.WithString("laundry", mcplib.Required(), mcplib.Description("idLaundry")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/dispenser", []mcpParamBinding{{PublicName: "laundry", WireName: "laundry", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("laundries_delete",
			mcplib.WithDescription("Elimina device o lavanderia (discriminator type). Required: type. Optional: device, laundry. Returns the Empty. Destructive."),
			mcplib.WithString("type", mcplib.Required(), mcplib.Description("Type")),
			mcplib.WithString("device", mcplib.Description("Device")),
			mcplib.WithString("laundry", mcplib.Description("Laundry")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/laundries", []mcpParamBinding{{PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "device", WireName: "device", Location: "query"}, {PublicName: "laundry", WireName: "laundry", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("laundries_get",
			mcplib.WithDescription("Lista lavanderie con devices. Returns array of Laundry."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/laundries", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("laundries_laundry-prices-save",
			mcplib.WithDescription("Salva prezzi/configurazione device intero. Optional: idDevice, idLaundry, pricesConfiguration. Returns the updated Empty."),
			mcplib.WithString("idDevice", mcplib.Description("Id device")),
			mcplib.WithString("idLaundry", mcplib.Description("Id laundry")),
			mcplib.WithString("pricesConfiguration", mcplib.Description("Prices configuration")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/laundries", []mcpParamBinding{{PublicName: "idDevice", WireName: "idDevice", Location: "body"}, {PublicName: "idLaundry", WireName: "idLaundry", Location: "body"}, {PublicName: "pricesConfiguration", WireName: "pricesConfiguration", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("laundries_save",
			mcplib.WithDescription("Crea o aggiorna lavanderia. Optional: address, catUser, city (plus 7 more). Returns the new Empty."),
			mcplib.WithString("address", mcplib.Description("Address")),
			mcplib.WithString("catUser", mcplib.Description("Cat user")),
			mcplib.WithString("city", mcplib.Description("City")),
			mcplib.WithString("configuration", mcplib.Description("Configuration")),
			mcplib.WithString("devices", mcplib.Description("Devices")),
			mcplib.WithString("idLCS", mcplib.Description("Serial number LCS (Laundry Control System)")),
			mcplib.WithString("idLaundry", mcplib.Description("Id laundry")),
			mcplib.WithString("lavapiuLaundryIdentifier", mcplib.Description("Lavapiu laundry identifier")),
			mcplib.WithString("managerUser", mcplib.Description("Manager user")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/laundries", []mcpParamBinding{{PublicName: "address", WireName: "address", Location: "body"}, {PublicName: "catUser", WireName: "catUser", Location: "body"}, {PublicName: "city", WireName: "city", Location: "body"}, {PublicName: "configuration", WireName: "configuration", Location: "body"}, {PublicName: "devices", WireName: "devices", Location: "body"}, {PublicName: "idLCS", WireName: "idLCS", Location: "body"}, {PublicName: "idLaundry", WireName: "idLaundry", Location: "body"}, {PublicName: "lavapiuLaundryIdentifier", WireName: "lavapiuLaundryIdentifier", Location: "body"}, {PublicName: "managerUser", WireName: "managerUser", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("lockers_get",
			mcplib.WithDescription("Lista locker/cassetti per LCS. Required: idLCS. Returns array of LockersGetItem."),
			mcplib.WithString("idLCS", mcplib.Required(), mcplib.Description("Serial number LCS (Laundry Control System)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/lockers", []mcpParamBinding{{PublicName: "idLCS", WireName: "idLCS", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("login_check",
			mcplib.WithDescription("Verifica stato login corrente. Returns the Access. (public)"),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/login", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("login_delete",
			mcplib.WithDescription("Logout (terminazione sessione). Returns the Empty. Destructive."),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/login", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("login_post",
			mcplib.WithDescription("Autentica utente con credenziali SHA1. Required: user, passwd, source (default: 1). Returns the new Access. (public)"),
			mcplib.WithString("user", mcplib.Required(), mcplib.Description("User")),
			mcplib.WithString("passwd", mcplib.Required(), mcplib.Description("Passwd")),
			mcplib.WithString("source", mcplib.Required(), mcplib.Description("Source")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/login", []mcpParamBinding{{PublicName: "user", WireName: "user", Location: "query"}, {PublicName: "passwd", WireName: "passwd", Location: "query"}, {PublicName: "source", WireName: "source", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("loyalties_list",
			mcplib.WithDescription("Lista promozioni (attive con ?now=, storiche con ?start=). Optional: now, start, timezone. Returns array of Loyalty."),
			mcplib.WithString("now", mcplib.Description("Now")),
			mcplib.WithString("start", mcplib.Description("Start")),
			mcplib.WithString("timezone", mcplib.Description("Timezone")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/loyalties", []mcpParamBinding{{PublicName: "now", WireName: "now", Location: "query"}, {PublicName: "start", WireName: "start", Location: "query"}, {PublicName: "timezone", WireName: "timezone", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("loyalties_loyalty-create",
			mcplib.WithDescription("Crea / aggiorna promozione. Optional: start, amount, award (plus 14 more). Returns the new LoyaltiesLoyaltyCreateResponse."),
			mcplib.WithString("start", mcplib.Description("Start")),
			mcplib.WithString("amount", mcplib.Description("Amount")),
			mcplib.WithString("award", mcplib.Description("Award")),
			mcplib.WithString("daysOfAward", mcplib.Description("Days of award")),
			mcplib.WithString("endDate", mcplib.Description("End date")),
			mcplib.WithString("erogationAtTheEnd", mcplib.Description("Erogation at the end")),
			mcplib.WithString("idLoyalty", mcplib.Description("Id loyalty")),
			mcplib.WithString("isEnabled", mcplib.Description("Is enabled")),
			mcplib.WithString("laundries", mcplib.Description("Laundries")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("notifications", mcplib.Description("Notifications")),
			mcplib.WithString("service", mcplib.Description("Service")),
			mcplib.WithString("startDate", mcplib.Description("Start date")),
			mcplib.WithString("target", mcplib.Description("Target")),
			mcplib.WithString("targetUserGroups", mcplib.Description("Target user groups")),
			mcplib.WithString("threshold", mcplib.Description("Threshold")),
			mcplib.WithString("type", mcplib.Description("Tipo loyalty (1-4 secondo ANALISI § 4)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/loyalties", []mcpParamBinding{{PublicName: "start", WireName: "start", Location: "query"}, {PublicName: "amount", WireName: "amount", Location: "body"}, {PublicName: "award", WireName: "award", Location: "body"}, {PublicName: "daysOfAward", WireName: "daysOfAward", Location: "body"}, {PublicName: "endDate", WireName: "endDate", Location: "body"}, {PublicName: "erogationAtTheEnd", WireName: "erogationAtTheEnd", Location: "body"}, {PublicName: "idLoyalty", WireName: "idLoyalty", Location: "body"}, {PublicName: "isEnabled", WireName: "isEnabled", Location: "body"}, {PublicName: "laundries", WireName: "laundries", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "notifications", WireName: "notifications", Location: "body"}, {PublicName: "service", WireName: "service", Location: "body"}, {PublicName: "startDate", WireName: "startDate", Location: "body"}, {PublicName: "target", WireName: "target", Location: "body"}, {PublicName: "targetUserGroups", WireName: "targetUserGroups", Location: "body"}, {PublicName: "threshold", WireName: "threshold", Location: "body"}, {PublicName: "type", WireName: "type", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("loyalties_loyalty-delete",
			mcplib.WithDescription("Cancella promozione. Required: idLoyalty. Returns the Empty. Destructive."),
			mcplib.WithString("idLoyalty", mcplib.Required(), mcplib.Description("Id loyalty")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/loyalties", []mcpParamBinding{{PublicName: "idLoyalty", WireName: "idLoyalty", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("message_get",
			mcplib.WithDescription("Lista messaggi push pianificati. Returns array of MessageGetItem."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/message", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("message_send",
			mcplib.WithDescription("Crea messaggio push (broadcast su laundries). Returns the new Empty."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/message", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("messages_users-send-email",
			mcplib.WithDescription("Invia email a clienti selezionati. Optional: body, recipients, subject. Returns the new Empty."),
			mcplib.WithString("body", mcplib.Description("Body")),
			mcplib.WithString("recipients", mcplib.Description("Recipients")),
			mcplib.WithString("subject", mcplib.Description("Subject")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/messages", []mcpParamBinding{{PublicName: "body", WireName: "body", Location: "body"}, {PublicName: "recipients", WireName: "recipients", Location: "body"}, {PublicName: "subject", WireName: "subject", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("notifications_delete",
			mcplib.WithDescription("Cancella notifica. Required: id_event. Returns the Empty. Destructive."),
			mcplib.WithString("id_event", mcplib.Required(), mcplib.Description("Id event")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/notifications", []mcpParamBinding{{PublicName: "id_event", WireName: "id_event", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("notifications_list",
			mcplib.WithDescription("Lista notifiche pianificate. Returns array of Notification."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/notifications", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("notifications_save",
			mcplib.WithDescription("Crea / modifica notifica calendar event. Optional: actions, endsAt, idEvent (plus 2 more). Returns the updated Empty."),
			mcplib.WithString("actions", mcplib.Description("Actions")),
			mcplib.WithString("endsAt", mcplib.Description("Ends at")),
			mcplib.WithString("idEvent", mcplib.Description("Id event")),
			mcplib.WithString("startsAt", mcplib.Description("Starts at")),
			mcplib.WithString("titleTraductions", mcplib.Description("Title traductions")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/notifications", []mcpParamBinding{{PublicName: "actions", WireName: "actions", Location: "body"}, {PublicName: "endsAt", WireName: "endsAt", Location: "body"}, {PublicName: "idEvent", WireName: "idEvent", Location: "body"}, {PublicName: "startsAt", WireName: "startsAt", Location: "body"}, {PublicName: "titleTraductions", WireName: "titleTraductions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("payments_get",
			mcplib.WithDescription("Configurazione gateway pagamento. Returns the PaymentsConfig."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/payments", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("payments_save",
			mcplib.WithDescription("Salva configurazione gateway pagamento. Optional: argentea, satispay. Returns the new Empty."),
			mcplib.WithString("argentea", mcplib.Description("Argentea")),
			mcplib.WithString("satispay", mcplib.Description("Satispay")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/payments", []mcpParamBinding{{PublicName: "argentea", WireName: "argentea", Location: "body"}, {PublicName: "satispay", WireName: "satispay", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("policy_accept",
			mcplib.WithDescription("Registra accettazione privacy + terms. Required: version, privacy, terms. Returns the updated Empty."),
			mcplib.WithString("version", mcplib.Required(), mcplib.Description("Version")),
			mcplib.WithString("privacy", mcplib.Required(), mcplib.Description("Privacy")),
			mcplib.WithString("terms", mcplib.Required(), mcplib.Description("Terms")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/policy", []mcpParamBinding{{PublicName: "version", WireName: "version", Location: "query"}, {PublicName: "privacy", WireName: "privacy", Location: "query"}, {PublicName: "terms", WireName: "terms", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("policy_get",
			mcplib.WithDescription("Versione corrente policy (privacy/terms). Returns the PolicyGetResponse. (public)"),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/policy", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("reboot_cashbox",
			mcplib.WithDescription("Reboot remoto cassa POS. Required: laundry. Returns the new Empty."),
			mcplib.WithString("laundry", mcplib.Required(), mcplib.Description("idLaundry")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/reboot", []mcpParamBinding{{PublicName: "laundry", WireName: "laundry", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("registration-bonus_get",
			mcplib.WithDescription("Configurazione bonus di registrazione. Returns the RegistrationBonus."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/registrationBonus", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("registration-bonus_save",
			mcplib.WithDescription("Salva configurazione bonus di registrazione. Optional: amount, currency, isEnabled. Returns the new Empty."),
			mcplib.WithString("amount", mcplib.Description("Amount")),
			mcplib.WithString("currency", mcplib.Description("Currency")),
			mcplib.WithString("isEnabled", mcplib.Description("Is enabled")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/registrationBonus", []mcpParamBinding{{PublicName: "amount", WireName: "amount", Location: "body"}, {PublicName: "currency", WireName: "currency", Location: "body"}, {PublicName: "isEnabled", WireName: "isEnabled", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("research_log",
			mcplib.WithDescription("Log query di ricerca training. Optional: filesResult, querySearch. Returns the new Empty."),
			mcplib.WithString("filesResult", mcplib.Description("Files result")),
			mcplib.WithString("querySearch", mcplib.Description("Query search")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/research", []mcpParamBinding{{PublicName: "filesResult", WireName: "filesResult", Location: "body"}, {PublicName: "querySearch", WireName: "querySearch", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("reservations_cancel",
			mcplib.WithDescription("Cancella prenotazione device. Required: device. Returns the updated Empty."),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Device")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/reservations", []mcpParamBinding{{PublicName: "device", WireName: "device", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("reservations_list",
			mcplib.WithDescription("Lista prenotazioni per lavanderia. Required: laundry. Returns array of Reservation."),
			mcplib.WithString("laundry", mcplib.Required(), mcplib.Description("idLaundry")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/reservations", []mcpParamBinding{{PublicName: "laundry", WireName: "laundry", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("reservationsconf_reservations-config-get",
			mcplib.WithDescription("Configurazione prenotazioni. Returns the ReservationsconfReservationsConfigGetResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/reservationsconf", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("reservationsconf_reservations-config-save",
			mcplib.WithDescription("Salva configurazione prenotazioni. Returns the new Empty."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/reservationsconf", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("statistics_query",
			mcplib.WithDescription("Query statistiche aggregate (discriminator ?type=X). Required: type, end, start. Optional: device, groupOfUsers, laundry (plus 1 more)."),
			mcplib.WithString("type", mcplib.Required(), mcplib.Description("Type")),
			mcplib.WithString("device", mcplib.Description("idDevice (modality DEVICE)")),
			mcplib.WithString("end", mcplib.Required(), mcplib.Description("'YYYY-MM-DD HH:mm:ss'")),
			mcplib.WithString("groupOfUsers", mcplib.Description("idGroup[] (modality GROUP_OF_USERS)")),
			mcplib.WithString("laundry", mcplib.Description("idLaundry (modality LAUNDRY)")),
			mcplib.WithString("start", mcplib.Required(), mcplib.Description("'YYYY-MM-DD HH:mm:ss'")),
			mcplib.WithString("user", mcplib.Description("idMedia (modality USER)")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/statistics", []mcpParamBinding{{PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "device", WireName: "device", Location: "body"}, {PublicName: "end", WireName: "end", Location: "body"}, {PublicName: "groupOfUsers", WireName: "groupOfUsers", Location: "body"}, {PublicName: "laundry", WireName: "laundry", Location: "body"}, {PublicName: "start", WireName: "start", Location: "body"}, {PublicName: "user", WireName: "user", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("tickets_list",
			mcplib.WithDescription("Lista ticket assistenza clienti per lavanderia. Required: laundry. Returns array of Ticket."),
			mcplib.WithString("laundry", mcplib.Required(), mcplib.Description("idLaundry")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/tickets", []mcpParamBinding{{PublicName: "laundry", WireName: "laundry", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("training_list",
			mcplib.WithDescription("Lista sezioni training per lingua. Required: language. Returns the TrainingListResponse."),
			mcplib.WithString("language", mcplib.Required(), mcplib.Description("Language")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/training", []mcpParamBinding{{PublicName: "language", WireName: "language", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("training_post-multiplex",
			mcplib.WithDescription("Crea sezione o carica file (discriminator ?type=). Required: type. Returns the new Empty."),
			mcplib.WithString("type", mcplib.Required(), mcplib.Description("Type")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/training", []mcpParamBinding{{PublicName: "type", WireName: "type", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("training_put-multiplex",
			mcplib.WithDescription("Mutazioni training (modify / delete / deleteFiles / download). Required: type. Optional: file."),
			mcplib.WithString("type", mcplib.Required(), mcplib.Description("Type")),
			mcplib.WithString("file", mcplib.Description("File")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/training", []mcpParamBinding{{PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "file", WireName: "file", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("updates_get",
			mcplib.WithDescription("Lista device aggiornati per LCS (eventualmente filtrato per device). Required: idLCS. Optional: idDevice. Returns array of Device."),
			mcplib.WithString("idLCS", mcplib.Required(), mcplib.Description("Serial number LCS (Laundry Control System)")),
			mcplib.WithString("idDevice", mcplib.Description("Id device")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/updates", []mcpParamBinding{{PublicName: "idLCS", WireName: "idLCS", Location: "query"}, {PublicName: "idDevice", WireName: "idDevice", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("updates_put",
			mcplib.WithDescription("Aggiorna firmware o configurazione device. Required: idLCS, key. Optional: firmware, operationDetails. Returns the updated Empty."),
			mcplib.WithString("idLCS", mcplib.Required(), mcplib.Description("Serial number LCS (Laundry Control System)")),
			mcplib.WithString("key", mcplib.Required(), mcplib.Description("Key")),
			mcplib.WithString("firmware", mcplib.Description("Firmware")),
			mcplib.WithString("operationDetails", mcplib.Description("Operation details")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/updates", []mcpParamBinding{{PublicName: "idLCS", WireName: "idLCS", Location: "query"}, {PublicName: "key", WireName: "key", Location: "query"}, {PublicName: "firmware", WireName: "firmware", Location: "body"}, {PublicName: "operationDetails", WireName: "operationDetails", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("users_add-group",
			mcplib.WithDescription("Crea nuovo gruppo utenti. Required: description. Returns the new UserGroup."),
			mcplib.WithString("description", mcplib.Required(), mcplib.Description("Description")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/users", []mcpParamBinding{{PublicName: "description", WireName: "description", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("users_delete",
			mcplib.WithDescription("Cancella utente. Required: user. Returns the Empty. Destructive."),
			mcplib.WithString("user", mcplib.Required(), mcplib.Description("User")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/users", []mcpParamBinding{{PublicName: "user", WireName: "user", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("users_get-list",
			mcplib.WithDescription("Lista clienti (default) o gruppi (type=groups). Optional: type."),
			mcplib.WithString("type", mcplib.Description("Type")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/users", []mcpParamBinding{{PublicName: "type", WireName: "type", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("users_put-multiplex",
			mcplib.WithDescription("Mutazioni utente — discriminator query ?type=X. Required: type. Optional: rechargeValue, media, serialNumber (plus 3 more). Returns the updated UsersPutMultiplexResponse."),
			mcplib.WithString("type", mcplib.Required(), mcplib.Description("Type")),
			mcplib.WithString("rechargeValue", mcplib.Description("Recharge value")),
			mcplib.WithString("media", mcplib.Description("Media")),
			mcplib.WithString("serialNumber", mcplib.Description("Serial number")),
			mcplib.WithString("user", mcplib.Description("User")),
			mcplib.WithString("language", mcplib.Description("Language")),
			mcplib.WithString("timezone", mcplib.Description("Timezone")),
			mcplib.WithString("body_json", mcplib.Description("Request body as a JSON object string (this endpoint accepts a polymorphic schema: oneOf/anyOf)")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/users", []mcpParamBinding{{PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "rechargeValue", WireName: "rechargeValue", Location: "query"}, {PublicName: "media", WireName: "media", Location: "query"}, {PublicName: "serialNumber", WireName: "serialNumber", Location: "query"}, {PublicName: "user", WireName: "user", Location: "query"}, {PublicName: "language", WireName: "language", Location: "query"}, {PublicName: "timezone", WireName: "timezone", Location: "query"}, {PublicName: "body_json", WireName: "body_json", Location: "body_json"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		// bodyJSONOverride holds an opaque body for endpoints whose request
		// schema is a oneOf/anyOf and was wired with a single body_json input.
		// When set, it replaces the bodyArgs map for POST/PUT/PATCH.
		var bodyJSONOverride json.RawMessage
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			case "body_json":
				if s, ok := v.(string); ok && s != "" {
					var parsed any
					if err := json.Unmarshal([]byte(s), &parsed); err != nil {
						return mcplib.NewToolResultError("body_json must be a valid JSON object: " + err.Error()), nil
					}
					if _, isMap := parsed.(map[string]any); !isMap {
						return mcplib.NewToolResultError(fmt.Sprintf("body_json must be a JSON object, got JSON %T", parsed)), nil
					}
					bodyJSONOverride = json.RawMessage(s)
				}
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			if len(bodyJSONOverride) > 0 {
				data, _, err = c.PostWithParams(path, params, bodyJSONOverride)
				break
			}
			data, _, err = c.PostWithParams(path, params, bodyArgs)
		case "PUT":
			if len(bodyJSONOverride) > 0 {
				data, _, err = c.PutWithParams(path, params, bodyJSONOverride)
				break
			}
			data, _, err = c.PutWithParams(path, params, bodyArgs)
		case "PATCH":
			if len(bodyJSONOverride) > 0 {
				data, _, err = c.PatchWithParams(path, params, bodyJSONOverride)
				break
			}
			data, _, err = c.PatchWithParams(path, params, bodyArgs)
		case "DELETE":
			data, _, err = c.DeleteWithParams(path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export BLOOMEST_LAUNDRY_MANAGER_COOKIE_AUTH=<your-key>" +
					"\n      Run 'bloomest-laundry-manager-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set it with: export BLOOMEST_LAUNDRY_MANAGER_COOKIE_AUTH=<your-key>" +
					"\n      Run 'bloomest-laundry-manager-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export BLOOMEST_LAUNDRY_MANAGER_COOKIE_AUTH=<your-key>" +
					"\n      Run 'bloomest-laundry-manager-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "bloomest-laundry-manager-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "bloomest-laundry-manager-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "bloomest-laundry-manager",
		"description": "Mapping non-ufficiale dell'API consumata dal gestionale web `www.bloomestlaundry.app/bloomest/` (v1.22.0 in...",
		"archetype":   "communication",
		"tool_count":  59,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion bloomest-laundry-manager-pp-cli binary.",
		"auth": map[string]any{
			"type":     "api_key",
			"env_vars": []map[string]any{},
		},
		"resources": []map[string]any{
			{
				"name":        "accesses",
				"description": "Utenti del gestionale web (login back-office). NON sono i clienti finali (vedi tag `users`). Hanno userLevel 1-11.",
				"endpoints":   []string{"access-delete", "access-save", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "actions",
				"description": "Coda azioni cassa (ricariche, sconti) per medias",
				"endpoints":   []string{"search"},
				"searchable":  true,
			},
			{
				"name":        "alarms",
				"description": "Allarmi e contatti di escalation",
				"endpoints":   []string{"get", "save"},
				"syncable":    true,
			},
			{
				"name":        "automation",
				"description": "Domotica / IoT (interruttori, sensori)",
				"endpoints":   []string{"get", "update"},
				"searchable":  true,
			},
			{
				"name":        "bloomest-laundry-manager-profile",
				"description": "Manage bloomest laundry manager profile",
				"endpoints":   []string{"change-password"},
			},
			{
				"name":        "bloomest-laundry-manager-version",
				"description": "Manage bloomest laundry manager version",
				"endpoints":   []string{"get"},
				"syncable":    true,
			},
			{
				"name":        "cashbox-updates",
				"description": "Manage cashbox updates",
				"endpoints":   []string{"cancel", "get", "schedule"},
				"syncable":    true,
			},
			{
				"name":        "comments",
				"description": "Community / commenti clienti",
				"endpoints":   []string{"delete", "list"},
				"syncable":    true,
			},
			{
				"name":        "contacts",
				"description": "Rubrica contatti aziendali",
				"endpoints":   []string{"list", "save"},
				"syncable":    true,
			},
			{
				"name":        "dispenser",
				"description": "Configurazione dispenser detersivi",
				"endpoints":   []string{"get", "save"},
			},
			{
				"name":        "laundries",
				"description": "Lavanderie e dispositivi (macchine)",
				"endpoints":   []string{"delete", "get", "laundry-prices-save", "save"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "lockers",
				"description": "Cassetti / locker",
				"endpoints":   []string{"get"},
				"searchable":  true,
			},
			{
				"name":        "login",
				"description": "Manage login",
				"endpoints":   []string{"check", "delete", "post"},
			},
			{
				"name":        "loyalties",
				"description": "Promozioni / fidelizzazione",
				"endpoints":   []string{"list", "loyalty-create", "loyalty-delete"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "message",
				"description": "Messaggi push a clienti (broadcast)",
				"endpoints":   []string{"get", "send"},
				"syncable":    true,
			},
			{
				"name":        "messages",
				"description": "Messaggi push a clienti (broadcast)",
				"endpoints":   []string{"users-send-email"},
				"searchable":  true,
			},
			{
				"name":        "notifications",
				"description": "Notifiche calendar event-based",
				"endpoints":   []string{"delete", "list", "save"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "payments",
				"description": "Configurazione gateway pagamento (Satispay, Argentea)",
				"endpoints":   []string{"get", "save"},
				"searchable":  true,
			},
			{
				"name":        "policy",
				"description": "Manage policy",
				"endpoints":   []string{"accept", "get"},
				"syncable":    true,
			},
			{
				"name":        "reboot",
				"description": "Manage reboot",
				"endpoints":   []string{"cashbox"},
			},
			{
				"name":        "registration-bonus",
				"description": "Bonus di benvenuto nuova registrazione",
				"endpoints":   []string{"get", "save"},
				"searchable":  true,
			},
			{
				"name":        "research",
				"description": "Manage research",
				"endpoints":   []string{"log"},
				"searchable":  true,
			},
			{
				"name":        "reservations",
				"description": "Prenotazioni macchine",
				"endpoints":   []string{"cancel", "list"},
			},
			{
				"name":        "reservationsconf",
				"description": "Manage reservationsconf",
				"endpoints":   []string{"reservations-config-get", "reservations-config-save"},
				"syncable":    true,
			},
			{
				"name":        "statistics",
				"description": "Statistiche aggregate (allarmi, corrispettivo Agenzia Entrate, card-report, movimenti, cicli, summaries, sales by...",
				"endpoints":   []string{"query"},
				"searchable":  true,
			},
			{
				"name":        "tickets",
				"description": "Ticket assistenza clienti",
				"endpoints":   []string{"list"},
			},
			{
				"name":        "training",
				"description": "Materiale formativo / knowledge base",
				"endpoints":   []string{"list", "post-multiplex", "put-multiplex"},
				"searchable":  true,
			},
			{
				"name":        "updates",
				"description": "Manage updates",
				"endpoints":   []string{"get", "put"},
				"searchable":  true,
			},
			{
				"name":        "users",
				"description": "Utenti finali (clienti) + gruppi + card",
				"endpoints":   []string{"add-group", "delete", "get-list", "put-multiplex"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		"playbook": []map[string]string{
			{"topic": "Message search", "insight": "Use the search tool on synced data rather than paginating through message history. Message APIs often have aggressive rate limits."},
			{"topic": "Channel health", "insight": "When analyzing channel activity, use the channel-health command or sql aggregation on synced messages. Don't iterate individual messages via API."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
