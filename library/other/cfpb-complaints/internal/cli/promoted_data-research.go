// Copyright 2026 avanderheyde and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newDataResearchPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagSize int
	var flagFrom int
	var flagSearchTerm string
	var flagCompany string
	var flagProduct string
	var flagIssue string
	var flagState string
	var flagDateReceivedMin string
	var flagDateReceivedMax string
	var flagHasNarrative bool
	var flagField string
	var flagSort string

	cmd := &cobra.Command{
		Use:         "data-research",
		Short:       "Search published consumer complaints",
		Long:        "Search published consumer complaints",
		Example:     "  cfpb-complaints-pp-cli data-research --company 'CAPITAL ONE FINANCIAL CORPORATION' --size 1 --select hits.hits._source.complaint_id",
		Annotations: map[string]string{"pp:endpoint": "data-research.list-complaints", "pp:method": "GET", "pp:path": "/data-research/consumer-complaints/search/api/v1/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/data-research/consumer-complaints/search/api/v1/"
			params := map[string]string{}
			if flagSize != 0 {
				params["size"] = formatCLIParamValue(flagSize)
			}
			if flagFrom != 0 {
				params["from"] = formatCLIParamValue(flagFrom)
			}
			if flagSearchTerm != "" {
				params["search_term"] = formatCLIParamValue(flagSearchTerm)
			}
			if flagCompany != "" {
				params["company"] = formatCLIParamValue(flagCompany)
			}
			if flagProduct != "" {
				params["product"] = formatCLIParamValue(flagProduct)
			}
			if flagIssue != "" {
				params["issue"] = formatCLIParamValue(flagIssue)
			}
			if flagState != "" {
				params["state"] = formatCLIParamValue(flagState)
			}
			if flagDateReceivedMin != "" {
				params["date_received_min"] = formatCLIParamValue(flagDateReceivedMin)
			}
			if flagDateReceivedMax != "" {
				params["date_received_max"] = formatCLIParamValue(flagDateReceivedMax)
			}
			if flagHasNarrative != false {
				params["has_narrative"] = formatCLIParamValue(flagHasNarrative)
			}
			if flagField != "" {
				params["field"] = formatCLIParamValue(flagField)
			}
			if flagSort != "" {
				params["sort"] = formatCLIParamValue(flagSort)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "data-research", false, path, params, nil, "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().IntVar(&flagSize, "size", 25, "Size")
	cmd.Flags().IntVar(&flagFrom, "from", 0, "From")
	cmd.Flags().StringVar(&flagSearchTerm, "search-term", "", "Search term")
	cmd.Flags().StringVar(&flagCompany, "company", "", "Company")
	cmd.Flags().StringVar(&flagProduct, "product", "", "Product")
	cmd.Flags().StringVar(&flagIssue, "issue", "", "Issue")
	cmd.Flags().StringVar(&flagState, "state", "", "State")
	cmd.Flags().StringVar(&flagDateReceivedMin, "date-received-min", "", "Date received min")
	cmd.Flags().StringVar(&flagDateReceivedMax, "date-received-max", "", "Date received max")
	cmd.Flags().BoolVar(&flagHasNarrative, "has-narrative", false, "Has narrative")
	cmd.Flags().StringVar(&flagField, "field", "", "Field")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Sort")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
