// Copyright 2026 dyzsasd. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
	"github.com/mvanhorn/printing-press-library/library/travel/jinko/internal/cliutil"
)

func newFlightsCreateCmd(flags *rootFlags) *cobra.Command {
	var flagProvider string
	var bodyCabinClass string
	var bodyCurrency string
	var bodyDirectOnly bool
	var bodyExcludeCarriers string
	var bodyIncludeCarriers string
	var bodyLocale string
	var bodyMaxPrice float64
	var bodyOfferToken string
	var bodyPassengersAdt int
	var bodyPassengersChd int
	var bodyPassengersInf int
	var bodyProvider2 string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Check a flight offer from search results to get confirmed availability and detailed fare options",
		Example:     "  jinko-pp-cli flights create --offer-token your-token-here",
		Annotations: map[string]string{"pp:endpoint": "flights.create", "pp:method": "POST", "pp:path": "/api/v1/flights/check"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("offer-token") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "offer-token")
				}
				if !cmd.Flags().Changed("passengers-adt") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "passengers-adt")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/flights/check"
			params := map[string]string{}
			if flagProvider != "" {
				params["provider"] = fmt.Sprintf("%v", flagProvider)
			}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCabinClass != "" {
					body["cabin_class"] = bodyCabinClass
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				if cmd.Flags().Changed("direct-only") {
					body["direct_only"] = bodyDirectOnly
				}
				if bodyExcludeCarriers != "" {
					body["exclude_carriers"] = cliutil.SplitCSV(bodyExcludeCarriers)
				}
				if bodyIncludeCarriers != "" {
					body["include_carriers"] = cliutil.SplitCSV(bodyIncludeCarriers)
				}
				if bodyLocale != "" {
					body["locale"] = bodyLocale
				}
				if bodyMaxPrice != 0.0 {
					body["max_price"] = bodyMaxPrice
				}
				if bodyOfferToken != "" {
					body["offer_token"] = bodyOfferToken
				}
				{
					nestedPassengers := map[string]any{}
					if bodyPassengersAdt != 0 {
						nestedPassengers["adt"] = bodyPassengersAdt
					}
					if bodyPassengersChd != 0 {
						nestedPassengers["chd"] = bodyPassengersChd
					}
					if bodyPassengersInf != 0 {
						nestedPassengers["inf"] = bodyPassengersInf
					}
					if len(nestedPassengers) > 0 {
						body["passengers"] = nestedPassengers
					}
				}
				if bodyProvider2 != "" {
					body["provider"] = bodyProvider2
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "flights", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "flights", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "flights", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "flights", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "flights", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "flights",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "flights", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "flights", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagProvider, "provider", "", "Target a specific provider (sabre, travelfusion). When omitted, all enabled providers are called.")
	cmd.Flags().StringVar(&bodyCabinClass, "cabin-class", "", "JIN-187 filter forwards.")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "Currency code for pricing (ISO 4217, e.g., 'USD', 'EUR')")
	cmd.Flags().BoolVar(&bodyDirectOnly, "direct-only", false, "Direct only")
	cmd.Flags().StringVar(&bodyExcludeCarriers, "exclude-carriers", "", "Exclude carriers")
	cmd.Flags().StringVar(&bodyIncludeCarriers, "include-carriers", "", "Include carriers")
	cmd.Flags().StringVar(&bodyLocale, "locale", "", "Locale for formatting (e.g., 'en-US')")
	cmd.Flags().Float64Var(&bodyMaxPrice, "max-price", 0.0, "Max price")
	cmd.Flags().StringVar(&bodyOfferToken, "offer-token", "", "The offer token from a flight search result (find_flight or find_destination)")
	cmd.Flags().IntVar(&bodyPassengersAdt, "passengers-adt", 0, "Number of adult passengers (12+ years), required, 1-9")
	cmd.Flags().IntVar(&bodyPassengersChd, "passengers-chd", 0, "Number of child passengers (2-11 years), optional, 0-8")
	cmd.Flags().IntVar(&bodyPassengersInf, "passengers-inf", 0, "Number of infant passengers (under 2 years), optional, 0-4")
	cmd.Flags().StringVar(&bodyProvider2, "provider-2", "", "Provider to target for this check (e.g., 'sabre', 'travelfusion').")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
