// Copyright 2026 dyzsasd. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
	"github.com/mvanhorn/printing-press-library/library/travel/jinko/internal/cliutil"
)

func newFlightsCreateSearchCmd(flags *rootFlags) *cobra.Command {
	var bodyAllowLiveShop bool
	var bodyCurrency string
	var bodyFiltersCabinClass string
	var bodyFiltersDatesDepartureDateRanges string
	var bodyFiltersDatesDepartureDates string
	var bodyFiltersDatesReturnDateRanges string
	var bodyFiltersDatesReturnDates string
	var bodyFiltersDatesStayDays int
	var bodyFiltersDirectOnly bool
	var bodyFiltersLocationsDestinationType string
	var bodyFiltersLocationsDestinations string
	var bodyFiltersLocationsOriginType string
	var bodyFiltersLocationsOrigins string
	var bodyFiltersTripType string
	var bodyLimit int
	var bodyLocale string
	var bodyOffset string
	var bodyPassengersAdt int
	var bodyPassengersAges string
	var bodyPassengersChd int
	var bodyPassengersInfLap int
	var bodyPassengersInfSeat int
	var bodyPassengersResidencyCountry string
	var bodyPassengersStudent int
	var bodyPassengersYouth int
	var bodyPriceConstraintsMaxTotal float64
	var bodySort string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-search",
		Short:       "Search flights (synchronous, filter-based)",
		Example:     "  jinko-pp-cli flights create-search",
		Annotations: map[string]string{"pp:endpoint": "flights.create-search", "pp:method": "POST", "pp:path": "/api/v1/flights/search"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/flights/search"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("allow-live-shop") {
					body["allow_live_shop"] = bodyAllowLiveShop
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				{
					nestedFilters := map[string]any{}
					if bodyFiltersCabinClass != "" {
						nestedFilters["cabin_class"] = bodyFiltersCabinClass
					}
					{
						nestedFiltersDates := map[string]any{}
						if bodyFiltersDatesDepartureDateRanges != "" {
							var parsedFiltersDatesDepartureDateRanges any
							if err := json.Unmarshal([]byte(bodyFiltersDatesDepartureDateRanges), &parsedFiltersDatesDepartureDateRanges); err != nil {
								return fmt.Errorf("parsing --filters-dates-departure-date-ranges JSON: %w", err)
							}
							nestedFiltersDates["departure_date_ranges"] = parsedFiltersDatesDepartureDateRanges
						}
						if bodyFiltersDatesDepartureDates != "" {
							nestedFiltersDates["departure_dates"] = cliutil.SplitCSV(bodyFiltersDatesDepartureDates)
						}
						if bodyFiltersDatesReturnDateRanges != "" {
							var parsedFiltersDatesReturnDateRanges any
							if err := json.Unmarshal([]byte(bodyFiltersDatesReturnDateRanges), &parsedFiltersDatesReturnDateRanges); err != nil {
								return fmt.Errorf("parsing --filters-dates-return-date-ranges JSON: %w", err)
							}
							nestedFiltersDates["return_date_ranges"] = parsedFiltersDatesReturnDateRanges
						}
						if bodyFiltersDatesReturnDates != "" {
							nestedFiltersDates["return_dates"] = cliutil.SplitCSV(bodyFiltersDatesReturnDates)
						}
						if bodyFiltersDatesStayDays != 0 {
							nestedFiltersDates["stay_days"] = bodyFiltersDatesStayDays
						}
						if len(nestedFiltersDates) > 0 {
							nestedFilters["dates"] = nestedFiltersDates
						}
					}
					if cmd.Flags().Changed("filters-direct-only") {
						nestedFilters["direct_only"] = bodyFiltersDirectOnly
					}
					{
						nestedFiltersLocations := map[string]any{}
						if bodyFiltersLocationsDestinationType != "" {
							nestedFiltersLocations["destination_type"] = bodyFiltersLocationsDestinationType
						}
						if bodyFiltersLocationsDestinations != "" {
							nestedFiltersLocations["destinations"] = cliutil.SplitCSV(bodyFiltersLocationsDestinations)
						}
						if bodyFiltersLocationsOriginType != "" {
							nestedFiltersLocations["origin_type"] = bodyFiltersLocationsOriginType
						}
						if bodyFiltersLocationsOrigins != "" {
							nestedFiltersLocations["origins"] = cliutil.SplitCSV(bodyFiltersLocationsOrigins)
						}
						if len(nestedFiltersLocations) > 0 {
							nestedFilters["locations"] = nestedFiltersLocations
						}
					}
					if bodyFiltersTripType != "" {
						nestedFilters["trip_type"] = bodyFiltersTripType
					}
					if len(nestedFilters) > 0 {
						body["filters"] = nestedFilters
					}
				}
				if bodyLimit != 0 {
					body["limit"] = bodyLimit
				}
				if bodyLocale != "" {
					body["locale"] = bodyLocale
				}
				if bodyOffset != "" {
					body["offset"] = bodyOffset
				}
				{
					nestedPassengers := map[string]any{}
					if bodyPassengersAdt != 0 {
						nestedPassengers["adt"] = bodyPassengersAdt
					}
					if bodyPassengersAges != "" {
						var parsedPassengersAges any
						if err := json.Unmarshal([]byte(bodyPassengersAges), &parsedPassengersAges); err != nil {
							return fmt.Errorf("parsing --passengers-ages JSON: %w", err)
						}
						nestedPassengers["ages"] = parsedPassengersAges
					}
					if bodyPassengersChd != 0 {
						nestedPassengers["chd"] = bodyPassengersChd
					}
					if bodyPassengersInfLap != 0 {
						nestedPassengers["inf_lap"] = bodyPassengersInfLap
					}
					if bodyPassengersInfSeat != 0 {
						nestedPassengers["inf_seat"] = bodyPassengersInfSeat
					}
					if bodyPassengersResidencyCountry != "" {
						nestedPassengers["residency_country"] = bodyPassengersResidencyCountry
					}
					if bodyPassengersStudent != 0 {
						nestedPassengers["student"] = bodyPassengersStudent
					}
					if bodyPassengersYouth != 0 {
						nestedPassengers["youth"] = bodyPassengersYouth
					}
					if len(nestedPassengers) > 0 {
						body["passengers"] = nestedPassengers
					}
				}
				{
					nestedPriceConstraints := map[string]any{}
					if bodyPriceConstraintsMaxTotal != 0.0 {
						nestedPriceConstraints["max_total"] = bodyPriceConstraintsMaxTotal
					}
					if len(nestedPriceConstraints) > 0 {
						body["price_constraints"] = nestedPriceConstraints
					}
				}
				if bodySort != "" {
					body["sort"] = bodySort
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "flights", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "flights", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "flights", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "flights", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "flights", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "flights",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "flights", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "flights", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAllowLiveShop, "allow-live-shop", false, "AllowLiveShop enables live shopping in parallel with cached search. Default false — only opted-in consumers (e.g.")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "Currency code for pricing (e.g., 'USD', 'EUR', 'GBP')")
	cmd.Flags().StringVar(&bodyFiltersCabinClass, "filters-cabin-class", "", "Cabin class filter (optional): filters by cabin class")
	cmd.Flags().StringVar(&bodyFiltersDatesDepartureDateRanges, "filters-dates-departure-date-ranges", "", "Multiple departure date ranges")
	cmd.Flags().StringVar(&bodyFiltersDatesDepartureDates, "filters-dates-departure-dates", "", "Multiple departure dates (list of specific dates)")
	cmd.Flags().StringVar(&bodyFiltersDatesReturnDateRanges, "filters-dates-return-date-ranges", "", "Multiple return date ranges")
	cmd.Flags().StringVar(&bodyFiltersDatesReturnDates, "filters-dates-return-dates", "", "Multiple return dates (list of specific dates)")
	cmd.Flags().IntVar(&bodyFiltersDatesStayDays, "filters-dates-stay-days", 0, "Length of stay in days (for roundtrip flights)")
	cmd.Flags().BoolVar(&bodyFiltersDirectOnly, "filters-direct-only", false, "Direct flights only (maps to lowest_direct_fare in ES)")
	cmd.Flags().StringVar(&bodyFiltersLocationsDestinationType, "filters-locations-destination-type", "", "'city' or 'airport' — defaults to 'city' if empty")
	cmd.Flags().StringVar(&bodyFiltersLocationsDestinations, "filters-locations-destinations", "", "One or more destination IATA codes (required)")
	cmd.Flags().StringVar(&bodyFiltersLocationsOriginType, "filters-locations-origin-type", "", "'city' or 'airport' — defaults to 'city' if empty")
	cmd.Flags().StringVar(&bodyFiltersLocationsOrigins, "filters-locations-origins", "", "One or more origin IATA codes (required)")
	cmd.Flags().StringVar(&bodyFiltersTripType, "filters-trip-type", "", "Trip type (required): 'oneway' or 'roundtrip'")
	cmd.Flags().IntVar(&bodyLimit, "limit", 0, "Limit - maximum number of results to return (default: 20, max: 100)")
	cmd.Flags().StringVar(&bodyLocale, "locale", "", "Locale code for results (e.g., 'en', 'fr', 'de', 'es', 'zh', 'ja', 'ko')")
	cmd.Flags().StringVar(&bodyOffset, "offset", "", "Offset - number of results to skip for pagination (default: 0)")
	cmd.Flags().IntVar(&bodyPassengersAdt, "passengers-adt", 0, "Adt")
	cmd.Flags().StringVar(&bodyPassengersAges, "passengers-ages", "", "Ages")
	cmd.Flags().IntVar(&bodyPassengersChd, "passengers-chd", 0, "Chd")
	cmd.Flags().IntVar(&bodyPassengersInfLap, "passengers-inf-lap", 0, "Inf lap")
	cmd.Flags().IntVar(&bodyPassengersInfSeat, "passengers-inf-seat", 0, "Inf seat")
	cmd.Flags().StringVar(&bodyPassengersResidencyCountry, "passengers-residency-country", "", "Residency country")
	cmd.Flags().IntVar(&bodyPassengersStudent, "passengers-student", 0, "Student")
	cmd.Flags().IntVar(&bodyPassengersYouth, "passengers-youth", 0, "Youth")
	cmd.Flags().Float64Var(&bodyPriceConstraintsMaxTotal, "price-constraints-max-total", 0.0, "Maximum total price in USD (will be converted to NUC millicents for ES query)")
	cmd.Flags().StringVar(&bodySort, "sort", "", "Sort criteria: 'lowest' (default) or 'recommendation'")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin (use this for deeply nested fields not exposed as flags)")

	return cmd
}
