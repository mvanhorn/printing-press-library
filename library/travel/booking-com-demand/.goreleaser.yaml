version: 2
project_name: booking-com-demand-pp-cli
changelog:
  disable: true
builds:
  - id: booking-com-demand-pp-cli
    main: ./cmd/booking-com-demand-pp-cli
    binary: booking-com-demand-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/travel/booking-com-demand/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: booking-com-demand-pp-mcp
    main: ./cmd/booking-com-demand-pp-mcp
    binary: booking-com-demand-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: booking-com-demand-pp-cli
    repository:
      owner: netnull
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Every Booking.com Demand API feature, plus offline search, price drift detection, and review-based property ranking..."
    install: |
      bin.install "booking-com-demand-pp-cli"
      bin.install "booking-com-demand-pp-mcp"
