// Copyright 2026 netnull. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCarsSearchCmd(flags *rootFlags) *cobra.Command {
	var bodyBookerCountry string
	var bodyCurrency string
	var bodyDriverAge int
	var bodyFiltersAirConditioning bool
	var bodyFiltersCarCategories string
	var bodyFiltersDepotLocationType string
	var bodyFiltersMileageType string
	var bodyFiltersNumberOfSeats int
	var bodyFiltersSupplierIds string
	var bodyFiltersTransmissionType string
	var bodyLanguage string
	var bodyMaximumResults int
	var bodyPage string
	var bodyPaymentTimings string
	var bodyRouteDropoffDatetime string
	var bodyRouteDropoffLocation string
	var bodyRoutePickupDatetime string
	var bodyRoutePickupLocation string
	var bodySortBy string
	var bodySortDirection string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "search",
		Short:       "Use this endpoint to retrieve the available car rentals matching the search criteria.",
		Example:     "  booking-com-demand-pp-cli cars search --currency example-value",
		Annotations: map[string]string{"pp:endpoint": "cars.search", "pp:method": "POST", "pp:path": "/cars/search", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("booker-country") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "booker-country")
				}
				if !cmd.Flags().Changed("currency") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "currency")
				}
				if !cmd.Flags().Changed("driver-age") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "driver-age")
				}
				if !cmd.Flags().Changed("route-dropoff-datetime") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "route-dropoff-datetime")
				}
				if !cmd.Flags().Changed("route-dropoff-location") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "route-dropoff-location")
				}
				if !cmd.Flags().Changed("route-pickup-datetime") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "route-pickup-datetime")
				}
				if !cmd.Flags().Changed("route-pickup-location") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "route-pickup-location")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/cars/search"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedBooker := map[string]any{}
					if bodyBookerCountry != "" {
						nestedBooker["country"] = bodyBookerCountry
					}
					if len(nestedBooker) > 0 {
						body["booker"] = nestedBooker
					}
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				{
					nestedDriver := map[string]any{}
					if bodyDriverAge != 0 {
						nestedDriver["age"] = bodyDriverAge
					}
					if len(nestedDriver) > 0 {
						body["driver"] = nestedDriver
					}
				}
				{
					nestedFilters := map[string]any{}
					if bodyFiltersAirConditioning != false {
						nestedFilters["air_conditioning"] = bodyFiltersAirConditioning
					}
					if bodyFiltersCarCategories != "" {
						nestedFilters["car_categories"] = bodyFiltersCarCategories
					}
					if bodyFiltersDepotLocationType != "" {
						nestedFilters["depot_location_type"] = bodyFiltersDepotLocationType
					}
					if bodyFiltersMileageType != "" {
						nestedFilters["mileage_type"] = bodyFiltersMileageType
					}
					if bodyFiltersNumberOfSeats != 0 {
						nestedFilters["number_of_seats"] = bodyFiltersNumberOfSeats
					}
					if bodyFiltersSupplierIds != "" {
						var parsedFiltersSupplierIds any
						if err := json.Unmarshal([]byte(bodyFiltersSupplierIds), &parsedFiltersSupplierIds); err != nil {
							return fmt.Errorf("parsing --filters-supplier-ids JSON: %w", err)
						}
						nestedFilters["supplier_ids"] = parsedFiltersSupplierIds
					}
					if bodyFiltersTransmissionType != "" {
						nestedFilters["transmission_type"] = bodyFiltersTransmissionType
					}
					if len(nestedFilters) > 0 {
						body["filters"] = nestedFilters
					}
				}
				if bodyLanguage != "" {
					body["language"] = bodyLanguage
				}
				if bodyMaximumResults != 0 {
					body["maximum_results"] = bodyMaximumResults
				}
				if bodyPage != "" {
					body["page"] = bodyPage
				}
				{
					nestedPayment := map[string]any{}
					if bodyPaymentTimings != "" {
						var parsedPaymentTimings any
						if err := json.Unmarshal([]byte(bodyPaymentTimings), &parsedPaymentTimings); err != nil {
							return fmt.Errorf("parsing --payment-timings JSON: %w", err)
						}
						nestedPayment["timings"] = parsedPaymentTimings
					}
					if len(nestedPayment) > 0 {
						body["payment"] = nestedPayment
					}
				}
				{
					nestedRoute := map[string]any{}
					{
						nestedRouteDropoff := map[string]any{}
						if bodyRouteDropoffDatetime != "" {
							nestedRouteDropoff["datetime"] = bodyRouteDropoffDatetime
						}
						if bodyRouteDropoffLocation != "" {
							nestedRouteDropoff["location"] = bodyRouteDropoffLocation
						}
						if len(nestedRouteDropoff) > 0 {
							nestedRoute["dropoff"] = nestedRouteDropoff
						}
					}
					{
						nestedRoutePickup := map[string]any{}
						if bodyRoutePickupDatetime != "" {
							nestedRoutePickup["datetime"] = bodyRoutePickupDatetime
						}
						if bodyRoutePickupLocation != "" {
							nestedRoutePickup["location"] = bodyRoutePickupLocation
						}
						if len(nestedRoutePickup) > 0 {
							nestedRoute["pickup"] = nestedRoutePickup
						}
					}
					if len(nestedRoute) > 0 {
						body["route"] = nestedRoute
					}
				}
				{
					nestedSort := map[string]any{}
					if bodySortBy != "" {
						nestedSort["by"] = bodySortBy
					}
					if bodySortDirection != "" {
						nestedSort["direction"] = bodySortDirection
					}
					if len(nestedSort) > 0 {
						body["sort"] = nestedSort
					}
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "cars",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyBookerCountry, "booker-country", "", "The booker country for showing the best price for that user and obeying laws regarding the display of taxes and fees.")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "A three-letter code that uniquely identifies a monetary currency as defined by the ISO 4217 standard. The full list...")
	cmd.Flags().IntVar(&bodyDriverAge, "driver-age", 0, "Driver age. Affect the availability and price of the products.")
	cmd.Flags().BoolVar(&bodyFiltersAirConditioning, "filters-air-conditioning", false, "Filters the results to include car rentals with or without air conditioning. Setting this value to true will only...")
	cmd.Flags().StringVar(&bodyFiltersCarCategories, "filters-car-categories", "", "Filters the result based on the selected car category.")
	cmd.Flags().StringVar(&bodyFiltersDepotLocationType, "filters-depot-location-type", "", "Indicates the specific type of location where the car rental depot is situated. This helps you accurately inform...")
	cmd.Flags().StringVar(&bodyFiltersMileageType, "filters-mileage-type", "", "Filters the results based on the mileage type associated with the rental vehicle")
	cmd.Flags().IntVar(&bodyFiltersNumberOfSeats, "filters-number-of-seats", 0, "Filters the results based on the number of seats in the vehicle. This allows you to specify a vehicle that can...")
	cmd.Flags().StringVar(&bodyFiltersSupplierIds, "filters-supplier-ids", "", "Filters the results based on the supplier ID of the vehicle. This allows you to retrieve vehicles from specific...")
	cmd.Flags().StringVar(&bodyFiltersTransmissionType, "filters-transmission-type", "", "Filters the results based on the transmission type of the vehicle.")
	cmd.Flags().StringVar(&bodyLanguage, "language", "", "Effects on the selected language of the target website after redirection by provided url.")
	cmd.Flags().IntVar(&bodyMaximumResults, "maximum-results", 100, "The maximum number of results to return per page.")
	cmd.Flags().StringVar(&bodyPage, "page", "", "Pagination token used to retrieve the next page of results. Obtained from `next_page`.")
	cmd.Flags().StringVar(&bodyPaymentTimings, "payment-timings", "", "Filters the results to include only car rentals that offer the specified payment timings. Use this parameter to...")
	cmd.Flags().StringVar(&bodyRouteDropoffDatetime, "route-dropoff-datetime", "", "Pick up / drop off datetime.")
	cmd.Flags().StringVar(&bodyRouteDropoffLocation, "route-dropoff-location", "", "Pick up / drop off location.")
	cmd.Flags().StringVar(&bodyRoutePickupDatetime, "route-pickup-datetime", "", "Pick up / drop off datetime.")
	cmd.Flags().StringVar(&bodyRoutePickupLocation, "route-pickup-location", "", "Pick up / drop off location.")
	cmd.Flags().StringVar(&bodySortBy, "sort-by", "", "The way to sort your results.")
	cmd.Flags().StringVar(&bodySortDirection, "sort-direction", "", "The direction you wish for your sort.by parameter to be sorted in.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
