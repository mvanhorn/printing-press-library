// Copyright 2026 netnull. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAccommodationsAvailabilityCmd(flags *rootFlags) *cobra.Command {
	var bodyAccommodation int
	var bodyBookerCountry string
	var bodyBookerPlatform string
	var bodyBookerState string
	var bodyBookerTravelPurpose string
	var bodyBookerUserGroups string
	var bodyCheckin string
	var bodyCheckout string
	var bodyCurrency string
	var bodyExtras string
	var bodyGuestsAllocation string
	var bodyGuestsChildren string
	var bodyGuestsNumberOfAdults int
	var bodyGuestsNumberOfRooms int
	var bodyPayment string
	var bodyProducts string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "availability",
		Aliases:     []string{"create"},
		Short:       "Use this endpoint to return detailed product availability, price and charges of the accommodation matching a given...",
		Example:     "  booking-com-demand-pp-cli accommodations availability --checkin 2026-01-15",
		Annotations: map[string]string{"pp:endpoint": "accommodations.availability", "pp:method": "POST", "pp:path": "/accommodations/availability"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("accommodation") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "accommodation")
				}
				if !cmd.Flags().Changed("booker-country") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "booker-country")
				}
				if !cmd.Flags().Changed("booker-platform") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "booker-platform")
				}
				if !cmd.Flags().Changed("checkin") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "checkin")
				}
				if !cmd.Flags().Changed("checkout") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "checkout")
				}
				if !cmd.Flags().Changed("guests-number-of-adults") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "guests-number-of-adults")
				}
				if !cmd.Flags().Changed("guests-number-of-rooms") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "guests-number-of-rooms")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/accommodations/availability"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAccommodation != 0 {
					body["accommodation"] = bodyAccommodation
				}
				{
					nestedBooker := map[string]any{}
					if bodyBookerCountry != "" {
						nestedBooker["country"] = bodyBookerCountry
					}
					if bodyBookerPlatform != "" {
						nestedBooker["platform"] = bodyBookerPlatform
					}
					if bodyBookerState != "" {
						nestedBooker["state"] = bodyBookerState
					}
					if bodyBookerTravelPurpose != "" {
						nestedBooker["travel_purpose"] = bodyBookerTravelPurpose
					}
					if bodyBookerUserGroups != "" {
						var parsedBookerUserGroups any
						if err := json.Unmarshal([]byte(bodyBookerUserGroups), &parsedBookerUserGroups); err != nil {
							return fmt.Errorf("parsing --booker-user-groups JSON: %w", err)
						}
						nestedBooker["user_groups"] = parsedBookerUserGroups
					}
					if len(nestedBooker) > 0 {
						body["booker"] = nestedBooker
					}
				}
				if bodyCheckin != "" {
					body["checkin"] = bodyCheckin
				}
				if bodyCheckout != "" {
					body["checkout"] = bodyCheckout
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				if bodyExtras != "" {
					var parsedExtras any
					if err := json.Unmarshal([]byte(bodyExtras), &parsedExtras); err != nil {
						return fmt.Errorf("parsing --extras JSON: %w", err)
					}
					body["extras"] = parsedExtras
				}
				{
					nestedGuests := map[string]any{}
					if bodyGuestsAllocation != "" {
						var parsedGuestsAllocation any
						if err := json.Unmarshal([]byte(bodyGuestsAllocation), &parsedGuestsAllocation); err != nil {
							return fmt.Errorf("parsing --guests-allocation JSON: %w", err)
						}
						nestedGuests["allocation"] = parsedGuestsAllocation
					}
					if bodyGuestsChildren != "" {
						var parsedGuestsChildren any
						if err := json.Unmarshal([]byte(bodyGuestsChildren), &parsedGuestsChildren); err != nil {
							return fmt.Errorf("parsing --guests-children JSON: %w", err)
						}
						nestedGuests["children"] = parsedGuestsChildren
					}
					if bodyGuestsNumberOfAdults != 0 {
						nestedGuests["number_of_adults"] = bodyGuestsNumberOfAdults
					}
					if bodyGuestsNumberOfRooms != 0 {
						nestedGuests["number_of_rooms"] = bodyGuestsNumberOfRooms
					}
					if len(nestedGuests) > 0 {
						body["guests"] = nestedGuests
					}
				}
				if bodyPayment != "" {
					body["payment"] = bodyPayment
				}
				if bodyProducts != "" {
					var parsedProducts any
					if err := json.Unmarshal([]byte(bodyProducts), &parsedProducts); err != nil {
						return fmt.Errorf("parsing --products JSON: %w", err)
					}
					body["products"] = parsedProducts
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "accommodations",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyAccommodation, "accommodation", 0, "A signed integer number that uniquely identifies an accommodation property. The full list can be obtained by calling...")
	cmd.Flags().StringVar(&bodyBookerCountry, "booker-country", "", "The booker country for showing the best price for that user and obeying laws regarding the display of taxes and fees.")
	cmd.Flags().StringVar(&bodyBookerPlatform, "booker-platform", "", "The booker platform for showing the platform based deals and prices.")
	cmd.Flags().StringVar(&bodyBookerState, "booker-state", "", "The booker state for showing the best price for that user and obeying laws regarding the display of taxes and fees....")
	cmd.Flags().StringVar(&bodyBookerTravelPurpose, "booker-travel-purpose", "", "The travel purpose of the booker.")
	cmd.Flags().StringVar(&bodyBookerUserGroups, "booker-user-groups", "", "The user groups that the booker is a member of.")
	cmd.Flags().StringVar(&bodyCheckin, "checkin", "", "The checkin date. Must be within 500 days in the future and in the format yyyy-mm-dd.")
	cmd.Flags().StringVar(&bodyCheckout, "checkout", "", "The checkout date. Must be later than {checkin}. Must be between 1 and 90 days after {checkin}. Must be within 500...")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "A three-letter code that uniquely identifies a monetary currency as defined by the ISO 4217 standard. The full list...")
	cmd.Flags().StringVar(&bodyExtras, "extras", "", "Input parameter to request for additional information about this product.include_bundle_variants must be passed in...")
	cmd.Flags().StringVar(&bodyGuestsAllocation, "guests-allocation", "", "The exact allocation of guests to rooms.")
	cmd.Flags().StringVar(&bodyGuestsChildren, "guests-children", "", "Array with the children ages.")
	cmd.Flags().IntVar(&bodyGuestsNumberOfAdults, "guests-number-of-adults", 0, "The number of adults for the search.")
	cmd.Flags().IntVar(&bodyGuestsNumberOfRooms, "guests-number-of-rooms", 0, "The number of rooms needed.")
	cmd.Flags().StringVar(&bodyPayment, "payment", "", "Payment input information to filter results.")
	cmd.Flags().StringVar(&bodyProducts, "products", "", "Products")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
