// Copyright 2026 nspage and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is seeded
// at generation time from the verified NovelFeature list that drives the
// SKILL.md feature section, so the command a `which` query returns is
// guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "scout budget", Description: "Band market listings by hourly/day rate so you can see what a city+activity actually costs.", Group: "Market intelligence", WhyItMatters: "Use when planning a budget before deep-diving individual venues."},
	{Command: "scout capacity", Description: "Histogram of guest capacity for a synced market so you can re-cut when headcount changes.", Group: "Market intelligence", WhyItMatters: "Use when guest count is the binding constraint, not price."},
	{Command: "shortlist compare", Description: "Join your favorites board to listing attributes for a sortable offline comparison table.", Group: "Shortlist intelligence", WhyItMatters: "Use when deciding among saved venues without reopening browser tabs."},
	{Command: "shortlist create-board", Description: "Create a Peerspace favorite board and attach a listing (cookie auth).", Group: "Shortlist intelligence", WhyItMatters: "Use when starting a new shortlist board from a space you want to save."},
	{Command: "shortlist add", Description: "Favorite a space by attaching its listing id to an existing favorite board (cookie auth).", Group: "Shortlist intelligence", WhyItMatters: "Use when saving a listing onto a board you already have without opening the website."},
	{Command: "shortlist hydrate", Description: "Fetch full listing page details (about, rules, parking, amenities) for board or listing IDs into SQLite.", Group: "Shortlist intelligence", WhyItMatters: "Use after shortlisting to pull deeper page blocks for stakeholder export and format filtering."},
	{Command: "venues get", Description: "Lookup or live-fetch one listing detail document (GET /v1/listings/{id}) including rules and parking.", Group: "Tech event planning", WhyItMatters: "Use when you need full page context for one venue without hydrating a whole board."},
	{Command: "calendar availability-start", Description: "List available start times for a space on a given day.", Group: "Booking readiness", WhyItMatters: "Use when checking if a shortlisted venue is free for your event day."},
	{Command: "calendar availability-end", Description: "List available end times after a chosen start slot.", Group: "Booking readiness", WhyItMatters: "Use after availability-start to pick an end time for a quote."},
	{Command: "contracts guest-quote", Description: "Price a guest booking request (contact host / request flow).", Group: "Booking readiness", WhyItMatters: "Use when preparing a request-to-book quote without the website UI."},
	{Command: "contracts inquiry-quote", Description: "Price a host inquiry while composing a message (message host form).", Group: "Booking readiness", WhyItMatters: "Use when filling the Message host form and checking estimated cost before send."},
	{Command: "contracts inquiry-send", Description: "Send a message/inquiry to a listing host (message host, contact owner).", Group: "Booking readiness", WhyItMatters: "Use to contact a host with dates, guest count, and a message (requires --yes)."},
	{Command: "calendar availability-month", Description: "Month-level availability for a space.", Group: "Booking readiness", WhyItMatters: "Use to see which days in a month have openings before picking a start time."},
	{Command: "spaces faqs-event", Description: "Event FAQs for a space.", Group: "Booking readiness", WhyItMatters: "Use to read host event FAQ answers for a space."},
	{Command: "verification listing", Description: "Check whether the guest needs verification for a listing.", Group: "Booking readiness", WhyItMatters: "Use before requesting a booking to see if ID verification will block you."},
	{Command: "messages unread", Description: "Inbox unread thread count (v2 messaging).", Group: "Agent-native plumbing", WhyItMatters: "Use for a lightweight inbox check while coordinating hosts."},
	{Command: "markets pulse", Description: "Cross-city rollup of median price, listing density, instant-book share, and capacity quantiles.", Group: "Market intelligence", WhyItMatters: "Use when comparing host cities for the same event type."},
	{Command: "shortlist delta", Description: "Show favorites added, removed, or changed since the last sync snapshot.", Group: "Shortlist intelligence", WhyItMatters: "Use to see what changed on your board without scrolling hearts."},
	{Command: "markets neighborhoods", Description: "Per-neighborhood listing stats plus optional --vibe tech keyword signals from descriptions (WiFi, projector, transit) and a built-in tech-cluster map.", Group: "Market intelligence", WhyItMatters: "Use to pick tech-friendly neighborhoods before refining a shortlist."},
	{Command: "shortlist similar", Description: "Find local listings near a favorite on capacity, price, and neighborhood.", Group: "Shortlist intelligence", WhyItMatters: "Use when a favorite is booked or too expensive and you need substitutes."},
	{Command: "projects scout", Description: "Combine project location metadata with fitting listings from the local store.", Group: "Shortlist intelligence", WhyItMatters: "Use when a saved project needs a ready market pack."},
	{Command: "sync coverage", Description: "Report which market query keys are present, row counts, and last-synced times.", Group: "Local state that compounds", WhyItMatters: "Use before scout/markets/sql so agents fail closed on empty markets."},
	{Command: "pulse", Description: "One cookie-auth shot: message thread count, favorites summary, profile identity, and optional recent shortlist activity.", Group: "Agent-native plumbing", WhyItMatters: "Use for a daily inbox + shortlist health check without opening Peerspace."},
	{Command: "venues recommend", Description: "Rank synced venues for tech meetups/workshops by headcount, date window, budget, and vibe keywords.", Group: "Tech event planning", WhyItMatters: "Use when an agent or planner needs a ranked shortlist for a tech event, not a raw search scroll."},
	{Command: "shortlist export", Description: "Export favorites as clean JSON plus a markdown block (price, capacity, amenities, fit notes) for Eventbrite/Luma/Slack.", Group: "Shortlist intelligence", WhyItMatters: "Use when turning a Peerspace shortlist into a Luma/Eventbrite draft or Slack approval message."},
	{Command: "shortlist drift", Description: "Track price and availability-field changes on favorites over time (Luma-style watch).", Group: "Shortlist intelligence", WhyItMatters: "Use to catch rate spikes or lost instant-book before you pitch a venue."},
	{Command: "scout gaps", Description: "Surface missing tech-event must-haves (WiFi, AV/projector, late access, flexible seating, transit) on any shortlist.", Group: "Tech event planning", WhyItMatters: "Use before finalizing a venue to see what each shortlist option lacks for a tech event."},
	{Command: "scout multi-city", Description: "Cross-city comparison with top venue options per city under shared tech-event constraints.", Group: "Tech event planning", WhyItMatters: "Use for multi-city meetup tours or choosing a host city under the same constraints."},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	why := strings.ToLower(e.WhyItMatters)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description (full query phrase).
	if strings.Contains(desc, query) {
		score += 2
	}
	// Per-token hits on description / why-it-matters so multi-word
	// agent phrasing like "message host" still resolves.
	for _, qt := range qTokens {
		if len(qt) < 3 {
			continue
		}
		if strings.Contains(desc, qt) {
			score += 1
		}
		if why != "" && strings.Contains(why, qt) {
			score += 1
		}
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"mcp:read-only":       "true",
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  peerspace-pp-cli which "stale tickets"
  peerspace-pp-cli which "bottleneck"
  peerspace-pp-cli which --limit 1 "send message"
  peerspace-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
