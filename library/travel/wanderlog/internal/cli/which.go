// Copyright 2026 zjsng and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string   `json:"command"`
	Description  string   `json:"description"`
	Group        string   `json:"group,omitempty"`
	WhyItMatters string   `json:"why_it_matters,omitempty"`
	Aliases      []string `json:"aliases,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "trips home", Description: "list my trips, home trips, own itineraries, account trip list for the authenticated Wanderlog account.", Group: "Authenticated trips", WhyItMatters: "Use this to discover editable trip keys (field: key) before plan outline or edits.", Aliases: []string{"list my trips"}},
	{Command: "plan clone", Description: "Create a new Wanderlog trip from a shared or public source plan, then fill it with the source plan template.", Group: "Plan cloning and fill", WhyItMatters: "Use this when the user wants to turn an existing shared Wanderlog plan into their own new editable trip."},
	{Command: "plan fill", Description: "Fill an existing Wanderlog trip from a shared or public source plan with dry-run and force safeguards.", Group: "Plan cloning and fill", WhyItMatters: "Use this when the user already created a target trip and wants to populate it from a shared template."},
	{Command: "plan preview", Description: "Inspect a shared plan and report dates, sections, blocks, resources, and clone warnings before any write.", Group: "Plan cloning and fill", WhyItMatters: "Use this before clone/fill to confirm what will be copied and whether credentials are needed."},
	{Command: "plan sections", Description: "List editable Wanderlog section indexes, day numbers, section ids, dates, block counts, and itinerary issues such as places closed on their dated section.", Group: "Agentic plan editing", WhyItMatters: "Use this before any itinerary edit so the agent targets the right day, section, block, and validity issues."},
	{Command: "plan outline", Description: "show days, slim day list, itinerary outline, section headings, stop names without dumping the full trip JSON.", Group: "Agentic plan editing", WhyItMatters: "Use this as the default inspect path instead of trips get.", Aliases: []string{"show days in my plan"}},
	{Command: "plan inspect", Description: "show days, slim day list, itinerary outline, section headings, stop names, and optional itinerary checks.", Group: "Agentic plan editing", WhyItMatters: "Use this after edits to verify counts, lodging coverage, and closed places."},
	{Command: "places autocomplete", Description: "verify address, geocode, look up place with Wanderlog Places API autocomplete.", Group: "Places", WhyItMatters: "Use this to confirm a place id before adding a stop."},
	{Command: "plan note add", Description: "Add a note block to a selected Wanderlog day or section through ShareDB.", Group: "Agentic plan editing", WhyItMatters: "Use this to add reminders, constraints, reservations, or planning notes."},
	{Command: "plan place add", Description: "Add a real place block to a selected Wanderlog day or section from a place id or query.", Group: "Agentic plan editing", WhyItMatters: "Use this to build the itinerary stop by stop."},
	{Command: "plan fill-day", Description: "Insert a batch of place stops into one Wanderlog day from --stops-json with optional start, end, note, and note_md.", Group: "Agentic plan editing", WhyItMatters: "Use this to draft a full day in one JSON0 batch instead of repeating plan place add."},
	{Command: "plan section swap-days", Description: "Swap the blocks arrays of two day sections in one JSON0 batch.", Group: "Agentic plan editing", WhyItMatters: "Use this to exchange two days' stops without moving each block."},
	{Command: "plan place replace", Description: "Replace only the nested place on an existing block, keeping text, schedule, attachments, hotel, and id.", Group: "Agentic plan editing", WhyItMatters: "Use this to correct a geocode without dropping notes or schedule."},
	{Command: "plan block apply", Description: "Preview or apply a JSON0 operation array from --ops-file.", Group: "Agentic plan editing", WhyItMatters: "Use this to submit a prepared ShareDB batch without inlining --op JSON."},
	{Command: "lodging search", Description: "Search Wanderlog lodging and hotel candidates using the itinerary Lodging button endpoint.", Group: "Lodging search", WhyItMatters: "Use this before choosing a hotel base so the agent compares lodging candidates from Wanderlog's own hotel search surface."},
	{Command: "plan reservation list", Description: "List first-class Wanderlog reservation and standalone attachment blocks, optionally filtered by kind.", Group: "Reservations and attachments", WhyItMatters: "Use this before adding or changing flights, lodging, rental cars, restaurants, trains, buses, ferries, cruises, or general attachments."},
	{Command: "plan reservation add", Description: "Add a first-class Wanderlog flight, lodging, rental car, restaurant, train, bus, ferry, cruise, or standalone attachment block.", Group: "Reservations and attachments", WhyItMatters: "Use this for the Reservations and attachments menu in plan mode instead of flattening bookings into generic notes."},
	{Command: "plan reservation add --kind lodging", Description: "Add a hotel or lodging base directly into the itinerary as a place block with hotel check-in, check-out, guest, and confirmation metadata. multi-night stay, span nights, accommodation bases, hotel every night.", Group: "Reservations and attachments", WhyItMatters: "Use this when choosing or recording the trip base because it anchors location planning, route assumptions, and lodging budget entries."},
	{Command: "plan reservation edit", Description: "Set or remove a field on a first-class reservation or standalone attachment block.", Group: "Reservations and attachments", WhyItMatters: "Use this to update confirmation numbers, dates, times, carriers, guest names, restaurant party size, or attachment titles."},
	{Command: "plan reservation remove", Description: "Remove a first-class reservation or standalone attachment block.", Group: "Reservations and attachments", WhyItMatters: "Use this to clean up cancelled bookings, stale travel segments, or test reservation blocks."},
	{Command: "plan block move", Description: "Move a note or place block within a day or across Wanderlog days.", Group: "Agentic plan editing", WhyItMatters: "Use this to reorder an itinerary after adding candidate stops."},
	{Command: "plan block delete", Description: "Delete a note or place block from a selected Wanderlog day or section.", Group: "Agentic plan editing", WhyItMatters: "Use this to remove test blocks or rejected itinerary candidates."},
	{Command: "plan block edit-text", Description: "plain text only unless --markdown. Cannot bold or bullets without --markdown. Edit or replace the note attached to a Wanderlog stop or block.", Group: "Agentic plan editing", WhyItMatters: "Use this to revise stop notes, reservation notes, or reminders after a block exists."},
	{Command: "plan block edit-text --markdown", Description: "format notes, bullets, rich text. Compile markdown bold and bullet lists into Wanderlog note formatting.", Group: "Agentic plan editing", WhyItMatters: "Use this to format notes with bullets rather than cloning a plan or applying raw JSON0.", Aliases: []string{"format notes with bullets"}},
	{Command: "plan block rename", Description: "rename place, rename lodging, change place name, display name on a Wanderlog block.", Group: "Agentic plan editing", WhyItMatters: "Use this to rename a hotel or place without editing protected place blobs via set-field.", Aliases: []string{"rename a hotel"}},
	{Command: "plan block set-field", Description: "Set or remove a supported or newly observed field on a Wanderlog block.", Group: "Agentic plan editing", WhyItMatters: "Use this for fields discovered in live plan data, such as schedule or metadata fields, without waiting for a bespoke command."},
	{Command: "plan section add-day", Description: "Insert a new day section into an editable Wanderlog plan and update trip date bounds.", Group: "Agentic plan editing", WhyItMatters: "Use this when the itinerary needs an extra travel day."},
	{Command: "plan section set-field", Description: "Set or remove fields on a Wanderlog section, including headings and rich text.", Group: "Agentic plan editing", WhyItMatters: "Use this to label days or adjust section-level notes."},
	{Command: "plan section delete", Description: "Delete an empty Wanderlog section and update trip day counts/date bounds.", Group: "Agentic plan editing", WhyItMatters: "Use this to remove unused days or sections after reviewing the target."},
	{Command: "plan block schedule", Description: "Set or clear start time, end time, duration, and timezone fields on a Wanderlog block. set stop times.", Group: "Agentic plan editing", WhyItMatters: "Use this to turn loose stops into a timed itinerary.", Aliases: []string{"set stop times"}},
	{Command: "plan block attachment add", Description: "Attach URL/file metadata to a Wanderlog block.", Group: "Agentic plan editing", WhyItMatters: "Use this for tickets, booking links, PDFs, and other planning artifacts."},
	{Command: "plan block attachment list", Description: "List attachment metadata on a Wanderlog block.", Group: "Agentic plan editing", WhyItMatters: "Use this before changing or removing existing block attachments."},
	{Command: "plan block attachment remove", Description: "Remove an attachment from a Wanderlog block.", Group: "Agentic plan editing", WhyItMatters: "Use this to clean up stale booking links or test attachments."},
	{Command: "plan checklist add", Description: "Add a checklist block to a selected Wanderlog day or section.", Group: "Agentic plan editing", WhyItMatters: "Use this for packing lists, pre-trip tasks, and shared planning todos."},
	{Command: "plan checklist item add", Description: "Add an item to an existing Wanderlog checklist block.", Group: "Agentic plan editing", WhyItMatters: "Use this to grow shared todo lists without replacing the whole block."},
	{Command: "plan checklist item check", Description: "Set checked state on a Wanderlog checklist item.", Group: "Agentic plan editing", WhyItMatters: "Use this to mark planning tasks complete or reopen them."},
	{Command: "plan checklist item remove", Description: "Remove an item from a Wanderlog checklist block.", Group: "Agentic plan editing", WhyItMatters: "Use this to clean up rejected or obsolete tasks."},
	{Command: "plan votes", Description: "who upvoted places, place votes, upvotedBy counts on itinerary stops (not comment votes).", Group: "Collaboration", WhyItMatters: "Use this to tally collaborator place picks; comments list is not votes.", Aliases: []string{"who upvoted places"}},
	{Command: "plan comments list", Description: "List comments for a Wanderlog plan.", Group: "Collaboration", WhyItMatters: "Use this to read discussion context from friends before editing."},
	{Command: "plan comments add", Description: "Add a comment or reply to a Wanderlog plan.", Group: "Collaboration", WhyItMatters: "Use this to ask collaborators questions or explain agent-made edits."},
	{Command: "plan comments edit", Description: "Edit an existing Wanderlog comment.", Group: "Collaboration", WhyItMatters: "Use this to correct an agent-authored comment."},
	{Command: "plan comments delete", Description: "Delete a Wanderlog comment.", Group: "Collaboration", WhyItMatters: "Use this to remove obsolete agent-authored comments."},
	{Command: "plan comments vote", Description: "Vote on a Wanderlog comment.", Group: "Collaboration", WhyItMatters: "Use this to reflect preference or acknowledgement in collaborator discussion."},
	{Command: "plan collaborators", Description: "Inspect collaborator/share metadata available on a Wanderlog tripPlan payload.", Group: "Collaboration", WhyItMatters: "Use this before account-specific sharing, invite, or permission workflows."},
	{Command: "plan collaborators invites", Description: "List pending Wanderlog trip invites.", Group: "Collaboration", WhyItMatters: "Use this to avoid duplicate invites and understand outstanding collaboration requests."},
	{Command: "plan collaborators invite", Description: "Send Wanderlog trip plan invites to friends by email or user id.", Group: "Collaboration", WhyItMatters: "Use this to bring friends into the editable trip plan after confirming recipients."},
	{Command: "plan collaborators add", Description: "Add a Wanderlog collaborator directly by user id.", Group: "Collaboration", WhyItMatters: "Use this when you have a confirmed Wanderlog user id and need direct collaborator access."},
	{Command: "plan collaborators remove", Description: "Remove a Wanderlog collaborator by user id.", Group: "Collaboration", WhyItMatters: "Use this to clean up accidental or outdated collaborator access."},
	{Command: "plan collaborators share-key", Description: "Create or refresh a Wanderlog share key with a permissions value.", Group: "Collaboration", WhyItMatters: "Use this to generate a share link with explicit permissions after checking current keys."},
	{Command: "plan budget summary", Description: "Summarize Wanderlog trip budget totals, expense counts, payment counts, and spending by category/currency.", Group: "Budget", WhyItMatters: "Use this before adding or changing costs so the agent understands the current budget state."},
	{Command: "plan budget csv", Description: "Export Wanderlog budget expenses as CSV from the confirmed expensesAsCSV endpoint.", Group: "Budget", WhyItMatters: "Use this when the user needs a spreadsheet-friendly budget export."},
	{Command: "plan budget set", Description: "Set Wanderlog trip budget total, currency, or debt simplification settings.", Group: "Budget", WhyItMatters: "Use this to establish the trip budget envelope before adding itemized expenses."},
	{Command: "plan budget expense list", Description: "List Wanderlog budget expenses with ids, amounts, categories, splits, dates, and block links.", Group: "Budget", WhyItMatters: "Use this before editing or removing an expense."},
	{Command: "plan budget expense add", Description: "Add a Wanderlog budget expense with category, payer, split mode, date, and optional itinerary block association.", Group: "Budget", WhyItMatters: "Use this to capture planned or actual travel costs agentically."},
	{Command: "plan budget expense edit", Description: "Edit a Wanderlog budget expense by id or index.", Group: "Budget", WhyItMatters: "Use this to correct amounts, categories, dates, splits, or linked itinerary blocks."},
	{Command: "plan budget expense remove", Description: "Remove a Wanderlog budget expense by id or index.", Group: "Budget", WhyItMatters: "Use this to clean up rejected or duplicate costs."},
	{Command: "plan budget payment list", Description: "List Wanderlog budget settlement payments.", Group: "Budget", WhyItMatters: "Use this before changing settlement records."},
	{Command: "plan budget payment add", Description: "Add a Wanderlog budget settlement payment from one collaborator to another.", Group: "Budget", WhyItMatters: "Use this to record who paid whom after expenses are split."},
	{Command: "plan budget payment remove", Description: "Remove a Wanderlog budget settlement payment by id or index.", Group: "Budget", WhyItMatters: "Use this to clean up incorrect settlement records."},
	{Command: "plan route day-body", Description: "Build a route optimization JSON body from the places in one itinerary day.", Group: "Agentic plan editing", WhyItMatters: "Use this to inspect what will be sent to Wanderlog route optimization."},
	{Command: "plan route optimize", Description: "Call Wanderlog route optimization with an exact JSON body or a day-derived body.", Group: "Agentic plan editing", WhyItMatters: "Use this to compute better ordering/travel paths, then apply moves deliberately."},
	{Command: "plan history", Description: "List local undo and redo journal entries for ShareDB itinerary edits.", Group: "Agentic plan editing", WhyItMatters: "Use this before undoing or redoing agent-applied itinerary changes."},
	{Command: "plan undo", Description: "Undo or reverse my last itinerary edit by applying the inverse operation for the latest applied ShareDB change.", Group: "Agentic plan editing", WhyItMatters: "Use this to undo the last accidental agent itinerary edit without reconstructing the original block manually."},
	{Command: "plan redo", Description: "Redo or restore my last undone itinerary edit by reapplying the latest undone ShareDB change.", Group: "Agentic plan editing", WhyItMatters: "Use this when an undo was too aggressive and the itinerary edit should be restored."},
	{Command: "plan raw op", Description: "Preview or apply an explicit ShareDB JSON0 operation array for unsupported Wanderlog fields, from --op or --ops-file.", Group: "Agentic plan editing", WhyItMatters: "Use this only as an escape hatch after inspecting live plan shape and dry-run output."},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+4  description contains the full query string (in addition to +2)
//	+5  query or token hits an Aliases entry
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichTokens(s string) []string {
	s = strings.NewReplacer("-", " ", "_", " ", "/", " ").Replace(s)
	return strings.Fields(s)
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := whichTokens(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
		// PATCH(amend-2026-08-23: full-query description bonus)
		score += 4
	}
	descTokens := whichTokens(desc)
	for _, qt := range qTokens {
		for _, dt := range descTokens {
			if qt == dt {
				score++
				break
			}
		}
	}
	// PATCH(amend-2026-08-23: alias ranking)
	if whichAliasHits(e.Aliases, query, qTokens) {
		score += 5
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func whichAliasHits(aliases []string, query string, qTokens []string) bool {
	for _, alias := range aliases {
		a := strings.ToLower(strings.TrimSpace(alias))
		if a == "" {
			continue
		}
		if a == query || strings.Contains(query, a) || strings.Contains(a, query) {
			return true
		}
		aTokens := whichTokens(a)
		for _, qt := range qTokens {
			for _, at := range aTokens {
				if qt == at {
					return true
				}
			}
		}
	}
	return false
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"mcp:read-only":       "true",
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  wanderlog-pp-cli which "stale tickets"
  wanderlog-pp-cli which "bottleneck"
  wanderlog-pp-cli which --limit 1 "send message"
  wanderlog-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
