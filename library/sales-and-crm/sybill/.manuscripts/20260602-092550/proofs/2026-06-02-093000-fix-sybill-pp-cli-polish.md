# Sybill CLI — Polish Pass

Scorecard 92 → 93. Verify 100% → 100%. Publish-validate FAIL → PASS. Tools-audit 4 → 0 pending. MCP Desc Quality 7 → 10.

Fixes: creator.name added to manifest; manuscripts bundled into CLI dir (incl. phase5-skip.json); MCP description overrides for health_check/rows_get/sources_get grounded in spec schemas; mcp:read-only on `which`; version thin-short accepted with rationale.

Skipped (non-defects): crm-autofill live 403 (environmental, no key); search has no read-only annotation (it's mcp:hidden); MCP Remote Transport/Tool Design 5/10 (structural for a small alpha API); manuscripts WARN (non-blocking).

ship_recommendation: ship. further_polish_recommended: no. Remaining issues: none.
