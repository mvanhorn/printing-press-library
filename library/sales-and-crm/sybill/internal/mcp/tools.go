// Copyright 2026 riccardovandra and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/sybill/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/sybill/internal/client"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/sybill/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/sybill/internal/config"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/sybill/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/sybill/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("accounts_get",
			mcplib.WithDescription("Get the detailed view for a single account. Required: accountId. Returns array of AccountContact."),
			mcplib.WithString("accountId", mcplib.Required(), mcplib.Description("Account id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/accounts/{accountId}", true, false, nil, []mcpParamBinding{{PublicName: "accountId", WireName: "accountId", Location: "path"}}, []string{"accountId"}),
	)
	s.AddTool(
		mcplib.NewTool("accounts_list",
			mcplib.WithDescription("List accounts accessible to the API key's organization. Optional: name, website, owner (plus 6 more). Returns array of AccountListItem."),
			mcplib.WithString("name", mcplib.Description("Filter by account name (case-insensitive partial match)")),
			mcplib.WithString("website", mcplib.Description("Filter by website URL (case-insensitive partial match)")),
			mcplib.WithString("owner", mcplib.Description("Filter by owner name or email (case-insensitive partial match)")),
			mcplib.WithString("createdBefore", mcplib.Description("Upper bound for created date (ISO 8601, e.g. 2025-06-30T00:00:00Z)")),
			mcplib.WithString("createdAfter", mcplib.Description("Lower bound for created date (ISO 8601)")),
			mcplib.WithString("lastActivityBefore", mcplib.Description("Upper bound for last activity date (ISO 8601)")),
			mcplib.WithString("lastActivityAfter", mcplib.Description("Lower bound for last activity date (ISO 8601)")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of results per page (1-50)")),
			mcplib.WithString("cursor", mcplib.Description("Opaque pagination cursor from a previous response")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/accounts", true, false, nil, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "query"}, {PublicName: "website", WireName: "website", Location: "query"}, {PublicName: "owner", WireName: "owner", Location: "query"}, {PublicName: "createdBefore", WireName: "createdBefore", Location: "query"}, {PublicName: "createdAfter", WireName: "createdAfter", Location: "query"}, {PublicName: "lastActivityBefore", WireName: "lastActivityBefore", Location: "query"}, {PublicName: "lastActivityAfter", WireName: "lastActivityAfter", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("conversations_delete",
			mcplib.WithDescription("Delete Conversation Ingest. Required: remoteId, sourceId. Returns the DeletedRecordResponse. Destructive."),
			mcplib.WithString("remoteId", mcplib.Required(), mcplib.Description("Remote id")),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v1/conversations", false, false, nil, []mcpParamBinding{{PublicName: "remoteId", WireName: "remoteId", Location: "query"}, {PublicName: "sourceId", WireName: "sourceId", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("conversations_get",
			mcplib.WithDescription("Get the detailed view for a single conversation. Required: conversationId. Returns array of Participant."),
			mcplib.WithString("conversationId", mcplib.Required(), mcplib.Description("Conversation id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/conversations/{conversationId}", true, false, nil, []mcpParamBinding{{PublicName: "conversationId", WireName: "conversationId", Location: "path"}}, []string{"conversationId"}),
	)
	s.AddTool(
		mcplib.NewTool("conversations_ingest",
			mcplib.WithDescription("Ingest Conversation. Required: createdAt, id, participants, sourceId, startedAt. Optional: displayName, endedAt, ownerEmails (plus 6 more). Returns the new RecordResponse."),
			mcplib.WithString("createdAt", mcplib.Required(), mcplib.Description("Created at")),
			mcplib.WithString("displayName", mcplib.Description("Display name")),
			mcplib.WithString("endedAt", mcplib.Description("Ended at")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("ownerEmails", mcplib.Description("Owner emails")),
			mcplib.WithString("participants", mcplib.Required(), mcplib.Description("Participants")),
			mcplib.WithBoolean("public", mcplib.Description("Public")),
			mcplib.WithString("recordingMimeType", mcplib.Description("Recording mime type")),
			mcplib.WithString("recordingUrl", mcplib.Description("Recording url")),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithString("startedAt", mcplib.Required(), mcplib.Description("Started at")),
			mcplib.WithString("transcript", mcplib.Description("Transcript")),
			mcplib.WithString("updatedAt", mcplib.Description("Updated at")),
			mcplib.WithNumber("version", mcplib.Description("Version")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/conversations", false, false, nil, []mcpParamBinding{{PublicName: "createdAt", WireName: "createdAt", Location: "body"}, {PublicName: "displayName", WireName: "displayName", Location: "body"}, {PublicName: "endedAt", WireName: "endedAt", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "ownerEmails", WireName: "ownerEmails", Location: "body"}, {PublicName: "participants", WireName: "participants", Location: "body"}, {PublicName: "public", WireName: "public", Location: "body"}, {PublicName: "recordingMimeType", WireName: "recordingMimeType", Location: "body"}, {PublicName: "recordingUrl", WireName: "recordingUrl", Location: "body"}, {PublicName: "sourceId", WireName: "sourceId", Location: "body"}, {PublicName: "startedAt", WireName: "startedAt", Location: "body"}, {PublicName: "transcript", WireName: "transcript", Location: "body"}, {PublicName: "updatedAt", WireName: "updatedAt", Location: "body"}, {PublicName: "version", WireName: "version", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("conversations_list",
			mcplib.WithDescription("List conversations accessible to the API key's organization. Optional: limit (default: 20), startedAfter, startedBefore (plus 6 more). Returns array of ConversationListItem."),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of results per page (1-50)")),
			mcplib.WithString("startedAfter", mcplib.Description("Lower bound for meeting start time (ISO 8601)")),
			mcplib.WithString("startedBefore", mcplib.Description("Upper bound for meeting start time (ISO 8601)")),
			mcplib.WithString("type", mcplib.Description("Filter by meeting type (e.g. EXTERNAL, INTERNAL)")),
			mcplib.WithString("title", mcplib.Description("Filter by meeting title (case-insensitive partial match)")),
			mcplib.WithString("attendees", mcplib.Description("Filter by an attendee. Accepts three shapes: a participant name fragment (e.g.")),
			mcplib.WithString("crmName", mcplib.Description("Filter by associated CRM deal or account name (case-insensitive exact-phrase match).")),
			mcplib.WithString("sourceId", mcplib.Description("Filter by conversation origin. Accepts a reserved native string (e.g.")),
			mcplib.WithString("cursor", mcplib.Description("Opaque pagination cursor from a previous response")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/conversations", true, false, nil, []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "startedAfter", WireName: "startedAfter", Location: "query"}, {PublicName: "startedBefore", WireName: "startedBefore", Location: "query"}, {PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "title", WireName: "title", Location: "query"}, {PublicName: "attendees", WireName: "attendees", Location: "query"}, {PublicName: "crmName", WireName: "crmName", Location: "query"}, {PublicName: "sourceId", WireName: "sourceId", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("deals_get",
			mcplib.WithDescription("Get the detailed view for a single deal. Required: dealId. Returns array of DealContact."),
			mcplib.WithString("dealId", mcplib.Required(), mcplib.Description("Deal id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/deals/{dealId}", true, false, nil, []mcpParamBinding{{PublicName: "dealId", WireName: "dealId", Location: "path"}}, []string{"dealId"}),
	)
	s.AddTool(
		mcplib.NewTool("deals_list",
			mcplib.WithDescription("List deals accessible to the API key's organization. Optional: name, stage, closed (plus 9 more). Returns array of DealListItem."),
			mcplib.WithString("name", mcplib.Description("Filter deals by name (case-insensitive partial match)")),
			mcplib.WithString("stage", mcplib.Description("Filter by pipeline stage name (case-insensitive partial match)")),
			mcplib.WithString("closed", mcplib.Description("Filter by closed status: true for won/lost deals, false for open")),
			mcplib.WithString("owner", mcplib.Description("Filter by deal owner name (case-insensitive partial match)")),
			mcplib.WithString("closeDateBefore", mcplib.Description("Upper bound for close date (ISO 8601, e.g. 2025-06-30T00:00:00Z)")),
			mcplib.WithString("closeDateAfter", mcplib.Description("Lower bound for close date (ISO 8601)")),
			mcplib.WithString("amountMin", mcplib.Description("Minimum deal amount")),
			mcplib.WithString("amountMax", mcplib.Description("Maximum deal amount")),
			mcplib.WithString("lastActivityBefore", mcplib.Description("Upper bound for last activity date (ISO 8601)")),
			mcplib.WithString("lastActivityAfter", mcplib.Description("Lower bound for last activity date (ISO 8601)")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of results per page (1-50)")),
			mcplib.WithString("cursor", mcplib.Description("Opaque pagination cursor from a previous response")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/deals", true, false, nil, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "query"}, {PublicName: "stage", WireName: "stage", Location: "query"}, {PublicName: "closed", WireName: "closed", Location: "query"}, {PublicName: "owner", WireName: "owner", Location: "query"}, {PublicName: "closeDateBefore", WireName: "closeDateBefore", Location: "query"}, {PublicName: "closeDateAfter", WireName: "closeDateAfter", Location: "query"}, {PublicName: "amountMin", WireName: "amountMin", Location: "query"}, {PublicName: "amountMax", WireName: "amountMax", Location: "query"}, {PublicName: "lastActivityBefore", WireName: "lastActivityBefore", Location: "query"}, {PublicName: "lastActivityAfter", WireName: "lastActivityAfter", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("documents_delete",
			mcplib.WithDescription("Delete Document Ingest. Required: remoteId, sourceId. Returns the DeletedRecordResponse. Destructive."),
			mcplib.WithString("remoteId", mcplib.Required(), mcplib.Description("Remote id")),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v1/documents", false, false, nil, []mcpParamBinding{{PublicName: "remoteId", WireName: "remoteId", Location: "query"}, {PublicName: "sourceId", WireName: "sourceId", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("documents_get",
			mcplib.WithDescription("Get Document. Required: documentId. Returns the DocumentDetail."),
			mcplib.WithString("documentId", mcplib.Required(), mcplib.Description("Document id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/documents/{documentId}", true, false, nil, []mcpParamBinding{{PublicName: "documentId", WireName: "documentId", Location: "path"}}, []string{"documentId"}),
	)
	s.AddTool(
		mcplib.NewTool("documents_ingest",
			mcplib.WithDescription("Ingest Document. Required: id, sourceId. Optional: author, content, contentType (default: text/plain) (plus 8 more). Returns the new RecordResponse."),
			mcplib.WithString("author", mcplib.Description("Author")),
			mcplib.WithString("content", mcplib.Description("Content")),
			mcplib.WithString("contentType", mcplib.Description("Content type")),
			mcplib.WithString("createdAt", mcplib.Description("Created at")),
			mcplib.WithString("displayName", mcplib.Description("Display name")),
			mcplib.WithString("filename", mcplib.Description("Filename")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("ownerEmails", mcplib.Description("Owner emails")),
			mcplib.WithBoolean("public", mcplib.Description("Public")),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithString("updatedAt", mcplib.Description("Updated at")),
			mcplib.WithString("url", mcplib.Description("Url")),
			mcplib.WithNumber("version", mcplib.Description("Version")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/documents", false, false, nil, []mcpParamBinding{{PublicName: "author", WireName: "author", Location: "body"}, {PublicName: "content", WireName: "content", Location: "body"}, {PublicName: "contentType", WireName: "contentType", Location: "body"}, {PublicName: "createdAt", WireName: "createdAt", Location: "body"}, {PublicName: "displayName", WireName: "displayName", Location: "body"}, {PublicName: "filename", WireName: "filename", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "ownerEmails", WireName: "ownerEmails", Location: "body"}, {PublicName: "public", WireName: "public", Location: "body"}, {PublicName: "sourceId", WireName: "sourceId", Location: "body"}, {PublicName: "updatedAt", WireName: "updatedAt", Location: "body"}, {PublicName: "url", WireName: "url", Location: "body"}, {PublicName: "version", WireName: "version", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("documents_list",
			mcplib.WithDescription("List Documents. Optional: sourceId, name, cursor (plus 1 more). Returns array of DocumentListItem."),
			mcplib.WithString("sourceId", mcplib.Description("Source id")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("cursor", mcplib.Description("Cursor")),
			mcplib.WithNumber("limit", mcplib.Description("Limit")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/documents", true, false, nil, []mcpParamBinding{{PublicName: "sourceId", WireName: "sourceId", Location: "query"}, {PublicName: "name", WireName: "name", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("documents_update",
			mcplib.WithDescription("Update Document Ingest. Required: remoteId, sourceId. Optional: content, contentType, displayName (plus 6 more). Returns the updated RecordResponse."),
			mcplib.WithString("remoteId", mcplib.Required(), mcplib.Description("Remote id")),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithString("content", mcplib.Description("Content")),
			mcplib.WithString("contentType", mcplib.Description("Content type")),
			mcplib.WithString("displayName", mcplib.Description("Display name")),
			mcplib.WithString("filename", mcplib.Description("Filename")),
			mcplib.WithString("ownerEmails", mcplib.Description("Owner emails")),
			mcplib.WithString("public", mcplib.Description("Public")),
			mcplib.WithString("updatedAt", mcplib.Description("Updated at")),
			mcplib.WithString("url", mcplib.Description("Url")),
			mcplib.WithNumber("version", mcplib.Description("Version")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PATCH", "/v1/documents", false, false, nil, []mcpParamBinding{{PublicName: "remoteId", WireName: "remoteId", Location: "query"}, {PublicName: "sourceId", WireName: "sourceId", Location: "query"}, {PublicName: "content", WireName: "content", Location: "body"}, {PublicName: "contentType", WireName: "contentType", Location: "body"}, {PublicName: "displayName", WireName: "displayName", Location: "body"}, {PublicName: "filename", WireName: "filename", Location: "body"}, {PublicName: "ownerEmails", WireName: "ownerEmails", Location: "body"}, {PublicName: "public", WireName: "public", Location: "body"}, {PublicName: "updatedAt", WireName: "updatedAt", Location: "body"}, {PublicName: "url", WireName: "url", Location: "body"}, {PublicName: "version", WireName: "version", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("health_check",
			mcplib.WithDescription("Check whether the Sybill API is reachable and responding. Takes no parameters. Returns a status object. Use this as a connectivity probe before running other commands, or to diagnose auth and reachability issues."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/health", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("messages_delete",
			mcplib.WithDescription("Delete Message Ingest. Required: remoteId, sourceId. Returns the DeletedRecordResponse. Destructive."),
			mcplib.WithString("remoteId", mcplib.Required(), mcplib.Description("Remote id")),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v1/messages", false, false, nil, []mcpParamBinding{{PublicName: "remoteId", WireName: "remoteId", Location: "query"}, {PublicName: "sourceId", WireName: "sourceId", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("messages_get",
			mcplib.WithDescription("Get Message. Required: messageId. Returns the MessageDetail."),
			mcplib.WithString("messageId", mcplib.Required(), mcplib.Description("Message id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/messages/{messageId}", true, false, nil, []mcpParamBinding{{PublicName: "messageId", WireName: "messageId", Location: "path"}}, []string{"messageId"}),
	)
	s.AddTool(
		mcplib.NewTool("messages_ingest",
			mcplib.WithDescription("Ingest Message. Required: body, createdAt, id, recipients, sender, sourceId. Optional: attachments, displayName, ownerEmails (plus 4 more). Returns the new RecordResponse."),
			mcplib.WithString("attachments", mcplib.Description("Attachments")),
			mcplib.WithString("body", mcplib.Required(), mcplib.Description("Body")),
			mcplib.WithString("createdAt", mcplib.Required(), mcplib.Description("Created at")),
			mcplib.WithString("displayName", mcplib.Description("Display name")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("ownerEmails", mcplib.Description("Owner emails")),
			mcplib.WithBoolean("public", mcplib.Description("Public")),
			mcplib.WithString("recipients", mcplib.Required(), mcplib.Description("Recipients")),
			mcplib.WithString("sender-email", mcplib.Description("Email")),
			mcplib.WithString("sender-id", mcplib.Description("Id")),
			mcplib.WithString("sender-name", mcplib.Description("Name")),
			mcplib.WithNumber("sender-version", mcplib.Description("Version")),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithString("threadId", mcplib.Description("Thread id")),
			mcplib.WithString("updatedAt", mcplib.Description("Updated at")),
			mcplib.WithNumber("version", mcplib.Description("Version")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/messages", false, false, nil, []mcpParamBinding{{PublicName: "attachments", WireName: "attachments", Location: "body"}, {PublicName: "body", WireName: "body", Location: "body"}, {PublicName: "createdAt", WireName: "createdAt", Location: "body"}, {PublicName: "displayName", WireName: "displayName", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "ownerEmails", WireName: "ownerEmails", Location: "body"}, {PublicName: "public", WireName: "public", Location: "body"}, {PublicName: "recipients", WireName: "recipients", Location: "body"}, {PublicName: "sender-email", WireName: "email", Location: "body", BodyPath: []string{"sender", "email"}}, {PublicName: "sender-id", WireName: "id", Location: "body", BodyPath: []string{"sender", "id"}}, {PublicName: "sender-name", WireName: "name", Location: "body", BodyPath: []string{"sender", "name"}}, {PublicName: "sender-version", WireName: "version", Location: "body", BodyPath: []string{"sender", "version"}}, {PublicName: "sourceId", WireName: "sourceId", Location: "body"}, {PublicName: "threadId", WireName: "threadId", Location: "body"}, {PublicName: "updatedAt", WireName: "updatedAt", Location: "body"}, {PublicName: "version", WireName: "version", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("messages_list",
			mcplib.WithDescription("List Messages. Optional: sourceId, remoteId, threadId (plus 5 more). Returns array of MessageListItem."),
			mcplib.WithString("sourceId", mcplib.Description("Source id")),
			mcplib.WithString("remoteId", mcplib.Description("Remote id")),
			mcplib.WithString("threadId", mcplib.Description("Thread id")),
			mcplib.WithString("participantEmail", mcplib.Description("Participant email")),
			mcplib.WithString("createdAfter", mcplib.Description("Created after")),
			mcplib.WithString("createdBefore", mcplib.Description("Created before")),
			mcplib.WithString("cursor", mcplib.Description("Cursor")),
			mcplib.WithNumber("limit", mcplib.Description("Limit")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/messages", true, false, nil, []mcpParamBinding{{PublicName: "sourceId", WireName: "sourceId", Location: "query"}, {PublicName: "remoteId", WireName: "remoteId", Location: "query"}, {PublicName: "threadId", WireName: "threadId", Location: "query"}, {PublicName: "participantEmail", WireName: "participantEmail", Location: "query"}, {PublicName: "createdAfter", WireName: "createdAfter", Location: "query"}, {PublicName: "createdBefore", WireName: "createdBefore", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("object-types_create",
			mcplib.WithDescription("Create Object Type. Required: displayName, fieldDefinitions, name, sourceId. Optional: version (default: 1). Returns array of FieldDefinitionResponse."),
			mcplib.WithString("displayName", mcplib.Required(), mcplib.Description("Display name")),
			mcplib.WithString("fieldDefinitions", mcplib.Required(), mcplib.Description("Field definitions")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithNumber("version", mcplib.Description("Version")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/object-types", false, false, nil, []mcpParamBinding{{PublicName: "displayName", WireName: "displayName", Location: "body"}, {PublicName: "fieldDefinitions", WireName: "fieldDefinitions", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "sourceId", WireName: "sourceId", Location: "body"}, {PublicName: "version", WireName: "version", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("object-types_delete",
			mcplib.WithDescription("Delete Object Type. Required: objectTypeId. Returns the DeletedObjectTypeResponse. Destructive."),
			mcplib.WithString("objectTypeId", mcplib.Required(), mcplib.Description("Object type id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v1/object-types/{objectTypeId}", false, false, nil, []mcpParamBinding{{PublicName: "objectTypeId", WireName: "objectTypeId", Location: "path"}}, []string{"objectTypeId"}),
	)
	s.AddTool(
		mcplib.NewTool("object-types_get",
			mcplib.WithDescription("Get Object Type. Required: objectTypeId. Returns array of FieldDefinitionResponse."),
			mcplib.WithString("objectTypeId", mcplib.Required(), mcplib.Description("Object type id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/object-types/{objectTypeId}", true, false, nil, []mcpParamBinding{{PublicName: "objectTypeId", WireName: "objectTypeId", Location: "path"}}, []string{"objectTypeId"}),
	)
	s.AddTool(
		mcplib.NewTool("object-types_list",
			mcplib.WithDescription("List Object Types. Optional: sourceId. Returns array of ObjectTypeResponse."),
			mcplib.WithString("sourceId", mcplib.Description("Source id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/object-types", true, false, nil, []mcpParamBinding{{PublicName: "sourceId", WireName: "sourceId", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("object-types_update",
			mcplib.WithDescription("Update Object Type. Required: objectTypeId. Optional: displayName, fieldDefinitions, version (default: 1). Returns array of FieldDefinitionResponse."),
			mcplib.WithString("objectTypeId", mcplib.Required(), mcplib.Description("Object type id")),
			mcplib.WithString("displayName", mcplib.Description("Display name")),
			mcplib.WithString("fieldDefinitions", mcplib.Description("Field definitions")),
			mcplib.WithNumber("version", mcplib.Description("Version")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PATCH", "/v1/object-types/{objectTypeId}", false, false, nil, []mcpParamBinding{{PublicName: "objectTypeId", WireName: "objectTypeId", Location: "path"}, {PublicName: "displayName", WireName: "displayName", Location: "body"}, {PublicName: "fieldDefinitions", WireName: "fieldDefinitions", Location: "body"}, {PublicName: "version", WireName: "version", Location: "body"}}, []string{"objectTypeId"}),
	)
	s.AddTool(
		mcplib.NewTool("rows_delete",
			mcplib.WithDescription("Delete Row Ingest. Required: remoteId, sourceId. Returns the DeletedRecordResponse. Destructive."),
			mcplib.WithString("remoteId", mcplib.Required(), mcplib.Description("Remote id")),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v1/rows", false, false, nil, []mcpParamBinding{{PublicName: "remoteId", WireName: "remoteId", Location: "query"}, {PublicName: "sourceId", WireName: "sourceId", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rows_get",
			mcplib.WithDescription("Fetch a single CRM row (a synced record such as a deal, contact, or account) by its id. Required: rowId (path). Returns the RowDetail with rowId, sourceId, objectTypeId, objectTypeName, remoteId, name, displayName, the row's fields map, and createdAt/updatedAt timestamps. Use this when you already have a rowId; use a list/search command to discover row ids first."),
			mcplib.WithString("rowId", mcplib.Required(), mcplib.Description("Row id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/rows/{rowId}", true, false, nil, []mcpParamBinding{{PublicName: "rowId", WireName: "rowId", Location: "path"}}, []string{"rowId"}),
	)
	s.AddTool(
		mcplib.NewTool("rows_ingest",
			mcplib.WithDescription("Ingest Row. Required: fields, id, name, objectTypeId, sourceId. Optional: createdAt, displayName, ownerEmails (plus 3 more). Returns the new RecordResponse."),
			mcplib.WithString("createdAt", mcplib.Description("Created at")),
			mcplib.WithString("displayName", mcplib.Description("Display name")),
			mcplib.WithString("fields", mcplib.Required(), mcplib.Description("Fields")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Id")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithString("objectTypeId", mcplib.Required(), mcplib.Description("Object type id")),
			mcplib.WithString("ownerEmails", mcplib.Description("Owner emails")),
			mcplib.WithBoolean("public", mcplib.Description("Public")),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithString("updatedAt", mcplib.Description("Updated at")),
			mcplib.WithNumber("version", mcplib.Description("Version")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/rows", false, false, nil, []mcpParamBinding{{PublicName: "createdAt", WireName: "createdAt", Location: "body"}, {PublicName: "displayName", WireName: "displayName", Location: "body"}, {PublicName: "fields", WireName: "fields", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "objectTypeId", WireName: "objectTypeId", Location: "body"}, {PublicName: "ownerEmails", WireName: "ownerEmails", Location: "body"}, {PublicName: "public", WireName: "public", Location: "body"}, {PublicName: "sourceId", WireName: "sourceId", Location: "body"}, {PublicName: "updatedAt", WireName: "updatedAt", Location: "body"}, {PublicName: "version", WireName: "version", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rows_list",
			mcplib.WithDescription("List Rows. Optional: sourceId, objectTypeId, remoteId (plus 5 more). Returns array of RowListItem."),
			mcplib.WithString("sourceId", mcplib.Description("Source id")),
			mcplib.WithString("objectTypeId", mcplib.Description("Object type id")),
			mcplib.WithString("remoteId", mcplib.Description("Remote id")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("createdAfter", mcplib.Description("Created after")),
			mcplib.WithString("createdBefore", mcplib.Description("Created before")),
			mcplib.WithString("cursor", mcplib.Description("Cursor")),
			mcplib.WithNumber("limit", mcplib.Description("Limit")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/rows", true, false, nil, []mcpParamBinding{{PublicName: "sourceId", WireName: "sourceId", Location: "query"}, {PublicName: "objectTypeId", WireName: "objectTypeId", Location: "query"}, {PublicName: "remoteId", WireName: "remoteId", Location: "query"}, {PublicName: "name", WireName: "name", Location: "query"}, {PublicName: "createdAfter", WireName: "createdAfter", Location: "query"}, {PublicName: "createdBefore", WireName: "createdBefore", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("rows_update",
			mcplib.WithDescription("Update Row Ingest Route. Required: remoteId, sourceId. Optional: displayName, fields, name (plus 4 more). Returns the updated RecordResponse."),
			mcplib.WithString("remoteId", mcplib.Required(), mcplib.Description("Remote id")),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithString("displayName", mcplib.Description("Display name")),
			mcplib.WithString("fields", mcplib.Description("Fields")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("ownerEmails", mcplib.Description("Owner emails")),
			mcplib.WithString("public", mcplib.Description("Public")),
			mcplib.WithString("updatedAt", mcplib.Description("Updated at")),
			mcplib.WithNumber("version", mcplib.Description("Version")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PATCH", "/v1/rows", false, false, nil, []mcpParamBinding{{PublicName: "remoteId", WireName: "remoteId", Location: "query"}, {PublicName: "sourceId", WireName: "sourceId", Location: "query"}, {PublicName: "displayName", WireName: "displayName", Location: "body"}, {PublicName: "fields", WireName: "fields", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "ownerEmails", WireName: "ownerEmails", Location: "body"}, {PublicName: "public", WireName: "public", Location: "body"}, {PublicName: "updatedAt", WireName: "updatedAt", Location: "body"}, {PublicName: "version", WireName: "version", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sources_create",
			mcplib.WithDescription("Create Source. Required: displayName, name. Optional: version (default: 1). Returns the new SourceResponse."),
			mcplib.WithString("displayName", mcplib.Required(), mcplib.Description("Display name")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithNumber("version", mcplib.Description("Version")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/sources", false, false, nil, []mcpParamBinding{{PublicName: "displayName", WireName: "displayName", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "version", WireName: "version", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sources_delete",
			mcplib.WithDescription("Delete Source. Required: sourceId. Returns the DeletedSourceResponse. Destructive."),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/v1/sources/{sourceId}", false, false, nil, []mcpParamBinding{{PublicName: "sourceId", WireName: "sourceId", Location: "path"}}, []string{"sourceId"}),
	)
	s.AddTool(
		mcplib.NewTool("sources_get",
			mcplib.WithDescription("Fetch a single data source (a connected integration that feeds rows into Sybill) by its id. Required: sourceId (path). Returns the SourceResponse with id, name, displayName, and createdAt/updatedAt timestamps. Use this to resolve a sourceId returned by rows_get into a human-readable source name."),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/sources/{sourceId}", true, false, nil, []mcpParamBinding{{PublicName: "sourceId", WireName: "sourceId", Location: "path"}}, []string{"sourceId"}),
	)
	s.AddTool(
		mcplib.NewTool("sources_list",
			mcplib.WithDescription("List Sources. Optional: name. Returns array of SourceResponse."),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/sources", true, false, nil, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sources_update",
			mcplib.WithDescription("Update Source. Required: sourceId, displayName. Optional: version (default: 1). Returns the updated SourceResponse."),
			mcplib.WithString("sourceId", mcplib.Required(), mcplib.Description("Source id")),
			mcplib.WithString("displayName", mcplib.Required(), mcplib.Description("Display name")),
			mcplib.WithNumber("version", mcplib.Description("Version")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PATCH", "/v1/sources/{sourceId}", false, false, nil, []mcpParamBinding{{PublicName: "sourceId", WireName: "sourceId", Location: "path"}, {PublicName: "displayName", WireName: "displayName", Location: "body"}, {PublicName: "version", WireName: "version", Location: "body"}}, []string{"sourceId"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
	BodyPath   []string
}

func setNestedBodyArg(body map[string]any, path []string, value any) {
	if len(path) == 0 {
		return
	}
	if len(path) == 1 {
		body[path[0]] = value
		return
	}
	current := body
	for _, key := range path[:len(path)-1] {
		next, ok := current[key].(map[string]any)
		if !ok {
			next = map[string]any{}
			current[key] = next
		}
		current = next
	}
	current[path[len(path)-1]] = value
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				if len(binding.BodyPath) > 0 {
					setNestedBodyArg(bodyArgs, binding.BodyPath, v)
				} else {
					bodyArgs[binding.WireName] = v
				}
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export SYBILL_API_KEY=<your-key>" +
					"\n      See API docs: https://www.sybill.ai" +
					"\n      Run 'sybill-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export SYBILL_API_KEY=<your-key>" +
					"\n      See API docs: https://www.sybill.ai" +
					"\n      Run 'sybill-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export SYBILL_API_KEY=<your-key>" +
					"\n      See API docs: https://www.sybill.ai" +
					"\n      Run 'sybill-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "sybill-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 60*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "sybill-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "sybill",
		"description": "Programmatic access to your Sybill workspace — conversations, insights, and AI-powered analysis.",
		"archetype":   "crm",
		"tool_count":  33,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion sybill-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "SYBILL_API_KEY",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
			"docs_url": "https://www.sybill.ai",
		},
		"resources": []map[string]any{
			{
				"name":        "accounts",
				"description": "Manage accounts",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "conversations",
				"description": "Manage conversations",
				"endpoints":   []string{"delete", "get", "ingest", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "deals",
				"description": "Manage deals",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "documents",
				"description": "Manage documents",
				"endpoints":   []string{"delete", "get", "ingest", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "health",
				"description": "Manage health",
				"endpoints":   []string{"check"},
				"syncable":    true,
			},
			{
				"name":        "messages",
				"description": "Manage messages",
				"endpoints":   []string{"delete", "get", "ingest", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "object-types",
				"description": "Manage object types",
				"endpoints":   []string{"create", "delete", "get", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "rows",
				"description": "Manage rows",
				"endpoints":   []string{"delete", "get", "ingest", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "sources",
				"description": "Manage sources",
				"endpoints":   []string{"create", "delete", "get", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass cursor parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Deals gone dark", "command": "deals dark", "description": "List open deals with no call activity in the last N days, so nothing stalls silently.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Weekly call digest", "command": "digest", "description": "Pull every call in a window, grouped by deal", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "CRM autofill pending diff", "command": "crm-autofill", "description": "Show the AI-suggested CRM field updates Sybill generated, as a reviewable field-by-field diff.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Account roll-up", "command": "account rollup", "description": "One offline view per account: call count, open deals by stage, contacts, and last activity.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Rep activity aggregation", "command": "activity", "description": "Per-rep breakdown: calls made, deals touched, and deals gone dark over a window.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Keyword / objection patterns", "command": "patterns", "description": "Count and locate transcript mentions of a term, grouped by deal and stage.", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Deals gone dark", "insight": ""},
			{"topic": "Weekly call digest", "insight": ""},
			{"topic": "CRM autofill pending diff", "insight": ""},
			{"topic": "Account roll-up", "insight": ""},
			{"topic": "Rep activity aggregation", "insight": ""},
			{"topic": "Keyword / objection patterns", "insight": ""},
			{"topic": "Contact lookup", "insight": "Use search for finding contacts by name/email. List endpoints return unsorted results and require pagination for large datasets."},
			{"topic": "Activity tracking", "insight": "When checking deal activity, sync first and query locally. CRM APIs often throttle activity-log endpoints heavily."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
