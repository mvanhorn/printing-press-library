// Copyright 2026 riccardovandra and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/sybill/internal/cliutil"
	"github.com/spf13/cobra"
)

func newConversationsIngestCmd(flags *rootFlags) *cobra.Command {
	var bodyCreatedAt string
	var bodyDisplayName string
	var bodyEndedAt string
	var bodyId string
	var bodyOwnerEmails string
	var bodyParticipants string
	var bodyPublic bool
	var bodyRecordingMimeType string
	var bodyRecordingUrl string
	var bodySourceId string
	var bodyStartedAt string
	var bodyTranscript string
	var bodyUpdatedAt string
	var bodyVersion int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "ingest",
		Aliases:     []string{"create"},
		Short:       "Ingest Conversation",
		Example:     "  sybill-pp-cli conversations ingest --created-at example-value",
		Annotations: map[string]string{"pp:endpoint": "conversations.ingest", "pp:method": "POST", "pp:path": "/v1/conversations"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("created-at") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "created-at")
				}
				if !cmd.Flags().Changed("id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "id")
				}
				if !cmd.Flags().Changed("participants") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "participants")
				}
				if !cmd.Flags().Changed("source-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-id")
				}
				if !cmd.Flags().Changed("started-at") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "started-at")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/conversations"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCreatedAt != "" {
					body["createdAt"] = bodyCreatedAt
				}
				if bodyDisplayName != "" {
					body["displayName"] = bodyDisplayName
				}
				if bodyEndedAt != "" {
					body["endedAt"] = bodyEndedAt
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyOwnerEmails != "" {
					body["ownerEmails"] = cliutil.SplitCSV(bodyOwnerEmails)
				}
				if bodyParticipants != "" {
					var parsedParticipants any
					if err := json.Unmarshal([]byte(bodyParticipants), &parsedParticipants); err != nil {
						return fmt.Errorf("parsing --participants JSON: %w", err)
					}
					body["participants"] = parsedParticipants
				}
				if cmd.Flags().Changed("public") {
					body["public"] = bodyPublic
				}
				if bodyRecordingMimeType != "" {
					body["recordingMimeType"] = bodyRecordingMimeType
				}
				if bodyRecordingUrl != "" {
					body["recordingUrl"] = bodyRecordingUrl
				}
				if bodySourceId != "" {
					body["sourceId"] = bodySourceId
				}
				if bodyStartedAt != "" {
					body["startedAt"] = bodyStartedAt
				}
				if bodyTranscript != "" {
					var parsedTranscript any
					if err := json.Unmarshal([]byte(bodyTranscript), &parsedTranscript); err != nil {
						return fmt.Errorf("parsing --transcript JSON: %w", err)
					}
					body["transcript"] = parsedTranscript
				}
				if bodyUpdatedAt != "" {
					body["updatedAt"] = bodyUpdatedAt
				}
				if bodyVersion != 0 {
					body["version"] = bodyVersion
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "conversations", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "conversations", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "conversations", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "conversations", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "conversations", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "conversations",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "conversations", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "conversations", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyCreatedAt, "created-at", "", "Created at")
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "Display name")
	cmd.Flags().StringVar(&bodyEndedAt, "ended-at", "", "Ended at")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().StringVar(&bodyOwnerEmails, "owner-emails", "", "Owner emails")
	cmd.Flags().StringVar(&bodyParticipants, "participants", "", "Participants")
	cmd.Flags().BoolVar(&bodyPublic, "public", false, "Public")
	cmd.Flags().StringVar(&bodyRecordingMimeType, "recording-mime-type", "", "Recording mime type")
	cmd.Flags().StringVar(&bodyRecordingUrl, "recording-url", "", "Recording url")
	cmd.Flags().StringVar(&bodySourceId, "source-id", "", "Source id")
	cmd.Flags().StringVar(&bodyStartedAt, "started-at", "", "Started at")
	cmd.Flags().StringVar(&bodyTranscript, "transcript", "", "Transcript")
	cmd.Flags().StringVar(&bodyUpdatedAt, "updated-at", "", "Updated at")
	cmd.Flags().IntVar(&bodyVersion, "version", 1, "Version")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
