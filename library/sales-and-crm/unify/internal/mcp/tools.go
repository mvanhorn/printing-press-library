// Copyright 2026 nate-larkin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/unify/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/unify/internal/client"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/unify/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/unify/internal/config"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/unify/internal/mcp/cobratree"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("data_create-object",
			mcplib.WithDescription("Create object. Required: api_name, description, display_name, provider. Returns the new DataCreateObjectResponse."),
			mcplib.WithString("api_name", mcplib.Required(), mcplib.Description("This is the unique identifier for the object at the API level. It is unique within Unify and cannot be changed.")),
			mcplib.WithString("description", mcplib.Required(), mcplib.Description("This is a description of the object. It is not required but will be shown in the UI to help understand the purpose...")),
			mcplib.WithString("display_name", mcplib.Required(), mcplib.Description("This is the user-facing object name shown within Unify. It is not required to be unique within Unify and can be...")),
			mcplib.WithString("provider", mcplib.Required(), mcplib.Description("The provider of an object represents who created and manages the object in Unify. This is used to differentiate...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/data/v1/objects", []mcpParamBinding{{PublicName: "api_name", WireName: "api_name", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "display_name", WireName: "display_name", Location: "body"}, {PublicName: "provider", WireName: "provider", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("data_create-object-attribute",
			mcplib.WithDescription("Create object attribute. Required: object_name. Returns the new DataCreateObjectAttributeResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/data/v1/objects/{object_name}/attributes", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}}, []string{"object_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_create-object-attribute-option",
			mcplib.WithDescription("Create object attribute option. Required: object_name, attribute_name, api_name, display_name. Returns the new DataCreateObjectAttributeOptionResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("attribute_name", mcplib.Required(), mcplib.Description("Attribute name")),
			mcplib.WithString("api_name", mcplib.Required(), mcplib.Description("This is the unique identifier for the attribute option at the API level. It is unique within the attribute and...")),
			mcplib.WithString("display_name", mcplib.Required(), mcplib.Description("This is the user-facing attribute option name that will be shown within Unify. It is unique within the object and...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/data/v1/objects/{object_name}/attributes/{attribute_name}/options", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "attribute_name", WireName: "attribute_name", Location: "path"}, {PublicName: "api_name", WireName: "api_name", Location: "body"}, {PublicName: "display_name", WireName: "display_name", Location: "body"}}, []string{"object_name", "attribute_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_create-object-record",
			mcplib.WithDescription("Create object record. Required: object_name, data. Optional: validation_mode. Returns the new DataCreateObjectRecordResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("validation_mode", mcplib.Description("Validation mode")),
			mcplib.WithString("data", mcplib.Required(), mcplib.Description("The attribute values for the new record. All required attributes on the object must be included.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/data/v1/objects/{object_name}/records", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "validation_mode", WireName: "validation_mode", Location: "query"}, {PublicName: "data", WireName: "data", Location: "body"}}, []string{"object_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_delete-object",
			mcplib.WithDescription("Delete object. Required: object_name. Returns the DataDeleteObjectResponse. Destructive."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/data/v1/objects/{object_name}", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}}, []string{"object_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_delete-object-attribute",
			mcplib.WithDescription("Delete object attribute. Required: object_name, attribute_name. Returns the DataDeleteObjectAttributeResponse. Destructive."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("attribute_name", mcplib.Required(), mcplib.Description("Attribute name")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/data/v1/objects/{object_name}/attributes/{attribute_name}", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "attribute_name", WireName: "attribute_name", Location: "path"}}, []string{"object_name", "attribute_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_delete-object-attribute-option",
			mcplib.WithDescription("Delete object attribute option. Required: object_name, attribute_name, option_name. Returns the DataDeleteObjectAttributeOptionResponse. Destructive."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("attribute_name", mcplib.Required(), mcplib.Description("Attribute name")),
			mcplib.WithString("option_name", mcplib.Required(), mcplib.Description("Option name")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/data/v1/objects/{object_name}/attributes/{attribute_name}/options/{option_name}", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "attribute_name", WireName: "attribute_name", Location: "path"}, {PublicName: "option_name", WireName: "option_name", Location: "path"}}, []string{"object_name", "attribute_name", "option_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_delete-object-record",
			mcplib.WithDescription("Delete object record. Required: object_name, record_id. Returns the DataDeleteObjectRecordResponse. Destructive."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("record_id", mcplib.Required(), mcplib.Description("Record id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/data/v1/objects/{object_name}/records/{record_id}", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "record_id", WireName: "record_id", Location: "path"}}, []string{"object_name", "record_id"}),
	)
	s.AddTool(
		mcplib.NewTool("data_find-unique-object-record",
			mcplib.WithDescription("Find unique object record. Required: object_name, match. Returns the new DataFindUniqueObjectRecordResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("match", mcplib.Required(), mcplib.Description("The attribute values to match against to find an existing record. At least one unique attribute must be included to...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/data/v1/objects/{object_name}/records/find-unique", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "match", WireName: "match", Location: "body"}}, []string{"object_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_get-object",
			mcplib.WithDescription("Get object. Required: object_name. Returns the DataGetObjectResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/data/v1/objects/{object_name}", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}}, []string{"object_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_get-object-attribute",
			mcplib.WithDescription("Get object attribute. Required: object_name, attribute_name. Returns the DataGetObjectAttributeResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("attribute_name", mcplib.Required(), mcplib.Description("Attribute name")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/data/v1/objects/{object_name}/attributes/{attribute_name}", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "attribute_name", WireName: "attribute_name", Location: "path"}}, []string{"object_name", "attribute_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_get-object-attribute-option",
			mcplib.WithDescription("Get object attribute option. Required: object_name, attribute_name, option_name. Returns the DataGetObjectAttributeOptionResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("attribute_name", mcplib.Required(), mcplib.Description("Attribute name")),
			mcplib.WithString("option_name", mcplib.Required(), mcplib.Description("Option name")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/data/v1/objects/{object_name}/attributes/{attribute_name}/options/{option_name}", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "attribute_name", WireName: "attribute_name", Location: "path"}, {PublicName: "option_name", WireName: "option_name", Location: "path"}}, []string{"object_name", "attribute_name", "option_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_get-object-record",
			mcplib.WithDescription("Get object record. Required: object_name, record_id. Returns the DataGetObjectRecordResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("record_id", mcplib.Required(), mcplib.Description("Record id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/data/v1/objects/{object_name}/records/{record_id}", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "record_id", WireName: "record_id", Location: "path"}}, []string{"object_name", "record_id"}),
	)
	s.AddTool(
		mcplib.NewTool("data_list-object-attribute-options",
			mcplib.WithDescription("List object attribute options. Required: object_name, attribute_name. Returns array of UobjectsUattributeOption."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("attribute_name", mcplib.Required(), mcplib.Description("Attribute name")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/data/v1/objects/{object_name}/attributes/{attribute_name}/options", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "attribute_name", WireName: "attribute_name", Location: "path"}}, []string{"object_name", "attribute_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_list-object-attributes",
			mcplib.WithDescription("List object attributes. Required: object_name. Returns array of UobjectsUattribute."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/data/v1/objects/{object_name}/attributes", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}}, []string{"object_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_list-objects",
			mcplib.WithDescription("List objects. Returns array of UobjectsUobject."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/data/v1/objects", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("data_update-object",
			mcplib.WithDescription("Update object. Required: object_name, description, display_name. Returns the updated DataUpdateObjectResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("description", mcplib.Required(), mcplib.Description("This is a description of the object. It is not required but will be shown in the UI to help understand the purpose...")),
			mcplib.WithString("display_name", mcplib.Required(), mcplib.Description("This is the user-facing object name shown within Unify. It is not required to be unique within Unify and can be...")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PATCH", "/data/v1/objects/{object_name}", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "display_name", WireName: "display_name", Location: "body"}}, []string{"object_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_update-object-attribute",
			mcplib.WithDescription("Update object attribute. Required: object_name, attribute_name. Returns the updated DataUpdateObjectAttributeResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("attribute_name", mcplib.Required(), mcplib.Description("Attribute name")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PATCH", "/data/v1/objects/{object_name}/attributes/{attribute_name}", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "attribute_name", WireName: "attribute_name", Location: "path"}}, []string{"object_name", "attribute_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_update-object-attribute-option",
			mcplib.WithDescription("Update object attribute option. Required: object_name, attribute_name, option_name, display_name. Returns the updated DataUpdateObjectAttributeOptionResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("attribute_name", mcplib.Required(), mcplib.Description("Attribute name")),
			mcplib.WithString("option_name", mcplib.Required(), mcplib.Description("Option name")),
			mcplib.WithString("display_name", mcplib.Required(), mcplib.Description("This is the user-facing attribute option name that will be shown within Unify. It is unique within the object and...")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PATCH", "/data/v1/objects/{object_name}/attributes/{attribute_name}/options/{option_name}", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "attribute_name", WireName: "attribute_name", Location: "path"}, {PublicName: "option_name", WireName: "option_name", Location: "path"}, {PublicName: "display_name", WireName: "display_name", Location: "body"}}, []string{"object_name", "attribute_name", "option_name"}),
	)
	s.AddTool(
		mcplib.NewTool("data_update-object-record",
			mcplib.WithDescription("Update object record. Required: object_name, record_id, data. Optional: validation_mode. Returns the updated DataUpdateObjectRecordResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("record_id", mcplib.Required(), mcplib.Description("Record id")),
			mcplib.WithString("validation_mode", mcplib.Description("Validation mode")),
			mcplib.WithString("data", mcplib.Required(), mcplib.Description("The attribute values to update in the record.")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PATCH", "/data/v1/objects/{object_name}/records/{record_id}", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "record_id", WireName: "record_id", Location: "path"}, {PublicName: "validation_mode", WireName: "validation_mode", Location: "query"}, {PublicName: "data", WireName: "data", Location: "body"}}, []string{"object_name", "record_id"}),
	)
	s.AddTool(
		mcplib.NewTool("data_upsert-object-record",
			mcplib.WithDescription("Upsert object record. Required: object_name, match. Optional: validation_mode, create, create_or_update (plus 3 more). Returns the new DataUpsertObjectRecordResponse."),
			mcplib.WithString("object_name", mcplib.Required(), mcplib.Description("Object name")),
			mcplib.WithString("validation_mode", mcplib.Description("Validation mode")),
			mcplib.WithString("create", mcplib.Description("The attribute values to use when creating a new record if no match is found.")),
			mcplib.WithString("create_or_update", mcplib.Description("The attribute values to apply during both creation and update operations.")),
			mcplib.WithString("create_or_update_if_empty", mcplib.Description("The attribute values to apply during both creation and update-if-empty operations.")),
			mcplib.WithString("match", mcplib.Required(), mcplib.Description("The attribute values to match against to find an existing record. At least one unique attribute must be included to...")),
			mcplib.WithString("update", mcplib.Description("The attribute values to use when updating an existing record if a match is found.")),
			mcplib.WithString("update_if_empty", mcplib.Description("The attribute values to update when a matching record is found and the existing attribute value on the record is `null`.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/data/v1/objects/{object_name}/records/upsert", []mcpParamBinding{{PublicName: "object_name", WireName: "object_name", Location: "path"}, {PublicName: "validation_mode", WireName: "validation_mode", Location: "query"}, {PublicName: "create", WireName: "create", Location: "body"}, {PublicName: "create_or_update", WireName: "create_or_update", Location: "body"}, {PublicName: "create_or_update_if_empty", WireName: "create_or_update_if_empty", Location: "body"}, {PublicName: "match", WireName: "match", Location: "body"}, {PublicName: "update", WireName: "update", Location: "body"}, {PublicName: "update_if_empty", WireName: "update_if_empty", Location: "body"}}, []string{"object_name"}),
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export UNIFY_API_KEY=<your-key>" +
					"\n      See API docs: https://www.unifygtm.com/support" +
					"\n      Run 'unify-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set it with: export UNIFY_API_KEY=<your-key>" +
					"\n      See API docs: https://www.unifygtm.com/support" +
					"\n      Run 'unify-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export UNIFY_API_KEY=<your-key>" +
					"\n      See API docs: https://www.unifygtm.com/support" +
					"\n      Run 'unify-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "unify-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "unify-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "unify",
		"description": "The read layer Unify's API doesn't ship. Local search, SQL, and coverage reports over a CRM that has no list-records...",
		"archetype":   "generic",
		"tool_count":  21,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion unify-pp-cli binary.",
		"auth": map[string]any{
			"type": "api_key",
			"env_vars": []map[string]any{
				{
					"name":        "UNIFY_API_KEY",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
			"docs_url": "https://www.unifygtm.com/support",
		},
		"resources": []map[string]any{
			{
				"name":        "data",
				"description": "Manage data",
				"endpoints":   []string{"create-object", "create-object-attribute", "create-object-attribute-option", "create-object-record", "delete-object", "delete-object-attribute", "delete-object-attribute-option", "delete-object-record", "find-unique-object-record", "get-object", "get-object-attribute", "get-object-attribute-option", "get-object-record", "list-object-attribute-options", "list-object-attributes", "list-objects", "update-object", "update-object-attribute", "update-object-attribute-option", "update-object-record", "upsert-object-record"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Local FTS5 search across all record types", "command": "search", "description": "Full-text search across every synced object's records in one command. Returns typed hits across company, person,...", "rationale": "The Unify Data API has no LIST or SEARCH endpoint for records. Search only exists if you build it from a local mirror.", "via": "mcp-command-mirror"},
			{"name": "Read-only SQL over the local mirror", "command": "sql", "description": "Run read-only SQL queries with joins across per-object record tables. Cross Unify standard objects with...", "rationale": "The API has no query/filter surface for records. SQL is the missing read layer.", "via": "mcp-command-mirror"},
			{"name": "Coverage report between two object tables", "command": "coverage", "description": "Set-difference report between two record tables on a shared key with optional bucketing by attribute and...", "rationale": "Coverage is a join + set-diff problem; the API exposes neither operation. Only computable locally.", "via": "mcp-command-mirror"},
			{"name": "Score-divergence audit", "command": "audit-scores", "description": "Flag records where two numeric attributes diverge beyond a threshold. Powers scoring sanity checks across...", "rationale": "Pure local SQL; the API doesn't expose comparisons across attributes.", "via": "mcp-command-mirror"},
			{"name": "Schema snapshot + diff", "command": "schema diff", "description": "Snapshot objects, attributes, and attribute options; diff two snapshots to find adds, removes, and type changes.", "rationale": "Unify has no server-side schema-change audit log.", "via": "mcp-command-mirror"},
			{"name": "Batch domain vetting", "command": "vet", "description": "Read a CSV column of match values, run find-unique in parallel for each, and enrich each row with...", "rationale": "find-unique is per-key only; batch vetting is exactly the workflow the no-list constraint makes impossible without...", "via": "mcp-command-mirror"},
			{"name": "CSV upsert plan preview", "command": "import-csv", "description": "Predict what an upsert from CSV will do (create / update / no-op counts per row) by combining the local mirror with...", "rationale": "Upsert has 6 merge modes; a dry-run plan from the API alone can't tell you which existing records will change.", "via": "mcp-command-mirror"},
			{"name": "Local reference trace", "command": "trace", "description": "Walk reference attributes (opportunities, people, owner) from a starting record without N+1 API calls.", "rationale": "Reference cardinality is in the schema; the API forces per-record GETs to follow refs. Local mirror traversal is the...", "via": "mcp-command-mirror"},
			{"name": "Watchlist-driven sync", "command": "watch", "description": "Persist match-keys for records you care about; sync refreshes them via parallel find-unique on every run.", "rationale": "No list endpoint exists, so sync needs an explicit ID list. The watchlist is the cold-start solution.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Local FTS5 search across all record types", "insight": "The Unify Data API has no LIST or SEARCH endpoint for records. Search only exists if you build it from a local mirror."},
			{"topic": "Read-only SQL over the local mirror", "insight": "The API has no query/filter surface for records. SQL is the missing read layer."},
			{"topic": "Coverage report between two object tables", "insight": "Coverage is a join + set-diff problem; the API exposes neither operation. Only computable locally."},
			{"topic": "Score-divergence audit", "insight": "Pure local SQL; the API doesn't expose comparisons across attributes."},
			{"topic": "Schema snapshot + diff", "insight": "Unify has no server-side schema-change audit log."},
			{"topic": "Batch domain vetting", "insight": "find-unique is per-key only; batch vetting is exactly the workflow the no-list constraint makes impossible without orchestration."},
			{"topic": "CSV upsert plan preview", "insight": "Upsert has 6 merge modes; a dry-run plan from the API alone can't tell you which existing records will change."},
			{"topic": "Local reference trace", "insight": "Reference cardinality is in the schema; the API forces per-record GETs to follow refs. Local mirror traversal is the optimization."},
			{"topic": "Watchlist-driven sync", "insight": "No list endpoint exists, so sync needs an explicit ID list. The watchlist is the cold-start solution."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
