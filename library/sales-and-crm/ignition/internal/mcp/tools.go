// Copyright 2026 corben-tech and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"math"
	"os"
	"path/filepath"
	"strconv"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/ignition/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/ignition/internal/client"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/ignition/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/ignition/internal/config"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/ignition/internal/learn"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/ignition/internal/mcp/bound"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/ignition/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/sales-and-crm/ignition/internal/store"
)

const (
	// MCP hosts can fan out tool calls faster than a human CLI session.
	// Keep them on the same polite-client limiter path instead of disabling
	// pacing with rate=0; users can still tune human CLI calls with --rate-limit.
	defaultMCPRateLimit = 2
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("acknowledgements_get",
			mcplib.WithDescription("Fetch acknowledgements. Required: operationName (default: acknowledgement). Optional: query, variables, extensions. Returns array of acknowledgement."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("adds_on_plan_trial_banner",
			mcplib.WithDescription("Fetch adds on plan trial banner. Required: operationName (default: addOnPlanTrialBanner). Optional: query, variables (default: map[]), extensions. Returns array of add_on_plan_trial_banner."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("apps_with_capability",
			mcplib.WithDescription("Fetch apps with capability. Required: operationName (default: appsWithCapability). Optional: query, variables, extensions. Returns array of apps_with_capability."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("auth-api_create_graphql",
			mcplib.WithDescription("POST /auth-api/graphql. Required: data, extensions, operationName, query, variables. Returns array of Graphql."),
			mcplib.WithObject("data", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("extensions-client-library-name", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("extensions-client-library-version", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("")),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("")),
			mcplib.WithObject("variables", mcplib.Required(), mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/auth-api/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "data", WireName: "data", Location: "query"}, {PublicName: "extensions-client-library-name", WireName: "name", Location: "body", BodyPath: []string{"extensions", "clientLibrary", "name"}}, {PublicName: "extensions-client-library-version", WireName: "version", Location: "body", BodyPath: []string{"extensions", "clientLibrary", "version"}}, {PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("billings_get",
			mcplib.WithDescription("Fetch billings. Required: operationName (default: billingDetails). Optional: query, variables (default: map[]), extensions. Returns array of billing_details."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("brandings_theme",
			mcplib.WithDescription("Fetch brandings theme. Required: operationName (default: brandingTheme). Optional: query, variables (default: map[]), extensions. Returns the new branding_theme."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_forms",
			mcplib.WithDescription("Fetch clients forms. Required: operationName (default: clientForms). Optional: query, variables, extensions. Returns array of client_forms."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_get",
			mcplib.WithDescription("Fetch clients. Required: operationName (default: client). Optional: query, variables, extensions. Returns array of client."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_get_2",
			mcplib.WithDescription("Fetch clients. Required: operationName (default: clientViewPage). Optional: query, variables, extensions. Returns array of client_view_page."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_proposals",
			mcplib.WithDescription("Fetch clients proposals. Required: operationName (default: clientProposals). Optional: query, variables, extensions. Returns array of client_proposals."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_summary_client",
			mcplib.WithDescription("Fetch clients summary client. Required: operationName (default: clientSummaryClient). Optional: query, variables, extensions. Returns array of client_summary_client."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_summary_rejected_payments",
			mcplib.WithDescription("Fetch clients summary rejected payments. Required: operationName (default: clientSummaryRejectedPayments). Optional: query, variables, extensions. Returns array of client_summary_rejected_payments."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_sync_status",
			mcplib.WithDescription("Fetch clients sync status. Required: operationName (default: getClientSyncStatus). Optional: query, variables, extensions. Returns array of get_client_sync_status."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_tags",
			mcplib.WithDescription("Fetch clients tags. Required: operationName (default: clientTags). Optional: query, variables, extensions. Returns array of client_tags."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("codes_version",
			mcplib.WithDescription("Fetch codes version. Required: operationName (default: codeVersion). Optional: query, variables (default: map[]), extensions. Returns array of code_version."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("compliances_availability",
			mcplib.WithDescription("Fetch compliances availability. Required: operationName (default: complianceAvailability). Optional: query, variables (default: map[]), extensions. Returns array of compliance_availability."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("currents_practice",
			mcplib.WithDescription("Fetch currents practice. Required: operationName (default: currentPractice). Optional: query, variables (default: map[]), extensions. Returns array of current_practice."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("currents_user",
			mcplib.WithDescription("Fetch currents user. Required: operationName (default: currentUser). Optional: query, variables (default: map[]), extensions. Returns array of current_user."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("features_gate",
			mcplib.WithDescription("Fetch features gate. Required: operationName (default: getFeatureGate). Optional: query, variables (default: map[featureName:deals]), extensions. Returns array of get_feature_gate."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("forms_templates",
			mcplib.WithDescription("Fetch forms templates. Required: operationName (default: formTemplates). Optional: query, variables, extensions. Returns the new form_templates."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("payments_settings",
			mcplib.WithDescription("Fetch payments settings. Required: operationName (default: paymentSettings). Optional: query, variables (default: map[]), extensions. Returns array of payment_settings."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("preferreds_proposal_editor",
			mcplib.WithDescription("Fetch preferreds proposal editor. Required: operationName (default: preferredProposalEditor). Optional: query, variables (default: map[]), extensions. Returns array of preferred_proposal_editor."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("proposals_get",
			mcplib.WithDescription("Fetch proposals. Required: operationName (default: proposalView). Optional: query, variables, extensions. Returns array of proposal_view."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("search_index_billing_items",
			mcplib.WithDescription("List all billing items via the search index. Records under results.nodes. Required: operationName (default: pagedBillingItems). Optional: query, variables, extensions (default: map[]). Returns array of billing_item_result."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("search_index_invoices",
			mcplib.WithDescription("List all invoices via the search index. Records under results.nodes. Required: operationName (default: pagedInvoices). Optional: query, variables, extensions (default: map[]). Returns array of invoice_result."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("search_index_proposals",
			mcplib.WithDescription("List all proposals via the search index (status, client, name). Filter by client-side node.client.id; there is no server-side client-id text filter. Required: operationName (default: pagedProposals). Optional: query, variables, extensions (default: map[]). Returns array of proposal_result."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON. textFilters entries are {attribute, condition, values:[...")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("site_navigation",
			mcplib.WithDescription("Fetch site navigation. Required: operationName. Optional: query, variables (default: map[]), extensions. Returns array of workspace_management_nav_availability."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("solos_plan_banner",
			mcplib.WithDescription("Fetch solos plan banner. Required: operationName (default: soloPlanBanner). Optional: query, variables (default: map[planId:solo2024]), extensions. Returns array of solo_plan_banner."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("unseens_count",
			mcplib.WithDescription("Fetch unseens count. Required: operationName (default: unseenCount). Optional: query, variables (default: map[]), extensions. Returns array of unseen_count."),
			mcplib.WithString("operationName", mcplib.Required(), mcplib.Description("GraphQL operation name")),
			mcplib.WithString("query", mcplib.Description("GraphQL query document")),
			mcplib.WithObject("variables", mcplib.Description("GraphQL variables as JSON")),
			mcplib.WithObject("extensions", mcplib.Description("GraphQL extensions as JSON")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "operationName", WireName: "operationName", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "variables", WireName: "variables", Location: "body"}, {PublicName: "extensions", WireName: "extensions", Location: "body"}}, []string{}),
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Synced records live in resources(resource_type, id, data); filter by resource_type and use json_extract on data, e.g. SELECT json_extract(data,'$.name') FROM resources WHERE resource_type='acknowledgements'.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
	BodyPath   []string
}

type mcpPageConfig struct {
	CursorParam    string
	NextCursorPath string
}

func formatMCPParamValue(v any) string {
	switch tv := v.(type) {
	case string:
		return tv
	case bool:
		return strconv.FormatBool(tv)
	case float64:
		if math.IsNaN(tv) || math.IsInf(tv, 0) {
			return strconv.FormatFloat(tv, 'f', -1, 64)
		}
		if math.Trunc(tv) == tv && math.Abs(tv) < 1e15 {
			return strconv.FormatInt(int64(tv), 10)
		}
		return strconv.FormatFloat(tv, 'f', -1, 64)
	case float32:
		f := float64(tv)
		if math.IsNaN(f) || math.IsInf(f, 0) {
			return strconv.FormatFloat(f, 'f', -1, 32)
		}
		if math.Trunc(f) == f && math.Abs(f) < 1e15 {
			return strconv.FormatInt(int64(f), 10)
		}
		return strconv.FormatFloat(f, 'f', -1, 32)
	default:
		// Composite values (a native []any / map[string]any from an array or
		// object param) reach this path when bound to a query or path slot;
		// JSON-encode them so the wire value is valid JSON rather than Go's
		// "[a b c]" / "map[...]" rendering. Body params never come through
		// here — they are stored natively in bodyArgs and marshalled there.
		if b, err := json.Marshal(v); err == nil {
			return string(b)
		}
		return fmt.Sprintf("%v", v)
	}
}
func setNestedBodyArg(body map[string]any, path []string, value any) {
	if len(path) == 0 {
		return
	}
	if len(path) == 1 {
		body[path[0]] = value
		return
	}
	current := body
	for _, key := range path[:len(path)-1] {
		next, ok := current[key].(map[string]any)
		if !ok {
			next = map[string]any{}
			current[key] = next
		}
		current = next
	}
	current[path[len(path)-1]] = value
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, pageConfig mcpPageConfig, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		mcpCursor := ""
		if pageConfig.CursorParam != "" {
			knownArgs["cursor"] = true
			if v, ok := args["cursor"]; ok {
				s, ok := v.(string)
				if !ok {
					return mcplib.NewToolResultError("cursor must be an opaque string returned by a previous MCP response"), nil
				}
				mcpCursor = s
				upstreamCursor, err := bound.UpstreamCursor(s)
				if err != nil {
					return mcplib.NewToolResultError(err.Error()), nil
				}
				if upstreamCursor != "" {
					params[pageConfig.CursorParam] = upstreamCursor
				}
			}
		}
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, formatMCPParamValue(v), 1)
			case "body":
				if len(binding.BodyPath) > 0 {
					setNestedBodyArg(bodyArgs, binding.BodyPath, v)
				} else {
					bodyArgs[binding.WireName] = v
				}
			default:
				params[binding.WireName] = formatMCPParamValue(v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, formatMCPParamValue(v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = formatMCPParamValue(v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Run 'ignition-pp-cli auth login --chrome' to refresh browser-session credentials." +
					"\n      See API docs: https://go.ignitionapp.com" +
					"\n      Run 'ignition-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API credentials." +
					"\n      Run 'ignition-pp-cli auth login --chrome' to refresh browser-session credentials." +
					"\n      See API docs: https://go.ignitionapp.com" +
					"\n      Run 'ignition-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource. Check that they have the required permissions and match the API's expected auth scheme." +
					"\n      Run 'ignition-pp-cli auth login --chrome' to refresh browser-session credentials." +
					"\n      See API docs: https://go.ignitionapp.com" +
					"\n      Run 'ignition-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		if binaryResponse {
			encoded := base64.StdEncoding.EncodeToString(data)
			out, err := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      encoded,
				"byte_count":       len(data),
			})
			if err != nil {
				return mcplib.NewToolResultError(fmt.Sprintf("encoding binary result: %v", err)), nil
			}
			if len(out) > bound.MaxBytes {
				return mcplib.NewToolResultError(fmt.Sprintf("binary response is too large for MCP text output: %d response bytes encode to %d base64 bytes and %d MCP result bytes, exceeding the %d byte budget. Use the companion CLI command with --output <file> to save the payload locally.", len(data), len(encoded), len(out), bound.MaxBytes)), nil
			}
			return mcplib.NewToolResultText(string(out)), nil
		}
		if pageConfig.CursorParam != "" {
			return mcpToolPageResultText(method, data, pageConfig, mcpCursor), nil
		}
		return mcpToolResultText(method, data), nil
	}
}

func mcpToolResultText(method string, data json.RawMessage) *mcplib.CallToolResult {
	return mcplib.NewToolResultText(bound.EndpointResponse(method, data))
}

func mcpToolPageResultText(method string, data json.RawMessage, pageConfig mcpPageConfig, cursor string) *mcplib.CallToolResult {
	return mcplib.NewToolResultText(bound.EndpointPageResponse(method, data, bound.PageOptions{
		Cursor:         cursor,
		CursorParam:    pageConfig.CursorParam,
		NextCursorPath: pageConfig.NextCursorPath,
	}))
}

func newMCPClient() (*client.Client, error) {
	cfg, err := newMCPConfig()
	if err != nil {
		return nil, err
	}
	return newMCPClientFromConfig(cfg), nil
}

func newMCPConfig() (*config.Config, error) {
	cfg, err := config.Load("")
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	return cfg, nil
}

func newMCPClientFromConfig(cfg *config.Config) *client.Client {
	c := client.New(cfg, 60*time.Second, defaultMCPRateLimit)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c
}

func mcpDBPath() (string, error) {
	dir, err := cliutil.DataDir()
	if err != nil {
		return "", err
	}
	return filepath.Join(dir, "data.db"), nil
}

type mcpStoreStatusKind string

const (
	mcpStoreStatusEmpty mcpStoreStatusKind = "empty"
	mcpStoreStatusReady mcpStoreStatusKind = "ready"
)

func openMCPReadOnlyStore(path string) (*store.Store, *mcplib.CallToolResult) {
	if _, err := os.Stat(path); err != nil {
		if os.IsNotExist(err) {
			return nil, mcplib.NewToolResultError(mcpMissingStoreMessage(path))
		}
		return nil, mcplib.NewToolResultError(fmt.Sprintf("checking local data store %s: %v", path, err))
	}
	db, err := store.OpenReadOnly(path)
	if err != nil {
		return nil, mcplib.NewToolResultError(fmt.Sprintf("opening local data store %s: %v. Run ignition-pp-cli sync to refresh the store, or use live endpoint MCP tools for unsynced data.", path, err))
	}
	return db, nil
}

func mcpMissingStoreMessage(path string) string {
	return fmt.Sprintf("No local data store found at %s. Run ignition-pp-cli sync before using MCP search/sql, or use live endpoint MCP tools for unsynced data.", path)
}

func mcpStoreStatus(db *store.Store) (mcpStoreStatusKind, error) {
	status, err := db.Status()
	if err != nil {
		return "", err
	}
	if len(status) == 0 {
		return mcpStoreStatusEmpty, nil
	}
	return mcpStoreStatusReady, nil
}

func mcpEmptyStoreNextStep() string {
	return "Run ignition-pp-cli sync to populate the local SQLite store before using MCP search/sql."
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate rejects multi-statement input, then applies an allowlist (SELECT or
// WITH only) AFTER stripping the leading whitespace, line comments, block
// comments, and semicolons that SQLite itself ignores before parsing. A naive
// HasPrefix check on a keyword blocklist is bypassable by prefixing the
// dangerous statement with "/* x */" or "-- x\n"; a naive leading-keyword
// allowlist is bypassable by appending "; ATTACH DATABASE ...". Combined with
// the empirical fact that modernc.org/sqlite's mode=ro does NOT block VACUUM
// INTO (writes a snapshot to a new file) or ATTACH DATABASE (opens a separate
// writable handle), either bypass produces silent exfiltration to an
// attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	stripped := stripLeadingSQLNoise(query)
	if hasTrailingSQLStatement(stripped) {
		return fmt.Errorf("only a single SELECT or WITH statement is allowed")
	}
	upper := strings.ToUpper(stripped)
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

// hasTrailingSQLStatement reports whether query contains a statement
// terminator followed by more executable SQL. A trailing semicolon is allowed;
// a second statement is not. Semicolons inside string literals, quoted
// identifiers, bracket identifiers, and comments are ignored to match SQLite's
// parser shape closely enough for this security gate.
func hasTrailingSQLStatement(query string) bool {
	inSingle := false
	inDouble := false
	inBacktick := false
	inBracket := false
	inLineComment := false
	inBlockComment := false

	for i := 0; i < len(query); i++ {
		ch := query[i]
		next := byte(0)
		if i+1 < len(query) {
			next = query[i+1]
		}

		switch {
		case inLineComment:
			if ch == '\n' {
				inLineComment = false
			}
			continue
		case inBlockComment:
			if ch == '*' && next == '/' {
				inBlockComment = false
				i++
			}
			continue
		case inSingle:
			if ch == '\'' {
				if next == '\'' {
					i++
					continue
				}
				inSingle = false
			}
			continue
		case inDouble:
			if ch == '"' {
				if next == '"' {
					i++
					continue
				}
				inDouble = false
			}
			continue
		case inBacktick:
			if ch == '`' {
				if next == '`' {
					i++
					continue
				}
				inBacktick = false
			}
			continue
		case inBracket:
			if ch == ']' {
				inBracket = false
			}
			continue
		}

		switch {
		case ch == '-' && next == '-':
			inLineComment = true
			i++
		case ch == '/' && next == '*':
			inBlockComment = true
			i++
		case ch == '\'':
			inSingle = true
		case ch == '"':
			inDouble = true
		case ch == '`':
			inBacktick = true
		case ch == '[':
			inBracket = true
		case ch == ';':
			if stripLeadingSQLNoise(query[i+1:]) != "" {
				return true
			}
			return false
		}
	}
	return false
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	path, err := mcpDBPath()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("resolving database: %v", err)), nil
	}
	db, toolErr := openMCPReadOnlyStore(path)
	if toolErr != nil {
		return toolErr, nil
	}
	defer db.Close()

	rows, err := db.DB().QueryContext(ctx, query)
	if err != nil {
		return mcplib.NewToolResultError(mcpSQLQueryError(err)), nil
	}
	defer rows.Close()

	cols, err := rows.Columns()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading columns: %v", err)), nil
	}
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		if err := rows.Scan(ptrs...); err != nil {
			return mcplib.NewToolResultError(fmt.Sprintf("scanning row: %v", err)), nil
		}
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}
	// rows.Next() stops on a mid-iteration error without failing the loop, so
	// skipping rows.Err() would return a truncated result set as success.
	if err := rows.Err(); err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading rows: %v", err)), nil
	}
	storeStatus, err := mcpStoreStatus(db)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading store status: %v", err)), nil
	}

	return toolResultJSON(mcpSQLEnvelope(results, cols, storeStatus))
}

func mcpSQLEnvelope(rows []map[string]any, columns []string, storeStatus mcpStoreStatusKind) map[string]any {
	if rows == nil {
		rows = []map[string]any{}
	}
	out := map[string]any{
		"count":        len(rows),
		"columns":      columns,
		"rows":         rows,
		"store_status": storeStatus,
		"resumable":    false,
	}
	if len(rows) == 0 {
		if storeStatus == mcpStoreStatusEmpty {
			out["next_step"] = mcpEmptyStoreNextStep()
		} else {
			out["next_step"] = "The read-only SQL query returned no rows. Check resource_type filters, json_extract paths, or run sync again if data may be stale."
		}
	}
	return out
}

func mcpSQLQueryError(err error) string {
	msg := err.Error()
	if strings.Contains(strings.ToLower(msg), "no such table") {
		return fmt.Sprintf("query failed: %v. Synced records live in resources(resource_type, id, data), not one SQL table per resource. Filter by resource_type, for example resource_type='acknowledgements', and read JSON fields with json_extract(data,'$.field').", err)
	}
	return fmt.Sprintf("query failed: %v", err)
}

// toolResultJSON renders v as the indented JSON body of an MCP text result,
// surfacing a marshal failure as a tool error instead of empty content.
func toolResultJSON(v any) (*mcplib.CallToolResult, error) {
	text, err := bound.JSON(v)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("encoding result: %v", err)), nil
	}
	return mcplib.NewToolResultText(text), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	paths := map[string]string{}
	if dir, err := cliutil.ConfigDir(); err == nil {
		paths["config_dir"] = dir
	}
	if dir, err := cliutil.DataDir(); err == nil {
		paths["data_dir"] = dir
	}
	if dir, err := cliutil.StateDir(); err == nil {
		paths["state_dir"] = dir
	}
	if dir, err := cliutil.CacheDir(); err == nil {
		paths["cache_dir"] = dir
	}
	ctx := map[string]any{
		"api":         "ignition",
		"description": "A read-only, agent-native CLI for Ignition proposals, invoices, billing, clients, and payments, with a local SQLite mirror and invoicing analytics the web UI does not offer.",
		"archetype":   "payments",
		"tool_count":  29,
		"paths":       paths,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion ignition-pp-cli binary.",
		// learn_protocol is generated from the single shared source of
		// truth (the exported constant internal/learn.RecallFirstProtocol)
		// also consumed by the CLI agent-context command, so the MCP and
		// CLI agent surfaces cannot drift.
		"learn_protocol": learn.RecallFirstProtocol,
		"auth": map[string]any{
			"type": "cookie",
			"env_vars": []map[string]any{
				{
					"name":        "IGNITION_SESSION_COOKIE",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
			"docs_url": "https://go.ignitionapp.com",
		},
		"resources": []map[string]any{
			{
				"name":        "acknowledgements",
				"description": "GraphQL BFF operations for acknowledgements",
				"endpoints":   []string{"get"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "adds",
				"description": "GraphQL BFF operations for adds",
				"endpoints":   []string{"on_plan_trial_banner"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "apps",
				"description": "GraphQL BFF operations for apps",
				"endpoints":   []string{"with_capability"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "auth-api",
				"description": "Operations on graphql",
				"endpoints":   []string{"create_graphql"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "billings",
				"description": "GraphQL BFF operations for billings",
				"endpoints":   []string{"get"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "brandings",
				"description": "GraphQL BFF operations for brandings",
				"endpoints":   []string{"theme"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "clients",
				"description": "GraphQL BFF operations for clients",
				"endpoints":   []string{"forms", "get", "get_2", "proposals", "summary_client", "summary_rejected_payments", "sync_status", "tags"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "codes",
				"description": "GraphQL BFF operations for codes",
				"endpoints":   []string{"version"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "compliances",
				"description": "GraphQL BFF operations for compliances",
				"endpoints":   []string{"availability"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "currents",
				"description": "GraphQL BFF operations for currents",
				"endpoints":   []string{"practice", "user"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "features",
				"description": "GraphQL BFF operations for features",
				"endpoints":   []string{"gate"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "forms",
				"description": "GraphQL BFF operations for forms",
				"endpoints":   []string{"templates"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "payments",
				"description": "GraphQL BFF operations for payments",
				"endpoints":   []string{"settings"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "preferreds",
				"description": "GraphQL BFF operations for preferreds",
				"endpoints":   []string{"proposal_editor"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "proposals",
				"description": "GraphQL BFF operations for proposals",
				"endpoints":   []string{"get"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "search_index",
				"description": "Verified paged search over the Ignition search index (proposals, invoices, billing items).",
				"endpoints":   []string{"billing_items", "invoices", "proposals"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "site",
				"description": "GraphQL BFF operations for site",
				"endpoints":   []string{"navigation"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "solos",
				"description": "GraphQL BFF operations for solos",
				"endpoints":   []string{"plan_banner"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "unseens",
				"description": "GraphQL BFF operations for unseens",
				"endpoints":   []string{"count"},
				"searchable":  true,
				"writable":    true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Outstanding-billing rollup", "command": "outstanding", "description": "See everything billed but not yet paid, summed per client.", "rationale": "Requires a local join of invoices and billing items by unpaid status that no single Ignition GraphQL call returns.", "via": "mcp-command-mirror"},
			{"name": "Proposal pipeline by status", "command": "pipeline", "description": "Count proposals across DRAFT, AWAITING_ACCEPTANCE, ACCEPTED, and LOST for the whole book of business.", "rationale": "Requires aggregating the full proposal search index locally by status enum.", "via": "mcp-command-mirror"},
			{"name": "Per-client billing summary", "command": "client-billing", "description": "One client's full money picture: what was proposed, invoiced, and still outstanding.", "rationale": "Requires joining a client's proposals, invoices, and billing items from the local mirror.", "via": "mcp-command-mirror"},
			{"name": "Unbilled work finder", "command": "unbilled", "description": "Surface accepted proposals that do not yet have a matching invoice or billing item.", "rationale": "Requires diffing accepted proposals against existing invoices locally; Ignition has no such report.", "via": "mcp-command-mirror"},
			{"name": "Rejected-payment watch", "command": "rejected-payments", "description": "List clients with a rejected or failed payment so collections can follow up.", "rationale": "Requires scanning per-client rejected-payment summaries across the synced client set.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Outstanding-billing rollup", "insight": "Requires a local join of invoices and billing items by unpaid status that no single Ignition GraphQL call returns."},
			{"topic": "Proposal pipeline by status", "insight": "Requires aggregating the full proposal search index locally by status enum."},
			{"topic": "Per-client billing summary", "insight": "Requires joining a client's proposals, invoices, and billing items from the local mirror."},
			{"topic": "Unbilled work finder", "insight": "Requires diffing accepted proposals against existing invoices locally; Ignition has no such report."},
			{"topic": "Rejected-payment watch", "insight": "Requires scanning per-client rejected-payment summaries across the synced client set."},
			{"topic": "Financial data", "insight": "Always use read-only operations for financial queries. Never use create/update tools for payment data without explicit user confirmation."},
			{"topic": "Reconciliation", "insight": "For reconciliation tasks, sync first then use sql for cross-referencing. API pagination over financial records is slow and rate-limited."},
		},
	}
	return toolResultJSON(ctx)
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
