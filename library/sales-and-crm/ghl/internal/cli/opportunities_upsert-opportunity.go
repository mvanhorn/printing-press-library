// Copyright 2026 alex-puckhaber. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOpportunitiesUpsertOpportunityCmd(flags *rootFlags) *cobra.Command {
	var bodyAssignedTo string
	var bodyFollowers string
	var bodyFollowersActionType string
	var bodyId string
	var bodyIsRemoveAllFollowers bool
	var bodyLocationId string
	var bodyLostReasonId string
	var bodyMonetaryValue string
	var bodyName string
	var bodyPipelineId string
	var bodyPipelineStageId string
	var bodyStatus string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "upsert-opportunity",
		Short:       "Upsert Opportunity",
		Example:     "  ghl-pp-cli opportunities upsert-opportunity --followers-action-type example-value",
		Annotations: map[string]string{"pp:endpoint": "opportunities.upsert-opportunity", "pp:method": "POST", "pp:path": "/opportunities/upsert"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("followers") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "followers")
				}
				if !cmd.Flags().Changed("followers-action-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "followers-action-type")
				}
				if !cmd.Flags().Changed("is-remove-all-followers") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "is-remove-all-followers")
				}
				if !cmd.Flags().Changed("location-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "location-id")
				}
				if !cmd.Flags().Changed("pipeline-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "pipeline-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/opportunities/upsert"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAssignedTo != "" {
					body["assignedTo"] = bodyAssignedTo
				}
				if bodyFollowers != "" {
					var parsedFollowers any
					if err := json.Unmarshal([]byte(bodyFollowers), &parsedFollowers); err != nil {
						return fmt.Errorf("parsing --followers JSON: %w", err)
					}
					body["followers"] = parsedFollowers
				}
				if bodyFollowersActionType != "" {
					body["followersActionType"] = bodyFollowersActionType
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyIsRemoveAllFollowers != false {
					body["isRemoveAllFollowers"] = bodyIsRemoveAllFollowers
				}
				if bodyLocationId != "" {
					body["locationId"] = bodyLocationId
				}
				if bodyLostReasonId != "" {
					body["lostReasonId"] = bodyLostReasonId
				}
				if bodyMonetaryValue != "" {
					var parsedMonetaryValue any
					if err := json.Unmarshal([]byte(bodyMonetaryValue), &parsedMonetaryValue); err != nil {
						return fmt.Errorf("parsing --monetary-value JSON: %w", err)
					}
					body["monetaryValue"] = parsedMonetaryValue
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPipelineId != "" {
					body["pipelineId"] = bodyPipelineId
				}
				if bodyPipelineStageId != "" {
					body["pipelineStageId"] = bodyPipelineStageId
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "opportunities",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAssignedTo, "assigned-to", "", "Assigned to")
	cmd.Flags().StringVar(&bodyFollowers, "followers", "", "contactId")
	cmd.Flags().StringVar(&bodyFollowersActionType, "followers-action-type", "", "followers action type")
	cmd.Flags().StringVar(&bodyId, "id", "", "opportunityId")
	cmd.Flags().BoolVar(&bodyIsRemoveAllFollowers, "is-remove-all-followers", false, "isRemoveAllFollowers")
	cmd.Flags().StringVar(&bodyLocationId, "location-id", "", "locationId")
	cmd.Flags().StringVar(&bodyLostReasonId, "lost-reason-id", "", "lost reason Id")
	cmd.Flags().StringVar(&bodyMonetaryValue, "monetary-value", "", "Monetary value")
	cmd.Flags().StringVar(&bodyName, "name", "", "name")
	cmd.Flags().StringVar(&bodyPipelineId, "pipeline-id", "", "pipeline Id")
	cmd.Flags().StringVar(&bodyPipelineStageId, "pipeline-stage-id", "", "Pipeline stage id")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Status")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
