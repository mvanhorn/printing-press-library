# hubspot-pp-cli Build Log

Manifest transcendence rows: 9 planned, 9 built. Phase 3 will not pass until all 9 ship.

## Priority 0 — foundation
- Generator emitted the typed `crm` table and resources schema (covers Priority 0 data layer for contacts)
- Added `contact_score_snapshots` and `contact_engagement_snapshots` migrations to `internal/store/extras.go` for cross-time analytics

## Priority 1 — absorbed (generated)
- 10 typed endpoint commands emitted from the spec (list/get/search/batch/create/update/archive/merge/gdpr-delete + properties)
- Auth correctly wired to `HUBSPOT_PRIVATE_APP_TOKEN` (canonical from `research.json`) with `HUBSPOT_OAUTH2` fallback (slug-derived)
- All absorbed rows resolve via `<binary> <leaf> [flags]`

## Priority 2 — transcendence (hand-coded)
All 9 novel commands implemented as hand-authored files. Headers stripped of "Generated by CLI Printing Press DO NOT EDIT" — regen merger will treat them as hand-authored.

| # | Command | Status | LoC | Notes |
|---|---------|--------|-----|-------|
| 1 | engagement-decay | built | ~120 | Window-based decay over local store; cursor-streamed |
| 2 | lifecycle-stuck | built | ~140 | Per-stage median dwell baseline computed from result set (min-N=5) |
| 3 | stale-but-valuable | built | ~110 | Composite value score = score + 10*deals + revenue/1000 |
| 4 | source-roi | built | ~130 | Min-contacts threshold protects against small-N noise |
| 5 | score-drift | built | ~150 | Lazy snapshot write on invocation; oldest-prior in [now-2W, now-W] |
| 6 | owner-overload | built | ~150 | Stage weights {lead:1, mql:2, sql:4, opp:8, customer:4} |
| 7 | silent-after-first-touch | built | ~100 | Sequence filter: exactly one touch then silence |
| 8 | duplicate-suspects | built | ~200 | Jaro-Winkler with first-char email-local blocking; scan-page cap |
| 9 | daily-digest | built | ~110 | Composes other helpers; mirrors Stripe daily-digest pattern from PR #915 |

## Shared helpers
- `internal/cli/contact_props.go` — parses HubSpot contact JSON (string→number, ISO timestamps, null-safe)
- `internal/cli/novel_helpers.go` — shared compute helpers (streamContacts, snapshot writers, drift computer, stage-promotion computer)

## Verification
- `go build ./cmd/hubspot-pp-cli` PASS
- `go vet ./...` clean
- `go test ./internal/cli/... ./internal/store/...` PASS (table-driven tests for helpers + 9 stub `t.Skip("TODO")` files that pass trivially)
- All 9 novel leaves: `--help` shows `Usage: hubspot-pp-cli <leaf> [flags]` (not parent fall-through)
- All 9 novel commands: `--dry-run` exits 0 (verify-friendly RunE)

## Skipped intentionally
- Per-stage entry timestamp tracking — HubSpot's default sync doesn't return `hs_lifecyclestage_*_date_entered_*` properties; lifecycle-stuck approximates with `createdate` and notes this in the help text
- Live MCP introspection — endpoint surface already covered by the generator's MCP tree mirror
- Write-side endpoints — generated for completeness from the spec (POST/PATCH/DELETE create/update/archive/batch_*) but never invoked by novel commands; SKILL.md anti-triggers clarify the read-only stance

## Printing Press issues observed
- None blocking. Generator handled the OAuth2 securityScheme correctly with the canonical env var override from `research.json`.
