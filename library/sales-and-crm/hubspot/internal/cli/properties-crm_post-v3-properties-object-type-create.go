// Copyright 2026 simplepathmedia. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newPropertiesCrmPostV3PropertiesObjectTypeCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyCalculationFormula string
	var bodyCurrencyPropertyName string
	var bodyDataSensitivity string
	var bodyDescription string
	var bodyDisplayOrder int
	var bodyExternalOptions bool
	var bodyFieldType string
	var bodyFormField bool
	var bodyGroupName string
	var bodyHasUniqueValue bool
	var bodyHidden bool
	var bodyLabel string
	var bodyName string
	var bodyNumberDisplayHint string
	var bodyOptions string
	var bodyReferencedObjectType string
	var bodyShowCurrencySymbol bool
	var bodyType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-v3-properties-object-type-create <objectType>",
		Aliases:     []string{"create"},
		Short:       "Create and return a copy of a new property for the specified object type.",
		Example:     "  hubspot-pp-cli properties-crm post-v3-properties-object-type-create example-value --field-type example-value",
		Annotations: map[string]string{"pp:endpoint": "properties-crm.post-v3-properties-object-type-create", "pp:method": "POST", "pp:path": "/crm/v3/properties/{objectType}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("field-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "field-type")
				}
				if !cmd.Flags().Changed("group-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "group-name")
				}
				if !cmd.Flags().Changed("label") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "label")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "type")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/crm/v3/properties/{objectType}"
			path = replacePathParam(path, "objectType", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCalculationFormula != "" {
					body["calculationFormula"] = bodyCalculationFormula
				}
				if bodyCurrencyPropertyName != "" {
					body["currencyPropertyName"] = bodyCurrencyPropertyName
				}
				if bodyDataSensitivity != "" {
					body["dataSensitivity"] = bodyDataSensitivity
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyDisplayOrder != 0 {
					body["displayOrder"] = bodyDisplayOrder
				}
				if bodyExternalOptions != false {
					body["externalOptions"] = bodyExternalOptions
				}
				if bodyFieldType != "" {
					body["fieldType"] = bodyFieldType
				}
				if bodyFormField != false {
					body["formField"] = bodyFormField
				}
				if bodyGroupName != "" {
					body["groupName"] = bodyGroupName
				}
				if bodyHasUniqueValue != false {
					body["hasUniqueValue"] = bodyHasUniqueValue
				}
				if bodyHidden != false {
					body["hidden"] = bodyHidden
				}
				if bodyLabel != "" {
					body["label"] = bodyLabel
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyNumberDisplayHint != "" {
					body["numberDisplayHint"] = bodyNumberDisplayHint
				}
				if bodyOptions != "" {
					var parsedOptions any
					if err := json.Unmarshal([]byte(bodyOptions), &parsedOptions); err != nil {
						return fmt.Errorf("parsing --options JSON: %w", err)
					}
					body["options"] = parsedOptions
				}
				if bodyReferencedObjectType != "" {
					body["referencedObjectType"] = bodyReferencedObjectType
				}
				if bodyShowCurrencySymbol != false {
					body["showCurrencySymbol"] = bodyShowCurrencySymbol
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "properties-crm",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCalculationFormula, "calculation-formula", "", "Represents a formula that is used to compute a calculated property.")
	cmd.Flags().StringVar(&bodyCurrencyPropertyName, "currency-property-name", "", "Currency property name")
	cmd.Flags().StringVar(&bodyDataSensitivity, "data-sensitivity", "", "Indicates the sensitivity level of the property, with options: highly_sensitive, non_sensitive, or sensitive.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A description of the property that will be shown as help text in HubSpot.")
	cmd.Flags().IntVar(&bodyDisplayOrder, "display-order", 0, "Properties are displayed in order starting with the lowest positive integer value. Values of -1 will cause the...")
	cmd.Flags().BoolVar(&bodyExternalOptions, "external-options", false, "Applicable only for 'enumeration' type properties. Should be set to true in conjunction with a...")
	cmd.Flags().StringVar(&bodyFieldType, "field-type", "", "Controls how the property appears in HubSpot.")
	cmd.Flags().BoolVar(&bodyFormField, "form-field", false, "Whether or not the property can be used in a HubSpot form.")
	cmd.Flags().StringVar(&bodyGroupName, "group-name", "", "The name of the property group the property belongs to.")
	cmd.Flags().BoolVar(&bodyHasUniqueValue, "has-unique-value", false, "Whether or not the property's value must be unique. Once set, this can't be changed.")
	cmd.Flags().BoolVar(&bodyHidden, "hidden", false, "If true, the property won't be visible and can't be used in HubSpot.")
	cmd.Flags().StringVar(&bodyLabel, "label", "", "A human-readable property label that will be shown in HubSpot.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The internal property name, which must be used when referencing the property via the API.")
	cmd.Flags().StringVar(&bodyNumberDisplayHint, "number-display-hint", "", "Number display hint")
	cmd.Flags().StringVar(&bodyOptions, "options", "", "A list of valid options for the property. This field is required for enumerated properties.")
	cmd.Flags().StringVar(&bodyReferencedObjectType, "referenced-object-type", "", "Should be set to 'OWNER' when 'externalOptions' is true, which causes the property to dynamically pull option values...")
	cmd.Flags().BoolVar(&bodyShowCurrencySymbol, "show-currency-symbol", false, "Show currency symbol")
	cmd.Flags().StringVar(&bodyType, "type", "", "The data type of the property.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
