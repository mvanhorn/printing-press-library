// Copyright 2026 simplepathmedia. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCrmPostV3ObjectsContactsSearchDoSearchCmd(flags *rootFlags) *cobra.Command {
	var bodyAfter string
	var bodyFilterGroups string
	var bodyLimit int
	var bodyProperties string
	var bodyQuery string
	var bodySorts string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-v3-objects-contacts-search-do-search",
		Short:       "Search for contacts by filtering on properties, searching through associations, and sorting results. Learn more...",
		Example:     "  hubspot-pp-cli crm post-v3-objects-contacts-search-do-search --after example-value",
		Annotations: map[string]string{"pp:endpoint": "crm.post-v3-objects-contacts-search-do-search", "pp:method": "POST", "pp:path": "/crm/v3/objects/contacts/search"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("after") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "after")
				}
				if !cmd.Flags().Changed("filter-groups") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "filter-groups")
				}
				if !cmd.Flags().Changed("limit") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "limit")
				}
				if !cmd.Flags().Changed("properties") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "properties")
				}
				if !cmd.Flags().Changed("sorts") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "sorts")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/crm/v3/objects/contacts/search"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAfter != "" {
					body["after"] = bodyAfter
				}
				if bodyFilterGroups != "" {
					var parsedFilterGroups any
					if err := json.Unmarshal([]byte(bodyFilterGroups), &parsedFilterGroups); err != nil {
						return fmt.Errorf("parsing --filter-groups JSON: %w", err)
					}
					body["filterGroups"] = parsedFilterGroups
				}
				if bodyLimit != 0 {
					body["limit"] = bodyLimit
				}
				if bodyProperties != "" {
					var parsedProperties any
					if err := json.Unmarshal([]byte(bodyProperties), &parsedProperties); err != nil {
						return fmt.Errorf("parsing --properties JSON: %w", err)
					}
					body["properties"] = parsedProperties
				}
				if bodyQuery != "" {
					body["query"] = bodyQuery
				}
				if bodySorts != "" {
					var parsedSorts any
					if err := json.Unmarshal([]byte(bodySorts), &parsedSorts); err != nil {
						return fmt.Errorf("parsing --sorts JSON: %w", err)
					}
					body["sorts"] = parsedSorts
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "crm",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAfter, "after", "", "A paging cursor token for retrieving subsequent pages.")
	cmd.Flags().StringVar(&bodyFilterGroups, "filter-groups", "", "Up to 6 groups of filters defining additional query criteria.")
	cmd.Flags().IntVar(&bodyLimit, "limit", 0, "The maximum results to return, up to 200 objects.")
	cmd.Flags().StringVar(&bodyProperties, "properties", "", "A list of property names to include in the response.")
	cmd.Flags().StringVar(&bodyQuery, "query", "", "The search query string, up to 3000 characters.")
	cmd.Flags().StringVar(&bodySorts, "sorts", "", "Specifies sorting order based on object properties.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
