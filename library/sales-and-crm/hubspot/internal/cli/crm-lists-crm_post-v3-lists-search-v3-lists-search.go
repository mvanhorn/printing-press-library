// Copyright 2026 simplepathmedia. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCrmListsCrmPostV3ListsSearchV3ListsSearchCmd(flags *rootFlags) *cobra.Command {
	var bodyAdditionalProperties string
	var bodyCount int
	var bodyListIds string
	var bodyObjectTypeId string
	var bodyOffset int
	var bodyProcessingTypes string
	var bodyQuery string
	var bodySort string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-v3-lists-search-v3-lists-search",
		Short:       "Post v3 lists search v3 lists search",
		Example:     "  hubspot-pp-cli crm-lists-crm post-v3-lists-search-v3-lists-search",
		Annotations: map[string]string{"pp:endpoint": "crm-lists-crm.post-v3-lists-search-v3-lists-search", "pp:method": "POST", "pp:path": "/crm/v3/lists/search"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("additional-properties") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "additional-properties")
				}
				if !cmd.Flags().Changed("offset") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "offset")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/crm/v3/lists/search"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAdditionalProperties != "" {
					var parsedAdditionalProperties any
					if err := json.Unmarshal([]byte(bodyAdditionalProperties), &parsedAdditionalProperties); err != nil {
						return fmt.Errorf("parsing --additional-properties JSON: %w", err)
					}
					body["additionalProperties"] = parsedAdditionalProperties
				}
				if bodyCount != 0 {
					body["count"] = bodyCount
				}
				if bodyListIds != "" {
					var parsedListIds any
					if err := json.Unmarshal([]byte(bodyListIds), &parsedListIds); err != nil {
						return fmt.Errorf("parsing --list-ids JSON: %w", err)
					}
					body["listIds"] = parsedListIds
				}
				if bodyObjectTypeId != "" {
					body["objectTypeId"] = bodyObjectTypeId
				}
				if bodyOffset != 0 {
					body["offset"] = bodyOffset
				}
				if bodyProcessingTypes != "" {
					var parsedProcessingTypes any
					if err := json.Unmarshal([]byte(bodyProcessingTypes), &parsedProcessingTypes); err != nil {
						return fmt.Errorf("parsing --processing-types JSON: %w", err)
					}
					body["processingTypes"] = parsedProcessingTypes
				}
				if bodyQuery != "" {
					body["query"] = bodyQuery
				}
				if bodySort != "" {
					body["sort"] = bodySort
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "crm-lists-crm",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAdditionalProperties, "additional-properties", "", "The property names of any additional list properties to include in the response. Properties that do not exist or...")
	cmd.Flags().IntVar(&bodyCount, "count", 0, "The number of lists to include in the response. Defaults to `20` if no value is provided. The max `count` is `500`.")
	cmd.Flags().StringVar(&bodyListIds, "list-ids", "", "ILS list ids to be included in search results. If not specified, all lists matching other criteria will be included")
	cmd.Flags().StringVar(&bodyObjectTypeId, "object-type-id", "", "Object type id")
	cmd.Flags().IntVar(&bodyOffset, "offset", 0, "Value used to paginate through lists. The `offset` provided in the response can be used in the next request to fetch...")
	cmd.Flags().StringVar(&bodyProcessingTypes, "processing-types", "", "List processing types to be included in search results. If not specified, all lists with all processing types will...")
	cmd.Flags().StringVar(&bodyQuery, "query", "", "The `query` that will be used to search for lists by list name. If no `query` is provided, then the results will...")
	cmd.Flags().StringVar(&bodySort, "sort", "", "Sort field and order")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
