// Copyright 2026 simplepathmedia. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newGroupsGetCrmV3PropertiesObjectTypeNameGetByNameCmd(flags *rootFlags) *cobra.Command {
	var flagLocale string

	cmd := &cobra.Command{
		Use:         "get-crm-v3-properties-object-type-name-get-by-name <groupName> <objectType>",
		Short:       "Read a property group identified by {groupName}.",
		Example:     "  hubspot-pp-cli groups get-crm-v3-properties-object-type-name-get-by-name example-resource example-value",
		Annotations: map[string]string{"pp:endpoint": "groups.get-crm-v3-properties-object-type-name-get-by-name", "pp:method": "GET", "pp:path": "/crm/v3/properties/{objectType}/groups/{groupName}", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/crm/v3/properties/{objectType}/groups/{groupName}"
			path = replacePathParam(path, "groupName", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("objectType is required\nUsage: %s <%s>", cmd.CommandPath(), "objectType"))
			}
			path = replacePathParam(path, "objectType", args[1])
			params := map[string]string{}
			if flagLocale != "" {
				params["locale"] = fmt.Sprintf("%v", flagLocale)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "groups", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagLocale, "locale", "", "Locale")

	return cmd
}
