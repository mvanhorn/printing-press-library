# Phase 4.95 Local Code Review — Findings

## Reviewers dispatched (parallel via Agent tool, direct-subagent fallback path)

- correctness reviewer
- security reviewer
- maintainability reviewer

## Round 1 outcome

**Findings cleared at round 1 (no autofix required).**

### Correctness — 3 findings, all non-issues on verification

- **Finding 1** (hierarchical `products_buyer-intent` hyphen variant): verified the sync layer and store schema use `products_buyer_intent` (underscore) exclusively. The hyphenated form appears only as an MCP tool name. The IN-clauses in `alt_track.go:120` and `buyer_intent_list.go:111` already cover the underscore variant correctly.
- **Finding 2** (watch.go `reviews` fallback to `resources` table): verified `reviews` is a typed table (`CREATE TABLE "reviews"` in store migrations); watch.go correctly queries `SELECT COUNT(*) FROM reviews WHERE products_id = ?`. No `resources`-table fallback needed.
- **Finding 3** (reviews.go subquery semantics): the `(products_id = ? OR products_id IN (SELECT id FROM products WHERE slug=? OR id=?))` shape is correct. When products are unsynced, returning no reviews is the right behavior (parent-cascade ensures normal usage has products synced before reviews).

### Security — clean

All SQL parameterized; no credential leakage; no shell-out; no path traversal beyond user's own FS.

### Maintainability — 12 findings, surfaced to polish (not autofixed)

Reviewer flagged duplicated `--since` parsing, store-open boilerplate, dogfood-cap, SQL fragments. All real DRY opportunities but represent **tradeoffs**: late-stage refactoring of working code introduces regression risk, and the duplication is across 5 sibling files with stable signatures. Surfacing for Phase 5.5 polish to consider.

### `go build ./...` and `go test ./internal/cli/...` — PASS after verification

## Verdict

Convergence reached in round 1: 0 in-scope blockers, 0 autofixes applied, 0 surface-to-user issues. Maintainability findings forwarded to Phase 5.5 polish as candidates.
