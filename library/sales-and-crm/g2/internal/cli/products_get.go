// Copyright 2026 rahul-bansal. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newProductsGetCmd(flags *rootFlags) *cobra.Command {
	var flagFilterCategoryId string
	var flagFilterProductId string
	var flagFilterReviewCountGteq string
	var flagFilterSlug string
	var flagFilterStarRating string
	var flagFilterVendorName string
	var flagFilterProductNameCont string
	var flagFilterProductNameEq string
	var flagFilterPublicDetailUrl string
	var flagFilterPublicDetailUrlCont string
	var flagFilterQuery string
	var flagFieldsProducts string
	var flagInclude string

	cmd := &cobra.Command{
		Use:         "get",
		Aliases:     []string{"list"},
		Short:       "**Requires `products.read` scope.**",
		Example:     "  g2-pp-cli products get",
		Annotations: map[string]string{"pp:endpoint": "products.get", "pp:method": "GET", "pp:path": "/api/v2/products", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v2/products"
			params := map[string]string{}
			if flagFilterCategoryId != "" {
				params["filter[category_id][]"] = fmt.Sprintf("%v", flagFilterCategoryId)
			}
			if flagFilterProductId != "" {
				params["filter[product_id][]"] = fmt.Sprintf("%v", flagFilterProductId)
			}
			if flagFilterReviewCountGteq != "" {
				params["filter[review_count_gteq]"] = fmt.Sprintf("%v", flagFilterReviewCountGteq)
			}
			if flagFilterSlug != "" {
				params["filter[slug][]"] = fmt.Sprintf("%v", flagFilterSlug)
			}
			if flagFilterStarRating != "" {
				params["filter[star_rating][]"] = fmt.Sprintf("%v", flagFilterStarRating)
			}
			if flagFilterVendorName != "" {
				params["filter[vendor_name]"] = fmt.Sprintf("%v", flagFilterVendorName)
			}
			if flagFilterProductNameCont != "" {
				params["filter[product_name_cont]"] = fmt.Sprintf("%v", flagFilterProductNameCont)
			}
			if flagFilterProductNameEq != "" {
				params["filter[product_name_eq]"] = fmt.Sprintf("%v", flagFilterProductNameEq)
			}
			if flagFilterPublicDetailUrl != "" {
				params["filter[public_detail_url][]"] = fmt.Sprintf("%v", flagFilterPublicDetailUrl)
			}
			if flagFilterPublicDetailUrlCont != "" {
				params["filter[public_detail_url_cont][]"] = fmt.Sprintf("%v", flagFilterPublicDetailUrlCont)
			}
			if flagFilterQuery != "" {
				params["filter[query]"] = fmt.Sprintf("%v", flagFilterQuery)
			}
			if flagFieldsProducts != "" {
				params["fields[products]"] = fmt.Sprintf("%v", flagFieldsProducts)
			}
			if flagInclude != "" {
				params["include"] = fmt.Sprintf("%v", flagInclude)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "products", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagFilterCategoryId, "filter-category-id", "", "Filter products by category id")
	cmd.Flags().StringVar(&flagFilterProductId, "filter-product-id", "", "Filter products by one or more product UUIDs")
	cmd.Flags().StringVar(&flagFilterReviewCountGteq, "filter-review-count-gteq", "", "Filter products by minimum review count")
	cmd.Flags().StringVar(&flagFilterSlug, "filter-slug", "", "Filter products by one or more product slugs (exact match)")
	cmd.Flags().StringVar(&flagFilterStarRating, "filter-star-rating", "", "Filter products by star rating")
	cmd.Flags().StringVar(&flagFilterVendorName, "filter-vendor-name", "", "Filter products by vendor name")
	cmd.Flags().StringVar(&flagFilterProductNameCont, "filter-product-name-cont", "", "Filter products by partial name match")
	cmd.Flags().StringVar(&flagFilterProductNameEq, "filter-product-name-eq", "", "Filter products by exact name match")
	cmd.Flags().StringVar(&flagFilterPublicDetailUrl, "filter-public-detail-url", "", "Filter products by exact vendor website URL (case-sensitive)")
	cmd.Flags().StringVar(&flagFilterPublicDetailUrlCont, "filter-public-detail-url-cont", "", "Filter by partial URL/domain match (substring)")
	cmd.Flags().StringVar(&flagFilterQuery, "filter-query", "", "Search for products using full-text search")
	cmd.Flags().StringVar(&flagFieldsProducts, "fields-products", "", "Comma separated list of fields for products to include in response")
	cmd.Flags().StringVar(&flagInclude, "include", "", "Comma-separated list of relationships to include (e.g. categories,discussions,screenshots,vendor)")

	return cmd
}
