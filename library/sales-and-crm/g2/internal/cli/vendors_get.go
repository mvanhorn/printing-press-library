// Copyright 2026 rahul-bansal. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newVendorsGetCmd(flags *rootFlags) *cobra.Command {
	var flagFilterUpdatedAtGt string
	var flagFilterUpdatedAtLt string
	var flagFilterCreatedAtGt string
	var flagFilterCreatedAtLt string
	var flagFieldsVendors string
	var flagInclude string

	cmd := &cobra.Command{
		Use:         "get",
		Aliases:     []string{"list"},
		Short:       "**Requires `vendors.read` scope.**",
		Example:     "  g2-pp-cli vendors get",
		Annotations: map[string]string{"pp:endpoint": "vendors.get", "pp:method": "GET", "pp:path": "/api/v2/vendors", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v2/vendors"
			params := map[string]string{}
			if flagFilterUpdatedAtGt != "" {
				params["filter[updated_at_gt]"] = fmt.Sprintf("%v", flagFilterUpdatedAtGt)
			}
			if flagFilterUpdatedAtLt != "" {
				params["filter[updated_at_lt]"] = fmt.Sprintf("%v", flagFilterUpdatedAtLt)
			}
			if flagFilterCreatedAtGt != "" {
				params["filter[created_at_gt]"] = fmt.Sprintf("%v", flagFilterCreatedAtGt)
			}
			if flagFilterCreatedAtLt != "" {
				params["filter[created_at_lt]"] = fmt.Sprintf("%v", flagFilterCreatedAtLt)
			}
			if flagFieldsVendors != "" {
				params["fields[vendors]"] = fmt.Sprintf("%v", flagFieldsVendors)
			}
			if flagInclude != "" {
				params["include"] = fmt.Sprintf("%v", flagInclude)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "vendors", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagFilterUpdatedAtGt, "filter-updated-at-gt", "", "Filter records updated after this date")
	cmd.Flags().StringVar(&flagFilterUpdatedAtLt, "filter-updated-at-lt", "", "Filter records updated before this date")
	cmd.Flags().StringVar(&flagFilterCreatedAtGt, "filter-created-at-gt", "", "Filter records created after this date")
	cmd.Flags().StringVar(&flagFilterCreatedAtLt, "filter-created-at-lt", "", "Filter records created before this date")
	cmd.Flags().StringVar(&flagFieldsVendors, "fields-vendors", "", "Comma separated list of fields for vendors to include in response")
	cmd.Flags().StringVar(&flagInclude, "include", "", "Comma-separated list of relationships to include (e.g. products)")

	return cmd
}
