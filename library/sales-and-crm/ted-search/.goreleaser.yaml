version: 2
project_name: ted-search-pp-cli
changelog:
  disable: true
builds:
  - id: ted-search-pp-cli
    main: ./cmd/ted-search-pp-cli
    binary: ted-search-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/sales-and-crm/ted-search/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: ted-search-pp-mcp
    main: ./cmd/ted-search-pp-mcp
    binary: ted-search-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: ted-search-pp-cli
    repository:
      owner: mathias-michel
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "The entire EU public procurement corpus — €815B/year — searchable offline, with B2B lead generation for..."
    install: |
      bin.install "ted-search-pp-cli"
      bin.install "ted-search-pp-mcp"
