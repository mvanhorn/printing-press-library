// Package sync contains Salesforce sync helpers that assemble account-scoped
// CRM data through Composite Graph, then apply rate-budget and sharing probes
// before records are persisted locally.
package sync
