{
  "api_name": "clarify",
  "base_url": "https://api.clarify.ai/v1",
  "description": "Every Clarify API operation as a typed command, plus the morning briefing, meeting prep, and pipeline analytics the autonomous CRM knows about but cannot run.",
  "mcp_ready": "full",
  "http_transport": "standard",
  "mcp": {
    "endpoint_tools": "hidden",
    "orchestration": "code"
  },
  "auth": {
    "type": "api_key",
    "header": "Authorization",
    "in": "header",
    "env_vars": [
      "CLARIFY_API_KEY"
    ],
    "env_var_specs": [
      {
        "name": "CLARIFY_API_KEY",
        "kind": "per_call",
        "required": true,
        "sensitive": true,
        "inferred": true
      }
    ]
  },
  "required_headers": [],
  "tools": [
    {
      "name": "campaigns_recipients_get",
      "description": "Returns the people enrolled in a campaign as an offset-paginated JSON:API collection, each with their per-recipient engagement — whether they opened, clicked, replied, or unsubscribed, and when each last happened. Filter by engagement with `filter[event]` (for example `filter[event]=clicked` to build a retargeting audience), by delivery state with `filter[status]`, or by name/email with `filter[q]`. Recipients are collapsed to one row per person, preferring their active run. Required: workspace, campaignId. Optional: page (default: map[limit:500 offset:0]), sortOrder, filter.",
      "method": "GET",
      "path": "/workspaces/{workspace}/campaigns/{campaignId}/recipients",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "campaignId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the campaign.",
          "required": true
        },
        {
          "name": "page",
          "type": "object",
          "location": "query",
          "description": "Offset pagination, e.g. `page[offset]=0\u0026page[limit]=100`."
        },
        {
          "name": "sortOrder",
          "type": "object",
          "location": "query",
          "description": "Sort order, e.g. `sortOrder[column]=_created_at\u0026sortOrder[dir]=DESC`."
        },
        {
          "name": "filter",
          "type": "object",
          "location": "query",
          "description": "Nested filter object, e.g. `filter[event]=clicked\u0026filter[status]=completed`."
        }
      ]
    },
    {
      "name": "comments_create",
      "description": "Creates a comment on a record. The comment body is rich text, and `owner_id` plus `entity` identify the record it is attached to. Returns the created comment row. Required: workspace, entity, message, owner_id. Optional: silent, _event_id, _id.",
      "method": "POST",
      "path": "/workspaces/{workspace}/comments",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "_event_id",
          "type": "string",
          "location": "body",
          "description": "An optional client-supplied ID for the comment creation event; generated by the server when omitted."
        },
        {
          "name": "_id",
          "type": "string",
          "location": "body",
          "description": "The unique ID of the comment. Generated by the server when omitted on create."
        },
        {
          "name": "entity",
          "type": "string",
          "location": "body",
          "description": "The object type of the record the comment is attached to. Must be an object that supports comments (e.g. `person`, `deal`, `task`, or a custom object such as `c_dog`). Lead-finder records (`tam_person`, `tam_company`) cannot be commented on.",
          "required": true
        },
        {
          "name": "message",
          "type": "array",
          "location": "body",
          "description": "The comment body, as a non-empty array of rich-text (BlockNote) nodes.",
          "required": true
        },
        {
          "name": "owner_id",
          "type": "string",
          "location": "body",
          "description": "The unique ID of the record the comment is attached to.",
          "required": true
        }
      ]
    },
    {
      "name": "comments_delete",
      "description": "Permanently deletes a comment. A comment can be deleted by its author, by an admin, or by the owner of the agent that posted it. Returns the deleted comment. This cannot be undone. Required: workspace, id. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/comments/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the comment.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "comments_get",
      "description": "Returns a single comment by its ID. Required: workspace, id.",
      "method": "GET",
      "path": "/workspaces/{workspace}/comments/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the comment.",
          "required": true
        }
      ]
    },
    {
      "name": "comments_update",
      "description": "Replaces the body of an existing comment. Only the comment’s author may edit it. Returns the updated comment. Required: workspace, id, message. Optional: silent.",
      "method": "PATCH",
      "path": "/workspaces/{workspace}/comments/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the comment.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "message",
          "type": "array",
          "location": "body",
          "description": "The comment body, as a non-empty array of rich-text (BlockNote) nodes.",
          "required": true
        }
      ]
    },
    {
      "name": "layouts_get-by-id",
      "description": "Returns a single layout by its ID. A layout defines the UI structure — navigation, record page regions — rendered for the workspace. Required: workspace, id.",
      "method": "GET",
      "path": "/workspaces/{workspace}/layouts/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the layout.",
          "required": true
        }
      ]
    },
    {
      "name": "layouts_update",
      "description": "Replaces the layout’s `tree` and returns the updated layout. Required: workspace, id, data. Optional: silent, meta.",
      "method": "PATCH",
      "path": "/workspaces/{workspace}/layouts/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the layout.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes-tree",
          "wire_name": "data.attributes.tree",
          "type": "string",
          "location": "body",
          "description": "The layout’s UI tree: a recursive structure of nodes describing app navigation, a record page’s regions, or a list’s navigation, depending on the layout ID.",
          "required": true
        },
        {
          "name": "data-id",
          "wire_name": "data.id",
          "type": "string",
          "location": "body",
          "description": "The unique ID of the resource to update.",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "The resource type; matches the `object` path parameter.",
          "required": true
        },
        {
          "name": "meta",
          "type": "string",
          "location": "body",
          "description": "Optional JSON:API meta member carrying request-specific options; not accepted for this resource."
        }
      ]
    },
    {
      "name": "layouts_reset_layout",
      "description": "Restores the layout to its default `tree` and returns the resulting layout. Required: workspace, id. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/layouts/{id}/reset",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the layout.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "lists_get-workspace",
      "description": "Returns every list across all object types in the workspace as a paginated JSON:API collection. Filter by object type with `filter[entity]`. Dynamic lists are excluded unless you pass a matching `filter`. Required: workspace. Optional: page (default: map[limit:500 offset:0]), sortOrder, filter (plus 1 more).",
      "method": "GET",
      "path": "/workspaces/{workspace}/lists",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "page",
          "type": "object",
          "location": "query",
          "description": "Offset pagination, e.g. `page[offset]=0\u0026page[limit]=100`."
        },
        {
          "name": "sortOrder",
          "type": "object",
          "location": "query",
          "description": "Sort order, e.g. `sortOrder[column]=_created_at\u0026sortOrder[dir]=DESC`."
        },
        {
          "name": "filter",
          "type": "object",
          "location": "query",
          "description": "Nested filter object, e.g. `filter[entity]=deal\u0026filter[type]=static`. When omitted, dynamic lists are excluded."
        },
        {
          "name": "search",
          "type": "string",
          "location": "query",
          "description": "Case-insensitive substring match against the list title."
        }
      ]
    },
    {
      "name": "meetings_recording_disable",
      "description": "Turns off recording for a meeting so no notetaker bot joins the call. Returns the updated meeting record. Required: workspace, meetingId. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/meetings/{meetingId}/recording",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "meetingId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the meeting.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "meetings_recording_enable",
      "description": "Turns on recording for a meeting so a notetaker bot joins the call. Returns the updated meeting record. Required: workspace, meetingId. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/meetings/{meetingId}/recording",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "meetingId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the meeting.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "meetings_recordings_confirm-media-upload",
      "description": "Finalizes a media upload after the file has been submitted to the presigned policy: validates the stored object and marks the recording complete. Returns the finalized recording. Required: workspace, meetingId, recordingId. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/meetings/{meetingId}/recordings/media/{recordingId}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "meetingId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the meeting.",
          "required": true
        },
        {
          "name": "recordingId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the recording.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "meetings_recordings_get-artifacts",
      "description": "Returns short-lived signed URLs for a recording’s video and transcript files. Either URL is `null` when that artifact is not available. Required: workspace, meetingId, id. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/meetings/{meetingId}/recordings/{id}/artifacts",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "meetingId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the meeting.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the recording.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "meetings_recordings_init-media-upload",
      "description": "Creates a pending recording on the meeting and returns a presigned S3 POST policy for uploading an audio or video file directly to storage. Uploaded media is stored for playback and is not transcribed. Optionally include a transcript in the same request so one recording carries both video and transcript. Submit the file to the returned policy, then confirm the upload. Pass `recordingId` to overwrite a completed manual-upload recording instead of creating a new one. Required: workspace, meetingId, contentType. Optional: silent, recordingId, transcript.",
      "method": "POST",
      "path": "/workspaces/{workspace}/meetings/{meetingId}/recordings/media",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "meetingId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the meeting.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "contentType",
          "type": "string",
          "location": "body",
          "description": "The MIME type of the media file to upload; must be an `audio/*` or `video/*` type. Uploaded media is stored for playback and is not transcribed.",
          "required": true
        },
        {
          "name": "recordingId",
          "type": "string",
          "location": "body",
          "description": "A completed manual-upload recording to overwrite. When omitted, a new recording is created on the meeting. Only the artifacts supplied in this request are overwritten; other stored artifacts are left intact."
        },
        {
          "name": "transcript",
          "type": "array",
          "location": "body",
          "description": "An optional transcript to attach to the same recording as the media, so one recording carries both. Applied to the recording when the upload is confirmed, then used to generate the summary."
        }
      ]
    },
    {
      "name": "meetings_recordings_upload-transcript",
      "description": "Creates a recording on the meeting from an externally captured transcript, so meetings recorded outside Clarify can be summarized and searched, and triggers summary generation. A meeting with a recording already gets a second recording. Pass `recordingId` to overwrite a completed manual-upload recording’s transcript instead. Returns the recording. Required: workspace, meetingId, transcript. Optional: silent, recordingId.",
      "method": "POST",
      "path": "/workspaces/{workspace}/meetings/{meetingId}/recordings/transcript",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "meetingId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the meeting.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "recordingId",
          "type": "string",
          "location": "body",
          "description": "A completed manual-upload recording to overwrite. When omitted, a new recording is created on the meeting. Only the artifacts supplied in this request are overwritten; other stored artifacts are left intact."
        },
        {
          "name": "transcript",
          "type": "array",
          "location": "body",
          "description": "The meeting transcript, as an ordered list of speaker segments.",
          "required": true
        }
      ]
    },
    {
      "name": "meetings_transcript_upload",
      "description": "Creates a recording for a meeting from an externally captured transcript, so meetings recorded outside Clarify can be summarized and searched. Returns the created recording. Required: workspace, meetingId, transcript. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/meetings/{meetingId}/transcript",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "meetingId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the meeting.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "transcript",
          "type": "array",
          "location": "body",
          "description": "The meeting transcript, as an ordered list of speaker segments.",
          "required": true
        }
      ]
    },
    {
      "name": "objects_access_get-object",
      "description": "Returns entity-wide access delegations for the object type as a JSON:API collection. Entity-wide access delegation grants another user the same access you have to every record of that object type. The response includes both delegations you have granted and delegations granted to you. Requires a user-backed API key; workspace-actor API keys are not supported. Required: workspace, object (default: meeting).",
      "method": "GET",
      "path": "/workspaces/{workspace}/objects/{object}/access",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type this delegation applies to: `meeting` or `message`.",
          "required": true
        }
      ]
    },
    {
      "name": "objects_access_give-object",
      "description": "Grants another user entity-wide access delegation: the same access you have to every record of the given object type. Requires a paid plan with access delegation enabled. Requires a user-backed API key; workspace-actor API keys are not supported. Required: workspace, object (default: meeting), data. Optional: silent. Returns the new ObjectAccessResponse.",
      "method": "POST",
      "path": "/workspaces/{workspace}/objects/{object}/access",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type this delegation applies to: `meeting` or `message`.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes-delegate-id",
          "wire_name": "data.attributes.delegate_id",
          "type": "string",
          "location": "body",
          "description": "The ID of the user to grant access to.",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "Type",
          "required": true
        }
      ]
    },
    {
      "name": "objects_access_revoke-object",
      "description": "Revokes a previously granted access delegation. Requires a paid plan with access delegation enabled. Returns 202 Accepted with an empty body. Requires a user-backed API key; workspace-actor API keys are not supported. Required: workspace, object (default: meeting), id. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/objects/{object}/access/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type this delegation applies to: `meeting` or `message`.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the access delegation.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "objects_lists_create",
      "description": "Creates a list on the given object type. A `dynamic` list derives its membership from the SQL `query`; a `static` list starts empty. Returns the created list. Required: workspace, object, description, layout, title, type. Optional: silent, emoji, options (default: map[]) (plus 3 more).",
      "method": "POST",
      "path": "/workspaces/{workspace}/objects/{object}/lists",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "description",
          "type": "string",
          "location": "body",
          "description": "A short description of the list.",
          "required": true
        },
        {
          "name": "emoji",
          "type": "string",
          "location": "body",
          "description": "The emoji shown next to the list, or `null` for none."
        },
        {
          "name": "layout",
          "type": "string",
          "location": "body",
          "description": "How the list is rendered in the app: `table` or `board`.",
          "required": true
        },
        {
          "name": "options-column-metrics",
          "wire_name": "options.columnMetrics",
          "type": "object",
          "location": "body",
          "description": "The aggregation metric shown in the table footer for each column, keyed by field path (e.g. `sum`, `count`)."
        },
        {
          "name": "options-column-widths",
          "wire_name": "options.columnWidths",
          "type": "object",
          "location": "body",
          "description": "Table column pixel widths, keyed by field path. Absent keys use the default width."
        },
        {
          "name": "options-hidden-board-columns",
          "wire_name": "options.hiddenBoardColumns",
          "type": "object",
          "location": "body",
          "description": "Board columns hidden from view, keyed by segment field path; each value lists the hidden enum values for that field (`null` is the 'Unassigned' column)."
        },
        {
          "name": "options-hidden-primary-cell",
          "wire_name": "options.hiddenPrimaryCell",
          "type": "bool",
          "location": "body",
          "description": "Whether the primary (first) cell is hidden on cards in board layout."
        },
        {
          "name": "options-segmented-by-in-board",
          "wire_name": "options.segmentedByInBoard",
          "type": "string",
          "location": "body",
          "description": "For board layout, the field path whose values group records into columns."
        },
        {
          "name": "options-show-field-names-in-board",
          "wire_name": "options.showFieldNamesInBoard",
          "type": "bool",
          "location": "body",
          "description": "Whether field names are shown on cards in board layout."
        },
        {
          "name": "query-nlq",
          "wire_name": "query.nlq",
          "type": "string",
          "location": "body",
          "description": "The original natural-language query, if the SQL was generated from one."
        },
        {
          "name": "query-sql",
          "wire_name": "query.sql",
          "type": "string",
          "location": "body",
          "description": "The SQL query that defines the list membership and ordering.",
          "required": true
        },
        {
          "name": "query-version",
          "wire_name": "query.version",
          "type": "float",
          "location": "body",
          "description": "The view query schema version, used for migrations; an omitted value is treated as version 0 (the oldest)."
        },
        {
          "name": "rank",
          "type": "float",
          "location": "body",
          "description": "The sort position of the list; defaults to appending after existing lists."
        },
        {
          "name": "state",
          "type": "string",
          "location": "body",
          "description": "Whether the list is created as a `draft` or `published`."
        },
        {
          "name": "title",
          "type": "string",
          "location": "body",
          "description": "The display name of the list.",
          "required": true
        },
        {
          "name": "type",
          "type": "string",
          "location": "body",
          "description": "The list type: `dynamic` (membership derives from `query`) or `static` (an explicit set of records).",
          "required": true
        }
      ]
    },
    {
      "name": "objects_lists_delete",
      "description": "Permanently deletes a list. The object’s default list and the last remaining list on an object cannot be deleted. This cannot be undone. Required: workspace, object, list. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/objects/{object}/lists/{list}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "list",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the list.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "objects_lists_get",
      "description": "Returns the lists defined on an object type as a paginated JSON:API collection. Dynamic lists are excluded unless you pass a matching `filter`. Required: workspace, object. Optional: page (default: map[limit:500 offset:0]), sortOrder, filter (plus 1 more).",
      "method": "GET",
      "path": "/workspaces/{workspace}/objects/{object}/lists",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "page",
          "type": "object",
          "location": "query",
          "description": "Offset pagination, e.g. `page[offset]=0\u0026page[limit]=100`."
        },
        {
          "name": "sortOrder",
          "type": "object",
          "location": "query",
          "description": "Sort order, e.g. `sortOrder[column]=_created_at\u0026sortOrder[dir]=DESC`."
        },
        {
          "name": "filter",
          "type": "object",
          "location": "query",
          "description": "Nested filter object, e.g. `filter[type]=static\u0026filter[state]=published`. When omitted, dynamic lists are excluded."
        },
        {
          "name": "search",
          "type": "string",
          "location": "query",
          "description": "Case-insensitive substring match against the list title."
        }
      ]
    },
    {
      "name": "objects_lists_get-resources",
      "description": "Returns a paginated JSON:API collection of the resources that belong to a specific list, applying the list’s membership on top of any `filter` you pass. Use `include` to embed related resources. Required: workspace, object, list. Optional: page (default: map[limit:500 offset:0]), sortOrder, include (plus 1 more).",
      "method": "GET",
      "path": "/workspaces/{workspace}/objects/{object}/lists/{list}/resources",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "list",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the list.",
          "required": true
        },
        {
          "name": "page",
          "type": "object",
          "location": "query",
          "description": "Offset pagination, e.g. `page[offset]=0\u0026page[limit]=100`."
        },
        {
          "name": "sortOrder",
          "type": "object",
          "location": "query",
          "description": "Sort order, e.g. `sortOrder[column]=_created_at\u0026sortOrder[dir]=DESC`."
        },
        {
          "name": "include",
          "type": "string",
          "location": "query",
          "description": "Comma-separated relationship names to embed under `relationships` and return in `included`, e."
        },
        {
          "name": "filter",
          "type": "object",
          "location": "query",
          "description": "Nested filter object keyed by field name, e.g. `filter[status]=active` or `filter[amount][Greater than]=100`. See the endpoint description for the full operator syntax."
        }
      ]
    },
    {
      "name": "objects_lists_get-rows-csv",
      "description": "Streams the rows of a list as a `text/csv` file download. The first row is a header of field names. Pass a `sql` statement in the body to export a custom query instead of the list’s own rows. Required: workspace, object, list. Optional: silent, search, sql.",
      "method": "POST",
      "path": "/workspaces/{workspace}/objects/{object}/lists/{list}/rows/csv",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "list",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the list.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "search-query",
          "wire_name": "search.query",
          "type": "string",
          "location": "body",
          "description": "Case-insensitive substring match against the rows."
        },
        {
          "name": "sql",
          "type": "string",
          "location": "body",
          "description": "Overrides the list’s own query with a custom SQL statement selecting the rows to export. Defaults to the list’s query."
        }
      ]
    },
    {
      "name": "objects_lists_get-workspaces",
      "description": "Returns a single list by ID as a JSON:API resource. Required: workspace, object, list.",
      "method": "GET",
      "path": "/workspaces/{workspace}/objects/{object}/lists/{list}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "list",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the list.",
          "required": true
        }
      ]
    },
    {
      "name": "objects_lists_publish",
      "description": "Publishes a draft list, making it visible to its audience. Returns the updated list. Required: workspace, object, list. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/objects/{object}/lists/{list}/publish",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "list",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the list.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "objects_lists_unpublish",
      "description": "Reverts a published list to draft state. Returns the updated list. Required: workspace, object, list. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/objects/{object}/lists/{list}/unpublish",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "list",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the list.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "objects_lists_update",
      "description": "Applies a partial update to a list: only the fields present in the body are changed. The object’s default list cannot be updated. Returns the updated list. Required: workspace, object, list. Optional: silent, description, emoji (plus 5 more).",
      "method": "PATCH",
      "path": "/workspaces/{workspace}/objects/{object}/lists/{list}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "list",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the list.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "description",
          "type": "string",
          "location": "body",
          "description": "A short description of the list."
        },
        {
          "name": "emoji",
          "type": "string",
          "location": "body",
          "description": "The emoji shown next to the list, or `null` if unset."
        },
        {
          "name": "layout",
          "type": "string",
          "location": "body",
          "description": "How the list is rendered in the app: `table` or `board`."
        },
        {
          "name": "options-column-metrics",
          "wire_name": "options.columnMetrics",
          "type": "object",
          "location": "body",
          "description": "The aggregation metric shown in the table footer for each column, keyed by field path (e.g. `sum`, `count`)."
        },
        {
          "name": "options-column-widths",
          "wire_name": "options.columnWidths",
          "type": "object",
          "location": "body",
          "description": "Table column pixel widths, keyed by field path. Absent keys use the default width."
        },
        {
          "name": "options-hidden-board-columns",
          "wire_name": "options.hiddenBoardColumns",
          "type": "object",
          "location": "body",
          "description": "Board columns hidden from view, keyed by segment field path; each value lists the hidden enum values for that field (`null` is the 'Unassigned' column)."
        },
        {
          "name": "options-hidden-primary-cell",
          "wire_name": "options.hiddenPrimaryCell",
          "type": "bool",
          "location": "body",
          "description": "Whether the primary (first) cell is hidden on cards in board layout."
        },
        {
          "name": "options-segmented-by-in-board",
          "wire_name": "options.segmentedByInBoard",
          "type": "string",
          "location": "body",
          "description": "For board layout, the field path whose values group records into columns."
        },
        {
          "name": "options-show-field-names-in-board",
          "wire_name": "options.showFieldNamesInBoard",
          "type": "bool",
          "location": "body",
          "description": "Whether field names are shown on cards in board layout."
        },
        {
          "name": "query-nlq",
          "wire_name": "query.nlq",
          "type": "string",
          "location": "body",
          "description": "The original natural-language query, if the SQL was generated from one."
        },
        {
          "name": "query-sql",
          "wire_name": "query.sql",
          "type": "string",
          "location": "body",
          "description": "The SQL query that defines the list membership and ordering.",
          "required": true
        },
        {
          "name": "query-version",
          "wire_name": "query.version",
          "type": "float",
          "location": "body",
          "description": "The view query schema version, used for migrations; an omitted value is treated as version 0 (the oldest)."
        },
        {
          "name": "rank",
          "type": "float",
          "location": "body",
          "description": "The sort position of the list relative to its siblings."
        },
        {
          "name": "title",
          "type": "string",
          "location": "body",
          "description": "The display name of the list."
        }
      ]
    },
    {
      "name": "objects_records_add-attachment",
      "description": "Links a previously uploaded file (see the upload-URL endpoint) to the record and returns the updated record. Required: workspace, object, id, attachmentId, key, name. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/attachments",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "attachmentId",
          "type": "string",
          "location": "body",
          "description": "The attachment ID returned by the signed-upload-URL request.",
          "required": true
        },
        {
          "name": "key",
          "type": "string",
          "location": "body",
          "description": "The storage key returned by the signed-upload-URL request.",
          "required": true
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "The file name, including its extension.",
          "required": true
        }
      ]
    },
    {
      "name": "objects_records_create",
      "description": "Creates a record of the given object type. Pass `match_on` with the name of a unique field to upsert: when an incoming value matches exactly one existing record, that record is updated instead of a new one being created. Required: workspace, object, data. Optional: silent, match_on. Returns the new CreateRecordResponse.",
      "method": "POST",
      "path": "/workspaces/{workspace}/objects/{object}/records",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes",
          "wire_name": "data.attributes",
          "type": "object",
          "location": "body",
          "description": "Field values keyed by field name; available fields depend on the object’s schema. System fields (prefixed with `_`) cannot be set.",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "The resource type; matches the `object` path parameter.",
          "required": true
        },
        {
          "name": "match_on",
          "type": "string",
          "location": "body",
          "description": "Enables upsert mode: the name of a unique field (marked unique in the schema, e.g. 'domains' for companies, 'email_addresses' for people) to match incoming records against existing ones. When a value matches exactly one existing record, the attributes are merged into it and its ID is returned instead of inserting a new row; a value that matches multiple existing records errors. Scalar unique fields match exactly; collection fields match case-insensitively. Non-unique fields are rejected."
        }
      ]
    },
    {
      "name": "objects_records_create-bulk",
      "description": "Creates multiple records of the given object type in one request. The request is validated as a whole: if any record fails validation, none are created. Pass `match_on` with the name of a unique field to upsert matching records instead of creating duplicates. Required: workspace, object, data. Optional: silent, match_on. Returns array of RecordsCreateBulkItem.",
      "method": "POST",
      "path": "/workspaces/{workspace}/objects/{object}/records/bulk",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data",
          "type": "array",
          "location": "body",
          "description": "The resources to create.",
          "required": true
        },
        {
          "name": "match_on",
          "type": "string",
          "location": "body",
          "description": "Enables upsert mode: the name of a unique field (marked unique in the schema, e.g. 'domains' for companies, 'email_addresses' for people) to match incoming records against existing ones. When a value matches exactly one existing record, the attributes are merged into it and its ID is returned instead of inserting a new row; a value that matches multiple existing records errors. Scalar unique fields match exactly; collection fields match case-insensitively. Non-unique fields are rejected."
        }
      ]
    },
    {
      "name": "objects_records_create-grant",
      "description": "Shares a single record of a shareable object (`list`, `meeting`, or `message`) with a user or the whole workspace at a given access level. Only the record’s owner or a workspace permission admin (lists), the record’s owner (messages), or a participant (meetings) can create grants. Requires a user-backed API key; workspace-actor API keys are not supported. Required: workspace, object (default: list), id, data. Optional: silent. Returns the new ObjectRecordAccessResponse.",
      "method": "POST",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/access",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The shareable object the record belongs to: `list`, `meeting`, or `message`.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the shared record.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes",
          "wire_name": "data.attributes",
          "type": "string",
          "location": "body",
          "description": "Attributes",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "Type",
          "required": true
        }
      ]
    },
    {
      "name": "objects_records_create-grants-bulk",
      "description": "Shares a single record with multiple users and/or the whole workspace in one request, up to 1000 grants per call. Only the record’s owner or a workspace permission admin (lists), the record’s owner (messages), or a participant (meetings) can create grants. Requires a user-backed API key; workspace-actor API keys are not supported. Required: workspace, object (default: list), id, data. Optional: silent. Returns array of RecordsCreateGrantsBulkItem.",
      "method": "POST",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/access/bulk",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The shareable object the record belongs to: `list`, `meeting`, or `message`.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the shared record.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data",
          "type": "array",
          "location": "body",
          "description": "Data",
          "required": true
        }
      ]
    },
    {
      "name": "objects_records_delete",
      "description": "Permanently deletes the records listed in `items`. This cannot be undone. Required: workspace, object, items. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/objects/{object}/records",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "items",
          "type": "string_csv_array",
          "location": "body",
          "description": "The IDs of the records to delete.",
          "required": true
        }
      ]
    },
    {
      "name": "objects_records_delete-attachment",
      "description": "Removes an attachment from the record and returns the updated record. This cannot be undone. Required: workspace, object, id, attachmentId. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/attachments/{attachmentId}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "attachmentId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the attachment.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "objects_records_delete-relationship",
      "description": "Removes the links to the records in `data` from the given relationship. The records themselves are not deleted. Required: workspace, object, id, relationship, data. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/relationships/{relationship}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "relationship",
          "type": "string",
          "location": "path",
          "description": "The relationship field name as defined in the object’s schema, e.g. `people` or `companies`.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data",
          "type": "array",
          "location": "body",
          "description": "The related records to link.",
          "required": true
        }
      ]
    },
    {
      "name": "objects_records_delete-workspaces",
      "description": "Permanently deletes a record. This cannot be undone. Required: workspace, object, id. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "objects_records_get",
      "description": "Returns a single record as a JSON:API resource. Use `include` to embed related records under `relationships`. Required: workspace, object, id. Optional: include.",
      "method": "GET",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "include",
          "type": "string",
          "location": "query",
          "description": "Comma-separated list of relationship names to resolve and embed under `relationships`, e.g. `include=companies,deals`."
        }
      ]
    },
    {
      "name": "objects_records_get-activities",
      "description": "Returns the activity feed for a record: a paginated, reverse-chronological list of change events (field updates, comments, relationship changes) grouped into activities. Each activity bundles one or more related events. Required: workspace, object, record. Optional: page (default: map[limit:500 offset:0]), sortOrder.",
      "method": "GET",
      "path": "/workspaces/{workspace}/objects/{object}/records/{record}/activities",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "record",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "page",
          "type": "object",
          "location": "query",
          "description": "Offset pagination, e.g. `page[offset]=0\u0026page[limit]=100`."
        },
        {
          "name": "sortOrder",
          "type": "object",
          "location": "query",
          "description": "Sort order, e.g. `sortOrder[column]=_created_at\u0026sortOrder[dir]=DESC`."
        }
      ]
    },
    {
      "name": "objects_records_get-attachment",
      "description": "Returns a short-lived signed URL for downloading the attachment contents. Required: workspace, object, id, attachmentId.",
      "method": "GET",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/attachments/{attachmentId}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "attachmentId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the attachment.",
          "required": true
        }
      ]
    },
    {
      "name": "objects_records_get-grants",
      "description": "Returns every access grant on a single record of a shareable object (`list`, `meeting`, or `message`) as a JSON:API collection. A grant shares the record with a single workspace member or the whole workspace at a given access level. Visible to anyone who can already read the record. Requires a user-backed API key; workspace-actor API keys are not supported. Required: workspace, object (default: list), id.",
      "method": "GET",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/access",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The shareable object the record belongs to: `list`, `meeting`, or `message`.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the shared record.",
          "required": true
        }
      ]
    },
    {
      "name": "objects_records_get-relationships",
      "description": "Returns the records linked to a record through the given relationship, e.g. the deals related to a person. Required: workspace, object, id, relationship. Optional: page (default: map[limit:500 offset:0]), sortOrder, include.",
      "method": "GET",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/relationships/{relationship}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "relationship",
          "type": "string",
          "location": "path",
          "description": "The relationship field name as defined in the object’s schema, e.g. `people` or `companies`.",
          "required": true
        },
        {
          "name": "page",
          "type": "object",
          "location": "query",
          "description": "Offset pagination, e.g. `page[offset]=0\u0026page[limit]=100`."
        },
        {
          "name": "sortOrder",
          "type": "object",
          "location": "query",
          "description": "Sort order, e.g. `sortOrder[column]=_created_at\u0026sortOrder[dir]=DESC`."
        },
        {
          "name": "include",
          "type": "string",
          "location": "query",
          "description": "Comma-separated list of nested relationship paths to resolve, relative to the parent record, e.g. `include=companies.deals`."
        }
      ]
    },
    {
      "name": "objects_records_get-signed-url-for-attachment-upload",
      "description": "Returns a pre-signed URL for uploading a file. Upload the file contents to `signedUrl` via HTTP PUT, then attach it to the record by POSTing the returned `key` and `attachmentId` to the attachments endpoint. Required: workspace, object, id, name. Optional: silent.",
      "method": "PUT",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/attachments",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "The file name, including its extension.",
          "required": true
        }
      ]
    },
    {
      "name": "objects_records_list-attachments",
      "description": "Returns the attachments on a record, both user uploads and files extracted from emails. Omit `page` to return all attachments. Required: workspace, object, id. Optional: page.",
      "method": "GET",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/attachments",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "page",
          "type": "object",
          "location": "query",
          "description": "Offset pagination, e.g. `page[offset]=0\u0026page[limit]=100`. Omit to return all attachments."
        }
      ]
    },
    {
      "name": "objects_records_merge",
      "description": "Merges the source records listed in `sources` into the target record. Field values and relationships are combined onto the target, and the source records are deleted. This cannot be undone. Required: workspace, object, record, data. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/objects/{object}/records/{record}/merges",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "record",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes-sources",
          "wire_name": "data.attributes.sources",
          "type": "string_csv_array",
          "location": "body",
          "description": "The IDs of the source records to merge into the target record; source records are deleted after the merge.",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "The resource type; matches the `object` path parameter.",
          "required": true
        }
      ]
    },
    {
      "name": "objects_records_revoke-grant",
      "description": "Permanently removes an access grant from a record. Only the record’s owner or a workspace permission admin (lists), the record’s owner (messages), or a participant (meetings) can manage grants. Returns 202 Accepted with an empty body. Requires a user-backed API key; workspace-actor API keys are not supported. Required: workspace, object (default: list), id, grantId. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/access/{grantId}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The shareable object the record belongs to: `list`, `meeting`, or `message`.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the shared record.",
          "required": true
        },
        {
          "name": "grantId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the grant.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "objects_records_update",
      "description": "Applies partial updates to multiple records in one request. Each entry names the record by `id` and lists only the fields to change. Required: workspace, object, data. Optional: silent, meta. Returns the updated RecordResponse.",
      "method": "PATCH",
      "path": "/workspaces/{workspace}/objects/{object}/records",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data",
          "type": "array",
          "location": "body",
          "description": "The per-resource changes to apply.",
          "required": true
        },
        {
          "name": "meta",
          "type": "object",
          "location": "body",
          "description": "Per-field write strategies, keyed by field name; controls how array, collection, and object values are merged instead of replaced."
        }
      ]
    },
    {
      "name": "objects_records_update-grant",
      "description": "Changes the access level of an existing grant on a record. Only the record’s owner or a workspace permission admin (lists), the record’s owner (messages), or a participant (meetings) can manage grants. Requires a user-backed API key; workspace-actor API keys are not supported. Required: workspace, object (default: list), id, grantId, data. Optional: silent. Returns the updated ObjectRecordAccessResponse.",
      "method": "PATCH",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/access/{grantId}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The shareable object the record belongs to: `list`, `meeting`, or `message`.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the shared record.",
          "required": true
        },
        {
          "name": "grantId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the grant.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes-access-level",
          "wire_name": "data.attributes.access_level",
          "type": "string",
          "location": "body",
          "description": "The new access level for the grant: `view` for read-only access, `edit` for read-write access.",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "Type",
          "required": true
        }
      ]
    },
    {
      "name": "objects_records_update-relationship",
      "description": "Replaces the full set of records linked through the given relationship with the records in `data`. Links not listed are removed; this is not additive. Required: workspace, object, id, relationship, data. Optional: silent. Partial update.",
      "method": "PATCH",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}/relationships/{relationship}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "relationship",
          "type": "string",
          "location": "path",
          "description": "The relationship field name as defined in the object’s schema, e.g. `people` or `companies`.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data",
          "type": "array",
          "location": "body",
          "description": "The related records to link.",
          "required": true
        }
      ]
    },
    {
      "name": "objects_records_update-workspaces",
      "description": "Applies a partial update to a record: only the fields present in `attributes` are changed, all other fields keep their current values. Required: workspace, object, id, data. Optional: silent, meta. Returns the updated RecordResponse.",
      "method": "PATCH",
      "path": "/workspaces/{workspace}/objects/{object}/records/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes",
          "wire_name": "data.attributes",
          "type": "object",
          "location": "body",
          "description": "The field values to change, keyed by field name; omitted fields keep their current values. System fields (prefixed with `_`) cannot be set.",
          "required": true
        },
        {
          "name": "data-id",
          "wire_name": "data.id",
          "type": "string",
          "location": "body",
          "description": "The unique ID of the resource to update.",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "The resource type; matches the `object` path parameter.",
          "required": true
        },
        {
          "name": "meta",
          "type": "object",
          "location": "body",
          "description": "Per-field write strategies, keyed by field name; controls how array, collection, and object values are merged instead of replaced."
        }
      ]
    },
    {
      "name": "objects_resources_create",
      "description": "Creates a resource of the given object type. The `type` in the body must match the `object` path parameter. Returns the created resource as a JSON:API document. Required: workspace, object, data. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/objects/{object}/resources",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes",
          "wire_name": "data.attributes",
          "type": "object",
          "location": "body",
          "description": "Field values keyed by field name; available fields depend on the object’s schema. System fields (prefixed with `_`) cannot be set.",
          "required": true
        },
        {
          "name": "data-relationships",
          "wire_name": "data.relationships",
          "type": "object",
          "location": "body",
          "description": "Related resources to link on creation, keyed by relationship name."
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "The resource type; matches the `object` path parameter.",
          "required": true
        }
      ]
    },
    {
      "name": "objects_resources_get",
      "description": "Returns a paginated JSON:API collection of an object’s resources. Use `include` to embed related resources and `filter` to narrow results by field — shorthand operators are parsed from the value (`filter[status]=active,pending`, `filter[amount]=\u003e100`, `filter[name]=*Smith*`, `filter[assigned]=null`), or name them explicitly (`filter[start][Is on or after]=2025-01-01`). To filter on a relationship field, include that relationship: `filter[author.name]=John\u0026include=author`. Required: workspace, object. Optional: page (default: map[limit:500 offset:0]), sortOrder, include (plus 1 more).",
      "method": "GET",
      "path": "/workspaces/{workspace}/objects/{object}/resources",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "page",
          "type": "object",
          "location": "query",
          "description": "Offset pagination, e.g. `page[offset]=0\u0026page[limit]=100`."
        },
        {
          "name": "sortOrder",
          "type": "object",
          "location": "query",
          "description": "Sort order, e.g. `sortOrder[column]=_created_at\u0026sortOrder[dir]=DESC`."
        },
        {
          "name": "include",
          "type": "string",
          "location": "query",
          "description": "Comma-separated relationship names to embed under `relationships` and return in `included`, e."
        },
        {
          "name": "filter",
          "type": "object",
          "location": "query",
          "description": "Nested filter object keyed by field name, e.g. `filter[status]=active` or `filter[amount][Greater than]=100`. See the endpoint description for the full operator syntax."
        }
      ]
    },
    {
      "name": "objects_resources_get-workspaces",
      "description": "Returns a single resource as a JSON:API document. Use `include` to embed related resources under `relationships` and return them in `included`. Required: workspace, object, id. Optional: include.",
      "method": "GET",
      "path": "/workspaces/{workspace}/objects/{object}/resources/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the record.",
          "required": true
        },
        {
          "name": "include",
          "type": "string",
          "location": "query",
          "description": "Comma-separated relationship names to embed under `relationships` and return in `included`, e."
        }
      ]
    },
    {
      "name": "schemas_create-custom-object",
      "description": "Creates a new custom object type in the workspace and returns its generated JSON Schema. The name is normalized to a `c_`-prefixed identifier (for example 'Sales Order' becomes `c_sales_order`). Required: workspace, name, plural. Optional: silent, backgroundColor, description (plus 2 more).",
      "method": "POST",
      "path": "/workspaces/{workspace}/schemas/objects",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "backgroundColor",
          "type": "string",
          "location": "body",
          "description": "Background color for the object avatar. A Tailwind color name rendered at the 500 shade (xClarifyPresentation.backgroundColor)."
        },
        {
          "name": "description",
          "type": "string",
          "location": "body",
          "description": "The description is stored as AI context for this object type"
        },
        {
          "name": "icon",
          "type": "string",
          "location": "body",
          "description": "Icon name for the object avatar. Must be one of the curated avatar icon names (xClarifyPresentation.icon)."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "The name will be normalized to a c_ prefixed identifier (e.g., 'Sales Order' becomes 'c_sales_order')",
          "required": true
        },
        {
          "name": "plural",
          "type": "string",
          "location": "body",
          "description": "The plural name will be used to display the object type in the UI",
          "required": true
        },
        {
          "name": "properties",
          "type": "object",
          "location": "body",
          "description": "Initial fields for the object, keyed by field name and valued by JSON Schema. Built-in system fields are always added and cannot be overridden."
        }
      ]
    },
    {
      "name": "schemas_delete-custom-object",
      "description": "Deletes a custom object type and all of its records. The deletion runs asynchronously: the response returns a task you can poll to track progress. Only custom (`c_*`) objects can be deleted. This cannot be undone. Required: workspace, object. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/schemas/objects/{object}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The custom object type to delete (a `c_*` identifier).",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "schemas_get",
      "description": "Returns every object schema in the workspace as a cursor-paginated list of JSON:API resources. Each resource holds the full JSON Schema for one object type, including its fields, relationships, and presentation metadata. Required: workspace.",
      "method": "GET",
      "path": "/workspaces/{workspace}/schemas",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        }
      ]
    },
    {
      "name": "schemas_patch-enum-field-values",
      "description": "Adds or removes options on enum (single- and multi-select) fields for one object type. The request carries exactly one item in `data`; each field in `meta` names an `append` or `remove` operation. To modify several object types, send separate requests. Required: workspace, data. Optional: silent. Returns array of SchemasPatchEnumFieldValuesItem.",
      "method": "PATCH",
      "path": "/workspaces/{workspace}/schemas",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data",
          "type": "array",
          "location": "body",
          "description": "A single-element array holding the enum-field patch to apply.",
          "required": true
        }
      ]
    },
    {
      "name": "schemas_update-entity",
      "description": "Replaces the full JSON Schema for an object type. The request body is the complete schema document, and its `$id` must match the object in the path. The change is applied asynchronously: the response returns a task you can poll to track progress. Required: workspace, object. Optional: silent.",
      "method": "PUT",
      "path": "/workspaces/{workspace}/schemas/objects/{object}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "object",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "schemas_activities_get-schema",
      "description": "Returns the history of schema changes for an object type as a paginated list of activities. Each activity groups the change events for one modification, such as adding or editing a field. Required: workspace, entity. Optional: page (default: map[limit:500 offset:0]), sortOrder.",
      "method": "GET",
      "path": "/workspaces/{workspace}/schemas/{entity}/activities",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "entity",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "page",
          "type": "object",
          "location": "query",
          "description": "Offset pagination, e.g. `page[offset]=0\u0026page[limit]=100`."
        },
        {
          "name": "sortOrder",
          "type": "object",
          "location": "query",
          "description": "Sort order, e.g. `sortOrder[column]=_created_at\u0026sortOrder[dir]=DESC`."
        }
      ]
    },
    {
      "name": "schemas_fields-order_update-schema-property-order",
      "description": "Sets the display order of an object type’s fields. The `order` array lists every field name in the desired order. The change is applied asynchronously and the response body is empty. Required: workspace, entity, data. Optional: silent. Partial update.",
      "method": "PATCH",
      "path": "/workspaces/{workspace}/schemas/{entity}/fields-order",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "entity",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes-order",
          "wire_name": "data.attributes.order",
          "type": "string_csv_array",
          "location": "body",
          "description": "The field names in the desired display order.",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "The JSON:API resource type.",
          "required": true
        }
      ]
    },
    {
      "name": "schemas_fields-visibility_update-schema-property-visibility",
      "description": "Shows or hides an object type’s fields on the record detail view. The `hiddenInDetails` map keys each field name to a boolean, where `true` hides the field. The change is applied asynchronously and the response body is empty. Required: workspace, entity, data. Optional: silent. Partial update.",
      "method": "PATCH",
      "path": "/workspaces/{workspace}/schemas/{entity}/fields-visibility",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "entity",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes-hidden-in-details",
          "wire_name": "data.attributes.hiddenInDetails",
          "type": "object",
          "location": "body",
          "description": "Field visibility on the detail view, keyed by field name. `true` hides the field.",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "The JSON:API resource type.",
          "required": true
        }
      ]
    },
    {
      "name": "schemas_properties_create-schema",
      "description": "Adds one or more non-relationship fields to an object type. Each key in `attributes` is a new field name mapped to its JSON Schema, and every type except `boolean` must include `'null'` — declare `['string', 'null']` rather than `'string'`, because the column behind such a field is nullable and a non-nullable declaration would reject its own stored values. Fields that already exist are rejected — this endpoint never overwrites. The change is applied asynchronously and the response body is empty. Required: workspace, entity, data. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/schemas/{entity}/properties",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "entity",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes",
          "wire_name": "data.attributes",
          "type": "object",
          "location": "body",
          "description": "New fields to add, keyed by field name and valued by JSON Schema.",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "The JSON:API resource type.",
          "required": true
        }
      ]
    },
    {
      "name": "schemas_relationships_create-schema-properties",
      "description": "Creates relationship fields between object types. The `attributes` map is keyed first by object type, then by field name, and both sides of each relationship must be supplied together so the pairing is consistent. On `one-to-many` and `many-to-many` sides, the field must declare `'oneOf': [{ '$ref': 'https://getclarify.ai/schemas/core/collectionOfIds' }, { 'type': 'null' }]` — the exact `$ref` string is required. The change is applied asynchronously and the response body is empty. Required: workspace, entity, data. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/schemas/{entity}/relationships",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "entity",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes",
          "wire_name": "data.attributes",
          "type": "object",
          "location": "body",
          "description": "Relationship fields keyed first by object type, then by field name. Both sides of each relationship must be supplied together.",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "The JSON:API resource type.",
          "required": true
        }
      ]
    },
    {
      "name": "schemas_relationships_delete-schema-property",
      "description": "Deletes a relationship field from an object type, along with its counterpart field on the related object. The deletion runs asynchronously: the response returns a task you can poll to track progress. Deleting an already-removed relationship is a no-op that still returns a task. Required: workspace, entity, fieldName. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/schemas/{entity}/relationships/{fieldName}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "entity",
          "type": "string",
          "location": "path",
          "description": "The object type: a built-in entity (`person`, `company`, `deal`, …) or a custom object (`c_*`).",
          "required": true
        },
        {
          "name": "fieldName",
          "type": "string",
          "location": "path",
          "description": "The name of the relationship field to delete.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "settings_delete-workspace",
      "description": "Removes the stored value of a workspace setting so it falls back to its default. Read-only settings are rejected, and some settings require admin permissions. Required: workspace, key. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/settings",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "key",
          "type": "string",
          "location": "body",
          "description": "The setting key, e.g. `orgDescription`. Read-only settings cannot be mutated.",
          "required": true
        }
      ]
    },
    {
      "name": "settings_read-all-workspace",
      "description": "Returns every workspace setting keyed by name, with defaults applied for settings the workspace has not overridden. Required: workspace.",
      "method": "GET",
      "path": "/workspaces/{workspace}/settings",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        }
      ]
    },
    {
      "name": "settings_read-workspace",
      "description": "Returns the value of a single workspace setting; the default value when the workspace has not overridden it. Required: workspace, key (default: orgDescription).",
      "method": "GET",
      "path": "/workspaces/{workspace}/settings/{key}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "key",
          "type": "string",
          "location": "path",
          "description": "The setting key, e.g. `orgDescription`. Read-only settings cannot be mutated.",
          "required": true
        }
      ]
    },
    {
      "name": "settings_write-workspace",
      "description": "Sets the value of a workspace setting by key. Read-only settings are rejected, and some settings require admin permissions. Required: workspace, key. Optional: silent, value.",
      "method": "POST",
      "path": "/workspaces/{workspace}/settings",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "key",
          "type": "string",
          "location": "body",
          "description": "The setting key, e.g. `orgDescription`. Read-only settings cannot be mutated.",
          "required": true
        },
        {
          "name": "value",
          "type": "string",
          "location": "body",
          "description": "The setting value; its type depends on the setting key."
        }
      ]
    },
    {
      "name": "users_get",
      "description": "Returns the workspace’s users as a paginated JSON:API list. Each user includes their roles. Required: workspace. Optional: page (default: map[limit:500 offset:0]), sortOrder.",
      "method": "GET",
      "path": "/workspaces/{workspace}/users",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "page",
          "type": "object",
          "location": "query",
          "description": "Offset pagination, e.g. `page[offset]=0\u0026page[limit]=100`."
        },
        {
          "name": "sortOrder",
          "type": "object",
          "location": "query",
          "description": "Sort order, e.g. `sortOrder[column]=_created_at\u0026sortOrder[dir]=DESC`."
        }
      ]
    },
    {
      "name": "users_get-workspaces",
      "description": "Returns a single workspace user as a JSON:API resource, including their roles and the time they were last active. Required: workspace, userId.",
      "method": "GET",
      "path": "/workspaces/{workspace}/users/{userId}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "userId",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the user.",
          "required": true
        }
      ]
    },
    {
      "name": "workflows_create",
      "description": "Creates a workflow from a trigger and a set of blocks. Create it disabled and enable it once the automation graph is complete. Returns the created workflow. Required: workspace, data. Optional: silent.",
      "method": "POST",
      "path": "/workspaces/{workspace}/workflows",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes-blocks",
          "wire_name": "data.attributes.blocks",
          "type": "object",
          "location": "body",
          "description": "The workflow’s blocks keyed by block id.",
          "required": true
        },
        {
          "name": "data-attributes-description",
          "wire_name": "data.attributes.description",
          "type": "string",
          "location": "body",
          "description": "A human-readable description of the workflow.",
          "required": true
        },
        {
          "name": "data-attributes-enabled",
          "wire_name": "data.attributes.enabled",
          "type": "bool",
          "location": "body",
          "description": "Whether the workflow is active and processing triggers.",
          "required": true
        },
        {
          "name": "data-attributes-exits",
          "wire_name": "data.attributes.exits",
          "type": "object",
          "location": "body",
          "description": "Exit blocks keyed by block id, run when a record leaves the workflow."
        },
        {
          "name": "data-attributes-name",
          "wire_name": "data.attributes.name",
          "type": "string",
          "location": "body",
          "description": "The workflow’s display name.",
          "required": true
        },
        {
          "name": "data-attributes-type",
          "wire_name": "data.attributes.type",
          "type": "string",
          "location": "body",
          "description": "The workflow type: `sequence` for an email sequence (campaign) or `workflow` for a general automation.",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "The resource type; matches the `object` path parameter.",
          "required": true
        }
      ]
    },
    {
      "name": "workflows_delete",
      "description": "Deletes a workflow. The deletion is applied asynchronously and the response body is empty. This cannot be undone. Required: workspace, id. Optional: silent. Destructive.",
      "method": "DELETE",
      "path": "/workspaces/{workspace}/workflows/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the workflow.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        }
      ]
    },
    {
      "name": "workflows_get",
      "description": "Returns the workspace’s workflows as an offset-paginated list of JSON:API resources. Use the `type` filter to return only sequences (campaigns) or general workflows. Required: workspace. Optional: page (default: map[limit:500 offset:0]), sortOrder, filter.",
      "method": "GET",
      "path": "/workspaces/{workspace}/workflows",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "page",
          "type": "object",
          "location": "query",
          "description": "Offset pagination, e.g. `page[offset]=0\u0026page[limit]=100`."
        },
        {
          "name": "sortOrder",
          "type": "object",
          "location": "query",
          "description": "Sort order, e.g. `sortOrder[column]=_created_at\u0026sortOrder[dir]=DESC`."
        },
        {
          "name": "filter",
          "type": "object",
          "location": "query",
          "description": "Filters applied to the workflow list."
        }
      ]
    },
    {
      "name": "workflows_get-workspaces",
      "description": "Returns a single workflow as a JSON:API resource, including its trigger and the blocks that make up its automation graph. Required: workspace, id.",
      "method": "GET",
      "path": "/workspaces/{workspace}/workflows/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the workflow.",
          "required": true
        }
      ]
    },
    {
      "name": "workflows_update",
      "description": "Applies a partial update to a workflow: only the fields present in `attributes` are changed. Use it to rename, enable or disable, or edit the trigger and blocks. Returns the updated workflow. Required: workspace, id, data. Optional: silent, meta.",
      "method": "PATCH",
      "path": "/workspaces/{workspace}/workflows/{id}",
      "params": [
        {
          "name": "workspace",
          "type": "string",
          "location": "path",
          "description": "The workspace slug — the `{slug}` segment of your Clarify workspace URL.",
          "required": true
        },
        {
          "name": "id",
          "type": "string",
          "location": "path",
          "description": "The unique ID of the workflow.",
          "required": true
        },
        {
          "name": "silent",
          "type": "bool",
          "location": "query",
          "description": "When true, suppresses in-app and Slack notifications for this mutation."
        },
        {
          "name": "data-attributes-blocks",
          "wire_name": "data.attributes.blocks",
          "type": "object",
          "location": "body",
          "description": "The workflow’s blocks keyed by block id."
        },
        {
          "name": "data-attributes-description",
          "wire_name": "data.attributes.description",
          "type": "string",
          "location": "body",
          "description": "A human-readable description of the workflow."
        },
        {
          "name": "data-attributes-enabled",
          "wire_name": "data.attributes.enabled",
          "type": "bool",
          "location": "body",
          "description": "Whether the workflow is active and processing triggers."
        },
        {
          "name": "data-attributes-name",
          "wire_name": "data.attributes.name",
          "type": "string",
          "location": "body",
          "description": "The workflow’s display name."
        },
        {
          "name": "data-attributes-published-at",
          "wire_name": "data.attributes.published_at",
          "type": "string",
          "location": "body",
          "description": "The time the workflow was published, or null to unpublish it."
        },
        {
          "name": "data-id",
          "wire_name": "data.id",
          "type": "string",
          "location": "body",
          "description": "The unique ID of the resource to update.",
          "required": true
        },
        {
          "name": "data-type",
          "wire_name": "data.type",
          "type": "string",
          "location": "body",
          "description": "The resource type; matches the `object` path parameter.",
          "required": true
        },
        {
          "name": "meta",
          "type": "string",
          "location": "body",
          "description": "Optional JSON:API meta member carrying request-specific options; not accepted for this resource."
        }
      ]
    }
  ],
  "novel_features": [
    {
      "name": "Meeting prep pack",
      "command": "prep",
      "description": "One command before a call: the meeting's attendees, their company, open deals, and transcript excerpts from past meetings with that company."
    },
    {
      "name": "Morning briefing",
      "command": "brief",
      "description": "Start-of-day overview: today's meetings joined to their companies, open deals, and yesterday's record activity, on one screen."
    },
    {
      "name": "Follow-up gaps",
      "command": "followup",
      "description": "The dropped-ball list: meetings with no subsequent activity, comment, or task on the linked deal or company."
    },
    {
      "name": "Stale-deal report",
      "command": "stale",
      "description": "Open deals with no activity in N days, grouped by pipeline stage."
    },
    {
      "name": "Record dossier",
      "command": "dossier",
      "description": "A complete background bundle on any record: fields, relationships, activities, comments, and related meetings with transcript references, in one compact payload."
    },
    {
      "name": "Pipeline velocity",
      "command": "velocity",
      "description": "Per-stage dwell time and stage-to-stage conversion counts, accrued from a local stage-history table across repeated runs (the first run reports the current stage distribution)."
    },
    {
      "name": "Duplicate finder",
      "command": "dupes",
      "description": "Finds likely duplicate people or companies by shared email, domain, or normalized name, and prints ready-to-run merge commands."
    }
  ]
}
