// Copyright 2026 Isaac Marks and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newObjectsListsUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagSilent bool
	var bodyDescription string
	var bodyEmoji string
	var bodyLayout string
	var bodyOptionsColumnMetrics string
	var bodyOptionsColumnWidths string
	var bodyOptionsHiddenBoardColumns string
	var bodyOptionsHiddenPrimaryCell bool
	var bodyOptionsSegmentedByInBoard string
	var bodyOptionsShowFieldNamesInBoard bool
	var bodyQueryNlq string
	var bodyQuerySql string
	var bodyQueryVersion float64
	var bodyRank float64
	var bodyTitle string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "update <workspace> <object> <list>",
		Short: "Applies a partial update to a list: only the fields present in the body are changed.",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  clarify-pp-cli objects lists update example-value example-value example-value",
		Annotations: map[string]string{"pp:endpoint": "lists.update", "pp:method": "PATCH", "pp:path": "/workspaces/{workspace}/objects/{object}/lists/{list}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <workspace> <object> <list>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <workspace> <object> <list>"))
			}
			if !stdinBody {
				if (cmd.Flags().Changed("query-nlq") || bodyQueryNlq != "") || (cmd.Flags().Changed("query-sql") || bodyQuerySql != "") || (cmd.Flags().Changed("query-version") || bodyQueryVersion != 0.0) {
					if !cmd.Flags().Changed("query-sql") && !flags.dryRun {
						return fmt.Errorf("required flag \"%s\" not set", "query-sql")
					}
				}
			}
			path := "/workspaces/{workspace}/objects/{object}/lists/{list}"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("workspace is required\nUsage: %s <%s>", cmd.CommandPath(), "workspace"))
			}
			path = replacePathParam(path, "workspace", args[0])
			if len(args) < 2 || args[1] == "" {
				return usageErr(fmt.Errorf("object is required\nUsage: %s <%s>", cmd.CommandPath(), "object"))
			}
			path = replacePathParam(path, "object", args[1])
			if len(args) < 3 || args[2] == "" {
				return usageErr(fmt.Errorf("list is required\nUsage: %s <%s>", cmd.CommandPath(), "list"))
			}
			path = replacePathParam(path, "list", args[2])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if cmd.Flags().Changed("silent") || flagSilent != false {
				params["silent"] = formatCLIParamValue(flagSilent)
			}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("description") || bodyDescription != "" {
					bodyMap["description"] = bodyDescription
				}
				if cmd.Flags().Changed("emoji") || bodyEmoji != "" {
					bodyMap["emoji"] = bodyEmoji
				}
				if cmd.Flags().Changed("layout") || bodyLayout != "" {
					bodyMap["layout"] = bodyLayout
				}
				{
					nestedOptions := map[string]any{}
					if cmd.Flags().Changed("options-column-metrics") || bodyOptionsColumnMetrics != "" {
						var parsedOptionsColumnMetrics any
						if err := json.Unmarshal([]byte(bodyOptionsColumnMetrics), &parsedOptionsColumnMetrics); err != nil {
							return fmt.Errorf("parsing --options-column-metrics JSON: %w", err)
						}
						asMap, ok := parsedOptionsColumnMetrics.(map[string]any)
						if !ok {
							return fmt.Errorf("--options-column-metrics must be a JSON object, got JSON %T", parsedOptionsColumnMetrics)
						}
						nestedOptions["columnMetrics"] = asMap
					}
					if cmd.Flags().Changed("options-column-widths") || bodyOptionsColumnWidths != "" {
						var parsedOptionsColumnWidths any
						if err := json.Unmarshal([]byte(bodyOptionsColumnWidths), &parsedOptionsColumnWidths); err != nil {
							return fmt.Errorf("parsing --options-column-widths JSON: %w", err)
						}
						asMap, ok := parsedOptionsColumnWidths.(map[string]any)
						if !ok {
							return fmt.Errorf("--options-column-widths must be a JSON object, got JSON %T", parsedOptionsColumnWidths)
						}
						nestedOptions["columnWidths"] = asMap
					}
					if cmd.Flags().Changed("options-hidden-board-columns") || bodyOptionsHiddenBoardColumns != "" {
						var parsedOptionsHiddenBoardColumns any
						if err := json.Unmarshal([]byte(bodyOptionsHiddenBoardColumns), &parsedOptionsHiddenBoardColumns); err != nil {
							return fmt.Errorf("parsing --options-hidden-board-columns JSON: %w", err)
						}
						asMap, ok := parsedOptionsHiddenBoardColumns.(map[string]any)
						if !ok {
							return fmt.Errorf("--options-hidden-board-columns must be a JSON object, got JSON %T", parsedOptionsHiddenBoardColumns)
						}
						nestedOptions["hiddenBoardColumns"] = asMap
					}
					if cmd.Flags().Changed("options-hidden-primary-cell") {
						nestedOptions["hiddenPrimaryCell"] = bodyOptionsHiddenPrimaryCell
					}
					if cmd.Flags().Changed("options-segmented-by-in-board") || bodyOptionsSegmentedByInBoard != "" {
						nestedOptions["segmentedByInBoard"] = bodyOptionsSegmentedByInBoard
					}
					if cmd.Flags().Changed("options-show-field-names-in-board") {
						nestedOptions["showFieldNamesInBoard"] = bodyOptionsShowFieldNamesInBoard
					}
					if len(nestedOptions) > 0 {
						bodyMap["options"] = nestedOptions
					}
				}
				{
					nestedQuery := map[string]any{}
					if cmd.Flags().Changed("query-nlq") || bodyQueryNlq != "" {
						nestedQuery["nlq"] = bodyQueryNlq
					}
					if cmd.Flags().Changed("query-sql") || bodyQuerySql != "" {
						nestedQuery["sql"] = bodyQuerySql
					}
					if cmd.Flags().Changed("query-version") || bodyQueryVersion != 0.0 {
						nestedQuery["version"] = bodyQueryVersion
					}
					if len(nestedQuery) > 0 {
						bodyMap["query"] = nestedQuery
					}
				}
				if cmd.Flags().Changed("rank") || bodyRank != 0.0 {
					bodyMap["rank"] = bodyRank
				}
				if cmd.Flags().Changed("title") || bodyTitle != "" {
					bodyMap["title"] = bodyTitle
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "lists", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "lists", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "lists", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "lists", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "lists", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "lists",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"data": true})
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "lists", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "lists", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&flagSilent, "silent", false, "When true, suppresses in-app and Slack notifications for this mutation.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A short description of the list.")
	cmd.Flags().StringVar(&bodyEmoji, "emoji", "", "The emoji shown next to the list, or `null` if unset.")
	cmd.Flags().StringVar(&bodyLayout, "layout", "", "How the list is rendered in the app: `table` or `board`.")
	cmd.Flags().StringVar(&bodyOptionsColumnMetrics, "options-column-metrics", "", "The aggregation metric shown in the table footer for each column, keyed by field path (e.g. `sum`, `count`).")
	cmd.Flags().StringVar(&bodyOptionsColumnWidths, "options-column-widths", "", "Table column pixel widths, keyed by field path. Absent keys use the default width.")
	cmd.Flags().StringVar(&bodyOptionsHiddenBoardColumns, "options-hidden-board-columns", "", "Board columns hidden from view, keyed by segment field path")
	cmd.Flags().BoolVar(&bodyOptionsHiddenPrimaryCell, "options-hidden-primary-cell", false, "Whether the primary (first) cell is hidden on cards in board layout.")
	cmd.Flags().StringVar(&bodyOptionsSegmentedByInBoard, "options-segmented-by-in-board", "", "For board layout, the field path whose values group records into columns.")
	cmd.Flags().BoolVar(&bodyOptionsShowFieldNamesInBoard, "options-show-field-names-in-board", false, "Whether field names are shown on cards in board layout.")
	cmd.Flags().StringVar(&bodyQueryNlq, "query-nlq", "", "The original natural-language query, if the SQL was generated from one.")
	cmd.Flags().StringVar(&bodyQuerySql, "query-sql", "", "The SQL query that defines the list membership and ordering.")
	cmd.Flags().Float64Var(&bodyQueryVersion, "query-version", 0.0, "The view query schema version, used for migrations; an omitted value is treated as version 0 (the oldest).")
	cmd.Flags().Float64Var(&bodyRank, "rank", 0.0, "The sort position of the list relative to its siblings.")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "The display name of the list.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
