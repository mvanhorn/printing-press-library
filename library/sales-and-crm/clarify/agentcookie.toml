# agentcookie.toml: secrets-bus adoption manifest v2
# See docs/spec-agentcookie-secrets-bus-v2-adoption.md
schema_version = 2
name = "clarify-pp-cli"
display_name = "Clarify"
description = "Every Clarify API operation as a typed command, plus the morning briefing, meeting prep, and pipeline analytics the autonomous CRM knows about but cannot run."
project_kind = "cli"

[secrets.file]
path = "~/.config/clarify-pp-cli/config.toml"

[sync]
default = false

[sync.keys]
CLARIFY_API_KEY = true
