# Fix 50: nested collection and total drift

Classify one-sided report dimensions against their nearest indexed collection, including missing or incompatible nested containers. Preserve pagination total presence and validity across pages so zero, missing, and malformed later totals produce typed truncation errors.
