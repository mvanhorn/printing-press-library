// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newContactsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyCf string
	var bodyCity string
	var bodyCountry string
	var bodyCustomFields string
	var bodyDepartmentId int
	var bodyDescription string
	var bodyFacebook string
	var bodyMobile string
	var bodyOwnerId int
	var bodyPriority string
	var bodyStartTime string
	var bodyState string
	var bodyStatus string
	var bodyStreet string
	var bodySubject string
	var bodyTicketId int
	var bodyTitle string
	var bodyTwitter string
	var bodyZip int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update <contactId>",
		Short:       "This API updates details of an existing contact.",
		Example:     "  zoho-desk-pp-cli contacts update 42 --priority example-value",
		Annotations: map[string]string{"pp:endpoint": "contacts.update", "pp:method": "PATCH", "pp:path": "/api/v1/contacts/{contactId}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("department-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "department-id")
				}
				if !cmd.Flags().Changed("owner-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "owner-id")
				}
				if !cmd.Flags().Changed("priority") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "priority")
				}
				if !cmd.Flags().Changed("start-time") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "start-time")
				}
				if !cmd.Flags().Changed("status") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "status")
				}
				if !cmd.Flags().Changed("subject") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "subject")
				}
				if !cmd.Flags().Changed("ticket-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "ticket-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/contacts/{contactId}"
			path = replacePathParam(path, "contactId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCf != "" {
					var parsedCf any
					if err := json.Unmarshal([]byte(bodyCf), &parsedCf); err != nil {
						return fmt.Errorf("parsing --cf JSON: %w", err)
					}
					body["cf"] = parsedCf
				}
				if bodyCity != "" {
					body["city"] = bodyCity
				}
				if bodyCountry != "" {
					body["country"] = bodyCountry
				}
				if bodyCustomFields != "" {
					var parsedCustomFields any
					if err := json.Unmarshal([]byte(bodyCustomFields), &parsedCustomFields); err != nil {
						return fmt.Errorf("parsing --custom-fields JSON: %w", err)
					}
					body["customFields"] = parsedCustomFields
				}
				if bodyDepartmentId != 0 {
					body["departmentId"] = bodyDepartmentId
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyFacebook != "" {
					body["facebook"] = bodyFacebook
				}
				if bodyMobile != "" {
					body["mobile"] = bodyMobile
				}
				if bodyOwnerId != 0 {
					body["ownerId"] = bodyOwnerId
				}
				if bodyPriority != "" {
					body["priority"] = bodyPriority
				}
				if bodyStartTime != "" {
					body["startTime"] = bodyStartTime
				}
				if bodyState != "" {
					body["state"] = bodyState
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodyStreet != "" {
					body["street"] = bodyStreet
				}
				if bodySubject != "" {
					body["subject"] = bodySubject
				}
				if bodyTicketId != 0 {
					body["ticketId"] = bodyTicketId
				}
				if bodyTitle != "" {
					body["title"] = bodyTitle
				}
				if bodyTwitter != "" {
					body["twitter"] = bodyTwitter
				}
				if bodyZip != 0 {
					body["zip"] = bodyZip
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "contacts",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCf, "cf", "", "Cf")
	cmd.Flags().StringVar(&bodyCity, "city", "", "Contact's city of residence")
	cmd.Flags().StringVar(&bodyCountry, "country", "", "Contact's country of residence")
	cmd.Flags().StringVar(&bodyCustomFields, "custom-fields", "", "Custom fields")
	cmd.Flags().IntVar(&bodyDepartmentId, "department-id", 0, "ID of the department from which the activities must be fetched")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Contact description")
	cmd.Flags().StringVar(&bodyFacebook, "facebook", "", "Facebook ID of the contact")
	cmd.Flags().StringVar(&bodyMobile, "mobile", "", "Mobile number of the contact")
	cmd.Flags().IntVar(&bodyOwnerId, "owner-id", 0, "ID of the user to whom the call is assigned")
	cmd.Flags().StringVar(&bodyPriority, "priority", "", "Priority of the call")
	cmd.Flags().StringVar(&bodyStartTime, "start-time", "", "Time when the call started. The value of this key must be recorded in the ISO date format of yyyy-MM-ddThh:mm:ss.SSSZ.")
	cmd.Flags().StringVar(&bodyState, "state", "", "Contact's state of residence")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Completion status of the call")
	cmd.Flags().StringVar(&bodyStreet, "street", "", "Contact's address")
	cmd.Flags().StringVar(&bodySubject, "subject", "", "Subject of the call")
	cmd.Flags().IntVar(&bodyTicketId, "ticket-id", 0, "ID of the ticket associated with the call")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "Contact title")
	cmd.Flags().StringVar(&bodyTwitter, "twitter", "", "Twitter ID of the contact")
	cmd.Flags().IntVar(&bodyZip, "zip", 0, "Zip code")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
