// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAutomationFeatureCountPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagFeature string
	var flagDepartmentId string
	var flagModule string

	cmd := &cobra.Command{
		Use:         "automation-feature-count",
		Short:       "This API is used to Get Automation Feature Limits, For BusinessHours and HoldiayList, There are no mandatory params,...",
		Long:        "Shortcut for 'automation-feature-count get-used-count-for-automation-feature'. This API is used to Get Automation Feature Limits, For BusinessHours and HoldiayList, There are no mandatory params,...",
		Example:     "  zoho-desk-pp-cli automation-feature-count --feature example-value --department-id 42 --module example-value",
		Annotations: map[string]string{"pp:endpoint": "automation-feature-count.get-used-count-for-automation-feature", "pp:method": "GET", "pp:path": "/api/v1/automationFeatureCount", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("feature") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "feature")
			}
			if !cmd.Flags().Changed("department-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "department-id")
			}
			if !cmd.Flags().Changed("module") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "module")
			}
			if cmd.Flags().Changed("feature") {
				allowedFeature := []string{"workflows", "businesshours", "holidaylist", "blueprints", "Skills", "SkillType", "SupportPlans", "automationAlerts", "fieldUpdates", "automationTasks", "customFunctions"}
				validFeature := false
				for _, v := range allowedFeature {
					if flagFeature == v {
						validFeature = true
						break
					}
				}
				if !validFeature {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "feature", flagFeature, allowedFeature)
				}
			}
			if cmd.Flags().Changed("module") {
				allowedModule := []string{"tickets", "contacts", "contracts", "accounts", "tasks", "products", "timeEntry", "events", "calls", "customModule"}
				validModule := false
				for _, v := range allowedModule {
					if flagModule == v {
						validModule = true
						break
					}
				}
				if !validModule {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "module", flagModule, allowedModule)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/automationFeatureCount"
			params := map[string]string{}
			if flagFeature != "" {
				params["feature"] = fmt.Sprintf("%v", flagFeature)
			}
			if flagDepartmentId != "" {
				params["departmentId"] = fmt.Sprintf("%v", flagDepartmentId)
			}
			if flagModule != "" {
				params["module"] = fmt.Sprintf("%v", flagModule)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "automation-feature-count", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagFeature, "feature", "", "Feature, allowed values @businesshours@,@holidaylist@,@Skills@,@SkillType@,@workflows@,@blueprints@,@automationAlerts... (one of: workflows, businesshours, holidaylist, blueprints, Skills, SkillType, SupportPlans, automationAlerts, fieldUpdates, automationTasks, customFunctions)")
	cmd.Flags().StringVar(&flagDepartmentId, "department-id", "", "Department Id")
	cmd.Flags().StringVar(&flagModule, "module", "", "Module, allowed values @tickets@,@contacts@,@contracts@,@accounts@,@tasks@,@products@,@timeEntry@,@events@,@calls@,@c... (one of: tickets, contacts, contracts, accounts, tasks, products, timeEntry, events, calls, customModule)")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
