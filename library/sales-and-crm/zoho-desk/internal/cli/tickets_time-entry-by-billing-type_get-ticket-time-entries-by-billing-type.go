// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTicketsTimeEntryByBillingTypeGetTicketTimeEntriesByBillingTypeCmd(flags *rootFlags) *cobra.Command {
	var flagInclude string
	var flagBillingType string

	cmd := &cobra.Command{
		Use:         "get-ticket-time-entries-by-billing-type <ticketId>",
		Aliases:     []string{"get"},
		Short:       "This API fetches time entries for a ticket created after recently modified time of the given billing type from your...",
		Example:     "  zoho-desk-pp-cli tickets time-entry-by-billing-type get-ticket-time-entries-by-billing-type 42 --include example-value --billing-type example-value",
		Annotations: map[string]string{"pp:endpoint": "time-entry-by-billing-type.get-ticket-time-entries-by-billing-type", "pp:method": "GET", "pp:path": "/api/v1/tickets/{ticketId}/timeEntryByBillingType", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("include") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "include")
			}
			if !cmd.Flags().Changed("billing-type") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "billing-type")
			}
			if cmd.Flags().Changed("include") {
				allowedInclude := []string{"owner"}
				validInclude := false
				for _, v := range allowedInclude {
					if flagInclude == v {
						validInclude = true
						break
					}
				}
				if !validInclude {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "include", flagInclude, allowedInclude)
				}
			}
			if cmd.Flags().Changed("billing-type") {
				allowedBillingType := []string{"FIXED_COST_FOR_TICKETS", "FIXED_COST_FOR_AGENTS", "SPECIFIC_COST_PER_PROFILE", "SPECIFIC_COST_PER_AGENT"}
				validBillingType := false
				for _, v := range allowedBillingType {
					if flagBillingType == v {
						validBillingType = true
						break
					}
				}
				if !validBillingType {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "billing-type", flagBillingType, allowedBillingType)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/tickets/{ticketId}/timeEntryByBillingType"
			path = replacePathParam(path, "ticketId", args[0])
			params := map[string]string{}
			if flagInclude != "" {
				params["include"] = fmt.Sprintf("%v", flagInclude)
			}
			if flagBillingType != "" {
				params["billingType"] = fmt.Sprintf("%v", flagBillingType)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "time-entry-by-billing-type", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagInclude, "include", "", "Allowed value is : @owner@ (one of: owner)")
	cmd.Flags().StringVar(&flagBillingType, "billing-type", "", "Allowed values are @FIXED_COST_FOR_TICKETS@ @FIXED_COST_FOR_AGENTS@ @SPECIFIC_COST_PER_AGENT@... (one of: FIXED_COST_FOR_TICKETS, FIXED_COST_FOR_AGENTS, SPECIFIC_COST_PER_PROFILE, SPECIFIC_COST_PER_AGENT)")

	return cmd
}
