// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCustomerHappinessLinkHolderPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagDepartmentId string
	var flagLanguageCode string

	cmd := &cobra.Command{
		Use:         "customer-happiness-link-holder",
		Short:       "This API provides an html placeholder to insert the customer feedback link into the reply mail. The link, when...",
		Long:        "Shortcut for 'customer-happiness-link-holder get'. This API provides an html placeholder to insert the customer feedback link into the reply mail. The link, when...",
		Example:     "  zoho-desk-pp-cli customer-happiness-link-holder",
		Annotations: map[string]string{"pp:endpoint": "customer-happiness-link-holder.get", "pp:method": "GET", "pp:path": "/api/v1/customerHappinessLinkHolder", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("language-code") {
				allowedLanguageCode := []string{"aa", "ab", "ae", "af-ZA", "ak", "am", "an", "ar-EG", "as-IN", "av", "ay", "az-AZ", "ba", "be-BY", "bg-BG", "bh-IN", "bi", "bm", "bn-IN", "bo", "br", "bs-BA", "ca-ES", "ce", "ch", "co", "cr", "cs-CZ", "cu", "cv", "cy", "da-DK", "de-DE", "dv-MV", "dz", "ee", "el-GR", "en-US", "eo", "es-ES", "et-EE", "eu-ES", "fa-IR", "ff", "fi-FI", "fj", "fo-FO", "fr-FR", "fy", "ga", "gd", "gl-ES", "gn", "gu-IN", "gv", "ha", "he-IL", "hi-IN", "ho", "hr-HR", "ht", "hu-HU", "hy-AM", "hz", "ia", "id-ID", "ie", "ig", "ii", "ik", "io", "is-IS", "it-IT", "iu", "ja-JP", "jv", "ka-GE", "kg", "ki", "kj", "kk-KZ", "kl", "km", "kn-IN", "ko-KR", "kr", "ks", "ku", "kv", "kw", "ky-KZ", "la", "lb", "lg", "li", "ln", "lo", "lt-LT", "lu", "lv-LV", "mg", "mh", "mi-NZ", "mk-MK", "ml-IN", "mn-MN", "mr-IN", "ms-MY", "mt-MT", "my-MM", "na", "nb-NO", "nd", "ne", "ng", "nl-NL", "nn-NO", "no", "nr", "nv", "ny", "oc", "oj", "om", "or", "os", "pa-IN", "pi", "pl-PL", "ps-AR", "pt-PT", "qu-BO", "rm", "rn", "ro-RO", "ru-RU", "rw", "sa-IN", "sc", "sd", "se-NO", "sg", "si", "sk-SK", "sl-SI", "sm", "sn", "so", "sq-AL", "sr-SP", "ss", "st", "su", "sv-SE", "sw-KE", "ta-IN", "te-IN", "tg", "th-TH", "ti", "tk", "tl-PH", "tn-ZA", "to", "tr-TR", "ts", "tt-RU", "tw", "ty", "ug", "uk-UA", "ur-PK", "uz-UZ", "ve", "vi-VN", "vo", "wa", "wo", "xh-ZA", "yi", "yo", "za", "zh-CN", "zu-ZA", "fr-CA", "en-GB", "zh-TW", "pt-BR"}
				validLanguageCode := false
				for _, v := range allowedLanguageCode {
					if flagLanguageCode == v {
						validLanguageCode = true
						break
					}
				}
				if !validLanguageCode {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "language-code", flagLanguageCode, allowedLanguageCode)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/customerHappinessLinkHolder"
			params := map[string]string{}
			if flagDepartmentId != "" {
				params["departmentId"] = fmt.Sprintf("%v", flagDepartmentId)
			}
			if flagLanguageCode != "" {
				params["languageCode"] = fmt.Sprintf("%v", flagLanguageCode)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "customer-happiness-link-holder", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDepartmentId, "department-id", "", "Unique ID of the department")
	cmd.Flags().StringVar(&flagLanguageCode, "language-code", "", "Localization languageCode of Customer Happiness page to be displayed (one of: aa, ab, ae, af-ZA, ak, am, an, ar-EG, as-IN, av, ay, az-AZ, ba, be-BY, bg-BG, bh-IN, bi, bm, bn-IN, bo, br, bs-BA, ca-ES, ce, ch, co, cr, cs-CZ, cu, cv, cy, da-DK, de-DE, dv-MV, dz, ee, el-GR, en-US, eo, es-ES, et-EE, eu-ES, fa-IR, ff, fi-FI, fj, fo-FO, fr-FR, fy, ga, gd, gl-ES, gn, gu-IN, gv, ha, he-IL, hi-IN, ho, hr-HR, ht, hu-HU, hy-AM, hz, ia, id-ID, ie, ig, ii, ik, io, is-IS, it-IT, iu, ja-JP, jv, ka-GE, kg, ki, kj, kk-KZ, kl, km, kn-IN, ko-KR, kr, ks, ku, kv, kw, ky-KZ, la, lb, lg, li, ln, lo, lt-LT, lu, lv-LV, mg, mh, mi-NZ, mk-MK, ml-IN, mn-MN, mr-IN, ms-MY, mt-MT, my-MM, na, nb-NO, nd, ne, ng, nl-NL, nn-NO, no, nr, nv, ny, oc, oj, om, or, os, pa-IN, pi, pl-PL, ps-AR, pt-PT, qu-BO, rm, rn, ro-RO, ru-RU, rw, sa-IN, sc, sd, se-NO, sg, si, sk-SK, sl-SI, sm, sn, so, sq-AL, sr-SP, ss, st, su, sv-SE, sw-KE, ta-IN, te-IN, tg, th-TH, ti, tk, tl-PH, tn-ZA, to, tr-TR, ts, tt-RU, tw, ty, ug, uk-UA, ur-PK, uz-UZ, ve, vi-VN, vo, wa, wo, xh-ZA, yi, yo, za, zh-CN, zu-ZA, fr-CA, en-GB, zh-TW, pt-BR)")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
