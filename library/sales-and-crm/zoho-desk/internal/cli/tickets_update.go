// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTicketsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAssigneeEmailId string
	var bodyAssigneeFirstName string
	var bodyAssigneeId int
	var bodyAssigneeLastName string
	var bodyAssigneePhotoURL string
	var bodyCategory string
	var bodyCf string
	var bodyChannel string
	var bodyClassification string
	var bodyContactId int
	var bodyCustomFields string
	var bodyDepartmentId int
	var bodyDepartmentName string
	var bodyDescription string
	var bodyErrorMessage string
	var bodyReason string
	var bodyResolution string
	var bodySharedDepartments string
	var bodyStatus string
	var bodySubCategory string
	var bodySubject string
	var bodyThreadId int
	var bodyTicketNumber int
	var bodyUploads string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update <ticketId>",
		Short:       "This API updates the details of an existing ticket.",
		Example:     "  zoho-desk-pp-cli tickets update 42 --category example-value",
		Annotations: map[string]string{"pp:endpoint": "tickets.update", "pp:method": "PATCH", "pp:path": "/api/v1/tickets/{ticketId}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("assignee-email-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "assignee-email-id")
				}
				if !cmd.Flags().Changed("assignee-first-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "assignee-first-name")
				}
				if !cmd.Flags().Changed("assignee-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "assignee-id")
				}
				if !cmd.Flags().Changed("assignee-last-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "assignee-last-name")
				}
				if !cmd.Flags().Changed("assignee-photo-url") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "assignee-photo-url")
				}
				if !cmd.Flags().Changed("category") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "category")
				}
				if !cmd.Flags().Changed("cf") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "cf")
				}
				if !cmd.Flags().Changed("channel") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "channel")
				}
				if !cmd.Flags().Changed("classification") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "classification")
				}
				if !cmd.Flags().Changed("custom-fields") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "custom-fields")
				}
				if !cmd.Flags().Changed("department-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "department-id")
				}
				if !cmd.Flags().Changed("department-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "department-name")
				}
				if !cmd.Flags().Changed("description") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "description")
				}
				if !cmd.Flags().Changed("error-message") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "error-message")
				}
				if !cmd.Flags().Changed("reason") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "reason")
				}
				if !cmd.Flags().Changed("status") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "status")
				}
				if !cmd.Flags().Changed("sub-category") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "sub-category")
				}
				if !cmd.Flags().Changed("subject") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "subject")
				}
				if !cmd.Flags().Changed("thread-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "thread-id")
				}
				if !cmd.Flags().Changed("ticket-number") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "ticket-number")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/tickets/{ticketId}"
			path = replacePathParam(path, "ticketId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedAssignee := map[string]any{}
					if bodyAssigneeEmailId != "" {
						nestedAssignee["emailId"] = bodyAssigneeEmailId
					}
					if bodyAssigneeFirstName != "" {
						nestedAssignee["firstName"] = bodyAssigneeFirstName
					}
					if bodyAssigneeId != 0 {
						nestedAssignee["id"] = bodyAssigneeId
					}
					if bodyAssigneeLastName != "" {
						nestedAssignee["lastName"] = bodyAssigneeLastName
					}
					if bodyAssigneePhotoURL != "" {
						nestedAssignee["photoURL"] = bodyAssigneePhotoURL
					}
					if len(nestedAssignee) > 0 {
						body["assignee"] = nestedAssignee
					}
				}
				if bodyCategory != "" {
					body["category"] = bodyCategory
				}
				if bodyCf != "" {
					var parsedCf any
					if err := json.Unmarshal([]byte(bodyCf), &parsedCf); err != nil {
						return fmt.Errorf("parsing --cf JSON: %w", err)
					}
					body["cf"] = parsedCf
				}
				if bodyChannel != "" {
					body["channel"] = bodyChannel
				}
				if bodyClassification != "" {
					body["classification"] = bodyClassification
				}
				if bodyContactId != 0 {
					body["contactId"] = bodyContactId
				}
				if bodyCustomFields != "" {
					var parsedCustomFields any
					if err := json.Unmarshal([]byte(bodyCustomFields), &parsedCustomFields); err != nil {
						return fmt.Errorf("parsing --custom-fields JSON: %w", err)
					}
					body["customFields"] = parsedCustomFields
				}
				if bodyDepartmentId != 0 {
					body["departmentId"] = bodyDepartmentId
				}
				if bodyDepartmentName != "" {
					body["departmentName"] = bodyDepartmentName
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyErrorMessage != "" {
					body["errorMessage"] = bodyErrorMessage
				}
				if bodyReason != "" {
					body["reason"] = bodyReason
				}
				if bodyResolution != "" {
					body["resolution"] = bodyResolution
				}
				if bodySharedDepartments != "" {
					var parsedSharedDepartments any
					if err := json.Unmarshal([]byte(bodySharedDepartments), &parsedSharedDepartments); err != nil {
						return fmt.Errorf("parsing --shared-departments JSON: %w", err)
					}
					body["sharedDepartments"] = parsedSharedDepartments
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodySubCategory != "" {
					body["subCategory"] = bodySubCategory
				}
				if bodySubject != "" {
					body["subject"] = bodySubject
				}
				if bodyThreadId != 0 {
					body["threadId"] = bodyThreadId
				}
				if bodyTicketNumber != 0 {
					body["ticketNumber"] = bodyTicketNumber
				}
				if bodyUploads != "" {
					var parsedUploads any
					if err := json.Unmarshal([]byte(bodyUploads), &parsedUploads); err != nil {
						return fmt.Errorf("parsing --uploads JSON: %w", err)
					}
					body["uploads"] = parsedUploads
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "tickets",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAssigneeEmailId, "assignee-email-id", "", "Email id")
	cmd.Flags().StringVar(&bodyAssigneeFirstName, "assignee-first-name", "", "First name")
	cmd.Flags().IntVar(&bodyAssigneeId, "assignee-id", 0, "Id")
	cmd.Flags().StringVar(&bodyAssigneeLastName, "assignee-last-name", "", "Last name")
	cmd.Flags().StringVar(&bodyAssigneePhotoURL, "assignee-photo-url", "", "Photo url")
	cmd.Flags().StringVar(&bodyCategory, "category", "", "Category of the ticket")
	cmd.Flags().StringVar(&bodyCf, "cf", "", "Cf")
	cmd.Flags().StringVar(&bodyChannel, "channel", "", "Channel")
	cmd.Flags().StringVar(&bodyClassification, "classification", "", "Type of ticket. Values supported are @Problem@, @Request@, @Question@, and @Others@.")
	cmd.Flags().IntVar(&bodyContactId, "contact-id", 0, "ID of the contact who raised the ticket. If a value is not available for this key, make sure to include the...")
	cmd.Flags().StringVar(&bodyCustomFields, "custom-fields", "", "Custom fields")
	cmd.Flags().IntVar(&bodyDepartmentId, "department-id", 0, "Department id")
	cmd.Flags().StringVar(&bodyDepartmentName, "department-name", "", "Department name")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description in the ticket")
	cmd.Flags().StringVar(&bodyErrorMessage, "error-message", "", "Error message")
	cmd.Flags().StringVar(&bodyReason, "reason", "", "Reason")
	cmd.Flags().StringVar(&bodyResolution, "resolution", "", "Resolution notes recorded in the ticket")
	cmd.Flags().StringVar(&bodySharedDepartments, "shared-departments", "", "Shared departments")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Status of the ticket. Includes the custom statuses configured in your help desk portal.")
	cmd.Flags().StringVar(&bodySubCategory, "sub-category", "", "Subcategory of the ticket")
	cmd.Flags().StringVar(&bodySubject, "subject", "", "Subject")
	cmd.Flags().IntVar(&bodyThreadId, "thread-id", 0, "Thread id")
	cmd.Flags().IntVar(&bodyTicketNumber, "ticket-number", 0, "Ticket number")
	cmd.Flags().StringVar(&bodyUploads, "uploads", "", "Uploads")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
