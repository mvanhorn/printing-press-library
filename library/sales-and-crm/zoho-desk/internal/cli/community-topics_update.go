// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCommunityTopicsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAttachmentIds string
	var bodyContent string
	var bodyFeaturedImageImageId int
	var bodyLabel string
	var bodyNotifyMentions bool
	var bodySeoDescription string
	var bodySeoKeywords string
	var bodySeoTitle string
	var bodySeoUseTitleAsPageTitle bool
	var bodySubject string
	var bodyType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update <topicId>",
		Short:       "This API helps update a published forum topic.",
		Example:     "  zoho-desk-pp-cli community-topics update 42",
		Annotations: map[string]string{"pp:endpoint": "community-topics.update", "pp:method": "PATCH", "pp:path": "/api/v1/communityTopics/{topicId}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("featured-image-image-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "featured-image-image-id")
				}
				if !cmd.Flags().Changed("seo-description") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "seo-description")
				}
				if !cmd.Flags().Changed("seo-keywords") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "seo-keywords")
				}
				if !cmd.Flags().Changed("seo-title") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "seo-title")
				}
				if !cmd.Flags().Changed("seo-use-title-as-page-title") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "seo-use-title-as-page-title")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/communityTopics/{topicId}"
			path = replacePathParam(path, "topicId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAttachmentIds != "" {
					var parsedAttachmentIds any
					if err := json.Unmarshal([]byte(bodyAttachmentIds), &parsedAttachmentIds); err != nil {
						return fmt.Errorf("parsing --attachment-ids JSON: %w", err)
					}
					body["attachmentIds"] = parsedAttachmentIds
				}
				if bodyContent != "" {
					body["content"] = bodyContent
				}
				{
					nestedFeaturedImage := map[string]any{}
					if bodyFeaturedImageImageId != 0 {
						nestedFeaturedImage["imageId"] = bodyFeaturedImageImageId
					}
					if len(nestedFeaturedImage) > 0 {
						body["featuredImage"] = nestedFeaturedImage
					}
				}
				if bodyLabel != "" {
					body["label"] = bodyLabel
				}
				if bodyNotifyMentions != false {
					body["notifyMentions"] = bodyNotifyMentions
				}
				{
					nestedSeo := map[string]any{}
					if bodySeoDescription != "" {
						nestedSeo["description"] = bodySeoDescription
					}
					if bodySeoKeywords != "" {
						nestedSeo["keywords"] = bodySeoKeywords
					}
					if bodySeoTitle != "" {
						nestedSeo["title"] = bodySeoTitle
					}
					if bodySeoUseTitleAsPageTitle != false {
						nestedSeo["useTitleAsPageTitle"] = bodySeoUseTitleAsPageTitle
					}
					if len(nestedSeo) > 0 {
						body["seo"] = nestedSeo
					}
				}
				if bodySubject != "" {
					body["subject"] = bodySubject
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "community-topics",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAttachmentIds, "attachment-ids", "", "Attachment ids")
	cmd.Flags().StringVar(&bodyContent, "content", "", "Body content of the topic")
	cmd.Flags().IntVar(&bodyFeaturedImageImageId, "featured-image-image-id", 0, "ID of the public image")
	cmd.Flags().StringVar(&bodyLabel, "label", "", "Status label of the topic. The value of this key varies according to the topic type. Values allowed are: @NOSTATUS@,...")
	cmd.Flags().BoolVar(&bodyNotifyMentions, "notify-mentions", false, "whether to notify mentioned user while updateing content.")
	cmd.Flags().StringVar(&bodySeoDescription, "seo-description", "", "SEO-specific description of the article")
	cmd.Flags().StringVar(&bodySeoKeywords, "seo-keywords", "", "SEO-specific keywords of the topic")
	cmd.Flags().StringVar(&bodySeoTitle, "seo-title", "", "SEO-specific title of the topic")
	cmd.Flags().BoolVar(&bodySeoUseTitleAsPageTitle, "seo-use-title-as-page-title", false, "Key that indicates whether to use the title as the page title or not.")
	cmd.Flags().StringVar(&bodySubject, "subject", "", "Title of the topic")
	cmd.Flags().StringVar(&bodyType, "type", "", "Type of topic. Values allowed are: @ANNOUNCEMENT@, @DISCUSSION@, @IDEA@, @PROBLEM@, and @QUESTION@.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
