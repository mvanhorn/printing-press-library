// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newArticlesTranslationsCreateArticleCmd(flags *rootFlags) *cobra.Command {
	var bodyAnswer string
	var bodyAttachmentIds string
	var bodyAuthorId int
	var bodyExpiryDate string
	var bodyIsSEOEnabled bool
	var bodyLocale string
	var bodyPermalink string
	var bodyPermission string
	var bodySeoDescription string
	var bodySeoKeywords string
	var bodySeoTitle string
	var bodyStatus string
	var bodyTags string
	var bodyTitle string
	var bodyTranslationState string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-article <articleId>",
		Short:       "This API adds a translated version of an article.",
		Example:     "  zoho-desk-pp-cli articles translations create-article 42 --answer example-value",
		Annotations: map[string]string{"pp:endpoint": "translations.create-article", "pp:method": "POST", "pp:path": "/api/v1/articles/{articleId}/translations"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("answer") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "answer")
				}
				if !cmd.Flags().Changed("author-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "author-id")
				}
				if !cmd.Flags().Changed("locale") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "locale")
				}
				if !cmd.Flags().Changed("status") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "status")
				}
				if !cmd.Flags().Changed("title") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "title")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/articles/{articleId}/translations"
			path = replacePathParam(path, "articleId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAnswer != "" {
					body["answer"] = bodyAnswer
				}
				if bodyAttachmentIds != "" {
					var parsedAttachmentIds any
					if err := json.Unmarshal([]byte(bodyAttachmentIds), &parsedAttachmentIds); err != nil {
						return fmt.Errorf("parsing --attachment-ids JSON: %w", err)
					}
					body["attachmentIds"] = parsedAttachmentIds
				}
				if bodyAuthorId != 0 {
					body["authorId"] = bodyAuthorId
				}
				if bodyExpiryDate != "" {
					body["expiryDate"] = bodyExpiryDate
				}
				if bodyIsSEOEnabled != false {
					body["isSEOEnabled"] = bodyIsSEOEnabled
				}
				if bodyLocale != "" {
					body["locale"] = bodyLocale
				}
				if bodyPermalink != "" {
					body["permalink"] = bodyPermalink
				}
				if bodyPermission != "" {
					body["permission"] = bodyPermission
				}
				if bodySeoDescription != "" {
					body["seoDescription"] = bodySeoDescription
				}
				if bodySeoKeywords != "" {
					body["seoKeywords"] = bodySeoKeywords
				}
				if bodySeoTitle != "" {
					body["seoTitle"] = bodySeoTitle
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodyTags != "" {
					var parsedTags any
					if err := json.Unmarshal([]byte(bodyTags), &parsedTags); err != nil {
						return fmt.Errorf("parsing --tags JSON: %w", err)
					}
					body["tags"] = parsedTags
				}
				if bodyTitle != "" {
					body["title"] = bodyTitle
				}
				if bodyTranslationState != "" {
					body["translationState"] = bodyTranslationState
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "translations",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAnswer, "answer", "", "The answer part of the article")
	cmd.Flags().StringVar(&bodyAttachmentIds, "attachment-ids", "", "Attachment ids")
	cmd.Flags().IntVar(&bodyAuthorId, "author-id", 0, "ID of the user who created the article")
	cmd.Flags().StringVar(&bodyExpiryDate, "expiry-date", "", "Schedule the expiry date of the article. format : (YYYY-MM-DD HH:MM)")
	cmd.Flags().BoolVar(&bodyIsSEOEnabled, "is-seo-enabled", false, "SEO enabled status of the article")
	cmd.Flags().StringVar(&bodyLocale, "locale", "", "Language code of the article translation")
	cmd.Flags().StringVar(&bodyPermalink, "permalink", "", "The permalink of the article")
	cmd.Flags().StringVar(&bodyPermission, "permission", "", "Permission of the article.Values allowed are !@ALL@! !@REGISTEREDUSERS@! !@AGENTS@!")
	cmd.Flags().StringVar(&bodySeoDescription, "seo-description", "", "SEO Description of the article")
	cmd.Flags().StringVar(&bodySeoKeywords, "seo-keywords", "", "SEO Keywords of the article")
	cmd.Flags().StringVar(&bodySeoTitle, "seo-title", "", "SEO Title of the article")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Status of the article !@Draft@! !@Published@! !@Review@!")
	cmd.Flags().StringVar(&bodyTags, "tags", "", "Tags")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "The title of the Article")
	cmd.Flags().StringVar(&bodyTranslationState, "translation-state", "", "Key that specifies the state of the translated article: !@OUTDATED@! !@UP-TO-DATE@!")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
