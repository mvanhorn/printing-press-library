// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAccountName string
	var bodyAnnualrevenue string
	var bodyCf string
	var bodyCity string
	var bodyCode string
	var bodyCountry string
	var bodyCustomFields string
	var bodyDescription string
	var bodyEmail string
	var bodyFax string
	var bodyIndustry string
	var bodyOwnerId int
	var bodyPhone string
	var bodyState string
	var bodyStreet string
	var bodyWebsite string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "This API creates an account in your help desk portal.",
		Example:     "  zoho-desk-pp-cli accounts create --account-name example-resource",
		Annotations: map[string]string{"pp:endpoint": "accounts.create", "pp:method": "POST", "pp:path": "/api/v1/accounts"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("account-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "account-name")
				}
				if !cmd.Flags().Changed("email") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "email")
				}
				if !cmd.Flags().Changed("phone") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "phone")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/accounts"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAccountName != "" {
					body["accountName"] = bodyAccountName
				}
				if bodyAnnualrevenue != "" {
					body["annualrevenue"] = bodyAnnualrevenue
				}
				if bodyCf != "" {
					var parsedCf any
					if err := json.Unmarshal([]byte(bodyCf), &parsedCf); err != nil {
						return fmt.Errorf("parsing --cf JSON: %w", err)
					}
					body["cf"] = parsedCf
				}
				if bodyCity != "" {
					body["city"] = bodyCity
				}
				if bodyCode != "" {
					body["code"] = bodyCode
				}
				if bodyCountry != "" {
					body["country"] = bodyCountry
				}
				if bodyCustomFields != "" {
					var parsedCustomFields any
					if err := json.Unmarshal([]byte(bodyCustomFields), &parsedCustomFields); err != nil {
						return fmt.Errorf("parsing --custom-fields JSON: %w", err)
					}
					body["customFields"] = parsedCustomFields
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyEmail != "" {
					body["email"] = bodyEmail
				}
				if bodyFax != "" {
					body["fax"] = bodyFax
				}
				if bodyIndustry != "" {
					body["industry"] = bodyIndustry
				}
				if bodyOwnerId != 0 {
					body["ownerId"] = bodyOwnerId
				}
				if bodyPhone != "" {
					body["phone"] = bodyPhone
				}
				if bodyState != "" {
					body["state"] = bodyState
				}
				if bodyStreet != "" {
					body["street"] = bodyStreet
				}
				if bodyWebsite != "" {
					body["website"] = bodyWebsite
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "accounts",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAccountName, "account-name", "", "Name of the account")
	cmd.Flags().StringVar(&bodyAnnualrevenue, "annualrevenue", "", "Annual revenue of the account")
	cmd.Flags().StringVar(&bodyCf, "cf", "", "Cf")
	cmd.Flags().StringVar(&bodyCity, "city", "", "City in which the account is based")
	cmd.Flags().StringVar(&bodyCode, "code", "", "Zip code")
	cmd.Flags().StringVar(&bodyCountry, "country", "", "Country in which the account is based")
	cmd.Flags().StringVar(&bodyCustomFields, "custom-fields", "", "Custom fields")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A short description of the account")
	cmd.Flags().StringVar(&bodyEmail, "email", "", "Email ID of the account")
	cmd.Flags().StringVar(&bodyFax, "fax", "", "Fax number of the account")
	cmd.Flags().StringVar(&bodyIndustry, "industry", "", "Industry in which the account operates")
	cmd.Flags().IntVar(&bodyOwnerId, "owner-id", 0, "ID of account owner")
	cmd.Flags().StringVar(&bodyPhone, "phone", "", "Phone number of the account")
	cmd.Flags().StringVar(&bodyState, "state", "", "State in which the account is based")
	cmd.Flags().StringVar(&bodyStreet, "street", "", "Address of the account")
	cmd.Flags().StringVar(&bodyWebsite, "website", "", "Website of the account")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
