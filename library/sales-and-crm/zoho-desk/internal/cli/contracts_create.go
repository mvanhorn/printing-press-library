// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newContractsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAccountId int
	var bodyAssociatedSLAId int
	var bodyAssociatedSupportPlanId int
	var bodyCf string
	var bodyContractName string
	var bodyContractNumber string
	var bodyCustomFields string
	var bodyDepartmentId int
	var bodyDescription string
	var bodyEndDate string
	var bodyNotificationAgentIds string
	var bodyNotifyBefore int
	var bodyOwnerId int
	var bodyProductId int
	var bodyStartDate string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "This API creates a contract in your helpdesk.",
		Example:     "  zoho-desk-pp-cli contracts create --contract-name example-resource",
		Annotations: map[string]string{"pp:endpoint": "contracts.create", "pp:method": "POST", "pp:path": "/api/v1/contracts"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("account-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "account-id")
				}
				if !cmd.Flags().Changed("associated-sla-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "associated-sla-id")
				}
				if !cmd.Flags().Changed("associated-support-plan-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "associated-support-plan-id")
				}
				if !cmd.Flags().Changed("contract-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "contract-name")
				}
				if !cmd.Flags().Changed("department-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "department-id")
				}
				if !cmd.Flags().Changed("owner-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "owner-id")
				}
				if !cmd.Flags().Changed("start-date") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "start-date")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/contracts"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAccountId != 0 {
					body["accountId"] = bodyAccountId
				}
				if bodyAssociatedSLAId != 0 {
					body["associatedSLAId"] = bodyAssociatedSLAId
				}
				if bodyAssociatedSupportPlanId != 0 {
					body["associatedSupportPlanId"] = bodyAssociatedSupportPlanId
				}
				if bodyCf != "" {
					var parsedCf any
					if err := json.Unmarshal([]byte(bodyCf), &parsedCf); err != nil {
						return fmt.Errorf("parsing --cf JSON: %w", err)
					}
					body["cf"] = parsedCf
				}
				if bodyContractName != "" {
					body["contractName"] = bodyContractName
				}
				if bodyContractNumber != "" {
					body["contractNumber"] = bodyContractNumber
				}
				if bodyCustomFields != "" {
					var parsedCustomFields any
					if err := json.Unmarshal([]byte(bodyCustomFields), &parsedCustomFields); err != nil {
						return fmt.Errorf("parsing --custom-fields JSON: %w", err)
					}
					body["customFields"] = parsedCustomFields
				}
				if bodyDepartmentId != 0 {
					body["departmentId"] = bodyDepartmentId
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyEndDate != "" {
					body["endDate"] = bodyEndDate
				}
				if bodyNotificationAgentIds != "" {
					var parsedNotificationAgentIds any
					if err := json.Unmarshal([]byte(bodyNotificationAgentIds), &parsedNotificationAgentIds); err != nil {
						return fmt.Errorf("parsing --notification-agent-ids JSON: %w", err)
					}
					body["notificationAgentIds"] = parsedNotificationAgentIds
				}
				if bodyNotifyBefore != 0 {
					body["notifyBefore"] = bodyNotifyBefore
				}
				if bodyOwnerId != 0 {
					body["ownerId"] = bodyOwnerId
				}
				if bodyProductId != 0 {
					body["productId"] = bodyProductId
				}
				if bodyStartDate != "" {
					body["startDate"] = bodyStartDate
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "contracts",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyAccountId, "account-id", 0, "ID of the account associated with the contract")
	cmd.Flags().IntVar(&bodyAssociatedSLAId, "associated-sla-id", 0, "ID of the SLA associated with the contract")
	cmd.Flags().IntVar(&bodyAssociatedSupportPlanId, "associated-support-plan-id", 0, "ID of the Associated Support Plan")
	cmd.Flags().StringVar(&bodyCf, "cf", "", "Cf")
	cmd.Flags().StringVar(&bodyContractName, "contract-name", "", "Name of the contract")
	cmd.Flags().StringVar(&bodyContractNumber, "contract-number", "", "Serial number of the contract")
	cmd.Flags().StringVar(&bodyCustomFields, "custom-fields", "", "Custom fields")
	cmd.Flags().IntVar(&bodyDepartmentId, "department-id", 0, "ID of the department to which the contract belongs")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A short description of the contract")
	cmd.Flags().StringVar(&bodyEndDate, "end-date", "", "Date until which the contract is valid")
	cmd.Flags().StringVar(&bodyNotificationAgentIds, "notification-agent-ids", "", "Notification agent ids")
	cmd.Flags().IntVar(&bodyNotifyBefore, "notify-before", 0, "Number of days before endDate when notifications about contract expiry must be sent to the agents concerned")
	cmd.Flags().IntVar(&bodyOwnerId, "owner-id", 0, "ID of the contract owner")
	cmd.Flags().IntVar(&bodyProductId, "product-id", 0, "ID of the product associated with the contract")
	cmd.Flags().StringVar(&bodyStartDate, "start-date", "", "Date from which the contract is valid")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
