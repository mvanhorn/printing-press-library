// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newDataSharingRulesUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAccounts string
	var bodyCalls string
	var bodyContacts string
	var bodyContracts string
	var bodyEvents string
	var bodyProducts string
	var bodyTasks string
	var bodyTickets string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "This API updates the data sharing rules configured in your help desk portal.",
		Example:     "  zoho-desk-pp-cli data-sharing-rules update",
		Annotations: map[string]string{"pp:endpoint": "data-sharing-rules.update", "pp:method": "PATCH", "pp:path": "/api/v1/dataSharingRules"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/dataSharingRules"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAccounts != "" {
					body["accounts"] = bodyAccounts
				}
				if bodyCalls != "" {
					body["calls"] = bodyCalls
				}
				if bodyContacts != "" {
					body["contacts"] = bodyContacts
				}
				if bodyContracts != "" {
					body["contracts"] = bodyContracts
				}
				if bodyEvents != "" {
					body["events"] = bodyEvents
				}
				if bodyProducts != "" {
					body["products"] = bodyProducts
				}
				if bodyTasks != "" {
					body["tasks"] = bodyTasks
				}
				if bodyTickets != "" {
					body["tickets"] = bodyTickets
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "data-sharing-rules",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAccounts, "accounts", "", "Accounts")
	cmd.Flags().StringVar(&bodyCalls, "calls", "", "Calls")
	cmd.Flags().StringVar(&bodyContacts, "contacts", "", "Contacts")
	cmd.Flags().StringVar(&bodyContracts, "contracts", "", "Contracts")
	cmd.Flags().StringVar(&bodyEvents, "events", "", "Events")
	cmd.Flags().StringVar(&bodyProducts, "products", "", "Products")
	cmd.Flags().StringVar(&bodyTasks, "tasks", "", "Tasks")
	cmd.Flags().StringVar(&bodyTickets, "tickets", "", "Tickets")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
