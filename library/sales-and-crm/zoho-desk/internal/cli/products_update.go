// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newProductsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyCf string
	var bodyContactId int
	var bodyCustomFields string
	var bodyDepartmentId int
	var bodyDepartmentIds string
	var bodyDescription string
	var bodyManufacturer string
	var bodyOwnerId int
	var bodyPriority string
	var bodyProductCategory string
	var bodyStartTime string
	var bodyStatus string
	var bodySubject string
	var bodyTicketId int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update <productId>",
		Short:       "This API updates details of a product in your help desk portal.",
		Example:     "  zoho-desk-pp-cli products update 42 --priority example-value",
		Annotations: map[string]string{"pp:endpoint": "products.update", "pp:method": "PATCH", "pp:path": "/api/v1/products/{productId}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("contact-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "contact-id")
				}
				if !cmd.Flags().Changed("department-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "department-id")
				}
				if !cmd.Flags().Changed("department-ids") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "department-ids")
				}
				if !cmd.Flags().Changed("owner-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "owner-id")
				}
				if !cmd.Flags().Changed("priority") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "priority")
				}
				if !cmd.Flags().Changed("start-time") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "start-time")
				}
				if !cmd.Flags().Changed("status") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "status")
				}
				if !cmd.Flags().Changed("subject") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "subject")
				}
				if !cmd.Flags().Changed("ticket-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "ticket-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/products/{productId}"
			path = replacePathParam(path, "productId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCf != "" {
					var parsedCf any
					if err := json.Unmarshal([]byte(bodyCf), &parsedCf); err != nil {
						return fmt.Errorf("parsing --cf JSON: %w", err)
					}
					body["cf"] = parsedCf
				}
				if bodyContactId != 0 {
					body["contactId"] = bodyContactId
				}
				if bodyCustomFields != "" {
					var parsedCustomFields any
					if err := json.Unmarshal([]byte(bodyCustomFields), &parsedCustomFields); err != nil {
						return fmt.Errorf("parsing --custom-fields JSON: %w", err)
					}
					body["customFields"] = parsedCustomFields
				}
				if bodyDepartmentId != 0 {
					body["departmentId"] = bodyDepartmentId
				}
				if bodyDepartmentIds != "" {
					var parsedDepartmentIds any
					if err := json.Unmarshal([]byte(bodyDepartmentIds), &parsedDepartmentIds); err != nil {
						return fmt.Errorf("parsing --department-ids JSON: %w", err)
					}
					body["departmentIds"] = parsedDepartmentIds
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyManufacturer != "" {
					body["manufacturer"] = bodyManufacturer
				}
				if bodyOwnerId != 0 {
					body["ownerId"] = bodyOwnerId
				}
				if bodyPriority != "" {
					body["priority"] = bodyPriority
				}
				if bodyProductCategory != "" {
					body["productCategory"] = bodyProductCategory
				}
				if bodyStartTime != "" {
					body["startTime"] = bodyStartTime
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodySubject != "" {
					body["subject"] = bodySubject
				}
				if bodyTicketId != 0 {
					body["ticketId"] = bodyTicketId
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "products",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCf, "cf", "", "Cf")
	cmd.Flags().IntVar(&bodyContactId, "contact-id", 0, "ID of the contact associated with the call")
	cmd.Flags().StringVar(&bodyCustomFields, "custom-fields", "", "Custom fields")
	cmd.Flags().IntVar(&bodyDepartmentId, "department-id", 0, "ID of the department from which the activities must be fetched")
	cmd.Flags().StringVar(&bodyDepartmentIds, "department-ids", "", "Department ids")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyManufacturer, "manufacturer", "", "Manufacturer")
	cmd.Flags().IntVar(&bodyOwnerId, "owner-id", 0, "Owner id")
	cmd.Flags().StringVar(&bodyPriority, "priority", "", "Priority of the call")
	cmd.Flags().StringVar(&bodyProductCategory, "product-category", "", "Product category")
	cmd.Flags().StringVar(&bodyStartTime, "start-time", "", "Time when the call started. The value of this key must be recorded in the ISO date format of yyyy-MM-ddThh:mm:ss.SSSZ.")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Completion status of the call")
	cmd.Flags().StringVar(&bodySubject, "subject", "", "Subject of the call")
	cmd.Flags().IntVar(&bodyTicketId, "ticket-id", 0, "ID of the ticket associated with the call")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
