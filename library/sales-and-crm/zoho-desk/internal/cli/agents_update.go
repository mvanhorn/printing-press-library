// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAgentsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAboutInfo string
	var bodyAssociatedChatDepartmentIds string
	var bodyAssociatedDepartmentIds string
	var bodyCountryCode string
	var bodyEmailId string
	var bodyExtn string
	var bodyFirstName string
	var bodyLangCode string
	var bodyLastName string
	var bodyMobile string
	var bodyPhone string
	var bodyProfileId int
	var bodyRoleId int
	var bodyRolePermissionType string
	var bodyStatus string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update <agentId>",
		Short:       "This API updates details of an agent.",
		Example:     "  zoho-desk-pp-cli agents update 42",
		Annotations: map[string]string{"pp:endpoint": "agents.update", "pp:method": "PATCH", "pp:path": "/api/v1/agents/{agentId}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/agents/{agentId}"
			path = replacePathParam(path, "agentId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAboutInfo != "" {
					body["aboutInfo"] = bodyAboutInfo
				}
				if bodyAssociatedChatDepartmentIds != "" {
					var parsedAssociatedChatDepartmentIds any
					if err := json.Unmarshal([]byte(bodyAssociatedChatDepartmentIds), &parsedAssociatedChatDepartmentIds); err != nil {
						return fmt.Errorf("parsing --associated-chat-department-ids JSON: %w", err)
					}
					body["associatedChatDepartmentIds"] = parsedAssociatedChatDepartmentIds
				}
				if bodyAssociatedDepartmentIds != "" {
					var parsedAssociatedDepartmentIds any
					if err := json.Unmarshal([]byte(bodyAssociatedDepartmentIds), &parsedAssociatedDepartmentIds); err != nil {
						return fmt.Errorf("parsing --associated-department-ids JSON: %w", err)
					}
					body["associatedDepartmentIds"] = parsedAssociatedDepartmentIds
				}
				if bodyCountryCode != "" {
					body["countryCode"] = bodyCountryCode
				}
				if bodyEmailId != "" {
					body["emailId"] = bodyEmailId
				}
				if bodyExtn != "" {
					body["extn"] = bodyExtn
				}
				if bodyFirstName != "" {
					body["firstName"] = bodyFirstName
				}
				if bodyLangCode != "" {
					body["langCode"] = bodyLangCode
				}
				if bodyLastName != "" {
					body["lastName"] = bodyLastName
				}
				if bodyMobile != "" {
					body["mobile"] = bodyMobile
				}
				if bodyPhone != "" {
					body["phone"] = bodyPhone
				}
				if bodyProfileId != 0 {
					body["profileId"] = bodyProfileId
				}
				if bodyRoleId != 0 {
					body["roleId"] = bodyRoleId
				}
				if bodyRolePermissionType != "" {
					body["rolePermissionType"] = bodyRolePermissionType
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "agents",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAboutInfo, "about-info", "", "About info")
	cmd.Flags().StringVar(&bodyAssociatedChatDepartmentIds, "associated-chat-department-ids", "", "Associated chat department ids")
	cmd.Flags().StringVar(&bodyAssociatedDepartmentIds, "associated-department-ids", "", "Associated department ids")
	cmd.Flags().StringVar(&bodyCountryCode, "country-code", "", "Country code")
	cmd.Flags().StringVar(&bodyEmailId, "email-id", "", "Email id")
	cmd.Flags().StringVar(&bodyExtn, "extn", "", "Extn")
	cmd.Flags().StringVar(&bodyFirstName, "first-name", "", "First name")
	cmd.Flags().StringVar(&bodyLangCode, "lang-code", "", "Lang code")
	cmd.Flags().StringVar(&bodyLastName, "last-name", "", "Last name")
	cmd.Flags().StringVar(&bodyMobile, "mobile", "", "Mobile")
	cmd.Flags().StringVar(&bodyPhone, "phone", "", "Phone")
	cmd.Flags().IntVar(&bodyProfileId, "profile-id", 0, "Profile id")
	cmd.Flags().IntVar(&bodyRoleId, "role-id", 0, "Role id")
	cmd.Flags().StringVar(&bodyRolePermissionType, "role-permission-type", "", "Role permission type")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Status")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
