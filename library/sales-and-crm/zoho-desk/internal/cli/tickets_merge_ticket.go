// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTicketsMergeTicketCmd(flags *rootFlags) *cobra.Command {
	var bodyIds string
	var bodySourceAssigneeId int
	var bodySourceCategory int
	var bodySourceCf string
	var bodySourceChannel int
	var bodySourceClassification int
	var bodySourceContactId int
	var bodySourceCustomFields string
	var bodySourceDescription int
	var bodySourceDueDate int
	var bodySourceEmail int
	var bodySourceEntitySkills int
	var bodySourcePhone int
	var bodySourcePriority int
	var bodySourceProductId int
	var bodySourceResolution int
	var bodySourceSecondaryContacts int
	var bodySourceStatus int
	var bodySourceSubCategory int
	var bodySourceSubject int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "ticket <ticketId>",
		Aliases:     []string{"create"},
		Short:       "This API merges two different tickets.",
		Example:     "  zoho-desk-pp-cli tickets merge ticket 42",
		Annotations: map[string]string{"pp:endpoint": "merge.ticket", "pp:method": "POST", "pp:path": "/api/v1/tickets/{ticketId}/merge"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("ids") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "ids")
				}
				if !cmd.Flags().Changed("source-assignee-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-assignee-id")
				}
				if !cmd.Flags().Changed("source-category") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-category")
				}
				if !cmd.Flags().Changed("source-cf") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-cf")
				}
				if !cmd.Flags().Changed("source-channel") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-channel")
				}
				if !cmd.Flags().Changed("source-classification") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-classification")
				}
				if !cmd.Flags().Changed("source-contact-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-contact-id")
				}
				if !cmd.Flags().Changed("source-custom-fields") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-custom-fields")
				}
				if !cmd.Flags().Changed("source-description") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-description")
				}
				if !cmd.Flags().Changed("source-due-date") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-due-date")
				}
				if !cmd.Flags().Changed("source-email") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-email")
				}
				if !cmd.Flags().Changed("source-entity-skills") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-entity-skills")
				}
				if !cmd.Flags().Changed("source-phone") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-phone")
				}
				if !cmd.Flags().Changed("source-priority") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-priority")
				}
				if !cmd.Flags().Changed("source-product-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-product-id")
				}
				if !cmd.Flags().Changed("source-resolution") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-resolution")
				}
				if !cmd.Flags().Changed("source-secondary-contacts") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-secondary-contacts")
				}
				if !cmd.Flags().Changed("source-status") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-status")
				}
				if !cmd.Flags().Changed("source-sub-category") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-sub-category")
				}
				if !cmd.Flags().Changed("source-subject") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-subject")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/tickets/{ticketId}/merge"
			path = replacePathParam(path, "ticketId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyIds != "" {
					var parsedIds any
					if err := json.Unmarshal([]byte(bodyIds), &parsedIds); err != nil {
						return fmt.Errorf("parsing --ids JSON: %w", err)
					}
					body["ids"] = parsedIds
				}
				{
					nestedSource := map[string]any{}
					if bodySourceAssigneeId != 0 {
						nestedSource["assigneeId"] = bodySourceAssigneeId
					}
					if bodySourceCategory != 0 {
						nestedSource["category"] = bodySourceCategory
					}
					if bodySourceCf != "" {
						var parsedSourceCf any
						if err := json.Unmarshal([]byte(bodySourceCf), &parsedSourceCf); err != nil {
							return fmt.Errorf("parsing --source-cf JSON: %w", err)
						}
						nestedSource["cf"] = parsedSourceCf
					}
					if bodySourceChannel != 0 {
						nestedSource["channel"] = bodySourceChannel
					}
					if bodySourceClassification != 0 {
						nestedSource["classification"] = bodySourceClassification
					}
					if bodySourceContactId != 0 {
						nestedSource["contactId"] = bodySourceContactId
					}
					if bodySourceCustomFields != "" {
						var parsedSourceCustomFields any
						if err := json.Unmarshal([]byte(bodySourceCustomFields), &parsedSourceCustomFields); err != nil {
							return fmt.Errorf("parsing --source-custom-fields JSON: %w", err)
						}
						nestedSource["customFields"] = parsedSourceCustomFields
					}
					if bodySourceDescription != 0 {
						nestedSource["description"] = bodySourceDescription
					}
					if bodySourceDueDate != 0 {
						nestedSource["dueDate"] = bodySourceDueDate
					}
					if bodySourceEmail != 0 {
						nestedSource["email"] = bodySourceEmail
					}
					if bodySourceEntitySkills != 0 {
						nestedSource["entitySkills"] = bodySourceEntitySkills
					}
					if bodySourcePhone != 0 {
						nestedSource["phone"] = bodySourcePhone
					}
					if bodySourcePriority != 0 {
						nestedSource["priority"] = bodySourcePriority
					}
					if bodySourceProductId != 0 {
						nestedSource["productId"] = bodySourceProductId
					}
					if bodySourceResolution != 0 {
						nestedSource["resolution"] = bodySourceResolution
					}
					if bodySourceSecondaryContacts != 0 {
						nestedSource["secondaryContacts"] = bodySourceSecondaryContacts
					}
					if bodySourceStatus != 0 {
						nestedSource["status"] = bodySourceStatus
					}
					if bodySourceSubCategory != 0 {
						nestedSource["subCategory"] = bodySourceSubCategory
					}
					if bodySourceSubject != 0 {
						nestedSource["subject"] = bodySourceSubject
					}
					if len(nestedSource) > 0 {
						body["source"] = nestedSource
					}
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "merge",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyIds, "ids", "", "Ids")
	cmd.Flags().IntVar(&bodySourceAssigneeId, "source-assignee-id", 0, "Assignee id")
	cmd.Flags().IntVar(&bodySourceCategory, "source-category", 0, "Category")
	cmd.Flags().StringVar(&bodySourceCf, "source-cf", "", "Cf")
	cmd.Flags().IntVar(&bodySourceChannel, "source-channel", 0, "Channel")
	cmd.Flags().IntVar(&bodySourceClassification, "source-classification", 0, "Classification")
	cmd.Flags().IntVar(&bodySourceContactId, "source-contact-id", 0, "Contact id")
	cmd.Flags().StringVar(&bodySourceCustomFields, "source-custom-fields", "", "Custom fields")
	cmd.Flags().IntVar(&bodySourceDescription, "source-description", 0, "Description")
	cmd.Flags().IntVar(&bodySourceDueDate, "source-due-date", 0, "Due date")
	cmd.Flags().IntVar(&bodySourceEmail, "source-email", 0, "Email")
	cmd.Flags().IntVar(&bodySourceEntitySkills, "source-entity-skills", 0, "Entity skills")
	cmd.Flags().IntVar(&bodySourcePhone, "source-phone", 0, "Phone")
	cmd.Flags().IntVar(&bodySourcePriority, "source-priority", 0, "Priority")
	cmd.Flags().IntVar(&bodySourceProductId, "source-product-id", 0, "Product id")
	cmd.Flags().IntVar(&bodySourceResolution, "source-resolution", 0, "Resolution")
	cmd.Flags().IntVar(&bodySourceSecondaryContacts, "source-secondary-contacts", 0, "Secondary contacts")
	cmd.Flags().IntVar(&bodySourceStatus, "source-status", 0, "Status")
	cmd.Flags().IntVar(&bodySourceSubCategory, "source-sub-category", 0, "Sub category")
	cmd.Flags().IntVar(&bodySourceSubject, "source-subject", 0, "Subject")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
