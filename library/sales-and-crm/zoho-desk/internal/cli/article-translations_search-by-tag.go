// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newArticleTranslationsSearchByTagCmd(flags *rootFlags) *cobra.Command {
	var flagInclude string
	var flagIsTrashed string
	var flagLimit int
	var flagFrom int
	var flagPermission string
	var flagTag string
	var flagLocale string
	var flagAuthorId string
	var flagCategoryId string
	var flagStatus string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "search-by-tag",
		Short:       "This API searches for the provided string from within the translation of articles.",
		Example:     "  zoho-desk-pp-cli article-translations search-by-tag",
		Annotations: map[string]string{"pp:endpoint": "article-translations.search-by-tag", "pp:method": "GET", "pp:path": "/api/v1/articleTranslations/searchByTag", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("locale") {
				allowedLocale := []string{"en", "ja", "zh", "es", "de", "fr", "tr", "ru", "pt", "it", "nl", "da", "sv", "pl", "ar", "he", "af", "cs", "fr-ca", "bg", "fi", "el", "hu", "id", "nb", "ro", "th", "uk", "vi", "ur", "hi", "te", "kn", "ta", "mr", "ko", "fa", "bn", "gu", "ms", "ml", "en-gb", "sk", "hr", "sl", "zh_TW", "zh-tw", "ca", "ka", "kk", "my", "km", "pt-br"}
				validLocale := false
				for _, v := range allowedLocale {
					if flagLocale == v {
						validLocale = true
						break
					}
				}
				if !validLocale {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "locale", flagLocale, allowedLocale)
				}
			}
			if cmd.Flags().Changed("status") {
				allowedStatus := []string{"ACTIVE", "DISABLED"}
				validStatus := false
				for _, v := range allowedStatus {
					if flagStatus == v {
						validStatus = true
						break
					}
				}
				if !validStatus {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "status", flagStatus, allowedStatus)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/articleTranslations/searchByTag"
			path = replacePathParam(path, "locale", fmt.Sprintf("%v", flagLocale))
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "article-translations", path, map[string]string{
				"include":    fmt.Sprintf("%v", flagInclude),
				"isTrashed":  fmt.Sprintf("%v", flagIsTrashed),
				"limit":      fmt.Sprintf("%v", flagLimit),
				"from":       fmt.Sprintf("%v", flagFrom),
				"permission": fmt.Sprintf("%v", flagPermission),
				"tag":        fmt.Sprintf("%v", flagTag),
				"authorId":   fmt.Sprintf("%v", flagAuthorId),
				"categoryId": fmt.Sprintf("%v", flagCategoryId),
				"status":     fmt.Sprintf("%v", flagStatus),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagInclude, "include", "", "The search of the article includes countInfo or availableLocaleTranslations. The allowed values are countInfo or...")
	cmd.Flags().StringVar(&flagIsTrashed, "is-trashed", "", "The articles that are even in the recycle bin should be searched. The allowed values are TRUE, FALSE Note: To search...")
	cmd.Flags().IntVar(&flagLimit, "limit", 0, "Number of accounts to fetch")
	cmd.Flags().IntVar(&flagFrom, "from", 1, "Index number, starting from which the accounts must be fetched")
	cmd.Flags().StringVar(&flagPermission, "permission", "", "Permission of the article.Values allowed are <br>ALL</br><br>REGISTEREDUSERS</br><br>AGENTS")
	cmd.Flags().StringVar(&flagTag, "tag", "", "The articles to be searched based on the tags.")
	cmd.Flags().StringVar(&flagLocale, "locale", "en", "Locale (one of: en, ja, zh, es, de, fr, tr, ru, pt, it, nl, da, sv, pl, ar, he, af, cs, fr-ca, bg, fi, el, hu, id, nb, ro, th, uk, vi, ur, hi, te, kn, ta, mr, ko, fa, bn, gu, ms, ml, en-gb, sk, hr, sl, zh_TW, zh-tw, ca, ka, kk, my, km, pt-br)")
	cmd.Flags().StringVar(&flagAuthorId, "author-id", "", "ID of article owner")
	cmd.Flags().StringVar(&flagCategoryId, "category-id", "", "ID of the category to which the article belongs")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Parameter that filters agents based on their activation status: @ACTIVE@ or @DISABLED@ (one of: ACTIVE, DISABLED)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
