// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTasksTimeEntryCreateTaskCmd(flags *rootFlags) *cobra.Command {
	var bodyAdditionalCost string
	var bodyAgentCostPerHour string
	var bodyCf string
	var bodyCustomFields string
	var bodyDescription string
	var bodyExecutedTime string
	var bodyFixedCost string
	var bodyHoursSpent int
	var bodyIsBillable bool
	var bodyMinutesSpent int
	var bodyMode string
	var bodyOwnerId int
	var bodyRequestChargeType string
	var bodySecondsSpent int
	var bodyTotalCost string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-task <taskId>",
		Aliases:     []string{"create"},
		Short:       "This API creates a time entry in your help desk",
		Example:     "  zoho-desk-pp-cli tasks time-entry create-task 42 --executed-time 2026-01-15T09:00:00Z",
		Annotations: map[string]string{"pp:endpoint": "time-entry.create-task", "pp:method": "POST", "pp:path": "/api/v1/tasks/{taskId}/timeEntry"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("executed-time") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "executed-time")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/tasks/{taskId}/timeEntry"
			path = replacePathParam(path, "taskId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAdditionalCost != "" {
					body["additionalCost"] = bodyAdditionalCost
				}
				if bodyAgentCostPerHour != "" {
					body["agentCostPerHour"] = bodyAgentCostPerHour
				}
				if bodyCf != "" {
					var parsedCf any
					if err := json.Unmarshal([]byte(bodyCf), &parsedCf); err != nil {
						return fmt.Errorf("parsing --cf JSON: %w", err)
					}
					body["cf"] = parsedCf
				}
				if bodyCustomFields != "" {
					var parsedCustomFields any
					if err := json.Unmarshal([]byte(bodyCustomFields), &parsedCustomFields); err != nil {
						return fmt.Errorf("parsing --custom-fields JSON: %w", err)
					}
					body["customFields"] = parsedCustomFields
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyExecutedTime != "" {
					body["executedTime"] = bodyExecutedTime
				}
				if bodyFixedCost != "" {
					body["fixedCost"] = bodyFixedCost
				}
				if bodyHoursSpent != 0 {
					body["hoursSpent"] = bodyHoursSpent
				}
				if bodyIsBillable != false {
					body["isBillable"] = bodyIsBillable
				}
				if bodyMinutesSpent != 0 {
					body["minutesSpent"] = bodyMinutesSpent
				}
				if bodyMode != "" {
					body["mode"] = bodyMode
				}
				if bodyOwnerId != 0 {
					body["ownerId"] = bodyOwnerId
				}
				if bodyRequestChargeType != "" {
					body["requestChargeType"] = bodyRequestChargeType
				}
				if bodySecondsSpent != 0 {
					body["secondsSpent"] = bodySecondsSpent
				}
				if bodyTotalCost != "" {
					body["totalCost"] = bodyTotalCost
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "time-entry",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAdditionalCost, "additional-cost", "", "Additional cost incurred")
	cmd.Flags().StringVar(&bodyAgentCostPerHour, "agent-cost-per-hour", "", "Per hour cost of an agent.")
	cmd.Flags().StringVar(&bodyCf, "cf", "", "Cf")
	cmd.Flags().StringVar(&bodyCustomFields, "custom-fields", "", "Custom fields")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description of the time entry")
	cmd.Flags().StringVar(&bodyExecutedTime, "executed-time", "", "Time when work related to the time entry was started.")
	cmd.Flags().StringVar(&bodyFixedCost, "fixed-cost", "", "Fixed cost")
	cmd.Flags().IntVar(&bodyHoursSpent, "hours-spent", 0, "Number of hours in the time entry.")
	cmd.Flags().BoolVar(&bodyIsBillable, "is-billable", false, "Billing Preference")
	cmd.Flags().IntVar(&bodyMinutesSpent, "minutes-spent", 0, "Number of minutes in the time entry.")
	cmd.Flags().StringVar(&bodyMode, "mode", "", "Mode configured in time tracking. Supported values are @Manual@ and @Auto@")
	cmd.Flags().IntVar(&bodyOwnerId, "owner-id", 0, "ID of the user associated with the time entry")
	cmd.Flags().StringVar(&bodyRequestChargeType, "request-charge-type", "", "Type of request in the ticket. Values supported are Customer Service, Upgrade Request, Product Consultation, and...")
	cmd.Flags().IntVar(&bodySecondsSpent, "seconds-spent", 0, "Number of seconds in the time entry.")
	cmd.Flags().StringVar(&bodyTotalCost, "total-cost", "", "Total cost incurred")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
