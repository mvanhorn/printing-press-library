// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrganizationsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAlias string
	var bodyCity string
	var bodyCompanyName string
	var bodyCountry string
	var bodyCurrencyLocale string
	var bodyDescription string
	var bodyEmployeeCount int
	var bodyFax string
	var bodyMobile string
	var bodyPhoneNumber string
	var bodyPortalName string
	var bodyPrimaryContact string
	var bodyRefValue string
	var bodyReferrerFrom string
	var bodyReferrerUrl string
	var bodyState string
	var bodyStreet string
	var bodyWebsite string
	var bodyZip string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update <organizationId>",
		Short:       "This API updates the details of an organization.",
		Example:     "  zoho-desk-pp-cli organizations update 42",
		Annotations: map[string]string{"pp:endpoint": "organizations.update", "pp:method": "PATCH", "pp:path": "/api/v1/organizations/{organizationId}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/organizations/{organizationId}"
			path = replacePathParam(path, "organizationId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAlias != "" {
					body["alias"] = bodyAlias
				}
				if bodyCity != "" {
					body["city"] = bodyCity
				}
				if bodyCompanyName != "" {
					body["companyName"] = bodyCompanyName
				}
				if bodyCountry != "" {
					body["country"] = bodyCountry
				}
				if bodyCurrencyLocale != "" {
					body["currencyLocale"] = bodyCurrencyLocale
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyEmployeeCount != 0 {
					body["employeeCount"] = bodyEmployeeCount
				}
				if bodyFax != "" {
					body["fax"] = bodyFax
				}
				if bodyMobile != "" {
					body["mobile"] = bodyMobile
				}
				if bodyPhoneNumber != "" {
					body["phoneNumber"] = bodyPhoneNumber
				}
				if bodyPortalName != "" {
					body["portalName"] = bodyPortalName
				}
				if bodyPrimaryContact != "" {
					body["primaryContact"] = bodyPrimaryContact
				}
				if bodyRefValue != "" {
					body["ref_value"] = bodyRefValue
				}
				if bodyReferrerFrom != "" {
					body["referrerFrom"] = bodyReferrerFrom
				}
				if bodyReferrerUrl != "" {
					body["referrerUrl"] = bodyReferrerUrl
				}
				if bodyState != "" {
					body["state"] = bodyState
				}
				if bodyStreet != "" {
					body["street"] = bodyStreet
				}
				if bodyWebsite != "" {
					body["website"] = bodyWebsite
				}
				if bodyZip != "" {
					body["zip"] = bodyZip
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "organizations",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAlias, "alias", "", "Alias")
	cmd.Flags().StringVar(&bodyCity, "city", "", "City")
	cmd.Flags().StringVar(&bodyCompanyName, "company-name", "", "Company name")
	cmd.Flags().StringVar(&bodyCountry, "country", "", "Country")
	cmd.Flags().StringVar(&bodyCurrencyLocale, "currency-locale", "", "Currency locale")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().IntVar(&bodyEmployeeCount, "employee-count", 0, "Employee count")
	cmd.Flags().StringVar(&bodyFax, "fax", "", "Fax")
	cmd.Flags().StringVar(&bodyMobile, "mobile", "", "Mobile")
	cmd.Flags().StringVar(&bodyPhoneNumber, "phone-number", "", "Phone number")
	cmd.Flags().StringVar(&bodyPortalName, "portal-name", "", "Portal name")
	cmd.Flags().StringVar(&bodyPrimaryContact, "primary-contact", "", "Primary contact")
	cmd.Flags().StringVar(&bodyRefValue, "ref-value", "", "Ref value")
	cmd.Flags().StringVar(&bodyReferrerFrom, "referrer-from", "", "Referrer from")
	cmd.Flags().StringVar(&bodyReferrerUrl, "referrer-url", "", "Referrer url")
	cmd.Flags().StringVar(&bodyState, "state", "", "State")
	cmd.Flags().StringVar(&bodyStreet, "street", "", "Street")
	cmd.Flags().StringVar(&bodyWebsite, "website", "", "Website")
	cmd.Flags().StringVar(&bodyZip, "zip", "", "Zip")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
