// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newDepartmentsAgentsGetInDepartmentCmd(flags *rootFlags) *cobra.Command {
	var flagFieldName string
	var flagSearchStr string
	var flagIsLightAgent bool
	var flagSortOrder string
	var flagLimit int
	var flagFrom int
	var flagIsConfirmed bool
	var flagStatus string
	var flagDepartmentId string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-in-department",
		Aliases:     []string{"get"},
		Short:       "This API lists the agents in a department.",
		Example:     "  zoho-desk-pp-cli departments agents get-in-department",
		Annotations: map[string]string{"pp:endpoint": "agents.get-in-department", "pp:method": "GET", "pp:path": "/api/v1/departments/{departmentId}/agents", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("field-name") {
				allowedFieldName := []string{"firstName", "lastName", "emailId", "name"}
				validFieldName := false
				for _, v := range allowedFieldName {
					if flagFieldName == v {
						validFieldName = true
						break
					}
				}
				if !validFieldName {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "field-name", flagFieldName, allowedFieldName)
				}
			}
			if cmd.Flags().Changed("sort-order") {
				allowedSortOrder := []string{"asc", "desc"}
				validSortOrder := false
				for _, v := range allowedSortOrder {
					if flagSortOrder == v {
						validSortOrder = true
						break
					}
				}
				if !validSortOrder {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort-order", flagSortOrder, allowedSortOrder)
				}
			}
			if cmd.Flags().Changed("status") {
				allowedStatus := []string{"ACTIVE", "DISABLED"}
				validStatus := false
				for _, v := range allowedStatus {
					if flagStatus == v {
						validStatus = true
						break
					}
				}
				if !validStatus {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "status", flagStatus, allowedStatus)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/departments/{departmentId}/agents"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "agents", path, map[string]string{
				"fieldName":    fmt.Sprintf("%v", flagFieldName),
				"searchStr":    fmt.Sprintf("%v", flagSearchStr),
				"isLightAgent": fmt.Sprintf("%v", flagIsLightAgent),
				"sortOrder":    fmt.Sprintf("%v", flagSortOrder),
				"limit":        fmt.Sprintf("%v", flagLimit),
				"from":         fmt.Sprintf("%v", flagFrom),
				"isConfirmed":  fmt.Sprintf("%v", flagIsConfirmed),
				"status":       fmt.Sprintf("%v", flagStatus),
				"departmentId": fmt.Sprintf("%v", flagDepartmentId),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagFieldName, "field-name", "", "Name of the field that must be used for searching and listing agents. Values allowed are: @firstName@, @lastName@,... (one of: firstName, lastName, emailId, name)")
	cmd.Flags().StringVar(&flagSearchStr, "search-str", "", "String to search for agents by first name, last name, or email ID. The string must contain at least one character....")
	cmd.Flags().BoolVar(&flagIsLightAgent, "is-light-agent", false, "Key that denotes if the agents must be light or non light.")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "", "To sort the available list of agents in either ascending or descending order. Values allowed are: @asc@ or @desc@ (one of: asc, desc)")
	cmd.Flags().IntVar(&flagLimit, "limit", 0, "Number of accounts to fetch")
	cmd.Flags().IntVar(&flagFrom, "from", 1, "Index number, starting from which the accounts must be fetched")
	cmd.Flags().BoolVar(&flagIsConfirmed, "is-confirmed", false, "Key that denotes if the agents must be confirmed or unconfirmed.")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Key that filters agents, based on their activation status: @ACTIVE@ or @DISABLED@ (one of: ACTIVE, DISABLED)")
	cmd.Flags().StringVar(&flagDepartmentId, "department-id", "", "Unique ID of the department")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
