// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTicketsCountByFieldValuesPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagDepartmentId string
	var flagCreatedTimeRange string
	var flagModifiedTimeRange string
	var flagCategory string
	var flagAssigneeId string
	var flagAccountId string
	var flagField string

	cmd := &cobra.Command{
		Use:         "tickets-count-by-field-values <contactId>",
		Short:       "This API returns the ticket count of your help desk, filtered by a specific field.",
		Long:        "Shortcut for 'tickets-count-by-field-values tickets_count_by_field_values'. This API returns the ticket count of your help desk, filtered by a specific field.",
		Example:     "  zoho-desk-pp-cli tickets-count-by-field-values 42 --field example-value",
		Annotations: map[string]string{"pp:endpoint": "tickets-count-by-field-values.tickets_count_by_field_values", "pp:method": "GET", "pp:path": "/api/v1/ticketsCountByFieldValues", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("field") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "field")
			}
			if cmd.Flags().Changed("field") {
				allowedField := []string{"statusType", "status", "priority", "channel", "spam", "overDue", "escalated"}
				validField := false
				for _, v := range allowedField {
					if flagField == v {
						validField = true
						break
					}
				}
				if !validField {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "field", flagField, allowedField)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/ticketsCountByFieldValues"
			if len(args) < 1 {
				// JSON envelope: {error, usage}. Written first; the
				// usageErr return preserves exit code 2 across modes.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "contactId is required",
						"usage": fmt.Sprintf("%s <%s>", cmd.CommandPath(), "contactId"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("contactId is required\nUsage: %s <%s>", cmd.CommandPath(), "contactId"))
			}
			params := map[string]string{}
			params["contactId"] = args[0]
			if flagDepartmentId != "" {
				params["departmentId"] = fmt.Sprintf("%v", flagDepartmentId)
			}
			if flagCreatedTimeRange != "" {
				params["createdTimeRange"] = fmt.Sprintf("%v", flagCreatedTimeRange)
			}
			if flagModifiedTimeRange != "" {
				params["modifiedTimeRange"] = fmt.Sprintf("%v", flagModifiedTimeRange)
			}
			if flagCategory != "" {
				params["category"] = fmt.Sprintf("%v", flagCategory)
			}
			if flagAssigneeId != "" {
				params["assigneeId"] = fmt.Sprintf("%v", flagAssigneeId)
			}
			if flagAccountId != "" {
				params["accountId"] = fmt.Sprintf("%v", flagAccountId)
			}
			if flagField != "" {
				params["field"] = fmt.Sprintf("%v", flagField)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "tickets-count-by-field-values", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDepartmentId, "department-id", "", "Unique ID of the department")
	cmd.Flags().StringVar(&flagCreatedTimeRange, "created-time-range", "", "Key that filters tickets created in a particular period of time. !Enter the from and to dates in the ISO date format...")
	cmd.Flags().StringVar(&flagModifiedTimeRange, "modified-time-range", "", "Key that filters tickets modified in a particular period of time. !Enter the from and to dates in the ISO date...")
	cmd.Flags().StringVar(&flagCategory, "category", "", "Category of the ticket")
	cmd.Flags().StringVar(&flagAssigneeId, "assignee-id", "", "ID of the agent who is assigned to resolve the ticket !@exact match@!")
	cmd.Flags().StringVar(&flagAccountId, "account-id", "", "ID of the account to which the ticket is mapped")
	cmd.Flags().StringVar(&flagField, "field", "", "Field by which the ticket count must be filtered. Values allowed are. !@status,@! !@priority,@! !@channel,@!... (one of: statusType, status, priority, channel, spam, overDue, escalated)")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
