// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newRoutingPreferencesUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAssignBacklog bool
	var bodyAssignBacklogsBy string
	var bodyAssignToOfflineAgents bool
	var bodyBackLogLimit int
	var bodyCustomThresholdVsAgents string
	var bodyDepartmentId int
	var bodyFreshTicketAssignMode string
	var bodyIsCustomThresholdEnabled bool
	var bodyIsImmediateAssignmentEnabled bool
	var bodyIsThresholdEnabled bool
	var bodyThresholdLimit int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "This API is used to Update the Routing Preferences for a Department",
		Example:     "  zoho-desk-pp-cli routing-preferences update",
		Annotations: map[string]string{"pp:endpoint": "routing-preferences.update", "pp:method": "PATCH", "pp:path": "/api/v1/routingPreferences"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("department-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "department-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/routingPreferences"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAssignBacklog != false {
					body["assignBacklog"] = bodyAssignBacklog
				}
				if bodyAssignBacklogsBy != "" {
					body["assignBacklogsBy"] = bodyAssignBacklogsBy
				}
				if bodyAssignToOfflineAgents != false {
					body["assignToOfflineAgents"] = bodyAssignToOfflineAgents
				}
				if bodyBackLogLimit != 0 {
					body["backLogLimit"] = bodyBackLogLimit
				}
				if bodyCustomThresholdVsAgents != "" {
					var parsedCustomThresholdVsAgents any
					if err := json.Unmarshal([]byte(bodyCustomThresholdVsAgents), &parsedCustomThresholdVsAgents); err != nil {
						return fmt.Errorf("parsing --custom-threshold-vs-agents JSON: %w", err)
					}
					body["customThresholdVsAgents"] = parsedCustomThresholdVsAgents
				}
				if bodyDepartmentId != 0 {
					body["departmentId"] = bodyDepartmentId
				}
				if bodyFreshTicketAssignMode != "" {
					body["freshTicketAssignMode"] = bodyFreshTicketAssignMode
				}
				if bodyIsCustomThresholdEnabled != false {
					body["isCustomThresholdEnabled"] = bodyIsCustomThresholdEnabled
				}
				if bodyIsImmediateAssignmentEnabled != false {
					body["isImmediateAssignmentEnabled"] = bodyIsImmediateAssignmentEnabled
				}
				if bodyIsThresholdEnabled != false {
					body["isThresholdEnabled"] = bodyIsThresholdEnabled
				}
				if bodyThresholdLimit != 0 {
					body["thresholdLimit"] = bodyThresholdLimit
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "routing-preferences",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAssignBacklog, "assign-backlog", false, "Assign backlog")
	cmd.Flags().StringVar(&bodyAssignBacklogsBy, "assign-backlogs-by", "", "Assign backlogs by")
	cmd.Flags().BoolVar(&bodyAssignToOfflineAgents, "assign-to-offline-agents", false, "Assign to offline agents")
	cmd.Flags().IntVar(&bodyBackLogLimit, "back-log-limit", 0, "Back log limit")
	cmd.Flags().StringVar(&bodyCustomThresholdVsAgents, "custom-threshold-vs-agents", "", "Custom threshold vs agents")
	cmd.Flags().IntVar(&bodyDepartmentId, "department-id", 0, "Department id")
	cmd.Flags().StringVar(&bodyFreshTicketAssignMode, "fresh-ticket-assign-mode", "", "Fresh ticket assign mode")
	cmd.Flags().BoolVar(&bodyIsCustomThresholdEnabled, "is-custom-threshold-enabled", false, "Is custom threshold enabled")
	cmd.Flags().BoolVar(&bodyIsImmediateAssignmentEnabled, "is-immediate-assignment-enabled", false, "Is immediate assignment enabled")
	cmd.Flags().BoolVar(&bodyIsThresholdEnabled, "is-threshold-enabled", false, "Is threshold enabled")
	cmd.Flags().IntVar(&bodyThresholdLimit, "threshold-limit", 0, "Threshold limit")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
