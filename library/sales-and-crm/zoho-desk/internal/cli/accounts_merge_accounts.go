// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsMergeAccountsCmd(flags *rootFlags) *cobra.Command {
	var bodyIds string
	var bodySourceAnnualrevenue int
	var bodySourceCf string
	var bodySourceCity int
	var bodySourceCode int
	var bodySourceCountry int
	var bodySourceCustomFields string
	var bodySourceDescription int
	var bodySourceEmail int
	var bodySourceFax int
	var bodySourceIndustry int
	var bodySourcePhone int
	var bodySourceState int
	var bodySourceStreet int
	var bodySourceWebsite int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "accounts <accountId>",
		Aliases:     []string{"create"},
		Short:       "This API merges two or more accounts.",
		Example:     "  zoho-desk-pp-cli accounts merge accounts 50",
		Annotations: map[string]string{"pp:endpoint": "merge.accounts", "pp:method": "POST", "pp:path": "/api/v1/accounts/{accountId}/merge"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("ids") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "ids")
				}
				if !cmd.Flags().Changed("source-annualrevenue") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-annualrevenue")
				}
				if !cmd.Flags().Changed("source-cf") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-cf")
				}
				if !cmd.Flags().Changed("source-city") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-city")
				}
				if !cmd.Flags().Changed("source-code") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-code")
				}
				if !cmd.Flags().Changed("source-country") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-country")
				}
				if !cmd.Flags().Changed("source-custom-fields") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-custom-fields")
				}
				if !cmd.Flags().Changed("source-description") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-description")
				}
				if !cmd.Flags().Changed("source-email") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-email")
				}
				if !cmd.Flags().Changed("source-fax") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-fax")
				}
				if !cmd.Flags().Changed("source-industry") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-industry")
				}
				if !cmd.Flags().Changed("source-phone") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-phone")
				}
				if !cmd.Flags().Changed("source-state") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-state")
				}
				if !cmd.Flags().Changed("source-street") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-street")
				}
				if !cmd.Flags().Changed("source-website") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-website")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/accounts/{accountId}/merge"
			path = replacePathParam(path, "accountId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyIds != "" {
					var parsedIds any
					if err := json.Unmarshal([]byte(bodyIds), &parsedIds); err != nil {
						return fmt.Errorf("parsing --ids JSON: %w", err)
					}
					body["ids"] = parsedIds
				}
				{
					nestedSource := map[string]any{}
					if bodySourceAnnualrevenue != 0 {
						nestedSource["annualrevenue"] = bodySourceAnnualrevenue
					}
					if bodySourceCf != "" {
						var parsedSourceCf any
						if err := json.Unmarshal([]byte(bodySourceCf), &parsedSourceCf); err != nil {
							return fmt.Errorf("parsing --source-cf JSON: %w", err)
						}
						nestedSource["cf"] = parsedSourceCf
					}
					if bodySourceCity != 0 {
						nestedSource["city"] = bodySourceCity
					}
					if bodySourceCode != 0 {
						nestedSource["code"] = bodySourceCode
					}
					if bodySourceCountry != 0 {
						nestedSource["country"] = bodySourceCountry
					}
					if bodySourceCustomFields != "" {
						var parsedSourceCustomFields any
						if err := json.Unmarshal([]byte(bodySourceCustomFields), &parsedSourceCustomFields); err != nil {
							return fmt.Errorf("parsing --source-custom-fields JSON: %w", err)
						}
						nestedSource["customFields"] = parsedSourceCustomFields
					}
					if bodySourceDescription != 0 {
						nestedSource["description"] = bodySourceDescription
					}
					if bodySourceEmail != 0 {
						nestedSource["email"] = bodySourceEmail
					}
					if bodySourceFax != 0 {
						nestedSource["fax"] = bodySourceFax
					}
					if bodySourceIndustry != 0 {
						nestedSource["industry"] = bodySourceIndustry
					}
					if bodySourcePhone != 0 {
						nestedSource["phone"] = bodySourcePhone
					}
					if bodySourceState != 0 {
						nestedSource["state"] = bodySourceState
					}
					if bodySourceStreet != 0 {
						nestedSource["street"] = bodySourceStreet
					}
					if bodySourceWebsite != 0 {
						nestedSource["website"] = bodySourceWebsite
					}
					if len(nestedSource) > 0 {
						body["source"] = nestedSource
					}
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "merge",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyIds, "ids", "", "Ids")
	cmd.Flags().IntVar(&bodySourceAnnualrevenue, "source-annualrevenue", 0, "Annualrevenue")
	cmd.Flags().StringVar(&bodySourceCf, "source-cf", "", "Cf")
	cmd.Flags().IntVar(&bodySourceCity, "source-city", 0, "City")
	cmd.Flags().IntVar(&bodySourceCode, "source-code", 0, "Code")
	cmd.Flags().IntVar(&bodySourceCountry, "source-country", 0, "Country")
	cmd.Flags().StringVar(&bodySourceCustomFields, "source-custom-fields", "", "Custom fields")
	cmd.Flags().IntVar(&bodySourceDescription, "source-description", 0, "Description")
	cmd.Flags().IntVar(&bodySourceEmail, "source-email", 0, "Email")
	cmd.Flags().IntVar(&bodySourceFax, "source-fax", 0, "Fax")
	cmd.Flags().IntVar(&bodySourceIndustry, "source-industry", 0, "Industry")
	cmd.Flags().IntVar(&bodySourcePhone, "source-phone", 0, "Phone")
	cmd.Flags().IntVar(&bodySourceState, "source-state", 0, "State")
	cmd.Flags().IntVar(&bodySourceStreet, "source-street", 0, "Street")
	cmd.Flags().IntVar(&bodySourceWebsite, "source-website", 0, "Website")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
