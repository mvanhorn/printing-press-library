// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newKbSectionsAddCmd(flags *rootFlags) *cobra.Command {
	var bodyArticleViewType string
	var bodyAssociatedDepartmentIds string
	var bodyDescription string
	var bodyFeedbackTicketConversionPreferenceAssigneeId int
	var bodyFeedbackTicketConversionPreferenceDepartmentId int
	var bodyFeedbackTicketConversionPreferenceStatus string
	var bodyGroupIds string
	var bodyIpAddresses string
	var bodyIsReviewEnabled bool
	var bodyLogoId int
	var bodyModerationType string
	var bodyName string
	var bodyPrimaryDepartmentId int
	var bodyStatus string
	var bodyTranslations string
	var bodyVisibility string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "add",
		Aliases:     []string{"create"},
		Short:       "This API creates a section in your helpdesk",
		Example:     "  zoho-desk-pp-cli kb-sections add --article-view-type example-value",
		Annotations: map[string]string{"pp:endpoint": "kb-sections.add", "pp:method": "POST", "pp:path": "/api/v1/kbSections"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("article-view-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "article-view-type")
				}
				if !cmd.Flags().Changed("description") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "description")
				}
				if !cmd.Flags().Changed("feedback-ticket-conversion-preference-assignee-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "feedback-ticket-conversion-preference-assignee-id")
				}
				if !cmd.Flags().Changed("feedback-ticket-conversion-preference-department-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "feedback-ticket-conversion-preference-department-id")
				}
				if !cmd.Flags().Changed("feedback-ticket-conversion-preference-status") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "feedback-ticket-conversion-preference-status")
				}
				if !cmd.Flags().Changed("group-ids") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "group-ids")
				}
				if !cmd.Flags().Changed("ip-addresses") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "ip-addresses")
				}
				if !cmd.Flags().Changed("is-review-enabled") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "is-review-enabled")
				}
				if !cmd.Flags().Changed("logo-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "logo-id")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("primary-department-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "primary-department-id")
				}
				if !cmd.Flags().Changed("status") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "status")
				}
				if !cmd.Flags().Changed("translations") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "translations")
				}
				if !cmd.Flags().Changed("visibility") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "visibility")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/kbSections"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyArticleViewType != "" {
					body["articleViewType"] = bodyArticleViewType
				}
				if bodyAssociatedDepartmentIds != "" {
					var parsedAssociatedDepartmentIds any
					if err := json.Unmarshal([]byte(bodyAssociatedDepartmentIds), &parsedAssociatedDepartmentIds); err != nil {
						return fmt.Errorf("parsing --associated-department-ids JSON: %w", err)
					}
					body["associatedDepartmentIds"] = parsedAssociatedDepartmentIds
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				{
					nestedFeedbackTicketConversionPreference := map[string]any{}
					if bodyFeedbackTicketConversionPreferenceAssigneeId != 0 {
						nestedFeedbackTicketConversionPreference["assigneeId"] = bodyFeedbackTicketConversionPreferenceAssigneeId
					}
					if bodyFeedbackTicketConversionPreferenceDepartmentId != 0 {
						nestedFeedbackTicketConversionPreference["departmentId"] = bodyFeedbackTicketConversionPreferenceDepartmentId
					}
					if bodyFeedbackTicketConversionPreferenceStatus != "" {
						nestedFeedbackTicketConversionPreference["status"] = bodyFeedbackTicketConversionPreferenceStatus
					}
					if len(nestedFeedbackTicketConversionPreference) > 0 {
						body["feedbackTicketConversionPreference"] = nestedFeedbackTicketConversionPreference
					}
				}
				if bodyGroupIds != "" {
					var parsedGroupIds any
					if err := json.Unmarshal([]byte(bodyGroupIds), &parsedGroupIds); err != nil {
						return fmt.Errorf("parsing --group-ids JSON: %w", err)
					}
					body["groupIds"] = parsedGroupIds
				}
				if bodyIpAddresses != "" {
					var parsedIpAddresses any
					if err := json.Unmarshal([]byte(bodyIpAddresses), &parsedIpAddresses); err != nil {
						return fmt.Errorf("parsing --ip-addresses JSON: %w", err)
					}
					body["ipAddresses"] = parsedIpAddresses
				}
				if bodyIsReviewEnabled != false {
					body["isReviewEnabled"] = bodyIsReviewEnabled
				}
				if bodyLogoId != 0 {
					body["logoId"] = bodyLogoId
				}
				if bodyModerationType != "" {
					body["moderationType"] = bodyModerationType
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPrimaryDepartmentId != 0 {
					body["primaryDepartmentId"] = bodyPrimaryDepartmentId
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodyTranslations != "" {
					var parsedTranslations any
					if err := json.Unmarshal([]byte(bodyTranslations), &parsedTranslations); err != nil {
						return fmt.Errorf("parsing --translations JSON: %w", err)
					}
					body["translations"] = parsedTranslations
				}
				if bodyVisibility != "" {
					body["visibility"] = bodyVisibility
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "kb-sections",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyArticleViewType, "article-view-type", "", "View to apply while listing help articles. Values allowed are: LISTVIEW and OVERVIEW.")
	cmd.Flags().StringVar(&bodyAssociatedDepartmentIds, "associated-department-ids", "", "Associated department ids")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A brief description of the category")
	cmd.Flags().IntVar(&bodyFeedbackTicketConversionPreferenceAssigneeId, "feedback-ticket-conversion-preference-assignee-id", 0, "ID of the user to whom the tickets must be assigned. If you do not pass a value for this key, the ticket remains...")
	cmd.Flags().IntVar(&bodyFeedbackTicketConversionPreferenceDepartmentId, "feedback-ticket-conversion-preference-department-id", 0, "ID of the department in which the tickets must be created")
	cmd.Flags().StringVar(&bodyFeedbackTicketConversionPreferenceStatus, "feedback-ticket-conversion-preference-status", "", "Activation status of auto-conversion for the category. Values allowed are: ENABLED and DISABLED")
	cmd.Flags().StringVar(&bodyGroupIds, "group-ids", "", "Group ids")
	cmd.Flags().StringVar(&bodyIpAddresses, "ip-addresses", "", "Ip addresses")
	cmd.Flags().BoolVar(&bodyIsReviewEnabled, "is-review-enabled", false, "Key that specifies whether the category must be reviewed or not")
	cmd.Flags().IntVar(&bodyLogoId, "logo-id", 0, "ID of the logo to set for the category")
	cmd.Flags().StringVar(&bodyModerationType, "moderation-type", "", "Type of moderation applied to the category. Values allowed are: @NONE@, @ALL_ACTION@, @FIRST_ACTION@")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name of the category")
	cmd.Flags().IntVar(&bodyPrimaryDepartmentId, "primary-department-id", 0, "ID of the department to which the category belongs")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Activation status of the category")
	cmd.Flags().StringVar(&bodyTranslations, "translations", "", "Translations")
	cmd.Flags().StringVar(&bodyVisibility, "visibility", "", "Visibility setting of the category. Values allowed are: AGENTS, GROUP_USERS, ALL_USERS, NONE, and PRIVATE_IP.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
