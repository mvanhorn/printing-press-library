// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newLayoutsLayoutRulesCreateCmd(flags *rootFlags) *cobra.Command {
	var flagLayoutId string
	var bodyCreatedBy int
	var bodyCreatedTime string
	var bodyDescription string
	var bodyDisplayLabel string
	var bodyExecuteInHelpCentre bool
	var bodyFieldConditions string
	var bodyFieldName string
	var bodyModifiedBy int
	var bodyModifiedTime string
	var bodyName string
	var bodyStatus string
	var bodySystemGenerated bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "This API creates a rule for a field in a layout.",
		Example:     "  zoho-desk-pp-cli layouts layout-rules create --layout-id 42 --description example-value",
		Annotations: map[string]string{"pp:endpoint": "layout-rules.create", "pp:method": "POST", "pp:path": "/api/v1/layouts/{layoutId}/layoutRules"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("layout-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "layout-id")
			}
			if !stdinBody {
				if !cmd.Flags().Changed("description") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "description")
				}
				if !cmd.Flags().Changed("field-conditions") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "field-conditions")
				}
				if !cmd.Flags().Changed("field-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "field-name")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/layouts/{layoutId}/layoutRules"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCreatedBy != 0 {
					body["createdBy"] = bodyCreatedBy
				}
				if bodyCreatedTime != "" {
					body["createdTime"] = bodyCreatedTime
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyDisplayLabel != "" {
					body["displayLabel"] = bodyDisplayLabel
				}
				if bodyExecuteInHelpCentre != false {
					body["executeInHelpCentre"] = bodyExecuteInHelpCentre
				}
				if bodyFieldConditions != "" {
					var parsedFieldConditions any
					if err := json.Unmarshal([]byte(bodyFieldConditions), &parsedFieldConditions); err != nil {
						return fmt.Errorf("parsing --field-conditions JSON: %w", err)
					}
					body["fieldConditions"] = parsedFieldConditions
				}
				if bodyFieldName != "" {
					body["fieldName"] = bodyFieldName
				}
				if bodyModifiedBy != 0 {
					body["modifiedBy"] = bodyModifiedBy
				}
				if bodyModifiedTime != "" {
					body["modifiedTime"] = bodyModifiedTime
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodySystemGenerated != false {
					body["systemGenerated"] = bodySystemGenerated
				}
			}
			params := map[string]string{}
			if flagLayoutId != "" {
				params["layoutId"] = fmt.Sprintf("%v", flagLayoutId)
			}
			data, statusCode, err := c.PostWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "layout-rules",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagLayoutId, "layout-id", "", "ID of the layout in which the dependency is mapped")
	cmd.Flags().IntVar(&bodyCreatedBy, "created-by", 0, "Created by")
	cmd.Flags().StringVar(&bodyCreatedTime, "created-time", "", "Created time")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyDisplayLabel, "display-label", "", "Display label")
	cmd.Flags().BoolVar(&bodyExecuteInHelpCentre, "execute-in-help-centre", false, "Execute in help centre")
	cmd.Flags().StringVar(&bodyFieldConditions, "field-conditions", "", "Field conditions")
	cmd.Flags().StringVar(&bodyFieldName, "field-name", "", "Field name")
	cmd.Flags().IntVar(&bodyModifiedBy, "modified-by", 0, "Modified by")
	cmd.Flags().StringVar(&bodyModifiedTime, "modified-time", "", "Modified time")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Status")
	cmd.Flags().BoolVar(&bodySystemGenerated, "system-generated", false, "System generated")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
