// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newSupportPlansCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyCreditDetailsCreditCycle string
	var bodyCreditDetailsCreditLimit int
	var bodyCreditDetailsIsAdditionalUsageAllowed bool
	var bodyCreditDetailsIsCarryForwardAllowed bool
	var bodyDepartmentId int
	var bodyName string
	var bodySlaId int
	var bodyStatus string
	var bodySupportType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "This API Creates a Support Plan",
		Example:     "  zoho-desk-pp-cli support-plans create --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "support-plans.create", "pp:method": "POST", "pp:path": "/api/v1/supportPlans"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("credit-details-credit-cycle") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "credit-details-credit-cycle")
				}
				if !cmd.Flags().Changed("credit-details-credit-limit") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "credit-details-credit-limit")
				}
				if !cmd.Flags().Changed("credit-details-is-additional-usage-allowed") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "credit-details-is-additional-usage-allowed")
				}
				if !cmd.Flags().Changed("credit-details-is-carry-forward-allowed") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "credit-details-is-carry-forward-allowed")
				}
				if !cmd.Flags().Changed("department-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "department-id")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("sla-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "sla-id")
				}
				if !cmd.Flags().Changed("status") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "status")
				}
				if !cmd.Flags().Changed("support-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "support-type")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/supportPlans"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedCreditDetails := map[string]any{}
					if bodyCreditDetailsCreditCycle != "" {
						nestedCreditDetails["creditCycle"] = bodyCreditDetailsCreditCycle
					}
					if bodyCreditDetailsCreditLimit != 0 {
						nestedCreditDetails["creditLimit"] = bodyCreditDetailsCreditLimit
					}
					if bodyCreditDetailsIsAdditionalUsageAllowed != false {
						nestedCreditDetails["isAdditionalUsageAllowed"] = bodyCreditDetailsIsAdditionalUsageAllowed
					}
					if bodyCreditDetailsIsCarryForwardAllowed != false {
						nestedCreditDetails["isCarryForwardAllowed"] = bodyCreditDetailsIsCarryForwardAllowed
					}
					if len(nestedCreditDetails) > 0 {
						body["creditDetails"] = nestedCreditDetails
					}
				}
				if bodyDepartmentId != 0 {
					body["departmentId"] = bodyDepartmentId
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodySlaId != 0 {
					body["slaId"] = bodySlaId
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodySupportType != "" {
					body["supportType"] = bodySupportType
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "support-plans",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCreditDetailsCreditCycle, "credit-details-credit-cycle", "", "Credit cycle")
	cmd.Flags().IntVar(&bodyCreditDetailsCreditLimit, "credit-details-credit-limit", 0, "Credit limit")
	cmd.Flags().BoolVar(&bodyCreditDetailsIsAdditionalUsageAllowed, "credit-details-is-additional-usage-allowed", false, "Is additional usage allowed")
	cmd.Flags().BoolVar(&bodyCreditDetailsIsCarryForwardAllowed, "credit-details-is-carry-forward-allowed", false, "Is carry forward allowed")
	cmd.Flags().IntVar(&bodyDepartmentId, "department-id", 0, "Department id")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().IntVar(&bodySlaId, "sla-id", 0, "Sla id")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Status")
	cmd.Flags().StringVar(&bodySupportType, "support-type", "", "Support type")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
