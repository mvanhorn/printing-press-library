// Copyright 2026 dan-bronson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTemplateFoldersAddCmd(flags *rootFlags) *cobra.Command {
	var bodyDepartmentId int
	var bodyFolderAccessDetailsAgents string
	var bodyFolderAccessDetailsRoles string
	var bodyFolderAccessDetailsRolesAndSubordinates string
	var bodyFolderAccessDetailsTeams string
	var bodyFolderAccessDetailsType string
	var bodyFolderDesc string
	var bodyFolderName string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "add",
		Aliases:     []string{"create"},
		Short:       "Adding a Template Folder",
		Example:     "  zoho-desk-pp-cli template-folders add --folder-name example-resource",
		Annotations: map[string]string{"pp:endpoint": "template-folders.add", "pp:method": "POST", "pp:path": "/api/v1/templateFolders"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("department-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "department-id")
				}
				if !cmd.Flags().Changed("folder-access-details-agents") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "folder-access-details-agents")
				}
				if !cmd.Flags().Changed("folder-access-details-roles") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "folder-access-details-roles")
				}
				if !cmd.Flags().Changed("folder-access-details-roles-and-subordinates") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "folder-access-details-roles-and-subordinates")
				}
				if !cmd.Flags().Changed("folder-access-details-teams") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "folder-access-details-teams")
				}
				if !cmd.Flags().Changed("folder-access-details-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "folder-access-details-type")
				}
				if !cmd.Flags().Changed("folder-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "folder-name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/templateFolders"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyDepartmentId != 0 {
					body["departmentId"] = bodyDepartmentId
				}
				{
					nestedFolderAccessDetails := map[string]any{}
					if bodyFolderAccessDetailsAgents != "" {
						var parsedFolderAccessDetailsAgents any
						if err := json.Unmarshal([]byte(bodyFolderAccessDetailsAgents), &parsedFolderAccessDetailsAgents); err != nil {
							return fmt.Errorf("parsing --folder-access-details-agents JSON: %w", err)
						}
						nestedFolderAccessDetails["agents"] = parsedFolderAccessDetailsAgents
					}
					if bodyFolderAccessDetailsRoles != "" {
						var parsedFolderAccessDetailsRoles any
						if err := json.Unmarshal([]byte(bodyFolderAccessDetailsRoles), &parsedFolderAccessDetailsRoles); err != nil {
							return fmt.Errorf("parsing --folder-access-details-roles JSON: %w", err)
						}
						nestedFolderAccessDetails["roles"] = parsedFolderAccessDetailsRoles
					}
					if bodyFolderAccessDetailsRolesAndSubordinates != "" {
						var parsedFolderAccessDetailsRolesAndSubordinates any
						if err := json.Unmarshal([]byte(bodyFolderAccessDetailsRolesAndSubordinates), &parsedFolderAccessDetailsRolesAndSubordinates); err != nil {
							return fmt.Errorf("parsing --folder-access-details-roles-and-subordinates JSON: %w", err)
						}
						nestedFolderAccessDetails["rolesAndSubordinates"] = parsedFolderAccessDetailsRolesAndSubordinates
					}
					if bodyFolderAccessDetailsTeams != "" {
						var parsedFolderAccessDetailsTeams any
						if err := json.Unmarshal([]byte(bodyFolderAccessDetailsTeams), &parsedFolderAccessDetailsTeams); err != nil {
							return fmt.Errorf("parsing --folder-access-details-teams JSON: %w", err)
						}
						nestedFolderAccessDetails["teams"] = parsedFolderAccessDetailsTeams
					}
					if bodyFolderAccessDetailsType != "" {
						nestedFolderAccessDetails["type"] = bodyFolderAccessDetailsType
					}
					if len(nestedFolderAccessDetails) > 0 {
						body["folderAccessDetails"] = nestedFolderAccessDetails
					}
				}
				if bodyFolderDesc != "" {
					body["folderDesc"] = bodyFolderDesc
				}
				if bodyFolderName != "" {
					body["folderName"] = bodyFolderName
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "template-folders",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyDepartmentId, "department-id", 0, "Department id")
	cmd.Flags().StringVar(&bodyFolderAccessDetailsAgents, "folder-access-details-agents", "", "Agents")
	cmd.Flags().StringVar(&bodyFolderAccessDetailsRoles, "folder-access-details-roles", "", "Roles")
	cmd.Flags().StringVar(&bodyFolderAccessDetailsRolesAndSubordinates, "folder-access-details-roles-and-subordinates", "", "Roles and subordinates")
	cmd.Flags().StringVar(&bodyFolderAccessDetailsTeams, "folder-access-details-teams", "", "Teams")
	cmd.Flags().StringVar(&bodyFolderAccessDetailsType, "folder-access-details-type", "", "Type")
	cmd.Flags().StringVar(&bodyFolderDesc, "folder-desc", "", "Folder desc")
	cmd.Flags().StringVar(&bodyFolderName, "folder-name", "", "Folder name")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
