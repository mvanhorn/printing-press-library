// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAdPublishingGoogleUpsertConversionCmd(flags *rootFlags) *cobra.Command {
	var bodyAttributionModel string
	var bodyCategory string
	var bodyClickThroughWindow float64
	var bodyConversionId string
	var bodyCountingType string
	var bodyLocationId string
	var bodyName string
	var bodyType string
	var bodyValueSettings string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "google-upsert-conversion",
		Short:       "Create or update a Google Ads conversion action",
		Example:     "  gohighlevel-pp-cli ad-publishing google-upsert-conversion --attribution-model example-value",
		Annotations: map[string]string{"pp:endpoint": "ad-publishing.google-upsert-conversion", "pp:method": "PUT", "pp:path": "/ad-publishing/google/conversions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("attribution-model") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "attribution-model")
				}
				if !cmd.Flags().Changed("category") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "category")
				}
				if !cmd.Flags().Changed("click-through-window") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "click-through-window")
				}
				if !cmd.Flags().Changed("counting-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "counting-type")
				}
				if !cmd.Flags().Changed("location-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "location-id")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "type")
				}
				if !cmd.Flags().Changed("value-settings") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "value-settings")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ad-publishing/google/conversions"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAttributionModel != "" {
					body["attributionModel"] = bodyAttributionModel
				}
				if bodyCategory != "" {
					body["category"] = bodyCategory
				}
				if bodyClickThroughWindow != 0.0 {
					body["clickThroughWindow"] = bodyClickThroughWindow
				}
				if bodyConversionId != "" {
					body["conversionId"] = bodyConversionId
				}
				if bodyCountingType != "" {
					body["countingType"] = bodyCountingType
				}
				if bodyLocationId != "" {
					body["locationId"] = bodyLocationId
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
				if bodyValueSettings != "" {
					var parsedValueSettings any
					if err := json.Unmarshal([]byte(bodyValueSettings), &parsedValueSettings); err != nil {
						return fmt.Errorf("parsing --value-settings JSON: %w", err)
					}
					body["valueSettings"] = parsedValueSettings
				}
			}
			data, statusCode, err := c.Put(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "ad-publishing",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAttributionModel, "attribution-model", "", "Attribution model used to credit conversions")
	cmd.Flags().StringVar(&bodyCategory, "category", "", "Conversion category")
	cmd.Flags().Float64Var(&bodyClickThroughWindow, "click-through-window", 0.0, "Click-through conversion window in days")
	cmd.Flags().StringVar(&bodyConversionId, "conversion-id", "", "Conversion identifier")
	cmd.Flags().StringVar(&bodyCountingType, "counting-type", "", "How conversions are counted per interaction")
	cmd.Flags().StringVar(&bodyLocationId, "location-id", "", "Location identifier")
	cmd.Flags().StringVar(&bodyName, "name", "", "Conversion name")
	cmd.Flags().StringVar(&bodyType, "type", "", "Conversion type")
	cmd.Flags().StringVar(&bodyValueSettings, "value-settings", "", "Value settings that control how monetary value is attributed to conversions")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
