// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newConversationAiSearchAgentCmd(flags *rootFlags) *cobra.Command {
	var flagStartAfter string
	var flagLimit float64
	var flagQuery string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "search-agent",
		Short:       "Searches for AI agents based on various criteria including name, status, and configuration. Supports advanced...",
		Example:     "  gohighlevel-pp-cli conversation-ai search-agent",
		Annotations: map[string]string{"pp:endpoint": "conversation-ai.search-agent", "pp:method": "GET", "pp:path": "/conversation-ai/agents/search", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/conversation-ai/agents/search"
			headerOverrides := map[string]string{
				"Version": "2021-04-15",
			}
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "conversation-ai", path, map[string]string{
				"startAfter": fmt.Sprintf("%v", flagStartAfter),
				"limit":      fmt.Sprintf("%v", flagLimit),
				"query":      fmt.Sprintf("%v", flagQuery),
			}, headerOverrides, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagStartAfter, "start-after", "", "Start after is the agent id to start after, Serving as skip, send empty when first page")
	cmd.Flags().Float64Var(&flagLimit, "limit", 0.0, "Records per page")
	cmd.Flags().StringVar(&flagQuery, "query", "", "query to search on agent name, must be provided in lowercase")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
