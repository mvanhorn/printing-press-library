// Copyright 2026 matthew.martin and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/commerce/square/internal/cliutil"
	"github.com/spf13/cobra"
)

func newTeamMembersCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyIdempotencyKey string
	var bodyTeamMemberAssignedLocationsAssignmentType string
	var bodyTeamMemberAssignedLocationsLocationIds string
	var bodyTeamMemberCreatedAt string
	var bodyTeamMemberEmailAddress string
	var bodyTeamMemberFamilyName string
	var bodyTeamMemberGivenName string
	var bodyTeamMemberId string
	var bodyTeamMemberIsOwner bool
	var bodyTeamMemberPhoneNumber string
	var bodyTeamMemberReferenceId string
	var bodyTeamMemberStatus string
	var bodyTeamMemberUpdatedAt string
	var bodyTeamMemberWageSettingCreatedAt string
	var bodyTeamMemberWageSettingIsOvertimeExempt bool
	var bodyTeamMemberWageSettingJobAssignments string
	var bodyTeamMemberWageSettingTeamMemberId string
	var bodyTeamMemberWageSettingUpdatedAt string
	var bodyTeamMemberWageSettingVersion int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Creates a single `TeamMember` object. The `TeamMember` object is returned on successful creates.",
		Example:     "  square-pp-cli team-members create",
		Annotations: map[string]string{"pp:endpoint": "team-members.create", "pp:method": "POST", "pp:path": "/v2/team-members"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/v2/team-members"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("idempotency-key") || bodyIdempotencyKey != "" {
					bodyMap["idempotency_key"] = bodyIdempotencyKey
				}
				{
					nestedTeamMember := map[string]any{}
					{
						nestedTeamMemberAssignedLocations := map[string]any{}
						if cmd.Flags().Changed("team-member-assigned-locations-assignment-type") || bodyTeamMemberAssignedLocationsAssignmentType != "" {
							nestedTeamMemberAssignedLocations["assignment_type"] = bodyTeamMemberAssignedLocationsAssignmentType
						}
						if cmd.Flags().Changed("team-member-assigned-locations-location-ids") {
							parsedTeamMemberAssignedLocationsLocationIds, parseErr := cliutil.ParseStringList(bodyTeamMemberAssignedLocationsLocationIds)
							if parseErr != nil {
								return fmt.Errorf("parsing --team-member-assigned-locations-location-ids list: %w", parseErr)
							}
							nestedTeamMemberAssignedLocations["location_ids"] = parsedTeamMemberAssignedLocationsLocationIds
						}
						if len(nestedTeamMemberAssignedLocations) > 0 {
							nestedTeamMember["assigned_locations"] = nestedTeamMemberAssignedLocations
						}
					}
					if cmd.Flags().Changed("team-member-created-at") || bodyTeamMemberCreatedAt != "" {
						nestedTeamMember["created_at"] = bodyTeamMemberCreatedAt
					}
					if cmd.Flags().Changed("team-member-email-address") || bodyTeamMemberEmailAddress != "" {
						nestedTeamMember["email_address"] = bodyTeamMemberEmailAddress
					}
					if cmd.Flags().Changed("team-member-family-name") || bodyTeamMemberFamilyName != "" {
						nestedTeamMember["family_name"] = bodyTeamMemberFamilyName
					}
					if cmd.Flags().Changed("team-member-given-name") || bodyTeamMemberGivenName != "" {
						nestedTeamMember["given_name"] = bodyTeamMemberGivenName
					}
					if cmd.Flags().Changed("team-member-id") || bodyTeamMemberId != "" {
						nestedTeamMember["id"] = bodyTeamMemberId
					}
					if cmd.Flags().Changed("team-member-is-owner") {
						nestedTeamMember["is_owner"] = bodyTeamMemberIsOwner
					}
					if cmd.Flags().Changed("team-member-phone-number") || bodyTeamMemberPhoneNumber != "" {
						nestedTeamMember["phone_number"] = bodyTeamMemberPhoneNumber
					}
					if cmd.Flags().Changed("team-member-reference-id") || bodyTeamMemberReferenceId != "" {
						nestedTeamMember["reference_id"] = bodyTeamMemberReferenceId
					}
					if cmd.Flags().Changed("team-member-status") || bodyTeamMemberStatus != "" {
						nestedTeamMember["status"] = bodyTeamMemberStatus
					}
					if cmd.Flags().Changed("team-member-updated-at") || bodyTeamMemberUpdatedAt != "" {
						nestedTeamMember["updated_at"] = bodyTeamMemberUpdatedAt
					}
					{
						nestedTeamMemberWageSetting := map[string]any{}
						if cmd.Flags().Changed("team-member-wage-setting-created-at") || bodyTeamMemberWageSettingCreatedAt != "" {
							nestedTeamMemberWageSetting["created_at"] = bodyTeamMemberWageSettingCreatedAt
						}
						if cmd.Flags().Changed("team-member-wage-setting-is-overtime-exempt") {
							nestedTeamMemberWageSetting["is_overtime_exempt"] = bodyTeamMemberWageSettingIsOvertimeExempt
						}
						if cmd.Flags().Changed("team-member-wage-setting-job-assignments") || bodyTeamMemberWageSettingJobAssignments != "" {
							var parsedTeamMemberWageSettingJobAssignments any
							if err := json.Unmarshal([]byte(bodyTeamMemberWageSettingJobAssignments), &parsedTeamMemberWageSettingJobAssignments); err != nil {
								return fmt.Errorf("parsing --team-member-wage-setting-job-assignments JSON: %w", err)
							}
							asArray, ok := parsedTeamMemberWageSettingJobAssignments.([]any)
							if !ok {
								return fmt.Errorf("--team-member-wage-setting-job-assignments must be a JSON array, got JSON %T", parsedTeamMemberWageSettingJobAssignments)
							}
							nestedTeamMemberWageSetting["job_assignments"] = asArray
						}
						if cmd.Flags().Changed("team-member-wage-setting-team-member-id") || bodyTeamMemberWageSettingTeamMemberId != "" {
							nestedTeamMemberWageSetting["team_member_id"] = bodyTeamMemberWageSettingTeamMemberId
						}
						if cmd.Flags().Changed("team-member-wage-setting-updated-at") || bodyTeamMemberWageSettingUpdatedAt != "" {
							nestedTeamMemberWageSetting["updated_at"] = bodyTeamMemberWageSettingUpdatedAt
						}
						if cmd.Flags().Changed("team-member-wage-setting-version") || bodyTeamMemberWageSettingVersion != 0 {
							nestedTeamMemberWageSetting["version"] = bodyTeamMemberWageSettingVersion
						}
						if len(nestedTeamMemberWageSetting) > 0 {
							nestedTeamMember["wage_setting"] = nestedTeamMemberWageSetting
						}
					}
					if len(nestedTeamMember) > 0 {
						bodyMap["team_member"] = nestedTeamMember
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "team-members", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "team-members", data, "errors")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "team-members", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "team-members", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "team-members", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "team-members",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "team-members", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "team-members", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyIdempotencyKey, "idempotency-key", "", "A unique string that identifies this `CreateTeamMember` request.")
	cmd.Flags().StringVar(&bodyTeamMemberAssignedLocationsAssignmentType, "team-member-assigned-locations-assignment-type", "", "Enumerates the possible assignment types that the team member can have.")
	cmd.Flags().StringVar(&bodyTeamMemberAssignedLocationsLocationIds, "team-member-assigned-locations-location-ids", "", "The explicit locations that the team member is assigned to.")
	cmd.Flags().StringVar(&bodyTeamMemberCreatedAt, "team-member-created-at", "", "The timestamp when the team member was created, in RFC 3339 format.")
	cmd.Flags().StringVar(&bodyTeamMemberEmailAddress, "team-member-email-address", "", "The email address associated with the team member.")
	cmd.Flags().StringVar(&bodyTeamMemberFamilyName, "team-member-family-name", "", "The family name (that is, the last name) associated with the team member.")
	cmd.Flags().StringVar(&bodyTeamMemberGivenName, "team-member-given-name", "", "The given name (that is, the first name) associated with the team member.")
	cmd.Flags().StringVar(&bodyTeamMemberId, "team-member-id", "", "The unique ID for the team member.")
	cmd.Flags().BoolVar(&bodyTeamMemberIsOwner, "team-member-is-owner", false, "Whether the team member is the owner of the Square account.")
	cmd.Flags().StringVar(&bodyTeamMemberPhoneNumber, "team-member-phone-number", "", "The team member's phone number, in E.164 format.")
	cmd.Flags().StringVar(&bodyTeamMemberReferenceId, "team-member-reference-id", "", "A second ID used to associate the team member with an entity in another system.")
	cmd.Flags().StringVar(&bodyTeamMemberStatus, "team-member-status", "", "Enumerates the possible statuses the team member can have within a business.")
	cmd.Flags().StringVar(&bodyTeamMemberUpdatedAt, "team-member-updated-at", "", "The timestamp when the team member was last updated, in RFC 3339 format.")
	cmd.Flags().StringVar(&bodyTeamMemberWageSettingCreatedAt, "team-member-wage-setting-created-at", "", "The timestamp when the wage setting was created, in RFC 3339 format.")
	cmd.Flags().BoolVar(&bodyTeamMemberWageSettingIsOvertimeExempt, "team-member-wage-setting-is-overtime-exempt", false, "Whether the team member is exempt from the overtime rules of the seller's country.")
	cmd.Flags().StringVar(&bodyTeamMemberWageSettingJobAssignments, "team-member-wage-setting-job-assignments", "", "**Required** The ordered list of jobs that the team member is assigned to.")
	cmd.Flags().StringVar(&bodyTeamMemberWageSettingTeamMemberId, "team-member-wage-setting-team-member-id", "", "The ID of the team member associated with the wage setting.")
	cmd.Flags().StringVar(&bodyTeamMemberWageSettingUpdatedAt, "team-member-wage-setting-updated-at", "", "The timestamp when the wage setting was last updated, in RFC 3339 format.")
	cmd.Flags().IntVar(&bodyTeamMemberWageSettingVersion, "team-member-wage-setting-version", 0, "**Read only** Used for resolving concurrency issues.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
