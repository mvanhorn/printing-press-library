// Copyright 2026 matthew.martin and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCustomersCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAddressAddressLine1 string
	var bodyAddressAddressLine2 string
	var bodyAddressAddressLine3 string
	var bodyAddressAdministrativeDistrictLevel1 string
	var bodyAddressAdministrativeDistrictLevel2 string
	var bodyAddressAdministrativeDistrictLevel3 string
	var bodyAddressCountry string
	var bodyAddressFirstName string
	var bodyAddressLastName string
	var bodyAddressLocality string
	var bodyAddressPostalCode string
	var bodyAddressSublocality string
	var bodyAddressSublocality2 string
	var bodyAddressSublocality3 string
	var bodyBirthday string
	var bodyCompanyName string
	var bodyEmailAddress string
	var bodyFamilyName string
	var bodyGivenName string
	var bodyIdempotencyKey string
	var bodyNickname string
	var bodyNote string
	var bodyPhoneNumber string
	var bodyReferenceId string
	var bodyTaxIdsEuVat string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Creates a new customer for a business.",
		Example:     "  square-pp-cli customers create",
		Annotations: map[string]string{"pp:endpoint": "customers.create", "pp:method": "POST", "pp:path": "/v2/customers"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/v2/customers"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				{
					nestedAddress := map[string]any{}
					if cmd.Flags().Changed("address-address-line-1") || bodyAddressAddressLine1 != "" {
						nestedAddress["address_line_1"] = bodyAddressAddressLine1
					}
					if cmd.Flags().Changed("address-address-line-2") || bodyAddressAddressLine2 != "" {
						nestedAddress["address_line_2"] = bodyAddressAddressLine2
					}
					if cmd.Flags().Changed("address-address-line-3") || bodyAddressAddressLine3 != "" {
						nestedAddress["address_line_3"] = bodyAddressAddressLine3
					}
					if cmd.Flags().Changed("address-administrative-district-level-1") || bodyAddressAdministrativeDistrictLevel1 != "" {
						nestedAddress["administrative_district_level_1"] = bodyAddressAdministrativeDistrictLevel1
					}
					if cmd.Flags().Changed("address-administrative-district-level-2") || bodyAddressAdministrativeDistrictLevel2 != "" {
						nestedAddress["administrative_district_level_2"] = bodyAddressAdministrativeDistrictLevel2
					}
					if cmd.Flags().Changed("address-administrative-district-level-3") || bodyAddressAdministrativeDistrictLevel3 != "" {
						nestedAddress["administrative_district_level_3"] = bodyAddressAdministrativeDistrictLevel3
					}
					if cmd.Flags().Changed("address-country") || bodyAddressCountry != "" {
						nestedAddress["country"] = bodyAddressCountry
					}
					if cmd.Flags().Changed("address-first-name") || bodyAddressFirstName != "" {
						nestedAddress["first_name"] = bodyAddressFirstName
					}
					if cmd.Flags().Changed("address-last-name") || bodyAddressLastName != "" {
						nestedAddress["last_name"] = bodyAddressLastName
					}
					if cmd.Flags().Changed("address-locality") || bodyAddressLocality != "" {
						nestedAddress["locality"] = bodyAddressLocality
					}
					if cmd.Flags().Changed("address-postal-code") || bodyAddressPostalCode != "" {
						nestedAddress["postal_code"] = bodyAddressPostalCode
					}
					if cmd.Flags().Changed("address-sublocality") || bodyAddressSublocality != "" {
						nestedAddress["sublocality"] = bodyAddressSublocality
					}
					if cmd.Flags().Changed("address-sublocality-2") || bodyAddressSublocality2 != "" {
						nestedAddress["sublocality_2"] = bodyAddressSublocality2
					}
					if cmd.Flags().Changed("address-sublocality-3") || bodyAddressSublocality3 != "" {
						nestedAddress["sublocality_3"] = bodyAddressSublocality3
					}
					if len(nestedAddress) > 0 {
						bodyMap["address"] = nestedAddress
					}
				}
				if cmd.Flags().Changed("birthday") || bodyBirthday != "" {
					bodyMap["birthday"] = bodyBirthday
				}
				if cmd.Flags().Changed("company-name") || bodyCompanyName != "" {
					bodyMap["company_name"] = bodyCompanyName
				}
				if cmd.Flags().Changed("email-address") || bodyEmailAddress != "" {
					bodyMap["email_address"] = bodyEmailAddress
				}
				if cmd.Flags().Changed("family-name") || bodyFamilyName != "" {
					bodyMap["family_name"] = bodyFamilyName
				}
				if cmd.Flags().Changed("given-name") || bodyGivenName != "" {
					bodyMap["given_name"] = bodyGivenName
				}
				if cmd.Flags().Changed("idempotency-key") || bodyIdempotencyKey != "" {
					bodyMap["idempotency_key"] = bodyIdempotencyKey
				}
				if cmd.Flags().Changed("nickname") || bodyNickname != "" {
					bodyMap["nickname"] = bodyNickname
				}
				if cmd.Flags().Changed("note") || bodyNote != "" {
					bodyMap["note"] = bodyNote
				}
				if cmd.Flags().Changed("phone-number") || bodyPhoneNumber != "" {
					bodyMap["phone_number"] = bodyPhoneNumber
				}
				if cmd.Flags().Changed("reference-id") || bodyReferenceId != "" {
					bodyMap["reference_id"] = bodyReferenceId
				}
				{
					nestedTaxIds := map[string]any{}
					if cmd.Flags().Changed("tax-ids-eu-vat") || bodyTaxIdsEuVat != "" {
						nestedTaxIds["eu_vat"] = bodyTaxIdsEuVat
					}
					if len(nestedTaxIds) > 0 {
						bodyMap["tax_ids"] = nestedTaxIds
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "customers", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "customers", data, "errors")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "customers", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "customers", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "customers", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "customers",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "customers", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "customers", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAddressAddressLine1, "address-address-line-1", "", "The first line of the address.")
	cmd.Flags().StringVar(&bodyAddressAddressLine2, "address-address-line-2", "", "The second line of the address, if any.")
	cmd.Flags().StringVar(&bodyAddressAddressLine3, "address-address-line-3", "", "The third line of the address, if any.")
	cmd.Flags().StringVar(&bodyAddressAdministrativeDistrictLevel1, "address-administrative-district-level-1", "", "A civil entity within the address's country. In the US, this is the state.")
	cmd.Flags().StringVar(&bodyAddressAdministrativeDistrictLevel2, "address-administrative-district-level-2", "", "A civil entity within the address's `administrative_district_level_1`. In the US, this is the county.")
	cmd.Flags().StringVar(&bodyAddressAdministrativeDistrictLevel3, "address-administrative-district-level-3", "", "A civil entity within the address's `administrative_district_level_2`, if any.")
	cmd.Flags().StringVar(&bodyAddressCountry, "address-country", "", "Indicates the country associated with another entity, such as a business.")
	cmd.Flags().StringVar(&bodyAddressFirstName, "address-first-name", "", "Optional first name when it's representing recipient.")
	cmd.Flags().StringVar(&bodyAddressLastName, "address-last-name", "", "Optional last name when it's representing recipient.")
	cmd.Flags().StringVar(&bodyAddressLocality, "address-locality", "", "The city or town of the address.")
	cmd.Flags().StringVar(&bodyAddressPostalCode, "address-postal-code", "", "The address's postal code. For a full list of field meanings by country, see [Working with Addresses](https://developer.")
	cmd.Flags().StringVar(&bodyAddressSublocality, "address-sublocality", "", "A civil region within the address's `locality`, if any.")
	cmd.Flags().StringVar(&bodyAddressSublocality2, "address-sublocality-2", "", "A civil region within the address's `sublocality`, if any.")
	cmd.Flags().StringVar(&bodyAddressSublocality3, "address-sublocality-3", "", "A civil region within the address's `sublocality_2`, if any.")
	cmd.Flags().StringVar(&bodyBirthday, "birthday", "", "The birthday associated with the customer profile, in `YYYY-MM-DD` or `MM-DD` format.")
	cmd.Flags().StringVar(&bodyCompanyName, "company-name", "", "A business name associated with the customer profile. The maximum length for this value is 500 characters.")
	cmd.Flags().StringVar(&bodyEmailAddress, "email-address", "", "The email address associated with the customer profile. The maximum length for this value is 254 characters.")
	cmd.Flags().StringVar(&bodyFamilyName, "family-name", "", "The family name (that is, the last name) associated with the customer profile.")
	cmd.Flags().StringVar(&bodyGivenName, "given-name", "", "The given name (that is, the first name) associated with the customer profile.")
	cmd.Flags().StringVar(&bodyIdempotencyKey, "idempotency-key", "", "The idempotency key for the request.	For more information, see [Idempotency](https://developer.squareup.")
	cmd.Flags().StringVar(&bodyNickname, "nickname", "", "A nickname for the customer profile. The maximum length for this value is 100 characters.")
	cmd.Flags().StringVar(&bodyNote, "note", "", "A custom note associated with the customer profile.")
	cmd.Flags().StringVar(&bodyPhoneNumber, "phone-number", "", "The phone number associated with the customer profile.")
	cmd.Flags().StringVar(&bodyReferenceId, "reference-id", "", "An optional second ID used to associate the customer profile with an entity in another system.")
	cmd.Flags().StringVar(&bodyTaxIdsEuVat, "tax-ids-eu-vat", "", "The EU VAT identification number for the customer. For example, `IE3426675K`.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
