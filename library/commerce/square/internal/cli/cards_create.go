// Copyright 2026 matthew.martin and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCardsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyCardBillingAddressAddressLine1 string
	var bodyCardBillingAddressAddressLine2 string
	var bodyCardBillingAddressAddressLine3 string
	var bodyCardBillingAddressAdministrativeDistrictLevel1 string
	var bodyCardBillingAddressAdministrativeDistrictLevel2 string
	var bodyCardBillingAddressAdministrativeDistrictLevel3 string
	var bodyCardBillingAddressCountry string
	var bodyCardBillingAddressFirstName string
	var bodyCardBillingAddressLastName string
	var bodyCardBillingAddressLocality string
	var bodyCardBillingAddressPostalCode string
	var bodyCardBillingAddressSublocality string
	var bodyCardBillingAddressSublocality2 string
	var bodyCardBillingAddressSublocality3 string
	var bodyCardBin string
	var bodyCardCardBrand string
	var bodyCardCardCoBrand string
	var bodyCardCardType string
	var bodyCardCardholderName string
	var bodyCardCreatedAt string
	var bodyCardCustomerId string
	var bodyCardDisabledAt string
	var bodyCardEnabled bool
	var bodyCardExpMonth int
	var bodyCardExpYear int
	var bodyCardFingerprint string
	var bodyCardHsaFsa bool
	var bodyCardId string
	var bodyCardIssuerAlert string
	var bodyCardIssuerAlertAt string
	var bodyCardLast4 string
	var bodyCardMerchantId string
	var bodyCardPrepaidType string
	var bodyCardReferenceId string
	var bodyCardVersion int
	var bodyIdempotencyKey string
	var bodySourceId string
	var bodyVerificationToken string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Adds a card on file to an existing merchant.",
		Example:     "  square-pp-cli cards create --idempotency-key your-token-here",
		Annotations: map[string]string{"pp:endpoint": "cards.create", "pp:method": "POST", "pp:path": "/v2/cards"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("idempotency-key") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "idempotency-key")
				}
				if !cmd.Flags().Changed("source-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "source-id")
				}
			}
			path := "/v2/cards"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				{
					nestedCard := map[string]any{}
					{
						nestedCardBillingAddress := map[string]any{}
						if cmd.Flags().Changed("card-billing-address-address-line-1") || bodyCardBillingAddressAddressLine1 != "" {
							nestedCardBillingAddress["address_line_1"] = bodyCardBillingAddressAddressLine1
						}
						if cmd.Flags().Changed("card-billing-address-address-line-2") || bodyCardBillingAddressAddressLine2 != "" {
							nestedCardBillingAddress["address_line_2"] = bodyCardBillingAddressAddressLine2
						}
						if cmd.Flags().Changed("card-billing-address-address-line-3") || bodyCardBillingAddressAddressLine3 != "" {
							nestedCardBillingAddress["address_line_3"] = bodyCardBillingAddressAddressLine3
						}
						if cmd.Flags().Changed("card-billing-address-administrative-district-level-1") || bodyCardBillingAddressAdministrativeDistrictLevel1 != "" {
							nestedCardBillingAddress["administrative_district_level_1"] = bodyCardBillingAddressAdministrativeDistrictLevel1
						}
						if cmd.Flags().Changed("card-billing-address-administrative-district-level-2") || bodyCardBillingAddressAdministrativeDistrictLevel2 != "" {
							nestedCardBillingAddress["administrative_district_level_2"] = bodyCardBillingAddressAdministrativeDistrictLevel2
						}
						if cmd.Flags().Changed("card-billing-address-administrative-district-level-3") || bodyCardBillingAddressAdministrativeDistrictLevel3 != "" {
							nestedCardBillingAddress["administrative_district_level_3"] = bodyCardBillingAddressAdministrativeDistrictLevel3
						}
						if cmd.Flags().Changed("card-billing-address-country") || bodyCardBillingAddressCountry != "" {
							nestedCardBillingAddress["country"] = bodyCardBillingAddressCountry
						}
						if cmd.Flags().Changed("card-billing-address-first-name") || bodyCardBillingAddressFirstName != "" {
							nestedCardBillingAddress["first_name"] = bodyCardBillingAddressFirstName
						}
						if cmd.Flags().Changed("card-billing-address-last-name") || bodyCardBillingAddressLastName != "" {
							nestedCardBillingAddress["last_name"] = bodyCardBillingAddressLastName
						}
						if cmd.Flags().Changed("card-billing-address-locality") || bodyCardBillingAddressLocality != "" {
							nestedCardBillingAddress["locality"] = bodyCardBillingAddressLocality
						}
						if cmd.Flags().Changed("card-billing-address-postal-code") || bodyCardBillingAddressPostalCode != "" {
							nestedCardBillingAddress["postal_code"] = bodyCardBillingAddressPostalCode
						}
						if cmd.Flags().Changed("card-billing-address-sublocality") || bodyCardBillingAddressSublocality != "" {
							nestedCardBillingAddress["sublocality"] = bodyCardBillingAddressSublocality
						}
						if cmd.Flags().Changed("card-billing-address-sublocality-2") || bodyCardBillingAddressSublocality2 != "" {
							nestedCardBillingAddress["sublocality_2"] = bodyCardBillingAddressSublocality2
						}
						if cmd.Flags().Changed("card-billing-address-sublocality-3") || bodyCardBillingAddressSublocality3 != "" {
							nestedCardBillingAddress["sublocality_3"] = bodyCardBillingAddressSublocality3
						}
						if len(nestedCardBillingAddress) > 0 {
							nestedCard["billing_address"] = nestedCardBillingAddress
						}
					}
					if cmd.Flags().Changed("card-bin") || bodyCardBin != "" {
						nestedCard["bin"] = bodyCardBin
					}
					if cmd.Flags().Changed("card-card-brand") || bodyCardCardBrand != "" {
						nestedCard["card_brand"] = bodyCardCardBrand
					}
					if cmd.Flags().Changed("card-card-co-brand") || bodyCardCardCoBrand != "" {
						nestedCard["card_co_brand"] = bodyCardCardCoBrand
					}
					if cmd.Flags().Changed("card-card-type") || bodyCardCardType != "" {
						nestedCard["card_type"] = bodyCardCardType
					}
					if cmd.Flags().Changed("card-cardholder-name") || bodyCardCardholderName != "" {
						nestedCard["cardholder_name"] = bodyCardCardholderName
					}
					if cmd.Flags().Changed("card-created-at") || bodyCardCreatedAt != "" {
						nestedCard["created_at"] = bodyCardCreatedAt
					}
					if cmd.Flags().Changed("card-customer-id") || bodyCardCustomerId != "" {
						nestedCard["customer_id"] = bodyCardCustomerId
					}
					if cmd.Flags().Changed("card-disabled-at") || bodyCardDisabledAt != "" {
						nestedCard["disabled_at"] = bodyCardDisabledAt
					}
					if cmd.Flags().Changed("card-enabled") {
						nestedCard["enabled"] = bodyCardEnabled
					}
					if cmd.Flags().Changed("card-exp-month") || bodyCardExpMonth != 0 {
						nestedCard["exp_month"] = bodyCardExpMonth
					}
					if cmd.Flags().Changed("card-exp-year") || bodyCardExpYear != 0 {
						nestedCard["exp_year"] = bodyCardExpYear
					}
					if cmd.Flags().Changed("card-fingerprint") || bodyCardFingerprint != "" {
						nestedCard["fingerprint"] = bodyCardFingerprint
					}
					if cmd.Flags().Changed("card-hsa-fsa") {
						nestedCard["hsa_fsa"] = bodyCardHsaFsa
					}
					if cmd.Flags().Changed("card-id") || bodyCardId != "" {
						nestedCard["id"] = bodyCardId
					}
					if cmd.Flags().Changed("card-issuer-alert") || bodyCardIssuerAlert != "" {
						nestedCard["issuer_alert"] = bodyCardIssuerAlert
					}
					if cmd.Flags().Changed("card-issuer-alert-at") || bodyCardIssuerAlertAt != "" {
						nestedCard["issuer_alert_at"] = bodyCardIssuerAlertAt
					}
					if cmd.Flags().Changed("card-last-4") || bodyCardLast4 != "" {
						nestedCard["last_4"] = bodyCardLast4
					}
					if cmd.Flags().Changed("card-merchant-id") || bodyCardMerchantId != "" {
						nestedCard["merchant_id"] = bodyCardMerchantId
					}
					if cmd.Flags().Changed("card-prepaid-type") || bodyCardPrepaidType != "" {
						nestedCard["prepaid_type"] = bodyCardPrepaidType
					}
					if cmd.Flags().Changed("card-reference-id") || bodyCardReferenceId != "" {
						nestedCard["reference_id"] = bodyCardReferenceId
					}
					if cmd.Flags().Changed("card-version") || bodyCardVersion != 0 {
						nestedCard["version"] = bodyCardVersion
					}
					if len(nestedCard) > 0 {
						bodyMap["card"] = nestedCard
					}
				}
				if cmd.Flags().Changed("idempotency-key") || bodyIdempotencyKey != "" {
					bodyMap["idempotency_key"] = bodyIdempotencyKey
				}
				if cmd.Flags().Changed("source-id") || bodySourceId != "" {
					bodyMap["source_id"] = bodySourceId
				}
				if cmd.Flags().Changed("verification-token") || bodyVerificationToken != "" {
					bodyMap["verification_token"] = bodyVerificationToken
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "cards", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "cards", data, "errors")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "cards", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "cards", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "cards", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "cards",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "cards", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "cards", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyCardBillingAddressAddressLine1, "card-billing-address-address-line-1", "", "The first line of the address.")
	cmd.Flags().StringVar(&bodyCardBillingAddressAddressLine2, "card-billing-address-address-line-2", "", "The second line of the address, if any.")
	cmd.Flags().StringVar(&bodyCardBillingAddressAddressLine3, "card-billing-address-address-line-3", "", "The third line of the address, if any.")
	cmd.Flags().StringVar(&bodyCardBillingAddressAdministrativeDistrictLevel1, "card-billing-address-administrative-district-level-1", "", "A civil entity within the address's country. In the US, this is the state.")
	cmd.Flags().StringVar(&bodyCardBillingAddressAdministrativeDistrictLevel2, "card-billing-address-administrative-district-level-2", "", "A civil entity within the address's `administrative_district_level_1`. In the US, this is the county.")
	cmd.Flags().StringVar(&bodyCardBillingAddressAdministrativeDistrictLevel3, "card-billing-address-administrative-district-level-3", "", "A civil entity within the address's `administrative_district_level_2`, if any.")
	cmd.Flags().StringVar(&bodyCardBillingAddressCountry, "card-billing-address-country", "", "Indicates the country associated with another entity, such as a business.")
	cmd.Flags().StringVar(&bodyCardBillingAddressFirstName, "card-billing-address-first-name", "", "Optional first name when it's representing recipient.")
	cmd.Flags().StringVar(&bodyCardBillingAddressLastName, "card-billing-address-last-name", "", "Optional last name when it's representing recipient.")
	cmd.Flags().StringVar(&bodyCardBillingAddressLocality, "card-billing-address-locality", "", "The city or town of the address.")
	cmd.Flags().StringVar(&bodyCardBillingAddressPostalCode, "card-billing-address-postal-code", "", "The address's postal code. For a full list of field meanings by country, see [Working with Addresses](https://developer.")
	cmd.Flags().StringVar(&bodyCardBillingAddressSublocality, "card-billing-address-sublocality", "", "A civil region within the address's `locality`, if any.")
	cmd.Flags().StringVar(&bodyCardBillingAddressSublocality2, "card-billing-address-sublocality-2", "", "A civil region within the address's `sublocality`, if any.")
	cmd.Flags().StringVar(&bodyCardBillingAddressSublocality3, "card-billing-address-sublocality-3", "", "A civil region within the address's `sublocality_2`, if any.")
	cmd.Flags().StringVar(&bodyCardBin, "card-bin", "", "The first six digits of the card number, known as the Bank Identification Number (BIN).")
	cmd.Flags().StringVar(&bodyCardCardBrand, "card-card-brand", "", "Indicates a card's brand, such as `VISA` or `MASTERCARD`.")
	cmd.Flags().StringVar(&bodyCardCardCoBrand, "card-card-co-brand", "", "Indicates the brand for a co-branded card.")
	cmd.Flags().StringVar(&bodyCardCardType, "card-card-type", "", "Indicates a card's type, such as `CREDIT` or `DEBIT`.")
	cmd.Flags().StringVar(&bodyCardCardholderName, "card-cardholder-name", "", "The name of the cardholder.")
	cmd.Flags().StringVar(&bodyCardCreatedAt, "card-created-at", "", "Timestamp for when the card object was created on Square’s servers. In RFC 3339 format, e.g., '2016-09-04T23:59:33.")
	cmd.Flags().StringVar(&bodyCardCustomerId, "card-customer-id", "", "**Required** The ID of a [customer](entity:Customer) to be associated with the card.")
	cmd.Flags().StringVar(&bodyCardDisabledAt, "card-disabled-at", "", "Timestamp for when the card object was disabled on Square’s servers. In RFC 3339 format, e.g., '2016-09-04T23:59:33.")
	cmd.Flags().BoolVar(&bodyCardEnabled, "card-enabled", false, "Indicates whether or not a card can be used for payments.")
	cmd.Flags().IntVar(&bodyCardExpMonth, "card-exp-month", 0, "The expiration month of the associated card as an integer between 1 and 12.")
	cmd.Flags().IntVar(&bodyCardExpYear, "card-exp-year", 0, "The four-digit year of the card's expiration date.")
	cmd.Flags().StringVar(&bodyCardFingerprint, "card-fingerprint", "", "Intended as a Square-assigned identifier, based on the card number")
	cmd.Flags().BoolVar(&bodyCardHsaFsa, "card-hsa-fsa", false, "Indicates whether the card is linked to a Health Savings Account (HSA) or Flexible Spending Account (FSA)")
	cmd.Flags().StringVar(&bodyCardId, "card-id", "", "Unique ID for this card. Generated by Square.")
	cmd.Flags().StringVar(&bodyCardIssuerAlert, "card-issuer-alert", "", "Indicates the type of issuer alert for a [card on file](entity:Card).")
	cmd.Flags().StringVar(&bodyCardIssuerAlertAt, "card-issuer-alert-at", "", "The timestamp of when the current issuer alert was received and processed, in RFC 3339 format.")
	cmd.Flags().StringVar(&bodyCardLast4, "card-last-4", "", "The last 4 digits of the card number.")
	cmd.Flags().StringVar(&bodyCardMerchantId, "card-merchant-id", "", "The ID of the merchant associated with the card.")
	cmd.Flags().StringVar(&bodyCardPrepaidType, "card-prepaid-type", "", "Indicates a card's prepaid type, such as `NOT_PREPAID` or `PREPAID`.")
	cmd.Flags().StringVar(&bodyCardReferenceId, "card-reference-id", "", "An optional user-defined reference ID that associates this card with another entity in an external system.")
	cmd.Flags().IntVar(&bodyCardVersion, "card-version", 0, "Current version number of the card. Increments with each card update.")
	cmd.Flags().StringVar(&bodyIdempotencyKey, "idempotency-key", "", "A unique string that identifies this CreateCard request.")
	cmd.Flags().StringVar(&bodySourceId, "source-id", "", "The ID of the source which represents the card information to be stored. This can be a card nonce or a payment id.")
	cmd.Flags().StringVar(&bodyVerificationToken, "verification-token", "", "An identifying token generated by [Payments.verifyBuyer()](https://developer.squareup.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
