// Copyright 2026 matthew.martin and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrdersCreateCustomAttributeDefinitionCmd(flags *rootFlags) *cobra.Command {
	var bodyCustomAttributeDefinitionCreatedAt string
	var bodyCustomAttributeDefinitionDescription string
	var bodyCustomAttributeDefinitionKey string
	var bodyCustomAttributeDefinitionName string
	var bodyCustomAttributeDefinitionSchema string
	var bodyCustomAttributeDefinitionUpdatedAt string
	var bodyCustomAttributeDefinitionVersion int
	var bodyCustomAttributeDefinitionVisibility string
	var bodyIdempotencyKey string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-custom-attribute-definition",
		Short:       "Creates an order-related custom attribute definition.",
		Example:     "  square-pp-cli orders create-custom-attribute-definition",
		Annotations: map[string]string{"pp:endpoint": "orders.create-custom-attribute-definition", "pp:method": "POST", "pp:path": "/v2/orders/custom-attribute-definitions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/v2/orders/custom-attribute-definitions"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				{
					nestedCustomAttributeDefinition := map[string]any{}
					if cmd.Flags().Changed("custom-attribute-definition-created-at") || bodyCustomAttributeDefinitionCreatedAt != "" {
						nestedCustomAttributeDefinition["created_at"] = bodyCustomAttributeDefinitionCreatedAt
					}
					if cmd.Flags().Changed("custom-attribute-definition-description") || bodyCustomAttributeDefinitionDescription != "" {
						nestedCustomAttributeDefinition["description"] = bodyCustomAttributeDefinitionDescription
					}
					if cmd.Flags().Changed("custom-attribute-definition-key") || bodyCustomAttributeDefinitionKey != "" {
						nestedCustomAttributeDefinition["key"] = bodyCustomAttributeDefinitionKey
					}
					if cmd.Flags().Changed("custom-attribute-definition-name") || bodyCustomAttributeDefinitionName != "" {
						nestedCustomAttributeDefinition["name"] = bodyCustomAttributeDefinitionName
					}
					if cmd.Flags().Changed("custom-attribute-definition-schema") || bodyCustomAttributeDefinitionSchema != "" {
						var parsedCustomAttributeDefinitionSchema any
						if err := json.Unmarshal([]byte(bodyCustomAttributeDefinitionSchema), &parsedCustomAttributeDefinitionSchema); err != nil {
							return fmt.Errorf("parsing --custom-attribute-definition-schema JSON: %w", err)
						}
						asMap, ok := parsedCustomAttributeDefinitionSchema.(map[string]any)
						if !ok {
							return fmt.Errorf("--custom-attribute-definition-schema must be a JSON object, got JSON %T", parsedCustomAttributeDefinitionSchema)
						}
						nestedCustomAttributeDefinition["schema"] = asMap
					}
					if cmd.Flags().Changed("custom-attribute-definition-updated-at") || bodyCustomAttributeDefinitionUpdatedAt != "" {
						nestedCustomAttributeDefinition["updated_at"] = bodyCustomAttributeDefinitionUpdatedAt
					}
					if cmd.Flags().Changed("custom-attribute-definition-version") || bodyCustomAttributeDefinitionVersion != 0 {
						nestedCustomAttributeDefinition["version"] = bodyCustomAttributeDefinitionVersion
					}
					if cmd.Flags().Changed("custom-attribute-definition-visibility") || bodyCustomAttributeDefinitionVisibility != "" {
						nestedCustomAttributeDefinition["visibility"] = bodyCustomAttributeDefinitionVisibility
					}
					if len(nestedCustomAttributeDefinition) > 0 {
						bodyMap["custom_attribute_definition"] = nestedCustomAttributeDefinition
					}
				}
				if cmd.Flags().Changed("idempotency-key") || bodyIdempotencyKey != "" {
					bodyMap["idempotency_key"] = bodyIdempotencyKey
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "orders", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "orders", data, "errors")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "orders", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "orders", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "orders", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "orders",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "orders", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "orders", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyCustomAttributeDefinitionCreatedAt, "custom-attribute-definition-created-at", "", "The timestamp that indicates when the custom attribute definition was created, in RFC 3339 format.")
	cmd.Flags().StringVar(&bodyCustomAttributeDefinitionDescription, "custom-attribute-definition-description", "", "Seller-oriented description of the custom attribute definition")
	cmd.Flags().StringVar(&bodyCustomAttributeDefinitionKey, "custom-attribute-definition-key", "", "The identifier of the custom attribute definition and its corresponding custom attributes.")
	cmd.Flags().StringVar(&bodyCustomAttributeDefinitionName, "custom-attribute-definition-name", "", "The name of the custom attribute definition for API and seller-facing UI purposes.")
	cmd.Flags().StringVar(&bodyCustomAttributeDefinitionSchema, "custom-attribute-definition-schema", "", "The JSON schema for the custom attribute definition")
	cmd.Flags().StringVar(&bodyCustomAttributeDefinitionUpdatedAt, "custom-attribute-definition-updated-at", "", "The timestamp that indicates when the custom attribute definition was created or most recently updated")
	cmd.Flags().IntVar(&bodyCustomAttributeDefinitionVersion, "custom-attribute-definition-version", 0, "Read only. The current version of the custom attribute definition.")
	cmd.Flags().StringVar(&bodyCustomAttributeDefinitionVisibility, "custom-attribute-definition-visibility", "", "The level of permission that a seller or other applications requires to view this custom attribute definition.")
	cmd.Flags().StringVar(&bodyIdempotencyKey, "idempotency-key", "", "A unique identifier for this request, used to ensure idempotency.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
