// Copyright 2026 matthew.martin and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newDevicesCreateCodeCmd(flags *rootFlags) *cobra.Command {
	var bodyDeviceCodeCode string
	var bodyDeviceCodeCreatedAt string
	var bodyDeviceCodeDeviceId string
	var bodyDeviceCodeId string
	var bodyDeviceCodeLocationId string
	var bodyDeviceCodeName string
	var bodyDeviceCodePairBy string
	var bodyDeviceCodePairedAt string
	var bodyDeviceCodeProductType string
	var bodyDeviceCodeStatus string
	var bodyDeviceCodeStatusChangedAt string
	var bodyIdempotencyKey string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-code",
		Aliases:     []string{"create"},
		Short:       "Creates a DeviceCode that can be used to login to a Square Terminal device to enter the connected terminal mode.",
		Example:     "  square-pp-cli devices create-code --idempotency-key your-token-here",
		Annotations: map[string]string{"pp:endpoint": "devices.create-code", "pp:method": "POST", "pp:path": "/v2/devices/codes"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("device-code-product-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "device-code-product-type")
				}
				if !cmd.Flags().Changed("idempotency-key") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "idempotency-key")
				}
			}
			path := "/v2/devices/codes"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				{
					nestedDeviceCode := map[string]any{}
					if cmd.Flags().Changed("device-code-code") || bodyDeviceCodeCode != "" {
						nestedDeviceCode["code"] = bodyDeviceCodeCode
					}
					if cmd.Flags().Changed("device-code-created-at") || bodyDeviceCodeCreatedAt != "" {
						nestedDeviceCode["created_at"] = bodyDeviceCodeCreatedAt
					}
					if cmd.Flags().Changed("device-code-device-id") || bodyDeviceCodeDeviceId != "" {
						nestedDeviceCode["device_id"] = bodyDeviceCodeDeviceId
					}
					if cmd.Flags().Changed("device-code-id") || bodyDeviceCodeId != "" {
						nestedDeviceCode["id"] = bodyDeviceCodeId
					}
					if cmd.Flags().Changed("device-code-location-id") || bodyDeviceCodeLocationId != "" {
						nestedDeviceCode["location_id"] = bodyDeviceCodeLocationId
					}
					if cmd.Flags().Changed("device-code-name") || bodyDeviceCodeName != "" {
						nestedDeviceCode["name"] = bodyDeviceCodeName
					}
					if cmd.Flags().Changed("device-code-pair-by") || bodyDeviceCodePairBy != "" {
						nestedDeviceCode["pair_by"] = bodyDeviceCodePairBy
					}
					if cmd.Flags().Changed("device-code-paired-at") || bodyDeviceCodePairedAt != "" {
						nestedDeviceCode["paired_at"] = bodyDeviceCodePairedAt
					}
					if cmd.Flags().Changed("device-code-product-type") || bodyDeviceCodeProductType != "" {
						nestedDeviceCode["product_type"] = bodyDeviceCodeProductType
					}
					if cmd.Flags().Changed("device-code-status") || bodyDeviceCodeStatus != "" {
						nestedDeviceCode["status"] = bodyDeviceCodeStatus
					}
					if cmd.Flags().Changed("device-code-status-changed-at") || bodyDeviceCodeStatusChangedAt != "" {
						nestedDeviceCode["status_changed_at"] = bodyDeviceCodeStatusChangedAt
					}
					if len(nestedDeviceCode) > 0 {
						bodyMap["device_code"] = nestedDeviceCode
					}
				}
				if cmd.Flags().Changed("idempotency-key") || bodyIdempotencyKey != "" {
					bodyMap["idempotency_key"] = bodyIdempotencyKey
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "devices", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "devices", data, "errors")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "devices", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "devices", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "devices", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "devices",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "devices", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "devices", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyDeviceCodeCode, "device-code-code", "", "The unique code that can be used to login.")
	cmd.Flags().StringVar(&bodyDeviceCodeCreatedAt, "device-code-created-at", "", "When this DeviceCode was created. Timestamp in RFC 3339 format.")
	cmd.Flags().StringVar(&bodyDeviceCodeDeviceId, "device-code-device-id", "", "The unique id of the device that used this code. Populated when the device is paired up.")
	cmd.Flags().StringVar(&bodyDeviceCodeId, "device-code-id", "", "The unique id for this device code.")
	cmd.Flags().StringVar(&bodyDeviceCodeLocationId, "device-code-location-id", "", "The location assigned to this code.")
	cmd.Flags().StringVar(&bodyDeviceCodeName, "device-code-name", "", "An optional user-defined name for the device code.")
	cmd.Flags().StringVar(&bodyDeviceCodePairBy, "device-code-pair-by", "", "When this DeviceCode will expire and no longer login. Timestamp in RFC 3339 format.")
	cmd.Flags().StringVar(&bodyDeviceCodePairedAt, "device-code-paired-at", "", "When this DeviceCode was paired. Timestamp in RFC 3339 format.")
	cmd.Flags().StringVar(&bodyDeviceCodeProductType, "device-code-product-type", "", "Product type")
	cmd.Flags().StringVar(&bodyDeviceCodeStatus, "device-code-status", "", "DeviceCode.Status enum.")
	cmd.Flags().StringVar(&bodyDeviceCodeStatusChangedAt, "device-code-status-changed-at", "", "When this DeviceCode's status was last changed. Timestamp in RFC 3339 format.")
	cmd.Flags().StringVar(&bodyIdempotencyKey, "idempotency-key", "", "A unique string that identifies this CreateDeviceCode request.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
