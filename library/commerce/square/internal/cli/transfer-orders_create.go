// Copyright 2026 matthew.martin and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTransferOrdersCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyIdempotencyKey string
	var bodyTransferOrderCreatedByTeamMemberId string
	var bodyTransferOrderDestinationLocationId string
	var bodyTransferOrderExpectedAt string
	var bodyTransferOrderLineItems string
	var bodyTransferOrderNotes string
	var bodyTransferOrderSourceLocationId string
	var bodyTransferOrderTrackingNumber string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Creates a new transfer order in [DRAFT](entity:TransferOrderStatus) status.",
		Example:     "  square-pp-cli transfer-orders create --idempotency-key your-token-here",
		Annotations: map[string]string{"pp:endpoint": "transfer-orders.create", "pp:method": "POST", "pp:path": "/v2/transfer-orders"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("idempotency-key") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "idempotency-key")
				}
				if !cmd.Flags().Changed("transfer-order-destination-location-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "transfer-order-destination-location-id")
				}
				if !cmd.Flags().Changed("transfer-order-source-location-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "transfer-order-source-location-id")
				}
			}
			path := "/v2/transfer-orders"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("idempotency-key") || bodyIdempotencyKey != "" {
					bodyMap["idempotency_key"] = bodyIdempotencyKey
				}
				{
					nestedTransferOrder := map[string]any{}
					if cmd.Flags().Changed("transfer-order-created-by-team-member-id") || bodyTransferOrderCreatedByTeamMemberId != "" {
						nestedTransferOrder["created_by_team_member_id"] = bodyTransferOrderCreatedByTeamMemberId
					}
					if cmd.Flags().Changed("transfer-order-destination-location-id") || bodyTransferOrderDestinationLocationId != "" {
						nestedTransferOrder["destination_location_id"] = bodyTransferOrderDestinationLocationId
					}
					if cmd.Flags().Changed("transfer-order-expected-at") || bodyTransferOrderExpectedAt != "" {
						nestedTransferOrder["expected_at"] = bodyTransferOrderExpectedAt
					}
					if cmd.Flags().Changed("transfer-order-line-items") || bodyTransferOrderLineItems != "" {
						var parsedTransferOrderLineItems any
						if err := json.Unmarshal([]byte(bodyTransferOrderLineItems), &parsedTransferOrderLineItems); err != nil {
							return fmt.Errorf("parsing --transfer-order-line-items JSON: %w", err)
						}
						asArray, ok := parsedTransferOrderLineItems.([]any)
						if !ok {
							return fmt.Errorf("--transfer-order-line-items must be a JSON array, got JSON %T", parsedTransferOrderLineItems)
						}
						nestedTransferOrder["line_items"] = asArray
					}
					if cmd.Flags().Changed("transfer-order-notes") || bodyTransferOrderNotes != "" {
						nestedTransferOrder["notes"] = bodyTransferOrderNotes
					}
					if cmd.Flags().Changed("transfer-order-source-location-id") || bodyTransferOrderSourceLocationId != "" {
						nestedTransferOrder["source_location_id"] = bodyTransferOrderSourceLocationId
					}
					if cmd.Flags().Changed("transfer-order-tracking-number") || bodyTransferOrderTrackingNumber != "" {
						nestedTransferOrder["tracking_number"] = bodyTransferOrderTrackingNumber
					}
					if len(nestedTransferOrder) > 0 {
						bodyMap["transfer_order"] = nestedTransferOrder
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "transfer-orders", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "transfer-orders", data, "errors")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "transfer-orders", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "transfer-orders", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "transfer-orders", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "transfer-orders",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "transfer-orders", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "transfer-orders", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyIdempotencyKey, "idempotency-key", "", "A unique string that identifies this CreateTransferOrder request.")
	cmd.Flags().StringVar(&bodyTransferOrderCreatedByTeamMemberId, "transfer-order-created-by-team-member-id", "", "ID of the [TeamMember](entity:TeamMember) creating this transfer order. Used for tracking and auditing purposes.")
	cmd.Flags().StringVar(&bodyTransferOrderDestinationLocationId, "transfer-order-destination-location-id", "", "The destination [Location](entity:Location) that will receive the items.")
	cmd.Flags().StringVar(&bodyTransferOrderExpectedAt, "transfer-order-expected-at", "", "Expected transfer date in RFC 3339 format (e.g. '2023-10-01T12:00:00Z').")
	cmd.Flags().StringVar(&bodyTransferOrderLineItems, "transfer-order-line-items", "", "List of [CatalogItemVariation](entity:CatalogItemVariation)s to transfer, including quantities")
	cmd.Flags().StringVar(&bodyTransferOrderNotes, "transfer-order-notes", "", "Optional notes about the transfer")
	cmd.Flags().StringVar(&bodyTransferOrderSourceLocationId, "transfer-order-source-location-id", "", "The source [Location](entity:Location) that will send the items.")
	cmd.Flags().StringVar(&bodyTransferOrderTrackingNumber, "transfer-order-tracking-number", "", "Optional shipment tracking number")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
