// Copyright 2026 Hugo Rossi and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"

	"github.com/spf13/cobra"
)

const browserHTMLAcceptHeader = "text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8"

func htmlResponseHeaders() map[string]string {
	return map[string]string{"Accept": browserHTMLAcceptHeader}
}

func printHTMLResponse(cmd *cobra.Command, data json.RawMessage, prov DataProvenance, flags *rootFlags) error {
	if flags.quiet {
		return nil
	}
	if flags.csv {
		return fmt.Errorf("--csv is not supported for HTML responses; use --plain for raw HTML or --json for a JSON string envelope")
	}
	if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.plain) {
		wrapped, err := wrapWithProvenance(data, prov)
		if err != nil {
			return err
		}
		return printOutput(cmd.OutOrStdout(), wrapped, true)
	}
	return printOutput(cmd.OutOrStdout(), data, false)
}
