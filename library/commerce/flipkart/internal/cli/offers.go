// Copyright 2026 rajiv-lokare. Licensed under Apache-2.0. See LICENSE.
//
// Hand-written Phase 3 addition. NOT generated by CLI Printing Press —
// safe to edit.

package cli

import (
	"github.com/spf13/cobra"
)

// newOffersCmd is the parent for cross-product offer analysis. Distinct
// from `product offers` (which resolves offers for a single product URL):
// this groups commands that reason across a shortlist of products.
func newOffersCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "offers",
		Short: "Analyze bank/card offers across a shortlist of Flipkart products.",
		RunE:  parentNoSubcommandRunE(flags),
	}
	cmd.AddCommand(newOffersBestCardCmd(flags))
	return cmd
}
