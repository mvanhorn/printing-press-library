// Copyright 2026 rajiv-lokare. Licensed under Apache-2.0. See LICENSE.
//
// Hand-written Phase 3 addition. NOT generated by CLI Printing Press —
// safe to edit.

package cli

import (
	"github.com/spf13/cobra"
)

// newProductCmd is the parent for product-detail commands that fetch a
// single Flipkart product page directly by URL (as opposed to `catalog`,
// which searches by keyword).
func newProductCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "product",
		Short: "Fetch a single Flipkart product's detail page by URL: price, rating, MRP, and bank offers.",
		RunE:  parentNoSubcommandRunE(flags),
	}
	cmd.AddCommand(newProductGetCmd(flags))
	cmd.AddCommand(newProductOffersCmd(flags))
	return cmd
}
