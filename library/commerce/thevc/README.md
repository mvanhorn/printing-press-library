# Thevc CLI

더브이씨 (THE VC) is Korea's leading startup investment database. This API provides
access to Korean startup profiles, funding rounds, investor data, rankings, and
company-specific news articles. All endpoints are public — no authentication required.
Designed for the Korean startup ecosystem: track funding, discover companies,
analyze investment patterns.

Example CLI usage:
  thevc rankings
  thevc profile revolut
  thevc export --format csv

Learn more at [Thevc](https://github.com/allenwlee).

## Install

### Go

```
go install github.com/mvanhorn/printing-press-library/library/other/thevc-pp-cli/cmd/thevc-pp-cli@latest
```

### Binary

Download from [Releases](https://github.com/mvanhorn/printing-press-library/releases).

## Quick Start

### 1. Install

See [Install](#install) above.

### 2. Verify Setup

```bash
thevc-pp-cli doctor
```

This checks your configuration.

### 3. Try Your First Command

```bash
thevc-pp-cli information list
```

## Usage

<!-- HELP_OUTPUT -->

## Commands

### information

Manage information

- **`thevc-pp-cli information get-latest-registered`** - Get recently registered organizations
- **`thevc-pp-cli information get-news-articles`** - Get news articles for an organization
- **`thevc-pp-cli information get-news-stats`** - Get news article statistics
- **`thevc-pp-cli information get-organization-profile`** - Get full organization profile

### interaction

Manage interaction

- **`thevc-pp-cli interaction get-rankings`** - Get organization rankings by popularity


## Output Formats

```bash
# Human-readable table (default in terminal, JSON when piped)
thevc-pp-cli information list

# JSON for scripting and agents
thevc-pp-cli information list --json

# Filter to specific fields
thevc-pp-cli information list --json --select id,name,status

# Dry run — show the request without sending
thevc-pp-cli information list --dry-run

# Agent mode — JSON + compact + no prompts in one flag
thevc-pp-cli information list --agent
```

## Agent Usage

This CLI is designed for AI agent consumption:

- **Non-interactive** - never prompts, every input is a flag
- **Pipeable** - `--json` output to stdout, errors to stderr
- **Filterable** - `--select id,name` returns only fields you need
- **Previewable** - `--dry-run` shows the request without sending
- **Retryable** - creates return "already exists" on retry, deletes return "already deleted"
- **Confirmable** - `--yes` for explicit confirmation of destructive actions
- **Piped input** - `echo '{"key":"value"}' | thevc-pp-cli <resource> create --stdin`
- **Cacheable** - GET responses cached for 5 minutes, bypass with `--no-cache`
- **Agent-safe by default** - no colors or formatting unless `--human-friendly` is set
- **Progress events** - paginated commands emit NDJSON events to stderr in default mode

Exit codes: `0` success, `2` usage error, `3` not found, `4` auth error, `5` API error, `7` rate limited, `10` config error.

## Use as MCP Server

This CLI ships a companion MCP server for use with Claude Desktop, Cursor, and other MCP-compatible tools.

### Claude Code

```bash
claude mcp add thevc thevc-pp-mcp
```

### Claude Desktop

Add to your Claude Desktop config (`~/Library/Application Support/Claude/claude_desktop_config.json`):

```json
{
  "mcpServers": {
    "thevc": {
      "command": "thevc-pp-mcp"
    }
  }
}
```

## Cookbook

Common workflows and recipes:

```bash
# List resources as JSON for scripting
thevc-pp-cli information list --json

# Filter to specific fields
thevc-pp-cli information list --json --select id,name,status

# Dry run to preview the request
thevc-pp-cli information list --dry-run

# Sync data locally for offline search
thevc-pp-cli sync

# Search synced data
thevc-pp-cli search "query"

# Export for backup
thevc-pp-cli export --format jsonl > backup.jsonl
```

## Health Check

```bash
thevc-pp-cli doctor
```

<!-- DOCTOR_OUTPUT -->

## Configuration

Config file: `~/.config/vc-korean-startup-pp-cli/config.toml`

Environment variables:

## Troubleshooting

**Authentication errors (exit code 4)**
- Run `thevc-pp-cli doctor` to check credentials

**Not found errors (exit code 3)**
- Check the resource ID is correct
- Run the `list` command to see available items

**Rate limit errors (exit code 7)**
- The CLI auto-retries with exponential backoff
- If persistent, wait a few minutes and try again

---

Generated by [CLI Printing Press](https://github.com/mvanhorn/cli-printing-press)
