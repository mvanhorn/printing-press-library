// Copyright 2026 bobe and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCouponsBatchCmd(flags *rootFlags) *cobra.Command {
	var bodyCode string
	var bodyAmount string
	var bodyStatus string
	var bodyDiscountType string
	var bodyDescription string
	var bodyDateExpires string
	var bodyDateExpiresGmt string
	var bodyIndividualUse bool
	var bodyProductIds string
	var bodyExcludedProductIds string
	var bodyUsageLimit int
	var bodyUsageLimitPerUser int
	var bodyLimitUsageToXItems int
	var bodyFreeShipping bool
	var bodyProductCategories string
	var bodyExcludedProductCategories string
	var bodyExcludeSaleItems bool
	var bodyMinimumAmount string
	var bodyMaximumAmount string
	var bodyEmailRestrictions string
	var bodyMetaData string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "batch",
		Short:       "Run batch for coupons.",
		Example:     "  woocommerce-pp-cli coupons batch",
		Annotations: map[string]string{"pp:endpoint": "coupons.batch", "pp:method": "POST", "pp:path": "/wc/v3/coupons/batch"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/wc/v3/coupons/batch"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if bodyCode != "" {
					bodyMap["code"] = bodyCode
				}
				if bodyAmount != "" {
					bodyMap["amount"] = bodyAmount
				}
				if bodyStatus != "" {
					bodyMap["status"] = bodyStatus
				}
				if bodyDiscountType != "" {
					bodyMap["discount_type"] = bodyDiscountType
				}
				if bodyDescription != "" {
					bodyMap["description"] = bodyDescription
				}
				if bodyDateExpires != "" {
					bodyMap["date_expires"] = bodyDateExpires
				}
				if bodyDateExpiresGmt != "" {
					bodyMap["date_expires_gmt"] = bodyDateExpiresGmt
				}
				if cmd.Flags().Changed("individual-use") {
					bodyMap["individual_use"] = bodyIndividualUse
				}
				if bodyProductIds != "" {
					bodyMap["product_ids"] = bodyProductIds
				}
				if bodyExcludedProductIds != "" {
					bodyMap["excluded_product_ids"] = bodyExcludedProductIds
				}
				if bodyUsageLimit != 0 {
					bodyMap["usage_limit"] = bodyUsageLimit
				}
				if bodyUsageLimitPerUser != 0 {
					bodyMap["usage_limit_per_user"] = bodyUsageLimitPerUser
				}
				if bodyLimitUsageToXItems != 0 {
					bodyMap["limit_usage_to_x_items"] = bodyLimitUsageToXItems
				}
				if cmd.Flags().Changed("free-shipping") {
					bodyMap["free_shipping"] = bodyFreeShipping
				}
				if bodyProductCategories != "" {
					bodyMap["product_categories"] = bodyProductCategories
				}
				if bodyExcludedProductCategories != "" {
					bodyMap["excluded_product_categories"] = bodyExcludedProductCategories
				}
				if cmd.Flags().Changed("exclude-sale-items") {
					bodyMap["exclude_sale_items"] = bodyExcludeSaleItems
				}
				if bodyMinimumAmount != "" {
					bodyMap["minimum_amount"] = bodyMinimumAmount
				}
				if bodyMaximumAmount != "" {
					bodyMap["maximum_amount"] = bodyMaximumAmount
				}
				if bodyEmailRestrictions != "" {
					bodyMap["email_restrictions"] = bodyEmailRestrictions
				}
				if bodyMetaData != "" {
					bodyMap["meta_data"] = bodyMetaData
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "coupons", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "coupons", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "coupons", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "coupons", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "coupons", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "coupons",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "coupons", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "coupons", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyCode, "code", "", "Coupon code.")
	cmd.Flags().StringVar(&bodyAmount, "amount", "", "The amount of discount. Should always be numeric, even if setting a percentage.")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "The status of the coupon. Should always be draft, published, or pending review")
	cmd.Flags().StringVar(&bodyDiscountType, "discount-type", "", "Determines the type of discount that will be applied.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Coupon description.")
	cmd.Flags().StringVar(&bodyDateExpires, "date-expires", "", "The date the coupon expires, in the site's timezone.")
	cmd.Flags().StringVar(&bodyDateExpiresGmt, "date-expires-gmt", "", "The date the coupon expires, as GMT.")
	cmd.Flags().BoolVar(&bodyIndividualUse, "individual-use", false, "If true, the coupon can only be used individually. Other applied coupons will be removed from the cart.")
	cmd.Flags().StringVar(&bodyProductIds, "product-ids", "", "List of product IDs the coupon can be used on. (comma-separated)")
	cmd.Flags().StringVar(&bodyExcludedProductIds, "excluded-product-ids", "", "List of product IDs the coupon cannot be used on. (comma-separated)")
	cmd.Flags().IntVar(&bodyUsageLimit, "usage-limit", 0, "How many times the coupon can be used in total.")
	cmd.Flags().IntVar(&bodyUsageLimitPerUser, "usage-limit-per-user", 0, "How many times the coupon can be used per customer.")
	cmd.Flags().IntVar(&bodyLimitUsageToXItems, "limit-usage-to-x-items", 0, "Max number of items in the cart the coupon can be applied to.")
	cmd.Flags().BoolVar(&bodyFreeShipping, "free-shipping", false, "If true and if the free shipping method requires a coupon, this coupon will enable free shipping.")
	cmd.Flags().StringVar(&bodyProductCategories, "product-categories", "", "List of category IDs the coupon applies to. (comma-separated)")
	cmd.Flags().StringVar(&bodyExcludedProductCategories, "excluded-product-categories", "", "List of category IDs the coupon does not apply to. (comma-separated)")
	cmd.Flags().BoolVar(&bodyExcludeSaleItems, "exclude-sale-items", false, "If true, this coupon will not be applied to items that have sale prices.")
	cmd.Flags().StringVar(&bodyMinimumAmount, "minimum-amount", "", "Minimum order amount that needs to be in the cart before coupon applies.")
	cmd.Flags().StringVar(&bodyMaximumAmount, "maximum-amount", "", "Maximum order amount allowed when using the coupon.")
	cmd.Flags().StringVar(&bodyEmailRestrictions, "email-restrictions", "", "List of email addresses that can use this coupon. (comma-separated)")
	cmd.Flags().StringVar(&bodyMetaData, "meta-data", "", "Meta data. (comma-separated)")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
