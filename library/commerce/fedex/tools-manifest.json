{
  "tools": [
    {
      "name": "get_rates",
      "description": "Get FedEx rate quotes for a shipment request. This calls FedEx but does not create a shipment.",
      "parameters": {
        "type": "object",
        "properties": {
          "request": {
            "type": "object",
            "description": "Exact FedEx REST request object for this operation."
          }
        },
        "required": ["request"]
      }
    },
    {
      "name": "validate_address",
      "description": "Validate and standardize an address with FedEx. This may be billable, is never retried automatically, and does not create a shipment.",
      "parameters": {
        "type": "object",
        "properties": {
          "request": {
            "type": "object",
            "description": "Exact FedEx REST request object for this operation."
          }
        },
        "required": ["request"]
      }
    },
    {
      "name": "validate_shipment",
      "description": "Validate a single-package shipment request without creating a shipment or label.",
      "parameters": {
        "type": "object",
        "properties": {
          "request": {
            "type": "object",
            "description": "Exact FedEx REST request object for this operation."
          }
        },
        "required": ["request"]
      }
    },
    {
      "name": "pickup_availability",
      "description": "Check FedEx Express or Ground pickup availability without scheduling a pickup.",
      "parameters": {
        "type": "object",
        "properties": {
          "request": {
            "type": "object",
            "description": "Exact FedEx REST request object for this operation."
          }
        },
        "required": ["request"]
      }
    },
    {
      "name": "create_label",
      "description": "Preview or, after bound confirmation, create one FedEx shipment and label.",
      "parameters": {
        "type": "object",
        "properties": {
          "request": {"type": "object"},
          "confirm": {"type": "boolean", "default": false},
          "operation_id": {"type": "string"},
          "confirmation_digest": {"type": "string"}
        },
        "required": ["request"]
      }
    },
    {
      "name": "cancel_shipment",
      "description": "Preview or, after bound confirmation, cancel one FedEx shipment.",
      "parameters": {
        "type": "object",
        "properties": {
          "request": {"type": "object"},
          "confirm": {"type": "boolean", "default": false},
          "operation_id": {"type": "string"},
          "confirmation_digest": {"type": "string"}
        },
        "required": ["request"]
      }
    },
    {
      "name": "schedule_pickup",
      "description": "Preview or, after bound confirmation, schedule one FedEx pickup.",
      "parameters": {
        "type": "object",
        "properties": {
          "request": {"type": "object"},
          "confirm": {"type": "boolean", "default": false},
          "operation_id": {"type": "string"},
          "confirmation_digest": {"type": "string"}
        },
        "required": ["request"]
      }
    },
    {
      "name": "cancel_pickup",
      "description": "Preview or, after bound confirmation, cancel one FedEx pickup.",
      "parameters": {
        "type": "object",
        "properties": {
          "request": {"type": "object"},
          "confirm": {"type": "boolean", "default": false},
          "operation_id": {"type": "string"},
          "confirmation_digest": {"type": "string"}
        },
        "required": ["request"]
      }
    }
  ]
}
