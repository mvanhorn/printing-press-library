version: 2
project_name: ecommerce-intel-pp-cli
changelog:
  disable: true
builds:
  - id: ecommerce-intel-pp-cli
    main: ./cmd/ecommerce-intel-pp-cli
    binary: ecommerce-intel-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/commerce/ecommerce-intel/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: ecommerce-intel-pp-cli
    repository:
      owner: user
      name: homebrew-tap
    homepage: "https://github.com/cathrynlavery/printing-press-library-private"
    description: "Private ecommerce intelligence CLI for Shopify, Klaviyo, GA4, GSC, Ahrefs, and GEO readiness."
    install: |
      bin.install "ecommerce-intel-pp-cli"
