version: 2
project_name: clickup-2-pp-cli
changelog:
  disable: true
builds:
  - id: clickup-2-pp-cli
    main: ./cmd/clickup-2-pp-cli
    binary: clickup-2-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/project-management/clickup-2/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: clickup-2-pp-mcp
    main: ./cmd/clickup-2-pp-mcp
    binary: clickup-2-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: clickup-2-pp-cli
    repository:
      owner: riccardovandra
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Every ClickUp endpoint plus a local SQLite mirror with offline search and cross-task analytics no other ClickUp CLI ships."
    install: |
      bin.install "clickup-2-pp-cli"
      bin.install "clickup-2-pp-mcp"
