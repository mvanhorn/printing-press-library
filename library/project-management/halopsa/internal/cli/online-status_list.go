// Copyright 2026 dstevens. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newOnlineStatusListCmd(flags *rootFlags) *cobra.Command {
	var flagTechID string
	var flagLastOnlineDate string
	var flagIsOnline bool
	var flagLastOnline string
	var flagAgentStatus int
	var flagStatusOveridden bool
	var flagFetchAll bool
	var flagIsLogout bool
	var flagCommand string
	var flagLastActive string
	var flagIsIdle bool
	var flagIdleWarn bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "List",
		Example:     "  halopsa-pp-cli online-status list",
		Annotations: map[string]string{"pp:endpoint": "online-status.list", "pp:method": "GET", "pp:path": "/OnlineStatus", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/OnlineStatus"
			params := map[string]string{}
			if flagTechID != "" {
				params["TechID"] = fmt.Sprintf("%v", flagTechID)
			}
			if flagLastOnlineDate != "" {
				params["LastOnlineDate"] = fmt.Sprintf("%v", flagLastOnlineDate)
			}
			if flagIsOnline != false {
				params["IsOnline"] = fmt.Sprintf("%v", flagIsOnline)
			}
			if flagLastOnline != "" {
				params["LastOnline"] = fmt.Sprintf("%v", flagLastOnline)
			}
			if flagAgentStatus != 0 {
				params["agent_status"] = fmt.Sprintf("%v", flagAgentStatus)
			}
			if flagStatusOveridden != false {
				params["status_overidden"] = fmt.Sprintf("%v", flagStatusOveridden)
			}
			if flagFetchAll != false {
				params["fetch_all"] = fmt.Sprintf("%v", flagFetchAll)
			}
			if flagIsLogout != false {
				params["is_logout"] = fmt.Sprintf("%v", flagIsLogout)
			}
			if flagCommand != "" {
				params["command"] = fmt.Sprintf("%v", flagCommand)
			}
			if flagLastActive != "" {
				params["last_active"] = fmt.Sprintf("%v", flagLastActive)
			}
			if flagIsIdle != false {
				params["is_idle"] = fmt.Sprintf("%v", flagIsIdle)
			}
			if flagIdleWarn != false {
				params["idle_warn"] = fmt.Sprintf("%v", flagIdleWarn)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "online-status", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagTechID, "tech-id", "", "Tech id")
	cmd.Flags().StringVar(&flagLastOnlineDate, "last-online-date", "", "Last online date")
	cmd.Flags().BoolVar(&flagIsOnline, "is-online", false, "Is online")
	cmd.Flags().StringVar(&flagLastOnline, "last-online", "", "Last online")
	cmd.Flags().IntVar(&flagAgentStatus, "agent-status", 0, "Agent status")
	cmd.Flags().BoolVar(&flagStatusOveridden, "status-overidden", false, "Status overidden")
	cmd.Flags().BoolVar(&flagFetchAll, "fetch-all", false, "Fetch all")
	cmd.Flags().BoolVar(&flagIsLogout, "is-logout", false, "Is logout")
	cmd.Flags().StringVar(&flagCommand, "command", "", "Command")
	cmd.Flags().StringVar(&flagLastActive, "last-active", "", "Last active")
	cmd.Flags().BoolVar(&flagIsIdle, "is-idle", false, "Is idle")
	cmd.Flags().BoolVar(&flagIdleWarn, "idle-warn", false, "Idle warn")

	return cmd
}
