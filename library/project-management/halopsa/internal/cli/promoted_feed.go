// Copyright 2026 dstevens. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newFeedPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagAccountmanagerId string
	var flagAgentOnly bool
	var flagCount int
	var flagExcludePrivate bool
	var flagFollowedOnly bool
	var flagNewerThanId string
	var flagOlderThanId string
	var flagOneAgentId string
	var flagOneAgentsTicketsId string
	var flagOneUserId string
	var flagRelatedAssetId string
	var flagRelatedClientId string
	var flagRelatedSiteId string
	var flagRelatedUserId string
	var flagUserOnly bool

	cmd := &cobra.Command{
		Use:         "feed",
		Short:       "Use this to return multiple Feed.<br> 				Requires authentication.",
		Long:        "Shortcut for 'feed list'. Use this to return multiple Feed.<br> 				Requires authentication.",
		Example:     "  halopsa-pp-cli feed",
		Annotations: map[string]string{"pp:endpoint": "feed.list", "pp:method": "GET", "pp:path": "/Feed", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Feed"
			params := map[string]string{}
			if flagAccountmanagerId != "" {
				params["accountmanager_id"] = fmt.Sprintf("%v", flagAccountmanagerId)
			}
			if flagAgentOnly != false {
				params["agent_only"] = fmt.Sprintf("%v", flagAgentOnly)
			}
			if flagCount != 0 {
				params["count"] = fmt.Sprintf("%v", flagCount)
			}
			if flagExcludePrivate != false {
				params["exclude_private"] = fmt.Sprintf("%v", flagExcludePrivate)
			}
			if flagFollowedOnly != false {
				params["followed_only"] = fmt.Sprintf("%v", flagFollowedOnly)
			}
			if flagNewerThanId != "" {
				params["newer_than_id"] = fmt.Sprintf("%v", flagNewerThanId)
			}
			if flagOlderThanId != "" {
				params["older_than_id"] = fmt.Sprintf("%v", flagOlderThanId)
			}
			if flagOneAgentId != "" {
				params["one_agent_id"] = fmt.Sprintf("%v", flagOneAgentId)
			}
			if flagOneAgentsTicketsId != "" {
				params["one_agents_tickets_id"] = fmt.Sprintf("%v", flagOneAgentsTicketsId)
			}
			if flagOneUserId != "" {
				params["one_user_id"] = fmt.Sprintf("%v", flagOneUserId)
			}
			if flagRelatedAssetId != "" {
				params["related_asset_id"] = fmt.Sprintf("%v", flagRelatedAssetId)
			}
			if flagRelatedClientId != "" {
				params["related_client_id"] = fmt.Sprintf("%v", flagRelatedClientId)
			}
			if flagRelatedSiteId != "" {
				params["related_site_id"] = fmt.Sprintf("%v", flagRelatedSiteId)
			}
			if flagRelatedUserId != "" {
				params["related_user_id"] = fmt.Sprintf("%v", flagRelatedUserId)
			}
			if flagUserOnly != false {
				params["user_only"] = fmt.Sprintf("%v", flagUserOnly)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "feed", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAccountmanagerId, "accountmanager-id", "", "Accountmanager id")
	cmd.Flags().BoolVar(&flagAgentOnly, "agent-only", false, "Agent only")
	cmd.Flags().IntVar(&flagCount, "count", 0, "Count")
	cmd.Flags().BoolVar(&flagExcludePrivate, "exclude-private", false, "Exclude private")
	cmd.Flags().BoolVar(&flagFollowedOnly, "followed-only", false, "Followed only")
	cmd.Flags().StringVar(&flagNewerThanId, "newer-than-id", "", "Newer than id")
	cmd.Flags().StringVar(&flagOlderThanId, "older-than-id", "", "Older than id")
	cmd.Flags().StringVar(&flagOneAgentId, "one-agent-id", "", "One agent id")
	cmd.Flags().StringVar(&flagOneAgentsTicketsId, "one-agents-tickets-id", "", "One agents tickets id")
	cmd.Flags().StringVar(&flagOneUserId, "one-user-id", "", "One user id")
	cmd.Flags().StringVar(&flagRelatedAssetId, "related-asset-id", "", "Related asset id")
	cmd.Flags().StringVar(&flagRelatedClientId, "related-client-id", "", "Related client id")
	cmd.Flags().StringVar(&flagRelatedSiteId, "related-site-id", "", "Related site id")
	cmd.Flags().StringVar(&flagRelatedUserId, "related-user-id", "", "Related user id")
	cmd.Flags().BoolVar(&flagUserOnly, "user-only", false, "User only")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
