// Copyright 2026 dstevens. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTicketTypeListCmd(flags *rootFlags) *cobra.Command {
	var flagAccessControlLevel int
	var flagAnonanduser bool
	var flagCanCreateOnly bool
	var flagCanEditOnly bool
	var flagCanagentsselect bool
	var flagCanusercreate bool
	var flagClientId string
	var flagDomain string
	var flagGroupId string
	var flagIncludeCurrent int
	var flagIncludeMandatoryFieldCheck bool
	var flagIsquicktimedropdown bool
	var flagItilType int
	var flagOutcomeId string
	var flagSearchtickets string
	var flagShowall bool
	var flagShowcounts bool
	var flagShowinactive bool
	var flagSprintsOnly bool
	var flagTicketareaId string
	var flagUserOnly bool
	var flagViewId string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Use this to return multiple RequestType.<br> 				Requires authentication.",
		Example:     "  halopsa-pp-cli ticket-type list",
		Annotations: map[string]string{"pp:endpoint": "ticket-type.list", "pp:method": "GET", "pp:path": "/TicketType", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/TicketType"
			params := map[string]string{}
			if flagAccessControlLevel != 0 {
				params["access_control_level"] = fmt.Sprintf("%v", flagAccessControlLevel)
			}
			if flagAnonanduser != false {
				params["anonanduser"] = fmt.Sprintf("%v", flagAnonanduser)
			}
			if flagCanCreateOnly != false {
				params["can_create_only"] = fmt.Sprintf("%v", flagCanCreateOnly)
			}
			if flagCanEditOnly != false {
				params["can_edit_only"] = fmt.Sprintf("%v", flagCanEditOnly)
			}
			if flagCanagentsselect != false {
				params["canagentsselect"] = fmt.Sprintf("%v", flagCanagentsselect)
			}
			if flagCanusercreate != false {
				params["canusercreate"] = fmt.Sprintf("%v", flagCanusercreate)
			}
			if flagClientId != "" {
				params["client_id"] = fmt.Sprintf("%v", flagClientId)
			}
			if flagDomain != "" {
				params["domain"] = fmt.Sprintf("%v", flagDomain)
			}
			if flagGroupId != "" {
				params["group_id"] = fmt.Sprintf("%v", flagGroupId)
			}
			if flagIncludeCurrent != 0 {
				params["include_current"] = fmt.Sprintf("%v", flagIncludeCurrent)
			}
			if flagIncludeMandatoryFieldCheck != false {
				params["include_mandatory_field_check"] = fmt.Sprintf("%v", flagIncludeMandatoryFieldCheck)
			}
			if flagIsquicktimedropdown != false {
				params["isquicktimedropdown"] = fmt.Sprintf("%v", flagIsquicktimedropdown)
			}
			if flagItilType != 0 {
				params["itil_type"] = fmt.Sprintf("%v", flagItilType)
			}
			if flagOutcomeId != "" {
				params["outcome_id"] = fmt.Sprintf("%v", flagOutcomeId)
			}
			if flagSearchtickets != "" {
				params["searchtickets"] = fmt.Sprintf("%v", flagSearchtickets)
			}
			if flagShowall != false {
				params["showall"] = fmt.Sprintf("%v", flagShowall)
			}
			if flagShowcounts != false {
				params["showcounts"] = fmt.Sprintf("%v", flagShowcounts)
			}
			if flagShowinactive != false {
				params["showinactive"] = fmt.Sprintf("%v", flagShowinactive)
			}
			if flagSprintsOnly != false {
				params["sprints_only"] = fmt.Sprintf("%v", flagSprintsOnly)
			}
			if flagTicketareaId != "" {
				params["ticketarea_id"] = fmt.Sprintf("%v", flagTicketareaId)
			}
			if flagUserOnly != false {
				params["user_only"] = fmt.Sprintf("%v", flagUserOnly)
			}
			if flagViewId != "" {
				params["view_id"] = fmt.Sprintf("%v", flagViewId)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "ticket-type", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagAccessControlLevel, "access-control-level", 0, "(int) Filters response based on the access control level.")
	cmd.Flags().BoolVar(&flagAnonanduser, "anonanduser", false, "(bool) Filter on ticket types that can be viewed by anonymous users.")
	cmd.Flags().BoolVar(&flagCanCreateOnly, "can-create-only", false, "(bool) Filters by the ticket types that are created.")
	cmd.Flags().BoolVar(&flagCanEditOnly, "can-edit-only", false, "(bool) Filters by the ticket types that can be edited.")
	cmd.Flags().BoolVar(&flagCanagentsselect, "canagentsselect", false, "(bool) Filter on ticket types that agents can select.")
	cmd.Flags().BoolVar(&flagCanusercreate, "canusercreate", false, "Canusercreate")
	cmd.Flags().StringVar(&flagClientId, "client-id", "", "(int) Filter Request Types by a particular customer.")
	cmd.Flags().StringVar(&flagDomain, "domain", "", "(string) For use with counts: reqs - tickets, opps - opportunities, prjs - projects.")
	cmd.Flags().StringVar(&flagGroupId, "group-id", "", "(int) Filters by the ticket type group.")
	cmd.Flags().IntVar(&flagIncludeCurrent, "include-current", 0, "(int) Includes this ticket type in the response - used alongside can create and can edit.")
	cmd.Flags().BoolVar(&flagIncludeMandatoryFieldCheck, "include-mandatory-field-check", false, "Include mandatory field check")
	cmd.Flags().BoolVar(&flagIsquicktimedropdown, "isquicktimedropdown", false, "(bool) Filters by the ticket types that display on quick time.")
	cmd.Flags().IntVar(&flagItilType, "itil-type", 0, "(int) Filter by itil type.")
	cmd.Flags().StringVar(&flagOutcomeId, "outcome-id", "", "Outcome id")
	cmd.Flags().StringVar(&flagSearchtickets, "searchtickets", "", "(string) Filter on ticket types based on tickets.")
	cmd.Flags().BoolVar(&flagShowall, "showall", false, "(bool) Admin override to show all ticket types, overriding tickets permissions.")
	cmd.Flags().BoolVar(&flagShowcounts, "showcounts", false, "(bool) Show the count of tickets in the response.")
	cmd.Flags().BoolVar(&flagShowinactive, "showinactive", false, "(bool) Include inactive ticket types in the response.")
	cmd.Flags().BoolVar(&flagSprintsOnly, "sprints-only", false, "Sprints only")
	cmd.Flags().StringVar(&flagTicketareaId, "ticketarea-id", "", "(int) Only for showcounts - ticket area filter.")
	cmd.Flags().BoolVar(&flagUserOnly, "user-only", false, "(bool) Filters by the ticket types that user can see.")
	cmd.Flags().StringVar(&flagViewId, "view-id", "", "(int) Only for showcounts - ticket view filter.")

	return cmd
}
