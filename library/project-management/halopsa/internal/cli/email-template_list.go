// Copyright 2026 dstevens. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newEmailTemplateListCmd(flags *rootFlags) *cobra.Command {
	var flagAccessControlLevel int
	var flagIgnoreMg bool
	var flagIsconfig bool
	var flagMessagegroup int
	var flagReleaseOnly bool
	var flagPortalcss bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Use this to return multiple MessageContent.<br> 				Requires authentication.",
		Example:     "  halopsa-pp-cli email-template list",
		Annotations: map[string]string{"pp:endpoint": "email-template.list", "pp:method": "GET", "pp:path": "/EmailTemplate", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/EmailTemplate"
			params := map[string]string{}
			if flagAccessControlLevel != 0 {
				params["access_control_level"] = fmt.Sprintf("%v", flagAccessControlLevel)
			}
			if flagIgnoreMg != false {
				params["ignore_mg"] = fmt.Sprintf("%v", flagIgnoreMg)
			}
			if flagIsconfig != false {
				params["isconfig"] = fmt.Sprintf("%v", flagIsconfig)
			}
			if flagMessagegroup != 0 {
				params["messagegroup"] = fmt.Sprintf("%v", flagMessagegroup)
			}
			if flagReleaseOnly != false {
				params["release_only"] = fmt.Sprintf("%v", flagReleaseOnly)
			}
			if flagPortalcss != false {
				params["portalcss"] = fmt.Sprintf("%v", flagPortalcss)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "email-template", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagAccessControlLevel, "access-control-level", 0, "Access control level")
	cmd.Flags().BoolVar(&flagIgnoreMg, "ignore-mg", false, "Ignore mg")
	cmd.Flags().BoolVar(&flagIsconfig, "isconfig", false, "Isconfig")
	cmd.Flags().IntVar(&flagMessagegroup, "messagegroup", 0, "Messagegroup")
	cmd.Flags().BoolVar(&flagReleaseOnly, "release-only", false, "Release only")
	cmd.Flags().BoolVar(&flagPortalcss, "portalcss", false, "Portalcss")

	return cmd
}
