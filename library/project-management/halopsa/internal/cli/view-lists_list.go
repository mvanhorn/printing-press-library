// Copyright 2026 dstevens. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newViewListsListCmd(flags *rootFlags) *cobra.Command {
	var flagConnectedinstanceId string
	var flagDomain string
	var flagGlobalonly bool
	var flagIstree bool
	var flagShowall bool
	var flagShowallforteam int
	var flagShowallfortech int
	var flagShowcounts bool
	var flagTicketareaId string
	var flagType string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Use this to return multiple ViewLists.<br> 				Requires authentication.",
		Example:     "  halopsa-pp-cli view-lists list",
		Annotations: map[string]string{"pp:endpoint": "view-lists.list", "pp:method": "GET", "pp:path": "/ViewLists", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ViewLists"
			params := map[string]string{}
			if flagConnectedinstanceId != "" {
				params["connectedinstance_id"] = fmt.Sprintf("%v", flagConnectedinstanceId)
			}
			if flagDomain != "" {
				params["domain"] = fmt.Sprintf("%v", flagDomain)
			}
			if flagGlobalonly != false {
				params["globalonly"] = fmt.Sprintf("%v", flagGlobalonly)
			}
			if flagIstree != false {
				params["istree"] = fmt.Sprintf("%v", flagIstree)
			}
			if flagShowall != false {
				params["showall"] = fmt.Sprintf("%v", flagShowall)
			}
			if flagShowallforteam != 0 {
				params["showallforteam"] = fmt.Sprintf("%v", flagShowallforteam)
			}
			if flagShowallfortech != 0 {
				params["showallfortech"] = fmt.Sprintf("%v", flagShowallfortech)
			}
			if flagShowcounts != false {
				params["showcounts"] = fmt.Sprintf("%v", flagShowcounts)
			}
			if flagTicketareaId != "" {
				params["ticketarea_id"] = fmt.Sprintf("%v", flagTicketareaId)
			}
			if flagType != "" {
				params["type"] = fmt.Sprintf("%v", flagType)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "view-lists", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagConnectedinstanceId, "connectedinstance-id", "", "Connectedinstance id")
	cmd.Flags().StringVar(&flagDomain, "domain", "", "Domain")
	cmd.Flags().BoolVar(&flagGlobalonly, "globalonly", false, "Globalonly")
	cmd.Flags().BoolVar(&flagIstree, "istree", false, "Istree")
	cmd.Flags().BoolVar(&flagShowall, "showall", false, "Showall")
	cmd.Flags().IntVar(&flagShowallforteam, "showallforteam", 0, "Showallforteam")
	cmd.Flags().IntVar(&flagShowallfortech, "showallfortech", 0, "Showallfortech")
	cmd.Flags().BoolVar(&flagShowcounts, "showcounts", false, "Showcounts")
	cmd.Flags().StringVar(&flagTicketareaId, "ticketarea-id", "", "Ticketarea id")
	cmd.Flags().StringVar(&flagType, "type", "", "Type")

	return cmd
}
