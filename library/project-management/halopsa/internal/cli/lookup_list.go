// Copyright 2026 dstevens. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newLookupListCmd(flags *rootFlags) *cobra.Command {
	var flagAccessControlLevel int
	var flagAssettypeId string
	var flagClientId string
	var flagClientname string
	var flagContractId string
	var flagCountryCodeId string
	var flagDbcCompanyId string
	var flagDomain string
	var flagExcludeNocharge bool
	var flagExcludeNolinkedtypes bool
	var flagExcludeZero bool
	var flagIscustomfield bool
	var flagIstree bool
	var flagLookupid string
	var flagOrdervaluetype int
	var flagOutcomeId string
	var flagShowallcodes bool
	var flagTicketId string
	var flagUnameaprestriction bool
	var flagUse int
	var flagUse2 int

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Use this to return multiple Lookup.<br> 				Requires authentication.",
		Example:     "  halopsa-pp-cli lookup list",
		Annotations: map[string]string{"pp:endpoint": "lookup.list", "pp:method": "GET", "pp:path": "/Lookup", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Lookup"
			params := map[string]string{}
			if flagAccessControlLevel != 0 {
				params["access_control_level"] = fmt.Sprintf("%v", flagAccessControlLevel)
			}
			if flagAssettypeId != "" {
				params["assettype_id"] = fmt.Sprintf("%v", flagAssettypeId)
			}
			if flagClientId != "" {
				params["client_id"] = fmt.Sprintf("%v", flagClientId)
			}
			if flagClientname != "" {
				params["clientname"] = fmt.Sprintf("%v", flagClientname)
			}
			if flagContractId != "" {
				params["contract_id"] = fmt.Sprintf("%v", flagContractId)
			}
			if flagCountryCodeId != "" {
				params["country_code_id"] = fmt.Sprintf("%v", flagCountryCodeId)
			}
			if flagDbcCompanyId != "" {
				params["dbc_company_id"] = fmt.Sprintf("%v", flagDbcCompanyId)
			}
			if flagDomain != "" {
				params["domain"] = fmt.Sprintf("%v", flagDomain)
			}
			if flagExcludeNocharge != false {
				params["exclude_nocharge"] = fmt.Sprintf("%v", flagExcludeNocharge)
			}
			if flagExcludeNolinkedtypes != false {
				params["exclude_nolinkedtypes"] = fmt.Sprintf("%v", flagExcludeNolinkedtypes)
			}
			if flagExcludeZero != false {
				params["exclude_zero"] = fmt.Sprintf("%v", flagExcludeZero)
			}
			if flagIscustomfield != false {
				params["iscustomfield"] = fmt.Sprintf("%v", flagIscustomfield)
			}
			if flagIstree != false {
				params["istree"] = fmt.Sprintf("%v", flagIstree)
			}
			if flagLookupid != "" {
				params["lookupid"] = fmt.Sprintf("%v", flagLookupid)
			}
			if flagOrdervaluetype != 0 {
				params["ordervaluetype"] = fmt.Sprintf("%v", flagOrdervaluetype)
			}
			if flagOutcomeId != "" {
				params["outcome_id"] = fmt.Sprintf("%v", flagOutcomeId)
			}
			if flagShowallcodes != false {
				params["showallcodes"] = fmt.Sprintf("%v", flagShowallcodes)
			}
			if flagTicketId != "" {
				params["ticket_id"] = fmt.Sprintf("%v", flagTicketId)
			}
			if flagUnameaprestriction != false {
				params["unameaprestriction"] = fmt.Sprintf("%v", flagUnameaprestriction)
			}
			if flagUse != 0 {
				params["use"] = fmt.Sprintf("%v", flagUse)
			}
			if flagUse2 != 0 {
				params["use2"] = fmt.Sprintf("%v", flagUse2)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "lookup", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagAccessControlLevel, "access-control-level", 0, "Access control level")
	cmd.Flags().StringVar(&flagAssettypeId, "assettype-id", "", "Assettype id")
	cmd.Flags().StringVar(&flagClientId, "client-id", "", "Client id")
	cmd.Flags().StringVar(&flagClientname, "clientname", "", "Clientname")
	cmd.Flags().StringVar(&flagContractId, "contract-id", "", "Contract id")
	cmd.Flags().StringVar(&flagCountryCodeId, "country-code-id", "", "Country code id")
	cmd.Flags().StringVar(&flagDbcCompanyId, "dbc-company-id", "", "Dbc company id")
	cmd.Flags().StringVar(&flagDomain, "domain", "", "Domain")
	cmd.Flags().BoolVar(&flagExcludeNocharge, "exclude-nocharge", false, "Exclude nocharge")
	cmd.Flags().BoolVar(&flagExcludeNolinkedtypes, "exclude-nolinkedtypes", false, "Exclude nolinkedtypes")
	cmd.Flags().BoolVar(&flagExcludeZero, "exclude-zero", false, "Exclude zero")
	cmd.Flags().BoolVar(&flagIscustomfield, "iscustomfield", false, "Iscustomfield")
	cmd.Flags().BoolVar(&flagIstree, "istree", false, "Istree")
	cmd.Flags().StringVar(&flagLookupid, "lookupid", "", "Lookupid")
	cmd.Flags().IntVar(&flagOrdervaluetype, "ordervaluetype", 0, "Ordervaluetype")
	cmd.Flags().StringVar(&flagOutcomeId, "outcome-id", "", "Outcome id")
	cmd.Flags().BoolVar(&flagShowallcodes, "showallcodes", false, "Showallcodes")
	cmd.Flags().StringVar(&flagTicketId, "ticket-id", "", "Ticket id")
	cmd.Flags().BoolVar(&flagUnameaprestriction, "unameaprestriction", false, "Unameaprestriction")
	cmd.Flags().IntVar(&flagUse, "use", 0, "Use")
	cmd.Flags().IntVar(&flagUse2, "use2", 0, "Use2")

	return cmd
}
