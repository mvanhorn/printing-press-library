// Copyright 2026 dstevens. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newIntegrationDataListIntegrationdata60Cmd(flags *rootFlags) *cobra.Command {
	var flagAzuretenants string
	var flagContinuationToken string
	var flagDatatype string
	var flagDetailsId string
	var flagDoPaginate bool
	var flagIsLookup bool
	var flagKeyPair bool
	var flagNextLink string
	var flagSearch string

	cmd := &cobra.Command{
		Use:         "list-integrationdata-60",
		Short:       ".<br> 				Requires authentication.",
		Example:     "  halopsa-pp-cli integration-data list-integrationdata-60",
		Annotations: map[string]string{"pp:endpoint": "integration-data.list-integrationdata-60", "pp:method": "GET", "pp:path": "/IntegrationData/Get/MicrosoftCSP", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/IntegrationData/Get/MicrosoftCSP"
			params := map[string]string{}
			if flagAzuretenants != "" {
				params["azuretenants"] = fmt.Sprintf("%v", flagAzuretenants)
			}
			if flagContinuationToken != "" {
				params["continuationToken"] = fmt.Sprintf("%v", flagContinuationToken)
			}
			if flagDatatype != "" {
				params["datatype"] = fmt.Sprintf("%v", flagDatatype)
			}
			if flagDetailsId != "" {
				params["detailsId"] = fmt.Sprintf("%v", flagDetailsId)
			}
			if flagDoPaginate != false {
				params["doPaginate"] = fmt.Sprintf("%v", flagDoPaginate)
			}
			if flagIsLookup != false {
				params["isLookup"] = fmt.Sprintf("%v", flagIsLookup)
			}
			if flagKeyPair != false {
				params["keyPair"] = fmt.Sprintf("%v", flagKeyPair)
			}
			if flagNextLink != "" {
				params["nextLink"] = fmt.Sprintf("%v", flagNextLink)
			}
			if flagSearch != "" {
				params["search"] = fmt.Sprintf("%v", flagSearch)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "integration-data", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAzuretenants, "azuretenants", "", "Azuretenants")
	cmd.Flags().StringVar(&flagContinuationToken, "continuation-token", "", "Continuation token")
	cmd.Flags().StringVar(&flagDatatype, "datatype", "", "Datatype")
	cmd.Flags().StringVar(&flagDetailsId, "details-id", "", "Details id")
	cmd.Flags().BoolVar(&flagDoPaginate, "do-paginate", false, "Do paginate")
	cmd.Flags().BoolVar(&flagIsLookup, "is-lookup", false, "Is lookup")
	cmd.Flags().BoolVar(&flagKeyPair, "key-pair", false, "Key pair")
	cmd.Flags().StringVar(&flagNextLink, "next-link", "", "Next link")
	cmd.Flags().StringVar(&flagSearch, "search", "", "Search")

	return cmd
}
