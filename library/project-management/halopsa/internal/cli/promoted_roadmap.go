// Copyright 2026 dstevens. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newRoadmapPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagHalocrm bool
	var flagHaloitsm bool
	var flagHalopsa bool
	var flagHaloservicedesk bool
	var flagOrder string
	var flagOrderdesc bool
	var flagProductId string
	var flagRoadmapcolumnview bool

	cmd := &cobra.Command{
		Use:         "roadmap",
		Short:       ".<br> 				Requires authentication.",
		Long:        "Shortcut for 'roadmap list'. .<br> 				Requires authentication.",
		Example:     "  halopsa-pp-cli roadmap",
		Annotations: map[string]string{"pp:endpoint": "roadmap.list", "pp:method": "GET", "pp:path": "/Roadmap", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Roadmap"
			params := map[string]string{}
			if flagHalocrm != false {
				params["halocrm"] = fmt.Sprintf("%v", flagHalocrm)
			}
			if flagHaloitsm != false {
				params["haloitsm"] = fmt.Sprintf("%v", flagHaloitsm)
			}
			if flagHalopsa != false {
				params["halopsa"] = fmt.Sprintf("%v", flagHalopsa)
			}
			if flagHaloservicedesk != false {
				params["haloservicedesk"] = fmt.Sprintf("%v", flagHaloservicedesk)
			}
			if flagOrder != "" {
				params["order"] = fmt.Sprintf("%v", flagOrder)
			}
			if flagOrderdesc != false {
				params["orderdesc"] = fmt.Sprintf("%v", flagOrderdesc)
			}
			if flagProductId != "" {
				params["product_id"] = fmt.Sprintf("%v", flagProductId)
			}
			if flagRoadmapcolumnview != false {
				params["roadmapcolumnview"] = fmt.Sprintf("%v", flagRoadmapcolumnview)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "roadmap", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&flagHalocrm, "halocrm", false, "Halocrm")
	cmd.Flags().BoolVar(&flagHaloitsm, "haloitsm", false, "Haloitsm")
	cmd.Flags().BoolVar(&flagHalopsa, "halopsa", false, "Halopsa")
	cmd.Flags().BoolVar(&flagHaloservicedesk, "haloservicedesk", false, "Haloservicedesk")
	cmd.Flags().StringVar(&flagOrder, "order", "", "Order")
	cmd.Flags().BoolVar(&flagOrderdesc, "orderdesc", false, "Orderdesc")
	cmd.Flags().StringVar(&flagProductId, "product-id", "", "Product id")
	cmd.Flags().BoolVar(&flagRoadmapcolumnview, "roadmapcolumnview", false, "Roadmapcolumnview")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
