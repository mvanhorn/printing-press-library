// Copyright 2026 dstevens. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newEmailTemplateCreateEmailtemplateCmd(flags *rootFlags) *cobra.Command {
	var bodyIsclone bool
	var bodyWarning string
	var bodyAccessControl string
	var bodyAccessControlLevel int
	var bodyAnnouncementEndDate string
	var bodyApprovalprocesses string
	var bodyApprovalprocessrules string
	var bodyBodyHtml string
	var bodyBodySms string
	var bodyBodyText string
	var bodyCallScript string
	var bodyDescription string
	var bodyDesignDesign string
	var bodyDesignHtml string
	var bodyEmailrules string
	var bodyGuid string
	var bodyHeaderText string
	var bodyId int
	var bodyIntent string
	var bodyIsSingleObj bool
	var bodyMailboxes string
	var bodyMessagegroupId int
	var bodyMultiLanguage bool
	var bodyName string
	var bodyNotifications string
	var bodyOutcomes string
	var bodyPreviewBody string
	var bodyPreviewCall string
	var bodyPreviewLanguage int
	var bodyPreviewSms string
	var bodyPreviewSubject string
	var bodyProducts string
	var bodySectionid int
	var bodyStatuses string
	var bodySuppliers string
	var bodyTemplateGroup int
	var bodyTicketToPreview int
	var bodyTickettypes string
	var bodyType int
	var bodyUse int
	var bodyVariables string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-emailtemplate",
		Short:       "Create emailtemplate",
		Example:     "  halopsa-pp-cli email-template create-emailtemplate",
		Annotations: map[string]string{"pp:endpoint": "email-template.create-emailtemplate", "pp:method": "POST", "pp:path": "/EmailTemplate/preview"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/EmailTemplate/preview"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("isclone") {
					body["_isclone"] = bodyIsclone
				}
				if bodyWarning != "" {
					body["_warning"] = bodyWarning
				}
				if bodyAccessControl != "" {
					var parsedAccessControl any
					if err := json.Unmarshal([]byte(bodyAccessControl), &parsedAccessControl); err != nil {
						return fmt.Errorf("parsing --access-control JSON: %w", err)
					}
					body["access_control"] = parsedAccessControl
				}
				if bodyAccessControlLevel != 0 {
					body["access_control_level"] = bodyAccessControlLevel
				}
				if bodyAnnouncementEndDate != "" {
					body["announcement_end_date"] = bodyAnnouncementEndDate
				}
				if bodyApprovalprocesses != "" {
					var parsedApprovalprocesses any
					if err := json.Unmarshal([]byte(bodyApprovalprocesses), &parsedApprovalprocesses); err != nil {
						return fmt.Errorf("parsing --approvalprocesses JSON: %w", err)
					}
					body["approvalprocesses"] = parsedApprovalprocesses
				}
				if bodyApprovalprocessrules != "" {
					var parsedApprovalprocessrules any
					if err := json.Unmarshal([]byte(bodyApprovalprocessrules), &parsedApprovalprocessrules); err != nil {
						return fmt.Errorf("parsing --approvalprocessrules JSON: %w", err)
					}
					body["approvalprocessrules"] = parsedApprovalprocessrules
				}
				if bodyBodyHtml != "" {
					body["body_html"] = bodyBodyHtml
				}
				if bodyBodySms != "" {
					body["body_sms"] = bodyBodySms
				}
				if bodyBodyText != "" {
					body["body_text"] = bodyBodyText
				}
				if bodyCallScript != "" {
					body["call_script"] = bodyCallScript
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				{
					nestedDesign := map[string]any{}
					if bodyDesignDesign != "" {
						nestedDesign["design"] = bodyDesignDesign
					}
					if bodyDesignHtml != "" {
						nestedDesign["html"] = bodyDesignHtml
					}
					if len(nestedDesign) > 0 {
						body["design"] = nestedDesign
					}
				}
				if bodyEmailrules != "" {
					var parsedEmailrules any
					if err := json.Unmarshal([]byte(bodyEmailrules), &parsedEmailrules); err != nil {
						return fmt.Errorf("parsing --emailrules JSON: %w", err)
					}
					body["emailrules"] = parsedEmailrules
				}
				if bodyGuid != "" {
					body["guid"] = bodyGuid
				}
				if bodyHeaderText != "" {
					body["header_text"] = bodyHeaderText
				}
				if bodyId != 0 {
					body["id"] = bodyId
				}
				if bodyIntent != "" {
					body["intent"] = bodyIntent
				}
				if cmd.Flags().Changed("is-single-obj") {
					body["is_single_obj"] = bodyIsSingleObj
				}
				if bodyMailboxes != "" {
					var parsedMailboxes any
					if err := json.Unmarshal([]byte(bodyMailboxes), &parsedMailboxes); err != nil {
						return fmt.Errorf("parsing --mailboxes JSON: %w", err)
					}
					body["mailboxes"] = parsedMailboxes
				}
				if bodyMessagegroupId != 0 {
					body["messagegroup_id"] = bodyMessagegroupId
				}
				if cmd.Flags().Changed("multi-language") {
					body["multi_language"] = bodyMultiLanguage
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyNotifications != "" {
					var parsedNotifications any
					if err := json.Unmarshal([]byte(bodyNotifications), &parsedNotifications); err != nil {
						return fmt.Errorf("parsing --notifications JSON: %w", err)
					}
					body["notifications"] = parsedNotifications
				}
				if bodyOutcomes != "" {
					var parsedOutcomes any
					if err := json.Unmarshal([]byte(bodyOutcomes), &parsedOutcomes); err != nil {
						return fmt.Errorf("parsing --outcomes JSON: %w", err)
					}
					body["outcomes"] = parsedOutcomes
				}
				if bodyPreviewBody != "" {
					body["preview_body"] = bodyPreviewBody
				}
				if bodyPreviewCall != "" {
					body["preview_call"] = bodyPreviewCall
				}
				if bodyPreviewLanguage != 0 {
					body["preview_language"] = bodyPreviewLanguage
				}
				if bodyPreviewSms != "" {
					body["preview_sms"] = bodyPreviewSms
				}
				if bodyPreviewSubject != "" {
					body["preview_subject"] = bodyPreviewSubject
				}
				if bodyProducts != "" {
					var parsedProducts any
					if err := json.Unmarshal([]byte(bodyProducts), &parsedProducts); err != nil {
						return fmt.Errorf("parsing --products JSON: %w", err)
					}
					body["products"] = parsedProducts
				}
				if bodySectionid != 0 {
					body["sectionid"] = bodySectionid
				}
				if bodyStatuses != "" {
					var parsedStatuses any
					if err := json.Unmarshal([]byte(bodyStatuses), &parsedStatuses); err != nil {
						return fmt.Errorf("parsing --statuses JSON: %w", err)
					}
					body["statuses"] = parsedStatuses
				}
				if bodySuppliers != "" {
					var parsedSuppliers any
					if err := json.Unmarshal([]byte(bodySuppliers), &parsedSuppliers); err != nil {
						return fmt.Errorf("parsing --suppliers JSON: %w", err)
					}
					body["suppliers"] = parsedSuppliers
				}
				if bodyTemplateGroup != 0 {
					body["template_group"] = bodyTemplateGroup
				}
				if bodyTicketToPreview != 0 {
					body["ticket_to_preview"] = bodyTicketToPreview
				}
				if bodyTickettypes != "" {
					var parsedTickettypes any
					if err := json.Unmarshal([]byte(bodyTickettypes), &parsedTickettypes); err != nil {
						return fmt.Errorf("parsing --tickettypes JSON: %w", err)
					}
					body["tickettypes"] = parsedTickettypes
				}
				if bodyType != 0 {
					body["type"] = bodyType
				}
				if bodyUse != 0 {
					body["use"] = bodyUse
				}
				if bodyVariables != "" {
					var parsedVariables any
					if err := json.Unmarshal([]byte(bodyVariables), &parsedVariables); err != nil {
						return fmt.Errorf("parsing --variables JSON: %w", err)
					}
					body["variables"] = parsedVariables
				}
			}
			data, statusCode, err := c.PostWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "email-template", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "email-template", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "email-template", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "email-template", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "email-template",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "email-template", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "email-template", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyIsclone, "isclone", false, "Isclone")
	cmd.Flags().StringVar(&bodyWarning, "warning", "", "Warning")
	cmd.Flags().StringVar(&bodyAccessControl, "access-control", "", "Access control")
	cmd.Flags().IntVar(&bodyAccessControlLevel, "access-control-level", 0, "Access control level")
	cmd.Flags().StringVar(&bodyAnnouncementEndDate, "announcement-end-date", "", "Announcement end date")
	cmd.Flags().StringVar(&bodyApprovalprocesses, "approvalprocesses", "", "Approvalprocesses")
	cmd.Flags().StringVar(&bodyApprovalprocessrules, "approvalprocessrules", "", "Approvalprocessrules")
	cmd.Flags().StringVar(&bodyBodyHtml, "body-html", "", "Body html")
	cmd.Flags().StringVar(&bodyBodySms, "body-sms", "", "Body sms")
	cmd.Flags().StringVar(&bodyBodyText, "body-text", "", "Body text")
	cmd.Flags().StringVar(&bodyCallScript, "call-script", "", "Call script")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyDesignDesign, "design-design", "", "Design")
	cmd.Flags().StringVar(&bodyDesignHtml, "design-html", "", "Html")
	cmd.Flags().StringVar(&bodyEmailrules, "emailrules", "", "Emailrules")
	cmd.Flags().StringVar(&bodyGuid, "guid", "", "Guid")
	cmd.Flags().StringVar(&bodyHeaderText, "header-text", "", "Header text")
	cmd.Flags().IntVar(&bodyId, "id", 0, "Id")
	cmd.Flags().StringVar(&bodyIntent, "intent", "", "Intent")
	cmd.Flags().BoolVar(&bodyIsSingleObj, "is-single-obj", false, "Is single obj")
	cmd.Flags().StringVar(&bodyMailboxes, "mailboxes", "", "Mailboxes")
	cmd.Flags().IntVar(&bodyMessagegroupId, "messagegroup-id", 0, "Messagegroup id")
	cmd.Flags().BoolVar(&bodyMultiLanguage, "multi-language", false, "Multi language")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyNotifications, "notifications", "", "Notifications")
	cmd.Flags().StringVar(&bodyOutcomes, "outcomes", "", "Outcomes")
	cmd.Flags().StringVar(&bodyPreviewBody, "preview-body", "", "Preview body")
	cmd.Flags().StringVar(&bodyPreviewCall, "preview-call", "", "Preview call")
	cmd.Flags().IntVar(&bodyPreviewLanguage, "preview-language", 0, "Preview language")
	cmd.Flags().StringVar(&bodyPreviewSms, "preview-sms", "", "Preview sms")
	cmd.Flags().StringVar(&bodyPreviewSubject, "preview-subject", "", "Preview subject")
	cmd.Flags().StringVar(&bodyProducts, "products", "", "Products")
	cmd.Flags().IntVar(&bodySectionid, "sectionid", 0, "Sectionid")
	cmd.Flags().StringVar(&bodyStatuses, "statuses", "", "Statuses")
	cmd.Flags().StringVar(&bodySuppliers, "suppliers", "", "Suppliers")
	cmd.Flags().IntVar(&bodyTemplateGroup, "template-group", 0, "Template group")
	cmd.Flags().IntVar(&bodyTicketToPreview, "ticket-to-preview", 0, "Ticket to preview")
	cmd.Flags().StringVar(&bodyTickettypes, "tickettypes", "", "Tickettypes")
	cmd.Flags().IntVar(&bodyType, "type", 0, "Type")
	cmd.Flags().IntVar(&bodyUse, "use", 0, "Use")
	cmd.Flags().StringVar(&bodyVariables, "variables", "", "Variables")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
