// Copyright 2026 Paul Byrne and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newWeworkYardiListLocationsByGeoCmd(flags *rootFlags) *cobra.Command {
	var flagCity string
	var flagBoundnwLat float64
	var flagBoundnwLng float64
	var flagBoundseLat float64
	var flagBoundseLng float64
	var flagUserLatitude float64
	var flagUserLongitude float64
	var flagAccountUUID string
	var flagIsAuthenticated bool
	var flagIsOnDemandUser bool

	cmd := &cobra.Command{
		Use:         "list-locations-by-geo",
		Short:       "List WeWork buildings within a geo bounding box",
		Example:     "  wework-pp-cli wework-yardi list-locations-by-geo",
		Annotations: map[string]string{"pp:endpoint": "wework-yardi.list-locations-by-geo", "pp:method": "GET", "pp:path": "/wework-yardi/ondemand/get-locations-by-geo", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			path := "/wework-yardi/ondemand/get-locations-by-geo"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagCity != "" {
				params["city"] = formatCLIParamValue(flagCity)
			}
			if flagBoundnwLat != 0.0 {
				params["boundnwLat"] = formatCLIParamValue(flagBoundnwLat)
			}
			if flagBoundnwLng != 0.0 {
				params["boundnwLng"] = formatCLIParamValue(flagBoundnwLng)
			}
			if flagBoundseLat != 0.0 {
				params["boundseLat"] = formatCLIParamValue(flagBoundseLat)
			}
			if flagBoundseLng != 0.0 {
				params["boundseLng"] = formatCLIParamValue(flagBoundseLng)
			}
			if flagUserLatitude != 0.0 {
				params["userLatitude"] = formatCLIParamValue(flagUserLatitude)
			}
			if flagUserLongitude != 0.0 {
				params["userLongitude"] = formatCLIParamValue(flagUserLongitude)
			}
			if flagAccountUUID != "" {
				params["accountUUID"] = formatCLIParamValue(flagAccountUUID)
			}
			if flagIsAuthenticated != false {
				params["isAuthenticated"] = formatCLIParamValue(flagIsAuthenticated)
			}
			if flagIsOnDemandUser != false {
				params["isOnDemandUser"] = formatCLIParamValue(flagIsOnDemandUser)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "wework-yardi", false, path, params, nil, "locationsByGeo", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			outputData := data
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagCity, "city", "", "City")
	cmd.Flags().Float64Var(&flagBoundnwLat, "boundnw-lat", 0.0, "Boundnw lat")
	cmd.Flags().Float64Var(&flagBoundnwLng, "boundnw-lng", 0.0, "Boundnw lng")
	cmd.Flags().Float64Var(&flagBoundseLat, "boundse-lat", 0.0, "Boundse lat")
	cmd.Flags().Float64Var(&flagBoundseLng, "boundse-lng", 0.0, "Boundse lng")
	cmd.Flags().Float64Var(&flagUserLatitude, "user-latitude", 0.0, "User latitude")
	cmd.Flags().Float64Var(&flagUserLongitude, "user-longitude", 0.0, "User longitude")
	cmd.Flags().StringVar(&flagAccountUUID, "account-uuid", "", "Account uuid")
	cmd.Flags().BoolVar(&flagIsAuthenticated, "is-authenticated", false, "Is authenticated")
	cmd.Flags().BoolVar(&flagIsOnDemandUser, "is-on-demand-user", false, "Is on demand user")

	return cmd
}
