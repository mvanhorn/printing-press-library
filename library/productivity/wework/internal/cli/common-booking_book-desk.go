// Copyright 2026 Paul Byrne and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCommonBookingBookDeskCmd(flags *rootFlags) *cobra.Command {
	var bodyApplicationType int
	var bodyCardUuid string
	var bodyCreditCharged bool
	var bodyCurrency string
	var bodyEndTime string
	var bodyLocationID string
	var bodyLocationType string
	var bodyMailData string
	var bodyNotes string
	var bodyPlatFormTypeEnum int
	var bodyReservationID string
	var bodySpaceID string
	var bodySpaceType string
	var bodySpaceTypeID int
	var bodyStartTime string
	var bodyTriggerCalendarEvent bool
	var bodyUTCOffset string
	var bodyWeWorkSpaceID string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "book-desk",
		Aliases:     []string{"create"},
		Short:       "Creates a real desk reservation and charges the member's saved payment account.",
		Example:     "  wework-pp-cli common-booking book-desk",
		Annotations: map[string]string{"pp:endpoint": "common-booking.book-desk", "pp:method": "POST", "pp:path": "/common-booking/"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/common-booking/"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("application-type") || bodyApplicationType != 0 {
					bodyMap["ApplicationType"] = bodyApplicationType
				}
				if cmd.Flags().Changed("card-uuid") || bodyCardUuid != "" {
					bodyMap["CardUuid"] = bodyCardUuid
				}
				if cmd.Flags().Changed("credit-charged") {
					bodyMap["CreditCharged"] = bodyCreditCharged
				}
				if cmd.Flags().Changed("currency") || bodyCurrency != "" {
					bodyMap["Currency"] = bodyCurrency
				}
				if cmd.Flags().Changed("end-time") || bodyEndTime != "" {
					bodyMap["EndTime"] = bodyEndTime
				}
				if cmd.Flags().Changed("location-id") || bodyLocationID != "" {
					bodyMap["LocationID"] = bodyLocationID
				}
				if cmd.Flags().Changed("location-type") || bodyLocationType != "" {
					bodyMap["LocationType"] = bodyLocationType
				}
				if cmd.Flags().Changed("mail-data") || bodyMailData != "" {
					var parsedMailData any
					if err := json.Unmarshal([]byte(bodyMailData), &parsedMailData); err != nil {
						return fmt.Errorf("parsing --mail-data JSON: %w", err)
					}
					asMap, ok := parsedMailData.(map[string]any)
					if !ok {
						return fmt.Errorf("--mail-data must be a JSON object, got JSON %T", parsedMailData)
					}
					bodyMap["MailData"] = asMap
				}
				if cmd.Flags().Changed("notes") || bodyNotes != "" {
					bodyMap["Notes"] = bodyNotes
				}
				if cmd.Flags().Changed("plat-form-type-enum") || bodyPlatFormTypeEnum != 0 {
					bodyMap["PlatFormTypeEnum"] = bodyPlatFormTypeEnum
				}
				if cmd.Flags().Changed("reservation-id") || bodyReservationID != "" {
					bodyMap["ReservationID"] = bodyReservationID
				}
				if cmd.Flags().Changed("space-id") || bodySpaceID != "" {
					bodyMap["SpaceID"] = bodySpaceID
				}
				if cmd.Flags().Changed("space-type") || bodySpaceType != "" {
					bodyMap["SpaceType"] = bodySpaceType
				}
				if cmd.Flags().Changed("space-type-id") || bodySpaceTypeID != 0 {
					bodyMap["SpaceTypeID"] = bodySpaceTypeID
				}
				if cmd.Flags().Changed("start-time") || bodyStartTime != "" {
					bodyMap["StartTime"] = bodyStartTime
				}
				if cmd.Flags().Changed("trigger-calendar-event") {
					bodyMap["TriggerCalendarEvent"] = bodyTriggerCalendarEvent
				}
				if cmd.Flags().Changed("utc-offset") || bodyUTCOffset != "" {
					bodyMap["UTCOffset"] = bodyUTCOffset
				}
				if cmd.Flags().Changed("we-work-space-id") || bodyWeWorkSpaceID != "" {
					bodyMap["WeWorkSpaceID"] = bodyWeWorkSpaceID
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "common-booking", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "common-booking", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "common-booking", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "common-booking", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "common-booking", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "common-booking",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "common-booking", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "common-booking", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().IntVar(&bodyApplicationType, "application-type", 0, "Application type")
	cmd.Flags().StringVar(&bodyCardUuid, "card-uuid", "", "Saved payment account UUID (book only)")
	cmd.Flags().BoolVar(&bodyCreditCharged, "credit-charged", false, "Credits vs card (book only)")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "Currency")
	cmd.Flags().StringVar(&bodyEndTime, "end-time", "", "End time")
	cmd.Flags().StringVar(&bodyLocationID, "location-id", "", "Location id")
	cmd.Flags().StringVar(&bodyLocationType, "location-type", "", "Location type")
	cmd.Flags().StringVar(&bodyMailData, "mail-data", "", "Mail data")
	cmd.Flags().StringVar(&bodyNotes, "notes", "", "Notes")
	cmd.Flags().IntVar(&bodyPlatFormTypeEnum, "plat-form-type-enum", 0, "Plat form type enum")
	cmd.Flags().StringVar(&bodyReservationID, "reservation-id", "", "Reservation id")
	cmd.Flags().StringVar(&bodySpaceID, "space-id", "", "Space id")
	cmd.Flags().StringVar(&bodySpaceType, "space-type", "", "Space type")
	cmd.Flags().IntVar(&bodySpaceTypeID, "space-type-id", 0, "Space type id")
	cmd.Flags().StringVar(&bodyStartTime, "start-time", "", "Start time")
	cmd.Flags().BoolVar(&bodyTriggerCalendarEvent, "trigger-calendar-event", false, "Trigger calendar event")
	cmd.Flags().StringVar(&bodyUTCOffset, "utc-offset", "", "Utcoffset")
	cmd.Flags().StringVar(&bodyWeWorkSpaceID, "we-work-space-id", "", "We work space id")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
