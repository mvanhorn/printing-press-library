// Copyright 2026 Vincent Colombo and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"math"
	"os"
	"path/filepath"
	"strconv"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/productivity/tiimo/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/productivity/tiimo/internal/client"
	"github.com/mvanhorn/printing-press-library/library/productivity/tiimo/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/productivity/tiimo/internal/config"
	"github.com/mvanhorn/printing-press-library/library/productivity/tiimo/internal/learn"
	"github.com/mvanhorn/printing-press-library/library/productivity/tiimo/internal/mcp/bound"
	"github.com/mvanhorn/printing-press-library/library/productivity/tiimo/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/productivity/tiimo/internal/platform"
	"github.com/mvanhorn/printing-press-library/library/productivity/tiimo/internal/store"
)

const (
	// MCP hosts can fan out tool calls faster than a human CLI session.
	// Keep them on the same polite-client limiter path instead of disabling
	// pacing with rate=0; users can still tune human CLI calls with --rate-limit.
	defaultMCPRateLimit = 2
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	installFreshTenantGate(s)
	s.AddTool(
		mcplib.NewTool("activities_create",
			mcplib.WithDescription("Create an activity on the timeline. Required: profile_id, title, start-time. Optional: description, end-time, duration (plus 5 more). Returns the new Activity."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithString("title", mcplib.Required(), mcplib.Description("Activity title")),
			mcplib.WithString("description", mcplib.Description("Longer note attached to the activity")),
			mcplib.WithString("start-time", mcplib.Required(), mcplib.Description("Local start, e.g. 2026-08-14T09:00:00 (no timezone suffix)")),
			mcplib.WithString("end-time", mcplib.Description("Local end, e.g. 2026-08-14T09:30:00")),
			mcplib.WithNumber("duration", mcplib.Description("Duration in SECONDS (1800 = 30 minutes)")),
			mcplib.WithString("type", mcplib.Description("Activity type as used by the app")),
			mcplib.WithBoolean("all-day", mcplib.Description("Treat as an all-day activity")),
			mcplib.WithString("icon-type", mcplib.Description("Icon kind; UnicodeEmoji means iconId holds a literal emoji")),
			mcplib.WithString("icon", mcplib.Description("Emoji character when icon-type is UnicodeEmoji")),
			mcplib.WithNumber("sort-priority", mcplib.Description("Ordering weight within its bucket")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/profiles/{profile_id}/activities", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}, {PublicName: "title", WireName: "title", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "start-time", WireName: "startTime", Location: "body"}, {PublicName: "end-time", WireName: "endTime", Location: "body"}, {PublicName: "duration", WireName: "duration", Location: "body"}, {PublicName: "type", WireName: "type", Location: "body"}, {PublicName: "all-day", WireName: "isAllDay", Location: "body"}, {PublicName: "icon-type", WireName: "iconType", Location: "body"}, {PublicName: "icon", WireName: "iconId", Location: "body"}, {PublicName: "sort-priority", WireName: "sortPriority", Location: "body"}}, []string{"profile_id"}),
	)
	s.AddTool(
		mcplib.NewTool("activities_delete",
			mcplib.WithDescription("Delete an activity. Required: profile_id, activity_id. Destructive."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithString("activity_id", mcplib.Required(), mcplib.Description("Activity UUID")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/api/profiles/{profile_id}/activities/{activity_id}", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}, {PublicName: "activity_id", WireName: "activity_id", Location: "path"}}, []string{"profile_id", "activity_id"}),
	)
	s.AddTool(
		mcplib.NewTool("activities_get",
			mcplib.WithDescription("Get a single activity by ID. Required: profile_id, activity_id. Returns the Activity."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithString("activity_id", mcplib.Required(), mcplib.Description("Activity UUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/profiles/{profile_id}/activities/{activity_id}", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}, {PublicName: "activity_id", WireName: "activity_id", Location: "path"}}, []string{"profile_id", "activity_id"}),
	)
	s.AddTool(
		mcplib.NewTool("activities_list",
			mcplib.WithDescription("List activities in a date window, keyed by day. Required: profile_id, from-date. Optional: to-date. Returns the ActivityDay."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithString("from-date", mcplib.Required(), mcplib.Description("Window start, YYYY-MM-DD. Required by the API.")),
			mcplib.WithString("to-date", mcplib.Description("Window end, YYYY-MM-DD. Defaults to the start date.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/profiles/{profile_id}/activities", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}, {PublicName: "from-date", WireName: "fromDate", Location: "query"}, {PublicName: "to-date", WireName: "toDate", Location: "query"}}, []string{"profile_id"}),
	)
	s.AddTool(
		mcplib.NewTool("activities_update",
			mcplib.WithDescription("Replace an activity. The API rejects PATCH with 405, so send the full object. Required: profile_id, activity_id. Optional: title, start-time, end-time (plus 2 more). Returns the updated Activity."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithString("activity_id", mcplib.Required(), mcplib.Description("Activity UUID")),
			mcplib.WithString("title", mcplib.Description("Activity title")),
			mcplib.WithString("start-time", mcplib.Description("Local start timestamp")),
			mcplib.WithString("end-time", mcplib.Description("Local end timestamp")),
			mcplib.WithNumber("duration", mcplib.Description("Duration in seconds")),
			mcplib.WithString("completed-at", mcplib.Description("Completion timestamp; set to mark the activity done")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/api/profiles/{profile_id}/activities/{activity_id}", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}, {PublicName: "activity_id", WireName: "activity_id", Location: "path"}, {PublicName: "title", WireName: "title", Location: "body"}, {PublicName: "start-time", WireName: "startTime", Location: "body"}, {PublicName: "end-time", WireName: "endTime", Location: "body"}, {PublicName: "duration", WireName: "duration", Location: "body"}, {PublicName: "completed-at", WireName: "completedAt", Location: "body"}}, []string{"profile_id", "activity_id"}),
	)
	s.AddTool(
		mcplib.NewTool("calendars_activities",
			mcplib.WithDescription("List imported activities from one linked calendar. These are read-only in Tiimo. Required: profile_id, calendar_id, from-date. Optional: to-date. Returns the ActivityDay."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithString("calendar_id", mcplib.Required(), mcplib.Description("Linked calendar UUID")),
			mcplib.WithString("from-date", mcplib.Required(), mcplib.Description("Window start, YYYY-MM-DD")),
			mcplib.WithString("to-date", mcplib.Description("Window end, YYYY-MM-DD")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/externalCalendar/profiles/{profile_id}/externalCalendars/{calendar_id}/activities", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}, {PublicName: "calendar_id", WireName: "calendar_id", Location: "path"}, {PublicName: "from-date", WireName: "fromDate", Location: "query"}, {PublicName: "to-date", WireName: "toDate", Location: "query"}}, []string{"profile_id", "calendar_id"}),
	)
	s.AddTool(
		mcplib.NewTool("calendars_list",
			mcplib.WithDescription("List linked external calendars. Required: profile_id. Returns array of LinkedCalendar."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/externalCalendar/profiles/{profile_id}/linkedCalendars", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}}, []string{"profile_id"}),
	)
	s.AddTool(
		mcplib.NewTool("configuration_get",
			mcplib.WithDescription("Feature flags, locked features, and client configuration. Returns the Configuration."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/configuration", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("profiles_get",
			mcplib.WithDescription("Get a single profile. Required: profile_id. Returns the Profile."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/profiles/{profile_id}", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}}, []string{"profile_id"}),
	)
	s.AddTool(
		mcplib.NewTool("profiles_list",
			mcplib.WithDescription("List every profile on the account. Returns array of Profile."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/profiles", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("routines_list",
			mcplib.WithDescription("List routines in a date window. Required: profile_id, from. Optional: to. Returns array of Routine."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithString("from", mcplib.Required(), mcplib.Description("Window start, YYYY-MM-DD. Required by the API.")),
			mcplib.WithString("to", mcplib.Description("Window end, YYYY-MM-DD")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/profiles/{profile_id}/routines", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}, {PublicName: "from", WireName: "from", Location: "query"}, {PublicName: "to", WireName: "to", Location: "query"}}, []string{"profile_id"}),
	)
	s.AddTool(
		mcplib.NewTool("tags_list",
			mcplib.WithDescription("List tags. Required: profile_id. Returns array of Tag."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/profiles/{profile_id}/tags", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}}, []string{"profile_id"}),
	)
	s.AddTool(
		mcplib.NewTool("todo_lists_list",
			mcplib.WithDescription("List to-do lists with their tasks. Required: profile_id. Returns the TodoTaskListEnvelope."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/profiles/{profile_id}/todo-task-lists", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}}, []string{"profile_id"}),
	)
	s.AddTool(
		mcplib.NewTool("todo_tasks_create",
			mcplib.WithDescription("Create a to-do task. Returns 200 with the created task. Required: profile_id, list-id, title. Optional: notes, duration, icon-type (default: UnicodeEmoji) (plus 1 more)."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithString("list-id", mcplib.Required(), mcplib.Description("Target to-do list UUID")),
			mcplib.WithString("title", mcplib.Required(), mcplib.Description("Task title")),
			mcplib.WithString("notes", mcplib.Description("Free-text notes")),
			mcplib.WithNumber("duration", mcplib.Description("Estimated duration in seconds")),
			mcplib.WithString("icon-type", mcplib.Description("Icon kind")),
			mcplib.WithString("icon", mcplib.Description("Emoji character when icon-type is UnicodeEmoji")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/profiles/{profile_id}/todo-tasks", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}, {PublicName: "list-id", WireName: "todoTaskListId", Location: "body"}, {PublicName: "title", WireName: "title", Location: "body"}, {PublicName: "notes", WireName: "notes", Location: "body"}, {PublicName: "duration", WireName: "duration", Location: "body"}, {PublicName: "icon-type", WireName: "iconType", Location: "body"}, {PublicName: "icon", WireName: "iconId", Location: "body"}}, []string{"profile_id"}),
	)
	s.AddTool(
		mcplib.NewTool("todo_tasks_delete",
			mcplib.WithDescription("Delete a to-do task. Required: profile_id, task_id. Destructive."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithString("task_id", mcplib.Required(), mcplib.Description("Task UUID")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/api/profiles/{profile_id}/todo-tasks/{task_id}", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}, {PublicName: "task_id", WireName: "task_id", Location: "path"}}, []string{"profile_id", "task_id"}),
	)
	s.AddTool(
		mcplib.NewTool("todo_tasks_get",
			mcplib.WithDescription("Get a single to-do task. Required: profile_id, task_id. Returns the TodoTask."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithString("task_id", mcplib.Required(), mcplib.Description("Task UUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/profiles/{profile_id}/todo-tasks/{task_id}", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}, {PublicName: "task_id", WireName: "task_id", Location: "path"}}, []string{"profile_id", "task_id"}),
	)
	s.AddTool(
		mcplib.NewTool("todo_tasks_list",
			mcplib.WithDescription("List to-do tasks. Required: profile_id. Returns array of TodoTask."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/profiles/{profile_id}/todo-tasks", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}}, []string{"profile_id"}),
	)
	s.AddTool(
		mcplib.NewTool("todo_tasks_update",
			mcplib.WithDescription("Update a to-do task. Note the API updates via the COLLECTION path; item-level PUT returns 405. Required: profile_id, task-id, list-id. Optional: title, checked, checked-at."),
			mcplib.WithString("profile_id", mcplib.Required(), mcplib.Description("Profile UUID")),
			mcplib.WithString("task-id", mcplib.Required(), mcplib.Description("Task UUID being updated")),
			mcplib.WithString("list-id", mcplib.Required(), mcplib.Description("Owning to-do list UUID")),
			mcplib.WithString("title", mcplib.Description("Task title")),
			mcplib.WithBoolean("checked", mcplib.Description("Completion state. To-dos use isChecked, not isCompleted.")),
			mcplib.WithString("checked-at", mcplib.Description("Completion timestamp")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/api/profiles/{profile_id}/todo-tasks", false, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "profile_id", WireName: "profile_id", Location: "path"}, {PublicName: "task-id", WireName: "taskId", Location: "body"}, {PublicName: "list-id", WireName: "todoTaskListId", Location: "body"}, {PublicName: "title", WireName: "title", Location: "body"}, {PublicName: "checked", WireName: "isChecked", Location: "body"}, {PublicName: "checked-at", WireName: "checkedAt", Location: "body"}}, []string{"profile_id"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Synced records live in resources(resource_type, id, data); filter by resource_type and use json_extract on data, e.g. SELECT json_extract(data,'$.name') FROM resources WHERE resource_type='activities'.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

type mcpPageConfig struct {
	CursorParam    string
	NextCursorPath string
}

func formatMCPParamValue(v any) string {
	switch tv := v.(type) {
	case string:
		return tv
	case bool:
		return strconv.FormatBool(tv)
	case float64:
		if math.IsNaN(tv) || math.IsInf(tv, 0) {
			return strconv.FormatFloat(tv, 'f', -1, 64)
		}
		if math.Trunc(tv) == tv && math.Abs(tv) < 1e15 {
			return strconv.FormatInt(int64(tv), 10)
		}
		return strconv.FormatFloat(tv, 'f', -1, 64)
	case float32:
		f := float64(tv)
		if math.IsNaN(f) || math.IsInf(f, 0) {
			return strconv.FormatFloat(f, 'f', -1, 32)
		}
		if math.Trunc(f) == f && math.Abs(f) < 1e15 {
			return strconv.FormatInt(int64(f), 10)
		}
		return strconv.FormatFloat(f, 'f', -1, 32)
	default:
		// Composite values (a native []any / map[string]any from an array or
		// object param) reach this path when bound to a query or path slot;
		// JSON-encode them so the wire value is valid JSON rather than Go's
		// "[a b c]" / "map[...]" rendering. Body params never come through
		// here — they are stored natively in bodyArgs and marshalled there.
		if b, err := json.Marshal(v); err == nil {
			return string(b)
		}
		return fmt.Sprintf("%v", v)
	}
}

func mcpPathValue(v any) string {
	return cliutil.EscapePathParam(formatMCPParamValue(v))
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, pageConfig mcpPageConfig, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, platformSession, err := newMCPClient(ctx)
		if err != nil {
			return mcpToolError(err.Error()), nil
		}
		if platformSession != nil {
			defer platformSession.ZeroCredentials()
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()
		if err := cli.AdoptMCPOutputSemantics(platformSession, args); err != nil {
			return mcpToolError(err.Error()), nil
		}

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		mcpCursor := ""
		if pageConfig.CursorParam != "" {
			knownArgs["cursor"] = true
			if v, ok := args["cursor"]; ok {
				s, ok := v.(string)
				if !ok {
					return mcpToolError("cursor must be an opaque string returned by a previous MCP response"), nil
				}
				mcpCursor = s
				upstreamCursor, err := bound.UpstreamCursor(s)
				if err != nil {
					return mcpToolError(err.Error()), nil
				}
				if upstreamCursor != "" {
					params[pageConfig.CursorParam] = upstreamCursor
				}
			}
		}
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, mcpPathValue(v), 1)
			case "header":
				if headers == nil {
					headers = map[string]string{}
				}
				headers[binding.WireName] = formatMCPParamValue(v)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = formatMCPParamValue(v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, mcpPathValue(v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = formatMCPParamValue(v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				if readOnly {
					data, err = c.GetWithHeaders(ctx, path, params, headers)
				} else {
					data, err = c.GetMutatingWithHeaders(ctx, path, params, headers)
				}
				break
			}
			if readOnly {
				data, err = c.Get(ctx, path, params)
			} else {
				data, err = c.GetMutating(ctx, path, params)
			}
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PutQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PutQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
			}
		case "PATCH":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PatchQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PatchQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
			}
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcpToolError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcpToolTextWithPlatform("already exists (no-op)", platformSession), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcpToolError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set it with: tiimo-pp-cli auth set-token <token> or export TIIMO_TOKEN=\"your-token-here\"" +
					"\n      See API docs: https://api.tiimoapp.com" +
					"\n      Run 'tiimo-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcpToolError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: tiimo-pp-cli auth set-token <token> or export TIIMO_TOKEN=\"your-token-here\"" +
					"\n      See API docs: https://api.tiimoapp.com" +
					"\n      Run 'tiimo-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcpToolError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource. Check that they have the required permissions and match the API's expected auth scheme." +
					"\n      Set it with: tiimo-pp-cli auth set-token <token> or export TIIMO_TOKEN=\"your-token-here\"" +
					"\n      See API docs: https://api.tiimoapp.com" +
					"\n      Run 'tiimo-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcpToolTextWithPlatform("already deleted (no-op)", platformSession), nil
				}
				return mcpToolError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcpToolError("rate limited: " + msg), nil
			default:
				return mcpToolError(msg), nil
			}
		}

		if binaryResponse {
			encoded := base64.StdEncoding.EncodeToString(data)
			out, err := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      encoded,
				"byte_count":       len(data),
			})
			if err != nil {
				return mcpToolError(fmt.Sprintf("encoding binary result: %v", err)), nil
			}
			if len(out) > bound.MaxBytes {
				return mcpToolError(fmt.Sprintf("binary response is too large for MCP text output: %d response bytes encode to %d base64 bytes and %d MCP result bytes, exceeding the %d byte budget. Use the companion CLI command with --output <file> to save the payload locally.", len(data), len(encoded), len(out), bound.MaxBytes)), nil
			}
			result := string(out)
			if platformSession != nil {
				result = bound.WithMetadata(result, platformSession.OutputMetadata())
			}
			return mcplib.NewToolResultText(result), nil
		}
		if pageConfig.CursorParam != "" {
			return mcpToolPageResultTextWithPlatform(method, data, pageConfig, mcpCursor, platformSession), nil
		}
		return mcpToolResultTextWithPlatform(method, data, platformSession), nil
	}
}

func mcpToolResultText(method string, data json.RawMessage) *mcplib.CallToolResult {
	return mcpToolResultTextWithPlatform(method, data, nil)
}

func mcpToolTextWithPlatform(result string, platformSession *platform.Session) *mcplib.CallToolResult {
	if platformSession != nil {
		result = bound.WithMetadata(result, platformSession.OutputMetadata())
	}
	return mcplib.NewToolResultText(result)
}

func mcpToolResultTextWithPlatform(method string, data json.RawMessage, platformSession *platform.Session) *mcplib.CallToolResult {
	result := bound.EndpointResponse(method, data)
	return mcpToolTextWithPlatform(result, platformSession)
}

// mcpToolError keeps provider-controlled typed endpoint errors within the MCP
// text-result budget just like successful endpoint results.
func mcpToolError(message string) *mcplib.CallToolResult {
	return mcplib.NewToolResultError(bound.Text(message))
}

func mcpToolPageResultText(method string, data json.RawMessage, pageConfig mcpPageConfig, cursor string) *mcplib.CallToolResult {
	return mcpToolPageResultTextWithPlatform(method, data, pageConfig, cursor, nil)
}

func mcpToolPageResultTextWithPlatform(method string, data json.RawMessage, pageConfig mcpPageConfig, cursor string, platformSession *platform.Session) *mcplib.CallToolResult {
	result := bound.EndpointPageResponse(method, data, bound.PageOptions{
		Cursor:         cursor,
		CursorParam:    pageConfig.CursorParam,
		NextCursorPath: pageConfig.NextCursorPath,
	})
	if platformSession != nil {
		result = bound.WithMetadata(result, platformSession.OutputMetadata())
	}
	return mcplib.NewToolResultText(result)
}

func newMCPClient(ctx context.Context) (*client.Client, *platform.Session, error) {
	cfg, err := newMCPConfig()
	if err != nil {
		return nil, nil, err
	}
	c := newMCPClientFromConfig(cfg)
	session, err := cli.BindMCPClient(ctx, c)
	if err != nil {
		return nil, nil, err
	}
	return c, session, nil
}

func newMCPConfig() (*config.Config, error) {
	cfg, err := config.Load("")
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	return cfg, nil
}

func newMCPClientFromConfig(cfg *config.Config) *client.Client {
	c := client.New(cfg, 60*time.Second, defaultMCPRateLimit)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c
}

func mcpDBPath() (string, error) {
	dir, err := cliutil.DataDir()
	if err != nil {
		return "", err
	}
	return filepath.Join(dir, "data.db"), nil
}

type mcpStoreStatusKind string

const (
	mcpStoreStatusEmpty mcpStoreStatusKind = "empty"
	mcpStoreStatusReady mcpStoreStatusKind = "ready"
)

func openMCPReadOnlyStore(path string) (*store.Store, *mcplib.CallToolResult) {
	if _, err := os.Stat(path); err != nil {
		if os.IsNotExist(err) {
			return nil, mcplib.NewToolResultError(mcpMissingStoreMessage(path))
		}
		return nil, mcplib.NewToolResultError(fmt.Sprintf("checking local data store %s: %v", path, err))
	}
	db, err := store.OpenReadOnly(path)
	if err != nil {
		return nil, mcplib.NewToolResultError(fmt.Sprintf("opening local data store %s: %v. Run tiimo-pp-cli sync to refresh the store, or use live endpoint MCP tools for unsynced data.", path, err))
	}
	return db, nil
}

func mcpMissingStoreMessage(path string) string {
	return fmt.Sprintf("No local data store found at %s. Run tiimo-pp-cli sync before using MCP search/sql, or use live endpoint MCP tools for unsynced data.", path)
}

func mcpStoreStatus(db *store.Store) (mcpStoreStatusKind, error) {
	status, err := db.Status()
	if err != nil {
		return "", err
	}
	if len(status) == 0 {
		return mcpStoreStatusEmpty, nil
	}
	return mcpStoreStatusReady, nil
}

func mcpEmptyStoreNextStep() string {
	return "Run tiimo-pp-cli sync to populate the local SQLite store before using MCP search/sql."
}

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	path, err := mcpDBPath()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("resolving database: %v", err)), nil
	}
	db, toolErr := openMCPReadOnlyStore(path)
	if toolErr != nil {
		return toolErr, nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}
	storeStatus, err := mcpStoreStatus(db)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading store status: %v", err)), nil
	}

	return toolResultJSON(mcpSearchEnvelope(results, storeStatus))
}

func mcpSearchEnvelope(results []json.RawMessage, storeStatus mcpStoreStatusKind) map[string]any {
	if results == nil {
		results = []json.RawMessage{}
	}
	out := map[string]any{
		"count":        len(results),
		"results":      results,
		"store_status": storeStatus,
		"resumable":    false,
	}
	if len(results) == 0 {
		if storeStatus == mcpStoreStatusEmpty {
			out["next_step"] = mcpEmptyStoreNextStep()
		} else {
			out["next_step"] = "No local search matches. Try a broader query, a lower-specificity FTS expression, or sync again if data may be stale."
		}
	}
	return out
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate rejects multi-statement input, then applies an allowlist (SELECT or
// WITH only) AFTER stripping the leading whitespace, line comments, block
// comments, and semicolons that SQLite itself ignores before parsing. A naive
// HasPrefix check on a keyword blocklist is bypassable by prefixing the
// dangerous statement with "/* x */" or "-- x\n"; a naive leading-keyword
// allowlist is bypassable by appending "; ATTACH DATABASE ...". Combined with
// the empirical fact that modernc.org/sqlite's mode=ro does NOT block VACUUM
// INTO (writes a snapshot to a new file) or ATTACH DATABASE (opens a separate
// writable handle), either bypass produces silent exfiltration to an
// attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	stripped := stripLeadingSQLNoise(query)
	if hasTrailingSQLStatement(stripped) {
		return fmt.Errorf("only a single SELECT or WITH statement is allowed")
	}
	upper := strings.ToUpper(stripped)
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

// hasTrailingSQLStatement reports whether query contains a statement
// terminator followed by more executable SQL. A trailing semicolon is allowed;
// a second statement is not. Semicolons inside string literals, quoted
// identifiers, bracket identifiers, and comments are ignored to match SQLite's
// parser shape closely enough for this security gate.
func hasTrailingSQLStatement(query string) bool {
	inSingle := false
	inDouble := false
	inBacktick := false
	inBracket := false
	inLineComment := false
	inBlockComment := false

	for i := 0; i < len(query); i++ {
		ch := query[i]
		next := byte(0)
		if i+1 < len(query) {
			next = query[i+1]
		}

		switch {
		case inLineComment:
			if ch == '\n' {
				inLineComment = false
			}
			continue
		case inBlockComment:
			if ch == '*' && next == '/' {
				inBlockComment = false
				i++
			}
			continue
		case inSingle:
			if ch == '\'' {
				if next == '\'' {
					i++
					continue
				}
				inSingle = false
			}
			continue
		case inDouble:
			if ch == '"' {
				if next == '"' {
					i++
					continue
				}
				inDouble = false
			}
			continue
		case inBacktick:
			if ch == '`' {
				if next == '`' {
					i++
					continue
				}
				inBacktick = false
			}
			continue
		case inBracket:
			if ch == ']' {
				inBracket = false
			}
			continue
		}

		switch {
		case ch == '-' && next == '-':
			inLineComment = true
			i++
		case ch == '/' && next == '*':
			inBlockComment = true
			i++
		case ch == '\'':
			inSingle = true
		case ch == '"':
			inDouble = true
		case ch == '`':
			inBacktick = true
		case ch == '[':
			inBracket = true
		case ch == ';':
			if stripLeadingSQLNoise(query[i+1:]) != "" {
				return true
			}
			return false
		}
	}
	return false
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	path, err := mcpDBPath()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("resolving database: %v", err)), nil
	}
	db, toolErr := openMCPReadOnlyStore(path)
	if toolErr != nil {
		return toolErr, nil
	}
	defer db.Close()

	rows, err := db.DB().QueryContext(ctx, query)
	if err != nil {
		return mcplib.NewToolResultError(mcpSQLQueryError(err)), nil
	}
	defer rows.Close()

	cols, err := rows.Columns()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading columns: %v", err)), nil
	}
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		if err := rows.Scan(ptrs...); err != nil {
			return mcplib.NewToolResultError(fmt.Sprintf("scanning row: %v", err)), nil
		}
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}
	// rows.Next() stops on a mid-iteration error without failing the loop, so
	// skipping rows.Err() would return a truncated result set as success.
	if err := rows.Err(); err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading rows: %v", err)), nil
	}
	storeStatus, err := mcpStoreStatus(db)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading store status: %v", err)), nil
	}

	return toolResultJSON(mcpSQLEnvelope(results, cols, storeStatus))
}

func mcpSQLEnvelope(rows []map[string]any, columns []string, storeStatus mcpStoreStatusKind) map[string]any {
	if rows == nil {
		rows = []map[string]any{}
	}
	out := map[string]any{
		"count":        len(rows),
		"columns":      columns,
		"rows":         rows,
		"store_status": storeStatus,
		"resumable":    false,
	}
	if len(rows) == 0 {
		if storeStatus == mcpStoreStatusEmpty {
			out["next_step"] = mcpEmptyStoreNextStep()
		} else {
			out["next_step"] = "The read-only SQL query returned no rows. Check resource_type filters, json_extract paths, or run sync again if data may be stale."
		}
	}
	return out
}

func mcpSQLQueryError(err error) string {
	msg := err.Error()
	if strings.Contains(strings.ToLower(msg), "no such table") {
		return fmt.Sprintf("query failed: %v. Synced records live in resources(resource_type, id, data), not one SQL table per resource. Filter by resource_type, for example resource_type='activities', and read JSON fields with json_extract(data,'$.field').", err)
	}
	return fmt.Sprintf("query failed: %v", err)
}

// toolResultJSON renders v as the indented JSON body of an MCP text result,
// surfacing a marshal failure as a tool error instead of empty content.
func toolResultJSON(v any) (*mcplib.CallToolResult, error) {
	text, err := bound.JSON(v)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("encoding result: %v", err)), nil
	}
	return mcplib.NewToolResultText(text), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	paths := map[string]string{}
	if dir, err := cliutil.ConfigDir(); err == nil {
		paths["config_dir"] = dir
	}
	if dir, err := cliutil.DataDir(); err == nil {
		paths["data_dir"] = dir
	}
	if dir, err := cliutil.StateDir(); err == nil {
		paths["state_dir"] = dir
	}
	if dir, err := cliutil.CacheDir(); err == nil {
		paths["cache_dir"] = dir
	}
	ctx := map[string]any{
		"api":         "tiimo",
		"description": "Your Tiimo plan, finally exportable, queryable, and yours.",
		"archetype":   "project-management",
		"tool_count":  18,
		"paths":       paths,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion tiimo-pp-cli binary.",
		// learn_protocol is generated from the single shared source of
		// truth (the exported constant internal/learn.RecallFirstProtocol)
		// also consumed by the CLI agent-context command, so the MCP and
		// CLI agent surfaces cannot drift.
		"learn_protocol": learn.RecallFirstProtocol,
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "TIIMO_TOKEN",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
			"docs_url": "https://api.tiimoapp.com",
		},
		"resources": []map[string]any{
			{
				"name":        "activities",
				"description": "Timeline activities - the scheduled items that make up a day",
				"endpoints":   []string{"create", "delete", "get", "list", "update"},
				"searchable":  true,
				"writable":    true,
			},
			{
				"name":        "calendars",
				"description": "External calendars linked into Tiimo",
				"endpoints":   []string{"activities", "list"},
				"searchable":  true,
			},
			{
				"name":        "configuration",
				"description": "Server-side feature configuration",
				"endpoints":   []string{"get"},
			},
			{
				"name":        "profiles",
				"description": "Tiimo profiles (an account can hold several shared profiles)",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "routines",
				"description": "Saved routines",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "tags",
				"description": "User-defined tags applied to activities and tasks",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "todo_lists",
				"description": "To-do lists that group tasks",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "todo_tasks",
				"description": "To-do tasks - the priority-bucketed list, separate from the timeline",
				"endpoints":   []string{"create", "delete", "get", "list", "update"},
				"searchable":  true,
				"writable":    true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Export", "command": "export", "description": "Stream a paginated resource straight to JSON or JSONL. Use backup for a whole-planner snapshot and feed for calendar or spreadsheet formats.", "rationale": "Tiimo declines data export as a stated design decision, so only an external tool can put your own schedule in your hands.", "via": "mcp-command-mirror"},
			{"name": "Calendar feed", "command": "feed", "description": "Generate a subscribable read-only iCalendar file of your Tiimo activities.", "rationale": "Users asked for exactly this as a compromise on two-way calendar sync and were declined; the local mirror makes it a one-liner.", "via": "mcp-command-mirror"},
			{"name": "Local backup", "command": "backup", "description": "Write every mirrored record - activities, to-dos, tags, routines, calendars - to one portable JSON snapshot. This is the whole-planner export Tiimo does not offer.", "rationale": "A product that declines to export your data offers no lock-in insurance; a schema-independent snapshot of the local mirror is that insurance.", "via": "mcp-command-mirror"},
			{"name": "Plan-versus-actual drift", "command": "drift", "description": "Show which activities consistently start late, overrun, or spend the most time paused.", "rationale": "Requires joining planned start and duration against the actual start, actual duration, and paused time across months of history - Tiimo records all of it and surfaces almost none of it.", "via": "mcp-command-mirror"},
			{"name": "Checklist stall points", "command": "stalls", "description": "Find the exact checklist step where a multi-step routine tends to break down.", "rationale": "Needs per-step checked state and timestamps across many days of nested checklists, which no single API response exposes.", "via": "mcp-command-mirror"},
			{"name": "Routine adherence", "command": "adherence", "description": "Completion rate for each recurring activity over a window.", "rationale": "Requires historical snapshots of recurring activities in the local store; the API only ever returns the current window.", "via": "mcp-command-mirror"},
			{"name": "Overlap detection", "command": "overlaps", "description": "List activities that are double-booked against each other.", "rationale": "Requested on Tiimo's feedback board and never shipped; a local interval join over the timeline answers it directly.", "via": "mcp-command-mirror"},
			{"name": "Free-gap finder", "command": "gaps", "description": "Find the unscheduled windows in a day that are at least a given length.", "rationale": "Users asked for visualization of gaps in the day; the complement of scheduled intervals is a local computation.", "via": "mcp-command-mirror"},
			{"name": "Rolling window", "command": "rolling", "description": "A true rolling multi-day view starting from today.", "rationale": "Tiimo's fixed week view means the following week arrives unannounced; a rolling window is trivial from the local mirror.", "via": "mcp-command-mirror"},
			{"name": "Day capacity", "command": "capacity", "description": "Committed versus free minutes per day, broken down by time-of-day bucket.", "rationale": "Requires summing activity durations per grouping bucket, which the app never totals for you.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Export", "insight": "Tiimo declines data export as a stated design decision, so only an external tool can put your own schedule in your hands."},
			{"topic": "Calendar feed", "insight": "Users asked for exactly this as a compromise on two-way calendar sync and were declined; the local mirror makes it a one-liner."},
			{"topic": "Local backup", "insight": "A product with no export path offers no lock-in insurance; the local mirror is the insurance."},
			{"topic": "Plan-versus-actual drift", "insight": "Requires joining planned start and duration against the actual start, actual duration, and paused time across months of history - Tiimo records all of it and surfaces almost none of it."},
			{"topic": "Checklist stall points", "insight": "Needs per-step checked state and timestamps across many days of nested checklists, which no single API response exposes."},
			{"topic": "Routine adherence", "insight": "Requires historical snapshots of recurring activities in the local store; the API only ever returns the current window."},
			{"topic": "Overlap detection", "insight": "Requested on Tiimo's feedback board and never shipped; a local interval join over the timeline answers it directly."},
			{"topic": "Free-gap finder", "insight": "Users asked for visualization of gaps in the day; the complement of scheduled intervals is a local computation."},
			{"topic": "Rolling window", "insight": "Tiimo's fixed week view means the following week arrives unannounced; a rolling window is trivial from the local mirror."},
			{"topic": "Day capacity", "insight": "Requires summing activity durations per grouping bucket, which the app never totals for you."},
			{"topic": "Finding stale work", "insight": "Use the stale command or sql query to find items not updated recently. More reliable than scanning list results manually."},
			{"topic": "Load analysis", "insight": "When analyzing team workload, filter by assignee and status. Raw counts without status filtering are misleading."},
			{"topic": "Bulk operations", "insight": "For bulk status changes, prefer update endpoints over delete+create. Most PM APIs track history on updates."},
		},
	}
	return toolResultJSON(ctx)
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
