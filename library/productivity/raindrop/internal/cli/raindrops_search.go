// Copyright 2026 srijits and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"
	"strconv"

	"github.com/spf13/cobra"
)

func newRaindropsSearchCmd(flags *rootFlags) *cobra.Command {
	var flagSearch string
	var flagCollection int
	var flagTags string
	var flagCreatedStart string
	var flagCreatedEnd string
	var flagImportant bool
	var flagMedia string
	var flagPerpage int
	var flagPage string
	var flagSort string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "search",
		Short:       "Search bookmarks with advanced filtering options",
		Example:     "  raindrop-pp-cli raindrops search",
		Annotations: map[string]string{"pp:endpoint": "raindrops.search", "pp:method": "GET", "pp:path": "/raindrops/0", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("sort") {
				allowedSort := []string{"+created", "-created", "+title", "-title", "+domain", "-domain", "+score", "-score"}
				validSort := false
				for _, v := range allowedSort {
					if flagSort == v {
						validSort = true
						break
					}
				}
				if !validSort {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSort, "sort", allowedSort)
				}
			}
			path := "/raindrops/0"
			if flagCollection != 0 {
				path = "/raindrops/" + strconv.Itoa(flagCollection)
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			if flagTags != "" {
				path = appendArrayQueryParam(path, "tags", flagTags, "form", true)
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "raindrops", path, map[string]string{
				"search":       formatCLIParamValue(flagSearch),
				"createdStart": formatCLIParamValue(flagCreatedStart),
				"createdEnd":   formatCLIParamValue(flagCreatedEnd),
				"important":    formatCLIParamValue(flagImportant),
				"media":        formatCLIParamValue(flagMedia),
				"perpage":      formatCLIParamValue(flagPerpage),
				"page":         formatCLIParamValue(flagPage),
				"sort":         formatCLIParamValue(flagSort),
			}, nil, flagAll, "page", "page", "perpage", 50, "", "", "items", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			outputData := collectionItemsForOutput(data, path)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagSearch, "search", "", "Search query for filtering results")
	cmd.Flags().IntVar(&flagCollection, "collection", 0, "Filter by collection ID")
	cmd.Flags().StringVar(&flagTags, "tags", "", "Filter by tags")
	cmd.Flags().StringVar(&flagCreatedStart, "created-start", "", "Filter by creation date (start)")
	cmd.Flags().StringVar(&flagCreatedEnd, "created-end", "", "Filter by creation date (end)")
	cmd.Flags().BoolVar(&flagImportant, "important", false, "Filter by important flag")
	cmd.Flags().StringVar(&flagMedia, "media", "", "Filter by media type")
	cmd.Flags().IntVar(&flagPerpage, "perpage", 50, "Number of items per page")
	cmd.Flags().StringVar(&flagPage, "page", "0", "Page number for pagination")
	cmd.Flags().StringVar(&flagSort, "sort", "-created", "Sort order for results (one of: +created, -created, +title, -title, +domain, -domain, +score, -score)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
