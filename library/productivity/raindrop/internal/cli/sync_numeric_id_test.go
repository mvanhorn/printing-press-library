// Copyright 2026 srijits and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"path/filepath"
	"reflect"
	"testing"

	"github.com/mvanhorn/printing-press-library/library/productivity/raindrop/internal/store"
)

func TestSyncSingleObject_PreservesLargeIntegerResourceIDs(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	db, err := store.Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer db.Close()

	if err := upsertSingleObject(db, "numeric_ids", json.RawMessage(`{"id": 55043301, "name": "large"}`)); err != nil {
		t.Fatalf("upsertSingleObject: %v", err)
	}

	var got string
	if err := db.DB().QueryRow(
		`SELECT id FROM resources WHERE resource_type = ?`,
		"numeric_ids",
	).Scan(&got); err != nil {
		t.Fatalf("query resource id: %v", err)
	}
	if got != "55043301" {
		t.Fatalf("resource id = %q, want %q", got, "55043301")
	}

	var literalMatches int
	if err := db.DB().QueryRow(
		`SELECT COUNT(*) FROM resources WHERE resource_type = ? AND id = '55043301'`,
		"numeric_ids",
	).Scan(&literalMatches); err != nil {
		t.Fatalf("count literal id matches: %v", err)
	}
	if literalMatches != 1 {
		t.Fatalf("literal id matches = %d, want 1", literalMatches)
	}
}

func TestRaindropsSyncUsesAllBookmarksEndpoint(t *testing.T) {
	t.Parallel()
	got, err := syncResourcePath("raindrops")
	if err != nil {
		t.Fatalf("syncResourcePath: %v", err)
	}
	if got != "/raindrops/0" {
		t.Fatalf("syncResourcePath(raindrops) = %q, want /raindrops/0", got)
	}
}

func TestDefaultSyncResourcesDoNotDuplicateAliases(t *testing.T) {
	t.Parallel()
	want := []string{"collections", "highlights", "raindrops", "tags"}
	if got := defaultSyncResources(); !reflect.DeepEqual(got, want) {
		t.Fatalf("defaultSyncResources() = %v, want %v", got, want)
	}
}
