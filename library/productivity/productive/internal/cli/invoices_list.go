// Copyright 2026 Derick Ng and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newInvoicesListCmd(flags *rootFlags) *cobra.Command {
	var flagFilterNumberContains string
	var flagFilterInvoiceTypeEq string
	var flagFilterCompanyEq string
	var flagFilterSubsidiaryEq string
	var flagFilterInvoicedOnGtEq string
	var flagFilterInvoicedOnLtEq string
	var flagFilterPayOnGtEq string
	var flagFilterPayOnLtEq string
	var flagFilterPaidOnGtEq string
	var flagFilterIsCreditedEq string
	var flagFilterCreatedAtGtEq string
	var flagPageSize int
	var flagPageNumber int
	var flagSort string
	var flagInclude string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "List invoices",
		Example:     "  productive-pp-cli invoices list",
		Annotations: map[string]string{"pp:endpoint": "invoices.list", "pp:method": "GET", "pp:path": "/invoices", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			path := "/invoices"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagFilterNumberContains != "" {
				params["filter[number][contains]"] = formatCLIParamValue(flagFilterNumberContains)
			}
			if flagFilterInvoiceTypeEq != "" {
				params["filter[invoice_type][eq]"] = formatCLIParamValue(flagFilterInvoiceTypeEq)
			}
			if flagFilterCompanyEq != "" {
				params["filter[company_id][eq]"] = formatCLIParamValue(flagFilterCompanyEq)
			}
			if flagFilterSubsidiaryEq != "" {
				params["filter[subsidiary_id][eq]"] = formatCLIParamValue(flagFilterSubsidiaryEq)
			}
			if flagFilterInvoicedOnGtEq != "" {
				params["filter[invoiced_on][gt_eq]"] = formatCLIParamValue(flagFilterInvoicedOnGtEq)
			}
			if flagFilterInvoicedOnLtEq != "" {
				params["filter[invoiced_on][lt_eq]"] = formatCLIParamValue(flagFilterInvoicedOnLtEq)
			}
			if flagFilterPayOnGtEq != "" {
				params["filter[pay_on][gt_eq]"] = formatCLIParamValue(flagFilterPayOnGtEq)
			}
			if flagFilterPayOnLtEq != "" {
				params["filter[pay_on][lt_eq]"] = formatCLIParamValue(flagFilterPayOnLtEq)
			}
			if flagFilterPaidOnGtEq != "" {
				params["filter[paid_on][gt_eq]"] = formatCLIParamValue(flagFilterPaidOnGtEq)
			}
			if flagFilterIsCreditedEq != "" {
				params["filter[credited]"] = formatCLIParamValue(flagFilterIsCreditedEq)
			}
			if flagFilterCreatedAtGtEq != "" {
				params["filter[created_at][gt_eq]"] = formatCLIParamValue(flagFilterCreatedAtGtEq)
			}
			if flagPageSize != 0 {
				params["page[size]"] = formatCLIParamValue(flagPageSize)
			}
			if flagPageNumber != 0 {
				params["page[number]"] = formatCLIParamValue(flagPageNumber)
			}
			if flagSort != "" {
				params["sort"] = formatCLIParamValue(flagSort)
			}
			if flagInclude != "" {
				params["include"] = formatCLIParamValue(flagInclude)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "invoices", true, path, params, nil, "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagFilterNumberContains, "number", "", "Invoice number contains")
	cmd.Flags().StringVar(&flagFilterInvoiceTypeEq, "invoice-type", "", "1=Invoice, 2=Credit note")
	cmd.Flags().StringVar(&flagFilterCompanyEq, "company", "", "Client company id")
	cmd.Flags().StringVar(&flagFilterSubsidiaryEq, "subsidiary", "", "Subsidiary id")
	cmd.Flags().StringVar(&flagFilterInvoicedOnGtEq, "issued-from", "", "Issued on/after YYYY-MM-DD")
	cmd.Flags().StringVar(&flagFilterInvoicedOnLtEq, "issued-to", "", "Issued on/before YYYY-MM-DD")
	cmd.Flags().StringVar(&flagFilterPayOnGtEq, "due-from", "", "Due on/after YYYY-MM-DD")
	cmd.Flags().StringVar(&flagFilterPayOnLtEq, "due-to", "", "Due on/before YYYY-MM-DD")
	cmd.Flags().StringVar(&flagFilterPaidOnGtEq, "paid-from", "", "Paid on/after YYYY-MM-DD")
	cmd.Flags().StringVar(&flagFilterIsCreditedEq, "is-credited", "", "true to only credited invoices")
	cmd.Flags().StringVar(&flagFilterCreatedAtGtEq, "created-after", "", "Created on/after (ISO)")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 100, "Results per page (max 200)")
	cmd.Flags().IntVar(&flagPageNumber, "page-number", 1, "Page number (1-based)")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Sort field; prefix - for descending (e.g. -created_at)")
	cmd.Flags().StringVar(&flagInclude, "include", "", "Comma-separated related resources to sideload")

	return cmd
}
