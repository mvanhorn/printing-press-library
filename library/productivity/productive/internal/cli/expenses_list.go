// Copyright 2026 Derick Ng and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newExpensesListCmd(flags *rootFlags) *cobra.Command {
	var flagFilterServiceEq string
	var flagFilterPersonEq string
	var flagFilterDateGtEq string
	var flagFilterDateLtEq string
	var flagFilterBudgetEq string
	var flagFilterCompanyEq string
	var flagFilterProjectEq string
	var flagFilterVendorEq string
	var flagFilterStatusEq string
	var flagFilterInvoicingStatusEq string
	var flagFilterApprovalStatusEq string
	var flagPageSize int
	var flagPageNumber int
	var flagSort string
	var flagInclude string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "List expenses",
		Example:     "  productive-pp-cli expenses list",
		Annotations: map[string]string{"pp:endpoint": "expenses.list", "pp:method": "GET", "pp:path": "/expenses", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			path := "/expenses"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagFilterServiceEq != "" {
				params["filter[service_id][eq]"] = formatCLIParamValue(flagFilterServiceEq)
			}
			if flagFilterPersonEq != "" {
				params["filter[person_id][eq]"] = formatCLIParamValue(flagFilterPersonEq)
			}
			if flagFilterDateGtEq != "" {
				params["filter[date][gt_eq]"] = formatCLIParamValue(flagFilterDateGtEq)
			}
			if flagFilterDateLtEq != "" {
				params["filter[date][lt_eq]"] = formatCLIParamValue(flagFilterDateLtEq)
			}
			if flagFilterBudgetEq != "" {
				params["filter[deal_id][eq]"] = formatCLIParamValue(flagFilterBudgetEq)
			}
			if flagFilterCompanyEq != "" {
				params["filter[company_id][eq]"] = formatCLIParamValue(flagFilterCompanyEq)
			}
			if flagFilterProjectEq != "" {
				params["filter[project_id][eq]"] = formatCLIParamValue(flagFilterProjectEq)
			}
			if flagFilterVendorEq != "" {
				params["filter[vendor_id][eq]"] = formatCLIParamValue(flagFilterVendorEq)
			}
			if flagFilterStatusEq != "" {
				params["filter[status][eq]"] = formatCLIParamValue(flagFilterStatusEq)
			}
			if flagFilterInvoicingStatusEq != "" {
				params["filter[invoicing_status][eq]"] = formatCLIParamValue(flagFilterInvoicingStatusEq)
			}
			if flagFilterApprovalStatusEq != "" {
				params["filter[approval_status][eq]"] = formatCLIParamValue(flagFilterApprovalStatusEq)
			}
			if flagPageSize != 0 {
				params["page[size]"] = formatCLIParamValue(flagPageSize)
			}
			if flagPageNumber != 0 {
				params["page[number]"] = formatCLIParamValue(flagPageNumber)
			}
			if flagSort != "" {
				params["sort"] = formatCLIParamValue(flagSort)
			}
			if flagInclude != "" {
				params["include"] = formatCLIParamValue(flagInclude)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "expenses", true, path, params, nil, "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagFilterServiceEq, "service", "", "Service id")
	cmd.Flags().StringVar(&flagFilterPersonEq, "person", "", "Person id")
	cmd.Flags().StringVar(&flagFilterDateGtEq, "date-from", "", "Only rows with date >= YYYY-MM-DD")
	cmd.Flags().StringVar(&flagFilterDateLtEq, "date-to", "", "Only rows with date <= YYYY-MM-DD")
	cmd.Flags().StringVar(&flagFilterBudgetEq, "budget", "", "Budget/deal id")
	cmd.Flags().StringVar(&flagFilterCompanyEq, "company", "", "Client company id")
	cmd.Flags().StringVar(&flagFilterProjectEq, "project", "", "Project id")
	cmd.Flags().StringVar(&flagFilterVendorEq, "vendor", "", "Vendor company id")
	cmd.Flags().StringVar(&flagFilterStatusEq, "status", "", "Payment status: 1=Paid, 2=Unpaid")
	cmd.Flags().StringVar(&flagFilterInvoicingStatusEq, "invoicing-status", "", "1=Not invoiced, 2=Drafted, 3=Finalized")
	cmd.Flags().StringVar(&flagFilterApprovalStatusEq, "approval-status", "", "1=Approved,2=Unapproved,3=Rejected,4=Auto-approved")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 100, "Results per page (max 200)")
	cmd.Flags().IntVar(&flagPageNumber, "page-number", 1, "Page number (1-based)")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Sort field; prefix - for descending (e.g. -created_at)")
	cmd.Flags().StringVar(&flagInclude, "include", "", "Comma-separated related resources to sideload")

	return cmd
}
