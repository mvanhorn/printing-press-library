// Copyright 2026 Derick Ng and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newPeopleListCmd(flags *rootFlags) *cobra.Command {
	var flagFilterFirstNameContains string
	var flagFilterLastNameContains string
	var flagFilterEmailContains string
	var flagFilterPersonTypeEq string
	var flagFilterStatusEq string
	var flagFilterCompanyEq string
	var flagFilterTitleContains string
	var flagFilterTeamEq string
	var flagFilterManagerEq string
	var flagFilterSubsidiaryEq string
	var flagFilterCustomRoleEq string
	var flagPageSize int
	var flagPageNumber int
	var flagSort string
	var flagInclude string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "List people",
		Example:     "  productive-pp-cli people list",
		Annotations: map[string]string{"pp:endpoint": "people.list", "pp:method": "GET", "pp:path": "/people", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			path := "/people"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagFilterFirstNameContains != "" {
				params["filter[first_name][contains]"] = formatCLIParamValue(flagFilterFirstNameContains)
			}
			if flagFilterLastNameContains != "" {
				params["filter[last_name][contains]"] = formatCLIParamValue(flagFilterLastNameContains)
			}
			if flagFilterEmailContains != "" {
				params["filter[email][contains]"] = formatCLIParamValue(flagFilterEmailContains)
			}
			if flagFilterPersonTypeEq != "" {
				params["filter[person_type][eq]"] = formatCLIParamValue(flagFilterPersonTypeEq)
			}
			if flagFilterStatusEq != "" {
				params["filter[status][eq]"] = formatCLIParamValue(flagFilterStatusEq)
			}
			if flagFilterCompanyEq != "" {
				params["filter[company_id][eq]"] = formatCLIParamValue(flagFilterCompanyEq)
			}
			if flagFilterTitleContains != "" {
				params["filter[title][contains]"] = formatCLIParamValue(flagFilterTitleContains)
			}
			if flagFilterTeamEq != "" {
				params["filter[team][eq]"] = formatCLIParamValue(flagFilterTeamEq)
			}
			if flagFilterManagerEq != "" {
				params["filter[manager_id][eq]"] = formatCLIParamValue(flagFilterManagerEq)
			}
			if flagFilterSubsidiaryEq != "" {
				params["filter[subsidiary_id][eq]"] = formatCLIParamValue(flagFilterSubsidiaryEq)
			}
			if flagFilterCustomRoleEq != "" {
				params["filter[custom_role_id][eq]"] = formatCLIParamValue(flagFilterCustomRoleEq)
			}
			if flagPageSize != 0 {
				params["page[size]"] = formatCLIParamValue(flagPageSize)
			}
			if flagPageNumber != 0 {
				params["page[number]"] = formatCLIParamValue(flagPageNumber)
			}
			if flagSort != "" {
				params["sort"] = formatCLIParamValue(flagSort)
			}
			if flagInclude != "" {
				params["include"] = formatCLIParamValue(flagInclude)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "people", true, path, params, nil, "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagFilterFirstNameContains, "first-name", "", "First name contains")
	cmd.Flags().StringVar(&flagFilterLastNameContains, "last-name", "", "Last name contains")
	cmd.Flags().StringVar(&flagFilterEmailContains, "email", "", "Email contains")
	cmd.Flags().StringVar(&flagFilterPersonTypeEq, "person-type", "", "1=User,2=Contact,3=Placeholder,4=Agent")
	cmd.Flags().StringVar(&flagFilterStatusEq, "status", "", "1=Active, 2=Deactivated")
	cmd.Flags().StringVar(&flagFilterCompanyEq, "company", "", "Company id")
	cmd.Flags().StringVar(&flagFilterTitleContains, "title", "", "Job title contains")
	cmd.Flags().StringVar(&flagFilterTeamEq, "team", "", "Team id")
	cmd.Flags().StringVar(&flagFilterManagerEq, "manager", "", "Manager person id")
	cmd.Flags().StringVar(&flagFilterSubsidiaryEq, "subsidiary", "", "Subsidiary id")
	cmd.Flags().StringVar(&flagFilterCustomRoleEq, "custom-role", "", "Permission set (role) id")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 100, "Results per page (max 200)")
	cmd.Flags().IntVar(&flagPageNumber, "page-number", 1, "Page number (1-based)")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Sort field; prefix - for descending (e.g. -created_at)")
	cmd.Flags().StringVar(&flagInclude, "include", "", "Comma-separated related resources to sideload")

	return cmd
}
