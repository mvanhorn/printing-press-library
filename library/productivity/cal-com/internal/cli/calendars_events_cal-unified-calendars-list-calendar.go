// Copyright 2026 trevin-chow. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCalendarsEventsCalUnifiedCalendarsListCalendarCmd(flags *rootFlags) *cobra.Command {
	var flagCalendar string
	var flagFrom string
	var flagTo string
	var flagTimeZone string
	var flagCalendarId string

	cmd := &cobra.Command{
		Use:   "cal-unified-calendars-list-calendar",
		Short: "List events in a date range for the authenticated user's calendar. Currently only Google Calendar is supported.",
		Example: "  cal-com-pp-cli calendars events cal-unified-calendars-list-calendar",
		Annotations: map[string]string{"pp:endpoint": "events.cal-unified-calendars-list-calendar", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("from") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "from")
			}
			if !cmd.Flags().Changed("to") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "to")
			}
			if cmd.Flags().Changed("calendar") {
				allowedCalendar := []string{ "google", "office365", "apple" }
				validCalendar := false
				for _, v := range allowedCalendar {
					if flagCalendar == v {
						validCalendar = true
						break
					}
				}
				if !validCalendar {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "calendar", flagCalendar, allowedCalendar)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/calendars/{calendar}/events"
			path = replacePathParam(path, "calendar", fmt.Sprintf("%v", flagCalendar))
			params := map[string]string{}
			if flagFrom != "" {
				params["from"] = fmt.Sprintf("%v", flagFrom)
			}
			if flagTo != "" {
				params["to"] = fmt.Sprintf("%v", flagTo)
			}
			if flagTimeZone != "" {
				params["timeZone"] = fmt.Sprintf("%v", flagTimeZone)
			}
			if flagCalendarId != "" {
				params["calendarId"] = fmt.Sprintf("%v", flagCalendarId)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "events", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagCalendar, "calendar", "google", "Calendar (one of: google, office365, apple)")
	cmd.Flags().StringVar(&flagFrom, "from", "", "Start of the date range (ISO 8601 date or date-time)")
	cmd.Flags().StringVar(&flagTo, "to", "", "End of the date range (ISO 8601 date or date-time)")
	cmd.Flags().StringVar(&flagTimeZone, "time-zone", "", "IANA time zone for the request (e.g. America/New_York)")
	cmd.Flags().StringVar(&flagCalendarId, "calendar-id", "primary", "Calendar ID. Use 'primary' for the user's primary calendar, or the external ID of a connected calendar.")

	return cmd
}
