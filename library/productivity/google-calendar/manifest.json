{
  "manifest_version": "0.3",
  "name": "google-calendar-pp-mcp",
  "display_name": "Google Calendar",
  "version": "4.14.0",
  "description": "Every gcalcli and MCP feature, plus a local SQLite mirror that makes free-time search, conflict detection, and 'what changed' instant and offline.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/google-calendar-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/google-calendar-pp-mcp",
      "args": [],
      "env": {
        "GOOGLE_CALENDAR_CLIENT_ID": "${user_config.google_calendar_client_id}",
        "GOOGLE_CALENDAR_CLIENT_SECRET": "${user_config.google_calendar_client_secret}",
        "GOOGLE_CALENDAR_TOKEN": "${user_config.google_calendar_token}"
      }
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  },
  "user_config": {
    "google_calendar_client_id": {
      "type": "string",
      "title": "GOOGLE_CALENDAR_CLIENT_ID",
      "description": "OAuth2 client ID from your Google Cloud project. Used by auth login.",
      "sensitive": false,
      "required": true
    },
    "google_calendar_client_secret": {
      "type": "string",
      "title": "GOOGLE_CALENDAR_CLIENT_SECRET",
      "description": "OAuth2 client secret from your Google Cloud project. Used by auth login.",
      "sensitive": true,
      "required": true
    },
    "google_calendar_token": {
      "type": "string",
      "title": "GOOGLE_CALENDAR_TOKEN",
      "description": "OAuth2 access token, populated automatically by auth login.",
      "sensitive": true,
      "required": false
    }
  }
}
