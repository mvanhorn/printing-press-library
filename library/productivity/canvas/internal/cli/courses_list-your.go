// Copyright 2026 martin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCoursesListYourCmd(flags *rootFlags) *cobra.Command {
	var flagEnrollmentType string
	var flagEnrollmentRole string
	var flagEnrollmentRoleId string
	var flagEnrollmentState string
	var flagExcludeBlueprintCourses bool
	var flagInclude string
	var flagState string

	cmd := &cobra.Command{
		Use:         "list-your",
		Aliases:     []string{"list"},
		Short:       "List your courses",
		Example:     "  canvas-lms-pp-cli courses list-your",
		Annotations: map[string]string{"pp:endpoint": "courses.list-your", "pp:method": "GET", "pp:path": "/v1/courses", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/courses"
			params := map[string]string{}
			if flagEnrollmentType != "" {
				params["enrollment_type"] = fmt.Sprintf("%v", flagEnrollmentType)
			}
			if flagEnrollmentRole != "" {
				params["enrollment_role"] = fmt.Sprintf("%v", flagEnrollmentRole)
			}
			if flagEnrollmentRoleId != "" {
				params["enrollment_role_id"] = fmt.Sprintf("%v", flagEnrollmentRoleId)
			}
			if flagEnrollmentState != "" {
				params["enrollment_state"] = fmt.Sprintf("%v", flagEnrollmentState)
			}
			if flagExcludeBlueprintCourses != false {
				params["exclude_blueprint_courses"] = fmt.Sprintf("%v", flagExcludeBlueprintCourses)
			}
			if flagInclude != "" {
				params["include"] = fmt.Sprintf("%v", flagInclude)
			}
			if flagState != "" {
				params["state"] = fmt.Sprintf("%v", flagState)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "courses", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagEnrollmentType, "enrollment-type", "", "When set, only return courses where the user is enrolled as this type. For example, set to 'teacher' to return only...")
	cmd.Flags().StringVar(&flagEnrollmentRole, "enrollment-role", "", "Deprecated When set, only return courses where the user is enrolled with the specified course-level role. This can...")
	cmd.Flags().StringVar(&flagEnrollmentRoleId, "enrollment-role-id", "", "When set, only return courses where the user is enrolled with the specified course-level role. This can be a role...")
	cmd.Flags().StringVar(&flagEnrollmentState, "enrollment-state", "", "When set, only return courses where the user has an enrollment with the given state. This will respect...")
	cmd.Flags().BoolVar(&flagExcludeBlueprintCourses, "exclude-blueprint-courses", false, "When set, only return courses that are not configured as blueprint courses.")
	cmd.Flags().StringVar(&flagInclude, "include", "", "- 'needs_grading_count': Optional information to include with each Course. When needs_grading_count is given, and...")
	cmd.Flags().StringVar(&flagState, "state", "", "If set, only return courses that are in the given state(s). By default, 'available' is returned for students and...")

	return cmd
}
