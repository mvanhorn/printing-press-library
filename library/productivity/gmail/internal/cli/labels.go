// Copyright 2026 Derik Parkinson and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"
)

func newLabelsCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:         "labels",
		Short:       "List, get, create (idempotent-by-name), and rename labels — no delete, no raw update/patch",
		Hidden:      true,
		Annotations: map[string]string{"pp:typed-exit-codes": "0,2"},
		RunE:        parentNoSubcommandRunE(flags),
	}

	// Raw label mutations (generated create/update/patch/delete) are
	// deliberately NOT registered (grill R1-C1, R2-C2). The only label
	// mutations in this binary are the hand-written idempotent-by-name
	// `create` and the ledgered `rename` (labels_safe.go). Label DELETE
	// exists nowhere — not even as a transport-allowlist shape.
	cmd.AddCommand(newLabelsGetCmd(flags))
	cmd.AddCommand(newLabelsListCmd(flags))
	cmd.AddCommand(newLabelsCreateSafeCmd(flags))
	cmd.AddCommand(newLabelsRenameCmd(flags))
	return cmd
}
