# Removal validation

The browser-only local `redact` and `workflow run` commands were removed from
this publish candidate. Their novel-feature entries, command registrations,
MCP feature descriptions, documentation, fixtures, and command tests were
removed as well. The remaining CLI does not advertise either command.

Full dogfood and publish validation were rerun after removal.
