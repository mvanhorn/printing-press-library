// Copyright 2026 Chris Hatton and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newApiConnectorsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAuthId string
	var bodyConfigRateLimitMaxRequestsPerWindow float64
	var bodyConfigRedirectsMaxRedirects float64
	var bodyConfigRetryMaxRetries float64
	var bodyConfigRetryRetryableStatusCodes string
	var bodyConfigTimeoutRequestSec float64
	var bodyDescription string
	var bodyName string
	var bodyParamsBody string
	var bodyParamsHeaders string
	var bodyParamsMethod string
	var bodyParamsPathParams string
	var bodyParamsQueryParams string
	var bodyParamsUrl string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "This endpoint creates a new API connector that defines how to call an external HTTP endpoint from within Sigma.",
		Example:     "  sigma-computing-pp-cli api-connectors create --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "api-connectors.create", "pp:method": "POST", "pp:path": "/v2/api-connectors"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("config-rate-limit-max-requests-per-window") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "config-rate-limit-max-requests-per-window")
				}
				if !cmd.Flags().Changed("config-redirects-max-redirects") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "config-redirects-max-redirects")
				}
				if !cmd.Flags().Changed("config-retry-max-retries") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "config-retry-max-retries")
				}
				if !cmd.Flags().Changed("config-retry-retryable-status-codes") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "config-retry-retryable-status-codes")
				}
				if !cmd.Flags().Changed("config-timeout-request-sec") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "config-timeout-request-sec")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("params-body") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "params-body")
				}
				if !cmd.Flags().Changed("params-headers") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "params-headers")
				}
				if !cmd.Flags().Changed("params-method") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "params-method")
				}
				if !cmd.Flags().Changed("params-path-params") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "params-path-params")
				}
				if !cmd.Flags().Changed("params-query-params") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "params-query-params")
				}
				if !cmd.Flags().Changed("params-url") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "params-url")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/api-connectors"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAuthId != "" {
					body["authId"] = bodyAuthId
				}
				{
					nestedConfig := map[string]any{}
					{
						nestedConfigRateLimit := map[string]any{}
						if bodyConfigRateLimitMaxRequestsPerWindow != 0.0 {
							nestedConfigRateLimit["maxRequestsPerWindow"] = bodyConfigRateLimitMaxRequestsPerWindow
						}
						if len(nestedConfigRateLimit) > 0 {
							nestedConfig["rateLimit"] = nestedConfigRateLimit
						}
					}
					{
						nestedConfigRedirects := map[string]any{}
						if bodyConfigRedirectsMaxRedirects != 0.0 {
							nestedConfigRedirects["maxRedirects"] = bodyConfigRedirectsMaxRedirects
						}
						if len(nestedConfigRedirects) > 0 {
							nestedConfig["redirects"] = nestedConfigRedirects
						}
					}
					{
						nestedConfigRetry := map[string]any{}
						if bodyConfigRetryMaxRetries != 0.0 {
							nestedConfigRetry["maxRetries"] = bodyConfigRetryMaxRetries
						}
						if bodyConfigRetryRetryableStatusCodes != "" {
							var parsedConfigRetryRetryableStatusCodes any
							if err := json.Unmarshal([]byte(bodyConfigRetryRetryableStatusCodes), &parsedConfigRetryRetryableStatusCodes); err != nil {
								return fmt.Errorf("parsing --config-retry-retryable-status-codes JSON: %w", err)
							}
							nestedConfigRetry["retryableStatusCodes"] = parsedConfigRetryRetryableStatusCodes
						}
						if len(nestedConfigRetry) > 0 {
							nestedConfig["retry"] = nestedConfigRetry
						}
					}
					{
						nestedConfigTimeout := map[string]any{}
						if bodyConfigTimeoutRequestSec != 0.0 {
							nestedConfigTimeout["requestSec"] = bodyConfigTimeoutRequestSec
						}
						if len(nestedConfigTimeout) > 0 {
							nestedConfig["timeout"] = nestedConfigTimeout
						}
					}
					if len(nestedConfig) > 0 {
						body["config"] = nestedConfig
					}
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				{
					nestedParams := map[string]any{}
					if bodyParamsBody != "" {
						nestedParams["body"] = bodyParamsBody
					}
					if bodyParamsHeaders != "" {
						var parsedParamsHeaders any
						if err := json.Unmarshal([]byte(bodyParamsHeaders), &parsedParamsHeaders); err != nil {
							return fmt.Errorf("parsing --params-headers JSON: %w", err)
						}
						nestedParams["headers"] = parsedParamsHeaders
					}
					if bodyParamsMethod != "" {
						nestedParams["method"] = bodyParamsMethod
					}
					if bodyParamsPathParams != "" {
						var parsedParamsPathParams any
						if err := json.Unmarshal([]byte(bodyParamsPathParams), &parsedParamsPathParams); err != nil {
							return fmt.Errorf("parsing --params-path-params JSON: %w", err)
						}
						nestedParams["pathParams"] = parsedParamsPathParams
					}
					if bodyParamsQueryParams != "" {
						var parsedParamsQueryParams any
						if err := json.Unmarshal([]byte(bodyParamsQueryParams), &parsedParamsQueryParams); err != nil {
							return fmt.Errorf("parsing --params-query-params JSON: %w", err)
						}
						nestedParams["queryParams"] = parsedParamsQueryParams
					}
					if bodyParamsUrl != "" {
						nestedParams["url"] = bodyParamsUrl
					}
					if len(nestedParams) > 0 {
						body["params"] = nestedParams
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "api-connectors", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "api-connectors", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "api-connectors", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "api-connectors", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "api-connectors", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "api-connectors",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "api-connectors", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "api-connectors", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAuthId, "auth-id", "", "Identifier of the API credential (`apiCredentialId`) used to authenticate requests.")
	cmd.Flags().Float64Var(&bodyConfigRateLimitMaxRequestsPerWindow, "config-rate-limit-max-requests-per-window", 0.0, "Max requests per window")
	cmd.Flags().Float64Var(&bodyConfigRedirectsMaxRedirects, "config-redirects-max-redirects", 0.0, "Max redirects")
	cmd.Flags().Float64Var(&bodyConfigRetryMaxRetries, "config-retry-max-retries", 0.0, "Max retries")
	cmd.Flags().StringVar(&bodyConfigRetryRetryableStatusCodes, "config-retry-retryable-status-codes", "", "Retryable status codes")
	cmd.Flags().Float64Var(&bodyConfigTimeoutRequestSec, "config-timeout-request-sec", 0.0, "Request sec")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Human-readable description.")
	cmd.Flags().StringVar(&bodyName, "name", "", "Display name of the connector.")
	cmd.Flags().StringVar(&bodyParamsBody, "params-body", "", "Raw request body template.")
	cmd.Flags().StringVar(&bodyParamsHeaders, "params-headers", "", "Static or dynamic request headers.")
	cmd.Flags().StringVar(&bodyParamsMethod, "params-method", "", "HTTP method to invoke.")
	cmd.Flags().StringVar(&bodyParamsPathParams, "params-path-params", "", "Path parameter placeholders resolved into the URL.")
	cmd.Flags().StringVar(&bodyParamsQueryParams, "params-query-params", "", "Query string parameters.")
	cmd.Flags().StringVar(&bodyParamsUrl, "params-url", "", "Request URL. Must resolve to a hostname matching the associated credential's allowlist.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
