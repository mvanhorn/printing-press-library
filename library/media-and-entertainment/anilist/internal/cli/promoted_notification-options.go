// Copyright 2026 avanderheyde and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newNotificationOptionsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagAbout string
	var flagTitleLanguage string
	var flagDisplayAdultContent bool
	var flagAiringNotifications bool
	var flagScoreFormat string
	var flagRowOrder string
	var flagProfileColor string
	var flagDonatorBadge string
	var flagTimezone string
	var flagActivityMergeTime int
	var flagStaffNameLanguage string
	var flagRestrictMessagesToFollowing bool
	var bodyDisabled bool
	var bodyType string

	cmd := &cobra.Command{
		Use:         "notification-options",
		Short:       "Update a notificationoption",
		Long:        "Update a notificationoption",
		Example:     "  anilist-pp-cli notification-options",
		Annotations: map[string]string{"pp:endpoint": "notification-options.update", "pp:method": "PATCH", "pp:path": "/graphql"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("title-language") {
				allowedTitleLanguage := []string{"\"Official title in it's native language\"", "\"Official title in it's native language, stylised by media creator\"", "\"The official english title\"", "\"The official english title, stylised by media creator\"", "\"The romanization of the native language title\"", "\"The romanization of the native language title, stylised by media creator\"", "ENGLISH", "ENGLISH_STYLISED", "NATIVE", "NATIVE_STYLISED", "ROMAJI", "ROMAJI_STYLISED"}
				validTitleLanguage := false
				for _, v := range allowedTitleLanguage {
					if flagTitleLanguage == v {
						validTitleLanguage = true
						break
					}
				}
				if !validTitleLanguage {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagTitleLanguage, "title-language", allowedTitleLanguage)
				}
			}
			if cmd.Flags().Changed("score-format") {
				allowedScoreFormat := []string{"\"A float from 0-10 with 1 decimal place\"", "\"An integer from 0-10\"", "\"An integer from 0-100\"", "\"An integer from 0-3. Should be represented in Smileys. 0 => No Score, 1 => :(, 2 => :|, 3 => :)\"", "\"An integer from 0-5. Should be represented in Stars\"", "POINT_10", "POINT_100", "POINT_10_DECIMAL", "POINT_3", "POINT_5"}
				validScoreFormat := false
				for _, v := range allowedScoreFormat {
					if flagScoreFormat == v {
						validScoreFormat = true
						break
					}
				}
				if !validScoreFormat {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagScoreFormat, "score-format", allowedScoreFormat)
				}
			}
			if cmd.Flags().Changed("staff-name-language") {
				allowedStaffNameLanguage := []string{"\"The romanization of the staff or character's native name\"", "\"The romanization of the staff or character's native name, with western name ordering\"", "\"The staff or character's name in their native language\"", "NATIVE", "ROMAJI", "ROMAJI_WESTERN"}
				validStaffNameLanguage := false
				for _, v := range allowedStaffNameLanguage {
					if flagStaffNameLanguage == v {
						validStaffNameLanguage = true
						break
					}
				}
				if !validStaffNameLanguage {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagStaffNameLanguage, "staff-name-language", allowedStaffNameLanguage)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/graphql"
			params := map[string]string{}
			if flagAbout != "" {
				params["about"] = formatCLIParamValue(flagAbout)
			}
			if flagTitleLanguage != "" {
				params["titleLanguage"] = formatCLIParamValue(flagTitleLanguage)
			}
			if flagDisplayAdultContent != false {
				params["displayAdultContent"] = formatCLIParamValue(flagDisplayAdultContent)
			}
			if flagAiringNotifications != false {
				params["airingNotifications"] = formatCLIParamValue(flagAiringNotifications)
			}
			if flagScoreFormat != "" {
				params["scoreFormat"] = formatCLIParamValue(flagScoreFormat)
			}
			if flagRowOrder != "" {
				params["rowOrder"] = formatCLIParamValue(flagRowOrder)
			}
			if flagProfileColor != "" {
				params["profileColor"] = formatCLIParamValue(flagProfileColor)
			}
			if flagDonatorBadge != "" {
				params["donatorBadge"] = formatCLIParamValue(flagDonatorBadge)
			}
			if flagTimezone != "" {
				params["timezone"] = formatCLIParamValue(flagTimezone)
			}
			if flagActivityMergeTime != 0 {
				params["activityMergeTime"] = formatCLIParamValue(flagActivityMergeTime)
			}
			if flagStaffNameLanguage != "" {
				params["staffNameLanguage"] = formatCLIParamValue(flagStaffNameLanguage)
			}
			if flagRestrictMessagesToFollowing != false {
				params["restrictMessagesToFollowing"] = formatCLIParamValue(flagRestrictMessagesToFollowing)
			}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			bodyMap := map[string]any{}
			var body any = bodyMap
			if cmd.Flags().Changed("disabled") {
				bodyMap["disabled"] = bodyDisabled
			}
			if bodyType != "" {
				bodyMap["type"] = bodyType
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "notification-options", data, "data.UpdateUser")
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagAbout, "about", "", "")
	cmd.Flags().StringVar(&flagTitleLanguage, "title-language", "", " (one of: \"Official title in it's native language\", \"Official title in it's native language, stylised by media creator\", \"The official english title\", \"The official english title, stylised by media creator\", \"The romanization of the native language title\", \"The romanization of the native language title, stylised by media creator\", ENGLISH, ENGLISH_STYLISED, NATIVE, NATIVE_STYLISED, ROMAJI, ROMAJI_STYLISED)")
	cmd.Flags().BoolVar(&flagDisplayAdultContent, "display-adult-content", false, "")
	cmd.Flags().BoolVar(&flagAiringNotifications, "airing-notifications", false, "")
	cmd.Flags().StringVar(&flagScoreFormat, "score-format", "", " (one of: \"A float from 0-10 with 1 decimal place\", \"An integer from 0-10\", \"An integer from 0-100\", \"An integer from 0-3. Should be represented in Smileys. 0 => No Score, 1 => :(, 2 => :|, 3 => :)\", \"An integer from 0-5. Should be represented in Stars\", POINT_10, POINT_100, POINT_10_DECIMAL, POINT_3, POINT_5)")
	cmd.Flags().StringVar(&flagRowOrder, "row-order", "", "")
	cmd.Flags().StringVar(&flagProfileColor, "profile-color", "", "")
	cmd.Flags().StringVar(&flagDonatorBadge, "donator-badge", "", "")
	cmd.Flags().StringVar(&flagTimezone, "timezone", "", "")
	cmd.Flags().IntVar(&flagActivityMergeTime, "activity-merge-time", 0, "")
	cmd.Flags().StringVar(&flagStaffNameLanguage, "staff-name-language", "", " (one of: \"The romanization of the staff or character's native name\", \"The romanization of the staff or character's native name, with western name ordering\", \"The staff or character's name in their native language\", NATIVE, ROMAJI, ROMAJI_WESTERN)")
	cmd.Flags().BoolVar(&flagRestrictMessagesToFollowing, "restrict-messages-to-following", false, "")
	cmd.Flags().BoolVar(&bodyDisabled, "disabled", false, "")
	cmd.Flags().StringVar(&bodyType, "type", "", "")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
