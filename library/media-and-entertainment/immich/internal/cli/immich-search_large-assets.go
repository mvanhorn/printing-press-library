// Copyright 2026 avanderheyde and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newImmichSearchLargeAssetsCmd(flags *rootFlags) *cobra.Command {
	var flagAlbumIds string
	var flagCity string
	var flagCountry string
	var flagCreatedAfter string
	var flagCreatedBefore string
	var flagIsEncoded bool
	var flagIsFavorite bool
	var flagIsMotion bool
	var flagIsNotInAlbum bool
	var flagIsOffline bool
	var flagLensModel string
	var flagLibraryId string
	var flagMake string
	var flagMinFileSize int
	var flagModel string
	var flagOcr string
	var flagPersonIds string
	var flagRating int
	var flagSize int
	var flagState string
	var flagTagIds string
	var flagTakenAfter string
	var flagTakenBefore string
	var flagTrashedAfter string
	var flagTrashedBefore string
	var flagType string
	var flagUpdatedAfter string
	var flagUpdatedBefore string
	var flagVisibility string
	var flagWithDeleted bool
	var flagWithExif bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "large-assets",
		Short:       "Search for assets that are considered large based on specified criteria.",
		Example:     "  immich-pp-cli immich-search large-assets",
		Annotations: map[string]string{"pp:endpoint": "immich-search.large-assets", "pp:method": "POST", "pp:path": "/search/large-assets"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("type") {
				allowedType := []string{"IMAGE", "VIDEO", "AUDIO", "OTHER"}
				validType := false
				for _, v := range allowedType {
					if flagType == v {
						validType = true
						break
					}
				}
				if !validType {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagType, "type", allowedType)
				}
			}
			if cmd.Flags().Changed("visibility") {
				allowedVisibility := []string{"archive", "timeline", "hidden", "locked"}
				validVisibility := false
				for _, v := range allowedVisibility {
					if flagVisibility == v {
						validVisibility = true
						break
					}
				}
				if !validVisibility {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagVisibility, "visibility", allowedVisibility)
				}
			}
			if !stdinBody {
			}
			path := "/search/large-assets"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			if flagAlbumIds != "" {
				path = appendArrayQueryParam(path, "albumIds", flagAlbumIds, "form", true)
			}
			if flagPersonIds != "" {
				path = appendArrayQueryParam(path, "personIds", flagPersonIds, "form", true)
			}
			if flagTagIds != "" {
				path = appendArrayQueryParam(path, "tagIds", flagTagIds, "form", true)
			}
			params := map[string]string{}
			if flagCity != "" {
				params["city"] = formatCLIParamValue(flagCity)
			}
			if flagCountry != "" {
				params["country"] = formatCLIParamValue(flagCountry)
			}
			if flagCreatedAfter != "" {
				params["createdAfter"] = formatCLIParamValue(flagCreatedAfter)
			}
			if flagCreatedBefore != "" {
				params["createdBefore"] = formatCLIParamValue(flagCreatedBefore)
			}
			if flagIsEncoded != false {
				params["isEncoded"] = formatCLIParamValue(flagIsEncoded)
			}
			if flagIsFavorite != false {
				params["isFavorite"] = formatCLIParamValue(flagIsFavorite)
			}
			if flagIsMotion != false {
				params["isMotion"] = formatCLIParamValue(flagIsMotion)
			}
			if flagIsNotInAlbum != false {
				params["isNotInAlbum"] = formatCLIParamValue(flagIsNotInAlbum)
			}
			if flagIsOffline != false {
				params["isOffline"] = formatCLIParamValue(flagIsOffline)
			}
			if flagLensModel != "" {
				params["lensModel"] = formatCLIParamValue(flagLensModel)
			}
			if flagLibraryId != "" {
				params["libraryId"] = formatCLIParamValue(flagLibraryId)
			}
			if flagMake != "" {
				params["make"] = formatCLIParamValue(flagMake)
			}
			if flagMinFileSize != 0 {
				params["minFileSize"] = formatCLIParamValue(flagMinFileSize)
			}
			if flagModel != "" {
				params["model"] = formatCLIParamValue(flagModel)
			}
			if flagOcr != "" {
				params["ocr"] = formatCLIParamValue(flagOcr)
			}
			if flagRating != 0 {
				params["rating"] = formatCLIParamValue(flagRating)
			}
			if flagSize != 0 {
				params["size"] = formatCLIParamValue(flagSize)
			}
			if flagState != "" {
				params["state"] = formatCLIParamValue(flagState)
			}
			if flagTakenAfter != "" {
				params["takenAfter"] = formatCLIParamValue(flagTakenAfter)
			}
			if flagTakenBefore != "" {
				params["takenBefore"] = formatCLIParamValue(flagTakenBefore)
			}
			if flagTrashedAfter != "" {
				params["trashedAfter"] = formatCLIParamValue(flagTrashedAfter)
			}
			if flagTrashedBefore != "" {
				params["trashedBefore"] = formatCLIParamValue(flagTrashedBefore)
			}
			if flagType != "" {
				params["type"] = formatCLIParamValue(flagType)
			}
			if flagUpdatedAfter != "" {
				params["updatedAfter"] = formatCLIParamValue(flagUpdatedAfter)
			}
			if flagUpdatedBefore != "" {
				params["updatedBefore"] = formatCLIParamValue(flagUpdatedBefore)
			}
			if flagVisibility != "" {
				params["visibility"] = formatCLIParamValue(flagVisibility)
			}
			if flagWithDeleted != false {
				params["withDeleted"] = formatCLIParamValue(flagWithDeleted)
			}
			if flagWithExif != false {
				params["withExif"] = formatCLIParamValue(flagWithExif)
			}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "immich-search", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "immich-search", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "immich-search", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "immich-search", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "immich-search", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "immich-search",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "immich-search", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "immich-search", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagAlbumIds, "album-ids", "", "Filter by album IDs")
	cmd.Flags().StringVar(&flagCity, "city", "", "Filter by city name")
	cmd.Flags().StringVar(&flagCountry, "country", "", "Filter by country name")
	cmd.Flags().StringVar(&flagCreatedAfter, "created-after", "", "Filter by creation date (after)")
	cmd.Flags().StringVar(&flagCreatedBefore, "created-before", "", "Filter by creation date (before)")
	cmd.Flags().BoolVar(&flagIsEncoded, "is-encoded", false, "Filter by encoded status")
	cmd.Flags().BoolVar(&flagIsFavorite, "is-favorite", false, "Filter by favorite status")
	cmd.Flags().BoolVar(&flagIsMotion, "is-motion", false, "Filter by motion photo status")
	cmd.Flags().BoolVar(&flagIsNotInAlbum, "is-not-in-album", false, "Filter assets not in any album")
	cmd.Flags().BoolVar(&flagIsOffline, "is-offline", false, "Filter by offline status")
	cmd.Flags().StringVar(&flagLensModel, "lens-model", "", "Filter by lens model")
	cmd.Flags().StringVar(&flagLibraryId, "library-id", "", "Library ID to filter by")
	cmd.Flags().StringVar(&flagMake, "make", "", "Filter by camera make")
	cmd.Flags().IntVar(&flagMinFileSize, "min-file-size", 0, "Minimum file size in bytes")
	cmd.Flags().StringVar(&flagModel, "model", "", "Filter by camera model")
	cmd.Flags().StringVar(&flagOcr, "ocr", "", "Filter by OCR text content")
	cmd.Flags().StringVar(&flagPersonIds, "person-ids", "", "Filter by person IDs")
	cmd.Flags().IntVar(&flagRating, "rating", 0, "Filter by rating [1-5], or null for unrated")
	cmd.Flags().IntVar(&flagSize, "size", 0, "Number of results to return")
	cmd.Flags().StringVar(&flagState, "state", "", "Filter by state/province name")
	cmd.Flags().StringVar(&flagTagIds, "tag-ids", "", "Filter by tag IDs")
	cmd.Flags().StringVar(&flagTakenAfter, "taken-after", "", "Filter by taken date (after)")
	cmd.Flags().StringVar(&flagTakenBefore, "taken-before", "", "Filter by taken date (before)")
	cmd.Flags().StringVar(&flagTrashedAfter, "trashed-after", "", "Filter by trash date (after)")
	cmd.Flags().StringVar(&flagTrashedBefore, "trashed-before", "", "Filter by trash date (before)")
	cmd.Flags().StringVar(&flagType, "type", "", "Type (one of: IMAGE, VIDEO, AUDIO, OTHER)")
	cmd.Flags().StringVar(&flagUpdatedAfter, "updated-after", "", "Filter by update date (after)")
	cmd.Flags().StringVar(&flagUpdatedBefore, "updated-before", "", "Filter by update date (before)")
	cmd.Flags().StringVar(&flagVisibility, "visibility", "", "Visibility (one of: archive, timeline, hidden, locked)")
	cmd.Flags().BoolVar(&flagWithDeleted, "with-deleted", false, "Include deleted assets")
	cmd.Flags().BoolVar(&flagWithExif, "with-exif", false, "Include EXIF data in response")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
