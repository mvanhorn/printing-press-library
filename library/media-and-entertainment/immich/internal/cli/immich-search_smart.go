// Copyright 2026 avanderheyde and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/immich/internal/cliutil"
	"github.com/spf13/cobra"
)

func newImmichSearchSmartCmd(flags *rootFlags) *cobra.Command {
	var bodyAlbumIds string
	var bodyCity string
	var bodyCountry string
	var bodyCreatedAfter string
	var bodyCreatedBefore string
	var bodyIsEncoded bool
	var bodyIsFavorite bool
	var bodyIsMotion bool
	var bodyIsNotInAlbum bool
	var bodyIsOffline bool
	var bodyLanguage string
	var bodyLensModel string
	var bodyLibraryId string
	var bodyMake string
	var bodyModel string
	var bodyOcr string
	var bodyPage string
	var bodyPersonIds string
	var bodyQuery string
	var bodyQueryAssetId string
	var bodyRating int
	var bodySize int
	var bodyState string
	var bodyTagIds string
	var bodyTakenAfter string
	var bodyTakenBefore string
	var bodyTrashedAfter string
	var bodyTrashedBefore string
	var bodyType string
	var bodyUpdatedAfter string
	var bodyUpdatedBefore string
	var bodyVisibility string
	var bodyWithDeleted bool
	var bodyWithExif bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "smart",
		Short:       "Perform a smart search for assets by using machine learning vectors to determine relevance.",
		Example:     "  immich-pp-cli immich-search smart",
		Annotations: map[string]string{"pp:endpoint": "immich-search.smart", "pp:method": "POST", "pp:path": "/search/smart"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/search/smart"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("album-ids") {
					parsedAlbumIds, parseErr := cliutil.ParseStringList(bodyAlbumIds)
					if parseErr != nil {
						return fmt.Errorf("parsing --album-ids list: %w", parseErr)
					}
					bodyMap["albumIds"] = parsedAlbumIds
				}
				if bodyCity != "" {
					bodyMap["city"] = bodyCity
				}
				if bodyCountry != "" {
					bodyMap["country"] = bodyCountry
				}
				if bodyCreatedAfter != "" {
					bodyMap["createdAfter"] = bodyCreatedAfter
				}
				if bodyCreatedBefore != "" {
					bodyMap["createdBefore"] = bodyCreatedBefore
				}
				if cmd.Flags().Changed("is-encoded") {
					bodyMap["isEncoded"] = bodyIsEncoded
				}
				if cmd.Flags().Changed("is-favorite") {
					bodyMap["isFavorite"] = bodyIsFavorite
				}
				if cmd.Flags().Changed("is-motion") {
					bodyMap["isMotion"] = bodyIsMotion
				}
				if cmd.Flags().Changed("is-not-in-album") {
					bodyMap["isNotInAlbum"] = bodyIsNotInAlbum
				}
				if cmd.Flags().Changed("is-offline") {
					bodyMap["isOffline"] = bodyIsOffline
				}
				if bodyLanguage != "" {
					bodyMap["language"] = bodyLanguage
				}
				if bodyLensModel != "" {
					bodyMap["lensModel"] = bodyLensModel
				}
				if bodyLibraryId != "" {
					bodyMap["libraryId"] = bodyLibraryId
				}
				if bodyMake != "" {
					bodyMap["make"] = bodyMake
				}
				if bodyModel != "" {
					bodyMap["model"] = bodyModel
				}
				if bodyOcr != "" {
					bodyMap["ocr"] = bodyOcr
				}
				if bodyPage != "" {
					bodyMap["page"] = bodyPage
				}
				if cmd.Flags().Changed("person-ids") {
					parsedPersonIds, parseErr := cliutil.ParseStringList(bodyPersonIds)
					if parseErr != nil {
						return fmt.Errorf("parsing --person-ids list: %w", parseErr)
					}
					bodyMap["personIds"] = parsedPersonIds
				}
				if bodyQuery != "" {
					bodyMap["query"] = bodyQuery
				}
				if bodyQueryAssetId != "" {
					bodyMap["queryAssetId"] = bodyQueryAssetId
				}
				if bodyRating != 0 {
					bodyMap["rating"] = bodyRating
				}
				if bodySize != 0 {
					bodyMap["size"] = bodySize
				}
				if bodyState != "" {
					bodyMap["state"] = bodyState
				}
				if cmd.Flags().Changed("tag-ids") {
					parsedTagIds, parseErr := cliutil.ParseStringList(bodyTagIds)
					if parseErr != nil {
						return fmt.Errorf("parsing --tag-ids list: %w", parseErr)
					}
					bodyMap["tagIds"] = parsedTagIds
				}
				if bodyTakenAfter != "" {
					bodyMap["takenAfter"] = bodyTakenAfter
				}
				if bodyTakenBefore != "" {
					bodyMap["takenBefore"] = bodyTakenBefore
				}
				if bodyTrashedAfter != "" {
					bodyMap["trashedAfter"] = bodyTrashedAfter
				}
				if bodyTrashedBefore != "" {
					bodyMap["trashedBefore"] = bodyTrashedBefore
				}
				if bodyType != "" {
					bodyMap["type"] = bodyType
				}
				if bodyUpdatedAfter != "" {
					bodyMap["updatedAfter"] = bodyUpdatedAfter
				}
				if bodyUpdatedBefore != "" {
					bodyMap["updatedBefore"] = bodyUpdatedBefore
				}
				if bodyVisibility != "" {
					bodyMap["visibility"] = bodyVisibility
				}
				if cmd.Flags().Changed("with-deleted") {
					bodyMap["withDeleted"] = bodyWithDeleted
				}
				if cmd.Flags().Changed("with-exif") {
					bodyMap["withExif"] = bodyWithExif
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "immich-search", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "immich-search", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "immich-search", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "immich-search", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "immich-search", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "immich-search",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "immich-search", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "immich-search", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAlbumIds, "album-ids", "", "Filter by album IDs")
	cmd.Flags().StringVar(&bodyCity, "city", "", "Filter by city name")
	cmd.Flags().StringVar(&bodyCountry, "country", "", "Filter by country name")
	cmd.Flags().StringVar(&bodyCreatedAfter, "created-after", "", "Filter by creation date (after)")
	cmd.Flags().StringVar(&bodyCreatedBefore, "created-before", "", "Filter by creation date (before)")
	cmd.Flags().BoolVar(&bodyIsEncoded, "is-encoded", false, "Filter by encoded status")
	cmd.Flags().BoolVar(&bodyIsFavorite, "is-favorite", false, "Filter by favorite status")
	cmd.Flags().BoolVar(&bodyIsMotion, "is-motion", false, "Filter by motion photo status")
	cmd.Flags().BoolVar(&bodyIsNotInAlbum, "is-not-in-album", false, "Filter assets not in any album")
	cmd.Flags().BoolVar(&bodyIsOffline, "is-offline", false, "Filter by offline status")
	cmd.Flags().StringVar(&bodyLanguage, "language", "", "Search language code")
	cmd.Flags().StringVar(&bodyLensModel, "lens-model", "", "Filter by lens model")
	cmd.Flags().StringVar(&bodyLibraryId, "library-id", "", "Library ID to filter by")
	cmd.Flags().StringVar(&bodyMake, "make", "", "Filter by camera make")
	cmd.Flags().StringVar(&bodyModel, "model", "", "Filter by camera model")
	cmd.Flags().StringVar(&bodyOcr, "ocr", "", "Filter by OCR text content")
	cmd.Flags().StringVar(&bodyPage, "page", "", "Page number")
	cmd.Flags().StringVar(&bodyPersonIds, "person-ids", "", "Filter by person IDs")
	cmd.Flags().StringVar(&bodyQuery, "query", "", "Natural language search query")
	cmd.Flags().StringVar(&bodyQueryAssetId, "query-asset-id", "", "Asset ID to use as search reference")
	cmd.Flags().IntVar(&bodyRating, "rating", 0, "Filter by rating [1-5], or null for unrated")
	cmd.Flags().IntVar(&bodySize, "size", 0, "Number of results to return")
	cmd.Flags().StringVar(&bodyState, "state", "", "Filter by state/province name")
	cmd.Flags().StringVar(&bodyTagIds, "tag-ids", "", "Filter by tag IDs")
	cmd.Flags().StringVar(&bodyTakenAfter, "taken-after", "", "Filter by taken date (after)")
	cmd.Flags().StringVar(&bodyTakenBefore, "taken-before", "", "Filter by taken date (before)")
	cmd.Flags().StringVar(&bodyTrashedAfter, "trashed-after", "", "Filter by trash date (after)")
	cmd.Flags().StringVar(&bodyTrashedBefore, "trashed-before", "", "Filter by trash date (before)")
	cmd.Flags().StringVar(&bodyType, "type", "", "Asset type")
	cmd.Flags().StringVar(&bodyUpdatedAfter, "updated-after", "", "Filter by update date (after)")
	cmd.Flags().StringVar(&bodyUpdatedBefore, "updated-before", "", "Filter by update date (before)")
	cmd.Flags().StringVar(&bodyVisibility, "visibility", "", "Asset visibility")
	cmd.Flags().BoolVar(&bodyWithDeleted, "with-deleted", false, "Include deleted assets")
	cmd.Flags().BoolVar(&bodyWithExif, "with-exif", false, "Include EXIF data in response")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
