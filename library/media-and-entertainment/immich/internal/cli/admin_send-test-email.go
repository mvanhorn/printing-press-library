// Copyright 2026 avanderheyde and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"
	"strconv"

	"github.com/spf13/cobra"
)

func newAdminSendTestEmailCmd(flags *rootFlags) *cobra.Command {
	var bodyEnabled string
	var bodyFrom string
	var bodyReplyTo string
	var bodyTransportHost string
	var bodyTransportIgnoreCert string
	var bodyTransportPassword string
	var bodyTransportPort int
	var bodyTransportSecure string
	var bodyTransportUsername string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "send-test-email",
		Short: "Send a test email using the provided SMTP configuration.",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  immich-pp-cli admin send-test-email --from example-value",
		Annotations: map[string]string{"pp:endpoint": "admin.send-test-email", "pp:method": "POST", "pp:path": "/admin/notifications/test-email"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("enabled") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "enabled")
				}
				if !cmd.Flags().Changed("from") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "from")
				}
				if !cmd.Flags().Changed("reply-to") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "reply-to")
				}
				if !cmd.Flags().Changed("transport-host") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "transport-host")
				}
				if !cmd.Flags().Changed("transport-ignore-cert") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "transport-ignore-cert")
				}
				if !cmd.Flags().Changed("transport-password") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "transport-password")
				}
				if !cmd.Flags().Changed("transport-port") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "transport-port")
				}
				if !cmd.Flags().Changed("transport-secure") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "transport-secure")
				}
				if !cmd.Flags().Changed("transport-username") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "transport-username")
				}
			}
			path := "/admin/notifications/test-email"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if bodyEnabled != "" {
					parsedEnabled, err := strconv.ParseBool(bodyEnabled)
					if err != nil {
						return fmt.Errorf("parsing --enabled as bool: %w", err)
					}
					bodyMap["enabled"] = parsedEnabled
				}
				if bodyFrom != "" {
					bodyMap["from"] = bodyFrom
				}
				if bodyReplyTo != "" {
					bodyMap["replyTo"] = bodyReplyTo
				}
				{
					nestedTransport := map[string]any{}
					if bodyTransportHost != "" {
						nestedTransport["host"] = bodyTransportHost
					}
					if bodyTransportIgnoreCert != "" {
						parsedTransportIgnoreCert, err := strconv.ParseBool(bodyTransportIgnoreCert)
						if err != nil {
							return fmt.Errorf("parsing --transport-ignore-cert as bool: %w", err)
						}
						nestedTransport["ignoreCert"] = parsedTransportIgnoreCert
					}
					if bodyTransportPassword != "" {
						nestedTransport["password"] = bodyTransportPassword
					}
					if bodyTransportPort != 0 {
						nestedTransport["port"] = bodyTransportPort
					}
					if bodyTransportSecure != "" {
						parsedTransportSecure, err := strconv.ParseBool(bodyTransportSecure)
						if err != nil {
							return fmt.Errorf("parsing --transport-secure as bool: %w", err)
						}
						nestedTransport["secure"] = parsedTransportSecure
					}
					if bodyTransportUsername != "" {
						nestedTransport["username"] = bodyTransportUsername
					}
					if len(nestedTransport) > 0 {
						bodyMap["transport"] = nestedTransport
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "admin", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "admin", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "admin",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyEnabled, "enabled", "", "Whether SMTP email notifications are enabled")
	cmd.Flags().StringVar(&bodyFrom, "from", "", "Email address to send from")
	cmd.Flags().StringVar(&bodyReplyTo, "reply-to", "", "Email address for replies")
	cmd.Flags().StringVar(&bodyTransportHost, "transport-host", "", "SMTP server hostname")
	cmd.Flags().StringVar(&bodyTransportIgnoreCert, "transport-ignore-cert", "", "Whether to ignore SSL certificate errors")
	cmd.Flags().StringVar(&bodyTransportPassword, "transport-password", "", "SMTP password")
	cmd.Flags().IntVar(&bodyTransportPort, "transport-port", 0, "SMTP server port")
	cmd.Flags().StringVar(&bodyTransportSecure, "transport-secure", "", "Whether to use secure connection (TLS/SSL)")
	cmd.Flags().StringVar(&bodyTransportUsername, "transport-username", "", "SMTP username")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
