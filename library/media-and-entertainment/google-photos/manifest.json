{
  "manifest_version": "0.3",
  "name": "google-photos-pp-mcp",
  "display_name": "Google Photos",
  "version": "3.9.1",
  "description": "Manage Google Photos app-created albums and media, upload files, and work with Picker sessions from the terminal.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/google-photos-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/google-photos-pp-mcp",
      "args": [],
      "env": {
        "GOOGLE_PHOTOS_TOKEN": "${user_config.google_photos_token}"
      }
    }
  },
  "user_config": {
    "google_photos_token": {
      "type": "string",
      "title": "GOOGLE_PHOTOS_TOKEN",
      "description": "Sets GOOGLE_PHOTOS_TOKEN for the Google Photos MCP server. Get a credential from https://console.cloud.google.com/apis/credentials.",
      "sensitive": true,
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
