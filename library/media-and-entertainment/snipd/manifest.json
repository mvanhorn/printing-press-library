{
  "manifest_version": "0.3",
  "name": "snipd-pp-mcp",
  "display_name": "Snipd",
  "version": "0.1.0",
  "description": "Turn your Snipd snips into a local, ranked, full-text-searchable corpus — search a concept across every note, quote, and transcript, pull the exact quote, and synthesize across shows from the command line, plus an MCP for agents.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/snipd-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/snipd-pp-mcp",
      "args": [],
      "env": {
        "SNIPD_TOKEN": "${user_config.snipd_token}"
      }
    }
  },
  "user_config": {
    "snipd_token": {
      "type": "string",
      "title": "SNIPD_TOKEN",
      "description": "Sets SNIPD_TOKEN for the Snipd MCP server.",
      "sensitive": true,
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
