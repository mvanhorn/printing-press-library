// Copyright 2026 kamibot-upgrade-automation. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/yt-studio/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/yt-studio/internal/client"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/yt-studio/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/yt-studio/internal/config"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/yt-studio/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/yt-studio/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("captions_list",
			mcplib.WithDescription("List caption tracks for a video. Required: videoId. Optional: part. Returns array of CaptionTrack."),
			mcplib.WithString("part", mcplib.Description("Always 'snippet'")),
			mcplib.WithString("videoId", mcplib.Required(), mcplib.Description("Video ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/youtube/v3/captions", []mcpParamBinding{{PublicName: "part", WireName: "part", Location: "query"}, {PublicName: "videoId", WireName: "videoId", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("categories_list",
			mcplib.WithDescription("List video categories for a region. Optional: part, regionCode. Returns array of VideoCategory."),
			mcplib.WithString("part", mcplib.Description("Always 'snippet'")),
			mcplib.WithString("regionCode", mcplib.Description("ISO 3166-1 alpha-2 region code (e.g. US, GB)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/youtube/v3/videoCategories", []mcpParamBinding{{PublicName: "part", WireName: "part", Location: "query"}, {PublicName: "regionCode", WireName: "regionCode", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("channels_get",
			mcplib.WithDescription("Get the authenticated user's channel (--self) or a single channel by id. Optional: self, id. Returns the Channel."),
			mcplib.WithString("self", mcplib.Description("Return the authenticated user's channel (sets mine=true,part=snippet,statistics,contentDetails)")),
			mcplib.WithString("id", mcplib.Description("Channel ID (alternative to --self)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/youtube/v3/channels", []mcpParamBinding{{PublicName: "self", WireName: "self", Location: "query"}, {PublicName: "id", WireName: "id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("channels_list",
			mcplib.WithDescription("List channel resources by id, mine=true, or forHandle. Optional: part, id, mine (plus 3 more). Returns array of Channel."),
			mcplib.WithString("part", mcplib.Description("Comma-separated part fields (snippet, statistics, contentDetails, brandingSettings)")),
			mcplib.WithString("id", mcplib.Description("Comma-separated channel IDs")),
			mcplib.WithString("mine", mcplib.Description("Return the authenticated user's channel")),
			mcplib.WithString("forHandle", mcplib.Description("Channel handle (e.g. @KamiVar)")),
			mcplib.WithString("maxResults", mcplib.Description("Max channels per page (1-50)")),
			mcplib.WithString("pageToken", mcplib.Description("Pagination token from prior response")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/youtube/v3/channels", []mcpParamBinding{{PublicName: "part", WireName: "part", Location: "query"}, {PublicName: "id", WireName: "id", Location: "query"}, {PublicName: "mine", WireName: "mine", Location: "query"}, {PublicName: "forHandle", WireName: "forHandle", Location: "query"}, {PublicName: "maxResults", WireName: "maxResults", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("comments_list",
			mcplib.WithDescription("List top-level comment threads for a video. Required: videoId. Optional: part, order, maxResults (plus 1 more). Returns array of CommentThread."),
			mcplib.WithString("part", mcplib.Description("Comma-separated part fields (snippet, replies)")),
			mcplib.WithString("videoId", mcplib.Required(), mcplib.Description("Video ID")),
			mcplib.WithString("order", mcplib.Description("time or relevance")),
			mcplib.WithString("maxResults", mcplib.Description("Max threads per page (1-100)")),
			mcplib.WithString("pageToken", mcplib.Description("Pagination token")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/youtube/v3/commentThreads", []mcpParamBinding{{PublicName: "part", WireName: "part", Location: "query"}, {PublicName: "videoId", WireName: "videoId", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "maxResults", WireName: "maxResults", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("discover_list",
			mcplib.WithDescription("Search for videos, channels, or playlists via the YouTube Data API search.list endpoint. WARNING: 100 quota units per call. Optional: part, q, channelId (plus 6 more). Returns array of SearchResult."),
			mcplib.WithString("part", mcplib.Description("Always 'snippet' for search")),
			mcplib.WithString("q", mcplib.Description("Query string")),
			mcplib.WithString("channelId", mcplib.Description("Restrict to a single channel")),
			mcplib.WithString("type", mcplib.Description("video, channel, or playlist")),
			mcplib.WithString("order", mcplib.Description("date, rating, relevance, title, videoCount, viewCount")),
			mcplib.WithString("publishedAfter", mcplib.Description("RFC3339 timestamp (e.g. 2026-05-01T00:00:00Z)")),
			mcplib.WithString("publishedBefore", mcplib.Description("RFC3339 timestamp")),
			mcplib.WithString("maxResults", mcplib.Description("Max items per page (1-50)")),
			mcplib.WithString("pageToken", mcplib.Description("Pagination token")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/youtube/v3/search", []mcpParamBinding{{PublicName: "part", WireName: "part", Location: "query"}, {PublicName: "q", WireName: "q", Location: "query"}, {PublicName: "channelId", WireName: "channelId", Location: "query"}, {PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "publishedAfter", WireName: "publishedAfter", Location: "query"}, {PublicName: "publishedBefore", WireName: "publishedBefore", Location: "query"}, {PublicName: "maxResults", WireName: "maxResults", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("playlist_items_list",
			mcplib.WithDescription("List items in a playlist. Required: playlistId. Optional: part, maxResults, pageToken. Returns array of PlaylistItem."),
			mcplib.WithString("part", mcplib.Description("Comma-separated part fields (snippet, contentDetails)")),
			mcplib.WithString("playlistId", mcplib.Required(), mcplib.Description("Playlist ID (e.g. UU... for an uploads playlist)")),
			mcplib.WithString("maxResults", mcplib.Description("Max items per page (1-50)")),
			mcplib.WithString("pageToken", mcplib.Description("Pagination token")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/youtube/v3/playlistItems", []mcpParamBinding{{PublicName: "part", WireName: "part", Location: "query"}, {PublicName: "playlistId", WireName: "playlistId", Location: "query"}, {PublicName: "maxResults", WireName: "maxResults", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("playlists_get",
			mcplib.WithDescription("Get a single playlist by ID. Required: playlist_id. Returns the Playlist."),
			mcplib.WithString("playlist_id", mcplib.Required(), mcplib.Description("Playlist ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/youtube/v3/playlists", []mcpParamBinding{{PublicName: "playlist_id", WireName: "playlist_id", Location: "query"}}, []string{"playlist_id"}),
	)
	s.AddTool(
		mcplib.NewTool("playlists_list",
			mcplib.WithDescription("List playlists by channelId or id. Optional: part, channelId, id (plus 3 more). Returns array of Playlist."),
			mcplib.WithString("part", mcplib.Description("Comma-separated part fields (snippet, contentDetails)")),
			mcplib.WithString("channelId", mcplib.Description("Channel ID to fetch playlists for")),
			mcplib.WithString("id", mcplib.Description("Comma-separated playlist IDs")),
			mcplib.WithString("mine", mcplib.Description("Return the authenticated user's playlists")),
			mcplib.WithString("maxResults", mcplib.Description("Max items per page (1-50)")),
			mcplib.WithString("pageToken", mcplib.Description("Pagination token")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/youtube/v3/playlists", []mcpParamBinding{{PublicName: "part", WireName: "part", Location: "query"}, {PublicName: "channelId", WireName: "channelId", Location: "query"}, {PublicName: "id", WireName: "id", Location: "query"}, {PublicName: "mine", WireName: "mine", Location: "query"}, {PublicName: "maxResults", WireName: "maxResults", Location: "query"}, {PublicName: "pageToken", WireName: "pageToken", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("videos_get",
			mcplib.WithDescription("Get a single video by ID. Required: video_id. Optional: part. Returns the Video."),
			mcplib.WithString("video_id", mcplib.Required(), mcplib.Description("Video ID")),
			mcplib.WithString("part", mcplib.Description("Comma-separated part fields")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/youtube/v3/videos", []mcpParamBinding{{PublicName: "video_id", WireName: "video_id", Location: "query"}, {PublicName: "part", WireName: "part", Location: "query"}}, []string{"video_id"}),
	)
	s.AddTool(
		mcplib.NewTool("videos_list",
			mcplib.WithDescription("List video metadata by id. Required: id. Optional: part, maxResults. Returns array of Video."),
			mcplib.WithString("part", mcplib.Description("Comma-separated part fields (snippet, statistics, contentDetails)")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Comma-separated video IDs (up to 50 per request)")),
			mcplib.WithString("maxResults", mcplib.Description("Max items per page (1-50)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/youtube/v3/videos", []mcpParamBinding{{PublicName: "part", WireName: "part", Location: "query"}, {PublicName: "id", WireName: "id", Location: "query"}, {PublicName: "maxResults", WireName: "maxResults", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.PostWithParams(path, params, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.PutWithParams(path, params, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.PatchWithParams(path, params, body)
		case "DELETE":
			data, _, err = c.DeleteWithParams(path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export YT_STUDIO_OAUTH_CLIENT_ID=<your-key>" +
					"\n      Get a key at: https://console.cloud.google.com/apis/credentials" +
					"\n      Create an OAuth 2.0 Desktop Client in Google Cloud Console, then run `yt-studio-pp-cli auth login`. Enable both the YouTube Data API v3 and YouTube Analytics API on the project first." +
					"\n      Run 'yt-studio-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your token may have expired. Re-run 'yt-studio-pp-cli auth login' to re-authenticate" +
					"\n      Set it with: export YT_STUDIO_OAUTH_CLIENT_ID=<your-key>" +
					"\n      Get a key at: https://console.cloud.google.com/apis/credentials" +
					"\n      Create an OAuth 2.0 Desktop Client in Google Cloud Console, then run `yt-studio-pp-cli auth login`. Enable both the YouTube Data API v3 and YouTube Analytics API on the project first." +
					"\n      Run 'yt-studio-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your token may lack required scopes. Re-run 'yt-studio-pp-cli auth login' to re-authorize" +
					"\n      Set it with: export YT_STUDIO_OAUTH_CLIENT_ID=<your-key>" +
					"\n      Get a key at: https://console.cloud.google.com/apis/credentials" +
					"\n      Create an OAuth 2.0 Desktop Client in Google Cloud Console, then run `yt-studio-pp-cli auth login`. Enable both the YouTube Data API v3 and YouTube Analytics API on the project first." +
					"\n      Run 'yt-studio-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "yt-studio-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "yt-studio-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "yt-studio",
		"description": "Every YouTube creator metric that matters, offline-queryable, with framework-audit binding to your scripts. The CLI...",
		"archetype":   "communication",
		"tool_count":  11,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion yt-studio-pp-cli binary.",
		"auth": map[string]any{
			"type": "oauth2",
			"env_vars": []map[string]any{
				{
					"name":        "YT_STUDIO_OAUTH_CLIENT_ID",
					"kind":        "auth_flow_input",
					"required":    true,
					"sensitive":   false,
					"description": "OAuth 2.0 Client ID from your Google Cloud Console Desktop Client",
				},
				{
					"name":        "YT_STUDIO_OAUTH_CLIENT_SECRET",
					"kind":        "auth_flow_input",
					"required":    true,
					"sensitive":   true,
					"description": "Set during initial auth setup.",
				},
			},
			"key_url":      "https://console.cloud.google.com/apis/credentials",
			"instructions": "Create an OAuth 2.0 Desktop Client in Google Cloud Console, then run `yt-studio-pp-cli auth login`. Enable both the YouTube Data API v3 and YouTube Analytics API on the project first.",
		},
		"resources": []map[string]any{
			{
				"name":        "captions",
				"description": "Caption tracks (read-only; transcript fetch deferred)",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "categories",
				"description": "YouTube video category metadata",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "channels",
				"description": "YouTube channels (own channel + competitor watchlist)",
				"endpoints":   []string{"info", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "comments",
				"description": "Video comment threads (read-only)",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "discover",
				"description": "YouTube search.list API (100 quota units per request — use sparingly; named 'discover' because 'search' is a...",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "playlistItems",
				"description": "Items within a playlist",
				"endpoints":   []string{"list"},
			},
			{
				"name":        "playlists",
				"description": "YouTube playlists",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "videos",
				"description": "YouTube videos (own + watchlist)",
				"endpoints":   []string{"get", "list"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Framework audit", "command": "framework-audit", "description": "Cross-check whether a published video hits the Signal / Belief-Shift / Action-CTA structure by joining retention...", "rationale": "Requires script binding via content-registry.md and a per-bucket retention store. No other YouTube tool has this join.", "via": "mcp-command-mirror"},
			{"name": "Retention curve", "command": "retention", "description": "Return the 100-bucket retention curve for a video as JSON or an ASCII sparkline, with the three sharpest drops...", "rationale": "Public Analytics API gives the curve, but only this CLI annotates drops and caches them per recorded_at for...", "via": "mcp-command-mirror"},
			{"name": "Retention cohort", "command": "retention-cohort", "description": "Average retention across videos whose title matches a regex (build-guide cohort vs rework cohort vs tier-list cohort).", "rationale": "Requires FTS on titles plus per-video retention rows in the local store.", "via": "mcp-command-mirror"},
			{"name": "CTR decay", "command": "ctr-decay", "description": "Compare first-72h CTR to day-30 CTR for a video; flags fast-decaying thumbnails.", "rationale": "Requires daily impression snapshots; cannot be computed from one API call.", "via": "mcp-command-mirror"},
			{"name": "Watchlist benchmark", "command": "vs-watchlist", "description": "Compare own channel against the watchlist on CTR, retention, or upload cadence — normalized scale across all channels.", "rationale": "Multi-channel normalized schema; competitor data via public Data API joined with own analytics.", "via": "mcp-command-mirror"},
			{"name": "Title pattern mining", "command": "title-patterns", "description": "Token-level analysis of words/phrases that correlate with above-median CTR (winners) or below-median (losers).", "rationale": "Pure local SQL over per-video CTR + title tokens; no API call.", "via": "mcp-command-mirror"},
			{"name": "Idea gap", "command": "idea-gap", "description": "Topics the watchlist covered in the last N days that the own channel has not.", "rationale": "Joins watchlist search snapshots against the own-channel video corpus via FTS.", "via": "mcp-command-mirror"},
			{"name": "Watchlist suggest", "command": "watchlist suggest", "description": "Auto-discover competitor channels for the watchlist via Search API; ranks by relevance and recent upload volume.", "rationale": "Combines Data API search + channels.list and applies a relevance heuristic agnostic to incumbents.", "via": "mcp-command-mirror"},
			{"name": "Hybrid quota-aware sync", "command": "sync", "description": "Incremental sync of own channel + watchlist with daily-quota budgeting; caps concurrency and prefers cache.", "rationale": "10K Data API quota/day means search.list calls cost 100 units each. The CLI tracks cumulative spend and skips live...", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Framework audit", "insight": "Requires script binding via content-registry.md and a per-bucket retention store. No other YouTube tool has this join."},
			{"topic": "Retention curve", "insight": "Public Analytics API gives the curve, but only this CLI annotates drops and caches them per recorded_at for compare-over-time."},
			{"topic": "Retention cohort", "insight": "Requires FTS on titles plus per-video retention rows in the local store."},
			{"topic": "CTR decay", "insight": "Requires daily impression snapshots; cannot be computed from one API call."},
			{"topic": "Watchlist benchmark", "insight": "Multi-channel normalized schema; competitor data via public Data API joined with own analytics."},
			{"topic": "Title pattern mining", "insight": "Pure local SQL over per-video CTR + title tokens; no API call."},
			{"topic": "Idea gap", "insight": "Joins watchlist search snapshots against the own-channel video corpus via FTS."},
			{"topic": "Watchlist suggest", "insight": "Combines Data API search + channels.list and applies a relevance heuristic agnostic to incumbents."},
			{"topic": "Hybrid quota-aware sync", "insight": "10K Data API quota/day means search.list calls cost 100 units each. The CLI tracks cumulative spend and skips live calls past budget."},
			{"topic": "Message search", "insight": "Use the search tool on synced data rather than paginating through message history. Message APIs often have aggressive rate limits."},
			{"topic": "Channel health", "insight": "When analyzing channel activity, use the channel-health command or sql aggregation on synced messages. Don't iterate individual messages via API."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
