// Copyright 2026 prisco-faiella. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/diretta/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/diretta/internal/client"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/diretta/internal/config"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/diretta/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/diretta/internal/store"
	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("match_detail",
			mcplib.WithDescription("Match general info, score, and basic data. FlashScore delimiter format. Required: matchId."),
			mcplib.WithString("matchId", mcplib.Required(), mcplib.Description("Match ID (e.g. vVn0EQM5)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/x/feed/dc_1_{matchId}", []mcpParamBinding{{PublicName: "matchId", WireName: "matchId", Location: "path"}}, []string{"matchId"}),
	)
	s.AddTool(
		mcplib.NewTool("match_events",
			mcplib.WithDescription("Match events: goals, cards, substitutions. FlashScore delimiter format. Required: matchId."),
			mcplib.WithString("matchId", mcplib.Required(), mcplib.Description("Match ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/x/feed/df_sui_1_{matchId}", []mcpParamBinding{{PublicName: "matchId", WireName: "matchId", Location: "path"}}, []string{"matchId"}),
	)
	s.AddTool(
		mcplib.NewTool("match_h2h",
			mcplib.WithDescription("Head-to-head history between the two teams. FlashScore delimiter format. Required: matchId."),
			mcplib.WithString("matchId", mcplib.Required(), mcplib.Description("Match ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/x/feed/df_hh_1_{matchId}", []mcpParamBinding{{PublicName: "matchId", WireName: "matchId", Location: "path"}}, []string{"matchId"}),
	)
	s.AddTool(
		mcplib.NewTool("match_lineups",
			mcplib.WithDescription("Team lineups and formations. FlashScore delimiter format. Required: matchId."),
			mcplib.WithString("matchId", mcplib.Required(), mcplib.Description("Match ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/x/feed/df_li_1_{matchId}", []mcpParamBinding{{PublicName: "matchId", WireName: "matchId", Location: "path"}}, []string{"matchId"}),
	)
	s.AddTool(
		mcplib.NewTool("match_stats",
			mcplib.WithDescription("Match statistics: possession, shots, corners, xG. FlashScore delimiter format. Required: matchId."),
			mcplib.WithString("matchId", mcplib.Required(), mcplib.Description("Match ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/x/feed/df_st_1_{matchId}", []mcpParamBinding{{PublicName: "matchId", WireName: "matchId", Location: "path"}}, []string{"matchId"}),
	)
	s.AddTool(
		mcplib.NewTool("matches_live",
			mcplib.WithDescription("Fetch Italian football matches currently in progress. No parameters required. Returns only live matches from today's feed, filtered by active status, in FlashScore delimiter format with current scores and match IDs. Prefer this over matches_today when you need only in-play matches to minimise noise."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/x/feed/f_1_0_3_it_1", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("matches_today",
			mcplib.WithDescription("Fetch all Italian football matches for today. No parameters required. Returns the full day's schedule and live results in FlashScore delimiter format including team names, match IDs, kick-off times, and current scores. Use this as the primary entry point for today's fixtures; use matches_live to filter to only in-progress matches."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/x/feed/f_1_0_3_it_1", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("matches_tomorrow",
			mcplib.WithDescription("Fetch Italian football fixtures scheduled for tomorrow. No parameters required. Returns a list of matches in FlashScore delimiter format including team names, kick-off times, and match IDs. Use this to retrieve the next day's schedule for planning pre-match analysis or odds queries."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/x/feed/f_1_1_3_it_1", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("matches_yesterday",
			mcplib.WithDescription("Fetch Italian football results from yesterday. No parameters required. Returns completed matches in FlashScore delimiter format with final scores, team names, and match IDs. Use this when you need recent results for form analysis or H2H lookups without running a full sync."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/x/feed/f_1_-1_3_it_1", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("odds_match_odds",
			mcplib.WithDescription("Betting odds for a match: 1X2, Under/Over, Asian handicap. Required: eventId. Optional: projectId (default: 2), geoIpCode (default: IT)."),
			mcplib.WithString("eventId", mcplib.Required(), mcplib.Description("Match ID")),
			mcplib.WithString("projectId", mcplib.Description("Project ID (2=Global)")),
			mcplib.WithString("geoIpCode", mcplib.Description("Geographic region code")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "https://2.ds.lsapp.eu/pq_graphql", []mcpParamBinding{{PublicName: "eventId", WireName: "eventId", Location: "query"}, {PublicName: "projectId", WireName: "projectId", Location: "body"}, {PublicName: "geoIpCode", WireName: "geoIpCode", Location: "body"}}, []string{"eventId"}),
	)
	s.AddTool(
		mcplib.NewTool("sports_all_today",
			mcplib.WithDescription("All sports today: calcio, tennis, basket, hockey, etc. FlashScore delimiter format."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/x/feed/f_2_0_3_it_1", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("standings_table",
			mcplib.WithDescription("League table for a tournament. FlashScore delimiter format. Required: tournamentId, season (default: 2024), page (default: 3), locale (default: it)."),
			mcplib.WithString("tournamentId", mcplib.Required(), mcplib.Description("Tournament ID (e.g. naYhNOaA for Serie A)")),
			mcplib.WithString("season", mcplib.Required(), mcplib.Description("Season year (e.g. 2024 for 2024/25 season)")),
			mcplib.WithString("page", mcplib.Required(), mcplib.Description("Page number for standings")),
			mcplib.WithString("locale", mcplib.Required(), mcplib.Description("Locale code (it=Italian)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/x/feed/tr_{tournamentId}_{season}_{page}_3_{locale}_1", []mcpParamBinding{{PublicName: "tournamentId", WireName: "tournamentId", Location: "path"}, {PublicName: "season", WireName: "season", Location: "path"}, {PublicName: "page", WireName: "page", Location: "path"}, {PublicName: "locale", WireName: "locale", Location: "path"}}, []string{"tournamentId", "season", "page", "locale"}),
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			data, _, err = c.PostWithParams(path, params, bodyArgs)
		case "PUT":
			data, _, err = c.PutWithParams(path, params, bodyArgs)
		case "PATCH":
			data, _, err = c.PatchWithParams(path, params, bodyArgs)
		case "DELETE":
			data, _, err = c.DeleteWithParams(path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + msg +
					"\nhint: check your API credentials." +
					"\n      Run 'diretta-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + msg +
					"\nhint: this API is configured without credentials; the service may be blocking the request by rate limit, geography, bot protection, or endpoint policy." +
					"\n      Run 'diretta-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "diretta-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "diretta-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "diretta",
		"description": "Risultati sportivi live, classifiche e statistiche da Diretta.it — zero setup, dati offline, MCP pronto.",
		"archetype":   "generic",
		"tool_count":  12,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion diretta-pp-cli binary.",
		"resources": []map[string]any{
			{
				"name":        "match",
				"description": "Individual match details",
				"endpoints":   []string{"detail", "events", "h2h", "lineups", "stats"},
				"searchable":  true,
			},
			{
				"name":        "matches",
				"description": "Football matches and results",
				"endpoints":   []string{"live", "today", "tomorrow", "yesterday"},
			},
			{
				"name":        "odds",
				"description": "Match betting odds via GraphQL",
				"endpoints":   []string{"match_odds"},
				"searchable":  true,
			},
			{
				"name":        "sports",
				"description": "Multi-sport feeds covering 30+ sports",
				"endpoints":   []string{"all_today"},
				"syncable":    true,
			},
			{
				"name":        "standings",
				"description": "Tournament standings and league tables",
				"endpoints":   []string{"table"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Team form digest", "command": "form", "description": "Shows a team's last N results with goals, cards, and venue split — home vs. away — from local cache.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "H2H + odds pivot", "command": "h2h", "description": "Shows historical head-to-head results alongside live odds for the next scheduled match between two teams.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Multi-match season export", "command": "export", "description": "Exports every match, statistic, and event for a full season of a competition as CSV or JSON.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Batch pregame report", "command": "pregame", "description": "For every match in a league on a given date, shows last-3 H2H and current odds in a single table.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Standings trend", "command": "standings-trend", "description": "Shows how each team's league position changed over the last N weeks using timestamped SQLite snapshots.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Sync health dashboard", "command": "sync-status", "description": "Reports the age, row counts, and freshness of every SQLite table so automation scripts can detect stale data before...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Team competitions cross-view", "command": "team-competitions", "description": "Lists every active competition a team is playing in, with the next fixture date for each — league, cup, and European.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Raw field explorer", "command": "raw", "description": "Dumps the raw FlashScore field-value pairs for any match ID, bypassing the normalizer — for debugging and protocol...", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Team form digest", "insight": ""},
			{"topic": "H2H + odds pivot", "insight": ""},
			{"topic": "Multi-match season export", "insight": ""},
			{"topic": "Batch pregame report", "insight": ""},
			{"topic": "Standings trend", "insight": ""},
			{"topic": "Sync health dashboard", "insight": ""},
			{"topic": "Team competitions cross-view", "insight": ""},
			{"topic": "Raw field explorer", "insight": ""},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
