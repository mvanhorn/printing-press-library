// Copyright 2026 jason-holt. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/drivethrurpg/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/drivethrurpg/internal/client"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/drivethrurpg/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/drivethrurpg/internal/config"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/drivethrurpg/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/drivethrurpg/internal/store"
	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("auth-key_create-token-from-application-key",
			mcplib.WithDescription("Exchange a DriveThruRPG Library App Application Key for API tokens. Required: applicationKey. Returns the new AuthToken."),
			mcplib.WithString("applicationKey", mcplib.Required(), mcplib.Description("DriveThruRPG Library App Application Key.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/auth_key", []mcpParamBinding{{PublicName: "applicationKey", WireName: "applicationKey", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("categories_get-category",
			mcplib.WithDescription("Get public category details. Required: categoryId. Returns the JsonApiResource."),
			mcplib.WithString("categoryId", mcplib.Required(), mcplib.Description("Category id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/categories/{categoryId}", []mcpParamBinding{{PublicName: "categoryId", WireName: "categoryId", Location: "path"}}, []string{"categoryId"}),
	)
	s.AddTool(
		mcplib.NewTool("categories_list",
			mcplib.WithDescription("List public categories. Optional: page (default: 1), pageSize (default: 20). Returns array of JsonApiItem."),
			mcplib.WithString("page", mcplib.Description("Page")),
			mcplib.WithString("pageSize", mcplib.Description("Page size")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/categories", []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("filters_get",
			mcplib.WithDescription("Get public filter details. Required: filterId. Returns the JsonApiResource."),
			mcplib.WithString("filterId", mcplib.Required(), mcplib.Description("Filter id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/filters/{filterId}", []mcpParamBinding{{PublicName: "filterId", WireName: "filterId", Location: "path"}}, []string{"filterId"}),
	)
	s.AddTool(
		mcplib.NewTool("filters_list",
			mcplib.WithDescription("List public filters. Optional: page (default: 1), pageSize (default: 20). Returns array of JsonApiItem."),
			mcplib.WithString("page", mcplib.Description("Page")),
			mcplib.WithString("pageSize", mcplib.Description("Page size")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/filters", []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("order-products_list-library-products",
			mcplib.WithDescription("List authenticated user's purchased library products. Optional: getChecksum (default: 1), getFilters (default: 0), page (default: 1) (plus 3 more). Returns array of LibraryProduct."),
			mcplib.WithString("getChecksum", mcplib.Description("Get checksum")),
			mcplib.WithString("getFilters", mcplib.Description("Get filters")),
			mcplib.WithString("page", mcplib.Description("Page")),
			mcplib.WithString("pageSize", mcplib.Description("Page size")),
			mcplib.WithString("library", mcplib.Description("Library")),
			mcplib.WithString("archived", mcplib.Description("Archived")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/order_products", []mcpParamBinding{{PublicName: "getChecksum", WireName: "getChecksum", Location: "query"}, {PublicName: "getFilters", WireName: "getFilters", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "library", WireName: "library", Location: "query"}, {PublicName: "archived", WireName: "archived", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("order-products_check_download",
			mcplib.WithDescription("Poll for a prepared purchased file download URL. Required: orderProductId, index. Optional: siteId (default: 10), getChecksums (default: 0). Returns the DownloadStatus."),
			mcplib.WithString("orderProductId", mcplib.Required(), mcplib.Description("Order product id")),
			mcplib.WithString("siteId", mcplib.Description("DriveThruRPG site id. The public Library App uses 10.")),
			mcplib.WithString("index", mcplib.Required(), mcplib.Description("File index from the owned product's files array.")),
			mcplib.WithString("getChecksums", mcplib.Description("Get checksums")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/order_products/{orderProductId}/check", []mcpParamBinding{{PublicName: "orderProductId", WireName: "orderProductId", Location: "path"}, {PublicName: "siteId", WireName: "siteId", Location: "query"}, {PublicName: "index", WireName: "index", Location: "query"}, {PublicName: "getChecksums", WireName: "getChecksums", Location: "query"}}, []string{"orderProductId"}),
	)
	s.AddTool(
		mcplib.NewTool("order-products_prepare_download",
			mcplib.WithDescription("Prepare a purchased file download URL. Required: orderProductId, index. Optional: siteId (default: 10), getChecksums (default: 0). Returns the DownloadStatus."),
			mcplib.WithString("orderProductId", mcplib.Required(), mcplib.Description("Order product id")),
			mcplib.WithString("siteId", mcplib.Description("DriveThruRPG site id. The public Library App uses 10.")),
			mcplib.WithString("index", mcplib.Required(), mcplib.Description("File index from the owned product's files array.")),
			mcplib.WithString("getChecksums", mcplib.Description("Get checksums")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/order_products/{orderProductId}/prepare", []mcpParamBinding{{PublicName: "orderProductId", WireName: "orderProductId", Location: "path"}, {PublicName: "siteId", WireName: "siteId", Location: "query"}, {PublicName: "index", WireName: "index", Location: "query"}, {PublicName: "getChecksums", WireName: "getChecksums", Location: "query"}}, []string{"orderProductId"}),
	)
	s.AddTool(
		mcplib.NewTool("products_get",
			mcplib.WithDescription("Get public product details. Required: productId. Returns the JsonApiResource."),
			mcplib.WithString("productId", mcplib.Required(), mcplib.Description("Product id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/products/{productId}", []mcpParamBinding{{PublicName: "productId", WireName: "productId", Location: "path"}}, []string{"productId"}),
	)
	s.AddTool(
		mcplib.NewTool("products_search",
			mcplib.WithDescription("Search and list public DriveThruRPG products. Optional: page (default: 1), pageSize (default: 20), keyword (plus 2 more). Returns array of JsonApiItem."),
			mcplib.WithString("page", mcplib.Description("Page")),
			mcplib.WithString("pageSize", mcplib.Description("Page size")),
			mcplib.WithString("keyword", mcplib.Description("Search keyword.")),
			mcplib.WithString("publisherId", mcplib.Description("Restrict results to a publisher id.")),
			mcplib.WithString("sort", mcplib.Description("API-native sort key.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/products", []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "keyword", WireName: "keyword", Location: "query"}, {PublicName: "publisherId", WireName: "publisherId", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("publishers_get",
			mcplib.WithDescription("Get public publisher details. Required: publisherId. Returns the JsonApiResource."),
			mcplib.WithString("publisherId", mcplib.Required(), mcplib.Description("Publisher id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/publishers/{publisherId}", []mcpParamBinding{{PublicName: "publisherId", WireName: "publisherId", Location: "path"}}, []string{"publisherId"}),
	)
	s.AddTool(
		mcplib.NewTool("reviews_list",
			mcplib.WithDescription("List public reviews, optionally for a product. Optional: page (default: 1), pageSize (default: 20), productId. Returns array of JsonApiItem."),
			mcplib.WithString("page", mcplib.Description("Page")),
			mcplib.WithString("pageSize", mcplib.Description("Page size")),
			mcplib.WithString("productId", mcplib.Description("Product id to filter reviews by.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/reviews", []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}, {PublicName: "productId", WireName: "productId", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("search-ahead_search_ahead",
			mcplib.WithDescription("Search ahead across products, publishers, categories, and filters. Required: keyword. Optional: pageSize (default: 20). Returns array of JsonApiItem."),
			mcplib.WithString("keyword", mcplib.Required(), mcplib.Description("Keyword")),
			mcplib.WithString("pageSize", mcplib.Description("Page size")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/search_ahead", []mcpParamBinding{{PublicName: "keyword", WireName: "keyword", Location: "query"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("special-offers_list",
			mcplib.WithDescription("List active public special offers. Optional: page (default: 1), pageSize (default: 20). Returns array of JsonApiItem."),
			mcplib.WithString("page", mcplib.Description("Page")),
			mcplib.WithString("pageSize", mcplib.Description("Page size")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/special_offers", []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query"}, {PublicName: "pageSize", WireName: "pageSize", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	RegisterIntentTools(s)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export DRIVETHRURPG_DTRPG_TOKEN=<your-key>" +
					"\n      Run 'drivethrurpg-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set it with: export DRIVETHRURPG_DTRPG_TOKEN=<your-key>" +
					"\n      Run 'drivethrurpg-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export DRIVETHRURPG_DTRPG_TOKEN=<your-key>" +
					"\n      Run 'drivethrurpg-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "drivethrurpg-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "drivethrurpg-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "drivethrurpg",
		"description": "Search the public DriveThruRPG catalog and download purchased files from an authenticated library.",
		"archetype":   "generic",
		"tool_count":  14,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion drivethrurpg-pp-cli binary.",
		"auth": map[string]any{
			"type": "api_key",
			"env_vars": []map[string]any{
				{
					"name":        "DRIVETHRURPG_APPLICATION_KEY",
					"kind":        "token_exchange",
					"required":    false,
					"sensitive":   true,
					"description": "Preferred DriveThruRPG Library App Application Key with My Library Access enabled.",
				},
				{
					"name":        "DRIVETHRURPG_DTRPG_TOKEN",
					"kind":        "per_call",
					"required":    false,
					"sensitive":   true,
					"description": "Optional already-exchanged DriveThruRPG API token.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "auth-key",
				"description": "Manage auth key",
				"endpoints":   []string{"create-token-from-application-key"},
			},
			{
				"name":        "categories",
				"description": "Manage categories",
				"endpoints":   []string{"get-category", "list"},
				"syncable":    true,
			},
			{
				"name":        "filters",
				"description": "Manage filters",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
			},
			{
				"name":        "order-products",
				"description": "Manage order products",
				"endpoints":   []string{"list-library-products"},
				"syncable":    true,
			},
			{
				"name":        "products",
				"description": "Manage products",
				"endpoints":   []string{"get", "search"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "publishers",
				"description": "Manage publishers",
				"endpoints":   []string{"get"},
			},
			{
				"name":        "reviews",
				"description": "Manage reviews",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "search-ahead",
				"description": "Manage search ahead",
				"endpoints":   []string{"search_ahead"},
				"searchable":  true,
			},
			{
				"name":        "special-offers",
				"description": "Manage special offers",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the pagesize parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
