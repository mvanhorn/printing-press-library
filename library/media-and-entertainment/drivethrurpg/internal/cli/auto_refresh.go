// Copyright 2026 Jason Holt and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.
// Stub added to satisfy root pre-run call injected by template merge on a tree
// generated before cache freshness was wired. Provides no-op auto-refresh so the
// CLI builds and runs; full implementation can be added by re-running generate
// after adding `cache: { enabled: true, ... }` to the spec if desired.

package cli

import (
	"context"
)

// readCommandResources maps command paths to resource types for auto-refresh
// decisions. Empty here because this tree predates the cache block; commands
// will see "skipped" meta.
var readCommandResources = map[string][]string{}

// autoRefreshIfStale is the stub entry point called from root PersistentPreRunE
// (and ensureFresh helpers). Always returns a skipped meta (as any, since
// rootFlags.freshnessMeta is any) so the CLI builds/runs without a full cache
// policy from a newer generator.
func autoRefreshIfStale(ctx context.Context, flags *rootFlags, resources []string) any {
	return map[string]any{
		"decision":  "skipped",
		"resources": resources,
		"source":    flags.dataSource,
		"reason":    "no-cache-config-in-spec (stub)",
	}
}

// ensureFreshForResources and ensureFreshForCommand are provided for any
// hand-authored novel commands that might call them (no-op in this stub).
func ensureFreshForResources(ctx context.Context, flags *rootFlags, resources ...string) any {
	meta := autoRefreshIfStale(ctx, flags, resources)
	flags.freshnessMeta = meta
	return meta
}

func ensureFreshForCommand(ctx context.Context, flags *rootFlags, commandPath string) any {
	resources, ok := readCommandResources[commandPath]
	if !ok {
		meta := map[string]any{
			"decision": "skipped",
			"reason":   "unregistered_command",
			"source":   flags.dataSource,
		}
		flags.freshnessMeta = meta
		return meta
	}
	return ensureFreshForResources(ctx, flags, resources...)
}
