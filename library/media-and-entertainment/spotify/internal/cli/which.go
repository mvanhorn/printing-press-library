// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "albums get-an", Description: "Get Album", Group: "albums"},
	{Command: "albums get-multiple", Description: "Get Several Albums", Group: "albums"},
	{Command: "albums tracks get-an-albums", Description: "Get Album Tracks", Group: "albums"},
	{Command: "artists get-an", Description: "Get Artist", Group: "artists"},
	{Command: "artists get-multiple", Description: "Get Several Artists", Group: "artists"},
	{Command: "artists albums get-an-artists", Description: "Get Artist's Albums", Group: "artists"},
	{Command: "artists related-artists get-an-artists", Description: "Get Artist's Related Artists", Group: "artists"},
	{Command: "artists top-tracks get-an-artists", Description: "Get Artist's Top Tracks", Group: "artists"},
	{Command: "audio-analysis get", Description: "Get Track's Audio Analysis", Group: "audio-analysis"},
	{Command: "audio-features get", Description: "Get Track's Audio Features", Group: "audio-features"},
	{Command: "audio-features get-several", Description: "Get Tracks' Audio Features", Group: "audio-features"},
	{Command: "audiobooks get-an", Description: "Get an Audiobook", Group: "audiobooks"},
	{Command: "audiobooks get-multiple", Description: "Get Several Audiobooks", Group: "audiobooks"},
	{Command: "audiobooks chapters get-audiobook", Description: "Get Audiobook Chapters", Group: "audiobooks"},
	{Command: "browse get-a-categories-playlists", Description: "Get Category's Playlists", Group: "browse"},
	{Command: "browse get-a-category", Description: "Get Single Browse Category", Group: "browse"},
	{Command: "browse get-categories", Description: "Get Several Browse Categories", Group: "browse"},
	{Command: "browse get-featured-playlists", Description: "Get Featured Playlists", Group: "browse"},
	{Command: "browse get-new-releases", Description: "Get New Releases", Group: "browse"},
	{Command: "chapters get-a", Description: "Get a Chapter", Group: "chapters"},
	{Command: "chapters get-several", Description: "Get Several Chapters", Group: "chapters"},
	{Command: "episodes get-an", Description: "Get Episode", Group: "episodes"},
	{Command: "episodes get-multiple", Description: "Get Several Episodes", Group: "episodes"},
	{Command: "markets get-available", Description: "Get Available Markets", Group: "markets"},
	{Command: "me add-to-queue", Description: "Add Item to Playback Queue", Group: "me"},
	{Command: "me check-current-user-follows", Description: "Check If User Follows Artists or Users", Group: "me"},
	{Command: "me check-users-saved-albums", Description: "Check User's Saved Albums", Group: "me"},
	{Command: "me check-users-saved-audiobooks", Description: "Check User's Saved Audiobooks", Group: "me"},
	{Command: "me check-users-saved-episodes", Description: "Check User's Saved Episodes", Group: "me"},
	{Command: "me check-users-saved-shows", Description: "Check User's Saved Shows", Group: "me"},
	{Command: "me check-users-saved-tracks", Description: "Check User's Saved Tracks", Group: "me"},
	{Command: "me follow-artists-users", Description: "Follow Artists or Users", Group: "me"},
	{Command: "me get-a-list-of-current-users-playlists", Description: "Get Current User's Playlists", Group: "me"},
	{Command: "me get-a-users-available-devices", Description: "Get Available Devices", Group: "me"},
	{Command: "me get-current-users-profile", Description: "Get Current User's Profile", Group: "me"},
	{Command: "me get-followed", Description: "Get Followed Artists", Group: "me"},
	{Command: "me get-information-about-the-users-current-playback", Description: "Get Playback State", Group: "me"},
	{Command: "me get-queue", Description: "Get the User's Queue", Group: "me"},
	{Command: "me get-recently-played", Description: "Get Recently Played Tracks", Group: "me"},
	{Command: "me get-the-users-currently-playing-track", Description: "Get Currently Playing Track", Group: "me"},
	{Command: "me get-users-saved-albums", Description: "Get User's Saved Albums", Group: "me"},
	{Command: "me get-users-saved-audiobooks", Description: "Get User's Saved Audiobooks", Group: "me"},
	{Command: "me get-users-saved-episodes", Description: "Get User's Saved Episodes", Group: "me"},
	{Command: "me get-users-saved-shows", Description: "Get User's Saved Shows", Group: "me"},
	{Command: "me get-users-saved-tracks", Description: "Get User's Saved Tracks", Group: "me"},
	{Command: "me get-users-top-artists-and-tracks", Description: "Get User's Top Items", Group: "me"},
	{Command: "me pause-a-users-playback", Description: "Pause Playback", Group: "me"},
	{Command: "me remove-albums-user", Description: "Remove Users' Saved Albums", Group: "me"},
	{Command: "me remove-audiobooks-user", Description: "Remove User's Saved Audiobooks", Group: "me"},
	{Command: "me remove-episodes-user", Description: "Remove User's Saved Episodes", Group: "me"},
	{Command: "me remove-shows-user", Description: "Remove User's Saved Shows", Group: "me"},
	{Command: "me remove-tracks-user", Description: "Remove User's Saved Tracks", Group: "me"},
	{Command: "me save-albums-user", Description: "Save Albums for Current User", Group: "me"},
	{Command: "me save-audiobooks-user", Description: "Save Audiobooks for Current User", Group: "me"},
	{Command: "me save-episodes-user", Description: "Save Episodes for Current User", Group: "me"},
	{Command: "me save-shows-user", Description: "Save Shows for Current User", Group: "me"},
	{Command: "me save-tracks-user", Description: "Save Tracks for Current User", Group: "me"},
	{Command: "me seek-to-position-in-currently-playing-track", Description: "Seek To Position", Group: "me"},
	{Command: "me set-repeat-mode-on-users-playback", Description: "Set Repeat Mode", Group: "me"},
	{Command: "me set-volume-for-users-playback", Description: "Set Playback Volume", Group: "me"},
	{Command: "me skip-users-playback-to-next-track", Description: "Skip To Next", Group: "me"},
	{Command: "me skip-users-playback-to-previous-track", Description: "Skip To Previous", Group: "me"},
	{Command: "me start-a-users-playback", Description: "Start/Resume Playback", Group: "me"},
	{Command: "me toggle-shuffle-for-users-playback", Description: "Toggle Playback Shuffle", Group: "me"},
	{Command: "me transfer-a-users-playback", Description: "Transfer Playback", Group: "me"},
	{Command: "me unfollow-artists-users", Description: "Unfollow Artists or Users", Group: "me"},
	{Command: "playlists change-details", Description: "Change Playlist Details", Group: "playlists"},
	{Command: "playlists get", Description: "Get Playlist", Group: "playlists"},
	{Command: "playlists followers check-if-user-follows-playlist", Description: "Check if Users Follow Playlist", Group: "playlists"},
	{Command: "playlists followers follow-playlist", Description: "Follow Playlist", Group: "playlists"},
	{Command: "playlists followers unfollow-playlist", Description: "Unfollow Playlist", Group: "playlists"},
	{Command: "playlists images get-playlist-cover", Description: "Get Playlist Cover Image", Group: "playlists"},
	{Command: "playlists images upload-custom-playlist-cover", Description: "Add Custom Playlist Cover Image", Group: "playlists"},
	{Command: "playlists tracks add-to-playlist", Description: "Add Items to Playlist", Group: "playlists"},
	{Command: "playlists tracks get-playlists", Description: "Get Playlist Items", Group: "playlists"},
	{Command: "playlists tracks remove-playlist", Description: "Remove Playlist Items", Group: "playlists"},
	{Command: "playlists tracks reorder-or-replace-playlists", Description: "Update Playlist Items", Group: "playlists"},
	{Command: "recommendations get", Description: "Get Recommendations", Group: "recommendations"},
	{Command: "recommendations get-genres", Description: "Get Available Genre Seeds", Group: "recommendations"},
	{Command: "search search", Description: "Search for Item", Group: "search"},
	{Command: "shows get-a", Description: "Get Show", Group: "shows"},
	{Command: "shows get-multiple", Description: "Get Several Shows", Group: "shows"},
	{Command: "shows episodes get-a-shows", Description: "Get Show Episodes", Group: "shows"},
	{Command: "tracks get", Description: "Get Track", Group: "tracks"},
	{Command: "tracks get-several", Description: "Get Several Tracks", Group: "tracks"},
	{Command: "users get-profile", Description: "Get User's Profile", Group: "users"},
	{Command: "users playlists create", Description: "Create Playlist", Group: "users"},
	{Command: "users playlists get-list-users", Description: "Get User's Playlists", Group: "users"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  spotify-web-pp-cli which "stale tickets"
  spotify-web-pp-cli which "bottleneck"
  spotify-web-pp-cli which --limit 1 "send message"
  spotify-web-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		enc := json.NewEncoder(w)
		enc.SetIndent("", "  ")
		return enc.Encode(matches)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
