// Copyright 2026 garrickpg and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSiteSiteImageCmd(flags *rootFlags) *cobra.Command {
	var flagMaxWidth int
	var flagMaxHeight int
	var flagHash string

	cmd := &cobra.Command{
		Use:         "site-image <siteId> <name>",
		Short:       "Site image as uploaded to the monitoring portal",
		Example:     "  solaredge-pp-cli site site-image 42 example-resource",
		Annotations: map[string]string{"pp:endpoint": "site.site-image", "pp:method": "GET", "pp:path": "/site/{siteId}/siteImage/{name}", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			path := "/site/{siteId}/siteImage/{name}"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("siteId is required\nUsage: %s <%s>", cmd.CommandPath(), "siteId"))
			}
			path = replacePathParam(path, "siteId", args[0])
			if len(args) < 2 || args[1] == "" {
				return usageErr(fmt.Errorf("name is required\nUsage: %s <%s>", cmd.CommandPath(), "name"))
			}
			path = replacePathParam(path, "name", args[1])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			headerOverrides := map[string]string{
				"X-Printing-Press-Binary-Response": "true",
			}
			params := map[string]string{}
			if flagMaxWidth != 0 {
				params["maxWidth"] = formatCLIParamValue(flagMaxWidth)
			}
			if flagMaxHeight != 0 {
				params["maxHeight"] = formatCLIParamValue(flagMaxHeight)
			}
			if flagHash != "" {
				params["hash"] = formatCLIParamValue(flagHash)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "site", false, path, params, headerOverrides, "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			_ = json.Valid
			_ = os.Stderr
			_ = prov
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.csv || flags.compact || flags.plain || flags.selectFields != "" {
				return fmt.Errorf("binary response cannot be rendered as structured output; redirect stdout or use --deliver file:<path>")
			}
			_, err = cmd.OutOrStdout().Write(data)
			return err
		},
	}
	cmd.Flags().IntVar(&flagMaxWidth, "max-width", 0, "Maximum width to scale this image")
	cmd.Flags().IntVar(&flagMaxHeight, "max-height", 0, "Maximum height to scale this image")
	cmd.Flags().StringVar(&flagHash, "hash", "", "Image hash, for conditional re-fetch")

	return cmd
}
