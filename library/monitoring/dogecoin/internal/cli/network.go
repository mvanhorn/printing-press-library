// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"
)

func newNetworkCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "network",
		Short: "Node network status — peers, connections, version",
	}

	cmd.AddCommand(newNetworkInfoCmd(flags))
	cmd.AddCommand(newNetworkPeersCmd(flags))
	return cmd
}

// peers is a separate top-level resource for health and breakdown
func newPeersCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "peers",
		Short: "Peer connection health and breakdown",
	}
	cmd.AddCommand(newPeersHealthCmd(flags))
	cmd.AddCommand(newPeersBreakdownCmd(flags))
	return cmd
}

// blocks is a separate top-level resource for found/log
func newBlocksCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "blocks",
		Short: "Block event detection and history",
	}
	cmd.AddCommand(newBlocksFoundCmd(flags))
	cmd.AddCommand(newBlocksLogCmd(flags))
	return cmd
}
