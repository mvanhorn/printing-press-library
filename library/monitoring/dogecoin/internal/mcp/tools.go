// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	"github.com/mvanhorn/printing-press-library/library/monitoring/dogecoin/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/monitoring/dogecoin/internal/client"
	"github.com/mvanhorn/printing-press-library/library/monitoring/dogecoin/internal/config"
	"github.com/mvanhorn/printing-press-library/library/monitoring/dogecoin/internal/mcp/cobratree"
	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("blockchain_count",
			mcplib.WithDescription("Get current block height. Returns the new BlockCount."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("blockchain_get",
			mcplib.WithDescription("Get block data by hash. Required: hash. Optional: verbose. Returns the new Block."),
			mcplib.WithString("hash", mcplib.Required(), mcplib.Description("Block hash")),
			mcplib.WithString("verbose", mcplib.Description("Verbosity level (0=hex, 1=json, 2=json+tx)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{{PublicName: "hash", WireName: "hash", Location: "query"}, {PublicName: "verbose", WireName: "verbose", Location: "query"}}, []string{"hash"}),
	)
	s.AddTool(
		mcplib.NewTool("blockchain_hash",
			mcplib.WithDescription("Get block hash at height. Required: height. Returns the new BlockHash."),
			mcplib.WithString("height", mcplib.Required(), mcplib.Description("Block height")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{{PublicName: "height", WireName: "height", Location: "query"}}, []string{"height"}),
	)
	s.AddTool(
		mcplib.NewTool("blockchain_info",
			mcplib.WithDescription("Get blockchain state (height, difficulty, sync progress). Returns the new BlockchainInfo."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mempool_fees",
			mcplib.WithDescription("Estimate fee per KB for target confirmation blocks. Required: blocks. Returns the new FeeEstimate."),
			mcplib.WithString("blocks", mcplib.Required(), mcplib.Description("Target confirmation blocks")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{{PublicName: "blocks", WireName: "blocks", Location: "query"}}, []string{"blocks"}),
	)
	s.AddTool(
		mcplib.NewTool("mempool_info",
			mcplib.WithDescription("Get mempool statistics (size, bytes). Returns the new MempoolInfo."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mining_info",
			mcplib.WithDescription("Get mining info (difficulty, hashrate, mempool). Returns the new MiningInfo."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mining_networkhashps",
			mcplib.WithDescription("Get network hash rate in H/s. Optional: blocks, height. Returns the new NetworkHashPS."),
			mcplib.WithString("blocks", mcplib.Description("Blocks to average over (default 120)")),
			mcplib.WithString("height", mcplib.Description("Chain height (-1 = current)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{{PublicName: "blocks", WireName: "blocks", Location: "query"}, {PublicName: "height", WireName: "height", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("network_info",
			mcplib.WithDescription("Get network info (version, peers, services). Returns the new NetworkInfo."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("network_peers",
			mcplib.WithDescription("Get detailed peer connection info. Returns array of PeerInfo."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("node_uptime",
			mcplib.WithDescription("Get node uptime in seconds. Returns the new Uptime."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("wallet_info",
			mcplib.WithDescription("Get wallet summary (balance, unconfirmed, immature). Returns the new WalletInfo."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("wallet_transactions",
			mcplib.WithDescription("List recent wallet transactions. Optional: count, skip. Returns array of Transaction."),
			mcplib.WithString("count", mcplib.Description("Number of transactions to return (default 10)")),
			mcplib.WithString("skip", mcplib.Description("Transactions to skip")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/", []mcpParamBinding{{PublicName: "count", WireName: "count", Location: "query"}, {PublicName: "skip", WireName: "skip", Location: "query"}}, []string{}),
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + msg +
					"\nhint: check your API credentials." +
					"\n      Run 'dogecoin-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + msg +
					"\nhint: this API is configured without credentials; the service may be blocking the request by rate limit, geography, bot protection, or endpoint policy." +
					"\n      Run 'dogecoin-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "dogecoin-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "dogecoin-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "dogecoin",
		"description": "Monitor your Dogecoin node with typed exit codes, SQLite trending, and native MCP — everything bitcoin-cli lacks...",
		"archetype":   "infrastructure",
		"tool_count":  13,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion dogecoin-pp-cli binary.",
		"resources": []map[string]any{
			{
				"name":        "blockchain",
				"description": "Blockchain state — height, difficulty, sync status",
				"endpoints":   []string{"count", "get", "hash", "info"},
			},
			{
				"name":        "mempool",
				"description": "Memory pool state — pending transactions and fee estimates",
				"endpoints":   []string{"fees", "info"},
			},
			{
				"name":        "mining",
				"description": "Mining metrics — hashrate, difficulty, pool status",
				"endpoints":   []string{"info", "networkhashps"},
			},
			{
				"name":        "network",
				"description": "Node network status — peers, connections, version",
				"endpoints":   []string{"info", "peers"},
			},
			{
				"name":        "node",
				"description": "Node management — uptime, diagnostics",
				"endpoints":   []string{"uptime"},
			},
			{
				"name":        "wallet",
				"description": "Wallet state — balance, transactions",
				"endpoints":   []string{"info", "transactions"},
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the count parameter (default 100).",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Mining Stats Compound", "command": "mining stats", "description": "One command returns hashrate + difficulty + network hashrate + block height — formatted for n8n shell nodes, XEMD...", "rationale": "No existing Dogecoin tool combines these into a single scriptable command with typed exit codes and --compact output.", "via": "mcp-command-mirror"},
			{"name": "Peer Health Check", "command": "peers health", "description": "Checks connected peer count and exits 0 (healthy) or 3 (below threshold) — n8n shell nodes branch on this exit code.", "rationale": "Typed exit codes for n8n branching require machine-readable pass/fail signals; no existing tool does this.", "via": "mcp-command-mirror"},
			{"name": "Blocks Found Window", "command": "blocks found", "description": "Detects if blocks were mined in a time window (--since 7d). Exit 0=found, exit 2=none found.", "rationale": "Block window detection with typed exit codes is not available in any existing Dogecoin monitoring tool.", "via": "mcp-command-mirror"},
			{"name": "Hashrate History Trend", "command": "hashrate history", "description": "Queries SQLite for historical hashrate over a time window — powers XEMD dashboard trend charts.", "rationale": "Requires local SQLite time-series populated by sync; no existing Dogecoin tool stores and queries historical hashrate.", "via": "mcp-command-mirror"},
			{"name": "Difficulty Trend Alert", "command": "difficulty trend", "description": "Detects difficulty spikes or drops from SQLite history. Exit 0=stable, exit 5=significant change detected.", "rationale": "Requires comparing multiple historical snapshots in local SQLite; no single RPC call provides this.", "via": "mcp-command-mirror"},
			{"name": "Mempool Status Compound", "command": "mempool status", "description": "Combines getmempoolinfo + estimatefee with human-readable fee tier labels (low/medium/high DOGE/KB).", "rationale": "Combines two RPC calls into a single monitoring-friendly view with fee tier context.", "via": "mcp-command-mirror"},
			{"name": "Node Doctor", "command": "doctor", "description": "Version obsolescence check, auth validation, peer count, sync progress, and uptime in one command. Emits warning...", "rationale": "Combines multiple RPC calls into a single diagnostic view with version threshold checking.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Mining Stats Compound", "insight": "No existing Dogecoin tool combines these into a single scriptable command with typed exit codes and --compact output."},
			{"topic": "Peer Health Check", "insight": "Typed exit codes for n8n branching require machine-readable pass/fail signals; no existing tool does this."},
			{"topic": "Blocks Found Window", "insight": "Block window detection with typed exit codes is not available in any existing Dogecoin monitoring tool."},
			{"topic": "Hashrate History Trend", "insight": "Requires local SQLite time-series populated by sync; no existing Dogecoin tool stores and queries historical hashrate."},
			{"topic": "Difficulty Trend Alert", "insight": "Requires comparing multiple historical snapshots in local SQLite; no single RPC call provides this."},
			{"topic": "Mempool Status Compound", "insight": "Combines two RPC calls into a single monitoring-friendly view with fee tier context."},
			{"topic": "Node Doctor", "insight": "Combines multiple RPC calls into a single diagnostic view with version threshold checking."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
