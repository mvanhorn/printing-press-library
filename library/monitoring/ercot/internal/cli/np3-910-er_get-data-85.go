// Copyright 2026 synota-inc. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newNp3910ErGetData85Cmd(flags *rootFlags) *cobra.Command {
	var flagSumHSLOutputSchedFrom float64
	var flagSumHSLOutputSchedTo float64
	var flagSumOutputSchedFrom float64
	var flagSumOutputSchedTo float64
	var flagSCEDTimestampFrom string
	var flagSCEDTimestampTo string
	var flagRepeatHourFlag bool
	var flagSumLSLOutputSchedFrom float64
	var flagSumLSLOutputSchedTo float64
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-data-85",
		Short:       "2 Day Aggregated Output Schedule North",
		Example:     "  ercot-pp-cli np3-910-er get-data-85",
		Annotations: map[string]string{"pp:endpoint": "np3-910-er.get-data-85", "pp:method": "GET", "pp:path": "/np3-910-er/2d_agg_out_sched_north", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/np3-910-er/2d_agg_out_sched_north"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "np3-910-er", path, map[string]string{
				"sumHSLOutputSchedFrom": fmt.Sprintf("%v", flagSumHSLOutputSchedFrom),
				"sumHSLOutputSchedTo":   fmt.Sprintf("%v", flagSumHSLOutputSchedTo),
				"sumOutputSchedFrom":    fmt.Sprintf("%v", flagSumOutputSchedFrom),
				"sumOutputSchedTo":      fmt.Sprintf("%v", flagSumOutputSchedTo),
				"SCEDTimestampFrom":     fmt.Sprintf("%v", flagSCEDTimestampFrom),
				"SCEDTimestampTo":       fmt.Sprintf("%v", flagSCEDTimestampTo),
				"repeatHourFlag":        fmt.Sprintf("%v", flagRepeatHourFlag),
				"sumLSLOutputSchedFrom": fmt.Sprintf("%v", flagSumLSLOutputSchedFrom),
				"sumLSLOutputSchedTo":   fmt.Sprintf("%v", flagSumLSLOutputSchedTo),
			}, nil, flagAll, "page", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().Float64Var(&flagSumHSLOutputSchedFrom, "sum-hsl-output-sched-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagSumHSLOutputSchedTo, "sum-hsl-output-sched-to", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagSumOutputSchedFrom, "sum-output-sched-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagSumOutputSchedTo, "sum-output-sched-to", 0.0, "Format - ####.###.")
	cmd.Flags().StringVar(&flagSCEDTimestampFrom, "sced-timestamp-from", "", "Format - yyyy-MM-ddTH24:mm:ss.")
	cmd.Flags().StringVar(&flagSCEDTimestampTo, "sced-timestamp-to", "", "Format - yyyy-MM-ddTH24:mm:ss.")
	cmd.Flags().BoolVar(&flagRepeatHourFlag, "repeat-hour-flag", false, "Format - true | false.")
	cmd.Flags().Float64Var(&flagSumLSLOutputSchedFrom, "sum-lsl-output-sched-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagSumLSLOutputSchedTo, "sum-lsl-output-sched-to", 0.0, "Format - ####.###.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
