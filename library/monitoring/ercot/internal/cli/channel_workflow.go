// Copyright 2026 synota-inc. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"time"

	"ercot-pp-cli/internal/store"
	"github.com/spf13/cobra"
)

func newWorkflowCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "workflow",
		Short: "Compound workflows that combine multiple API operations",
		RunE:  parentNoSubcommandRunE(flags),
	}
	cmd.AddCommand(newWorkflowArchiveCmd(flags))
	cmd.AddCommand(newWorkflowStatusCmd(flags))

	return cmd
}
func newWorkflowArchiveCmd(flags *rootFlags) *cobra.Command {
	var dbPath string
	var full bool

	cmd := &cobra.Command{
		Use:   "archive",
		Short: "Sync all resources to local store for offline access and search",
		Long: `Archive fetches all syncable resources from the API and stores them in a
local SQLite database. Supports incremental sync (only new data since last run)
and full resync. After archiving, use 'search' for instant full-text search.`,
		Example: `  # Archive all resources
  ercot-pp-cli workflow archive

  # Full re-archive (ignore previous sync state)
  ercot-pp-cli workflow archive --full`,
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			c.NoCache = true

			if dbPath == "" {
				dbPath = defaultDBPath("ercot-pp-cli")
			}
			s, err := store.OpenWithContext(cmd.Context(), dbPath)
			if err != nil {
				return fmt.Errorf("opening store: %w", err)
			}
			defer s.Close()

			resources := []string{"np3-233-cd", "np3-565-cd", "np3-566-cd", "np3-910-er", "np3-910-er-2d-agg-gen-summary", "np3-910-er-2d-agg-gen-summary-houston", "np3-910-er-2d-agg-gen-summary-north", "np3-910-er-2d-agg-gen-summary-south", "np3-910-er-2d-agg-gen-summary-west", "np3-910-er-2d-agg-load-summary", "np3-910-er-2d-agg-load-summary-houston", "np3-910-er-2d-agg-load-summary-north", "np3-910-er-2d-agg-load-summary-south", "np3-910-er-2d-agg-load-summary-west", "np3-910-er-2d-agg-out-sched", "np3-910-er-2d-agg-out-sched-houston", "np3-910-er-2d-agg-out-sched-north", "np3-910-er-2d-agg-out-sched-south", "np3-910-er-2d-agg-out-sched-west", "np3-911-er", "np3-911-er-2d-agg-as-offers-ecrsm", "np3-911-er-2d-agg-as-offers-ecrss", "np3-911-er-2d-agg-as-offers-offns", "np3-911-er-2d-agg-as-offers-regdn", "np3-911-er-2d-agg-as-offers-regup", "np3-911-er-2d-agg-as-offers-rrsffr", "np3-911-er-2d-agg-as-offers-rrspfr", "np3-911-er-2d-agg-as-offers-rrsufr", "np3-911-er-2d-cleared-dam-as-ecrsm", "np3-911-er-2d-cleared-dam-as-ecrss", "np3-911-er-2d-cleared-dam-as-nspin", "np3-911-er-2d-cleared-dam-as-regdn", "np3-911-er-2d-cleared-dam-as-regup", "np3-911-er-2d-cleared-dam-as-rrsffr", "np3-911-er-2d-cleared-dam-as-rrspfr", "np3-911-er-2d-cleared-dam-as-rrsufr", "np3-911-er-2d-self-arranged-as-ecrsm", "np3-911-er-2d-self-arranged-as-ecrss", "np3-911-er-2d-self-arranged-as-nspin", "np3-911-er-2d-self-arranged-as-nspnm", "np3-911-er-2d-self-arranged-as-regdn", "np3-911-er-2d-self-arranged-as-regup", "np3-911-er-2d-self-arranged-as-rrsffr", "np3-911-er-2d-self-arranged-as-rrspfr", "np3-911-er-2d-self-arranged-as-rrsufr", "np3-965-er", "np3-965-er-60-hdl-ldl-man-override", "np3-965-er-60-load-res-data-in-sced", "np3-965-er-60-sced-dsr-load-data", "np3-965-er-60-sced-qse-self-arranged-as", "np3-965-er-60-sced-smne-gen-res", "np3-966-er", "np3-966-er-60-dam-energy-bid-awards", "np3-966-er-60-dam-energy-only-offer-awards", "np3-966-er-60-dam-energy-only-offers", "np3-966-er-60-dam-gen-res-as-offers", "np3-966-er-60-dam-gen-res-data", "np3-966-er-60-dam-load-res-as-offers", "np3-966-er-60-dam-load-res-data", "np3-966-er-60-dam-ptp-obl-bid-awards", "np3-966-er-60-dam-ptp-obl-bids", "np3-966-er-60-dam-ptp-obl-opt", "np3-966-er-60-dam-ptp-obl-opt-awards", "np3-966-er-60-dam-qse-self-as", "np3-990-ex", "np3-990-ex-60-sasm-gen-res-as-offer-awards", "np3-990-ex-60-sasm-load-res-as-offer-awards", "np3-990-ex-60-sasm-load-res-as-offers", "np3-991-ex", "np4-159-cd", "np4-179-cd", "np4-183-cd", "np4-188-cd", "np4-190-cd", "np4-191-cd", "np4-196-m", "np4-196-m-dam-price-corrections-eblmp", "np4-196-m-dam-price-corrections-mcpc", "np4-197-m", "np4-197-m-rtm-price-corrections-eblmp", "np4-197-m-rtm-price-corrections-shadow", "np4-197-m-rtm-price-corrections-soglmp", "np4-197-m-rtm-price-corrections-sogprice", "np4-197-m-rtm-price-corrections-splmp", "np4-33-cd", "np4-523-cd", "np4-732-cd", "np4-733-cd", "np4-737-cd", "np4-738-cd", "np4-742-cd", "np4-743-cd", "np4-745-cd", "np4-746-cd", "np6-322-cd", "np6-345-cd", "np6-346-cd", "np6-787-cd", "np6-788-cd", "np6-86-cd", "np6-905-cd", "np6-970-cd"}
			totalSynced := 0

			// --full clears the cursor here because syncResource reads
			// existingCursor unconditionally; its full param only gates the
			// since filter, not cursor reset. Mirrors newSyncCmd's pattern.
			if full {
				for _, resource := range resources {
					_ = s.SaveSyncState(resource, "", 0)
				}
			}

			for _, resource := range resources {
				res := syncResource(c, s, resource, "", full, 100, false, nil)
				if res.Err != nil {
					fmt.Fprintf(cmd.ErrOrStderr(), "  %s: error: %v\n", resource, res.Err)
					continue
				}
				if res.Warn != nil {
					fmt.Fprintf(cmd.ErrOrStderr(), "  %s: warning: %v\n", resource, res.Warn)
					continue
				}
				totalSynced += res.Count
				fmt.Fprintf(cmd.ErrOrStderr(), "  %s: %d synced\n", resource, res.Count)
			}

			if flags.asJSON {
				enc := json.NewEncoder(cmd.OutOrStdout())
				enc.SetIndent("", "  ")
				return enc.Encode(map[string]any{
					"resources_synced": len(resources),
					"total_items":      totalSynced,
					"store_path":       dbPath,
					"timestamp":        time.Now().UTC().Format(time.RFC3339),
				})
			}

			fmt.Fprintf(cmd.OutOrStdout(), "Archived %d items across %d resources to %s\n", totalSynced, len(resources), dbPath)
			return nil
		},
	}

	cmd.Flags().StringVar(&dbPath, "db", "", "Database path (default: ~/.local/share/ercot-pp-cli/data.db)")
	cmd.Flags().BoolVar(&full, "full", false, "Full re-archive (ignore previous sync state)")

	return cmd
}

func newWorkflowStatusCmd(flags *rootFlags) *cobra.Command {
	var dbPath string

	cmd := &cobra.Command{
		Use:         "status",
		Short:       "Show local archive status and sync state for all resources",
		Annotations: map[string]string{"mcp:read-only": "true"},
		Example: `  # Show archive status
  ercot-pp-cli workflow status

  # Show status as JSON
  ercot-pp-cli workflow status --json`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if dbPath == "" {
				dbPath = defaultDBPath("ercot-pp-cli")
			}
			s, err := store.OpenWithContext(cmd.Context(), dbPath)
			if err != nil {
				return fmt.Errorf("opening store: %w", err)
			}
			defer s.Close()

			status, err := s.Status()
			if err != nil {
				return err
			}

			if flags.asJSON {
				enc := json.NewEncoder(cmd.OutOrStdout())
				enc.SetIndent("", "  ")
				return enc.Encode(status)
			}

			if len(status) == 0 {
				fmt.Fprintln(cmd.OutOrStdout(), "No archived data. Run 'workflow archive' to sync.")
				return nil
			}

			fmt.Fprintln(cmd.OutOrStdout(), "Archive Status:")
			total := 0
			for resource, count := range status {
				fmt.Fprintf(cmd.OutOrStdout(), "  %-30s %d items\n", resource, count)
				total += count
			}
			fmt.Fprintf(cmd.OutOrStdout(), "\n  Total: %d items\n", total)
			fmt.Fprintf(cmd.OutOrStdout(), "  Store: %s\n", dbPath)
			return nil
		},
	}

	cmd.Flags().StringVar(&dbPath, "db", "", "Database path")

	return cmd
}

// defaultDBPath is defined in helpers.go
