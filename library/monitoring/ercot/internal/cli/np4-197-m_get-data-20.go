// Copyright 2026 synota-inc. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newNp4197MGetData20Cmd(flags *rootFlags) *cobra.Command {
	var flagDeliveryDateFrom string
	var flagDeliveryDateTo string
	var flagDeliveryHourFrom int
	var flagDeliveryHourTo int
	var flagDeliveryIntervalFrom int
	var flagDeliveryIntervalTo int
	var flagResourceType string
	var flagResourceName string
	var flagMeterName string
	var flagPriceOriginalFrom float64
	var flagPriceOriginalTo float64
	var flagPriceCorrectedFrom float64
	var flagPriceCorrectedTo float64
	var flagPriceCorrectionTimeFrom string
	var flagPriceCorrectionTimeTo string
	var flagDSTFlag bool
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-data-20",
		Short:       "RTM Price Corrections for SOG Price",
		Example:     "  ercot-pp-cli np4-197-m get-data-20",
		Annotations: map[string]string{"pp:endpoint": "np4-197-m.get-data-20", "pp:method": "GET", "pp:path": "/np4-197-m/rtm_price_corrections_sogprice", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/np4-197-m/rtm_price_corrections_sogprice"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "np4-197-m", path, map[string]string{
				"deliveryDateFrom":        fmt.Sprintf("%v", flagDeliveryDateFrom),
				"deliveryDateTo":          fmt.Sprintf("%v", flagDeliveryDateTo),
				"deliveryHourFrom":        fmt.Sprintf("%v", flagDeliveryHourFrom),
				"deliveryHourTo":          fmt.Sprintf("%v", flagDeliveryHourTo),
				"deliveryIntervalFrom":    fmt.Sprintf("%v", flagDeliveryIntervalFrom),
				"deliveryIntervalTo":      fmt.Sprintf("%v", flagDeliveryIntervalTo),
				"resourceType":            fmt.Sprintf("%v", flagResourceType),
				"resourceName":            fmt.Sprintf("%v", flagResourceName),
				"meterName":               fmt.Sprintf("%v", flagMeterName),
				"priceOriginalFrom":       fmt.Sprintf("%v", flagPriceOriginalFrom),
				"priceOriginalTo":         fmt.Sprintf("%v", flagPriceOriginalTo),
				"priceCorrectedFrom":      fmt.Sprintf("%v", flagPriceCorrectedFrom),
				"priceCorrectedTo":        fmt.Sprintf("%v", flagPriceCorrectedTo),
				"priceCorrectionTimeFrom": fmt.Sprintf("%v", flagPriceCorrectionTimeFrom),
				"priceCorrectionTimeTo":   fmt.Sprintf("%v", flagPriceCorrectionTimeTo),
				"DSTFlag":                 fmt.Sprintf("%v", flagDSTFlag),
			}, nil, flagAll, "page", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDeliveryDateFrom, "delivery-date-from", "", "Format - yyyy-MM-dd.")
	cmd.Flags().StringVar(&flagDeliveryDateTo, "delivery-date-to", "", "Format - yyyy-MM-dd.")
	cmd.Flags().IntVar(&flagDeliveryHourFrom, "delivery-hour-from", 0, "Format - ###.")
	cmd.Flags().IntVar(&flagDeliveryHourTo, "delivery-hour-to", 0, "Format - ###.")
	cmd.Flags().IntVar(&flagDeliveryIntervalFrom, "delivery-interval-from", 0, "Format - ###.")
	cmd.Flags().IntVar(&flagDeliveryIntervalTo, "delivery-interval-to", 0, "Format - ###.")
	cmd.Flags().StringVar(&flagResourceType, "resource-type", "", "Format - abc123.")
	cmd.Flags().StringVar(&flagResourceName, "resource-name", "", "Format - abc123.")
	cmd.Flags().StringVar(&flagMeterName, "meter-name", "", "Format - abc123.")
	cmd.Flags().Float64Var(&flagPriceOriginalFrom, "price-original-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagPriceOriginalTo, "price-original-to", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagPriceCorrectedFrom, "price-corrected-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagPriceCorrectedTo, "price-corrected-to", 0.0, "Format - ####.###.")
	cmd.Flags().StringVar(&flagPriceCorrectionTimeFrom, "price-correction-time-from", "", "Format - yyyy-MM-ddTH24:mm:ss.")
	cmd.Flags().StringVar(&flagPriceCorrectionTimeTo, "price-correction-time-to", "", "Format - yyyy-MM-ddTH24:mm:ss.")
	cmd.Flags().BoolVar(&flagDSTFlag, "dst-flag", false, "Format - true | false.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
