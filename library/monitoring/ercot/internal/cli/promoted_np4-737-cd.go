// Copyright 2026 synota-inc. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newNp4737CdPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagSTPPFSystemWideFrom float64
	var flagSTPPFSystemWideTo float64
	var flagPVGRPPSystemWideFrom float64
	var flagPVGRPPSystemWideTo float64
	var flagDSTFlag bool
	var flagDeliveryDateFrom string
	var flagDeliveryDateTo string
	var flagHourEndingFrom int
	var flagHourEndingTo int
	var flagActualSystemWideFrom float64
	var flagActualSystemWideTo float64
	var flagCOPHSLSystemWideFrom float64
	var flagCOPHSLSystemWideTo float64
	var flagPostedDatetimeFrom string
	var flagPostedDatetimeTo string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "np4-737-cd",
		Short:       "Solar Power Production - Hourly Averaged Actual and Forecasted Values",
		Long:        "Shortcut for 'np4-737-cd get-data-13'. Solar Power Production - Hourly Averaged Actual and Forecasted Values",
		Example:     "  ercot-pp-cli np4-737-cd",
		Annotations: map[string]string{"pp:endpoint": "np4-737-cd.get-data-13", "pp:method": "GET", "pp:path": "/np4-737-cd/spp_hrly_avrg_actl_fcast", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/np4-737-cd/spp_hrly_avrg_actl_fcast"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "np4-737-cd", path, map[string]string{
				"STPPFSystemWideFrom":  fmt.Sprintf("%v", flagSTPPFSystemWideFrom),
				"STPPFSystemWideTo":    fmt.Sprintf("%v", flagSTPPFSystemWideTo),
				"PVGRPPSystemWideFrom": fmt.Sprintf("%v", flagPVGRPPSystemWideFrom),
				"PVGRPPSystemWideTo":   fmt.Sprintf("%v", flagPVGRPPSystemWideTo),
				"DSTFlag":              fmt.Sprintf("%v", flagDSTFlag),
				"deliveryDateFrom":     fmt.Sprintf("%v", flagDeliveryDateFrom),
				"deliveryDateTo":       fmt.Sprintf("%v", flagDeliveryDateTo),
				"hourEndingFrom":       fmt.Sprintf("%v", flagHourEndingFrom),
				"hourEndingTo":         fmt.Sprintf("%v", flagHourEndingTo),
				"actualSystemWideFrom": fmt.Sprintf("%v", flagActualSystemWideFrom),
				"actualSystemWideTo":   fmt.Sprintf("%v", flagActualSystemWideTo),
				"COPHSLSystemWideFrom": fmt.Sprintf("%v", flagCOPHSLSystemWideFrom),
				"COPHSLSystemWideTo":   fmt.Sprintf("%v", flagCOPHSLSystemWideTo),
				"postedDatetimeFrom":   fmt.Sprintf("%v", flagPostedDatetimeFrom),
				"postedDatetimeTo":     fmt.Sprintf("%v", flagPostedDatetimeTo),
			}, nil, flagAll, "page", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().Float64Var(&flagSTPPFSystemWideFrom, "stppf-system-wide-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagSTPPFSystemWideTo, "stppf-system-wide-to", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagPVGRPPSystemWideFrom, "pvgrpp-system-wide-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagPVGRPPSystemWideTo, "pvgrpp-system-wide-to", 0.0, "Format - ####.###.")
	cmd.Flags().BoolVar(&flagDSTFlag, "dst-flag", false, "Format - true | false.")
	cmd.Flags().StringVar(&flagDeliveryDateFrom, "delivery-date-from", "", "Format - yyyy-MM-dd.")
	cmd.Flags().StringVar(&flagDeliveryDateTo, "delivery-date-to", "", "Format - yyyy-MM-dd.")
	cmd.Flags().IntVar(&flagHourEndingFrom, "hour-ending-from", 0, "Format - ###.")
	cmd.Flags().IntVar(&flagHourEndingTo, "hour-ending-to", 0, "Format - ###.")
	cmd.Flags().Float64Var(&flagActualSystemWideFrom, "actual-system-wide-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagActualSystemWideTo, "actual-system-wide-to", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagCOPHSLSystemWideFrom, "cophsl-system-wide-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagCOPHSLSystemWideTo, "cophsl-system-wide-to", 0.0, "Format - ####.###.")
	cmd.Flags().StringVar(&flagPostedDatetimeFrom, "posted-datetime-from", "", "Format - yyyy-MM-ddTH24:mm:ss.")
	cmd.Flags().StringVar(&flagPostedDatetimeTo, "posted-datetime-to", "", "Format - yyyy-MM-ddTH24:mm:ss.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
