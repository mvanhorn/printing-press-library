// Copyright 2026 synota-inc. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newNp4197MGetData22Cmd(flags *rootFlags) *cobra.Command {
	var flagSCEDTimestampFrom string
	var flagSCEDTimestampTo string
	var flagConstraintIdFrom int
	var flagConstraintIdTo int
	var flagConstraintName string
	var flagContingencyName string
	var flagShadowPriceOriginalFrom float64
	var flagShadowPriceOriginalTo float64
	var flagShadowPriceCorrectedFrom float64
	var flagShadowPriceCorrectedTo float64
	var flagLimitOriginalFrom float64
	var flagLimitOriginalTo float64
	var flagLimitCorrectedFrom float64
	var flagLimitCorrectedTo float64
	var flagValueOriginalFrom float64
	var flagValueOriginalTo float64
	var flagValueCorrectedFrom float64
	var flagValueCorrectedTo float64
	var flagPriceCorrectionTimeFrom string
	var flagPriceCorrectionTimeTo string
	var flagDSTFlag bool
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-data-22",
		Short:       "RTM Price Corrections for Shadow Prices",
		Example:     "  ercot-pp-cli np4-197-m get-data-22",
		Annotations: map[string]string{"pp:endpoint": "np4-197-m.get-data-22", "pp:method": "GET", "pp:path": "/np4-197-m/rtm_price_corrections_shadow", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/np4-197-m/rtm_price_corrections_shadow"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "np4-197-m", path, map[string]string{
				"SCEDTimestampFrom":        fmt.Sprintf("%v", flagSCEDTimestampFrom),
				"SCEDTimestampTo":          fmt.Sprintf("%v", flagSCEDTimestampTo),
				"constraintIdFrom":         fmt.Sprintf("%v", flagConstraintIdFrom),
				"constraintIdTo":           fmt.Sprintf("%v", flagConstraintIdTo),
				"constraintName":           fmt.Sprintf("%v", flagConstraintName),
				"contingencyName":          fmt.Sprintf("%v", flagContingencyName),
				"shadowPriceOriginalFrom":  fmt.Sprintf("%v", flagShadowPriceOriginalFrom),
				"shadowPriceOriginalTo":    fmt.Sprintf("%v", flagShadowPriceOriginalTo),
				"shadowPriceCorrectedFrom": fmt.Sprintf("%v", flagShadowPriceCorrectedFrom),
				"shadowPriceCorrectedTo":   fmt.Sprintf("%v", flagShadowPriceCorrectedTo),
				"limitOriginalFrom":        fmt.Sprintf("%v", flagLimitOriginalFrom),
				"limitOriginalTo":          fmt.Sprintf("%v", flagLimitOriginalTo),
				"limitCorrectedFrom":       fmt.Sprintf("%v", flagLimitCorrectedFrom),
				"limitCorrectedTo":         fmt.Sprintf("%v", flagLimitCorrectedTo),
				"valueOriginalFrom":        fmt.Sprintf("%v", flagValueOriginalFrom),
				"valueOriginalTo":          fmt.Sprintf("%v", flagValueOriginalTo),
				"valueCorrectedFrom":       fmt.Sprintf("%v", flagValueCorrectedFrom),
				"valueCorrectedTo":         fmt.Sprintf("%v", flagValueCorrectedTo),
				"priceCorrectionTimeFrom":  fmt.Sprintf("%v", flagPriceCorrectionTimeFrom),
				"priceCorrectionTimeTo":    fmt.Sprintf("%v", flagPriceCorrectionTimeTo),
				"DSTFlag":                  fmt.Sprintf("%v", flagDSTFlag),
			}, nil, flagAll, "page", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagSCEDTimestampFrom, "sced-timestamp-from", "", "Format - yyyy-MM-ddTH24:mm:ss.")
	cmd.Flags().StringVar(&flagSCEDTimestampTo, "sced-timestamp-to", "", "Format - yyyy-MM-ddTH24:mm:ss.")
	cmd.Flags().IntVar(&flagConstraintIdFrom, "constraint-id-from", 0, "Format - ###.")
	cmd.Flags().IntVar(&flagConstraintIdTo, "constraint-id-to", 0, "Format - ###.")
	cmd.Flags().StringVar(&flagConstraintName, "constraint-name", "", "Format - abc123.")
	cmd.Flags().StringVar(&flagContingencyName, "contingency-name", "", "Format - abc123.")
	cmd.Flags().Float64Var(&flagShadowPriceOriginalFrom, "shadow-price-original-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagShadowPriceOriginalTo, "shadow-price-original-to", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagShadowPriceCorrectedFrom, "shadow-price-corrected-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagShadowPriceCorrectedTo, "shadow-price-corrected-to", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagLimitOriginalFrom, "limit-original-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagLimitOriginalTo, "limit-original-to", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagLimitCorrectedFrom, "limit-corrected-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagLimitCorrectedTo, "limit-corrected-to", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagValueOriginalFrom, "value-original-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagValueOriginalTo, "value-original-to", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagValueCorrectedFrom, "value-corrected-from", 0.0, "Format - ####.###.")
	cmd.Flags().Float64Var(&flagValueCorrectedTo, "value-corrected-to", 0.0, "Format - ####.###.")
	cmd.Flags().StringVar(&flagPriceCorrectionTimeFrom, "price-correction-time-from", "", "Format - yyyy-MM-ddTH24:mm:ss.")
	cmd.Flags().StringVar(&flagPriceCorrectionTimeTo, "price-correction-time-to", "", "Format - yyyy-MM-ddTH24:mm:ss.")
	cmd.Flags().BoolVar(&flagDSTFlag, "dst-flag", false, "Format - true | false.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
