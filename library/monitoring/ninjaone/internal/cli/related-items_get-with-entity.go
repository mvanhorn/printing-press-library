// Copyright 2026 "Chris Carson" and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newRelatedItemsGetWithEntityCmd(flags *rootFlags) *cobra.Command {
	var flagRelEntityType string

	cmd := &cobra.Command{
		Use:         "get-with-entity <relEntityId>",
		Short:       "List related items for a specific related entity",
		Example:     "  ninjaone-pp-cli related-items get-with-entity 42 --rel-entity-type ORGANIZATION",
		Annotations: map[string]string{"pp:endpoint": "related-items.get-with-entity", "pp:method": "GET", "pp:path": "/v2/related-items/with-related-entity/{relEntityType}/{relEntityId}", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if cmd.Flags().Changed("rel-entity-type") {
				allowedRelEntityType := []string{"ORGANIZATION", "DOCUMENT", "LOCATION", "NODE", "ATTACHMENT", "TECHNICIAN", "CREDENTIAL", "CHECKLIST", "END_USER", "CONTACT", "KB_DOCUMENT", "SOFTWARE_LICENSE"}
				validRelEntityType := false
				for _, v := range allowedRelEntityType {
					if flagRelEntityType == v {
						validRelEntityType = true
						break
					}
				}
				if !validRelEntityType {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagRelEntityType, "rel-entity-type", allowedRelEntityType)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/related-items/with-related-entity/{relEntityType}/{relEntityId}"
			path = replacePathParam(path, "relEntityId", args[0])
			path = replacePathParam(path, "relEntityType", fmt.Sprintf("%v", flagRelEntityType))
			params := map[string]string{}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "related-items", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagRelEntityType, "rel-entity-type", "ORGANIZATION", "Rel entity type (one of: ORGANIZATION, DOCUMENT, LOCATION, NODE, ATTACHMENT, TECHNICIAN, CREDENTIAL, CHECKLIST, END_USER, CONTACT, KB_DOCUMENT, SOFTWARE_LICENSE)")

	return cmd
}
