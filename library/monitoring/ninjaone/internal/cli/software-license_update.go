// Copyright 2026 "Chris Carson" and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newSoftwareLicenseUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAssigmentAutomationSettingsAssignmentType string
	var bodyAssigmentAutomationSettingsType string
	var bodyAssignedLicenses string
	var bodyCost float64
	var bodyCostMode string
	var bodyCurrentUsage int
	var bodyDescription string
	var bodyName string
	var bodyNote string
	var bodyNotificationChannelInfoEmail string
	var bodyNotificationChannelInfoPushNotification string
	var bodyNotificationChannelInfoSms string
	var bodyPublisherId string
	var bodyPurchaseDate int
	var bodyQuantity int
	var bodyScopeGlobal bool
	var bodyScopeLocationIds string
	var bodyScopeOrganizationIds string
	var bodyTermAutoRenewal bool
	var bodyTermDaysBeforeExpiration int
	var bodyTermExpirationDate float64
	var bodyTermGenerateActivityAlert bool
	var bodyTermHasNotifiedExpirationDate bool
	var bodyTermHasNotifiedUpToRenewal bool
	var bodyTermRenewalUnit string
	var bodyTermValue int
	var bodyType string
	var bodyUnassignedLicenses string
	var bodyVendorId string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update <licenseId>",
		Short:       "Update a Software License with the provided metadata",
		Example:     "  ninjaone-pp-cli software-license update 42 --description example-value",
		Annotations: map[string]string{"pp:endpoint": "software-license.update", "pp:method": "PUT", "pp:path": "/v2/software-license/{licenseId}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("assigment-automation-settings-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "assigment-automation-settings-type")
				}
				if !cmd.Flags().Changed("current-usage") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "current-usage")
				}
				if !cmd.Flags().Changed("description") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "description")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("publisher-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "publisher-id")
				}
				if !cmd.Flags().Changed("purchase-date") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "purchase-date")
				}
				if !cmd.Flags().Changed("quantity") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "quantity")
				}
				if !cmd.Flags().Changed("type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "type")
				}
				if !cmd.Flags().Changed("vendor-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "vendor-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/software-license/{licenseId}"
			path = replacePathParam(path, "licenseId", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedAssigmentAutomationSettings := map[string]any{}
					if bodyAssigmentAutomationSettingsAssignmentType != "" {
						nestedAssigmentAutomationSettings["assignmentType"] = bodyAssigmentAutomationSettingsAssignmentType
					}
					if bodyAssigmentAutomationSettingsType != "" {
						nestedAssigmentAutomationSettings["type"] = bodyAssigmentAutomationSettingsType
					}
					if len(nestedAssigmentAutomationSettings) > 0 {
						body["assigmentAutomationSettings"] = nestedAssigmentAutomationSettings
					}
				}
				if bodyAssignedLicenses != "" {
					var parsedAssignedLicenses any
					if err := json.Unmarshal([]byte(bodyAssignedLicenses), &parsedAssignedLicenses); err != nil {
						return fmt.Errorf("parsing --assigned-licenses JSON: %w", err)
					}
					body["assignedLicenses"] = parsedAssignedLicenses
				}
				if bodyCost != 0.0 {
					body["cost"] = bodyCost
				}
				if bodyCostMode != "" {
					body["costMode"] = bodyCostMode
				}
				if bodyCurrentUsage != 0 {
					body["currentUsage"] = bodyCurrentUsage
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyNote != "" {
					body["note"] = bodyNote
				}
				{
					nestedNotificationChannelInfo := map[string]any{}
					if bodyNotificationChannelInfoEmail != "" {
						nestedNotificationChannelInfo["email"] = bodyNotificationChannelInfoEmail
					}
					if bodyNotificationChannelInfoPushNotification != "" {
						nestedNotificationChannelInfo["pushNotification"] = bodyNotificationChannelInfoPushNotification
					}
					if bodyNotificationChannelInfoSms != "" {
						nestedNotificationChannelInfo["sms"] = bodyNotificationChannelInfoSms
					}
					if len(nestedNotificationChannelInfo) > 0 {
						body["notificationChannelInfo"] = nestedNotificationChannelInfo
					}
				}
				if bodyPublisherId != "" {
					body["publisherId"] = bodyPublisherId
				}
				if bodyPurchaseDate != 0 {
					body["purchaseDate"] = bodyPurchaseDate
				}
				if bodyQuantity != 0 {
					body["quantity"] = bodyQuantity
				}
				{
					nestedScope := map[string]any{}
					if cmd.Flags().Changed("scope-global") {
						nestedScope["global"] = bodyScopeGlobal
					}
					if bodyScopeLocationIds != "" {
						var parsedScopeLocationIds any
						if err := json.Unmarshal([]byte(bodyScopeLocationIds), &parsedScopeLocationIds); err != nil {
							return fmt.Errorf("parsing --scope-location-ids JSON: %w", err)
						}
						nestedScope["locationIds"] = parsedScopeLocationIds
					}
					if bodyScopeOrganizationIds != "" {
						var parsedScopeOrganizationIds any
						if err := json.Unmarshal([]byte(bodyScopeOrganizationIds), &parsedScopeOrganizationIds); err != nil {
							return fmt.Errorf("parsing --scope-organization-ids JSON: %w", err)
						}
						nestedScope["organizationIds"] = parsedScopeOrganizationIds
					}
					if len(nestedScope) > 0 {
						body["scope"] = nestedScope
					}
				}
				{
					nestedTerm := map[string]any{}
					if cmd.Flags().Changed("term-auto-renewal") {
						nestedTerm["autoRenewal"] = bodyTermAutoRenewal
					}
					if bodyTermDaysBeforeExpiration != 0 {
						nestedTerm["daysBeforeExpiration"] = bodyTermDaysBeforeExpiration
					}
					if bodyTermExpirationDate != 0.0 {
						nestedTerm["expirationDate"] = bodyTermExpirationDate
					}
					if cmd.Flags().Changed("term-generate-activity-alert") {
						nestedTerm["generateActivityAlert"] = bodyTermGenerateActivityAlert
					}
					if cmd.Flags().Changed("term-has-notified-expiration-date") {
						nestedTerm["hasNotifiedExpirationDate"] = bodyTermHasNotifiedExpirationDate
					}
					if cmd.Flags().Changed("term-has-notified-up-to-renewal") {
						nestedTerm["hasNotifiedUpToRenewal"] = bodyTermHasNotifiedUpToRenewal
					}
					if bodyTermRenewalUnit != "" {
						nestedTerm["renewalUnit"] = bodyTermRenewalUnit
					}
					if bodyTermValue != 0 {
						nestedTerm["value"] = bodyTermValue
					}
					if len(nestedTerm) > 0 {
						body["term"] = nestedTerm
					}
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
				if bodyUnassignedLicenses != "" {
					var parsedUnassignedLicenses any
					if err := json.Unmarshal([]byte(bodyUnassignedLicenses), &parsedUnassignedLicenses); err != nil {
						return fmt.Errorf("parsing --unassigned-licenses JSON: %w", err)
					}
					body["unassignedLicenses"] = parsedUnassignedLicenses
				}
				if bodyVendorId != "" {
					body["vendorId"] = bodyVendorId
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "software-license", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "software-license", data, "licenseAssignmentList")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "software-license", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "software-license", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "software-license", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "software-license",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "software-license", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "software-license", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAssigmentAutomationSettingsAssignmentType, "assigment-automation-settings-assignment-type", "", "Assignment type")
	cmd.Flags().StringVar(&bodyAssigmentAutomationSettingsType, "assigment-automation-settings-type", "", "Type")
	cmd.Flags().StringVar(&bodyAssignedLicenses, "assigned-licenses", "", "Indicates the ids of the assigned licensees")
	cmd.Flags().Float64Var(&bodyCost, "cost", 0.0, "Indicates the cost of the software license")
	cmd.Flags().StringVar(&bodyCostMode, "cost-mode", "", "Indicates the cost mode of the software license")
	cmd.Flags().IntVar(&bodyCurrentUsage, "current-usage", 0, "Indicates the current usage of the software license")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Indicates a description for the software license")
	cmd.Flags().StringVar(&bodyName, "name", "", "Indicates of the name of the software license")
	cmd.Flags().StringVar(&bodyNote, "note", "", "Indicates a note about the software license")
	cmd.Flags().StringVar(&bodyNotificationChannelInfoEmail, "notification-channel-info-email", "", "Email")
	cmd.Flags().StringVar(&bodyNotificationChannelInfoPushNotification, "notification-channel-info-push-notification", "", "Push notification")
	cmd.Flags().StringVar(&bodyNotificationChannelInfoSms, "notification-channel-info-sms", "", "Sms")
	cmd.Flags().StringVar(&bodyPublisherId, "publisher-id", "", "Identifier of the publisher")
	cmd.Flags().IntVar(&bodyPurchaseDate, "purchase-date", 0, "Indicates the purchase date of the software license in unix epoch milliseconds")
	cmd.Flags().IntVar(&bodyQuantity, "quantity", 0, "Indicates the quantity of the software license")
	cmd.Flags().BoolVar(&bodyScopeGlobal, "scope-global", false, "Global")
	cmd.Flags().StringVar(&bodyScopeLocationIds, "scope-location-ids", "", "Location ids")
	cmd.Flags().StringVar(&bodyScopeOrganizationIds, "scope-organization-ids", "", "Organization ids")
	cmd.Flags().BoolVar(&bodyTermAutoRenewal, "term-auto-renewal", false, "Auto renewal")
	cmd.Flags().IntVar(&bodyTermDaysBeforeExpiration, "term-days-before-expiration", 0, "Days before expiration")
	cmd.Flags().Float64Var(&bodyTermExpirationDate, "term-expiration-date", 0.0, "Expiration date")
	cmd.Flags().BoolVar(&bodyTermGenerateActivityAlert, "term-generate-activity-alert", false, "Generate activity alert")
	cmd.Flags().BoolVar(&bodyTermHasNotifiedExpirationDate, "term-has-notified-expiration-date", false, "Has notified expiration date")
	cmd.Flags().BoolVar(&bodyTermHasNotifiedUpToRenewal, "term-has-notified-up-to-renewal", false, "Has notified up to renewal")
	cmd.Flags().StringVar(&bodyTermRenewalUnit, "term-renewal-unit", "", "Renewal unit")
	cmd.Flags().IntVar(&bodyTermValue, "term-value", 0, "Value")
	cmd.Flags().StringVar(&bodyType, "type", "", "Indicates the type of the software license")
	cmd.Flags().StringVar(&bodyUnassignedLicenses, "unassigned-licenses", "", "Indicates the ids of the unassigned licensees")
	cmd.Flags().StringVar(&bodyVendorId, "vendor-id", "", "Identifier of the vendor")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
