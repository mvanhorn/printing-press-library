// Copyright 2026 "Chris Carson" and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"
	"strconv"

	"github.com/mvanhorn/printing-press-library/library/monitoring/ninjaone/internal/cliutil"
	"github.com/spf13/cobra"
)

func newTicketingCreate2Cmd(flags *rootFlags) *cobra.Command {
	var bodyAdditionalAssignedTechnicianIds string
	var bodyAssignedAppUserId string
	var bodyAttributes string
	var bodyCcEmails string
	var bodyCcUids string
	var bodyClientId string
	var bodyDescriptionBody string
	var bodyDescriptionDuplicateInIncidents bool
	var bodyDescriptionHtmlBody string
	var bodyDescriptionPublic string
	var bodyDescriptionTimeTracked int
	var bodyFollowupTime float64
	var bodyLocationId string
	var bodyNodeId string
	var bodyParentTicketId string
	var bodyPriority string
	var bodyRequesterUid string
	var bodySeverity string
	var bodyStatus string
	var bodySubject string
	var bodyTags string
	var bodyTicketFormId string
	var bodyType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-2",
		Aliases:     []string{"create"},
		Short:       "Create a new ticket, does not accept files",
		Example:     "  ninjaone-pp-cli ticketing create-2 --status example-value",
		Annotations: map[string]string{"pp:endpoint": "ticketing.create-2", "pp:method": "POST", "pp:path": "/v2/ticketing/ticket"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("client-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "client-id")
				}
				if !cmd.Flags().Changed("description-public") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "description-public")
				}
				if !cmd.Flags().Changed("subject") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "subject")
				}
				if !cmd.Flags().Changed("ticket-form-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "ticket-form-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/ticketing/ticket"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAdditionalAssignedTechnicianIds != "" {
					var parsedAdditionalAssignedTechnicianIds any
					if err := json.Unmarshal([]byte(bodyAdditionalAssignedTechnicianIds), &parsedAdditionalAssignedTechnicianIds); err != nil {
						return fmt.Errorf("parsing --additional-assigned-technician-ids JSON: %w", err)
					}
					body["additionalAssignedTechnicianIds"] = parsedAdditionalAssignedTechnicianIds
				}
				if bodyAssignedAppUserId != "" {
					body["assignedAppUserId"] = bodyAssignedAppUserId
				}
				if bodyAttributes != "" {
					var parsedAttributes any
					if err := json.Unmarshal([]byte(bodyAttributes), &parsedAttributes); err != nil {
						return fmt.Errorf("parsing --attributes JSON: %w", err)
					}
					body["attributes"] = parsedAttributes
				}
				{
					nestedCc := map[string]any{}
					if bodyCcEmails != "" {
						nestedCc["emails"] = cliutil.SplitCSV(bodyCcEmails)
					}
					if bodyCcUids != "" {
						nestedCc["uids"] = cliutil.SplitCSV(bodyCcUids)
					}
					if len(nestedCc) > 0 {
						body["cc"] = nestedCc
					}
				}
				if bodyClientId != "" {
					body["clientId"] = bodyClientId
				}
				{
					nestedDescription := map[string]any{}
					if bodyDescriptionBody != "" {
						nestedDescription["body"] = bodyDescriptionBody
					}
					if cmd.Flags().Changed("description-duplicate-in-incidents") {
						nestedDescription["duplicateInIncidents"] = bodyDescriptionDuplicateInIncidents
					}
					if bodyDescriptionHtmlBody != "" {
						nestedDescription["htmlBody"] = bodyDescriptionHtmlBody
					}
					if bodyDescriptionPublic != "" {
						parsedDescriptionPublic, err := strconv.ParseBool(bodyDescriptionPublic)
						if err != nil {
							return fmt.Errorf("parsing --description-public as bool: %w", err)
						}
						nestedDescription["public"] = parsedDescriptionPublic
					}
					if bodyDescriptionTimeTracked != 0 {
						nestedDescription["timeTracked"] = bodyDescriptionTimeTracked
					}
					if len(nestedDescription) > 0 {
						body["description"] = nestedDescription
					}
				}
				if bodyFollowupTime != 0.0 {
					body["followupTime"] = bodyFollowupTime
				}
				if bodyLocationId != "" {
					body["locationId"] = bodyLocationId
				}
				if bodyNodeId != "" {
					body["nodeId"] = bodyNodeId
				}
				if bodyParentTicketId != "" {
					body["parentTicketId"] = bodyParentTicketId
				}
				if bodyPriority != "" {
					body["priority"] = bodyPriority
				}
				if bodyRequesterUid != "" {
					body["requesterUid"] = bodyRequesterUid
				}
				if bodySeverity != "" {
					body["severity"] = bodySeverity
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodySubject != "" {
					body["subject"] = bodySubject
				}
				if bodyTags != "" {
					body["tags"] = cliutil.SplitCSV(bodyTags)
				}
				if bodyTicketFormId != "" {
					body["ticketFormId"] = bodyTicketFormId
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "ticketing", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "ticketing", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ticketing", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ticketing", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ticketing", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "ticketing",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ticketing", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "ticketing", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAdditionalAssignedTechnicianIds, "additional-assigned-technician-ids", "", "Additional assigned technician identifiers")
	cmd.Flags().StringVar(&bodyAssignedAppUserId, "assigned-app-user-id", "", "Assigned app user id")
	cmd.Flags().StringVar(&bodyAttributes, "attributes", "", "Attributes")
	cmd.Flags().StringVar(&bodyCcEmails, "cc-emails", "", "emails")
	cmd.Flags().StringVar(&bodyCcUids, "cc-uids", "", "List of user uids to be carbon-copied")
	cmd.Flags().StringVar(&bodyClientId, "client-id", "", "Client (Organization) identifier")
	cmd.Flags().StringVar(&bodyDescriptionBody, "description-body", "", "Body")
	cmd.Flags().BoolVar(&bodyDescriptionDuplicateInIncidents, "description-duplicate-in-incidents", false, "Duplicate in incidents")
	cmd.Flags().StringVar(&bodyDescriptionHtmlBody, "description-html-body", "", "Html body")
	cmd.Flags().StringVar(&bodyDescriptionPublic, "description-public", "", "Public")
	cmd.Flags().IntVar(&bodyDescriptionTimeTracked, "description-time-tracked", 0, "time in seconds")
	cmd.Flags().Float64Var(&bodyFollowupTime, "followup-time", 0.0, "Follow-up time for the ticket in epoch seconds")
	cmd.Flags().StringVar(&bodyLocationId, "location-id", "", "Location identifier")
	cmd.Flags().StringVar(&bodyNodeId, "node-id", "", "Device identifier")
	cmd.Flags().StringVar(&bodyParentTicketId, "parent-ticket-id", "", "Ticket parent identifier")
	cmd.Flags().StringVar(&bodyPriority, "priority", "NONE", "Priority")
	cmd.Flags().StringVar(&bodyRequesterUid, "requester-uid", "", "Requester uid")
	cmd.Flags().StringVar(&bodySeverity, "severity", "NONE", "Severity")
	cmd.Flags().StringVar(&bodyStatus, "status", "1000", "Status")
	cmd.Flags().StringVar(&bodySubject, "subject", "", "Subject")
	cmd.Flags().StringVar(&bodyTags, "tags", "", "Tags")
	cmd.Flags().StringVar(&bodyTicketFormId, "ticket-form-id", "", "Ticket form identifier")
	cmd.Flags().StringVar(&bodyType, "type", "", "Type of ticket")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
