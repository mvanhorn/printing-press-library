// Copyright 2026 "Chris Carson" and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newItamCreateUnmanagedDevicePublicApiCmd(flags *rootFlags) *cobra.Command {
	var bodyAssetFieldsAssetExpectedLifetime string
	var bodyAssetFieldsAssetId string
	var bodyAssetFieldsAssetPurchaseAmount float64
	var bodyAssetFieldsAssetPurchaseDate float64
	var bodyAssetFieldsAssetStatus string
	var bodyAssetFieldsSerialNumber string
	var bodyAssignedUserUid string
	var bodyLocationId string
	var bodyName string
	var bodyOrgId string
	var bodyRoleId string
	var bodyWarrantyEndDate float64
	var bodyWarrantyStartDate float64
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-unmanaged-device-public-api",
		Short:       "Create an Unmanaged Device with the provided details",
		Example:     "  ninjaone-pp-cli itam create-unmanaged-device-public-api --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "itam.create-unmanaged-device-public-api", "pp:method": "POST", "pp:path": "/v2/itam/unmanaged-device"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("location-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "location-id")
				}
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("org-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "org-id")
				}
				if !cmd.Flags().Changed("role-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "role-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/itam/unmanaged-device"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedAssetFields := map[string]any{}
					if bodyAssetFieldsAssetExpectedLifetime != "" {
						nestedAssetFields["assetExpectedLifetime"] = bodyAssetFieldsAssetExpectedLifetime
					}
					if bodyAssetFieldsAssetId != "" {
						nestedAssetFields["assetId"] = bodyAssetFieldsAssetId
					}
					if bodyAssetFieldsAssetPurchaseAmount != 0.0 {
						nestedAssetFields["assetPurchaseAmount"] = bodyAssetFieldsAssetPurchaseAmount
					}
					if bodyAssetFieldsAssetPurchaseDate != 0.0 {
						nestedAssetFields["assetPurchaseDate"] = bodyAssetFieldsAssetPurchaseDate
					}
					if bodyAssetFieldsAssetStatus != "" {
						nestedAssetFields["assetStatus"] = bodyAssetFieldsAssetStatus
					}
					if bodyAssetFieldsSerialNumber != "" {
						nestedAssetFields["serialNumber"] = bodyAssetFieldsSerialNumber
					}
					if len(nestedAssetFields) > 0 {
						body["assetFields"] = nestedAssetFields
					}
				}
				if bodyAssignedUserUid != "" {
					body["assignedUserUid"] = bodyAssignedUserUid
				}
				if bodyLocationId != "" {
					body["locationId"] = bodyLocationId
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyOrgId != "" {
					body["orgId"] = bodyOrgId
				}
				if bodyRoleId != "" {
					body["roleId"] = bodyRoleId
				}
				if bodyWarrantyEndDate != 0.0 {
					body["warrantyEndDate"] = bodyWarrantyEndDate
				}
				if bodyWarrantyStartDate != 0.0 {
					body["warrantyStartDate"] = bodyWarrantyStartDate
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "itam", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "itam", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "itam", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "itam", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "itam", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "itam",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "itam", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "itam", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAssetFieldsAssetExpectedLifetime, "asset-fields-asset-expected-lifetime", "", "The expected lifetime of the new device")
	cmd.Flags().StringVar(&bodyAssetFieldsAssetId, "asset-fields-asset-id", "", "The asset id to use for the new device")
	cmd.Flags().Float64Var(&bodyAssetFieldsAssetPurchaseAmount, "asset-fields-asset-purchase-amount", 0.0, "The purchase amount of the new device")
	cmd.Flags().Float64Var(&bodyAssetFieldsAssetPurchaseDate, "asset-fields-asset-purchase-date", 0.0, "The purchase date of the new device")
	cmd.Flags().StringVar(&bodyAssetFieldsAssetStatus, "asset-fields-asset-status", "", "The asset status to use for the new device")
	cmd.Flags().StringVar(&bodyAssetFieldsSerialNumber, "asset-fields-serial-number", "", "The SerialNumber to use for the new device")
	cmd.Flags().StringVar(&bodyAssignedUserUid, "assigned-user-uid", "", "The UUID of the user this device is assigned to")
	cmd.Flags().StringVar(&bodyLocationId, "location-id", "", "The id of the location the device belongs to")
	cmd.Flags().StringVar(&bodyName, "name", "", "The display name of the device to be created")
	cmd.Flags().StringVar(&bodyOrgId, "org-id", "", "The id of the organization the device belongs to")
	cmd.Flags().StringVar(&bodyRoleId, "role-id", "", "The id of the role to use for this device. Must be a sub-role of Unmanaged Device")
	cmd.Flags().Float64Var(&bodyWarrantyEndDate, "warranty-end-date", 0.0, "The warranty end date for the device, in unix epoch seconds")
	cmd.Flags().Float64Var(&bodyWarrantyStartDate, "warranty-start-date", 0.0, "The warranty start date for the device, in unix epoch seconds")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
