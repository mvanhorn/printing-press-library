// Copyright 2026 laci141 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"io"
	"testing"
)

// TestNovelSafetyHelpWires smoke-tests that the safety command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelSafetyHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"safety", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("safety --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelSafetyBehavior is the placeholder for table-driven tests of
// the safety command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelSafetyBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
func TestNovelSafetyBehavior(t *testing.T) {
	cases := []struct {
		name string
		v    safetyView
		want string
	}{
		{"prefers normalized name", safetyView{Query: "Keytruda", NormalizedName: "pembrolizumab"}, "pembrolizumab"},
		{"falls back to query", safetyView{Query: "MadeUpDrug"}, "MadeUpDrug"},
	}
	for _, tc := range cases {
		tc := tc
		t.Run(tc.name, func(t *testing.T) {
			t.Parallel()
			if got := compareSafetyLabel(tc.v); got != tc.want {
				t.Fatalf("compareSafetyLabel(%+v) = %q, want %q", tc.v, got, tc.want)
			}
		})
	}
}
