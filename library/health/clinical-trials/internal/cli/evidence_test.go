// Copyright 2026 laci141 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"io"
	"testing"
)

// TestNovelEvidenceHelpWires smoke-tests that the evidence command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelEvidenceHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"evidence", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("evidence --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelEvidenceBehavior is the placeholder for table-driven tests of
// the evidence command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelEvidenceBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
func TestNovelEvidenceBehavior(t *testing.T) {
	cases := []struct {
		in   string
		want bool
	}{
		{"NCT04280705", true},
		{"nct04280705", true},
		{"  NCT123 ", true},
		{"long covid", false},
		{"", false},
	}
	for _, tc := range cases {
		tc := tc
		t.Run(tc.in, func(t *testing.T) {
			t.Parallel()
			if got := looksLikeNCT(tc.in); got != tc.want {
				t.Fatalf("looksLikeNCT(%q) = %v, want %v", tc.in, got, tc.want)
			}
		})
	}
}
