// Copyright 2026 laci141 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"io"
	"testing"
)

// TestNovelSponsorsHelpWires smoke-tests that the sponsors command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelSponsorsHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"sponsors", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("sponsors --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelSponsorsBehavior is the placeholder for table-driven tests of
// the sponsors command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelSponsorsBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
func TestNovelSponsorsBehavior(t *testing.T) {
	cases := []struct {
		class string
		want  string
	}{
		{"INDUSTRY", "industry"},
		{"industry", "industry"},
		{"NIH", "government"},
		{"FED", "government"},
		{"OTHER_GOV", "government"},
		{"OTHER", "academic/other"},
		{"NETWORK", "academic/other"},
		{"INDIV", "academic/other"},
		{"", "unknown"},
		{"AMBIG", "unknown"},
	}
	for _, tc := range cases {
		tc := tc
		t.Run(tc.class, func(t *testing.T) {
			t.Parallel()
			if got := sponsorClassLabel(tc.class); got != tc.want {
				t.Fatalf("sponsorClassLabel(%q) = %q, want %q", tc.class, got, tc.want)
			}
		})
	}
}
