// Copyright 2026 Som Samantray and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTextToSpeechTextToSpeechCmd(flags *rootFlags) *cobra.Command {
	var bodyDictId string
	var bodyEnableCachedResponses bool
	var bodyEnablePreprocessing bool
	var bodyLanguageCode string
	var bodyLoudness float64
	var bodyModel string
	var bodyOutputAudioCodec string
	var bodyPace float64
	var bodyPitch float64
	var bodySpeaker string
	var bodySpeechSampleRate int
	var bodyTemperature float64
	var bodyText string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "text-to-speech",
		Short:       "Converts text into spoken audio.",
		Example:     "  sarvam-pp-cli text-to-speech text-to-speech --language-code bn-IN",
		Annotations: map[string]string{"pp:endpoint": "text-to-speech.text_to_speech", "pp:method": "POST", "pp:path": "/text-to-speech"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("language-code") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "language-code")
				}
				if !cmd.Flags().Changed("text") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "text")
				}
			}
			path := "/text-to-speech"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("dict-id") || bodyDictId != "" {
					bodyMap["dict_id"] = bodyDictId
				}
				if cmd.Flags().Changed("enable-cached-responses") {
					bodyMap["enable_cached_responses"] = bodyEnableCachedResponses
				}
				if cmd.Flags().Changed("enable-preprocessing") {
					bodyMap["enable_preprocessing"] = bodyEnablePreprocessing
				}
				if cmd.Flags().Changed("language-code") || bodyLanguageCode != "" {
					bodyMap["language_code"] = bodyLanguageCode
				}
				if cmd.Flags().Changed("loudness") || bodyLoudness != 0.0 {
					bodyMap["loudness"] = bodyLoudness
				}
				if cmd.Flags().Changed("model") || bodyModel != "" {
					bodyMap["model"] = bodyModel
				}
				if cmd.Flags().Changed("output-audio-codec") || bodyOutputAudioCodec != "" {
					bodyMap["output_audio_codec"] = bodyOutputAudioCodec
				}
				if cmd.Flags().Changed("pace") || bodyPace != 0.0 {
					bodyMap["pace"] = bodyPace
				}
				if cmd.Flags().Changed("pitch") || bodyPitch != 0.0 {
					bodyMap["pitch"] = bodyPitch
				}
				if cmd.Flags().Changed("speaker") || bodySpeaker != "" {
					bodyMap["speaker"] = bodySpeaker
				}
				if cmd.Flags().Changed("speech-sample-rate") || bodySpeechSampleRate != 0 {
					bodyMap["speech_sample_rate"] = bodySpeechSampleRate
				}
				if cmd.Flags().Changed("temperature") || bodyTemperature != 0.0 {
					bodyMap["temperature"] = bodyTemperature
				}
				if cmd.Flags().Changed("text") || bodyText != "" {
					bodyMap["text"] = bodyText
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "text-to-speech", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "text-to-speech", data, "audios")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "text-to-speech", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "text-to-speech", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "text-to-speech", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "text-to-speech",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "text-to-speech", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "text-to-speech", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyDictId, "dict-id", "", "Pronunciation dictionary ID to apply. bulbul:v3 only")
	cmd.Flags().BoolVar(&bodyEnableCachedResponses, "enable-cached-responses", false, "Enable response caching (beta, bulbul:v1/v2 only)")
	cmd.Flags().BoolVar(&bodyEnablePreprocessing, "enable-preprocessing", false, "Normalize English words and numeric entities. bulbul:v2 only")
	cmd.Flags().StringVar(&bodyLanguageCode, "language-code", "", "Language code in BCP-47 format")
	cmd.Flags().Float64Var(&bodyLoudness, "loudness", 0.0, "Loudness control (0.3 to 3.0). bulbul:v2 only")
	cmd.Flags().StringVar(&bodyModel, "model", "", "TTS model. bulbul:v3 is the latest with 30+ voices")
	cmd.Flags().StringVar(&bodyOutputAudioCodec, "output-audio-codec", "wav", "Output audio codec")
	cmd.Flags().Float64Var(&bodyPace, "pace", 1.000000, "Speed of audio. bulbul:v3: 0.5-2.0, bulbul:v2: 0.3-3.0")
	cmd.Flags().Float64Var(&bodyPitch, "pitch", 0.0, "Pitch control (-0.75 to 0.75). bulbul:v2 only")
	cmd.Flags().StringVar(&bodySpeaker, "speaker", "", "Speaker voice (lowercase). Default shubh for bulbul:v3, anushka for bulbul:v2")
	cmd.Flags().IntVar(&bodySpeechSampleRate, "speech-sample-rate", 24000, "Sample rate of the output audio")
	cmd.Flags().Float64Var(&bodyTemperature, "temperature", 0.600000, "Randomness/expressiveness (0.01 to 2.0). bulbul:v3 only")
	cmd.Flags().StringVar(&bodyText, "text", "", "Text to convert to speech. Max 2500 chars for bulbul:v3, 1500 for bulbul:v2. Use commas for numbers larger than 4 digits")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
