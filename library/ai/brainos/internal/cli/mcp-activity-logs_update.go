// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newMcpActivityLogsUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagSessionId string
	var flagServerId string
	var flagAction string
	var flagEndpoint string
	var flagRequestBody string
	var flagResponseBody string
	var flagStatusCode string
	var flagErrorMessage string
	var flagLatencies string
	var flagMetadata string
	var flagClientIp string
	var flagUserAgent string
	var flagCreatedAt string
	var flagTool string
	var flagArguments string
	var flagResult string
	var flagExecutionMs string
	var flagServerIdText string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Update",
		Example:     "  brainos-pp-cli mcp-activity-logs update",
		Annotations: map[string]string{"pp:endpoint": "mcp-activity-logs.update", "pp:method": "PATCH", "pp:path": "/mcp_activity_logs"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/mcp_activity_logs"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
			}
			params := map[string]string{}
			if flagId != "" {
				params["id"] = fmt.Sprintf("%v", flagId)
			}
			if flagSessionId != "" {
				params["session_id"] = fmt.Sprintf("%v", flagSessionId)
			}
			if flagServerId != "" {
				params["server_id"] = fmt.Sprintf("%v", flagServerId)
			}
			if flagAction != "" {
				params["action"] = fmt.Sprintf("%v", flagAction)
			}
			if flagEndpoint != "" {
				params["endpoint"] = fmt.Sprintf("%v", flagEndpoint)
			}
			if flagRequestBody != "" {
				params["request_body"] = fmt.Sprintf("%v", flagRequestBody)
			}
			if flagResponseBody != "" {
				params["response_body"] = fmt.Sprintf("%v", flagResponseBody)
			}
			if flagStatusCode != "" {
				params["status_code"] = fmt.Sprintf("%v", flagStatusCode)
			}
			if flagErrorMessage != "" {
				params["error_message"] = fmt.Sprintf("%v", flagErrorMessage)
			}
			if flagLatencies != "" {
				params["latencies"] = fmt.Sprintf("%v", flagLatencies)
			}
			if flagMetadata != "" {
				params["metadata"] = fmt.Sprintf("%v", flagMetadata)
			}
			if flagClientIp != "" {
				params["client_ip"] = fmt.Sprintf("%v", flagClientIp)
			}
			if flagUserAgent != "" {
				params["user_agent"] = fmt.Sprintf("%v", flagUserAgent)
			}
			if flagCreatedAt != "" {
				params["created_at"] = fmt.Sprintf("%v", flagCreatedAt)
			}
			if flagTool != "" {
				params["tool"] = fmt.Sprintf("%v", flagTool)
			}
			if flagArguments != "" {
				params["arguments"] = fmt.Sprintf("%v", flagArguments)
			}
			if flagResult != "" {
				params["result"] = fmt.Sprintf("%v", flagResult)
			}
			if flagExecutionMs != "" {
				params["execution_ms"] = fmt.Sprintf("%v", flagExecutionMs)
			}
			if flagServerIdText != "" {
				params["server_id_text"] = fmt.Sprintf("%v", flagServerIdText)
			}
			data, statusCode, err := c.PatchWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "mcp-activity-logs",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagSessionId, "session-id", "", "Session id")
	cmd.Flags().StringVar(&flagServerId, "server-id", "", "Server id")
	cmd.Flags().StringVar(&flagAction, "action", "", "Action")
	cmd.Flags().StringVar(&flagEndpoint, "endpoint", "", "Endpoint")
	cmd.Flags().StringVar(&flagRequestBody, "request-body", "", "Request body")
	cmd.Flags().StringVar(&flagResponseBody, "response-body", "", "Response body")
	cmd.Flags().StringVar(&flagStatusCode, "status-code", "", "Status code")
	cmd.Flags().StringVar(&flagErrorMessage, "error-message", "", "Error message")
	cmd.Flags().StringVar(&flagLatencies, "latencies", "", "Latencies")
	cmd.Flags().StringVar(&flagMetadata, "metadata", "", "Metadata")
	cmd.Flags().StringVar(&flagClientIp, "client-ip", "", "Client ip")
	cmd.Flags().StringVar(&flagUserAgent, "user-agent", "", "User agent")
	cmd.Flags().StringVar(&flagCreatedAt, "created-at", "", "Created at")
	cmd.Flags().StringVar(&flagTool, "tool", "", "Tool")
	cmd.Flags().StringVar(&flagArguments, "arguments", "", "Arguments")
	cmd.Flags().StringVar(&flagResult, "result", "", "Result")
	cmd.Flags().StringVar(&flagExecutionMs, "execution-ms", "", "Execution ms")
	cmd.Flags().StringVar(&flagServerIdText, "server-id-text", "", "Server id text")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
