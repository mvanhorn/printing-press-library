// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newExecutorBriefsDeleteCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagTriggerName string
	var flagGoal string
	var flagContext string
	var flagFolderIds string
	var flagDateRange string
	var flagFidelityMode string
	var flagCreatedAt string
	var flagExpiresAt string

	cmd := &cobra.Command{
		Use:         "delete",
		Short:       "Delete",
		Example:     "  brainos-pp-cli executor-briefs delete",
		Annotations: map[string]string{"pp:endpoint": "executor-briefs.delete", "pp:method": "DELETE", "pp:path": "/executor_briefs"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/executor_briefs"
			params := map[string]string{}
			if flagId != "" {
				params["id"] = fmt.Sprintf("%v", flagId)
			}
			if flagTriggerName != "" {
				params["trigger_name"] = fmt.Sprintf("%v", flagTriggerName)
			}
			if flagGoal != "" {
				params["goal"] = fmt.Sprintf("%v", flagGoal)
			}
			if flagContext != "" {
				params["context"] = fmt.Sprintf("%v", flagContext)
			}
			if flagFolderIds != "" {
				params["folder_ids"] = fmt.Sprintf("%v", flagFolderIds)
			}
			if flagDateRange != "" {
				params["date_range"] = fmt.Sprintf("%v", flagDateRange)
			}
			if flagFidelityMode != "" {
				params["fidelity_mode"] = fmt.Sprintf("%v", flagFidelityMode)
			}
			if flagCreatedAt != "" {
				params["created_at"] = fmt.Sprintf("%v", flagCreatedAt)
			}
			if flagExpiresAt != "" {
				params["expires_at"] = fmt.Sprintf("%v", flagExpiresAt)
			}
			data, statusCode, err := c.DeleteWithParams(path, params)
			if err != nil {
				return classifyDeleteError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "delete",
					"resource": "executor-briefs",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagTriggerName, "trigger-name", "", "Trigger name")
	cmd.Flags().StringVar(&flagGoal, "goal", "", "Goal")
	cmd.Flags().StringVar(&flagContext, "context", "", "Context")
	cmd.Flags().StringVar(&flagFolderIds, "folder-ids", "", "Folder ids")
	cmd.Flags().StringVar(&flagDateRange, "date-range", "", "Date range")
	cmd.Flags().StringVar(&flagFidelityMode, "fidelity-mode", "", "Fidelity mode")
	cmd.Flags().StringVar(&flagCreatedAt, "created-at", "", "Created at")
	cmd.Flags().StringVar(&flagExpiresAt, "expires-at", "", "Expires at")

	return cmd
}
