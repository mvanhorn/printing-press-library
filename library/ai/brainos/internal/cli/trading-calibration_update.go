// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTradingCalibrationUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagComputedAt string
	var flagWindowStart string
	var flagWindowEnd string
	var flagSetupType string
	var flagRegime string
	var flagTradeCount string
	var flagWins string
	var flagLosses string
	var flagWinRate string
	var flagAvgWinPnl string
	var flagAvgLossPnl string
	var flagExpectancy string
	var flagBrierScore string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Sentinel v1.1 Wave 2: Brier score + win rate + expectancy by setup_type x regime x window; NULL setup_type/regime =...",
		Example:     "  brainos-pp-cli trading-calibration update",
		Annotations: map[string]string{"pp:endpoint": "trading-calibration.update", "pp:method": "PATCH", "pp:path": "/trading_calibration"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/trading_calibration"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
			}
			params := map[string]string{}
			if flagId != "" {
				params["id"] = fmt.Sprintf("%v", flagId)
			}
			if flagComputedAt != "" {
				params["computed_at"] = fmt.Sprintf("%v", flagComputedAt)
			}
			if flagWindowStart != "" {
				params["window_start"] = fmt.Sprintf("%v", flagWindowStart)
			}
			if flagWindowEnd != "" {
				params["window_end"] = fmt.Sprintf("%v", flagWindowEnd)
			}
			if flagSetupType != "" {
				params["setup_type"] = fmt.Sprintf("%v", flagSetupType)
			}
			if flagRegime != "" {
				params["regime"] = fmt.Sprintf("%v", flagRegime)
			}
			if flagTradeCount != "" {
				params["trade_count"] = fmt.Sprintf("%v", flagTradeCount)
			}
			if flagWins != "" {
				params["wins"] = fmt.Sprintf("%v", flagWins)
			}
			if flagLosses != "" {
				params["losses"] = fmt.Sprintf("%v", flagLosses)
			}
			if flagWinRate != "" {
				params["win_rate"] = fmt.Sprintf("%v", flagWinRate)
			}
			if flagAvgWinPnl != "" {
				params["avg_win_pnl"] = fmt.Sprintf("%v", flagAvgWinPnl)
			}
			if flagAvgLossPnl != "" {
				params["avg_loss_pnl"] = fmt.Sprintf("%v", flagAvgLossPnl)
			}
			if flagExpectancy != "" {
				params["expectancy"] = fmt.Sprintf("%v", flagExpectancy)
			}
			if flagBrierScore != "" {
				params["brier_score"] = fmt.Sprintf("%v", flagBrierScore)
			}
			data, statusCode, err := c.PatchWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "trading-calibration",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagComputedAt, "computed-at", "", "Computed at")
	cmd.Flags().StringVar(&flagWindowStart, "window-start", "", "Window start")
	cmd.Flags().StringVar(&flagWindowEnd, "window-end", "", "Window end")
	cmd.Flags().StringVar(&flagSetupType, "setup-type", "", "Setup type")
	cmd.Flags().StringVar(&flagRegime, "regime", "", "Regime")
	cmd.Flags().StringVar(&flagTradeCount, "trade-count", "", "Trade count")
	cmd.Flags().StringVar(&flagWins, "wins", "", "Wins")
	cmd.Flags().StringVar(&flagLosses, "losses", "", "Losses")
	cmd.Flags().StringVar(&flagWinRate, "win-rate", "", "Win rate")
	cmd.Flags().StringVar(&flagAvgWinPnl, "avg-win-pnl", "", "Avg win pnl")
	cmd.Flags().StringVar(&flagAvgLossPnl, "avg-loss-pnl", "", "Avg loss pnl")
	cmd.Flags().StringVar(&flagExpectancy, "expectancy", "", "Expectancy")
	cmd.Flags().StringVar(&flagBrierScore, "brier-score", "", "Brier score")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
