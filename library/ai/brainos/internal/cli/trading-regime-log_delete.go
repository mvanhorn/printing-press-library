// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTradingRegimeLogDeleteCmd(flags *rootFlags) *cobra.Command {
	var flagTradeDate string
	var flagClassifiedAt string
	var flagRegime string
	var flagSpxAbove50dma string
	var flagSpxAbove200dma string
	var flagVixLevel string
	var flagVixTermStructure string
	var flagPctAbove50dma string
	var flagHyCreditSpread string
	var flagTenYearYield string
	var flagTwosTensCurve string
	var flagDxyLevel string
	var flagDxyVs50dma string
	var flagSourceRefs string
	var flagNotes string

	cmd := &cobra.Command{
		Use:         "delete",
		Short:       "Sentinel v1.1 Wave 2 + Sentinel v4 (shared): daily SPX/VIX regime classification; A=trending bull, B=late-cycle,...",
		Example:     "  brainos-pp-cli trading-regime-log delete",
		Annotations: map[string]string{"pp:endpoint": "trading-regime-log.delete", "pp:method": "DELETE", "pp:path": "/trading_regime_log"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/trading_regime_log"
			params := map[string]string{}
			if flagTradeDate != "" {
				params["trade_date"] = fmt.Sprintf("%v", flagTradeDate)
			}
			if flagClassifiedAt != "" {
				params["classified_at"] = fmt.Sprintf("%v", flagClassifiedAt)
			}
			if flagRegime != "" {
				params["regime"] = fmt.Sprintf("%v", flagRegime)
			}
			if flagSpxAbove50dma != "" {
				params["spx_above_50dma"] = fmt.Sprintf("%v", flagSpxAbove50dma)
			}
			if flagSpxAbove200dma != "" {
				params["spx_above_200dma"] = fmt.Sprintf("%v", flagSpxAbove200dma)
			}
			if flagVixLevel != "" {
				params["vix_level"] = fmt.Sprintf("%v", flagVixLevel)
			}
			if flagVixTermStructure != "" {
				params["vix_term_structure"] = fmt.Sprintf("%v", flagVixTermStructure)
			}
			if flagPctAbove50dma != "" {
				params["pct_above_50dma"] = fmt.Sprintf("%v", flagPctAbove50dma)
			}
			if flagHyCreditSpread != "" {
				params["hy_credit_spread"] = fmt.Sprintf("%v", flagHyCreditSpread)
			}
			if flagTenYearYield != "" {
				params["ten_year_yield"] = fmt.Sprintf("%v", flagTenYearYield)
			}
			if flagTwosTensCurve != "" {
				params["twos_tens_curve"] = fmt.Sprintf("%v", flagTwosTensCurve)
			}
			if flagDxyLevel != "" {
				params["dxy_level"] = fmt.Sprintf("%v", flagDxyLevel)
			}
			if flagDxyVs50dma != "" {
				params["dxy_vs_50dma"] = fmt.Sprintf("%v", flagDxyVs50dma)
			}
			if flagSourceRefs != "" {
				params["source_refs"] = fmt.Sprintf("%v", flagSourceRefs)
			}
			if flagNotes != "" {
				params["notes"] = fmt.Sprintf("%v", flagNotes)
			}
			data, statusCode, err := c.DeleteWithParams(path, params)
			if err != nil {
				return classifyDeleteError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "delete",
					"resource": "trading-regime-log",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagTradeDate, "trade-date", "", "Trade date")
	cmd.Flags().StringVar(&flagClassifiedAt, "classified-at", "", "Classified at")
	cmd.Flags().StringVar(&flagRegime, "regime", "", "Regime")
	cmd.Flags().StringVar(&flagSpxAbove50dma, "spx-above-50dma", "", "Spx above 50dma")
	cmd.Flags().StringVar(&flagSpxAbove200dma, "spx-above-200dma", "", "Spx above 200dma")
	cmd.Flags().StringVar(&flagVixLevel, "vix-level", "", "Vix level")
	cmd.Flags().StringVar(&flagVixTermStructure, "vix-term-structure", "", "Vix term structure")
	cmd.Flags().StringVar(&flagPctAbove50dma, "pct-above-50dma", "", "Pct above 50dma")
	cmd.Flags().StringVar(&flagHyCreditSpread, "hy-credit-spread", "", "Hy credit spread")
	cmd.Flags().StringVar(&flagTenYearYield, "ten-year-yield", "", "Ten year yield")
	cmd.Flags().StringVar(&flagTwosTensCurve, "twos-tens-curve", "", "Twos tens curve")
	cmd.Flags().StringVar(&flagDxyLevel, "dxy-level", "", "Dxy level")
	cmd.Flags().StringVar(&flagDxyVs50dma, "dxy-vs-50dma", "", "Dxy vs 50dma")
	cmd.Flags().StringVar(&flagSourceRefs, "source-refs", "", "Source refs")
	cmd.Flags().StringVar(&flagNotes, "notes", "", "Notes")

	return cmd
}
