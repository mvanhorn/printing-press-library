// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newNatureosDualModelTasksListCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagCreatedAt string
	var flagUpdatedAt string
	var flagGoal string
	var flagProjectSlug string
	var flagTaskType string
	var flagCycle string
	var flagStep string
	var flagSourceModel string
	var flagPrompt string
	var flagOutput string
	var flagBrainThoughtIds string
	var flagBrainContextSnapshot string
	var flagConfidence string
	var flagStatus string
	var flagType string
	var flagTopic string
	var flagMetadata string
	var flagSelect string
	var flagOrder string
	var flagOffset string
	var flagLimit string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "List",
		Example:     "  brainos-pp-cli natureos-dual-model-tasks list",
		Annotations: map[string]string{"pp:endpoint": "natureos-dual-model-tasks.list", "pp:method": "GET", "pp:path": "/natureos_dual_model_tasks", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/natureos_dual_model_tasks"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "natureos-dual-model-tasks", path, map[string]string{
				"id":                     fmt.Sprintf("%v", flagId),
				"created_at":             fmt.Sprintf("%v", flagCreatedAt),
				"updated_at":             fmt.Sprintf("%v", flagUpdatedAt),
				"goal":                   fmt.Sprintf("%v", flagGoal),
				"project_slug":           fmt.Sprintf("%v", flagProjectSlug),
				"task_type":              fmt.Sprintf("%v", flagTaskType),
				"cycle":                  fmt.Sprintf("%v", flagCycle),
				"step":                   fmt.Sprintf("%v", flagStep),
				"source_model":           fmt.Sprintf("%v", flagSourceModel),
				"prompt":                 fmt.Sprintf("%v", flagPrompt),
				"output":                 fmt.Sprintf("%v", flagOutput),
				"brain_thought_ids":      fmt.Sprintf("%v", flagBrainThoughtIds),
				"brain_context_snapshot": fmt.Sprintf("%v", flagBrainContextSnapshot),
				"confidence":             fmt.Sprintf("%v", flagConfidence),
				"status":                 fmt.Sprintf("%v", flagStatus),
				"type":                   fmt.Sprintf("%v", flagType),
				"topic":                  fmt.Sprintf("%v", flagTopic),
				"metadata":               fmt.Sprintf("%v", flagMetadata),
				"select":                 fmt.Sprintf("%v", flagSelect),
				"order":                  fmt.Sprintf("%v", flagOrder),
				"offset":                 fmt.Sprintf("%v", flagOffset),
				"limit":                  fmt.Sprintf("%v", flagLimit),
			}, nil, flagAll, "offset", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagCreatedAt, "created-at", "", "Created at")
	cmd.Flags().StringVar(&flagUpdatedAt, "updated-at", "", "Updated at")
	cmd.Flags().StringVar(&flagGoal, "goal", "", "Goal")
	cmd.Flags().StringVar(&flagProjectSlug, "project-slug", "", "Project slug")
	cmd.Flags().StringVar(&flagTaskType, "task-type", "", "Task type")
	cmd.Flags().StringVar(&flagCycle, "cycle", "", "Cycle")
	cmd.Flags().StringVar(&flagStep, "step", "", "Step")
	cmd.Flags().StringVar(&flagSourceModel, "source-model", "", "Source model")
	cmd.Flags().StringVar(&flagPrompt, "prompt", "", "Prompt")
	cmd.Flags().StringVar(&flagOutput, "output", "", "Output")
	cmd.Flags().StringVar(&flagBrainThoughtIds, "brain-thought-ids", "", "Brain thought ids")
	cmd.Flags().StringVar(&flagBrainContextSnapshot, "brain-context-snapshot", "", "Brain context snapshot")
	cmd.Flags().StringVar(&flagConfidence, "confidence", "", "Confidence")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Status")
	cmd.Flags().StringVar(&flagType, "type", "", "Type")
	cmd.Flags().StringVar(&flagTopic, "topic", "", "Topic")
	cmd.Flags().StringVar(&flagMetadata, "metadata", "", "Metadata")
	cmd.Flags().StringVar(&flagSelect, "select", "", "Filtering Columns")
	cmd.Flags().StringVar(&flagOrder, "order", "", "Ordering")
	cmd.Flags().StringVar(&flagOffset, "offset", "", "Limiting and Pagination")
	cmd.Flags().StringVar(&flagLimit, "limit", "", "Limiting and Pagination")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
