// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newDocumentsUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagSourceType string
	var flagSourceId string
	var flagChunkIndex string
	var flagContent string
	var flagEmbedding string
	var flagMetadata string
	var flagCreatedAt string
	var flagUpdatedAt string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Update",
		Example:     "  brainos-pp-cli documents update",
		Annotations: map[string]string{"pp:endpoint": "documents.update", "pp:method": "PATCH", "pp:path": "/documents"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/documents"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
			}
			params := map[string]string{}
			if flagId != "" {
				params["id"] = fmt.Sprintf("%v", flagId)
			}
			if flagSourceType != "" {
				params["source_type"] = fmt.Sprintf("%v", flagSourceType)
			}
			if flagSourceId != "" {
				params["source_id"] = fmt.Sprintf("%v", flagSourceId)
			}
			if flagChunkIndex != "" {
				params["chunk_index"] = fmt.Sprintf("%v", flagChunkIndex)
			}
			if flagContent != "" {
				params["content"] = fmt.Sprintf("%v", flagContent)
			}
			if flagEmbedding != "" {
				params["embedding"] = fmt.Sprintf("%v", flagEmbedding)
			}
			if flagMetadata != "" {
				params["metadata"] = fmt.Sprintf("%v", flagMetadata)
			}
			if flagCreatedAt != "" {
				params["created_at"] = fmt.Sprintf("%v", flagCreatedAt)
			}
			if flagUpdatedAt != "" {
				params["updated_at"] = fmt.Sprintf("%v", flagUpdatedAt)
			}
			data, statusCode, err := c.PatchWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "documents",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagSourceType, "source-type", "", "Source type")
	cmd.Flags().StringVar(&flagSourceId, "source-id", "", "Source id")
	cmd.Flags().StringVar(&flagChunkIndex, "chunk-index", "", "Chunk index")
	cmd.Flags().StringVar(&flagContent, "content", "", "Content")
	cmd.Flags().StringVar(&flagEmbedding, "embedding", "", "Embedding")
	cmd.Flags().StringVar(&flagMetadata, "metadata", "", "Metadata")
	cmd.Flags().StringVar(&flagCreatedAt, "created-at", "", "Created at")
	cmd.Flags().StringVar(&flagUpdatedAt, "updated-at", "", "Updated at")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
