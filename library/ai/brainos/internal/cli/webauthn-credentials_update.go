// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newWebauthnCredentialsUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagUserId string
	var flagCredentialId string
	var flagPublicKey string
	var flagCounter string
	var flagDeviceType string
	var flagBackedUp string
	var flagTransports string
	var flagCreatedAt string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Update",
		Example:     "  brainos-pp-cli webauthn-credentials update",
		Annotations: map[string]string{"pp:endpoint": "webauthn-credentials.update", "pp:method": "PATCH", "pp:path": "/webauthn_credentials"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/webauthn_credentials"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
			}
			params := map[string]string{}
			if flagId != "" {
				params["id"] = fmt.Sprintf("%v", flagId)
			}
			if flagUserId != "" {
				params["user_id"] = fmt.Sprintf("%v", flagUserId)
			}
			if flagCredentialId != "" {
				params["credential_id"] = fmt.Sprintf("%v", flagCredentialId)
			}
			if flagPublicKey != "" {
				params["public_key"] = fmt.Sprintf("%v", flagPublicKey)
			}
			if flagCounter != "" {
				params["counter"] = fmt.Sprintf("%v", flagCounter)
			}
			if flagDeviceType != "" {
				params["device_type"] = fmt.Sprintf("%v", flagDeviceType)
			}
			if flagBackedUp != "" {
				params["backed_up"] = fmt.Sprintf("%v", flagBackedUp)
			}
			if flagTransports != "" {
				params["transports"] = fmt.Sprintf("%v", flagTransports)
			}
			if flagCreatedAt != "" {
				params["created_at"] = fmt.Sprintf("%v", flagCreatedAt)
			}
			data, statusCode, err := c.PatchWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "webauthn-credentials",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagUserId, "user-id", "", "User id")
	cmd.Flags().StringVar(&flagCredentialId, "credential-id", "", "Credential id")
	cmd.Flags().StringVar(&flagPublicKey, "public-key", "", "Public key")
	cmd.Flags().StringVar(&flagCounter, "counter", "", "Counter")
	cmd.Flags().StringVar(&flagDeviceType, "device-type", "", "Device type")
	cmd.Flags().StringVar(&flagBackedUp, "backed-up", "", "Backed up")
	cmd.Flags().StringVar(&flagTransports, "transports", "", "Transports")
	cmd.Flags().StringVar(&flagCreatedAt, "created-at", "", "Created at")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
