// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTradingCalibrationListCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagComputedAt string
	var flagWindowStart string
	var flagWindowEnd string
	var flagSetupType string
	var flagRegime string
	var flagTradeCount string
	var flagWins string
	var flagLosses string
	var flagWinRate string
	var flagAvgWinPnl string
	var flagAvgLossPnl string
	var flagExpectancy string
	var flagBrierScore string
	var flagSelect string
	var flagOrder string
	var flagOffset string
	var flagLimit string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Sentinel v1.1 Wave 2: Brier score + win rate + expectancy by setup_type x regime x window; NULL setup_type/regime =...",
		Example:     "  brainos-pp-cli trading-calibration list",
		Annotations: map[string]string{"pp:endpoint": "trading-calibration.list", "pp:method": "GET", "pp:path": "/trading_calibration", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/trading_calibration"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "trading-calibration", path, map[string]string{
				"id":           fmt.Sprintf("%v", flagId),
				"computed_at":  fmt.Sprintf("%v", flagComputedAt),
				"window_start": fmt.Sprintf("%v", flagWindowStart),
				"window_end":   fmt.Sprintf("%v", flagWindowEnd),
				"setup_type":   fmt.Sprintf("%v", flagSetupType),
				"regime":       fmt.Sprintf("%v", flagRegime),
				"trade_count":  fmt.Sprintf("%v", flagTradeCount),
				"wins":         fmt.Sprintf("%v", flagWins),
				"losses":       fmt.Sprintf("%v", flagLosses),
				"win_rate":     fmt.Sprintf("%v", flagWinRate),
				"avg_win_pnl":  fmt.Sprintf("%v", flagAvgWinPnl),
				"avg_loss_pnl": fmt.Sprintf("%v", flagAvgLossPnl),
				"expectancy":   fmt.Sprintf("%v", flagExpectancy),
				"brier_score":  fmt.Sprintf("%v", flagBrierScore),
				"select":       fmt.Sprintf("%v", flagSelect),
				"order":        fmt.Sprintf("%v", flagOrder),
				"offset":       fmt.Sprintf("%v", flagOffset),
				"limit":        fmt.Sprintf("%v", flagLimit),
			}, nil, flagAll, "offset", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagComputedAt, "computed-at", "", "Computed at")
	cmd.Flags().StringVar(&flagWindowStart, "window-start", "", "Window start")
	cmd.Flags().StringVar(&flagWindowEnd, "window-end", "", "Window end")
	cmd.Flags().StringVar(&flagSetupType, "setup-type", "", "Setup type")
	cmd.Flags().StringVar(&flagRegime, "regime", "", "Regime")
	cmd.Flags().StringVar(&flagTradeCount, "trade-count", "", "Trade count")
	cmd.Flags().StringVar(&flagWins, "wins", "", "Wins")
	cmd.Flags().StringVar(&flagLosses, "losses", "", "Losses")
	cmd.Flags().StringVar(&flagWinRate, "win-rate", "", "Win rate")
	cmd.Flags().StringVar(&flagAvgWinPnl, "avg-win-pnl", "", "Avg win pnl")
	cmd.Flags().StringVar(&flagAvgLossPnl, "avg-loss-pnl", "", "Avg loss pnl")
	cmd.Flags().StringVar(&flagExpectancy, "expectancy", "", "Expectancy")
	cmd.Flags().StringVar(&flagBrierScore, "brier-score", "", "Brier score")
	cmd.Flags().StringVar(&flagSelect, "select", "", "Filtering Columns")
	cmd.Flags().StringVar(&flagOrder, "order", "", "Ordering")
	cmd.Flags().StringVar(&flagOffset, "offset", "", "Limiting and Pagination")
	cmd.Flags().StringVar(&flagLimit, "limit", "", "Limiting and Pagination")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
