// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newMcpapikeysDeleteCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagServerId string
	var flagKeyHash string
	var flagRefreshToken string
	var flagExpiresAt string
	var flagUpdatedAt string
	var flagCreatedAt string
	var flagAccessToken string

	cmd := &cobra.Command{
		Use:         "delete",
		Short:       "Delete",
		Example:     "  brainos-pp-cli mcpapikeys delete",
		Annotations: map[string]string{"pp:endpoint": "mcpapikeys.delete", "pp:method": "DELETE", "pp:path": "/mcpapikeys"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/mcpapikeys"
			params := map[string]string{}
			if flagId != "" {
				params["id"] = fmt.Sprintf("%v", flagId)
			}
			if flagServerId != "" {
				params["server_id"] = fmt.Sprintf("%v", flagServerId)
			}
			if flagKeyHash != "" {
				params["key_hash"] = fmt.Sprintf("%v", flagKeyHash)
			}
			if flagRefreshToken != "" {
				params["refresh_token"] = fmt.Sprintf("%v", flagRefreshToken)
			}
			if flagExpiresAt != "" {
				params["expires_at"] = fmt.Sprintf("%v", flagExpiresAt)
			}
			if flagUpdatedAt != "" {
				params["updated_at"] = fmt.Sprintf("%v", flagUpdatedAt)
			}
			if flagCreatedAt != "" {
				params["created_at"] = fmt.Sprintf("%v", flagCreatedAt)
			}
			if flagAccessToken != "" {
				params["access_token"] = fmt.Sprintf("%v", flagAccessToken)
			}
			data, statusCode, err := c.DeleteWithParams(path, params)
			if err != nil {
				return classifyDeleteError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "delete",
					"resource": "mcpapikeys",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagServerId, "server-id", "", "Server id")
	cmd.Flags().StringVar(&flagKeyHash, "key-hash", "", "Key hash")
	cmd.Flags().StringVar(&flagRefreshToken, "refresh-token", "", "Refresh token")
	cmd.Flags().StringVar(&flagExpiresAt, "expires-at", "", "Expires at")
	cmd.Flags().StringVar(&flagUpdatedAt, "updated-at", "", "Updated at")
	cmd.Flags().StringVar(&flagCreatedAt, "created-at", "", "Created at")
	cmd.Flags().StringVar(&flagAccessToken, "access-token", "", "Access token")

	return cmd
}
