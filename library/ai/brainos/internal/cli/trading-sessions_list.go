// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTradingSessionsListCmd(flags *rootFlags) *cobra.Command {
	var flagTradeDate string
	var flagSessionStart string
	var flagSessionEnd string
	var flagAccountMode string
	var flagTradesTaken string
	var flagWins string
	var flagLosses string
	var flagBreakevens string
	var flagGrossPnl string
	var flagNetPnl string
	var flagAccountPctChange string
	var flagRulesViolatedCount string
	var flagRegime string
	var flagOperatorState string
	var flagHaltActive string
	var flagHaltReason string
	var flagNotes string
	var flagSelect string
	var flagOrder string
	var flagOffset string
	var flagLimit string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Sentinel v1.1 Wave 2: per-day rollup of trades + operator state + halt status; mirrors state/session-summary-*.txt;...",
		Example:     "  brainos-pp-cli trading-sessions list",
		Annotations: map[string]string{"pp:endpoint": "trading-sessions.list", "pp:method": "GET", "pp:path": "/trading_sessions", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/trading_sessions"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "trading-sessions", path, map[string]string{
				"trade_date":           fmt.Sprintf("%v", flagTradeDate),
				"session_start":        fmt.Sprintf("%v", flagSessionStart),
				"session_end":          fmt.Sprintf("%v", flagSessionEnd),
				"account_mode":         fmt.Sprintf("%v", flagAccountMode),
				"trades_taken":         fmt.Sprintf("%v", flagTradesTaken),
				"wins":                 fmt.Sprintf("%v", flagWins),
				"losses":               fmt.Sprintf("%v", flagLosses),
				"breakevens":           fmt.Sprintf("%v", flagBreakevens),
				"gross_pnl":            fmt.Sprintf("%v", flagGrossPnl),
				"net_pnl":              fmt.Sprintf("%v", flagNetPnl),
				"account_pct_change":   fmt.Sprintf("%v", flagAccountPctChange),
				"rules_violated_count": fmt.Sprintf("%v", flagRulesViolatedCount),
				"regime":               fmt.Sprintf("%v", flagRegime),
				"operator_state":       fmt.Sprintf("%v", flagOperatorState),
				"halt_active":          fmt.Sprintf("%v", flagHaltActive),
				"halt_reason":          fmt.Sprintf("%v", flagHaltReason),
				"notes":                fmt.Sprintf("%v", flagNotes),
				"select":               fmt.Sprintf("%v", flagSelect),
				"order":                fmt.Sprintf("%v", flagOrder),
				"offset":               fmt.Sprintf("%v", flagOffset),
				"limit":                fmt.Sprintf("%v", flagLimit),
			}, nil, flagAll, "offset", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagTradeDate, "trade-date", "", "Trade date")
	cmd.Flags().StringVar(&flagSessionStart, "session-start", "", "Session start")
	cmd.Flags().StringVar(&flagSessionEnd, "session-end", "", "Session end")
	cmd.Flags().StringVar(&flagAccountMode, "account-mode", "", "Account mode")
	cmd.Flags().StringVar(&flagTradesTaken, "trades-taken", "", "Trades taken")
	cmd.Flags().StringVar(&flagWins, "wins", "", "Wins")
	cmd.Flags().StringVar(&flagLosses, "losses", "", "Losses")
	cmd.Flags().StringVar(&flagBreakevens, "breakevens", "", "Breakevens")
	cmd.Flags().StringVar(&flagGrossPnl, "gross-pnl", "", "Gross pnl")
	cmd.Flags().StringVar(&flagNetPnl, "net-pnl", "", "Net pnl")
	cmd.Flags().StringVar(&flagAccountPctChange, "account-pct-change", "", "Account pct change")
	cmd.Flags().StringVar(&flagRulesViolatedCount, "rules-violated-count", "", "Rules violated count")
	cmd.Flags().StringVar(&flagRegime, "regime", "", "Regime")
	cmd.Flags().StringVar(&flagOperatorState, "operator-state", "", "Operator state")
	cmd.Flags().StringVar(&flagHaltActive, "halt-active", "", "Halt active")
	cmd.Flags().StringVar(&flagHaltReason, "halt-reason", "", "Halt reason")
	cmd.Flags().StringVar(&flagNotes, "notes", "", "Notes")
	cmd.Flags().StringVar(&flagSelect, "select", "", "Filtering Columns")
	cmd.Flags().StringVar(&flagOrder, "order", "", "Ordering")
	cmd.Flags().StringVar(&flagOffset, "offset", "", "Limiting and Pagination")
	cmd.Flags().StringVar(&flagLimit, "limit", "", "Limiting and Pagination")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
