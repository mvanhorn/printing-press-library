// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAgentMessagesDeleteCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagSender string
	var flagRecipient string
	var flagMessageType string
	var flagContent string
	var flagStatus string
	var flagPriority string
	var flagCreatedAt string
	var flagReadAt string
	var flagProcessedAt string
	var flagBrainCapturedAt string

	cmd := &cobra.Command{
		Use:         "delete",
		Short:       "1) pg_notify (instant via trigger) 2) Supabase Realtime WebSocket subscription 3) 5-min poll fallback (trigger...",
		Example:     "  brainos-pp-cli agent-messages delete",
		Annotations: map[string]string{"pp:endpoint": "agent-messages.delete", "pp:method": "DELETE", "pp:path": "/agent_messages"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/agent_messages"
			params := map[string]string{}
			if flagId != "" {
				params["id"] = fmt.Sprintf("%v", flagId)
			}
			if flagSender != "" {
				params["sender"] = fmt.Sprintf("%v", flagSender)
			}
			if flagRecipient != "" {
				params["recipient"] = fmt.Sprintf("%v", flagRecipient)
			}
			if flagMessageType != "" {
				params["message_type"] = fmt.Sprintf("%v", flagMessageType)
			}
			if flagContent != "" {
				params["content"] = fmt.Sprintf("%v", flagContent)
			}
			if flagStatus != "" {
				params["status"] = fmt.Sprintf("%v", flagStatus)
			}
			if flagPriority != "" {
				params["priority"] = fmt.Sprintf("%v", flagPriority)
			}
			if flagCreatedAt != "" {
				params["created_at"] = fmt.Sprintf("%v", flagCreatedAt)
			}
			if flagReadAt != "" {
				params["read_at"] = fmt.Sprintf("%v", flagReadAt)
			}
			if flagProcessedAt != "" {
				params["processed_at"] = fmt.Sprintf("%v", flagProcessedAt)
			}
			if flagBrainCapturedAt != "" {
				params["brain_captured_at"] = fmt.Sprintf("%v", flagBrainCapturedAt)
			}
			data, statusCode, err := c.DeleteWithParams(path, params)
			if err != nil {
				return classifyDeleteError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "delete",
					"resource": "agent-messages",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagSender, "sender", "", "Sender")
	cmd.Flags().StringVar(&flagRecipient, "recipient", "", "Recipient")
	cmd.Flags().StringVar(&flagMessageType, "message-type", "", "Message type")
	cmd.Flags().StringVar(&flagContent, "content", "", "Content")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Status")
	cmd.Flags().StringVar(&flagPriority, "priority", "", "Priority")
	cmd.Flags().StringVar(&flagCreatedAt, "created-at", "", "Created at")
	cmd.Flags().StringVar(&flagReadAt, "read-at", "", "Read at")
	cmd.Flags().StringVar(&flagProcessedAt, "processed-at", "", "Processed at")
	cmd.Flags().StringVar(&flagBrainCapturedAt, "brain-captured-at", "", "Brain captured at")

	return cmd
}
