// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTradingPremortemsUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagTradeDate string
	var flagCreatedAt string
	var flagTicker string
	var flagSetupType string
	var flagConviction string
	var flagEntryPrice string
	var flagStopPrice string
	var flagTargetPrice string
	var flagPositionDollars string
	var flagShares string
	var flagRiskDollars string
	var flagCatalyst string
	var flagTimeWindow string
	var flagHypothesis string
	var flagPTargetHit string
	var flagPStopHit string
	var flagPTimeout string
	var flagAccountId string
	var flagAccountMode string
	var flagSide string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Sentinel v1.1 Wave 2: pre-trade hypothesis + sized-risk capture before order entry; mirrors state/premortem-*.jsonl.",
		Example:     "  brainos-pp-cli trading-premortems update",
		Annotations: map[string]string{"pp:endpoint": "trading-premortems.update", "pp:method": "PATCH", "pp:path": "/trading_premortems"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/trading_premortems"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
			}
			params := map[string]string{}
			if flagId != "" {
				params["id"] = fmt.Sprintf("%v", flagId)
			}
			if flagTradeDate != "" {
				params["trade_date"] = fmt.Sprintf("%v", flagTradeDate)
			}
			if flagCreatedAt != "" {
				params["created_at"] = fmt.Sprintf("%v", flagCreatedAt)
			}
			if flagTicker != "" {
				params["ticker"] = fmt.Sprintf("%v", flagTicker)
			}
			if flagSetupType != "" {
				params["setup_type"] = fmt.Sprintf("%v", flagSetupType)
			}
			if flagConviction != "" {
				params["conviction"] = fmt.Sprintf("%v", flagConviction)
			}
			if flagEntryPrice != "" {
				params["entry_price"] = fmt.Sprintf("%v", flagEntryPrice)
			}
			if flagStopPrice != "" {
				params["stop_price"] = fmt.Sprintf("%v", flagStopPrice)
			}
			if flagTargetPrice != "" {
				params["target_price"] = fmt.Sprintf("%v", flagTargetPrice)
			}
			if flagPositionDollars != "" {
				params["position_dollars"] = fmt.Sprintf("%v", flagPositionDollars)
			}
			if flagShares != "" {
				params["shares"] = fmt.Sprintf("%v", flagShares)
			}
			if flagRiskDollars != "" {
				params["risk_dollars"] = fmt.Sprintf("%v", flagRiskDollars)
			}
			if flagCatalyst != "" {
				params["catalyst"] = fmt.Sprintf("%v", flagCatalyst)
			}
			if flagTimeWindow != "" {
				params["time_window"] = fmt.Sprintf("%v", flagTimeWindow)
			}
			if flagHypothesis != "" {
				params["hypothesis"] = fmt.Sprintf("%v", flagHypothesis)
			}
			if flagPTargetHit != "" {
				params["p_target_hit"] = fmt.Sprintf("%v", flagPTargetHit)
			}
			if flagPStopHit != "" {
				params["p_stop_hit"] = fmt.Sprintf("%v", flagPStopHit)
			}
			if flagPTimeout != "" {
				params["p_timeout"] = fmt.Sprintf("%v", flagPTimeout)
			}
			if flagAccountId != "" {
				params["account_id"] = fmt.Sprintf("%v", flagAccountId)
			}
			if flagAccountMode != "" {
				params["account_mode"] = fmt.Sprintf("%v", flagAccountMode)
			}
			if flagSide != "" {
				params["side"] = fmt.Sprintf("%v", flagSide)
			}
			data, statusCode, err := c.PatchWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "trading-premortems",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagTradeDate, "trade-date", "", "Trade date")
	cmd.Flags().StringVar(&flagCreatedAt, "created-at", "", "Created at")
	cmd.Flags().StringVar(&flagTicker, "ticker", "", "Ticker")
	cmd.Flags().StringVar(&flagSetupType, "setup-type", "", "Setup type")
	cmd.Flags().StringVar(&flagConviction, "conviction", "", "Conviction")
	cmd.Flags().StringVar(&flagEntryPrice, "entry-price", "", "Entry price")
	cmd.Flags().StringVar(&flagStopPrice, "stop-price", "", "Stop price")
	cmd.Flags().StringVar(&flagTargetPrice, "target-price", "", "Target price")
	cmd.Flags().StringVar(&flagPositionDollars, "position-dollars", "", "Position dollars")
	cmd.Flags().StringVar(&flagShares, "shares", "", "Shares")
	cmd.Flags().StringVar(&flagRiskDollars, "risk-dollars", "", "Risk dollars")
	cmd.Flags().StringVar(&flagCatalyst, "catalyst", "", "Catalyst")
	cmd.Flags().StringVar(&flagTimeWindow, "time-window", "", "Time window")
	cmd.Flags().StringVar(&flagHypothesis, "hypothesis", "", "Hypothesis")
	cmd.Flags().StringVar(&flagPTargetHit, "p-target-hit", "", "P target hit")
	cmd.Flags().StringVar(&flagPStopHit, "p-stop-hit", "", "P stop hit")
	cmd.Flags().StringVar(&flagPTimeout, "p-timeout", "", "P timeout")
	cmd.Flags().StringVar(&flagAccountId, "account-id", "", "Account id")
	cmd.Flags().StringVar(&flagAccountMode, "account-mode", "", "Account mode")
	cmd.Flags().StringVar(&flagSide, "side", "", "Side")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
