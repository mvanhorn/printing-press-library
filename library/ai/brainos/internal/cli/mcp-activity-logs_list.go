// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newMcpActivityLogsListCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagSessionId string
	var flagServerId string
	var flagAction string
	var flagEndpoint string
	var flagRequestBody string
	var flagResponseBody string
	var flagStatusCode string
	var flagErrorMessage string
	var flagLatencies string
	var flagMetadata string
	var flagClientIp string
	var flagUserAgent string
	var flagCreatedAt string
	var flagTool string
	var flagArguments string
	var flagResult string
	var flagExecutionMs string
	var flagServerIdText string
	var flagSelect string
	var flagOrder string
	var flagOffset string
	var flagLimit string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "List",
		Example:     "  brainos-pp-cli mcp-activity-logs list",
		Annotations: map[string]string{"pp:endpoint": "mcp-activity-logs.list", "pp:method": "GET", "pp:path": "/mcp_activity_logs", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/mcp_activity_logs"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "mcp-activity-logs", path, map[string]string{
				"id":             fmt.Sprintf("%v", flagId),
				"session_id":     fmt.Sprintf("%v", flagSessionId),
				"server_id":      fmt.Sprintf("%v", flagServerId),
				"action":         fmt.Sprintf("%v", flagAction),
				"endpoint":       fmt.Sprintf("%v", flagEndpoint),
				"request_body":   fmt.Sprintf("%v", flagRequestBody),
				"response_body":  fmt.Sprintf("%v", flagResponseBody),
				"status_code":    fmt.Sprintf("%v", flagStatusCode),
				"error_message":  fmt.Sprintf("%v", flagErrorMessage),
				"latencies":      fmt.Sprintf("%v", flagLatencies),
				"metadata":       fmt.Sprintf("%v", flagMetadata),
				"client_ip":      fmt.Sprintf("%v", flagClientIp),
				"user_agent":     fmt.Sprintf("%v", flagUserAgent),
				"created_at":     fmt.Sprintf("%v", flagCreatedAt),
				"tool":           fmt.Sprintf("%v", flagTool),
				"arguments":      fmt.Sprintf("%v", flagArguments),
				"result":         fmt.Sprintf("%v", flagResult),
				"execution_ms":   fmt.Sprintf("%v", flagExecutionMs),
				"server_id_text": fmt.Sprintf("%v", flagServerIdText),
				"select":         fmt.Sprintf("%v", flagSelect),
				"order":          fmt.Sprintf("%v", flagOrder),
				"offset":         fmt.Sprintf("%v", flagOffset),
				"limit":          fmt.Sprintf("%v", flagLimit),
			}, nil, flagAll, "offset", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagSessionId, "session-id", "", "Session id")
	cmd.Flags().StringVar(&flagServerId, "server-id", "", "Server id")
	cmd.Flags().StringVar(&flagAction, "action", "", "Action")
	cmd.Flags().StringVar(&flagEndpoint, "endpoint", "", "Endpoint")
	cmd.Flags().StringVar(&flagRequestBody, "request-body", "", "Request body")
	cmd.Flags().StringVar(&flagResponseBody, "response-body", "", "Response body")
	cmd.Flags().StringVar(&flagStatusCode, "status-code", "", "Status code")
	cmd.Flags().StringVar(&flagErrorMessage, "error-message", "", "Error message")
	cmd.Flags().StringVar(&flagLatencies, "latencies", "", "Latencies")
	cmd.Flags().StringVar(&flagMetadata, "metadata", "", "Metadata")
	cmd.Flags().StringVar(&flagClientIp, "client-ip", "", "Client ip")
	cmd.Flags().StringVar(&flagUserAgent, "user-agent", "", "User agent")
	cmd.Flags().StringVar(&flagCreatedAt, "created-at", "", "Created at")
	cmd.Flags().StringVar(&flagTool, "tool", "", "Tool")
	cmd.Flags().StringVar(&flagArguments, "arguments", "", "Arguments")
	cmd.Flags().StringVar(&flagResult, "result", "", "Result")
	cmd.Flags().StringVar(&flagExecutionMs, "execution-ms", "", "Execution ms")
	cmd.Flags().StringVar(&flagServerIdText, "server-id-text", "", "Server id text")
	cmd.Flags().StringVar(&flagSelect, "select", "", "Filtering Columns")
	cmd.Flags().StringVar(&flagOrder, "order", "", "Ordering")
	cmd.Flags().StringVar(&flagOffset, "offset", "", "Limiting and Pagination")
	cmd.Flags().StringVar(&flagLimit, "limit", "", "Limiting and Pagination")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
