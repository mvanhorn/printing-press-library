// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newNatureosDualModelTasksUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagCreatedAt string
	var flagUpdatedAt string
	var flagGoal string
	var flagProjectSlug string
	var flagTaskType string
	var flagCycle string
	var flagStep string
	var flagSourceModel string
	var flagPrompt string
	var flagOutput string
	var flagBrainThoughtIds string
	var flagBrainContextSnapshot string
	var flagConfidence string
	var flagStatus string
	var flagType string
	var flagTopic string
	var flagMetadata string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Update",
		Example:     "  brainos-pp-cli natureos-dual-model-tasks update",
		Annotations: map[string]string{"pp:endpoint": "natureos-dual-model-tasks.update", "pp:method": "PATCH", "pp:path": "/natureos_dual_model_tasks"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/natureos_dual_model_tasks"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
			}
			params := map[string]string{}
			if flagId != "" {
				params["id"] = fmt.Sprintf("%v", flagId)
			}
			if flagCreatedAt != "" {
				params["created_at"] = fmt.Sprintf("%v", flagCreatedAt)
			}
			if flagUpdatedAt != "" {
				params["updated_at"] = fmt.Sprintf("%v", flagUpdatedAt)
			}
			if flagGoal != "" {
				params["goal"] = fmt.Sprintf("%v", flagGoal)
			}
			if flagProjectSlug != "" {
				params["project_slug"] = fmt.Sprintf("%v", flagProjectSlug)
			}
			if flagTaskType != "" {
				params["task_type"] = fmt.Sprintf("%v", flagTaskType)
			}
			if flagCycle != "" {
				params["cycle"] = fmt.Sprintf("%v", flagCycle)
			}
			if flagStep != "" {
				params["step"] = fmt.Sprintf("%v", flagStep)
			}
			if flagSourceModel != "" {
				params["source_model"] = fmt.Sprintf("%v", flagSourceModel)
			}
			if flagPrompt != "" {
				params["prompt"] = fmt.Sprintf("%v", flagPrompt)
			}
			if flagOutput != "" {
				params["output"] = fmt.Sprintf("%v", flagOutput)
			}
			if flagBrainThoughtIds != "" {
				params["brain_thought_ids"] = fmt.Sprintf("%v", flagBrainThoughtIds)
			}
			if flagBrainContextSnapshot != "" {
				params["brain_context_snapshot"] = fmt.Sprintf("%v", flagBrainContextSnapshot)
			}
			if flagConfidence != "" {
				params["confidence"] = fmt.Sprintf("%v", flagConfidence)
			}
			if flagStatus != "" {
				params["status"] = fmt.Sprintf("%v", flagStatus)
			}
			if flagType != "" {
				params["type"] = fmt.Sprintf("%v", flagType)
			}
			if flagTopic != "" {
				params["topic"] = fmt.Sprintf("%v", flagTopic)
			}
			if flagMetadata != "" {
				params["metadata"] = fmt.Sprintf("%v", flagMetadata)
			}
			data, statusCode, err := c.PatchWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "natureos-dual-model-tasks",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagCreatedAt, "created-at", "", "Created at")
	cmd.Flags().StringVar(&flagUpdatedAt, "updated-at", "", "Updated at")
	cmd.Flags().StringVar(&flagGoal, "goal", "", "Goal")
	cmd.Flags().StringVar(&flagProjectSlug, "project-slug", "", "Project slug")
	cmd.Flags().StringVar(&flagTaskType, "task-type", "", "Task type")
	cmd.Flags().StringVar(&flagCycle, "cycle", "", "Cycle")
	cmd.Flags().StringVar(&flagStep, "step", "", "Step")
	cmd.Flags().StringVar(&flagSourceModel, "source-model", "", "Source model")
	cmd.Flags().StringVar(&flagPrompt, "prompt", "", "Prompt")
	cmd.Flags().StringVar(&flagOutput, "output", "", "Output")
	cmd.Flags().StringVar(&flagBrainThoughtIds, "brain-thought-ids", "", "Brain thought ids")
	cmd.Flags().StringVar(&flagBrainContextSnapshot, "brain-context-snapshot", "", "Brain context snapshot")
	cmd.Flags().StringVar(&flagConfidence, "confidence", "", "Confidence")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Status")
	cmd.Flags().StringVar(&flagType, "type", "", "Type")
	cmd.Flags().StringVar(&flagTopic, "topic", "", "Topic")
	cmd.Flags().StringVar(&flagMetadata, "metadata", "", "Metadata")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
