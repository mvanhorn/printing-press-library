// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTradingPostmortemsUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagId string
	var flagTradeDate string
	var flagClosedAt string
	var flagPremortemId string
	var flagTicker string
	var flagSetupType string
	var flagEntryTime string
	var flagEntryPrice string
	var flagStopPrice string
	var flagTargetPrice string
	var flagExitTime string
	var flagExitPrice string
	var flagShares string
	var flagGrossPnl string
	var flagFees string
	var flagNetPnl string
	var flagOutcome string
	var flagConviction string
	var flagCatalyst string
	var flagRulesViolated string
	var flagNotes string
	var flagAccountId string
	var flagAccountMode string
	var flagSide string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Sentinel v1.1 Wave 2: closed-trade outcome + lessons; mirrors state/postmortem-*.jsonl; FK to trading_premortems for...",
		Example:     "  brainos-pp-cli trading-postmortems update",
		Annotations: map[string]string{"pp:endpoint": "trading-postmortems.update", "pp:method": "PATCH", "pp:path": "/trading_postmortems"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/trading_postmortems"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
			}
			params := map[string]string{}
			if flagId != "" {
				params["id"] = fmt.Sprintf("%v", flagId)
			}
			if flagTradeDate != "" {
				params["trade_date"] = fmt.Sprintf("%v", flagTradeDate)
			}
			if flagClosedAt != "" {
				params["closed_at"] = fmt.Sprintf("%v", flagClosedAt)
			}
			if flagPremortemId != "" {
				params["premortem_id"] = fmt.Sprintf("%v", flagPremortemId)
			}
			if flagTicker != "" {
				params["ticker"] = fmt.Sprintf("%v", flagTicker)
			}
			if flagSetupType != "" {
				params["setup_type"] = fmt.Sprintf("%v", flagSetupType)
			}
			if flagEntryTime != "" {
				params["entry_time"] = fmt.Sprintf("%v", flagEntryTime)
			}
			if flagEntryPrice != "" {
				params["entry_price"] = fmt.Sprintf("%v", flagEntryPrice)
			}
			if flagStopPrice != "" {
				params["stop_price"] = fmt.Sprintf("%v", flagStopPrice)
			}
			if flagTargetPrice != "" {
				params["target_price"] = fmt.Sprintf("%v", flagTargetPrice)
			}
			if flagExitTime != "" {
				params["exit_time"] = fmt.Sprintf("%v", flagExitTime)
			}
			if flagExitPrice != "" {
				params["exit_price"] = fmt.Sprintf("%v", flagExitPrice)
			}
			if flagShares != "" {
				params["shares"] = fmt.Sprintf("%v", flagShares)
			}
			if flagGrossPnl != "" {
				params["gross_pnl"] = fmt.Sprintf("%v", flagGrossPnl)
			}
			if flagFees != "" {
				params["fees"] = fmt.Sprintf("%v", flagFees)
			}
			if flagNetPnl != "" {
				params["net_pnl"] = fmt.Sprintf("%v", flagNetPnl)
			}
			if flagOutcome != "" {
				params["outcome"] = fmt.Sprintf("%v", flagOutcome)
			}
			if flagConviction != "" {
				params["conviction"] = fmt.Sprintf("%v", flagConviction)
			}
			if flagCatalyst != "" {
				params["catalyst"] = fmt.Sprintf("%v", flagCatalyst)
			}
			if flagRulesViolated != "" {
				params["rules_violated"] = fmt.Sprintf("%v", flagRulesViolated)
			}
			if flagNotes != "" {
				params["notes"] = fmt.Sprintf("%v", flagNotes)
			}
			if flagAccountId != "" {
				params["account_id"] = fmt.Sprintf("%v", flagAccountId)
			}
			if flagAccountMode != "" {
				params["account_mode"] = fmt.Sprintf("%v", flagAccountMode)
			}
			if flagSide != "" {
				params["side"] = fmt.Sprintf("%v", flagSide)
			}
			data, statusCode, err := c.PatchWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "trading-postmortems",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagId, "id", "", "Id")
	cmd.Flags().StringVar(&flagTradeDate, "trade-date", "", "Trade date")
	cmd.Flags().StringVar(&flagClosedAt, "closed-at", "", "Closed at")
	cmd.Flags().StringVar(&flagPremortemId, "premortem-id", "", "Premortem id")
	cmd.Flags().StringVar(&flagTicker, "ticker", "", "Ticker")
	cmd.Flags().StringVar(&flagSetupType, "setup-type", "", "Setup type")
	cmd.Flags().StringVar(&flagEntryTime, "entry-time", "", "Entry time")
	cmd.Flags().StringVar(&flagEntryPrice, "entry-price", "", "Entry price")
	cmd.Flags().StringVar(&flagStopPrice, "stop-price", "", "Stop price")
	cmd.Flags().StringVar(&flagTargetPrice, "target-price", "", "Target price")
	cmd.Flags().StringVar(&flagExitTime, "exit-time", "", "Exit time")
	cmd.Flags().StringVar(&flagExitPrice, "exit-price", "", "Exit price")
	cmd.Flags().StringVar(&flagShares, "shares", "", "Shares")
	cmd.Flags().StringVar(&flagGrossPnl, "gross-pnl", "", "Gross pnl")
	cmd.Flags().StringVar(&flagFees, "fees", "", "Fees")
	cmd.Flags().StringVar(&flagNetPnl, "net-pnl", "", "Net pnl")
	cmd.Flags().StringVar(&flagOutcome, "outcome", "", "Outcome")
	cmd.Flags().StringVar(&flagConviction, "conviction", "", "Conviction")
	cmd.Flags().StringVar(&flagCatalyst, "catalyst", "", "Catalyst")
	cmd.Flags().StringVar(&flagRulesViolated, "rules-violated", "", "Rules violated")
	cmd.Flags().StringVar(&flagNotes, "notes", "", "Notes")
	cmd.Flags().StringVar(&flagAccountId, "account-id", "", "Account id")
	cmd.Flags().StringVar(&flagAccountMode, "account-mode", "", "Account mode")
	cmd.Flags().StringVar(&flagSide, "side", "", "Side")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
