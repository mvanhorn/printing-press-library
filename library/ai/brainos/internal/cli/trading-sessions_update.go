// Copyright 2026 devin. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newTradingSessionsUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagTradeDate string
	var flagSessionStart string
	var flagSessionEnd string
	var flagAccountMode string
	var flagTradesTaken string
	var flagWins string
	var flagLosses string
	var flagBreakevens string
	var flagGrossPnl string
	var flagNetPnl string
	var flagAccountPctChange string
	var flagRulesViolatedCount string
	var flagRegime string
	var flagOperatorState string
	var flagHaltActive string
	var flagHaltReason string
	var flagNotes string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Sentinel v1.1 Wave 2: per-day rollup of trades + operator state + halt status; mirrors state/session-summary-*.txt;...",
		Example:     "  brainos-pp-cli trading-sessions update",
		Annotations: map[string]string{"pp:endpoint": "trading-sessions.update", "pp:method": "PATCH", "pp:path": "/trading_sessions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/trading_sessions"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
			}
			params := map[string]string{}
			if flagTradeDate != "" {
				params["trade_date"] = fmt.Sprintf("%v", flagTradeDate)
			}
			if flagSessionStart != "" {
				params["session_start"] = fmt.Sprintf("%v", flagSessionStart)
			}
			if flagSessionEnd != "" {
				params["session_end"] = fmt.Sprintf("%v", flagSessionEnd)
			}
			if flagAccountMode != "" {
				params["account_mode"] = fmt.Sprintf("%v", flagAccountMode)
			}
			if flagTradesTaken != "" {
				params["trades_taken"] = fmt.Sprintf("%v", flagTradesTaken)
			}
			if flagWins != "" {
				params["wins"] = fmt.Sprintf("%v", flagWins)
			}
			if flagLosses != "" {
				params["losses"] = fmt.Sprintf("%v", flagLosses)
			}
			if flagBreakevens != "" {
				params["breakevens"] = fmt.Sprintf("%v", flagBreakevens)
			}
			if flagGrossPnl != "" {
				params["gross_pnl"] = fmt.Sprintf("%v", flagGrossPnl)
			}
			if flagNetPnl != "" {
				params["net_pnl"] = fmt.Sprintf("%v", flagNetPnl)
			}
			if flagAccountPctChange != "" {
				params["account_pct_change"] = fmt.Sprintf("%v", flagAccountPctChange)
			}
			if flagRulesViolatedCount != "" {
				params["rules_violated_count"] = fmt.Sprintf("%v", flagRulesViolatedCount)
			}
			if flagRegime != "" {
				params["regime"] = fmt.Sprintf("%v", flagRegime)
			}
			if flagOperatorState != "" {
				params["operator_state"] = fmt.Sprintf("%v", flagOperatorState)
			}
			if flagHaltActive != "" {
				params["halt_active"] = fmt.Sprintf("%v", flagHaltActive)
			}
			if flagHaltReason != "" {
				params["halt_reason"] = fmt.Sprintf("%v", flagHaltReason)
			}
			if flagNotes != "" {
				params["notes"] = fmt.Sprintf("%v", flagNotes)
			}
			data, statusCode, err := c.PatchWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "trading-sessions",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagTradeDate, "trade-date", "", "Trade date")
	cmd.Flags().StringVar(&flagSessionStart, "session-start", "", "Session start")
	cmd.Flags().StringVar(&flagSessionEnd, "session-end", "", "Session end")
	cmd.Flags().StringVar(&flagAccountMode, "account-mode", "", "Account mode")
	cmd.Flags().StringVar(&flagTradesTaken, "trades-taken", "", "Trades taken")
	cmd.Flags().StringVar(&flagWins, "wins", "", "Wins")
	cmd.Flags().StringVar(&flagLosses, "losses", "", "Losses")
	cmd.Flags().StringVar(&flagBreakevens, "breakevens", "", "Breakevens")
	cmd.Flags().StringVar(&flagGrossPnl, "gross-pnl", "", "Gross pnl")
	cmd.Flags().StringVar(&flagNetPnl, "net-pnl", "", "Net pnl")
	cmd.Flags().StringVar(&flagAccountPctChange, "account-pct-change", "", "Account pct change")
	cmd.Flags().StringVar(&flagRulesViolatedCount, "rules-violated-count", "", "Rules violated count")
	cmd.Flags().StringVar(&flagRegime, "regime", "", "Regime")
	cmd.Flags().StringVar(&flagOperatorState, "operator-state", "", "Operator state")
	cmd.Flags().StringVar(&flagHaltActive, "halt-active", "", "Halt active")
	cmd.Flags().StringVar(&flagHaltReason, "halt-reason", "", "Halt reason")
	cmd.Flags().StringVar(&flagNotes, "notes", "", "Notes")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
