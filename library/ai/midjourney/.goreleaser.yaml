version: 2
project_name: midjourney-pp-cli
changelog:
  disable: true
builds:
  - id: midjourney-pp-cli
    main: ./cmd/midjourney-pp-cli
    binary: midjourney-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/ai/midjourney/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: midjourney-pp-mcp
    main: ./cmd/midjourney-pp-mcp
    binary: midjourney-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: midjourney-pp-cli
    repository:
      owner: mvanhorn
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Inspect Midjourney jobs, queue, folders, and discovery feeds from the terminal"
    install: |
      bin.install "midjourney-pp-cli"
      bin.install "midjourney-pp-mcp"
