// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/ai/ollama-cloud/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/ai/ollama-cloud/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/ai/ollama-cloud/internal/client"
	"github.com/mvanhorn/printing-press-library/library/ai/ollama-cloud/internal/config"
	"github.com/mvanhorn/printing-press-library/library/ai/ollama-cloud/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/ai/ollama-cloud/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("chat_chat",
			mcplib.WithDescription("Native Ollama chat endpoint. Supports streaming. Required: messages, model. Optional: format, keep_alive, options (plus 2 more). Returns the new ChatResponse."),
			mcplib.WithString("format", mcplib.Description("Format")),
			mcplib.WithString("keep_alive", mcplib.Description("Keep alive")),
			mcplib.WithString("messages", mcplib.Required(), mcplib.Description("Messages")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("Model")),
			mcplib.WithString("options", mcplib.Description("Generation parameters")),
			mcplib.WithString("stream", mcplib.Description("Stream")),
			mcplib.WithString("tools", mcplib.Description("Tools")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/chat", []mcpParamBinding{{PublicName: "format", WireName: "format", Location: "body"},{PublicName: "keep_alive", WireName: "keep_alive", Location: "body"},{PublicName: "messages", WireName: "messages", Location: "body"},{PublicName: "model", WireName: "model", Location: "body"},{PublicName: "options", WireName: "options", Location: "body"},{PublicName: "stream", WireName: "stream", Location: "body"},{PublicName: "tools", WireName: "tools", Location: "body"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("chat_completions",
			mcplib.WithDescription("OpenAI-compatible chat completions endpoint. Required: messages, model. Optional: frequency_penalty, max_tokens, n (default: 1) (plus 8 more). Returns the new OpenAichatResponse."),
			mcplib.WithString("frequency_penalty", mcplib.Description("Frequency penalty")),
			mcplib.WithString("max_tokens", mcplib.Description("Max tokens")),
			mcplib.WithString("messages", mcplib.Required(), mcplib.Description("Messages")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("Model")),
			mcplib.WithString("n", mcplib.Description("N")),
			mcplib.WithString("presence_penalty", mcplib.Description("Presence penalty")),
			mcplib.WithString("seed", mcplib.Description("Seed")),
			mcplib.WithString("stop", mcplib.Description("Stop")),
			mcplib.WithString("stream", mcplib.Description("Stream")),
			mcplib.WithString("temperature", mcplib.Description("Temperature")),
			mcplib.WithString("tool_choice", mcplib.Description("Tool choice")),
			mcplib.WithString("tools", mcplib.Description("Tools")),
			mcplib.WithString("top_p", mcplib.Description("Top p")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/chat/completions", []mcpParamBinding{{PublicName: "frequency_penalty", WireName: "frequency_penalty", Location: "body"},{PublicName: "max_tokens", WireName: "max_tokens", Location: "body"},{PublicName: "messages", WireName: "messages", Location: "body"},{PublicName: "model", WireName: "model", Location: "body"},{PublicName: "n", WireName: "n", Location: "body"},{PublicName: "presence_penalty", WireName: "presence_penalty", Location: "body"},{PublicName: "seed", WireName: "seed", Location: "body"},{PublicName: "stop", WireName: "stop", Location: "body"},{PublicName: "stream", WireName: "stream", Location: "body"},{PublicName: "temperature", WireName: "temperature", Location: "body"},{PublicName: "tool_choice", WireName: "tool_choice", Location: "body"},{PublicName: "tools", WireName: "tools", Location: "body"},{PublicName: "top_p", WireName: "top_p", Location: "body"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("embeddings_embed",
			mcplib.WithDescription("Native Ollama embeddings endpoint. Required: model, prompt. Optional: keep_alive, options. Returns the new EmbeddingsResponse."),
			mcplib.WithString("keep_alive", mcplib.Description("Keep alive")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("Model")),
			mcplib.WithString("options", mcplib.Description("Generation parameters")),
			mcplib.WithString("prompt", mcplib.Required(), mcplib.Description("Prompt")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/embeddings", []mcpParamBinding{{PublicName: "keep_alive", WireName: "keep_alive", Location: "body"},{PublicName: "model", WireName: "model", Location: "body"},{PublicName: "options", WireName: "options", Location: "body"},{PublicName: "prompt", WireName: "prompt", Location: "body"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("embeddings_openai-embed",
			mcplib.WithDescription("Generate embeddings (OpenAI-compatible). Required: input, model. Optional: dimensions, encoding_format (default: float). Returns array of EmbeddingsOpenaiEmbedItem."),
			mcplib.WithString("dimensions", mcplib.Description("Dimensions")),
			mcplib.WithString("encoding_format", mcplib.Description("Encoding format")),
			mcplib.WithString("input", mcplib.Required(), mcplib.Description("Input")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("Model")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/v1/embeddings", []mcpParamBinding{{PublicName: "dimensions", WireName: "dimensions", Location: "body"},{PublicName: "encoding_format", WireName: "encoding_format", Location: "body"},{PublicName: "input", WireName: "input", Location: "body"},{PublicName: "model", WireName: "model", Location: "body"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("models_models",
			mcplib.WithDescription("Catalog in OpenAI list-models format. Returns array of OpenAimodel."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/v1/models", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("ps_ps",
			mcplib.WithDescription("Shows currently-loaded models. On Ollama Cloud this typically reflects models with active sessions. Returns the PsResponse."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/ps", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("show_show",
			mcplib.WithDescription("Returns model metadata, template, modelfile, capabilities. Required: name. Optional: verbose (default: false)."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithString("verbose", mcplib.Description("Verbose")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/show", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body"},{PublicName: "verbose", WireName: "verbose", Location: "body"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("tags_tags",
			mcplib.WithDescription("Returns the live catalog of hosted Ollama Cloud models."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/tags", []mcpParamBinding{ }, []string{ }),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export OLLAMA_CLOUD_API_KEY=<your-key>" +
					"\n      Run 'ollama-cloud-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export OLLAMA_CLOUD_API_KEY=<your-key>" +
					"\n      Run 'ollama-cloud-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export OLLAMA_CLOUD_API_KEY=<your-key>" +
					"\n      Run 'ollama-cloud-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "ollama-cloud-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "ollama-cloud-pp-cli", "data.db")
}
// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "ollama-cloud",
		"description": "Routes every prompt to the right hosted Ollama model. Wraps chat, embeddings, catalog, and the OpenAI-compatible...",
		"archetype":   "communication",
		"tool_count":  8,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion ollama-cloud-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name": "OLLAMA_CLOUD_API_KEY",
					"kind": "per_call",
					"required": true,
					"sensitive": true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name": "chat",
				"description": "Manage chat",
				"endpoints": []string{"chat", "completions",  },
				"searchable": true,
			},
			{
				"name": "embeddings",
				"description": "Manage embeddings",
				"endpoints": []string{"embed", "openai-embed",  },
				"searchable": true,
			},
			{
				"name": "models",
				"description": "Manage models",
				"endpoints": []string{"models",  },
				"syncable": true,
			},
			{
				"name": "ps",
				"description": "Manage ps",
				"endpoints": []string{"ps",  },
				"syncable": true,
			},
			{
				"name": "show",
				"description": "Manage show",
				"endpoints": []string{"show",  },
				"searchable": true,
			},
			{
				"name": "tags",
				"description": "Manage tags",
				"endpoints": []string{"tags",  },
				"syncable": true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Model advisor", "command": "advise", "description": "Picks the right Ollama Cloud model for a prompt by combining live catalog, heuristic prompt-feature extraction,...", "rationale": "Requires correlating live catalog state with prompt features, session context, budget, and curated per-model...", "via": "mcp-command-mirror"},
			{"name": "Side-by-side comparison", "command": "compare", "description": "Runs the same prompt against N hosted models in parallel and emits side-by-side response, tokens, and latency.", "rationale": "Requires the same portable per-model metadata that powers the advisor, plus parallel chat dispatch with latency+token accounting...", "via": "mcp-command-mirror"},
			{"name": "Advisor explain", "command": "advise", "description": "With --explain, advise emits the full scoring trace: feature extraction, per-model scores, filter passes, tiebreak...", "rationale": "Only possible because advise is heuristic-first and auditable; an LLM-only router can't show its work.", "via": "mcp-command-mirror"},
			{"name": "Advisor replay", "command": "advise-replay", "description": "Replays advisor recommendations and reports divergence between recommended models and actually-chosen models. Foundation for the divergence canary.", "rationale": "Requires the advisor log (we own) plus catalog metadata (we own); no other tool persists both.", "via": "mcp-command-mirror"},
			{"name": "Budget probe", "command": "budget", "description": "Probes the free-tier weekly cap with a 1-token chat. Parses Ollama Cloud's 429 prose and emits a structured verdict...", "rationale": "Requires recognising Ollama Cloud's specific 429 envelope ('you have reached your weekly usage limit') whether it...", "via": "mcp-command-mirror"},
			{"name": "Cost trace", "command": "cost-trace", "description": "Aggregates advisor-log cost estimates over a time window; compares per-model and per-task-hint spend.", "rationale": "Requires the advisor log (we own) plus portable cost metadata; no other tool persists both.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Model advisor", "insight": "Requires correlating live catalog state with prompt features, session context, budget, and curated per-model metadata that no other tool exposes in a single decision."},
			{"topic": "Side-by-side comparison", "insight": "Requires the same portable per-model metadata that powers the advisor, plus parallel chat dispatch with latency+token tracking."},
			{"topic": "Advisor explain", "insight": "Only possible because advise is heuristic-first and auditable; an LLM-only router can't show its work."},
			{"topic": "Advisor replay", "insight": "Requires the advisor log (we own) plus catalog metadata (we own); no other tool persists both."},
			{"topic": "Budget probe", "insight": "Requires recognising Ollama Cloud's specific 429 envelope (\"you have reached your weekly usage limit\") whether it surfaces as HTTP status 429 or post-retry error prose."},
			{"topic": "Cost trace", "insight": "Requires the advisor log (we own) plus portable cost metadata; no other tool persists both."},
			{"topic": "Message search", "insight": "Use the search tool on synced data rather than paginating through message history. Message APIs often have aggressive rate limits."},
			{"topic": "Channel health", "insight": "When analyzing channel activity, use the channel-health command or sql aggregation on synced messages. Don't iterate individual messages via API."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
