// Copyright 2026 Som Samantray and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newVectorsUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagXPineconeApiVersion string
	var bodyDryRun bool
	var bodyFilter string
	var bodyId string
	var bodyNamespace string
	var bodySetMetadata string
	var bodySparseValuesIndices string
	var bodySparseValuesValues string
	var bodyValues string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "update",
		Short: "Update records by ID or by metadata in a namespace.",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  pinecone-pp-cli vectors update --x-pinecone-api-version example-value",
		Annotations: map[string]string{"pp:endpoint": "vectors.update", "pp:method": "POST", "pp:path": "https://{index_host}/vectors/update"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if (cmd.Flags().Changed("sparse-values-indices") || bodySparseValuesIndices != "") || (cmd.Flags().Changed("sparse-values-values") || bodySparseValuesValues != "") {
					if !cmd.Flags().Changed("sparse-values-indices") && !flags.dryRun {
						return fmt.Errorf("required flag \"%s\" not set", "sparse-values-indices")
					}
					if !cmd.Flags().Changed("sparse-values-values") && !flags.dryRun {
						return fmt.Errorf("required flag \"%s\" not set", "sparse-values-values")
					}
				}
			}
			path := "https://{index_host}/vectors/update"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			headerOverrides := map[string]string{}

			if cmd.Flags().Changed("x-pinecone-api-version") || flagXPineconeApiVersion != "" {
				headerOverrides["X-Pinecone-Api-Version"] = formatCLIParamValue(flagXPineconeApiVersion)
			}

			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("dry-run") {
					bodyMap["dryRun"] = bodyDryRun
				}
				if cmd.Flags().Changed("filter") || bodyFilter != "" {
					var parsedFilter any
					if err := json.Unmarshal([]byte(bodyFilter), &parsedFilter); err != nil {
						return fmt.Errorf("parsing --filter JSON: %w", err)
					}
					asMap, ok := parsedFilter.(map[string]any)
					if !ok {
						return fmt.Errorf("--filter must be a JSON object, got JSON %T", parsedFilter)
					}
					bodyMap["filter"] = asMap
				}
				if cmd.Flags().Changed("id") || bodyId != "" {
					bodyMap["id"] = bodyId
				}
				if cmd.Flags().Changed("namespace") || bodyNamespace != "" {
					bodyMap["namespace"] = bodyNamespace
				}
				if cmd.Flags().Changed("set-metadata") || bodySetMetadata != "" {
					var parsedSetMetadata any
					if err := json.Unmarshal([]byte(bodySetMetadata), &parsedSetMetadata); err != nil {
						return fmt.Errorf("parsing --set-metadata JSON: %w", err)
					}
					asMap, ok := parsedSetMetadata.(map[string]any)
					if !ok {
						return fmt.Errorf("--set-metadata must be a JSON object, got JSON %T", parsedSetMetadata)
					}
					bodyMap["setMetadata"] = asMap
				}
				{
					nestedSparseValues := map[string]any{}
					if cmd.Flags().Changed("sparse-values-indices") || bodySparseValuesIndices != "" {
						var parsedSparseValuesIndices any
						if err := json.Unmarshal([]byte(bodySparseValuesIndices), &parsedSparseValuesIndices); err != nil {
							return fmt.Errorf("parsing --sparse-values-indices JSON: %w", err)
						}
						asArray, ok := parsedSparseValuesIndices.([]any)
						if !ok {
							return fmt.Errorf("--sparse-values-indices must be a JSON array, got JSON %T", parsedSparseValuesIndices)
						}
						nestedSparseValues["indices"] = asArray
					}
					if cmd.Flags().Changed("sparse-values-values") || bodySparseValuesValues != "" {
						var parsedSparseValuesValues any
						if err := json.Unmarshal([]byte(bodySparseValuesValues), &parsedSparseValuesValues); err != nil {
							return fmt.Errorf("parsing --sparse-values-values JSON: %w", err)
						}
						asArray, ok := parsedSparseValuesValues.([]any)
						if !ok {
							return fmt.Errorf("--sparse-values-values must be a JSON array, got JSON %T", parsedSparseValuesValues)
						}
						nestedSparseValues["values"] = asArray
					}
					if len(nestedSparseValues) > 0 {
						bodyMap["sparseValues"] = nestedSparseValues
					}
				}
				if cmd.Flags().Changed("values") || bodyValues != "" {
					var parsedValues any
					if err := json.Unmarshal([]byte(bodyValues), &parsedValues); err != nil {
						return fmt.Errorf("parsing --values JSON: %w", err)
					}
					asArray, ok := parsedValues.([]any)
					if !ok {
						return fmt.Errorf("--values must be a JSON array, got JSON %T", parsedValues)
					}
					bodyMap["values"] = asArray
				}
			}
			data, statusCode, err := c.PostWithParamsAndHeaders(cmd.Context(), path, params, body, headerOverrides)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "vectors", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "vectors", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "vectors", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "vectors", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "vectors", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "vectors",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "vectors", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "vectors", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagXPineconeApiVersion, "x-pinecone-api-version", "2026-04", "Required date-based version header")
	cmd.Flags().BoolVar(&bodyDryRun, "dry-run", false, "If `true`, return the number of records that match the `filter`, but do not execute the update. Default is `false`.")
	cmd.Flags().StringVar(&bodyFilter, "filter", "", "A metadata filter expression.")
	cmd.Flags().StringVar(&bodyId, "id", "", "Vector's unique id.")
	cmd.Flags().StringVar(&bodyNamespace, "namespace", "", "The namespace containing the record to update.")
	cmd.Flags().StringVar(&bodySetMetadata, "set-metadata", "", "Metadata to set for the record.")
	cmd.Flags().StringVar(&bodySparseValuesIndices, "sparse-values-indices", "", "The indices of the sparse data.")
	cmd.Flags().StringVar(&bodySparseValuesValues, "sparse-values-values", "", "The corresponding values of the sparse data, which must be with the same length as the indices.")
	cmd.Flags().StringVar(&bodyValues, "values", "", "Vector data.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
