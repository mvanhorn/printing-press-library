// Copyright 2026 Anchal Sharma and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
	"github.com/mvanhorn/printing-press-library/library/ai/valyu/internal/cliutil"
)

func newWorkflowsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyDescription string
	var bodyIcon string
	var bodySlug string
	var bodySubtitle string
	var bodyTags string
	var bodyTitle string
	var bodyVersionChangelog string
	var bodyVersionDeliverables string
	var bodyVersionEstimatedTime string
	var bodyVersionOutputFormats string
	var bodyVersionPrompt string
	var bodyVersionRecommendedMode string
	var bodyVersionReportFormat string
	var bodyVersionStrategy string
	var bodyVersionToolsBrowserUse bool
	var bodyVersionToolsCharts bool
	var bodyVersionToolsCodeExecution bool
	var bodyVersionToolsScreenshots bool
	var bodyVersionVariables string
	var bodyVertical string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "**Beta.** Create a private workflow owned by your organization. Subject to your org's workflow quota (default 100).",
		Example:     "  valyu-pp-cli workflows create --slug example-value",
		Annotations: map[string]string{"pp:endpoint": "workflows.create", "pp:method": "POST", "pp:path": "/v1/workflows"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("slug") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "slug")
				}
				if !cmd.Flags().Changed("title") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "title")
				}
				if !cmd.Flags().Changed("version-prompt") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "version-prompt")
				}
				if !cmd.Flags().Changed("version-report-format") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "version-report-format")
				}
				if !cmd.Flags().Changed("version-strategy") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "version-strategy")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/workflows"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyIcon != "" {
					body["icon"] = bodyIcon
				}
				if bodySlug != "" {
					body["slug"] = bodySlug
				}
				if bodySubtitle != "" {
					body["subtitle"] = bodySubtitle
				}
				if bodyTags != "" {
					body["tags"] = cliutil.SplitCSV(bodyTags)
				}
				if bodyTitle != "" {
					body["title"] = bodyTitle
				}
				{
					nestedVersion := map[string]any{}
					if bodyVersionChangelog != "" {
						nestedVersion["changelog"] = bodyVersionChangelog
					}
					if bodyVersionDeliverables != "" {
						var parsedVersionDeliverables any
						if err := json.Unmarshal([]byte(bodyVersionDeliverables), &parsedVersionDeliverables); err != nil {
							return fmt.Errorf("parsing --version-deliverables JSON: %w", err)
						}
						nestedVersion["deliverables"] = parsedVersionDeliverables
					}
					if bodyVersionEstimatedTime != "" {
						nestedVersion["estimated_time"] = bodyVersionEstimatedTime
					}
					if bodyVersionOutputFormats != "" {
						var parsedVersionOutputFormats any
						if err := json.Unmarshal([]byte(bodyVersionOutputFormats), &parsedVersionOutputFormats); err != nil {
							return fmt.Errorf("parsing --version-output-formats JSON: %w", err)
						}
						nestedVersion["output_formats"] = parsedVersionOutputFormats
					}
					if bodyVersionPrompt != "" {
						nestedVersion["prompt"] = bodyVersionPrompt
					}
					if bodyVersionRecommendedMode != "" {
						nestedVersion["recommended_mode"] = bodyVersionRecommendedMode
					}
					if bodyVersionReportFormat != "" {
						nestedVersion["report_format"] = bodyVersionReportFormat
					}
					if bodyVersionStrategy != "" {
						nestedVersion["strategy"] = bodyVersionStrategy
					}
					{
						nestedVersionTools := map[string]any{}
						if cmd.Flags().Changed("version-tools-browser-use") {
							nestedVersionTools["browser_use"] = bodyVersionToolsBrowserUse
						}
						if cmd.Flags().Changed("version-tools-charts") {
							nestedVersionTools["charts"] = bodyVersionToolsCharts
						}
						if cmd.Flags().Changed("version-tools-code-execution") {
							nestedVersionTools["code_execution"] = bodyVersionToolsCodeExecution
						}
						if cmd.Flags().Changed("version-tools-screenshots") {
							nestedVersionTools["screenshots"] = bodyVersionToolsScreenshots
						}
						if len(nestedVersionTools) > 0 {
							nestedVersion["tools"] = nestedVersionTools
						}
					}
					if bodyVersionVariables != "" {
						var parsedVersionVariables any
						if err := json.Unmarshal([]byte(bodyVersionVariables), &parsedVersionVariables); err != nil {
							return fmt.Errorf("parsing --version-variables JSON: %w", err)
						}
						nestedVersion["variables"] = parsedVersionVariables
					}
					if len(nestedVersion) > 0 {
						body["version"] = nestedVersion
					}
				}
				if bodyVertical != "" {
					body["vertical"] = bodyVertical
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "workflows", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "workflows", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "workflows", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "workflows", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "workflows", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "workflows",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "workflows", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "workflows", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyIcon, "icon", "", "Icon")
	cmd.Flags().StringVar(&bodySlug, "slug", "", "Unique slug within your org.")
	cmd.Flags().StringVar(&bodySubtitle, "subtitle", "", "Subtitle")
	cmd.Flags().StringVar(&bodyTags, "tags", "", "Tags")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "Title")
	cmd.Flags().StringVar(&bodyVersionChangelog, "version-changelog", "", "Notes describing what changed. Required when publishing a new version on update.")
	cmd.Flags().StringVar(&bodyVersionDeliverables, "version-deliverables", "", "Deliverables produced by the workflow. Up to 20.")
	cmd.Flags().StringVar(&bodyVersionEstimatedTime, "version-estimated-time", "", "Estimated time")
	cmd.Flags().StringVar(&bodyVersionOutputFormats, "version-output-formats", "", "Output formats the workflow produces.")
	cmd.Flags().StringVar(&bodyVersionPrompt, "version-prompt", "", "Prompt template with `{key}` placeholders. Up to 64 KB.")
	cmd.Flags().StringVar(&bodyVersionRecommendedMode, "version-recommended-mode", "", "Recommended DeepResearch mode for a workflow. Maps directly to the DeepResearch `mode` parameter.")
	cmd.Flags().StringVar(&bodyVersionReportFormat, "version-report-format", "", "Report format instructions. Maps to DeepResearch `report_format`. Up to 64 KB.")
	cmd.Flags().StringVar(&bodyVersionStrategy, "version-strategy", "", "Research strategy. Maps to DeepResearch `research_strategy`. Up to 64 KB.")
	cmd.Flags().BoolVar(&bodyVersionToolsBrowserUse, "version-tools-browser-use", false, "Enable autonomous browser sessions.")
	cmd.Flags().BoolVar(&bodyVersionToolsCharts, "version-tools-charts", false, "Enable chart and graph generation in the report.")
	cmd.Flags().BoolVar(&bodyVersionToolsCodeExecution, "version-tools-code-execution", false, "Enable code execution in a sandboxed environment.")
	cmd.Flags().BoolVar(&bodyVersionToolsScreenshots, "version-tools-screenshots", false, "Enable visual screenshot capture of web pages.")
	cmd.Flags().StringVar(&bodyVersionVariables, "version-variables", "", "Typed template variables. Up to 50.")
	cmd.Flags().StringVar(&bodyVertical, "vertical", "", "Vertical")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
