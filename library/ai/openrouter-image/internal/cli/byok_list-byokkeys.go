// Copyright 2026 neal-kyle and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newByokListByokkeysCmd(flags *rootFlags) *cobra.Command {
	var flagOffset string
	var flagLimit int
	var flagWorkspaceId string
	var flagProvider string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list-byokkeys",
		Aliases:     []string{"list"},
		Short:       "List the bring-your-own-key (BYOK) provider credentials for the authenticated entity's default workspace.",
		Example:     "  openrouter-image-pp-cli byok list-byokkeys",
		Annotations: map[string]string{"pp:endpoint": "byok.list-byokkeys", "pp:method": "GET", "pp:path": "/byok", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("provider") {
				allowedProvider := []string{"ai21", "aion-labs", "akashml", "alibaba", "amazon-bedrock", "amazon-nova", "ambient", "anthropic", "arcee-ai", "atlas-cloud", "avian", "azure", "baidu", "baseten", "black-forest-labs", "byteplus", "cerebras", "chutes", "cirrascale", "clarifai", "cloudflare", "cohere", "coreweave", "crusoe", "darkbloom", "decart", "deepgram", "deepinfra", "deepseek", "dekallm", "digitalocean", "featherless", "fireworks", "fish-audio", "friendli", "gmicloud", "google-ai-studio", "google-vertex", "groq", "heygen", "inception", "inceptron", "inferact-vllm", "inference-net", "infermatic", "inflection", "io-net", "ionstream", "krea", "liquid", "mancer", "mara", "meta", "minimax", "mistral", "modal", "modelrun", "modular", "moonshotai", "morph", "ncompass", "nebius", "nex-agi", "nextbit", "novita", "nvidia", "open-inference", "openai", "parasail", "perceptron", "perplexity", "phala", "poolside", "quiver", "recraft", "reka", "relace", "runway", "sail-research", "sakana", "sakana-ai", "sambanova", "seed", "siliconflow", "sourceful", "stepfun", "streamlake", "switchpoint", "tencent", "tenstorrent", "thinkingmachines", "together", "upstage", "venice", "wafer", "wandb", "wandb-legacy", "xai", "xiaomi", "z-ai"}
				validProvider := false
				for _, v := range allowedProvider {
					if flagProvider == v {
						validProvider = true
						break
					}
				}
				if !validProvider {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagProvider, "provider", allowedProvider)
				}
			}
			path := "/byok"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "byok", path, map[string]string{
				"offset":       formatCLIParamValue(flagOffset),
				"limit":        formatCLIParamValue(flagLimit),
				"workspace_id": formatCLIParamValue(flagWorkspaceId),
				"provider":     formatCLIParamValue(flagProvider),
			}, nil, flagAll, "offset", "offset", "limit", 50, "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagOffset, "offset", "0", "Number of records to skip for pagination")
	cmd.Flags().IntVar(&flagLimit, "limit", 50, "Maximum number of records to return (max 100)")
	cmd.Flags().StringVar(&flagWorkspaceId, "workspace-id", "", "Optional workspace ID to filter by. Defaults to the authenticated entity's default workspace.")
	cmd.Flags().StringVar(&flagProvider, "provider", "", "Optional provider slug to filter by (e.g. `openai`, `anthropic`, `amazon-bedrock`). (one of: ai21, aion-labs, akashml, alibaba, amazon-bedrock, amazon-nova, ambient, anthropic, arcee-ai, atlas-cloud, avian, azure, baidu, baseten, black-forest-labs, byteplus, cerebras, chutes, cirrascale, clarifai, cloudflare, cohere, coreweave, crusoe, darkbloom, decart, deepgram, deepinfra, deepseek, dekallm, digitalocean, featherless, fireworks, fish-audio, friendli, gmicloud, google-ai-studio, google-vertex, groq, heygen, inception, inceptron, inferact-vllm, inference-net, infermatic, inflection, io-net, ionstream, krea, liquid, mancer, mara, meta, minimax, mistral, modal, modelrun, modular, moonshotai, morph, ncompass, nebius, nex-agi, nextbit, novita, nvidia, open-inference, openai, parasail, perceptron, perplexity, phala, poolside, quiver, recraft, reka, relace, runway, sail-research, sakana, sakana-ai, sambanova, seed, siliconflow, sourceful, stepfun, streamlake, switchpoint, tencent, tenstorrent, thinkingmachines, together, upstage, venice, wafer, wandb, wandb-legacy, xai, xiaomi, z-ai)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
