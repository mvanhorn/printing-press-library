// Copyright 2026 neal-kyle and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newWorkspacesCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyDefaultImageModel string
	var bodyDefaultProviderSort string
	var bodyDefaultTextModel string
	var bodyDescription string
	var bodyIoLoggingApiKeyIds string
	var bodyIoLoggingSamplingRate float64
	var bodyIsDataDiscountLoggingEnabled bool
	var bodyIsObservabilityBroadcastEnabled bool
	var bodyIsObservabilityIoLoggingEnabled bool
	var bodyName string
	var bodySlug string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create a new workspace for the authenticated user.",
		Example:     "  openrouter-image-pp-cli workspaces create --name Production",
		Annotations: map[string]string{"pp:endpoint": "workspaces.create", "pp:method": "POST", "pp:path": "/workspaces"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("slug") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "slug")
				}
			}
			path := "/workspaces"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if bodyDefaultImageModel != "" {
					bodyMap["default_image_model"] = bodyDefaultImageModel
				}
				if bodyDefaultProviderSort != "" {
					bodyMap["default_provider_sort"] = bodyDefaultProviderSort
				}
				if bodyDefaultTextModel != "" {
					bodyMap["default_text_model"] = bodyDefaultTextModel
				}
				if bodyDescription != "" {
					bodyMap["description"] = bodyDescription
				}
				if bodyIoLoggingApiKeyIds != "" {
					var parsedIoLoggingApiKeyIds any
					if err := json.Unmarshal([]byte(bodyIoLoggingApiKeyIds), &parsedIoLoggingApiKeyIds); err != nil {
						return fmt.Errorf("parsing --io-logging-api-key-ids JSON: %w", err)
					}
					asArray, ok := parsedIoLoggingApiKeyIds.([]any)
					if !ok {
						return fmt.Errorf("--io-logging-api-key-ids must be a JSON array, got JSON %T", parsedIoLoggingApiKeyIds)
					}
					bodyMap["io_logging_api_key_ids"] = asArray
				}
				if bodyIoLoggingSamplingRate != 0.0 {
					bodyMap["io_logging_sampling_rate"] = bodyIoLoggingSamplingRate
				}
				if cmd.Flags().Changed("is-data-discount-logging-enabled") {
					bodyMap["is_data_discount_logging_enabled"] = bodyIsDataDiscountLoggingEnabled
				}
				if cmd.Flags().Changed("is-observability-broadcast-enabled") {
					bodyMap["is_observability_broadcast_enabled"] = bodyIsObservabilityBroadcastEnabled
				}
				if cmd.Flags().Changed("is-observability-io-logging-enabled") {
					bodyMap["is_observability_io_logging_enabled"] = bodyIsObservabilityIoLoggingEnabled
				}
				if bodyName != "" {
					bodyMap["name"] = bodyName
				}
				if bodySlug != "" {
					bodyMap["slug"] = bodySlug
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "workspaces", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "workspaces", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "workspaces", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "workspaces", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "workspaces", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "workspaces",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "workspaces", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "workspaces", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyDefaultImageModel, "default-image-model", "", "Default image model for this workspace")
	cmd.Flags().StringVar(&bodyDefaultProviderSort, "default-provider-sort", "", "Default provider sort preference (price, throughput, latency, exacto)")
	cmd.Flags().StringVar(&bodyDefaultTextModel, "default-text-model", "", "Default text model for this workspace")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description of the workspace")
	cmd.Flags().StringVar(&bodyIoLoggingApiKeyIds, "io-logging-api-key-ids", "", "Optional array of API key IDs to filter I/O logging")
	cmd.Flags().Float64Var(&bodyIoLoggingSamplingRate, "io-logging-sampling-rate", 0.0, "Sampling rate for I/O logging (0.0001-1)")
	cmd.Flags().BoolVar(&bodyIsDataDiscountLoggingEnabled, "is-data-discount-logging-enabled", false, "Whether data discount logging is enabled")
	cmd.Flags().BoolVar(&bodyIsObservabilityBroadcastEnabled, "is-observability-broadcast-enabled", false, "Whether broadcast is enabled")
	cmd.Flags().BoolVar(&bodyIsObservabilityIoLoggingEnabled, "is-observability-io-logging-enabled", false, "Whether private logging is enabled")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name for the new workspace")
	cmd.Flags().StringVar(&bodySlug, "slug", "", "URL-friendly slug (lowercase alphanumeric segments separated by single hyphens, no leading/trailing hyphens)")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
