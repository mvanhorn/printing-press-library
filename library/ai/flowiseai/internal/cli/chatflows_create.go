// Copyright 2026 daniel-larson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newChatflowsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAnalytic string
	var bodyApiConfig string
	var bodyApikeyid string
	var bodyCategory string
	var bodyChatbotConfig string
	var bodyCreatedDate string
	var bodyDeployed bool
	var bodyFlowData string
	var bodyId string
	var bodyIsPublic bool
	var bodyName string
	var bodySpeechToText string
	var bodyType string
	var bodyUpdatedDate string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create a new chatflow with the provided details",
		Example:     "  flowiseai-pp-cli chatflows create",
		Annotations: map[string]string{"pp:endpoint": "chatflows.create", "pp:method": "POST", "pp:path": "/chatflows"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/chatflows"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAnalytic != "" {
					body["analytic"] = bodyAnalytic
				}
				if bodyApiConfig != "" {
					body["apiConfig"] = bodyApiConfig
				}
				if bodyApikeyid != "" {
					body["apikeyid"] = bodyApikeyid
				}
				if bodyCategory != "" {
					body["category"] = bodyCategory
				}
				if bodyChatbotConfig != "" {
					body["chatbotConfig"] = bodyChatbotConfig
				}
				if bodyCreatedDate != "" {
					body["createdDate"] = bodyCreatedDate
				}
				if bodyDeployed != false {
					body["deployed"] = bodyDeployed
				}
				if bodyFlowData != "" {
					body["flowData"] = bodyFlowData
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyIsPublic != false {
					body["isPublic"] = bodyIsPublic
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodySpeechToText != "" {
					body["speechToText"] = bodySpeechToText
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
				if bodyUpdatedDate != "" {
					body["updatedDate"] = bodyUpdatedDate
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "chatflows",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAnalytic, "analytic", "", "Analytic")
	cmd.Flags().StringVar(&bodyApiConfig, "api-config", "", "Api config")
	cmd.Flags().StringVar(&bodyApikeyid, "apikeyid", "", "Apikeyid")
	cmd.Flags().StringVar(&bodyCategory, "category", "", "Category")
	cmd.Flags().StringVar(&bodyChatbotConfig, "chatbot-config", "", "Chatbot config")
	cmd.Flags().StringVar(&bodyCreatedDate, "created-date", "", "Created date")
	cmd.Flags().BoolVar(&bodyDeployed, "deployed", false, "Deployed")
	cmd.Flags().StringVar(&bodyFlowData, "flow-data", "", "Flow data")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().BoolVar(&bodyIsPublic, "is-public", false, "Is public")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodySpeechToText, "speech-to-text", "", "Speech to text")
	cmd.Flags().StringVar(&bodyType, "type", "", "Type")
	cmd.Flags().StringVar(&bodyUpdatedDate, "updated-date", "", "Updated date")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
