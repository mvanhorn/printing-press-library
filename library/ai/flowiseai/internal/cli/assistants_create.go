// Copyright 2026 daniel-larson. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAssistantsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyCreatedDate string
	var bodyCredential string
	var bodyDetailsDescription string
	var bodyDetailsId string
	var bodyDetailsInstructions string
	var bodyDetailsModel string
	var bodyDetailsName string
	var bodyDetailsTemperature float64
	var bodyDetailsToolResources string
	var bodyDetailsTools string
	var bodyDetailsTopP float64
	var bodyIconSrc string
	var bodyId string
	var bodyUpdatedDate string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create a new assistant with the provided details",
		Example:     "  flowiseai-pp-cli assistants create",
		Annotations: map[string]string{"pp:endpoint": "assistants.create", "pp:method": "POST", "pp:path": "/assistants"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/assistants"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCreatedDate != "" {
					body["createdDate"] = bodyCreatedDate
				}
				if bodyCredential != "" {
					body["credential"] = bodyCredential
				}
				{
					nestedDetails := map[string]any{}
					if bodyDetailsDescription != "" {
						nestedDetails["description"] = bodyDetailsDescription
					}
					if bodyDetailsId != "" {
						nestedDetails["id"] = bodyDetailsId
					}
					if bodyDetailsInstructions != "" {
						nestedDetails["instructions"] = bodyDetailsInstructions
					}
					if bodyDetailsModel != "" {
						nestedDetails["model"] = bodyDetailsModel
					}
					if bodyDetailsName != "" {
						nestedDetails["name"] = bodyDetailsName
					}
					if bodyDetailsTemperature != 0.0 {
						nestedDetails["temperature"] = bodyDetailsTemperature
					}
					if bodyDetailsToolResources != "" {
						var parsedDetailsToolResources any
						if err := json.Unmarshal([]byte(bodyDetailsToolResources), &parsedDetailsToolResources); err != nil {
							return fmt.Errorf("parsing --details-tool-resources JSON: %w", err)
						}
						nestedDetails["tool_resources"] = parsedDetailsToolResources
					}
					if bodyDetailsTools != "" {
						var parsedDetailsTools any
						if err := json.Unmarshal([]byte(bodyDetailsTools), &parsedDetailsTools); err != nil {
							return fmt.Errorf("parsing --details-tools JSON: %w", err)
						}
						nestedDetails["tools"] = parsedDetailsTools
					}
					if bodyDetailsTopP != 0.0 {
						nestedDetails["top_p"] = bodyDetailsTopP
					}
					if len(nestedDetails) > 0 {
						body["details"] = nestedDetails
					}
				}
				if bodyIconSrc != "" {
					body["iconSrc"] = bodyIconSrc
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyUpdatedDate != "" {
					body["updatedDate"] = bodyUpdatedDate
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "assistants",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCreatedDate, "created-date", "", "Created date")
	cmd.Flags().StringVar(&bodyCredential, "credential", "", "Credential")
	cmd.Flags().StringVar(&bodyDetailsDescription, "details-description", "", "Description")
	cmd.Flags().StringVar(&bodyDetailsId, "details-id", "", "Id")
	cmd.Flags().StringVar(&bodyDetailsInstructions, "details-instructions", "", "Instructions")
	cmd.Flags().StringVar(&bodyDetailsModel, "details-model", "", "Model")
	cmd.Flags().StringVar(&bodyDetailsName, "details-name", "", "Name")
	cmd.Flags().Float64Var(&bodyDetailsTemperature, "details-temperature", 0.0, "Temperature")
	cmd.Flags().StringVar(&bodyDetailsToolResources, "details-tool-resources", "", "Tool resources")
	cmd.Flags().StringVar(&bodyDetailsTools, "details-tools", "", "Tools")
	cmd.Flags().Float64Var(&bodyDetailsTopP, "details-top-p", 0.0, "Top p")
	cmd.Flags().StringVar(&bodyIconSrc, "icon-src", "", "Icon src")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().StringVar(&bodyUpdatedDate, "updated-date", "", "Updated date")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
