{
  "manifest_version": "0.3",
  "name": "rundown-pp-mcp",
  "display_name": "The Rundown University",
  "version": "0.1.0",
  "description": "Search 280+ community AI workflows offline, and rank them by week - two things the site's own API cannot do.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/rundown-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/rundown-pp-mcp",
      "args": [],
      "env": {
        "PRINTING_PRESS_CLIENT_PROFILE": "${user_config.printing_press_client_profile}"
      }
    }
  },
  "user_config": {
    "printing_press_client_profile": {
      "type": "string",
      "title": "Client profile",
      "description": "Binds the MCP server to an existing tenant-gated Printing Press client profile.",
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
