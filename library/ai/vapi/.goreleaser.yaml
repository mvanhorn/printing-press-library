version: 2
project_name: vapi-pp-cli
changelog:
  disable: true
builds:
  - id: vapi-pp-cli
    main: ./cmd/vapi-pp-cli
    binary: vapi-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X vapi-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: vapi-pp-mcp
    main: ./cmd/vapi-pp-mcp
    binary: vapi-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: vapi-pp-cli
    repository:
      owner: 
      name: homebrew-tap
    homepage: "https://github.com//vapi-pp-cli"
    description: "Voice AI for developers."
    install: |
      bin.install "vapi-pp-cli"
      bin.install "vapi-pp-mcp"
