// Copyright 2026 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/ai/vapi/internal/cliutil"
	"github.com/spf13/cobra"
)

func newStructuredOutputRunCmd(flags *rootFlags) *cobra.Command {
	var bodyCallIds string
	var bodyPreviewEnabled bool
	var bodyStructuredOutputAssistantIds string
	var bodyStructuredOutputCompliancePlanForceStoreOnHipaaEnabled bool
	var bodyStructuredOutputDescription string
	var bodyStructuredOutputModel string
	var bodyStructuredOutputName string
	var bodyStructuredOutputRegex string
	var bodyStructuredOutputSchemaDescription string
	var bodyStructuredOutputSchemaEnum string
	var bodyStructuredOutputSchemaFormat string
	var bodyStructuredOutputSchemaPattern string
	var bodyStructuredOutputSchemaProperties string
	var bodyStructuredOutputSchemaRequired string
	var bodyStructuredOutputSchemaTitle string
	var bodyStructuredOutputSchemaType string
	var bodyStructuredOutputType string
	var bodyStructuredOutputWorkflowIds string
	var bodyStructuredOutputId string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "run",
		Short:       "Run Structured Output",
		Example:     "  vapi-pp-cli structured-output run",
		Annotations: map[string]string{"pp:endpoint": "structured-output.run", "pp:method": "POST", "pp:path": "/structured-output/run"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("call-ids") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "call-ids")
				}
				if !cmd.Flags().Changed("structured-output-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "structured-output-name")
				}
				if !cmd.Flags().Changed("structured-output-schema-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "structured-output-schema-type")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/structured-output/run"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCallIds != "" {
					body["callIds"] = cliutil.SplitCSV(bodyCallIds)
				}
				if cmd.Flags().Changed("preview-enabled") {
					body["previewEnabled"] = bodyPreviewEnabled
				}
				{
					nestedStructuredOutput := map[string]any{}
					if bodyStructuredOutputAssistantIds != "" {
						nestedStructuredOutput["assistantIds"] = cliutil.SplitCSV(bodyStructuredOutputAssistantIds)
					}
					{
						nestedStructuredOutputCompliancePlan := map[string]any{}
						if cmd.Flags().Changed("structured-output-compliance-plan-force-store-on-hipaa-enabled") {
							nestedStructuredOutputCompliancePlan["forceStoreOnHipaaEnabled"] = bodyStructuredOutputCompliancePlanForceStoreOnHipaaEnabled
						}
						if len(nestedStructuredOutputCompliancePlan) > 0 {
							nestedStructuredOutput["compliancePlan"] = nestedStructuredOutputCompliancePlan
						}
					}
					if bodyStructuredOutputDescription != "" {
						nestedStructuredOutput["description"] = bodyStructuredOutputDescription
					}
					if bodyStructuredOutputModel != "" {
						nestedStructuredOutput["model"] = bodyStructuredOutputModel
					}
					if bodyStructuredOutputName != "" {
						nestedStructuredOutput["name"] = bodyStructuredOutputName
					}
					if bodyStructuredOutputRegex != "" {
						nestedStructuredOutput["regex"] = bodyStructuredOutputRegex
					}
					{
						nestedStructuredOutputSchema := map[string]any{}
						if bodyStructuredOutputSchemaDescription != "" {
							nestedStructuredOutputSchema["description"] = bodyStructuredOutputSchemaDescription
						}
						if bodyStructuredOutputSchemaEnum != "" {
							nestedStructuredOutputSchema["enum"] = cliutil.SplitCSV(bodyStructuredOutputSchemaEnum)
						}
						if bodyStructuredOutputSchemaFormat != "" {
							nestedStructuredOutputSchema["format"] = bodyStructuredOutputSchemaFormat
						}
						if bodyStructuredOutputSchemaPattern != "" {
							nestedStructuredOutputSchema["pattern"] = bodyStructuredOutputSchemaPattern
						}
						if bodyStructuredOutputSchemaProperties != "" {
							var parsedStructuredOutputSchemaProperties any
							if err := json.Unmarshal([]byte(bodyStructuredOutputSchemaProperties), &parsedStructuredOutputSchemaProperties); err != nil {
								return fmt.Errorf("parsing --structured-output-schema-properties JSON: %w", err)
							}
							nestedStructuredOutputSchema["properties"] = parsedStructuredOutputSchemaProperties
						}
						if bodyStructuredOutputSchemaRequired != "" {
							nestedStructuredOutputSchema["required"] = cliutil.SplitCSV(bodyStructuredOutputSchemaRequired)
						}
						if bodyStructuredOutputSchemaTitle != "" {
							nestedStructuredOutputSchema["title"] = bodyStructuredOutputSchemaTitle
						}
						if bodyStructuredOutputSchemaType != "" {
							nestedStructuredOutputSchema["type"] = bodyStructuredOutputSchemaType
						}
						if len(nestedStructuredOutputSchema) > 0 {
							nestedStructuredOutput["schema"] = nestedStructuredOutputSchema
						}
					}
					if bodyStructuredOutputType != "" {
						nestedStructuredOutput["type"] = bodyStructuredOutputType
					}
					if bodyStructuredOutputWorkflowIds != "" {
						nestedStructuredOutput["workflowIds"] = cliutil.SplitCSV(bodyStructuredOutputWorkflowIds)
					}
					if len(nestedStructuredOutput) > 0 {
						body["structuredOutput"] = nestedStructuredOutput
					}
				}
				if bodyStructuredOutputId != "" {
					body["structuredOutputId"] = bodyStructuredOutputId
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "structured-output", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "structured-output", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "structured-output", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "structured-output", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "structured-output", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "structured-output",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "structured-output", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "structured-output", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyCallIds, "call-ids", "", "This is the array of callIds that will be updated with the new structured output value.")
	cmd.Flags().BoolVar(&bodyPreviewEnabled, "preview-enabled", false, "This is the preview flag for the re-run.")
	cmd.Flags().StringVar(&bodyStructuredOutputAssistantIds, "structured-output-assistant-ids", "", "These are the assistant IDs that this structured output is linked to.")
	cmd.Flags().BoolVar(&bodyStructuredOutputCompliancePlanForceStoreOnHipaaEnabled, "structured-output-compliance-plan-force-store-on-hipaa-enabled", false, "Force storage for this output under HIPAA. Only enable if output contains no sensitive data.")
	cmd.Flags().StringVar(&bodyStructuredOutputDescription, "structured-output-description", "", "This is the description of what the structured output extracts.")
	cmd.Flags().StringVar(&bodyStructuredOutputModel, "structured-output-model", "", "This is the model that will be used to extract the structured output.")
	cmd.Flags().StringVar(&bodyStructuredOutputName, "structured-output-name", "", "This is the name of the structured output.")
	cmd.Flags().StringVar(&bodyStructuredOutputRegex, "structured-output-regex", "", "This is the regex pattern to match against the transcript. Only used when type is 'regex'.")
	cmd.Flags().StringVar(&bodyStructuredOutputSchemaDescription, "structured-output-schema-description", "", "This is the description to help the model understand what it needs to output.")
	cmd.Flags().StringVar(&bodyStructuredOutputSchemaEnum, "structured-output-schema-enum", "", "This array specifies the allowed values that can be used to restrict the output of the model.")
	cmd.Flags().StringVar(&bodyStructuredOutputSchemaFormat, "structured-output-schema-format", "", "This is the format of the string. To pass a regex, use the `pattern` property instead.")
	cmd.Flags().StringVar(&bodyStructuredOutputSchemaPattern, "structured-output-schema-pattern", "", "This is the pattern of the string. This is a regex that will be used to validate the data in question.")
	cmd.Flags().StringVar(&bodyStructuredOutputSchemaProperties, "structured-output-schema-properties", "", "This is required if the type is 'object'. This specifies the properties of the object.")
	cmd.Flags().StringVar(&bodyStructuredOutputSchemaRequired, "structured-output-schema-required", "", "This is a list of properties that are required. This only makes sense if the type is 'object'.")
	cmd.Flags().StringVar(&bodyStructuredOutputSchemaTitle, "structured-output-schema-title", "", "This is the title of the schema.")
	cmd.Flags().StringVar(&bodyStructuredOutputSchemaType, "structured-output-schema-type", "", "This is the type of output you'd like.")
	cmd.Flags().StringVar(&bodyStructuredOutputType, "structured-output-type", "", "This is the type of structured output. - 'ai': Uses an LLM to extract structured data from the conversation (default).")
	cmd.Flags().StringVar(&bodyStructuredOutputWorkflowIds, "structured-output-workflow-ids", "", "These are the workflow IDs that this structured output is linked to.")
	cmd.Flags().StringVar(&bodyStructuredOutputId, "structured-output-id", "", "This is the ID of the structured output that will be run.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin (use this for deeply nested fields not exposed as flags)")

	return cmd
}
