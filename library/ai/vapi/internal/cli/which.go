// Copyright 2026 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "assistant create", Description: "Create Assistant", Group: "assistant"},
	{Command: "assistant find-all", Description: "List Assistants", Group: "assistant"},
	{Command: "assistant find-one", Description: "Get Assistant", Group: "assistant"},
	{Command: "assistant remove", Description: "Delete Assistant", Group: "assistant"},
	{Command: "assistant update", Description: "Update Assistant", Group: "assistant"},
	{Command: "call create", Description: "Create Call", Group: "call"},
	{Command: "call delete-data", Description: "Delete Call", Group: "call"},
	{Command: "call find-all", Description: "List Calls", Group: "call"},
	{Command: "call find-one", Description: "Get Call", Group: "call"},
	{Command: "call update", Description: "Update Call", Group: "call"},
	{Command: "call assistant-recording call-artifact-download", Description: "Download Call Assistant Recording", Group: "call"},
	{Command: "call call-logs call-artifact-download", Description: "Download Call Logs", Group: "call"},
	{Command: "call customer-recording call-artifact-download", Description: "Download Call Customer Recording", Group: "call"},
	{Command: "call mono-recording call-artifact-download", Description: "Download Call Mono Recording", Group: "call"},
	{Command: "call pcap call-artifact-download", Description: "Download Call Packet Capture (pcap)", Group: "call"},
	{Command: "call stereo-recording call-artifact-download", Description: "Download Call Stereo Recording", Group: "call"},
	{Command: "call video-recording call-artifact-download", Description: "Download Call Video Recording", Group: "call"},
	{Command: "campaign create", Description: "Create Campaign", Group: "campaign"},
	{Command: "campaign find-all", Description: "List Campaigns", Group: "campaign"},
	{Command: "campaign find-one", Description: "Get Campaign", Group: "campaign"},
	{Command: "campaign remove", Description: "Delete Campaign", Group: "campaign"},
	{Command: "campaign update", Description: "Update Campaign", Group: "campaign"},
	{Command: "chat create", Description: "Creates a new chat with optional SMS delivery via transport field. Requires at least one of: assistantId/assistant, sessionId, or previousChatId. Note: sessionId and previousChatId are mutually exclusive. Transport field enables SMS delivery with two modes: (1) New conversation - provide transport.phoneNumberId and transport.customer to create a new session, (2) Existing conversation - provide sessionId to use existing session data. Cannot specify both sessionId and transport fields together. The transport.useLLMGeneratedMessageForOutbound flag controls whether input is processed by LLM (true, default) or forwarded directly as SMS (false).", Group: "chat"},
	{Command: "chat create-open-aichat", Description: "Create Chat (OpenAI Compatible)", Group: "chat"},
	{Command: "chat delete", Description: "Delete Chat", Group: "chat"},
	{Command: "chat get", Description: "Get Chat", Group: "chat"},
	{Command: "chat list", Description: "List Chats", Group: "chat"},
	{Command: "eval create", Description: "Create Eval", Group: "eval"},
	{Command: "eval get", Description: "Get Eval", Group: "eval"},
	{Command: "eval get-paginated", Description: "List Evals", Group: "eval"},
	{Command: "eval get-run", Description: "Get Eval Run", Group: "eval"},
	{Command: "eval get-runs-paginated", Description: "List Eval Runs", Group: "eval"},
	{Command: "eval remove", Description: "Delete Eval", Group: "eval"},
	{Command: "eval remove-run", Description: "Delete Eval Run", Group: "eval"},
	{Command: "eval run", Description: "Create Eval Run", Group: "eval"},
	{Command: "eval update", Description: "Update Eval", Group: "eval"},
	{Command: "file create", Description: "Upload File", Group: "file"},
	{Command: "file find-all", Description: "List Files", Group: "file"},
	{Command: "file find-one", Description: "Get File", Group: "file"},
	{Command: "file remove", Description: "Delete File", Group: "file"},
	{Command: "file update", Description: "Update File", Group: "file"},
	{Command: "observability scorecard-create", Description: "Create Scorecard", Group: "observability"},
	{Command: "observability scorecard-get", Description: "Get Scorecard", Group: "observability"},
	{Command: "observability scorecard-get-paginated", Description: "List Scorecards", Group: "observability"},
	{Command: "observability scorecard-remove", Description: "Delete Scorecard", Group: "observability"},
	{Command: "observability scorecard-update", Description: "Update Scorecard", Group: "observability"},
	{Command: "phone-number create", Description: "Create Phone Number", Group: "phone-number"},
	{Command: "phone-number find-all", Description: "List Phone Numbers", Group: "phone-number"},
	{Command: "phone-number find-all-paginated", Description: "List Phone Numbers", Group: "phone-number"},
	{Command: "phone-number find-one", Description: "Get Phone Number", Group: "phone-number"},
	{Command: "phone-number remove", Description: "Delete Phone Number", Group: "phone-number"},
	{Command: "phone-number update", Description: "Update Phone Number", Group: "phone-number"},
	{Command: "provider resource-create-resource", Description: "Create Provider Resource", Group: "provider"},
	{Command: "provider resource-delete-resource", Description: "Delete Provider Resource", Group: "provider"},
	{Command: "provider resource-get-resource", Description: "Get Provider Resource", Group: "provider"},
	{Command: "provider resource-get-resources-paginated", Description: "List Provider Resources", Group: "provider"},
	{Command: "provider resource-update-resource", Description: "Update Provider Resource", Group: "provider"},
	{Command: "reporting insight-create", Description: "Create Insight", Group: "reporting"},
	{Command: "reporting insight-find-all", Description: "Get Insights", Group: "reporting"},
	{Command: "reporting insight-find-one", Description: "Get Insight", Group: "reporting"},
	{Command: "reporting insight-preview", Description: "Preview Insight", Group: "reporting"},
	{Command: "reporting insight-remove", Description: "Delete Insight", Group: "reporting"},
	{Command: "reporting insight-run", Description: "Run Insight", Group: "reporting"},
	{Command: "reporting insight-update", Description: "Update Insight", Group: "reporting"},
	{Command: "session create", Description: "Create Session", Group: "session"},
	{Command: "session find-all-paginated", Description: "List Sessions", Group: "session"},
	{Command: "session find-one", Description: "Get Session", Group: "session"},
	{Command: "session remove", Description: "Delete Session", Group: "session"},
	{Command: "session update", Description: "Update Session", Group: "session"},
	{Command: "squad create", Description: "Create Squad", Group: "squad"},
	{Command: "squad find-all", Description: "List Squads", Group: "squad"},
	{Command: "squad find-one", Description: "Get Squad", Group: "squad"},
	{Command: "squad remove", Description: "Delete Squad", Group: "squad"},
	{Command: "squad update", Description: "Update Squad", Group: "squad"},
	{Command: "structured-output create", Description: "Create Structured Output", Group: "structured-output"},
	{Command: "structured-output find-all", Description: "List Structured Outputs", Group: "structured-output"},
	{Command: "structured-output find-one", Description: "Get Structured Output", Group: "structured-output"},
	{Command: "structured-output remove", Description: "Delete Structured Output", Group: "structured-output"},
	{Command: "structured-output run", Description: "Run Structured Output", Group: "structured-output"},
	{Command: "structured-output update", Description: "Update Structured Output", Group: "structured-output"},
	{Command: "tool create", Description: "Create Tool", Group: "tool"},
	{Command: "tool find-all", Description: "List Tools", Group: "tool"},
	{Command: "tool find-one", Description: "Get Tool", Group: "tool"},
	{Command: "tool remove", Description: "Delete Tool", Group: "tool"},
	{Command: "tool update", Description: "Update Tool", Group: "tool"},
	{Command: "vapi-analytics query", Description: "Create Analytics Queries", Group: "vapi-analytics"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  vapi-pp-cli which "stale tickets"
  vapi-pp-cli which "bottleneck"
  vapi-pp-cli which --limit 1 "send message"
  vapi-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
