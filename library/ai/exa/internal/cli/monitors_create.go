// Copyright 2026 Anchal Sharma and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/ai/exa/internal/cliutil"
	"github.com/spf13/cobra"
)

func newMonitorsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyMetadata string
	var bodyName string
	var bodyOutputSchema string
	var bodySearchContentsContext string
	var bodySearchContentsFilterEmptyResults bool
	var bodySearchContentsHighlights string
	var bodySearchContentsLivecrawl string
	var bodySearchContentsLivecrawlTimeout int
	var bodySearchContentsMaxAgeHours int
	var bodySearchContentsSubpageTarget string
	var bodySearchContentsSubpages int
	var bodySearchContentsSummary string
	var bodySearchContentsText string
	var bodySearchNumResults int
	var bodySearchQuery string
	var bodyTriggerPeriod string
	var bodyTriggerType string
	var bodyWebhookEvents string
	var bodyWebhookUrl string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Creates a new Monitor to run recurring Exa searches on a schedule.",
		Example:     "  exa-pp-cli monitors create",
		Annotations: map[string]string{"pp:endpoint": "monitors.create", "pp:method": "POST", "pp:path": "/monitors"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("search-query") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "search-query")
				}
				if !cmd.Flags().Changed("trigger-period") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "trigger-period")
				}
				if !cmd.Flags().Changed("trigger-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "trigger-type")
				}
				if !cmd.Flags().Changed("webhook-url") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "webhook-url")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/monitors"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyMetadata != "" {
					var parsedMetadata any
					if err := json.Unmarshal([]byte(bodyMetadata), &parsedMetadata); err != nil {
						return fmt.Errorf("parsing --metadata JSON: %w", err)
					}
					body["metadata"] = parsedMetadata
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyOutputSchema != "" {
					body["outputSchema"] = bodyOutputSchema
				}
				{
					nestedSearch := map[string]any{}
					{
						nestedSearchContents := map[string]any{}
						if bodySearchContentsContext != "" {
							nestedSearchContents["context"] = bodySearchContentsContext
						}
						if cmd.Flags().Changed("search-contents-filter-empty-results") {
							nestedSearchContents["filterEmptyResults"] = bodySearchContentsFilterEmptyResults
						}
						if bodySearchContentsHighlights != "" {
							nestedSearchContents["highlights"] = bodySearchContentsHighlights
						}
						if bodySearchContentsLivecrawl != "" {
							nestedSearchContents["livecrawl"] = bodySearchContentsLivecrawl
						}
						if bodySearchContentsLivecrawlTimeout != 0 {
							nestedSearchContents["livecrawlTimeout"] = bodySearchContentsLivecrawlTimeout
						}
						if bodySearchContentsMaxAgeHours != 0 {
							nestedSearchContents["maxAgeHours"] = bodySearchContentsMaxAgeHours
						}
						if bodySearchContentsSubpageTarget != "" {
							nestedSearchContents["subpageTarget"] = bodySearchContentsSubpageTarget
						}
						if bodySearchContentsSubpages != 0 {
							nestedSearchContents["subpages"] = bodySearchContentsSubpages
						}
						if bodySearchContentsSummary != "" {
							nestedSearchContents["summary"] = bodySearchContentsSummary
						}
						if bodySearchContentsText != "" {
							nestedSearchContents["text"] = bodySearchContentsText
						}
						if len(nestedSearchContents) > 0 {
							nestedSearch["contents"] = nestedSearchContents
						}
					}
					if bodySearchNumResults != 0 {
						nestedSearch["numResults"] = bodySearchNumResults
					}
					if bodySearchQuery != "" {
						nestedSearch["query"] = bodySearchQuery
					}
					if len(nestedSearch) > 0 {
						body["search"] = nestedSearch
					}
				}
				{
					nestedTrigger := map[string]any{}
					if bodyTriggerPeriod != "" {
						nestedTrigger["period"] = bodyTriggerPeriod
					}
					if bodyTriggerType != "" {
						nestedTrigger["type"] = bodyTriggerType
					}
					if len(nestedTrigger) > 0 {
						body["trigger"] = nestedTrigger
					}
				}
				{
					nestedWebhook := map[string]any{}
					if bodyWebhookEvents != "" {
						nestedWebhook["events"] = cliutil.SplitCSV(bodyWebhookEvents)
					}
					if bodyWebhookUrl != "" {
						nestedWebhook["url"] = bodyWebhookUrl
					}
					if len(nestedWebhook) > 0 {
						body["webhook"] = nestedWebhook
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "monitors", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "monitors", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "monitors", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "monitors", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "monitors", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "monitors",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "monitors", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "monitors", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Optional key-value metadata. Echoed back in webhook deliveries so you can route updates to systems like Slack.")
	cmd.Flags().StringVar(&bodyName, "name", "", "An optional name for the monitor")
	cmd.Flags().StringVar(&bodyOutputSchema, "output-schema", "", "Controls the format of the run output. Defaults to `{ 'type': 'text' }` if not specified.")
	cmd.Flags().StringVar(&bodySearchContentsContext, "search-contents-context", "", "Deprecated: Use highlights or text instead. Returns page contents as a combined context string.")
	cmd.Flags().BoolVar(&bodySearchContentsFilterEmptyResults, "search-contents-filter-empty-results", false, "Filter out results with no content")
	cmd.Flags().StringVar(&bodySearchContentsHighlights, "search-contents-highlights", "", "Text snippets the LLM identifies as most relevant from each page.")
	cmd.Flags().StringVar(&bodySearchContentsLivecrawl, "search-contents-livecrawl", "", "Crawl strategy for fetching page content")
	cmd.Flags().IntVar(&bodySearchContentsLivecrawlTimeout, "search-contents-livecrawl-timeout", 0, "The timeout for livecrawling in milliseconds.")
	cmd.Flags().IntVar(&bodySearchContentsMaxAgeHours, "search-contents-max-age-hours", 0, "Maximum age of cached content in hours.")
	cmd.Flags().StringVar(&bodySearchContentsSubpageTarget, "search-contents-subpage-target", "", "Term to find specific subpages of search results. Can be a single string or an array of strings.")
	cmd.Flags().IntVar(&bodySearchContentsSubpages, "search-contents-subpages", 0, "The number of subpages to crawl. The actual number crawled may be limited by system constraints.")
	cmd.Flags().StringVar(&bodySearchContentsSummary, "search-contents-summary", "", "Return an LLM-generated summary. Pass `true` for defaults, or an object with `query` and `maxTokens`.")
	cmd.Flags().StringVar(&bodySearchContentsText, "search-contents-text", "", "Text extraction options for each result.")
	cmd.Flags().IntVar(&bodySearchNumResults, "search-num-results", 0, "Number of results to return. Limits vary by search type. The maximum public limit is 100 results.")
	cmd.Flags().StringVar(&bodySearchQuery, "search-query", "", "The query string for the search.")
	cmd.Flags().StringVar(&bodyTriggerPeriod, "trigger-period", "", "A duration string specifying how often the monitor runs (e.g., '1h', '6h', '1d', '7d'). Single-unit only.")
	cmd.Flags().StringVar(&bodyTriggerType, "trigger-type", "", "The type of trigger. Currently only `interval` is supported.")
	cmd.Flags().StringVar(&bodyWebhookEvents, "webhook-events", "", "Which events to subscribe to. Defaults to all events if not specified.")
	cmd.Flags().StringVar(&bodyWebhookUrl, "webhook-url", "", "The HTTPS URL to receive webhook events. Must not point to localhost or private IP ranges.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin (use this for deeply nested fields not exposed as flags)")

	return cmd
}
