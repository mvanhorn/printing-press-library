// Copyright 2026 Som Samantray and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAgentCreateRunCmd(flags *rootFlags) *cobra.Command {
	var flagAccept string
	var flagExaBeta string
	var bodyBudgetMaxCostDollars float64
	var bodyDataSources string
	var bodyEffort string
	var bodyInputData string
	var bodyInputExclusion string
	var bodyMetadata string
	var bodyOutputSchema string
	var bodyPreviousRunId string
	var bodyQuery string
	var bodySystemPrompt string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "create-run",
		Short: "Create an asynchronous Agent run. By default, the API returns the run object immediately.",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  exa-pp-cli agent create-run --query example-value",
		Annotations: map[string]string{"pp:endpoint": "agent.create-run", "pp:method": "POST", "pp:path": "/agent/runs"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if cmd.Flags().Changed("accept") {
				allowedAccept := []string{"application/json", "text/event-stream"}
				validAccept := false
				for _, v := range allowedAccept {
					if flagAccept == v {
						validAccept = true
						break
					}
				}
				if !validAccept {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagAccept, "accept", allowedAccept)
				}
			}
			if !stdinBody {
				if !cmd.Flags().Changed("query") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "query")
				}
			}
			path := "/agent/runs"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			headerOverrides := map[string]string{}

			if cmd.Flags().Changed("accept") || flagAccept != "" {
				headerOverrides["Accept"] = formatCLIParamValue(flagAccept)
			}

			if cmd.Flags().Changed("exa-beta") || flagExaBeta != "" {
				headerOverrides["Exa-Beta"] = formatCLIParamValue(flagExaBeta)
			}

			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				{
					nestedBudget := map[string]any{}
					if cmd.Flags().Changed("budget-max-cost-dollars") || bodyBudgetMaxCostDollars != 0.0 {
						nestedBudget["maxCostDollars"] = bodyBudgetMaxCostDollars
					}
					if len(nestedBudget) > 0 {
						bodyMap["budget"] = nestedBudget
					}
				}
				if cmd.Flags().Changed("data-sources") || bodyDataSources != "" {
					var parsedDataSources any
					if err := json.Unmarshal([]byte(bodyDataSources), &parsedDataSources); err != nil {
						return fmt.Errorf("parsing --data-sources JSON: %w", err)
					}
					asArray, ok := parsedDataSources.([]any)
					if !ok {
						return fmt.Errorf("--data-sources must be a JSON array, got JSON %T", parsedDataSources)
					}
					bodyMap["dataSources"] = asArray
				}
				if cmd.Flags().Changed("effort") || bodyEffort != "" {
					bodyMap["effort"] = bodyEffort
				}
				{
					nestedInput := map[string]any{}
					if cmd.Flags().Changed("input-data") || bodyInputData != "" {
						var parsedInputData any
						if err := json.Unmarshal([]byte(bodyInputData), &parsedInputData); err != nil {
							return fmt.Errorf("parsing --input-data JSON: %w", err)
						}
						asArray, ok := parsedInputData.([]any)
						if !ok {
							return fmt.Errorf("--input-data must be a JSON array, got JSON %T", parsedInputData)
						}
						nestedInput["data"] = asArray
					}
					if cmd.Flags().Changed("input-exclusion") || bodyInputExclusion != "" {
						var parsedInputExclusion any
						if err := json.Unmarshal([]byte(bodyInputExclusion), &parsedInputExclusion); err != nil {
							return fmt.Errorf("parsing --input-exclusion JSON: %w", err)
						}
						asArray, ok := parsedInputExclusion.([]any)
						if !ok {
							return fmt.Errorf("--input-exclusion must be a JSON array, got JSON %T", parsedInputExclusion)
						}
						nestedInput["exclusion"] = asArray
					}
					if len(nestedInput) > 0 {
						bodyMap["input"] = nestedInput
					}
				}
				if cmd.Flags().Changed("metadata") || bodyMetadata != "" {
					var parsedMetadata any
					if err := json.Unmarshal([]byte(bodyMetadata), &parsedMetadata); err != nil {
						return fmt.Errorf("parsing --metadata JSON: %w", err)
					}
					asMap, ok := parsedMetadata.(map[string]any)
					if !ok {
						return fmt.Errorf("--metadata must be a JSON object, got JSON %T", parsedMetadata)
					}
					bodyMap["metadata"] = asMap
				}
				if cmd.Flags().Changed("output-schema") || bodyOutputSchema != "" {
					if looksLikeJSONComposite(bodyOutputSchema) {
						var parsedOutputSchema any
						if err := json.Unmarshal([]byte(bodyOutputSchema), &parsedOutputSchema); err != nil {
							return fmt.Errorf("parsing --output-schema JSON: %w", err)
						}
						bodyMap["outputSchema"] = parsedOutputSchema
					} else {
						bodyMap["outputSchema"] = bodyOutputSchema
					}
				}
				if cmd.Flags().Changed("previous-run-id") || bodyPreviousRunId != "" {
					bodyMap["previousRunId"] = bodyPreviousRunId
				}
				if cmd.Flags().Changed("query") || bodyQuery != "" {
					bodyMap["query"] = bodyQuery
				}
				if cmd.Flags().Changed("system-prompt") || bodySystemPrompt != "" {
					bodyMap["systemPrompt"] = bodySystemPrompt
				}
			}
			data, statusCode, err := c.PostWithParamsAndHeaders(cmd.Context(), path, params, body, headerOverrides)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "agent", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "agent", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "agent", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "agent", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "agent", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "agent",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "agent", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "agent", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagAccept, "accept", "", "Set to `text/event-stream` to receive server-sent events. (one of: application/json, text/event-stream)")
	cmd.Flags().StringVar(&flagExaBeta, "exa-beta", "", "Comma-separated beta feature tokens for opting into experimental features.")
	cmd.Flags().Float64Var(&bodyBudgetMaxCostDollars, "budget-max-cost-dollars", 0.0, "Maximum amount this run can spend in US dollars.")
	cmd.Flags().StringVar(&bodyDataSources, "data-sources", "", "Exa Connect data providers to enable for the run. Each entry enables all of that provider's tools.")
	cmd.Flags().StringVar(&bodyEffort, "effort", "auto", "Cost and reasoning effort preference for the run. `auto` lets Exa choose the appropriate effort.")
	cmd.Flags().StringVar(&bodyInputData, "input-data", "", "Records the agent should process or enrich.")
	cmd.Flags().StringVar(&bodyInputExclusion, "input-exclusion", "", "Records or entities the agent should avoid returning.")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Caller-provided metadata stored with the run.")
	cmd.Flags().StringVar(&bodyOutputSchema, "output-schema", "", "Output schema")
	cmd.Flags().StringVar(&bodyPreviousRunId, "previous-run-id", "", "Completed run ID to continue from. Must belong to the same team.")
	cmd.Flags().StringVar(&bodyQuery, "query", "", "Natural-language question or instructions for the request.")
	cmd.Flags().StringVar(&bodySystemPrompt, "system-prompt", "", "Additional instructions that guide generated output or agent behavior.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
