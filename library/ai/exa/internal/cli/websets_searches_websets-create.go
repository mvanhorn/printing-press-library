// Copyright 2026 Som Samantray and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newWebsetsSearchesWebsetsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyBehavior string
	var bodyCount float64
	var bodyCriteria string
	var bodyEntity string
	var bodyExclude string
	var bodyMaxPeoplePerCompany int
	var bodyMetadata string
	var bodyQuery string
	var bodyRecall bool
	var bodyScope string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "websets-create <webset>",
		Short: "Creates a new Search for the Webset.",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  exa-pp-cli websets searches websets-create example-value --query example-value",
		Annotations: map[string]string{"pp:endpoint": "searches.websets-create", "pp:method": "POST", "pp:path": "https://api.exa.ai/websets/v0/websets/{webset}/searches"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <webset>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <webset>"))
			}
			if !stdinBody {
				if !cmd.Flags().Changed("count") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "count")
				}
				if !cmd.Flags().Changed("query") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "query")
				}
			}
			path := "https://api.exa.ai/websets/v0/websets/{webset}/searches"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("webset is required\nUsage: %s <%s>", cmd.CommandPath(), "webset"))
			}
			path = replacePathParam(path, "webset", args[0])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("behavior") || bodyBehavior != "" {
					bodyMap["behavior"] = bodyBehavior
				}
				if cmd.Flags().Changed("count") || bodyCount != 0.0 {
					bodyMap["count"] = bodyCount
				}
				if cmd.Flags().Changed("criteria") || bodyCriteria != "" {
					var parsedCriteria any
					if err := json.Unmarshal([]byte(bodyCriteria), &parsedCriteria); err != nil {
						return fmt.Errorf("parsing --criteria JSON: %w", err)
					}
					asArray, ok := parsedCriteria.([]any)
					if !ok {
						return fmt.Errorf("--criteria must be a JSON array, got JSON %T", parsedCriteria)
					}
					bodyMap["criteria"] = asArray
				}
				if cmd.Flags().Changed("entity") || bodyEntity != "" {
					if looksLikeJSONComposite(bodyEntity) {
						var parsedEntity any
						if err := json.Unmarshal([]byte(bodyEntity), &parsedEntity); err != nil {
							return fmt.Errorf("parsing --entity JSON: %w", err)
						}
						bodyMap["entity"] = parsedEntity
					} else {
						bodyMap["entity"] = bodyEntity
					}
				}
				if cmd.Flags().Changed("exclude") || bodyExclude != "" {
					var parsedExclude any
					if err := json.Unmarshal([]byte(bodyExclude), &parsedExclude); err != nil {
						return fmt.Errorf("parsing --exclude JSON: %w", err)
					}
					asArray, ok := parsedExclude.([]any)
					if !ok {
						return fmt.Errorf("--exclude must be a JSON array, got JSON %T", parsedExclude)
					}
					bodyMap["exclude"] = asArray
				}
				if cmd.Flags().Changed("max-people-per-company") || bodyMaxPeoplePerCompany != 0 {
					bodyMap["maxPeoplePerCompany"] = bodyMaxPeoplePerCompany
				}
				if cmd.Flags().Changed("metadata") || bodyMetadata != "" {
					var parsedMetadata any
					if err := json.Unmarshal([]byte(bodyMetadata), &parsedMetadata); err != nil {
						return fmt.Errorf("parsing --metadata JSON: %w", err)
					}
					asMap, ok := parsedMetadata.(map[string]any)
					if !ok {
						return fmt.Errorf("--metadata must be a JSON object, got JSON %T", parsedMetadata)
					}
					bodyMap["metadata"] = asMap
				}
				if cmd.Flags().Changed("query") || bodyQuery != "" {
					bodyMap["query"] = bodyQuery
				}
				if cmd.Flags().Changed("recall") {
					bodyMap["recall"] = bodyRecall
				}
				if cmd.Flags().Changed("scope") || bodyScope != "" {
					var parsedScope any
					if err := json.Unmarshal([]byte(bodyScope), &parsedScope); err != nil {
						return fmt.Errorf("parsing --scope JSON: %w", err)
					}
					asArray, ok := parsedScope.([]any)
					if !ok {
						return fmt.Errorf("--scope must be a JSON array, got JSON %T", parsedScope)
					}
					bodyMap["scope"] = asArray
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "searches", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "searches", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "searches", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "searches", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "searches", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "searches",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "searches", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "searches", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyBehavior, "behavior", "override", "How this search interacts with existing items in the Webset: - **override**")
	cmd.Flags().Float64Var(&bodyCount, "count", 0.0, "Number of Items the Search will attempt to find.")
	cmd.Flags().StringVar(&bodyCriteria, "criteria", "", "Criteria every item is evaluated against.")
	cmd.Flags().StringVar(&bodyEntity, "entity", "", "Entity the search will return results for.")
	cmd.Flags().StringVar(&bodyExclude, "exclude", "", "Sources (existing imports or websets) to exclude from search results.")
	cmd.Flags().IntVar(&bodyMaxPeoplePerCompany, "max-people-per-company", 0, "Optional soft cap for people searches.")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "Set of key-value pairs you want to associate with this object.")
	cmd.Flags().StringVar(&bodyQuery, "query", "", "Natural language search query describing what you are looking for.")
	cmd.Flags().BoolVar(&bodyRecall, "recall", false, "Whether to provide an estimate of how many total relevant results could exist for this search.")
	cmd.Flags().StringVar(&bodyScope, "scope", "", "Limit the search to specific sources (existing imports).")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
