// Copyright 2026 krishna-vardhan. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newNameUpdateEmbedContentBatchGenerativelanguageBatchesUpdateEmbedContentBatchCmd(flags *rootFlags) *cobra.Command {
	var flagUpdateMask string
	var bodyCreateTime string
	var bodyDisplayName string
	var bodyEndTime string
	var bodyModel string
	var bodyPriority int
	var bodyState string
	var bodyUpdateTime string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "generativelanguage-batches-update-embed-content-batch <name>",
		Aliases: []string{"update"},
		Short: "Updates a batch of EmbedContent requests for batch processing",
		Example: "  gemini-pp-cli name-update-embed-content-batch generativelanguage-batches-update-embed-content-batch example-resource",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/{name}:updateEmbedContentBatch"
			path = replacePathParam(path, "name", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyCreateTime != "" {
					body["createTime"] = bodyCreateTime
				}
				if bodyDisplayName != "" {
					body["displayName"] = bodyDisplayName
				}
				if bodyEndTime != "" {
					body["endTime"] = bodyEndTime
				}
				if bodyModel != "" {
					body["model"] = bodyModel
				}
				if bodyPriority != 0 {
					body["priority"] = bodyPriority
				}
				if bodyState != "" {
					body["state"] = bodyState
				}
				if bodyUpdateTime != "" {
					body["updateTime"] = bodyUpdateTime
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping
				filtered := data
				if flags.compact {
					filtered = compactFields(filtered)
				}
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "name-update-embed-content-batch",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagUpdateMask, "update-mask", "", "Optional. The list of fields to update.")
	cmd.Flags().StringVar(&bodyCreateTime, "create-time", "", "Output only. The time at which the batch was created.")
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "Required. The user-defined name of this batch.")
	cmd.Flags().StringVar(&bodyEndTime, "end-time", "", "Output only. The time at which the batch processing completed.")
	cmd.Flags().StringVar(&bodyModel, "model", "", "Required. The name of the `Model` to use for generating the completion. Format: `models/{model}`.")
	cmd.Flags().IntVar(&bodyPriority, "priority", 0, "Optional. The priority of the batch. Batches with a higher priority value will be processed before batches with a...")
	cmd.Flags().StringVar(&bodyState, "state", "", "Output only. The state of the batch.")
	cmd.Flags().StringVar(&bodyUpdateTime, "update-time", "", "Output only. The time at which the batch was last updated.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
