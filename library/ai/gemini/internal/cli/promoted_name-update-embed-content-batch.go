// Copyright 2026 krishna-vardhan. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newNameUpdateEmbedContentBatchPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagUpdateMask string
	var bodyBatchStats string
	var bodyCreateTime string
	var bodyDisplayName string
	var bodyEndTime string
	var bodyInputConfig string
	var bodyModel string
	var bodyOutput string
	var bodyPriority int
	var bodyState string
	var bodyUpdateTime string

	cmd := &cobra.Command{
		Use:   "name-update-embed-content-batch <name>",
		Short: "Updates a batch of EmbedContent requests for batch processing.",
		Long:  "Shortcut for 'name-update-embed-content-batch generativelanguage-batches-update-embed-content-batch'. Updates a batch of EmbedContent requests for batch processing.",
		Example: "  gemini-pp-cli name-update-embed-content-batch example-resource",
		Annotations: map[string]string{"pp:endpoint": "name-update-embed-content-batch.generativelanguage-batches-update-embed-content-batch", "pp:method": "PATCH", "pp:path": "/v1/{name}:updateEmbedContentBatch"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/{name}:updateEmbedContentBatch"
			if len(args) < 1 {
				// JSON envelope: {error, usage}. Written first; the
				// usageErr return preserves exit code 2 across modes.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "name is required",
						"usage": fmt.Sprintf("%s <%s>", cmd.CommandPath(), "name"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("name is required\nUsage: %s <%s>", cmd.CommandPath(), "name"))
			}
			path = replacePathParam(path, "name", args[0])
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyBatchStats != "" {
				var parsedBatchStats any
				if err := json.Unmarshal([]byte(bodyBatchStats), &parsedBatchStats); err != nil {
					return fmt.Errorf("parsing --batch-stats JSON: %w", err)
				}
				body["batchStats"] = parsedBatchStats
			}
			if bodyCreateTime != "" {
				body["createTime"] = bodyCreateTime
			}
			if bodyDisplayName != "" {
				body["displayName"] = bodyDisplayName
			}
			if bodyEndTime != "" {
				body["endTime"] = bodyEndTime
			}
			if bodyInputConfig != "" {
				var parsedInputConfig any
				if err := json.Unmarshal([]byte(bodyInputConfig), &parsedInputConfig); err != nil {
					return fmt.Errorf("parsing --input-config JSON: %w", err)
				}
				body["inputConfig"] = parsedInputConfig
			}
			if bodyModel != "" {
				body["model"] = bodyModel
			}
			if bodyOutput != "" {
				var parsedOutput any
				if err := json.Unmarshal([]byte(bodyOutput), &parsedOutput); err != nil {
					return fmt.Errorf("parsing --output JSON: %w", err)
				}
				body["output"] = parsedOutput
			}
			if bodyPriority != 0 {
				body["priority"] = bodyPriority
			}
			if bodyState != "" {
				body["state"] = bodyState
			}
			if bodyUpdateTime != "" {
				body["updateTime"] = bodyUpdateTime
			}
			data, _, err := c.Patch(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagUpdateMask, "update-mask", "", "Optional. The list of fields to update.")
	cmd.Flags().StringVar(&bodyBatchStats, "batch-stats", "", "Stats about the batch.")
	cmd.Flags().StringVar(&bodyCreateTime, "create-time", "", "Output only. The time at which the batch was created.")
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "Required. The user-defined name of this batch.")
	cmd.Flags().StringVar(&bodyEndTime, "end-time", "", "Output only. The time at which the batch processing completed.")
	cmd.Flags().StringVar(&bodyInputConfig, "input-config", "", "Configures the input to the batch request.")
	cmd.Flags().StringVar(&bodyModel, "model", "", "Required. The name of the `Model` to use for generating the completion. Format: `models/{model}`.")
	cmd.Flags().StringVar(&bodyOutput, "output", "", "The output of a batch request. This is returned in the `AsyncBatchEmbedContentResponse` or the...")
	cmd.Flags().IntVar(&bodyPriority, "priority", 0, "Optional. The priority of the batch. Batches with a higher priority value will be processed before batches with a...")
	cmd.Flags().StringVar(&bodyState, "state", "", "Output only. The state of the batch.")
	cmd.Flags().StringVar(&bodyUpdateTime, "update-time", "", "Output only. The time at which the batch was last updated.")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
