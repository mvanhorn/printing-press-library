// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAgentsExportMetricResultsCmd(flags *rootFlags) *cobra.Command {
	var flagAgentId string
	var flagDeploymentId string
	var flagMetricId string
	var flagCallId string
	var flagStartDate string
	var flagEndDate string

	cmd := &cobra.Command{
		Use:         "export-metric-results",
		Short:       "Export metric results to a CSV file. This endpoint streams at most 100k results as the CSV file directly to the...",
		Example:     "  cartesia-pp-cli agents export-metric-results",
		Annotations: map[string]string{"pp:endpoint": "agents.export-metric-results", "pp:method": "GET", "pp:path": "/agents/metrics/results/export", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/agents/metrics/results/export"
			params := map[string]string{}
			if flagAgentId != "" {
				params["agent_id"] = fmt.Sprintf("%v", flagAgentId)
			}
			if flagDeploymentId != "" {
				params["deployment_id"] = fmt.Sprintf("%v", flagDeploymentId)
			}
			if flagMetricId != "" {
				params["metric_id"] = fmt.Sprintf("%v", flagMetricId)
			}
			if flagCallId != "" {
				params["call_id"] = fmt.Sprintf("%v", flagCallId)
			}
			if flagStartDate != "" {
				params["start_date"] = fmt.Sprintf("%v", flagStartDate)
			}
			if flagEndDate != "" {
				params["end_date"] = fmt.Sprintf("%v", flagEndDate)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "agents", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAgentId, "agent-id", "", "The ID of the agent.")
	cmd.Flags().StringVar(&flagDeploymentId, "deployment-id", "", "The ID of the deployment.")
	cmd.Flags().StringVar(&flagMetricId, "metric-id", "", "The ID of the metric.")
	cmd.Flags().StringVar(&flagCallId, "call-id", "", "The ID of the call.")
	cmd.Flags().StringVar(&flagStartDate, "start-date", "", "Filter metric results created at or after this ISO 8601 date/time (e.g. 2024-04-01T00:00:00Z).")
	cmd.Flags().StringVar(&flagEndDate, "end-date", "", "Filter metric results created before or at this ISO 8601 date/time (e.g. 2024-04-30T23:59:59Z).")

	return cmd
}
