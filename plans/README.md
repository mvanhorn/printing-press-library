# Implementation Plans

Generated by the improve skill on 2026-06-15. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add agent-friendly Figma node discovery commands | P1 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- Plan 001 has no dependencies. It is the first slice for making `figma-pp-cli` more Mario/agent friendly without changing authentication, write surfaces, or the generated Figma REST commands.

## Findings considered and rejected

- Add label-based wrappers for `images`, `frame extract`, and `dev-mode dump` in the first slice: deferred. The useful primitive is node discovery by label; once `agent outline` and `agent find-node` exist, agents can pass the returned ID to existing commands. Wrappers can be a follow-up after the matching contract is proven.
- Modify `registry.json`, `cli-skills/pp-figma/SKILL.md`, release metadata, or MCP manifests: rejected. Repo conventions mark these as generated or automation-owned for normal published-CLI behavior tweaks.
