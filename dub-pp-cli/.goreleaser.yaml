version: 2
project_name: dub-cli
changelog:
  disable: true
builds:
  - id: dub-cli
    main: ./cmd/dub-cli
    binary: dub-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/trevin-chow/dub-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: dub-mcp
    main: ./cmd/dub-mcp
    binary: dub-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: dub-cli
    repository:
      owner: trevin-chow
      name: homebrew-tap
    homepage: "https://github.com/trevin-chow/dub-cli"
    description: "Dub is the modern link attribution platform for short links, conversion tracking, and affiliate programs."
    install: |
      bin.install "dub-cli"
      bin.install "dub-mcp"
